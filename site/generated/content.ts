// Generated by scripts/generate-content.mjs. Do not edit manually.

export type Chapter = {
  number: number;
  slug: string;
  title: string;
  kind: "chapter" | "summary";
  partNumber: number;
  partName: string;
  markdown: string;
  headings: string[];
  quickRefresher: string;
  excerpt: string;
  readingMinutes: number;
  searchText: string;
};

export type RoadmapPart = {
  label: string;
  title: string;
  chapters: Array<{ number: number; title: string; slug: string | null }>;
};

export type QuestionRoadmapSection = {
  title: string;
  questions: Array<{ number: number; title: string }>;
};

export type QuestionAnswer = {
  number: number;
  question: string;
  answer: string;
  explanation: string;
  details: string;
};

export const chapters: Chapter[] = [
  {
    number: 1,
    slug: "01-javascript-runtime-model",
    title: "JavaScript Runtime Model",
    kind: "chapter",
    partNumber: 1,
    partName: "Runtime Foundations",
    markdown:
      '# Chapter 1 — JavaScript Runtime Model\n\n## Learning objectives\n\nAfter completing this chapter, you should be able to:\n\n- distinguish ECMAScript, engine, host, and framework responsibilities;\n- explain what “JavaScript is single-threaded” does and does not mean;\n- trace browser code through synchronous execution, microtasks, tasks, and rendering opportunities;\n- explain why promises and `async` functions do not make CPU-heavy work parallel;\n- connect main-thread availability to browser and React responsiveness.\n\n## Quick Refresher\n\n- A JavaScript runtime combines ECMAScript, an engine, and a host.\n- ECMAScript defines semantics; it does not define one universal event loop.\n- Browsers and Node.js implement the same language but expose different globals and scheduling.\n- React and other frameworks add scheduling policy on top of the host, not a new event loop.\n- In a browser agent, one callback runs to completion before another begins.\n- Promise reactions are microtasks; timers run in later tasks.\n- DOM updates do not guarantee paint; the browser waits for a rendering opportunity.\n- `Promise`, `async`, and React transitions do not make CPU-heavy work parallel.\n\n## Why This Matters\n\nMany weak interview answers treat JavaScript, V8, browser APIs, the event loop, and React as one machine. That model cannot explain why `document` exists only in a browser, why promise reactions are delayed, or why a React DOM update may appear before paint.\n\nA senior engineer locates responsibility fast: engine JavaScript, host scheduling, rendering, or framework work.\n\nA useful interview habit is to ask: **which layer owns this behavior, in which environment?**\n\n## Core Mental Model\n\nTreat a frontend application as four cooperating layers:\n\n| Layer          | Responsibility                                  | Examples                                             |\n| -------------- | ----------------------------------------------- | ---------------------------------------------------- |\n| **ECMAScript** | Defines the language\'s observable semantics     | Functions, promises, execution contexts, jobs        |\n| **Engine**     | Implements and optimizes ECMAScript             | V8, SpiderMonkey, JavaScriptCore, garbage collection |\n| **Host**       | Supplies APIs, scheduling, and rendering        | DOM, timers, networking, browser event loops         |\n| **Framework**  | Adds application-level scheduling and UI policy | React rendering, batching, transitions               |\n\nThe boundaries describe who owns a contract, not physically isolated software. Browsers and engines integrate closely, and React uses browser facilities. Still, the distinction prevents claims such as “JavaScript\'s event loop paints the page.”\n\nAsk: **which layer owns this behavior, in which environment?**\n\n## Formal Model\n\n### ECMAScript is hosted\n\nECMA-262 defines the language and provides hooks where a host must participate. This is why browser and Node.js runtimes can implement the same language while exposing different globals and scheduling behavior.\n\n- `Promise`, `Array`, `Map`, and `globalThis` are defined by ECMAScript.\n- `document`, browser timers, and browser rendering are host facilities.\n- `process`, `setImmediate`, and filesystem APIs are Node.js facilities.\n\nExample: `Promise.resolve().then(...)` is ECMAScript promise reaction work. `document.querySelector(...)` is a browser host operation provided by the DOM.\n\nAn API appearing in several hosts does not make it part of ECMAScript. For example, browsers and modern Node.js versions both expose `fetch`, but each host supplies it.\n\n### Agent and run-to-completion\n\nAn ECMAScript **agent** is a specification-level unit that executes ECMAScript jobs. Only one job is actively evaluated in an agent at a time, and a started job runs to completion before another begins in that agent.\n\nThis gives a precise version of “JavaScript is single-threaded”:\n\n> In a typical browser window agent, ordinary JavaScript callbacks do not execute concurrently with one another. The browser can still use other threads, and workers can execute JavaScript in separate agents.\n\nAn agent is an abstract spec concept, not a promise of a one-to-one OS thread.\n\n### Job, task, microtask, and rendering\n\nThese terms belong to different layers:\n\n| Term                      | Meaning in this chapter                                                                             |\n| ------------------------- | --------------------------------------------------------------------------------------------------- |\n| **Job**                   | An ECMAScript unit of work; promise reactions run as jobs                                           |\n| **Task**                  | Browser-scheduled work such as running a script, dispatching an event, or invoking a timer callback |\n| **Microtask**             | Browser scheduling used for promise reactions and `queueMicrotask` callbacks                        |\n| **Rendering opportunity** | A point when the browser may update style, layout, and pixels                                       |\n\n**What about macrotasks?** “Macrotask” is a widely used informal name for a browser task, usually used to contrast tasks with microtasks. The HTML Standard uses **task**, not macrotask. In an interview, it is fine to say “macrotask” if you clarify that you mean an HTML task, such as event dispatch or a timer callback.\n\nWhen a promise reaction becomes runnable, ECMAScript asks the host to enqueue its job. A browser queues it as a microtask, drains microtasks after each task, then selects the next task.\n\nRendering is separate. A DOM mutation updates browser state, but paint waits for a rendering opportunity. Browsers may also skip paint after a microtask checkpoint.\n\nTwo additional terms appear throughout the handbook:\n\n- An **execution context** is specification state used while evaluating code. It is related to, but not identical to, a physical engine stack frame.\n- A **realm** provides a global object, a global environment, and its own intrinsic objects. This is why values from another window can pass `Array.isArray` while failing `instanceof Array` in the current realm.\n\nChapters 2–4 develop execution contexts, stacks, realms, and environments in detail.\n\n## Visual Model\n\n![JavaScript runtime responsibilities and browser scheduling flow](/javascript-runtime-model.svg)\n\nThe upper path explains responsibility: ECMAScript defines the language rules, an engine executes them, and the browser host supplies APIs, scheduling, and rendering. The lower path explains browser order: select a task, run its JavaScript to completion, drain microtasks, possibly render, and continue with another task.\n\nThis is a reasoning model, not a complete browser architecture. The browser may perform networking, rasterization, garbage collection, and other work on additional threads.\n\n## Step-by-Step Runtime Walkthrough\n\nRun this in a browser page containing `\u003cbutton id="save">Save\u003c/button>`:\n\n```js\nconst button = document.querySelector("#save");\n\nbutton.addEventListener("click", () => {\n  console.log("handler: start");\n  button.textContent = "Saving…";\n\n  Promise.resolve().then(() => {\n    console.log("promise");\n    button.dataset.status = "pending";\n  });\n\n  setTimeout(() => {\n    console.log("timer");\n    button.textContent = "Saved";\n  }, 0);\n\n  console.log("handler: end");\n});\n```\n\nFor one click, the console order is:\n\n```text\nhandler: start\nhandler: end\npromise\ntimer\n```\n\nTrace it by ownership and scheduling:\n\n1. **The browser dispatches the click as a task.** It invokes the registered JavaScript callback.\n2. **The callback runs synchronously.** It logs `handler: start` and changes DOM state to `Saving…`. The browser has not necessarily painted that state.\n3. **`.then` schedules later work.** The promise is already fulfilled, but its reaction is not called inline. ECMAScript creates a promise reaction job, which the browser queues as a microtask.\n4. **`setTimeout` schedules a later task.** A delay of `0` is a minimum threshold, not an instruction to run immediately.\n5. **The handler finishes.** It logs `handler: end` and returns. No other callback interrupted it.\n6. **The browser drains microtasks.** The promise reaction logs `promise` and changes `data-status`.\n7. **The browser may render.** If a rendering update occurs now, the user can see `Saving…`.\n8. **A later task runs the timer callback.** It logs `timer` and changes the text to `Saved`.\n\nThe console order is constrained, but the intermediate paint is not. A browser may present only `Saved` if no visible frame occurs between the two DOM changes.\n\n### Why “make it async” does not prevent blocking\n\nNow place expensive synchronous work in the handler:\n\n```js\nbutton.addEventListener("click", () => {\n  button.textContent = "Working…";\n\n  const startedAt = performance.now();\n  while (performance.now() - startedAt \u003c 2_000) {\n    // CPU-heavy application work\n  }\n\n  button.textContent = "Complete";\n});\n```\n\nThe user may never see `Working…`: both writes occur in one task, and the main thread cannot paint while the loop is running. Moving the loop into `.then` or an `async` function still executes it on the same agent. Depending on the workload, the real options are to reduce the computation, split it into bounded chunks that yield, move it to a Web Worker, or perform it elsewhere.\n\n### Browser claims do not automatically apply to Node.js\n\nNode.js adds scheduling facilities such as `process.nextTick` and event-loop phases implemented with libuv. In practice, `process.nextTick` is often ordered before other promise jobs and `queueMicrotask`, and the exact semantics can vary with Node.js and libuv versions. A good interview answer therefore says “in a browser” or “in Node.js” before making scheduling guarantees.\n\n## Common Misconceptions\n\n| Claim                                               | Better explanation                                                                                                |\n| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |\n| “The event loop is part of JavaScript.”             | ECMAScript defines jobs and host hooks; browsers and Node.js provide different event-loop integrations.           |\n| “Web APIs run inside the engine.”                   | The host owns DOM, timers, networking, and rendering; the engine executes associated JavaScript callbacks.        |\n| “A fulfilled promise calls `.then` immediately.”    | Fulfillment is promise state. The reaction still runs later as a job, integrated as a microtask in browsers.      |\n| “A zero-delay timer runs immediately.”              | Its callback can run only in a later task after the current work and eligible microtasks.                         |\n| “Promises move work to another thread.”             | Promises represent eventual results; their executors and reactions still run as JavaScript on the relevant agent. |\n| “React transitions move work off-thread.”            | Transitions adjust React update priority and scheduling eligibility, but the underlying JavaScript still runs on the same main-thread agent. |\n| “React concurrent rendering is parallel rendering.” | React cooperatively schedules eligible work; it cannot preempt arbitrary synchronous JavaScript.                  |\n\n## React Connection\n\nReact runs inside the same host constraints:\n\n- **Render is JavaScript work.** Expensive component evaluation occupies the main thread.\n- **Commit is not paint.** React may mutate the DOM during commit; the browser decides when pixels appear.\n- **Scheduling requires a yield.** React cannot interrupt a long event handler or CPU loop that does not return control.\n- **Transitions shift priority, not thread ownership.** `startTransition` and `useDeferredValue` make updates less urgent, but the work still executes on the same main-thread agent.\n- **Interview cue:** call out whether the issue is framework scheduling or browser rendering, not just “React is slow.”\n\n```jsx\nfunction SearchButton() {\n  const [status, setStatus] = React.useState("Idle");\n\n  function handleClick() {\n    setStatus("Working");\n\n    const startedAt = performance.now();\n    while (performance.now() - startedAt \u003c 2_000) {\n      // Blocks input, paint, and React scheduling.\n    }\n\n    setStatus("Done");\n  }\n\n  return \u003cbutton onClick={handleClick}>{status}\u003c/button>;\n}\n```\n\nReact may batch updates, but the deeper problem is that the handler monopolizes the main thread. A transition changes update priority; it does not move the loop to another thread.\n\n## Performance and Memory Implications\n\nUse the runtime layers to choose evidence rather than guessing from elapsed time:\n\n| Symptom                        | Inspect                            | Useful evidence                                  |\n| ------------------------------ | ---------------------------------- | ------------------------------------------------ |\n| Long callback or frozen input  | Application/framework JavaScript   | Browser Performance trace and CPU profile        |\n| Slow network completion        | Host and network                   | Request waterfall and server timing              |\n| DOM updated but pixels delayed | Main thread and rendering pipeline | Style, layout, paint, and screenshots in a trace |\n| Repeated component work        | React                              | React Profiler correlated with a browser trace   |\n| Increasing retained memory     | Actual reference graph             | Heap snapshots and retaining paths               |\n\nA function awaiting a request may span seconds while consuming little CPU; a 100 ms synchronous callback may visibly delay input. Microtasks can also harm responsiveness if they continually enqueue more microtasks and prevent the browser from reaching tasks or rendering.\n\n## Debugging Techniques\n\nFor a slow browser interaction:\n\n1. Record one representative interaction in the browser Performance panel.\n2. Locate the input task and inspect its call tree.\n3. Separate JavaScript, React, style, layout, paint, and garbage-collection work.\n4. Compare DOM changes with screenshot frames when visible timing matters.\n5. Use the Bottom-up view to find aggregate CPU cost.\n6. Confirm findings in a representative production build; development diagnostics can add work.\n\nA “long task” marker identifies blocked main-thread time, not its cause. Async stack traces can connect a callback to where it was scheduled, but they are diagnostic metadata—not one continuously retained synchronous call stack.\n\nFor Node.js, use Node\'s inspector and profiles, and verify the deployed Node.js version before relying on detailed phase ordering.\n\n## Interview Questions\n\n### Level 1 — Fundamentals\n\n**Question:** What is a JavaScript runtime?\n\n**Model answer:**\n\nA JavaScript runtime is the complete environment that executes JavaScript. ECMAScript defines language semantics, an engine implements them, and a host supplies APIs and scheduling. A browser provides the DOM, rendering, and browser event loops; Node.js provides a different host environment. Frameworks such as React run within those constraints.\n\n### Level 2 — Applied understanding\n\n**Question:** Why does a fulfilled promise\'s `.then` handler run later while the promise executor runs synchronously?\n\n**Model answer:**\n\nThe constructor calls the executor while creating the promise. A `.then` callback is a reaction: ECMAScript schedules it as a promise job even when the promise is already fulfilled. In a browser, the host integrates that job as a microtask, so it runs only after current synchronous execution finishes.\n\n### Level 3 — Senior reasoning\n\n**Question:** A click handler shows a spinner, performs 800 ms of synchronous work, and hides the spinner. Why may the spinner never appear?\n\n**Model answer:**\n\nBoth DOM changes and the computation occur in one task. The DOM state changes, but the browser cannot normally paint while the handler occupies the main thread. By the next rendering opportunity, the spinner is already hidden. I would profile first, then reduce the work, deliberately yield between bounded chunks, or move CPU-heavy computation to a worker. A promise or transition alone would not solve the blocking.\n\n### Level 4 — Deep follow-up\n\n**Question:** Does ECMAScript guarantee that promise callbacks run before `setTimeout(..., 0)` callbacks?\n\n**Model answer:**\n\nNot as a universal cross-host statement. ECMAScript defines promise jobs, but `setTimeout` is a host API. In browsers, promise reactions are microtasks and timer callbacks are tasks, so the relevant microtask checkpoint precedes a later timer task. Other hosts have their own scheduling integrations, so the environment must be named.\n\n## Exercises\n\n### 1. Predict browser output\n\n```js\nconsole.log("A");\n\nsetTimeout(() => console.log("B"), 0);\n\nPromise.resolve()\n  .then(() => {\n    console.log("C");\n    queueMicrotask(() => console.log("D"));\n  })\n  .then(() => console.log("E"));\n\nconsole.log("F");\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe output is `A`, `F`, `C`, `D`, `E`, `B`. The initial task logs `A` and `F`. The first reaction logs `C`, queues `D`, and then causes the next promise reaction to be queued. The microtask queue is FIFO here, so `D` precedes `E`. The timer runs in a later task.\n\n\u003c/details>\n\n### 2. Repair the explanation\n\nIdentify the problems:\n\n> JavaScript\'s event loop sends Web APIs to the microtask queue, runs microtasks in parallel when the stack is empty, paints, and then executes timers at their requested delays.\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\n- There is no universal JavaScript event loop; scheduling is host-specific.\n- Web APIs do not all produce microtasks.\n- Microtasks execute one at a time in the relevant agent, not in parallel.\n- A browser is not required to paint after every microtask checkpoint.\n- A timer delay is a minimum threshold, not an exact execution time.\n\n\u003c/details>\n\n### 3. Diagnose a React interaction\n\nA React search screen calls `startTransition`, synchronously sorts 500,000 records, and then calls the transition\'s state setter. Typing still freezes. Explain why and propose a correction.\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\n`startTransition` marks eligible state updates as non-urgent; it does not move arbitrary computation to another thread. The sort monopolizes the main thread before the state update. Profile it, then avoid repeated sorting, process bounded chunks with deliberate yielding, or move the work to a Web Worker. A transition may still help prioritize the render after the result is available.\n\n\u003c/details>\n\n## Chapter Summary\n\n- **Responsibility:** ECMAScript defines semantics, an engine implements them, a host supplies APIs and scheduling, and React adds framework policy.\n- **Scheduling:** in a browser, current JavaScript completes before microtasks run; timer callbacks run in later tasks.\n- **Rendering:** DOM mutation and visible paint are different events.\n- **Concurrency:** promises and React scheduling do not make CPU-heavy JavaScript parallel.\n- **Interview habit:** qualify runtime claims by layer and host.\n\n### Interview-ready explanation\n\nA JavaScript runtime combines ECMAScript, an engine, and a host. ECMAScript defines execution semantics and promise jobs; an engine implements them; a browser or Node.js supplies APIs and scheduling. In a browser, callbacks in one agent run to completion, promise reactions are integrated as microtasks, and rendering can happen only after JavaScript yields and the browser reaches a rendering opportunity. React works within those constraints: it can prioritize eligible render work, but it cannot preempt a blocking callback or move arbitrary computation to another thread.\n\n## Further Reading\n\n- [ECMA-262: Overview](https://tc39.es/ecma262/#sec-overview)\n- [ECMA-262: Jobs and Host Operations to Enqueue Jobs](https://tc39.es/ecma262/#sec-jobs-and-host-operations-to-enqueue-jobs)\n- [ECMA-262: Agents](https://tc39.es/ecma262/#sec-agents)\n- [WHATWG HTML: Event loops](https://html.spec.whatwg.org/multipage/webappapis.html#event-loops)\n- [Node.js: The Node.js Event Loop](https://nodejs.org/en/learn/asynchronous-work/event-loop-timers-and-nexttick)\n- [React: Render and Commit](https://react.dev/learn/render-and-commit)\n- [React: `useTransition`](https://react.dev/reference/react/useTransition)',
    headings: [
      "Learning objectives",
      "Quick Refresher",
      "Why This Matters",
      "Core Mental Model",
      "Formal Model",
      "Visual Model",
      "Step-by-Step Runtime Walkthrough",
      "Common Misconceptions",
      "React Connection",
      "Performance and Memory Implications",
      "Debugging Techniques",
      "Interview Questions",
      "Exercises",
      "Chapter Summary",
      "Further Reading",
    ],
    quickRefresher:
      "A JavaScript runtime combines ECMAScript, an engine, and a host. ECMAScript defines semantics; it does not define one universal event loop. Browsers and Node.js implement the same language but expose different globals and scheduling. React and other frameworks add scheduling policy on top of the host, not a new event loop. In a browser agent, one callback runs to completion before another begins. Promise reactions are microtasks; timers run in later tasks. DOM updates do not guarantee paint; the browser waits for a rendering opportunity. Promise , async , and React transitions do not make CPU heavy work parallel.",
    excerpt:
      "Many weak interview answers treat JavaScript, V8, browser APIs, the event loop, and React as one machine. That model cannot explain why document exists only in a browser, why promise reactions are delayed, or why a React DOM update may appear before paint.",
    readingMinutes: 12,
    searchText:
      "chapter 1 — javascript runtime model learning objectives after completing this chapter, you should be able to: distinguish ecmascript, engine, host, and framework responsibilities; explain what “javascript is single threaded” does and does not mean; trace browser code through synchronous execution, microtasks, tasks, and rendering opportunities; explain why promises and async functions do not make cpu heavy work parallel; connect main thread availability to browser and react responsiveness. quick refresher a javascript runtime combines ecmascript, an engine, and a host. ecmascript defines semantics; it does not define one universal event loop. browsers and node.js implement the same language but expose different globals and scheduling. react and other frameworks add scheduling policy on top of the host, not a new event loop. in a browser agent, one callback runs to completion before another begins. promise reactions are microtasks; timers run in later tasks. dom updates do not guarantee paint; the browser waits for a rendering opportunity. promise , async , and react transitions do not make cpu heavy work parallel. why this matters many weak interview answers treat javascript, v8, browser apis, the event loop, and react as one machine. that model cannot explain why document exists only in a browser, why promise reactions are delayed, or why a react dom update may appear before paint. a senior engineer locates responsibility fast: engine javascript, host scheduling, rendering, or framework work. a useful interview habit is to ask: which layer owns this behavior, in which environment? core mental model treat a frontend application as four cooperating layers: layer responsibility examples ecmascript defines the language's observable semantics functions, promises, execution contexts, jobs engine implements and optimizes ecmascript v8, spidermonkey, javascriptcore, garbage collection host supplies apis, scheduling, and rendering dom, timers, networking, browser event loops framework adds application level scheduling and ui policy react rendering, batching, transitions the boundaries describe who owns a contract, not physically isolated software. browsers and engines integrate closely, and react uses browser facilities. still, the distinction prevents claims such as “javascript's event loop paints the page.” ask: which layer owns this behavior, in which environment? formal model ecmascript is hosted ecma 262 defines the language and provides hooks where a host must participate. this is why browser and node.js runtimes can implement the same language while exposing different globals and scheduling behavior. promise , array , map , and globalthis are defined by ecmascript. document , browser timers, and browser rendering are host facilities. process , setimmediate , and filesystem apis are node.js facilities. example: promise.resolve().then(...) is ecmascript promise reaction work. document.queryselector(...) is a browser host operation provided by the dom. an api appearing in several hosts does not make it part of ecmascript. for example, browsers and modern node.js versions both expose fetch , but each host supplies it. agent and run to completion an ecmascript agent is a specification level unit that executes ecmascript jobs. only one job is actively evaluated in an agent at a time, and a started job runs to completion before another begins in that agent. this gives a precise version of “javascript is single threaded”: in a typical browser window agent, ordinary javascript callbacks do not execute concurrently with one another. the browser can still use other threads, and workers can execute javascript in separate agents. an agent is an abstract spec concept, not a promise of a one to one os thread. job, task, microtask, and rendering these terms belong to different layers: term meaning in this chapter job an ecmascript unit of work; promise reactions run as jobs task browser scheduled work such as running a script, dispatching an event, or invoking a timer callback microtask browser scheduling used for promise reactions and queuemicrotask callbacks rendering opportunity a point when the browser may update style, layout, and pixels what about macrotasks? “macrotask” is a widely used informal name for a browser task, usually used to contrast tasks with microtasks. the html standard uses task , not macrotask. in an interview, it is fine to say “macrotask” if you clarify that you mean an html task, such as event dispatch or a timer callback. when a promise reaction becomes runnable, ecmascript asks the host to enqueue its job. a browser queues it as a microtask, drains microtasks after each task, then selects the next task. rendering is separate. a dom mutation updates browser state, but paint waits for a rendering opportunity. browsers may also skip paint after a microtask checkpoint. two additional terms appear throughout the handbook: an execution context is specification state used while evaluating code. it is related to, but not identical to, a physical engine stack frame. a realm provides a global object, a global environment, and its own intrinsic objects. this is why values from another window can pass array.isarray while failing instanceof array in the current realm. chapters 2–4 develop execution contexts, stacks, realms, and environments in detail. visual model the upper path explains responsibility: ecmascript defines the language rules, an engine executes them, and the browser host supplies apis, scheduling, and rendering. the lower path explains browser order: select a task, run its javascript to completion, drain microtasks, possibly render, and continue with another task. this is a reasoning model, not a complete browser architecture. the browser may perform networking, rasterization, garbage collection, and other work on additional threads. step by step runtime walkthrough run this in a browser page containing save : for one click, the console order is: trace it by ownership and scheduling: 1. the browser dispatches the click as a task. it invokes the registered javascript callback. 2. the callback runs synchronously. it logs handler: start and changes dom state to saving… . the browser has not necessarily painted that state. 3. .then schedules later work. the promise is already fulfilled, but its reaction is not called inline. ecmascript creates a promise reaction job, which the browser queues as a microtask. 4. settimeout schedules a later task. a delay of 0 is a minimum threshold, not an instruction to run immediately. 5. the handler finishes. it logs handler: end and returns. no other callback interrupted it. 6. the browser drains microtasks. the promise reaction logs promise and changes data status . 7. the browser may render. if a rendering update occurs now, the user can see saving… . 8. a later task runs the timer callback. it logs timer and changes the text to saved . the console order is constrained, but the intermediate paint is not. a browser may present only saved if no visible frame occurs between the two dom changes. why “make it async” does not prevent blocking now place expensive synchronous work in the handler: the user may never see working… : both writes occur in one task, and the main thread cannot paint while the loop is running. moving the loop into .then or an async function still executes it on the same agent. depending on the workload, the real options are to reduce the computation, split it into bounded chunks that yield, move it to a web worker, or perform it elsewhere. browser claims do not automatically apply to node.js node.js adds scheduling facilities such as process.nexttick and event loop phases implemented with libuv. in practice, process.nexttick is often ordered before other promise jobs and queuemicrotask , and the exact semantics can vary with node.js and libuv versions. a good interview answer therefore says “in a browser” or “in node.js” before making scheduling guarantees. common misconceptions claim better explanation “the event loop is part of javascript.” ecmascript defines jobs and host hooks; browsers and node.js provide different event loop integrations. “web apis run inside the engine.” the host owns dom, timers, networking, and rendering; the engine executes associated javascript callbacks. “a fulfilled promise calls .then immediately.” fulfillment is promise state. the reaction still runs later as a job, integrated as a microtask in browsers. “a zero delay timer runs immediately.” its callback can run only in a later task after the current work and eligible microtasks. “promises move work to another thread.” promises represent eventual results; their executors and reactions still run as javascript on the relevant agent. “react transitions move work off thread.” transitions adjust react update priority and scheduling eligibility, but the underlying javascript still runs on the same main thread agent. “react concurrent rendering is parallel rendering.” react cooperatively schedules eligible work; it cannot preempt arbitrary synchronous javascript. react connection react runs inside the same host constraints: render is javascript work. expensive component evaluation occupies the main thread. commit is not paint. react may mutate the dom during commit; the browser decides when pixels appear. scheduling requires a yield. react cannot interrupt a long event handler or cpu loop that does not return control. transitions shift priority, not thread ownership. starttransition and usedeferredvalue make updates less urgent, but the work still executes on the same main thread agent. interview cue: call out whether the issue is framework scheduling or browser rendering, not just “react is slow.” react may batch updates, but the deeper problem is that the handler monopolizes the main thread. a transition changes update priority; it does not move the loop to another thread. performance and memory implications use the runtime layers to choose evidence rather than guessing from elapsed time: symptom inspect useful evidence long callback or frozen input application/framework javascript browser performance trace and cpu profile slow network completion host and network request waterfall and server timing dom updated but pixels delayed main thread and rendering pipeline style, layout, paint, and screenshots in a trace repeated component work react react profiler correlated with a browser trace increasing retained memory actual reference graph heap snapshots and retaining paths a function awaiting a request may span seconds while consuming little cpu; a 100 ms synchronous callback may visibly delay input. microtasks can also harm responsiveness if they continually enqueue more microtasks and prevent the browser from reaching tasks or rendering. debugging techniques for a slow browser interaction: 1. record one representative interaction in the browser performance panel. 2. locate the input task and inspect its call tree. 3. separate javascript, react, style, layout, paint, and garbage collection work. 4. compare dom changes with screenshot frames when visible timing matters. 5. use the bottom up view to find aggregate cpu cost. 6. confirm findings in a representative production build; development diagnostics can add work. a “long task” marker identifies blocked main thread time, not its cause. async stack traces can connect a callback to where it was scheduled, but they are diagnostic metadata—not one continuously retained synchronous call stack. for node.js, use node's inspector and profiles, and verify the deployed node.js version before relying on detailed phase ordering. interview questions level 1 — fundamentals question: what is a javascript runtime? model answer: a javascript runtime is the complete environment that executes javascript. ecmascript defines language semantics, an engine implements them, and a host supplies apis and scheduling. a browser provides the dom, rendering, and browser event loops; node.js provides a different host environment. frameworks such as react run within those constraints. level 2 — applied understanding question: why does a fulfilled promise's .then handler run later while the promise executor runs synchronously? model answer: the constructor calls the executor while creating the promise. a .then callback is a reaction: ecmascript schedules it as a promise job even when the promise is already fulfilled. in a browser, the host integrates that job as a microtask, so it runs only after current synchronous execution finishes. level 3 — senior reasoning question: a click handler shows a spinner, performs 800 ms of synchronous work, and hides the spinner. why may the spinner never appear? model answer: both dom changes and the computation occur in one task. the dom state changes, but the browser cannot normally paint while the handler occupies the main thread. by the next rendering opportunity, the spinner is already hidden. i would profile first, then reduce the work, deliberately yield between bounded chunks, or move cpu heavy computation to a worker. a promise or transition alone would not solve the blocking. level 4 — deep follow up question: does ecmascript guarantee that promise callbacks run before settimeout(..., 0) callbacks? model answer: not as a universal cross host statement. ecmascript defines promise jobs, but settimeout is a host api. in browsers, promise reactions are microtasks and timer callbacks are tasks, so the relevant microtask checkpoint precedes a later timer task. other hosts have their own scheduling integrations, so the environment must be named. exercises 1. predict browser output solution the output is a , f , c , d , e , b . the initial task logs a and f . the first reaction logs c , queues d , and then causes the next promise reaction to be queued. the microtask queue is fifo here, so d precedes e . the timer runs in a later task. 2. repair the explanation identify the problems: javascript's event loop sends web apis to the microtask queue, runs microtasks in parallel when the stack is empty, paints, and then executes timers at their requested delays. solution there is no universal javascript event loop; scheduling is host specific. web apis do not all produce microtasks. microtasks execute one at a time in the relevant agent, not in parallel. a browser is not required to paint after every microtask checkpoint. a timer delay is a minimum threshold, not an exact execution time. 3. diagnose a react interaction a react search screen calls starttransition , synchronously sorts 500,000 records, and then calls the transition's state setter. typing still freezes. explain why and propose a correction. solution starttransition marks eligible state updates as non urgent; it does not move arbitrary computation to another thread. the sort monopolizes the main thread before the state update. profile it, then avoid repeated sorting, process bounded chunks with deliberate yielding, or move the work to a web worker. a transition may still help prioritize the render after the result is available. chapter summary responsibility: ecmascript defines semantics, an engine implements them, a host supplies apis and scheduling, and react adds framework policy. scheduling: in a browser, current javascript completes before microtasks run; timer callbacks run in later tasks. rendering: dom mutation and visible paint are different events. concurrency: promises and react scheduling do not make cpu heavy javascript parallel. interview habit: qualify runtime claims by layer and host. interview ready explanation a javascript runtime combines ecmascript, an engine, and a host. ecmascript defines execution semantics and promise jobs; an engine implements them; a browser or node.js supplies apis and scheduling. in a browser, callbacks in one agent run to completion, promise reactions are integrated as microtasks, and rendering can happen only after javascript yields and the browser reaches a rendering opportunity. react works within those constraints: it can prioritize eligible render work, but it cannot preempt a blocking callback or move arbitrary computation to another thread. further reading ecma 262: overview ecma 262: jobs and host operations to enqueue jobs ecma 262: agents whatwg html: event loops node.js: the node.js event loop react: render and commit react: usetransition",
  },
  {
    number: 2,
    slug: "02-execution-contexts",
    title: "Execution Contexts",
    kind: "chapter",
    partNumber: 1,
    partName: "Runtime Foundations",
    markdown:
      "# Chapter 2 — Execution Contexts\n\n## Learning objectives\n\nAfter completing this chapter, you should be able to:\n\n- define an execution context without treating it as a JavaScript object;\n- trace how calls and returns change the running execution context;\n- distinguish execution contexts, lexical environments, and engine stack frames;\n- explain declaration instantiation without relying on a universal “creation phase”;\n- explain how async functions suspend without blocking a synchronous stack frame;\n- connect fresh function invocations to React render behavior.\n\n## Quick Refresher\n\n- An execution context is ECMAScript's internal model for an active or suspended evaluation.\n- The running execution context is normally the top entry of an agent's context stack.\n- A function call normally suspends the caller and pushes a new function context.\n- Returning removes the callee context and resumes the caller.\n- Every invocation has distinct runtime state and local bindings.\n- An execution context, a lexical environment, and an engine stack frame are related but different models.\n- Every React component render is a fresh function invocation with fresh local bindings.\n\n## Why This Matters\n\nExecution contexts explain how control moves between scripts, functions, generators, and async functions. Interviewers rarely want a list of specification fields. They want to know whether you can identify which invocation is running, which invocation owns a local value, and what happens when evaluation returns or suspends.\n\nA poor model leads to practical mistakes: assuming a closure retains an entire call stack, believing `await` leaves a normal stack frame blocked, or expecting a React component's local variables to persist across renders.\n\n## Core Mental Model\n\nAn execution context is ECMAScript's **control record for one active or suspended evaluation**. It tracks enough state to answer:\n\n- Which code is being evaluated?\n- Which function, script, module, and realm does it belong to?\n- Where should evaluation continue after a call returns or suspended work resumes?\n\nAt most one context per agent is running at a time. It is normally the top entry of the execution-context stack.\n\nKeep three models separate:\n\n| Concept                 | What it explains                         | What it is not                                    |\n| ----------------------- | ---------------------------------------- | ------------------------------------------------- |\n| **Execution context**   | Evaluation state and transfer of control | A JavaScript object                               |\n| **Lexical environment** | Bindings and identifier resolution       | The dynamic caller chain                          |\n| **Engine stack frame**  | Implementation storage for a call        | A required one-to-one representation of a context |\n\nCalling a function changes the context stack. It does not change the called function's lexical parent.\n\n## Formal Model\n\n### Running context and context stack\n\nEach ECMAScript agent has an execution-context stack. Its top entry is the **running execution context**. Calling an ordinary function normally creates a function context, suspends the caller, pushes the callee, and makes it current. When the callee completes, its context is removed and the caller resumes.\n\nThis is specification machinery. An engine may inline a call, eliminate storage, or reconstruct frames for debugging while preserving the required behavior.\n\n### What a context contains\n\nEvery context carries evaluation state and associations such as:\n\n- the function being evaluated, or `null` for script or module code;\n- the relevant realm;\n- the originating script or module, when one exists;\n- enough state to perform, suspend, and resume evaluation.\n\nEvaluation algorithms also use lexical, variable, and private environment components associated with the running context. Those environments contain bindings; the context itself should not be imagined as a user-visible object containing all local variables.\n\n### Calling an ordinary function\n\nAn ordinary call can be summarized as:\n\n1. Evaluate the callee and arguments in the caller.\n2. Create a function context for the invocation.\n3. Suspend the caller and make the callee context current.\n4. Establish parameters, `this`, and the function's environments.\n5. Instantiate declarations required by the body.\n6. Evaluate the body and produce a completion.\n7. Remove the callee context and resume the caller.\n\nA `throw` produces an abrupt completion rather than a normal return. The context still unwinds through defined completion rules.\n\n### Declaration instantiation, not source movement\n\n“Creation phase” is common teaching shorthand, but ECMAScript defines specific declaration-instantiation algorithms for functions, scripts, modules, and blocks. For a function call, parameters and declarations are prepared before body statements execute, but their initialization differs:\n\n| Declaration             | Initial state before its declaration statement |\n| ----------------------- | ---------------------------------------------- |\n| Function declaration    | Generally initialized with its function object |\n| `var`                   | Initialized to `undefined`                     |\n| `let`, `const`, `class` | Binding exists but remains uninitialized       |\n\nNothing is physically moved to the top of the source file. “Hoisting” describes observable initialization behavior; it is not a source-code transformation.\n\n### Suspension is not an ordinary blocked call\n\nGenerators and async functions can suspend and later resume evaluation. The specification preserves the necessary context state, but an engine does not need to retain one normal native stack frame throughout the pause.\n\nThis distinction becomes visible with `await`: the async function starts synchronously, suspends, returns a promise to its caller, and resumes later through promise-job scheduling.\n\n## Visual Model\n\nAn ordinary function call follows one compact control cycle:\n\n![Execution context call, suspension, and return cycle](/execution-context-cycle.svg)\n\nRead it as a change in which context owns control. A call suspends the caller and makes a new callee context the running context. Completion removes the callee context and continues the caller immediately after the call.\n\nThis is a specification model, not a promise about physical engine frames. It also does not describe lexical scope: identifier lookup follows lexical environments, not the chain of callers.\n\n## Step-by-Step Runtime Walkthrough\n\nThe preceding trace demonstrates four rules:\n\n1. **One invocation, one context.** The call to `calculateTax` creates invocation-specific state, including its `amount` parameter.\n2. **The top context runs.** While `multiply` is current, the script and `calculateTax` contexts are suspended.\n3. **Returns unwind in reverse call order.** `multiply` completes before `calculateTax` can continue and return.\n4. **Scope is not the call stack.** `taxRate` is found through lexical environments, not by searching callers.\n\nNow compare an async function:\n\n```js\nasync function loadProfile() {\n  console.log(\"load: start\");\n  await null;\n  console.log(\"load: resumed\");\n}\n\nconsole.log(\"script: start\");\nloadProfile();\nconsole.log(\"script: end\");\n```\n\nThe output is:\n\n```text\nscript: start\nload: start\nscript: end\nload: resumed\n```\n\n`loadProfile` begins synchronously. At `await`, its evaluation suspends and the call returns a promise. The script continues without a blocked synchronous frame. Promise-job machinery later resumes the async evaluation. DevTools may display an async stack linking both sides, but that is diagnostic causality—not one uninterrupted physical call stack.\n\n## Common Misconceptions\n\n| Claim                                            | Better explanation                                                                                 |\n| ------------------------------------------------ | -------------------------------------------------------------------------------------------------- |\n| “A context is an object containing locals.”      | It is inaccessible specification state; bindings are modeled by environment records.               |\n| “Execution context and scope are the same.”      | Context tracks evaluation; lexical environments determine identifier resolution.                   |\n| “The context stack is exactly the engine stack.” | The engine may inline, optimize, or reconstruct frames while preserving semantics.                 |\n| “Hoisting moves declarations.”                   | Declaration-instantiation algorithms create and initialize bindings according to declaration type. |\n| “Returning deletes every local immediately.”     | The context leaves the stack, but reachable objects and captured environments can remain.          |\n| “A timer resumes the context that scheduled it.” | A timer callback normally runs as a new function invocation with a new context.                    |\n\n## React Connection\n\nA function component is still a function. Every render is a fresh invocation with a fresh execution context and fresh local bindings.\n\n```jsx\nfunction ProductSearch({ query }) {\n  const normalizedQuery = query.trim().toLowerCase();\n\n  function handleSearch() {\n    console.log(normalizedQuery);\n  }\n\n  return \u003cbutton onClick={handleSearch}>Search\u003c/button>;\n}\n```\n\nEach render creates a new `normalizedQuery` binding and a new `handleSearch` function. The component call completes, but the handler may keep that render's environment reachable. React state persists because React stores it outside any single component invocation and supplies a snapshot during a later render.\n\nThis also explains why a plain local variable cannot serve as persistent component storage. In development, Strict Mode may invoke components more than once to expose impure rendering; each call is a real invocation, not React resetting one context.\n\n## Performance and Memory Implications\n\nThe specification does not require one allocation or physical frame per conceptual context. Engines can inline calls, reuse storage, or materialize debugging information only when necessary. Do not estimate performance by counting boxes in a context diagram.\n\nFor memory reasoning, separate active execution from reachability:\n\n- a completed call is no longer active;\n- a closure may retain selected environment bindings;\n- a suspended generator or async evaluation retains state needed for resumption;\n- a paused debugger may keep values reachable.\n\nRecursion can exhaust an implementation's stack, but ECMAScript defines no universal maximum call depth.\n\n## Debugging Techniques\n\nWhen paused inside a function, inspect two DevTools panes separately:\n\n- **Call Stack** reconstructs the dynamic path of invocations.\n- **Scope** shows bindings available to the selected frame.\n\nSelecting another recursive frame changes the invocation-specific locals displayed. It does not change the function's lexical structure.\n\nTreat stack traces as diagnostics. Optimized code may be inlined, and async tooling may add causal frames from earlier scheduling points. Those frames are useful evidence about execution history, not a literal dump of ECMAScript's context stack.\n\n## Interview Questions\n\n### Level 1 — Fundamentals\n\n**Question:** What is an execution context?\n\n**Model answer:**\n\nAn execution context is ECMAScript's internal mechanism for tracking code evaluation. It carries associations such as the current function, realm, source script or module, and enough state to run, suspend, or resume. The running context is normally the top of an agent's context stack. It is not a JavaScript object and does not have to map one-to-one to an engine frame.\n\n### Level 2 — Applied understanding\n\n**Question:** Does each call to the same function receive a new execution context?\n\n**Model answer:**\n\nYes. Each active invocation has distinct evaluation state and normally a distinct function context and function environment. That is why recursive calls and calls with different arguments have independent local bindings. The function object can be shared while invocation state differs.\n\n### Level 3 — Senior reasoning\n\n**Question:** What happens to an async function's execution context at `await`?\n\n**Model answer:**\n\nThe function starts synchronously. At `await`, its evaluation can suspend and return a promise to the caller, which then continues. Promise-job machinery later resumes the async evaluation with a fulfillment value or rejection. The engine does not need to leave one normal native stack frame blocking the thread during the wait.\n\n### Level 4 — Deep follow-up\n\n**Question:** Does a closure preserve its outer function's execution context?\n\n**Model answer:**\n\nThe precise model is that the closure retains access to its creation environment. Captured bindings can remain reachable after the outer call's context has left the context stack. The engine does not need to retain the entire context or physical frame. In memory debugging, I inspect actual retaining paths rather than assuming every local survives.\n\n## Exercises\n\n### 1. Trace running contexts\n\n```js\nfunction first(value) {\n  return second(value + 1);\n}\n\nfunction second(value) {\n  return value * 2;\n}\n\nconsole.log(first(5));\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe script runs first. Calling `first` suspends it and makes the `first` context current. Calling `second` suspends `first`. `second` returns `12`, so `first` resumes and returns `12`; the script then resumes and logs it.\n\n\u003c/details>\n\n### 2. Predict async output\n\n```js\nasync function run() {\n  console.log(\"B\");\n  await Promise.resolve();\n  console.log(\"D\");\n}\n\nconsole.log(\"A\");\nrun();\nconsole.log(\"C\");\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe output is `A`, `B`, `C`, `D`. `run` starts synchronously and suspends at `await`. The script logs `C`; the async continuation resumes later through promise-job scheduling and logs `D`.\n\n\u003c/details>\n\n### 3. Diagnose the React assumption\n\nWhy does `let previousCount = 0` inside a function component not remember the value from its previous render?\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nEvery render is a new function invocation with a fresh local binding, so `previousCount` is initialized again. React state and refs persist because React stores their data outside a particular invocation. Use state when changes affect rendering and a ref for mutable data that should survive renders without causing one.\n\n\u003c/details>\n\n## Chapter Summary\n\n- **Control:** the running execution context is normally the top of an agent's context stack.\n- **Invocation:** each function call has distinct evaluation state and local bindings.\n- **Scope:** lexical environments determine identifier resolution; callers do not.\n- **Implementation:** contexts do not map rigidly to physical engine frames.\n- **Suspension:** `await` can suspend evaluation without blocking a synchronous frame.\n- **React:** every component render is a fresh function invocation with fresh locals.\n\n### Interview-ready explanation\n\nAn execution context is ECMAScript's internal model for tracking code evaluation. A function call normally suspends the caller, creates and pushes a new context for the callee, and removes it when the callee completes. Contexts are not JavaScript objects and need not map one-to-one to engine frames. Bindings live in environment records, so lexical scope is separate from dynamic call order. That distinction explains why closures can retain bindings after a call finishes, why async functions can suspend without blocking a native stack frame, and why each React render receives fresh local variables.\n\n## Further Reading\n\n- [ECMA-262: Execution Contexts](https://tc39.es/ecma262/#sec-execution-contexts)\n- [ECMA-262: PrepareForOrdinaryCall](https://tc39.es/ecma262/#sec-prepareforordinarycall)\n- [ECMA-262: FunctionDeclarationInstantiation](https://tc39.es/ecma262/#sec-functiondeclarationinstantiation)\n- [ECMA-262: Async Functions](https://tc39.es/ecma262/#sec-async-function-definitions)\n- [React: Keeping Components Pure](https://react.dev/learn/keeping-components-pure)\n- [React: State as a Snapshot](https://react.dev/learn/state-as-a-snapshot)",
    headings: [
      "Learning objectives",
      "Quick Refresher",
      "Why This Matters",
      "Core Mental Model",
      "Formal Model",
      "Visual Model",
      "Step-by-Step Runtime Walkthrough",
      "Common Misconceptions",
      "React Connection",
      "Performance and Memory Implications",
      "Debugging Techniques",
      "Interview Questions",
      "Exercises",
      "Chapter Summary",
      "Further Reading",
    ],
    quickRefresher:
      "An execution context is ECMAScript's internal model for an active or suspended evaluation. The running execution context is normally the top entry of an agent's context stack. A function call normally suspends the caller and pushes a new function context. Returning removes the callee context and resumes the caller. Every invocation has distinct runtime state and local bindings. An execution context, a lexical environment, and an engine stack frame are related but different models. Every React component render is a fresh function invocation with fresh local bindings.",
    excerpt:
      "Execution contexts explain how control moves between scripts, functions, generators, and async functions. Interviewers rarely want a list of specification fields. They want to know whether you can identify which invocation is running, which invocation owns a local value, and what happens when evaluation returns or suspends.",
    readingMinutes: 10,
    searchText:
      "chapter 2 — execution contexts learning objectives after completing this chapter, you should be able to: define an execution context without treating it as a javascript object; trace how calls and returns change the running execution context; distinguish execution contexts, lexical environments, and engine stack frames; explain declaration instantiation without relying on a universal “creation phase”; explain how async functions suspend without blocking a synchronous stack frame; connect fresh function invocations to react render behavior. quick refresher an execution context is ecmascript's internal model for an active or suspended evaluation. the running execution context is normally the top entry of an agent's context stack. a function call normally suspends the caller and pushes a new function context. returning removes the callee context and resumes the caller. every invocation has distinct runtime state and local bindings. an execution context, a lexical environment, and an engine stack frame are related but different models. every react component render is a fresh function invocation with fresh local bindings. why this matters execution contexts explain how control moves between scripts, functions, generators, and async functions. interviewers rarely want a list of specification fields. they want to know whether you can identify which invocation is running, which invocation owns a local value, and what happens when evaluation returns or suspends. a poor model leads to practical mistakes: assuming a closure retains an entire call stack, believing await leaves a normal stack frame blocked, or expecting a react component's local variables to persist across renders. core mental model an execution context is ecmascript's control record for one active or suspended evaluation . it tracks enough state to answer: which code is being evaluated? which function, script, module, and realm does it belong to? where should evaluation continue after a call returns or suspended work resumes? at most one context per agent is running at a time. it is normally the top entry of the execution context stack. keep three models separate: concept what it explains what it is not execution context evaluation state and transfer of control a javascript object lexical environment bindings and identifier resolution the dynamic caller chain engine stack frame implementation storage for a call a required one to one representation of a context calling a function changes the context stack. it does not change the called function's lexical parent. formal model running context and context stack each ecmascript agent has an execution context stack. its top entry is the running execution context . calling an ordinary function normally creates a function context, suspends the caller, pushes the callee, and makes it current. when the callee completes, its context is removed and the caller resumes. this is specification machinery. an engine may inline a call, eliminate storage, or reconstruct frames for debugging while preserving the required behavior. what a context contains every context carries evaluation state and associations such as: the function being evaluated, or null for script or module code; the relevant realm; the originating script or module, when one exists; enough state to perform, suspend, and resume evaluation. evaluation algorithms also use lexical, variable, and private environment components associated with the running context. those environments contain bindings; the context itself should not be imagined as a user visible object containing all local variables. calling an ordinary function an ordinary call can be summarized as: 1. evaluate the callee and arguments in the caller. 2. create a function context for the invocation. 3. suspend the caller and make the callee context current. 4. establish parameters, this , and the function's environments. 5. instantiate declarations required by the body. 6. evaluate the body and produce a completion. 7. remove the callee context and resume the caller. a throw produces an abrupt completion rather than a normal return. the context still unwinds through defined completion rules. declaration instantiation, not source movement “creation phase” is common teaching shorthand, but ecmascript defines specific declaration instantiation algorithms for functions, scripts, modules, and blocks. for a function call, parameters and declarations are prepared before body statements execute, but their initialization differs: declaration initial state before its declaration statement function declaration generally initialized with its function object var initialized to undefined let , const , class binding exists but remains uninitialized nothing is physically moved to the top of the source file. “hoisting” describes observable initialization behavior; it is not a source code transformation. suspension is not an ordinary blocked call generators and async functions can suspend and later resume evaluation. the specification preserves the necessary context state, but an engine does not need to retain one normal native stack frame throughout the pause. this distinction becomes visible with await : the async function starts synchronously, suspends, returns a promise to its caller, and resumes later through promise job scheduling. visual model an ordinary function call follows one compact control cycle: read it as a change in which context owns control. a call suspends the caller and makes a new callee context the running context. completion removes the callee context and continues the caller immediately after the call. this is a specification model, not a promise about physical engine frames. it also does not describe lexical scope: identifier lookup follows lexical environments, not the chain of callers. step by step runtime walkthrough the preceding trace demonstrates four rules: 1. one invocation, one context. the call to calculatetax creates invocation specific state, including its amount parameter. 2. the top context runs. while multiply is current, the script and calculatetax contexts are suspended. 3. returns unwind in reverse call order. multiply completes before calculatetax can continue and return. 4. scope is not the call stack. taxrate is found through lexical environments, not by searching callers. now compare an async function: the output is: loadprofile begins synchronously. at await , its evaluation suspends and the call returns a promise. the script continues without a blocked synchronous frame. promise job machinery later resumes the async evaluation. devtools may display an async stack linking both sides, but that is diagnostic causality—not one uninterrupted physical call stack. common misconceptions claim better explanation “a context is an object containing locals.” it is inaccessible specification state; bindings are modeled by environment records. “execution context and scope are the same.” context tracks evaluation; lexical environments determine identifier resolution. “the context stack is exactly the engine stack.” the engine may inline, optimize, or reconstruct frames while preserving semantics. “hoisting moves declarations.” declaration instantiation algorithms create and initialize bindings according to declaration type. “returning deletes every local immediately.” the context leaves the stack, but reachable objects and captured environments can remain. “a timer resumes the context that scheduled it.” a timer callback normally runs as a new function invocation with a new context. react connection a function component is still a function. every render is a fresh invocation with a fresh execution context and fresh local bindings. each render creates a new normalizedquery binding and a new handlesearch function. the component call completes, but the handler may keep that render's environment reachable. react state persists because react stores it outside any single component invocation and supplies a snapshot during a later render. this also explains why a plain local variable cannot serve as persistent component storage. in development, strict mode may invoke components more than once to expose impure rendering; each call is a real invocation, not react resetting one context. performance and memory implications the specification does not require one allocation or physical frame per conceptual context. engines can inline calls, reuse storage, or materialize debugging information only when necessary. do not estimate performance by counting boxes in a context diagram. for memory reasoning, separate active execution from reachability: a completed call is no longer active; a closure may retain selected environment bindings; a suspended generator or async evaluation retains state needed for resumption; a paused debugger may keep values reachable. recursion can exhaust an implementation's stack, but ecmascript defines no universal maximum call depth. debugging techniques when paused inside a function, inspect two devtools panes separately: call stack reconstructs the dynamic path of invocations. scope shows bindings available to the selected frame. selecting another recursive frame changes the invocation specific locals displayed. it does not change the function's lexical structure. treat stack traces as diagnostics. optimized code may be inlined, and async tooling may add causal frames from earlier scheduling points. those frames are useful evidence about execution history, not a literal dump of ecmascript's context stack. interview questions level 1 — fundamentals question: what is an execution context? model answer: an execution context is ecmascript's internal mechanism for tracking code evaluation. it carries associations such as the current function, realm, source script or module, and enough state to run, suspend, or resume. the running context is normally the top of an agent's context stack. it is not a javascript object and does not have to map one to one to an engine frame. level 2 — applied understanding question: does each call to the same function receive a new execution context? model answer: yes. each active invocation has distinct evaluation state and normally a distinct function context and function environment. that is why recursive calls and calls with different arguments have independent local bindings. the function object can be shared while invocation state differs. level 3 — senior reasoning question: what happens to an async function's execution context at await ? model answer: the function starts synchronously. at await , its evaluation can suspend and return a promise to the caller, which then continues. promise job machinery later resumes the async evaluation with a fulfillment value or rejection. the engine does not need to leave one normal native stack frame blocking the thread during the wait. level 4 — deep follow up question: does a closure preserve its outer function's execution context? model answer: the precise model is that the closure retains access to its creation environment. captured bindings can remain reachable after the outer call's context has left the context stack. the engine does not need to retain the entire context or physical frame. in memory debugging, i inspect actual retaining paths rather than assuming every local survives. exercises 1. trace running contexts solution the script runs first. calling first suspends it and makes the first context current. calling second suspends first . second returns 12 , so first resumes and returns 12 ; the script then resumes and logs it. 2. predict async output solution the output is a , b , c , d . run starts synchronously and suspends at await . the script logs c ; the async continuation resumes later through promise job scheduling and logs d . 3. diagnose the react assumption why does let previouscount = 0 inside a function component not remember the value from its previous render? solution every render is a new function invocation with a fresh local binding, so previouscount is initialized again. react state and refs persist because react stores their data outside a particular invocation. use state when changes affect rendering and a ref for mutable data that should survive renders without causing one. chapter summary control: the running execution context is normally the top of an agent's context stack. invocation: each function call has distinct evaluation state and local bindings. scope: lexical environments determine identifier resolution; callers do not. implementation: contexts do not map rigidly to physical engine frames. suspension: await can suspend evaluation without blocking a synchronous frame. react: every component render is a fresh function invocation with fresh locals. interview ready explanation an execution context is ecmascript's internal model for tracking code evaluation. a function call normally suspends the caller, creates and pushes a new context for the callee, and removes it when the callee completes. contexts are not javascript objects and need not map one to one to engine frames. bindings live in environment records, so lexical scope is separate from dynamic call order. that distinction explains why closures can retain bindings after a call finishes, why async functions can suspend without blocking a native stack frame, and why each react render receives fresh local variables. further reading ecma 262: execution contexts ecma 262: prepareforordinarycall ecma 262: functiondeclarationinstantiation ecma 262: async functions react: keeping components pure react: state as a snapshot",
  },
  {
    number: 3,
    slug: "03-the-call-stack",
    title: "The Call Stack",
    kind: "chapter",
    partNumber: 1,
    partName: "Runtime Foundations",
    markdown:
      '# Chapter 3 — The Call Stack\n\n## Learning objectives\n\nAfter completing this chapter, you should be able to:\n\n- trace synchronous calls in last-in, first-out order;\n- distinguish the execution-context stack, engine frames, and stack traces;\n- explain how returns and thrown errors unwind active calls;\n- explain why `try/catch` does not cross a later asynchronous boundary;\n- reason about recursion limits without inventing a universal maximum;\n- distinguish JavaScript call stacks from React component stacks.\n\n## Quick Refresher\n\n- The call stack is the practical model for unfinished synchronous calls.\n- The most recent callee runs first and completes before its caller resumes.\n- ECMAScript specifies an execution-context stack; engine frames are implementation details.\n- A thrown value propagates outward through active calls until a matching `catch` handles it.\n- A later callback starts a new synchronous call chain; the scheduling call is no longer active.\n- Async stack traces preserve causality, not live frames.\n- Stack depth does not measure duration, and ECMAScript defines no universal recursion limit.\n\n## Why This Matters\n\nStack questions test whether you can reconstruct the path to a failure, understand why an error was—or was not—caught, and distinguish a blocked main thread from an asynchronous wait. These skills matter when debugging production bundles, React render failures, and callbacks whose visible stack no longer includes their scheduling origin.\n\nThe terminology can blur easily. ECMAScript specifies execution-context transitions. Engines implement frames and expose formatted stack traces. For ordinary synchronous calls the models align closely, but they are not identical guarantees.\n\n## Core Mental Model\n\nFor synchronous code, treat the stack as a last-in, first-out record of unfinished calls:\n\n1. A call places the callee above its caller.\n2. The callee runs while the caller waits.\n3. Returning removes the callee and resumes the caller.\n4. Throwing exits calls until an active handler is found.\n\nThe top frame is currently executing. Lower frames explain how control arrived there.\n\nKeep these terms separate:\n\n| Term                        | What it describes                                     | Important qualification                          |\n| --------------------------- | ----------------------------------------------------- | ------------------------------------------------ |\n| **Execution-context stack** | ECMAScript\'s specification model for control transfer | Normative but not a memory-layout requirement    |\n| **Engine frame**            | Implementation bookkeeping for a call                 | Can be optimized, inlined, or reconstructed      |\n| **Stack trace**             | A diagnostic snapshot or reconstruction               | Can outlive the calls and include async ancestry |\n\n## Formal Model\n\n### Calls and returns\n\nEach ECMAScript agent has an execution-context stack whose top entry is the running context. An ordinary call normally suspends the caller, pushes a callee context, evaluates the function body, removes the callee context, and resumes the caller.\n\nThe familiar call-stack diagram is therefore grounded in specification behavior, even though ECMAScript does not prescribe a physical native stack.\n\n### Completion records and unwinding\n\nECMAScript uses **Completion Records** to model evaluation results. Their types include `normal`, `return`, `throw`, `break`, and `continue`. Every type other than `normal` is an **abrupt completion**.\n\n- `return` supplies a function result and exits that call.\n- `throw` propagates through nested evaluations and calls until handled.\n- `try`, `catch`, and `finally` can transform, preserve, or replace a pending completion.\n\n“Stack unwinding” is practical language for calls being exited while a throw searches outward for a handler. JavaScript permits throwing any value, but `Error` objects are normally preferable because they carry a name, message, and implementation-provided diagnostic stack.\n\n### Stack traces are diagnostics\n\n`error.stack` is widely available but not standardized by ECMA-262. Its format, capture timing, frame limit, and async information can differ by engine. APIs such as `Error.captureStackTrace` and `Error.stackTraceLimit` are V8-specific.\n\nOptimized code complicates the picture further: engines can inline calls or reconstruct source-level frames for debugging. Use a trace to locate an execution path, not as proof of a required internal frame layout.\n\n### Async boundaries\n\nTimers, events, and promise continuations execute after their scheduling code has finished. Their callbacks begin new synchronous call chains from host or job-processing machinery. The original scheduling function and its `try/catch` are no longer active.\n\nDevTools may display an async parent such as “scheduled from.” That is causal metadata answering **what scheduled this?** It is not a live frame waiting on the stack.\n\n## Visual Model\n\nConsider:\n\n```js\nfunction readSubtotal(order) {\n  return order.subtotal;\n}\n\nfunction addTax(order) {\n  return readSubtotal(order) * 1.2;\n}\n\nfunction formatTotal(order) {\n  return `$${addTax(order).toFixed(2)}`;\n}\n\nconsole.log(formatTotal({ subtotal: 100 }));\n```\n\nThe stack keeps unfinished calls below the function that is currently running:\n\n![Call stack growth and last-in first-out unwinding](/call-stack-lifo.svg)\n\nOnly the top frame runs. Every frame below it represents an unfinished caller. Calls push frames; returns or uncaught throws remove them in last-in, first-out order until an earlier caller can resume.\n\nThe diagram shows dynamic calls, not lexical nesting, captured variables, or guaranteed physical engine frames.\n\n## Step-by-Step Runtime Walkthrough\n\nThe output is `$120.00`. The important states are:\n\n1. The script calls `formatTotal`; the script waits.\n2. `formatTotal` calls `addTax`; both earlier calls remain unfinished.\n3. `addTax` calls `readSubtotal`, producing maximum application depth.\n4. `readSubtotal` returns `100`; `addTax` resumes and produces `120`.\n5. `addTax` returns; `formatTotal` formats and returns the string.\n6. The script resumes and logs the result.\n\nNow consider error propagation:\n\n```js\nfunction parsePrice(rawPrice) {\n  const price = Number(rawPrice);\n  if (!Number.isFinite(price)) throw new TypeError("Price must be numeric");\n  return price;\n}\n\nfunction normalizeProduct(payload) {\n  return { id: payload.id, price: parsePrice(payload.price) };\n}\n\nfunction handleProduct(payload) {\n  try {\n    return normalizeProduct(payload);\n  } catch (error) {\n    console.error("Invalid product", error);\n    return null;\n  }\n}\n```\n\n`parsePrice` throws instead of returning normally. The throw exits `parsePrice`, propagates through `normalizeProduct`, and reaches the active `catch` in `handleProduct`. That handler converts the abrupt path into a normal return of `null`.\n\n### Why `try/catch` does not cross a later task\n\n```js\ntry {\n  setTimeout(() => {\n    throw new Error("Timer failed");\n  }, 0);\n} catch (error) {\n  console.log("caught", error.message);\n}\n```\n\nThe catch does not run. The `try` surrounds the synchronous call to `setTimeout`. That call returns and its stack unwinds before the host invokes the timer callback in a later task. Handle the error inside the callback or represent the later result with a promise and handle its rejection.\n\n## Common Misconceptions\n\n| Claim                                          | Better explanation                                                                       |\n| ---------------------------------------------- | ---------------------------------------------------------------------------------------- |\n| “The stack stores every function that ran.”    | It contains unfinished synchronous calls; traces and profiles record history separately. |\n| “A stack trace is the live stack.”             | It is a snapshot or reconstruction that can outlive the calls it describes.              |\n| “Displayed frames always match source calls.”  | Optimization, inlining, bundling, and source maps affect what tools display.             |\n| “Async code stays on the stack while waiting.” | Later callbacks begin new synchronous call chains; async frames preserve causality only. |\n| “Stack overflow has a standard depth.”         | Limits vary by engine, platform, function shape, and available resources.                |\n| “Catching an error repairs the operation.”     | A catch stops propagation, but state may already require rollback or validation.         |\n\n## React Connection\n\nReact render work is still a synchronous JavaScript call chain. A deep stack is not automatically slow; total work matters. A shallow function containing a long loop can block input and paint more severely than many quick nested calls.\n\nUse the browser Performance panel to measure main-thread work and the React Profiler to attribute render and commit work to components.\n\nA JavaScript stack and a React **component stack** answer different questions:\n\n- A JavaScript stack shows active function callers.\n- A component stack shows ancestry in the rendered React tree.\n\nThis is why a parent\'s ordinary `try/catch` does not catch a descendant render failure:\n\n```jsx\nfunction Parent() {\n  try {\n    return \u003cChild />;\n  } catch {\n    return \u003cp>Could not render child.\u003c/p>;\n  }\n}\n```\n\n`Parent` returns an element description. React invokes `Child` later in its own traversal, after the parent call has returned. Use an error boundary for descendant render failures. Event-handler and unrelated async errors still require handling in their own execution paths.\n\n## Performance and Memory Implications\n\n- **Depth is not duration.** Use a CPU profile or performance trace to measure time.\n- **Recursion consumes bounded implementation resources.** Prefer iteration or an explicit work stack when input can create extreme depth; do not rely on universal tail-call optimization.\n- **Capturing stacks has a cost.** Error creation, high trace limits, and retained diagnostic metadata can consume CPU and memory.\n- **Popping a frame does not prove collection.** Closures, suspended work, logs, or debugger state can retain referenced values.\n\n## Debugging Techniques\n\nRead a conventional trace from the top application frame outward: the top shows where the failure surfaced; lower frames reconstruct callers.\n\nIn DevTools:\n\n1. Enable **Pause on uncaught exceptions**.\n2. Temporarily include caught exceptions when handling occurs too far from the cause.\n3. Inspect the Call Stack and invocation-specific locals at the pause.\n4. Distinguish authored, framework, and generated frames.\n5. Verify that production source maps match the exact deployed build.\n6. Treat async frames as scheduling ancestry rather than live callers.\n\n`console.trace()` is useful for the current path. Avoid parsing `error.stack` as a stable cross-browser format.\n\n## Interview Questions\n\n### Level 1 — Fundamentals\n\n**Question:** What is the JavaScript call stack?\n\n**Model answer:**\n\nThe call stack is the practical model for unfinished synchronous calls. A callee runs above its suspended caller and returns control in last-in, first-out order. ECMAScript formally specifies an execution-context stack, while engines implement and optimize frames. A stack trace is therefore diagnostic output, not a standardized memory layout.\n\n### Level 2 — Applied understanding\n\n**Question:** How does a thrown error move through the stack?\n\n**Model answer:**\n\nA throw produces an abrupt completion. If the current evaluation does not handle it, it propagates outward and active calls unwind until a matching `catch` handles it or the throw reaches the host as uncaught. A `catch` can return normally, rethrow, or throw a different error.\n\n### Level 3 — Senior reasoning\n\n**Question:** Why can `try/catch` around `setTimeout` not catch an error from its callback?\n\n**Model answer:**\n\nThe `try` surrounds only the synchronous scheduling call. `setTimeout` returns and that call chain finishes. The host invokes the callback in a later task with a new synchronous stack, so the old catch is no longer active. Async stack tooling may show the scheduling origin, but that does not restore the handler.\n\n### Level 4 — Deep follow-up\n\n**Question:** Can a stack trace establish runtime cost or lexical scope?\n\n**Model answer:**\n\nNo. A trace shows a dynamic call path, not the time consumed by each frame; performance requires profiling. It also shows callers rather than lexical outer environments, so it cannot establish identifier resolution. Optimization, source transformation, and async reconstruction further affect displayed frames.\n\n## Exercises\n\n### 1. Trace maximum depth\n\n```js\nfunction a(value) {\n  return b(value) + 1;\n}\n\nfunction b(value) {\n  return c(value * 2);\n}\n\nfunction c(value) {\n  return value - 3;\n}\n\nconsole.log(a(5));\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nAt maximum application depth the order from bottom to top is script → `a` → `b` → `c`. `c(10)` returns `7`, `b` returns `7`, and `a` adds `1`, so the output is `8`.\n\n\u003c/details>\n\n### 2. Predict `finally`\n\n```js\nfunction getStatus() {\n  try {\n    throw new Error("failed");\n  } catch {\n    return "recovered";\n  } finally {\n    console.log("cleanup");\n  }\n}\n\nconsole.log(getStatus());\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe output is `cleanup`, then `recovered`. `finally` runs before the pending return completes. If `finally` returned or threw, that new abrupt completion would replace the earlier return.\n\n\u003c/details>\n\n### 3. Explain the React boundary\n\nWhy does `try { return \u003cChild /> } catch { ... }` in a parent not catch an error thrown when React renders `Child`?\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe parent returns an element description; it does not call `Child` inside that `try`. React invokes `Child` later during its traversal, after the parent call has returned. Use a React error boundary for descendant render errors and handle event or async errors in their own paths.\n\n\u003c/details>\n\n## Chapter Summary\n\n- **Order:** unfinished synchronous calls follow last-in, first-out control flow.\n- **Unwinding:** returns and throws remove active calls; throws continue until handled.\n- **Diagnostics:** stack traces represent execution paths but are not standardized live stacks.\n- **Async:** later callbacks run on new synchronous stacks.\n- **Performance:** stack depth does not measure duration, and recursion limits are implementation-specific.\n- **React:** JavaScript stacks and component stacks represent different relationships.\n\n### Interview-ready explanation\n\nThe call stack is the practical last-in, first-out model for unfinished synchronous calls. A callee runs above a suspended caller, and returns remove calls in reverse order. Throws propagate as abrupt completions until an active catch handles them. ECMAScript specifies an execution-context stack, while engines implement and optimize physical frames, so stack traces are diagnostics rather than standardized memory layouts. Async callbacks run on later synchronous stacks; tools may link their scheduling ancestry, but the original callers and catch blocks are no longer active.\n\n## Further Reading\n\n- [ECMA-262: Completion Record Specification Type](https://tc39.es/ecma262/#sec-completion-record-specification-type)\n- [ECMA-262: Execution Contexts](https://tc39.es/ecma262/#sec-execution-contexts)\n- [ECMA-262: The `try` Statement](https://tc39.es/ecma262/#sec-try-statement)\n- [V8: Stack Trace API](https://v8.dev/docs/stack-trace-api)\n- [Chrome DevTools: JavaScript Debugging Reference](https://developer.chrome.com/docs/devtools/javascript/reference)\n- [React: Component error boundary APIs](https://react.dev/reference/react/Component#catching-rendering-errors-with-an-error-boundary)',
    headings: [
      "Learning objectives",
      "Quick Refresher",
      "Why This Matters",
      "Core Mental Model",
      "Formal Model",
      "Visual Model",
      "Step-by-Step Runtime Walkthrough",
      "Common Misconceptions",
      "React Connection",
      "Performance and Memory Implications",
      "Debugging Techniques",
      "Interview Questions",
      "Exercises",
      "Chapter Summary",
      "Further Reading",
    ],
    quickRefresher:
      "The call stack is the practical model for unfinished synchronous calls. The most recent callee runs first and completes before its caller resumes. ECMAScript specifies an execution context stack; engine frames are implementation details. A thrown value propagates outward through active calls until a matching catch handles it. A later callback starts a new synchronous call chain; the scheduling call is no longer active. Async stack traces preserve causality, not live frames. Stack depth does not measure duration, and ECMAScript defines no universal recursion limit.",
    excerpt:
      "Stack questions test whether you can reconstruct the path to a failure, understand why an error was—or was not—caught, and distinguish a blocked main thread from an asynchronous wait. These skills matter when debugging production bundles, React render failures, and callbacks whose visible stack no longer includes their scheduling origin.",
    readingMinutes: 9,
    searchText:
      "chapter 3 — the call stack learning objectives after completing this chapter, you should be able to: trace synchronous calls in last in, first out order; distinguish the execution context stack, engine frames, and stack traces; explain how returns and thrown errors unwind active calls; explain why try/catch does not cross a later asynchronous boundary; reason about recursion limits without inventing a universal maximum; distinguish javascript call stacks from react component stacks. quick refresher the call stack is the practical model for unfinished synchronous calls. the most recent callee runs first and completes before its caller resumes. ecmascript specifies an execution context stack; engine frames are implementation details. a thrown value propagates outward through active calls until a matching catch handles it. a later callback starts a new synchronous call chain; the scheduling call is no longer active. async stack traces preserve causality, not live frames. stack depth does not measure duration, and ecmascript defines no universal recursion limit. why this matters stack questions test whether you can reconstruct the path to a failure, understand why an error was—or was not—caught, and distinguish a blocked main thread from an asynchronous wait. these skills matter when debugging production bundles, react render failures, and callbacks whose visible stack no longer includes their scheduling origin. the terminology can blur easily. ecmascript specifies execution context transitions. engines implement frames and expose formatted stack traces. for ordinary synchronous calls the models align closely, but they are not identical guarantees. core mental model for synchronous code, treat the stack as a last in, first out record of unfinished calls: 1. a call places the callee above its caller. 2. the callee runs while the caller waits. 3. returning removes the callee and resumes the caller. 4. throwing exits calls until an active handler is found. the top frame is currently executing. lower frames explain how control arrived there. keep these terms separate: term what it describes important qualification execution context stack ecmascript's specification model for control transfer normative but not a memory layout requirement engine frame implementation bookkeeping for a call can be optimized, inlined, or reconstructed stack trace a diagnostic snapshot or reconstruction can outlive the calls and include async ancestry formal model calls and returns each ecmascript agent has an execution context stack whose top entry is the running context. an ordinary call normally suspends the caller, pushes a callee context, evaluates the function body, removes the callee context, and resumes the caller. the familiar call stack diagram is therefore grounded in specification behavior, even though ecmascript does not prescribe a physical native stack. completion records and unwinding ecmascript uses completion records to model evaluation results. their types include normal , return , throw , break , and continue . every type other than normal is an abrupt completion . return supplies a function result and exits that call. throw propagates through nested evaluations and calls until handled. try , catch , and finally can transform, preserve, or replace a pending completion. “stack unwinding” is practical language for calls being exited while a throw searches outward for a handler. javascript permits throwing any value, but error objects are normally preferable because they carry a name, message, and implementation provided diagnostic stack. stack traces are diagnostics error.stack is widely available but not standardized by ecma 262. its format, capture timing, frame limit, and async information can differ by engine. apis such as error.capturestacktrace and error.stacktracelimit are v8 specific. optimized code complicates the picture further: engines can inline calls or reconstruct source level frames for debugging. use a trace to locate an execution path, not as proof of a required internal frame layout. async boundaries timers, events, and promise continuations execute after their scheduling code has finished. their callbacks begin new synchronous call chains from host or job processing machinery. the original scheduling function and its try/catch are no longer active. devtools may display an async parent such as “scheduled from.” that is causal metadata answering what scheduled this? it is not a live frame waiting on the stack. visual model consider: the stack keeps unfinished calls below the function that is currently running: only the top frame runs. every frame below it represents an unfinished caller. calls push frames; returns or uncaught throws remove them in last in, first out order until an earlier caller can resume. the diagram shows dynamic calls, not lexical nesting, captured variables, or guaranteed physical engine frames. step by step runtime walkthrough the output is $120.00 . the important states are: 1. the script calls formattotal ; the script waits. 2. formattotal calls addtax ; both earlier calls remain unfinished. 3. addtax calls readsubtotal , producing maximum application depth. 4. readsubtotal returns 100 ; addtax resumes and produces 120 . 5. addtax returns; formattotal formats and returns the string. 6. the script resumes and logs the result. now consider error propagation: parseprice throws instead of returning normally. the throw exits parseprice , propagates through normalizeproduct , and reaches the active catch in handleproduct . that handler converts the abrupt path into a normal return of null . why try/catch does not cross a later task the catch does not run. the try surrounds the synchronous call to settimeout . that call returns and its stack unwinds before the host invokes the timer callback in a later task. handle the error inside the callback or represent the later result with a promise and handle its rejection. common misconceptions claim better explanation “the stack stores every function that ran.” it contains unfinished synchronous calls; traces and profiles record history separately. “a stack trace is the live stack.” it is a snapshot or reconstruction that can outlive the calls it describes. “displayed frames always match source calls.” optimization, inlining, bundling, and source maps affect what tools display. “async code stays on the stack while waiting.” later callbacks begin new synchronous call chains; async frames preserve causality only. “stack overflow has a standard depth.” limits vary by engine, platform, function shape, and available resources. “catching an error repairs the operation.” a catch stops propagation, but state may already require rollback or validation. react connection react render work is still a synchronous javascript call chain. a deep stack is not automatically slow; total work matters. a shallow function containing a long loop can block input and paint more severely than many quick nested calls. use the browser performance panel to measure main thread work and the react profiler to attribute render and commit work to components. a javascript stack and a react component stack answer different questions: a javascript stack shows active function callers. a component stack shows ancestry in the rendered react tree. this is why a parent's ordinary try/catch does not catch a descendant render failure: parent returns an element description. react invokes child later in its own traversal, after the parent call has returned. use an error boundary for descendant render failures. event handler and unrelated async errors still require handling in their own execution paths. performance and memory implications depth is not duration. use a cpu profile or performance trace to measure time. recursion consumes bounded implementation resources. prefer iteration or an explicit work stack when input can create extreme depth; do not rely on universal tail call optimization. capturing stacks has a cost. error creation, high trace limits, and retained diagnostic metadata can consume cpu and memory. popping a frame does not prove collection. closures, suspended work, logs, or debugger state can retain referenced values. debugging techniques read a conventional trace from the top application frame outward: the top shows where the failure surfaced; lower frames reconstruct callers. in devtools: 1. enable pause on uncaught exceptions . 2. temporarily include caught exceptions when handling occurs too far from the cause. 3. inspect the call stack and invocation specific locals at the pause. 4. distinguish authored, framework, and generated frames. 5. verify that production source maps match the exact deployed build. 6. treat async frames as scheduling ancestry rather than live callers. console.trace() is useful for the current path. avoid parsing error.stack as a stable cross browser format. interview questions level 1 — fundamentals question: what is the javascript call stack? model answer: the call stack is the practical model for unfinished synchronous calls. a callee runs above its suspended caller and returns control in last in, first out order. ecmascript formally specifies an execution context stack, while engines implement and optimize frames. a stack trace is therefore diagnostic output, not a standardized memory layout. level 2 — applied understanding question: how does a thrown error move through the stack? model answer: a throw produces an abrupt completion. if the current evaluation does not handle it, it propagates outward and active calls unwind until a matching catch handles it or the throw reaches the host as uncaught. a catch can return normally, rethrow, or throw a different error. level 3 — senior reasoning question: why can try/catch around settimeout not catch an error from its callback? model answer: the try surrounds only the synchronous scheduling call. settimeout returns and that call chain finishes. the host invokes the callback in a later task with a new synchronous stack, so the old catch is no longer active. async stack tooling may show the scheduling origin, but that does not restore the handler. level 4 — deep follow up question: can a stack trace establish runtime cost or lexical scope? model answer: no. a trace shows a dynamic call path, not the time consumed by each frame; performance requires profiling. it also shows callers rather than lexical outer environments, so it cannot establish identifier resolution. optimization, source transformation, and async reconstruction further affect displayed frames. exercises 1. trace maximum depth solution at maximum application depth the order from bottom to top is script → a → b → c . c(10) returns 7 , b returns 7 , and a adds 1 , so the output is 8 . 2. predict finally solution the output is cleanup , then recovered . finally runs before the pending return completes. if finally returned or threw, that new abrupt completion would replace the earlier return. 3. explain the react boundary why does try { return } catch { ... } in a parent not catch an error thrown when react renders child ? solution the parent returns an element description; it does not call child inside that try . react invokes child later during its traversal, after the parent call has returned. use a react error boundary for descendant render errors and handle event or async errors in their own paths. chapter summary order: unfinished synchronous calls follow last in, first out control flow. unwinding: returns and throws remove active calls; throws continue until handled. diagnostics: stack traces represent execution paths but are not standardized live stacks. async: later callbacks run on new synchronous stacks. performance: stack depth does not measure duration, and recursion limits are implementation specific. react: javascript stacks and component stacks represent different relationships. interview ready explanation the call stack is the practical last in, first out model for unfinished synchronous calls. a callee runs above a suspended caller, and returns remove calls in reverse order. throws propagate as abrupt completions until an active catch handles them. ecmascript specifies an execution context stack, while engines implement and optimize physical frames, so stack traces are diagnostics rather than standardized memory layouts. async callbacks run on later synchronous stacks; tools may link their scheduling ancestry, but the original callers and catch blocks are no longer active. further reading ecma 262: completion record specification type ecma 262: execution contexts ecma 262: the try statement v8: stack trace api chrome devtools: javascript debugging reference react: component error boundary apis",
  },
  {
    number: 4,
    slug: "04-lexical-environments-and-environment-records",
    title: "Lexical Environments and Environment Records",
    kind: "chapter",
    partNumber: 1,
    partName: "Runtime Foundations",
    markdown:
      '# Chapter 4 — Lexical Environments and Environment Records\n\n## Learning objectives\n\nAfter completing this chapter, you should be able to:\n\n- explain bindings without describing scope as a JavaScript object;\n- trace lexical lookup through `[[OuterEnv]]` links;\n- distinguish binding creation from initialization;\n- explain temporal dead zone, global bindings, and live imports;\n- connect retained environments to closures and React renders.\n\n## Quick Refresher\n\n- An environment record associates identifier names with bindings.\n- A binding has behavior and state; it is not necessarily an object property.\n- `[[OuterEnv]]` points to the lexically enclosing environment record.\n- Lookup stops at the first record containing the name.\n- An uninitialized binding throws when read; lookup does not continue outward.\n- Closures retain access to bindings from their creation environment, not an active call stack.\n\n## Why This Matters\n\n“JavaScript searches outer scopes” predicts simple examples, but senior interviews go further: Why can an inner `let` throw before its declaration instead of reading an outer variable? Why does top-level `let` differ from top-level `var`? Why do imports update when an exporter changes?\n\nEnvironment records provide one model for all of these behaviors. They also prevent two common mistakes: treating local variables as properties of an execution context and treating the temporal dead zone as a period in which a variable does not exist.\n\n## Core Mental Model\n\nThink of an environment record as a specification-level **set of bindings plus an outer link**:\n\n- the record answers whether it contains a name and how that binding behaves;\n- `[[OuterEnv]]` leads to the lexically enclosing record;\n- different record types support different language constructs.\n\nA binding is more than a name/value pair. It can be mutable or immutable, initialized or uninitialized, deletable or non-deletable, and direct or linked to an exported binding.\n\nEnvironment records are not ordinary JavaScript objects. Engines may keep values in registers, stack storage, or optimized internal structures as long as observable behavior matches the specification.\n\n## Visual Model\n\n![Environment record bindings, lifecycle states, and outer links](/environment-record-anatomy.svg)\n\nThe current record owns bindings with distinct rules and states. Its `[[OuterEnv]]` link connects it to the lexically enclosing record. An uninitialized binding already exists and therefore shadows the same name outside, even though reading it throws.\n\n## Formal Model\n\n### Binding lifecycle\n\nThe specification separates the important binding operations:\n\n1. **Create** the binding.\n2. **Initialize** it with a value.\n3. **Read or update** it according to its rules.\n4. **Delete** it only when that binding kind permits deletion.\n\nThis separation explains the temporal dead zone. A lexical binding may already exist and shadow an outer name while still being uninitialized.\n\n```js\nconst status = "outer";\n\n{\n  console.log(status); // ReferenceError\n  const status = "inner";\n}\n```\n\nBefore the block body runs, its own `status` binding is created. Lookup finds that binding and stops. Reading it before initialization throws; JavaScript does not fall back to the outer `status`.\n\n### Outer environment links\n\nWhen a name is needed, resolution starts in the current environment record:\n\n1. If the record contains the name, use that binding.\n2. Otherwise, follow `[[OuterEnv]]`.\n3. Repeat until a binding is found or the chain reaches `null`.\n\nThe chain follows **where code was defined**, not who called it. That distinction is the foundation of lexical scope and closures. Chapter 5 develops the identifier-resolution algorithm in detail.\n\n### Record types worth knowing\n\n| Record type | Main role                                            | Interview-relevant behavior                                  |\n| ----------- | ---------------------------------------------------- | ------------------------------------------------------------ |\n| Declarative | `let`, `const`, `class`, and other language bindings | Bindings are not object properties                           |\n| Function    | One function invocation                              | Adds function-specific state such as `this` where applicable |\n| Module      | Module-local and imported bindings                   | Imports are indirect, live, and read-only to the importer    |\n| Object      | Exposes object properties as bindings                | Used by mechanisms such as `with`                            |\n| Global      | Top-level classic-script bindings                    | Combines declarative and object-backed records               |\n\nFunction and module records specialize declarative behavior. “One call creates one function environment” is a useful default model, although parameters, direct `eval`, and other edge cases can require additional environments.\n\n### Global bindings are split\n\nA classic script\'s global environment combines two sides:\n\n- eligible top-level `var` and function declarations use an object-backed record and may become properties of `globalThis`;\n- top-level `let`, `const`, and `class` use a declarative record and do not become global-object properties.\n\n```js\nvar legacyMode = true;\nlet modernMode = true;\n\nconsole.log(globalThis.legacyMode); // true in a browser classic script\nconsole.log(globalThis.modernMode); // undefined\n```\n\nModules use module environment records instead. Their top-level declarations do not become properties of the global object.\n\n### Module imports are live bindings\n\nAn import does not receive a one-time copy. It indirectly refers to the exporter\'s binding, so an importer observes later updates made by the exporter. The importer cannot reassign the imported name.\n\nDetailed module linking and evaluation belong to Chapters 35–38.\n\n## Step-by-Step Runtime Walkthrough\n\n```js\nconst currency = "USD";\n\nfunction formatPrice(amount) {\n  const precision = 2;\n\n  if (amount === 0) {\n    const label = "Free";\n    return label;\n  }\n\n  return `${currency} ${amount.toFixed(precision)}`;\n}\n\nconsole.log(formatPrice(12));\nconsole.log(formatPrice(0));\n```\n\nThe output is:\n\n```text\nUSD 12.00\nFree\n```\n\nTrace the environments:\n\n1. Script evaluation creates the outer `currency` binding.\n2. Each call creates fresh function bindings for `amount` and `precision`.\n3. `currency` is absent locally, so lookup follows the function\'s outer link.\n4. On the zero path, entering the block creates a nested `label` binding.\n5. Returning finishes the active evaluations. An environment can still remain reachable if a closure needs one of its bindings.\n\n## Important Examples\n\n### Per-iteration bindings\n\n```js\nconst handlers = [];\n\nfor (let index = 0; index \u003c 3; index += 1) {\n  handlers.push(() => index);\n}\n\nconsole.log(handlers.map((handler) => handler())); // [0, 1, 2]\n```\n\nA `for` loop with a lexical declaration creates a distinct per-iteration binding. Each callback closes over a different binding. With `var`, the callbacks normally share one function- or global-scoped binding and produce `[3, 3, 3]`.\n\n### `const` protects the binding, not the object\n\n```js\nconst settings = { theme: "dark" };\n\nsettings.theme = "light"; // allowed\nsettings = {}; // TypeError\n```\n\nThe binding cannot be reassigned after initialization. That rule does not freeze the referenced object.\n\n### Closures observe bindings, not copied values\n\n```js\nfunction createCounter() {\n  let count = 0;\n  return () => ++count;\n}\n\nconst next = createCounter();\nconsole.log(next()); // 1\nconsole.log(next()); // 2\n```\n\nThe returned function retains access to the `count` binding. The completed call does not remain on the call stack.\n\n## Common Misconceptions\n\n| Claim                                                | Better explanation                                                                                  |\n| ---------------------------------------------------- | --------------------------------------------------------------------------------------------------- |\n| “Scope is an object containing variables.”           | Environment records are inaccessible specification mechanisms; only some records are object-backed. |\n| “The TDZ means the variable does not exist.”         | The binding exists but is uninitialized, so it shadows outer bindings and throws when read.         |\n| “`const` makes a value immutable.”                   | It prevents reassignment of the binding; referenced objects may remain mutable.                     |\n| “A closure copies outer values.”                     | A closure retains access to bindings from its creation environment.                                 |\n| “Top-level declarations are properties of `window`.” | Top-level lexical and module bindings are not global-object properties.                             |\n\n## React Connection\n\nEvery component render is a fresh function invocation with fresh bindings:\n\n```jsx\nfunction SearchResults({ query, items }) {\n  const normalizedQuery = query.trim().toLowerCase();\n\n  function handleReport() {\n    console.log(normalizedQuery);\n  }\n\n  return \u003cbutton onClick={handleReport}>Report query\u003c/button>;\n}\n```\n\n`handleReport` retains access to the `normalizedQuery` binding from the render in which that function was created. A later render creates a new binding and usually a new callback; it does not rewrite the environment captured by the old callback. This is the language basis of stale-closure bugs and React\'s “state as a snapshot” explanation.\n\nOrdinary locals cannot persist component state across renders. State and refs persist because React stores their data outside any one component invocation.\n\n## Performance and Memory Implications\n\nSource-level scopes do not map directly to heap allocations. Engines can keep uncaptured values in registers or stack storage and materialize environments only when semantics, debugging, or deoptimization require them.\n\nA closure is not automatically a leak. Retention becomes a problem when an unwanted long-lived reference—often an event listener, subscription, timer, or cache—keeps the closure and its captured data reachable.\n\nWhen debugging memory, inspect the retaining path and ask:\n\n- Which function remains reachable?\n- What keeps that function alive?\n- Which captured value accounts for the retained memory?\n- Does removing the listener, subscription, timer, or cache entry release it?\n\n## Debugging Techniques\n\n### Compare Scope and Call Stack\n\nPause inside a nested function in DevTools. **Call Stack** shows dynamic callers; **Scope** shows visible local, block, closure, script, and global bindings. The panels answer different questions and should not be treated as the same chain.\n\n### Diagnose a TDZ error\n\nWhen you see “Cannot access before initialization”:\n\n1. Find the nearest lexical declaration with the same name.\n2. Identify the environment that owns it.\n3. Check whether evaluation reads it before initialization.\n4. For modules, also consider circular evaluation order.\n\nChanging `let` to `var` changes semantics and may only hide the ordering defect.\n\n## Interview Questions\n\n### Level 1 — Fundamentals\n\n**Question:** What is a lexical environment?\n\n**Model answer:** JavaScript models identifier bindings with environment records connected through `[[OuterEnv]]`. A record knows which names it contains and how those bindings behave. Resolution follows the lexical chain determined by where code was defined. These records are specification mechanisms, not ordinary JavaScript objects.\n\n### Level 2 — Applied understanding\n\n**Question:** Why does a shadowed `let` throw before its declaration instead of reading the outer variable?\n\n**Model answer:** Block setup creates the inner binding before the statements run, but the declaration initializes it later. Resolution finds the inner binding and stops. Reading that uninitialized binding throws a `ReferenceError`, so the outer name is never considered.\n\n### Level 3 — Senior reasoning\n\n**Question:** Why do callbacks created by `for (let ...)` observe different indices?\n\n**Model answer:** The loop creates a distinct binding for each iteration. Each callback closes over its iteration\'s binding. With `var`, the callbacks normally share one function- or global-scoped binding and therefore observe its final value.\n\n### Level 4 — Deep follow-up\n\n**Question:** Is a top-level binding always a property of `globalThis`?\n\n**Model answer:** No. In classic scripts, eligible `var` and function declarations use the object-backed side of the global environment, while top-level `let`, `const`, and `class` use its declarative side. Modules use module environments. Only the object-backed case normally appears as a `globalThis` property.\n\n## Exercises\n\n### 1. Predict shadowing behavior\n\n```js\nconst value = 1;\n\n{\n  const value = 2;\n  console.log(value);\n}\n\nconsole.log(value);\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe output is `2` and then `1`. The block owns a distinct binding; after the block finishes, lookup starts in the outer environment again.\n\n\u003c/details>\n\n### 2. Explain the error\n\n```js\nlet enabled = true;\n\nfunction check() {\n  console.log(enabled);\n  let enabled = false;\n}\n\ncheck();\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe function\'s local `enabled` binding exists before body evaluation reaches its declaration, but it is uninitialized. It shadows the outer binding, so the read throws a `ReferenceError`.\n\n\u003c/details>\n\n### 3. Diagnose a React callback\n\nA callback created during an old render reads an old `query` after state changes. Why can React not update the callback\'s existing environment?\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe function retains the environment from the render that created it. A later render creates new bindings; it cannot change which environment the existing function captured. The application must use the newer callback, correct dependencies, a functional update, or deliberately current mutable data through a ref.\n\n\u003c/details>\n\n## Chapter Summary\n\n- Environment records associate names with bindings and link outward through `[[OuterEnv]]`.\n- Lookup follows lexical nesting and stops at the first record containing the name.\n- Binding creation and initialization are separate, which explains temporal dead zone behavior.\n- Global lexical bindings, global object properties, and module bindings are not interchangeable.\n- Closures retain access to bindings; React callbacks therefore retain values from a particular render.\n\n### Interview-ready explanation\n\nJavaScript models lexical scope with environment records. Each record contains bindings and points to its lexically enclosing record through `[[OuterEnv]]`. Lookup stops at the first record containing a name. Because creation and initialization are separate, an inner lexical binding can shadow an outer name while still throwing in the temporal dead zone. Closures keep access to bindings from the environment where they were created, which also explains why React callbacks can observe values from an older render.\n\n## Further Reading\n\n- [ECMA-262: Environment Records](https://tc39.es/ecma262/#sec-environment-records)\n- [ECMA-262: Global Environment Records](https://tc39.es/ecma262/#sec-global-environment-records)\n- [ECMA-262: Module Environment Records](https://tc39.es/ecma262/#sec-module-environment-records)\n- [ECMA-262: BlockDeclarationInstantiation](https://tc39.es/ecma262/#sec-blockdeclarationinstantiation)\n- [React: State as a Snapshot](https://react.dev/learn/state-as-a-snapshot)\n- [Chrome DevTools: JavaScript Debugging Reference](https://developer.chrome.com/docs/devtools/javascript/reference)',
    headings: [
      "Learning objectives",
      "Quick Refresher",
      "Why This Matters",
      "Core Mental Model",
      "Visual Model",
      "Formal Model",
      "Step-by-Step Runtime Walkthrough",
      "Important Examples",
      "Common Misconceptions",
      "React Connection",
      "Performance and Memory Implications",
      "Debugging Techniques",
      "Interview Questions",
      "Exercises",
      "Chapter Summary",
      "Further Reading",
    ],
    quickRefresher:
      "An environment record associates identifier names with bindings. A binding has behavior and state; it is not necessarily an object property. [[OuterEnv]] points to the lexically enclosing environment record. Lookup stops at the first record containing the name. An uninitialized binding throws when read; lookup does not continue outward. Closures retain access to bindings from their creation environment, not an active call stack.",
    excerpt:
      "“JavaScript searches outer scopes” predicts simple examples, but senior interviews go further: Why can an inner let throw before its declaration instead of reading an outer variable? Why does top level let differ from top level var ? Why do imports update when an exporter changes?",
    readingMinutes: 9,
    searchText:
      "chapter 4 — lexical environments and environment records learning objectives after completing this chapter, you should be able to: explain bindings without describing scope as a javascript object; trace lexical lookup through [[outerenv]] links; distinguish binding creation from initialization; explain temporal dead zone, global bindings, and live imports; connect retained environments to closures and react renders. quick refresher an environment record associates identifier names with bindings. a binding has behavior and state; it is not necessarily an object property. [[outerenv]] points to the lexically enclosing environment record. lookup stops at the first record containing the name. an uninitialized binding throws when read; lookup does not continue outward. closures retain access to bindings from their creation environment, not an active call stack. why this matters “javascript searches outer scopes” predicts simple examples, but senior interviews go further: why can an inner let throw before its declaration instead of reading an outer variable? why does top level let differ from top level var ? why do imports update when an exporter changes? environment records provide one model for all of these behaviors. they also prevent two common mistakes: treating local variables as properties of an execution context and treating the temporal dead zone as a period in which a variable does not exist. core mental model think of an environment record as a specification level set of bindings plus an outer link : the record answers whether it contains a name and how that binding behaves; [[outerenv]] leads to the lexically enclosing record; different record types support different language constructs. a binding is more than a name/value pair. it can be mutable or immutable, initialized or uninitialized, deletable or non deletable, and direct or linked to an exported binding. environment records are not ordinary javascript objects. engines may keep values in registers, stack storage, or optimized internal structures as long as observable behavior matches the specification. visual model the current record owns bindings with distinct rules and states. its [[outerenv]] link connects it to the lexically enclosing record. an uninitialized binding already exists and therefore shadows the same name outside, even though reading it throws. formal model binding lifecycle the specification separates the important binding operations: 1. create the binding. 2. initialize it with a value. 3. read or update it according to its rules. 4. delete it only when that binding kind permits deletion. this separation explains the temporal dead zone. a lexical binding may already exist and shadow an outer name while still being uninitialized. before the block body runs, its own status binding is created. lookup finds that binding and stops. reading it before initialization throws; javascript does not fall back to the outer status . outer environment links when a name is needed, resolution starts in the current environment record: 1. if the record contains the name, use that binding. 2. otherwise, follow [[outerenv]] . 3. repeat until a binding is found or the chain reaches null . the chain follows where code was defined , not who called it. that distinction is the foundation of lexical scope and closures. chapter 5 develops the identifier resolution algorithm in detail. record types worth knowing record type main role interview relevant behavior declarative let , const , class , and other language bindings bindings are not object properties function one function invocation adds function specific state such as this where applicable module module local and imported bindings imports are indirect, live, and read only to the importer object exposes object properties as bindings used by mechanisms such as with global top level classic script bindings combines declarative and object backed records function and module records specialize declarative behavior. “one call creates one function environment” is a useful default model, although parameters, direct eval , and other edge cases can require additional environments. global bindings are split a classic script's global environment combines two sides: eligible top level var and function declarations use an object backed record and may become properties of globalthis ; top level let , const , and class use a declarative record and do not become global object properties. modules use module environment records instead. their top level declarations do not become properties of the global object. module imports are live bindings an import does not receive a one time copy. it indirectly refers to the exporter's binding, so an importer observes later updates made by the exporter. the importer cannot reassign the imported name. detailed module linking and evaluation belong to chapters 35–38. step by step runtime walkthrough the output is: trace the environments: 1. script evaluation creates the outer currency binding. 2. each call creates fresh function bindings for amount and precision . 3. currency is absent locally, so lookup follows the function's outer link. 4. on the zero path, entering the block creates a nested label binding. 5. returning finishes the active evaluations. an environment can still remain reachable if a closure needs one of its bindings. important examples per iteration bindings a for loop with a lexical declaration creates a distinct per iteration binding. each callback closes over a different binding. with var , the callbacks normally share one function or global scoped binding and produce [3, 3, 3] . const protects the binding, not the object the binding cannot be reassigned after initialization. that rule does not freeze the referenced object. closures observe bindings, not copied values the returned function retains access to the count binding. the completed call does not remain on the call stack. common misconceptions claim better explanation “scope is an object containing variables.” environment records are inaccessible specification mechanisms; only some records are object backed. “the tdz means the variable does not exist.” the binding exists but is uninitialized, so it shadows outer bindings and throws when read. “ const makes a value immutable.” it prevents reassignment of the binding; referenced objects may remain mutable. “a closure copies outer values.” a closure retains access to bindings from its creation environment. “top level declarations are properties of window .” top level lexical and module bindings are not global object properties. react connection every component render is a fresh function invocation with fresh bindings: handlereport retains access to the normalizedquery binding from the render in which that function was created. a later render creates a new binding and usually a new callback; it does not rewrite the environment captured by the old callback. this is the language basis of stale closure bugs and react's “state as a snapshot” explanation. ordinary locals cannot persist component state across renders. state and refs persist because react stores their data outside any one component invocation. performance and memory implications source level scopes do not map directly to heap allocations. engines can keep uncaptured values in registers or stack storage and materialize environments only when semantics, debugging, or deoptimization require them. a closure is not automatically a leak. retention becomes a problem when an unwanted long lived reference—often an event listener, subscription, timer, or cache—keeps the closure and its captured data reachable. when debugging memory, inspect the retaining path and ask: which function remains reachable? what keeps that function alive? which captured value accounts for the retained memory? does removing the listener, subscription, timer, or cache entry release it? debugging techniques compare scope and call stack pause inside a nested function in devtools. call stack shows dynamic callers; scope shows visible local, block, closure, script, and global bindings. the panels answer different questions and should not be treated as the same chain. diagnose a tdz error when you see “cannot access before initialization”: 1. find the nearest lexical declaration with the same name. 2. identify the environment that owns it. 3. check whether evaluation reads it before initialization. 4. for modules, also consider circular evaluation order. changing let to var changes semantics and may only hide the ordering defect. interview questions level 1 — fundamentals question: what is a lexical environment? model answer: javascript models identifier bindings with environment records connected through [[outerenv]] . a record knows which names it contains and how those bindings behave. resolution follows the lexical chain determined by where code was defined. these records are specification mechanisms, not ordinary javascript objects. level 2 — applied understanding question: why does a shadowed let throw before its declaration instead of reading the outer variable? model answer: block setup creates the inner binding before the statements run, but the declaration initializes it later. resolution finds the inner binding and stops. reading that uninitialized binding throws a referenceerror , so the outer name is never considered. level 3 — senior reasoning question: why do callbacks created by for (let ...) observe different indices? model answer: the loop creates a distinct binding for each iteration. each callback closes over its iteration's binding. with var , the callbacks normally share one function or global scoped binding and therefore observe its final value. level 4 — deep follow up question: is a top level binding always a property of globalthis ? model answer: no. in classic scripts, eligible var and function declarations use the object backed side of the global environment, while top level let , const , and class use its declarative side. modules use module environments. only the object backed case normally appears as a globalthis property. exercises 1. predict shadowing behavior solution the output is 2 and then 1 . the block owns a distinct binding; after the block finishes, lookup starts in the outer environment again. 2. explain the error solution the function's local enabled binding exists before body evaluation reaches its declaration, but it is uninitialized. it shadows the outer binding, so the read throws a referenceerror . 3. diagnose a react callback a callback created during an old render reads an old query after state changes. why can react not update the callback's existing environment? solution the function retains the environment from the render that created it. a later render creates new bindings; it cannot change which environment the existing function captured. the application must use the newer callback, correct dependencies, a functional update, or deliberately current mutable data through a ref. chapter summary environment records associate names with bindings and link outward through [[outerenv]] . lookup follows lexical nesting and stops at the first record containing the name. binding creation and initialization are separate, which explains temporal dead zone behavior. global lexical bindings, global object properties, and module bindings are not interchangeable. closures retain access to bindings; react callbacks therefore retain values from a particular render. interview ready explanation javascript models lexical scope with environment records. each record contains bindings and points to its lexically enclosing record through [[outerenv]] . lookup stops at the first record containing a name. because creation and initialization are separate, an inner lexical binding can shadow an outer name while still throwing in the temporal dead zone. closures keep access to bindings from the environment where they were created, which also explains why react callbacks can observe values from an older render. further reading ecma 262: environment records ecma 262: global environment records ecma 262: module environment records ecma 262: blockdeclarationinstantiation react: state as a snapshot chrome devtools: javascript debugging reference",
  },
  {
    number: 5,
    slug: "05-scope-and-identifier-resolution",
    title: "Scope and Identifier Resolution",
    kind: "chapter",
    partNumber: 1,
    partName: "Runtime Foundations",
    markdown:
      "# Chapter 5 — Scope and Identifier Resolution\n\n## Learning objectives\n\nAfter completing this chapter, you should be able to:\n\n- trace identifier resolution through lexical environments;\n- distinguish lexical scope from dynamic call order;\n- explain shadowing and unresolvable references;\n- distinguish binding lookup from object property lookup;\n- reason about `typeof`, modules, and accidental globals;\n- diagnose scope mistakes in React applications.\n\n## Quick Refresher\n\n- JavaScript is lexically scoped: where a function is defined determines its outer scope.\n- Resolution searches the current environment and then follows `[[OuterEnv]]` links.\n- The nearest matching binding wins.\n- A caller's local variables do not enter the callee's scope.\n- An unresolvable name and an uninitialized binding are different conditions.\n- In `obj.key`, only `obj` is resolved lexically; `key` is a property name.\n- Module bindings are isolated from global scope but shared by consumers of that module instance.\n\n## Why This Matters\n\nSenior scope questions test more than “inner code can access outer variables.” They combine callbacks, shadowing, modules, temporal dead zones, globals, and property access. The core skill is identifying which lookup mechanism applies.\n\nKeep three structures separate:\n\n| Structure                 | Answers                                                   |\n| ------------------------- | --------------------------------------------------------- |\n| Lexical environment chain | Which binding does this identifier mean?                  |\n| Call stack                | Which function is running, and which callers are waiting? |\n| Prototype chain           | Where can this object property be found?                  |\n\nConfusing these structures produces plausible but incorrect explanations.\n\n## Core Mental Model\n\nWhen JavaScript evaluates an identifier such as `timeout`:\n\n1. Check the current environment for a `timeout` binding.\n2. If found, stop and create an internal reference to that binding.\n3. Otherwise, follow `[[OuterEnv]]` and repeat.\n4. If the chain reaches `null`, the reference is unresolvable.\n\nFinding a binding and reading its value are separate operations. Resolution can therefore find an uninitialized binding and then throw when code tries to read it.\n\nThe outer chain comes from lexical nesting—where code was created—not the sequence of functions that called it.\n\n## Visual Model\n\n![Lexical identifier lookup stops at the nearest binding](/identifier-lookup.svg)\n\nThe lookup begins where `timeout` is used. Because the current block does not contain that name, resolution follows `[[OuterEnv]]`. It stops at the function binding, creates a reference to it, and never reaches the same name in module scope.\n\n## Formal Model\n\n### Resolve first, then read or write\n\nEvaluating an identifier uses specification machinery commonly summarized as `ResolveBinding(name)`. The search produces an internal **Reference Record** describing the binding or the fact that no binding was found. JavaScript code cannot inspect or store this record directly.\n\nThe next operation depends on context:\n\n- reading uses the reference to obtain a value;\n- assignment uses it as a destination;\n- a normal read through an unresolvable reference throws `ReferenceError`;\n- reading a resolved but uninitialized binding also throws `ReferenceError`.\n\nThese errors have different causes: one name was not found; the other was found but is not ready to be read.\n\n### Lexical scope is not dynamic scope\n\n```js\nconst mode = \"global\";\n\nfunction readMode() {\n  return mode;\n}\n\nfunction callWithLocalMode() {\n  const mode = \"local\";\n  return readMode();\n}\n\nconsole.log(callWithLocalMode()); // global\n```\n\n`callWithLocalMode` is the runtime caller, but its local `mode` is not part of `readMode`'s lexical chain. `readMode` searches the environment captured where it was defined and finds the global binding.\n\n### Shadowing stops the search\n\nAn inner declaration shadows an outer declaration with the same name. Resolution stops at the nearest environment containing the name, even if that binding is uninitialized.\n\n```js\nconst status = \"ready\";\n\n{\n  console.log(status); // ReferenceError\n  const status = \"pending\";\n}\n```\n\nThe block's `status` exists before its declaration initializes it. The outer `status` is never considered.\n\n### Binding lookup is not property lookup\n\n```js\nconst timeout = 1000;\nconst settings = { timeout: 5000 };\n\nconsole.log(timeout); // lexical binding: 1000\nconsole.log(settings.timeout); // object property: 5000\n```\n\nIn `settings.timeout`, JavaScript resolves only `settings` through lexical scope. It then looks up the `timeout` property on the resulting object, potentially using its prototype chain.\n\nDestructuring can connect the two mechanisms without making them identical:\n\n```js\nconst { timeout: requestTimeout } = settings;\n```\n\n`timeout` is the property key; `requestTimeout` is the new lexical binding.\n\n### The two important `typeof` cases\n\n```js\nconsole.log(typeof missingName); // 'undefined'\n\n{\n  console.log(typeof pendingValue); // ReferenceError\n  let pendingValue = 1;\n}\n```\n\n`typeof` has special behavior for an **unresolvable** name and returns `'undefined'`. It does not suppress the error for a **resolved but uninitialized** lexical binding.\n\n### Assignment to an undeclared name\n\nAssigning through an unresolvable reference throws in strict code. Legacy non-strict script semantics may instead create a global-object property:\n\n```js\nresult = 42;\n```\n\nModules are always strict, so this mistake throws. Depending on implicit global creation is unsafe and environment-sensitive; declare the intended binding.\n\n### Scope categories\n\n- **Block scope:** `let`, `const`, `class`, and applicable block functions.\n- **Function scope:** parameters, `var`, and function-body declarations.\n- **Script global scope:** top-level declarations stored through the global environment's declarative or object-backed side.\n- **Module scope:** module-local declarations and live import bindings; module code is strict.\n\nModule scope prevents global-name collisions, but it does not provide per-component or per-request state.\n\n## Step-by-Step Runtime Walkthrough\n\n```js\nconst currency = \"USD\";\n\nfunction formatOrder(order) {\n  const prefix = \"Order\";\n\n  function formatLine(item) {\n    const total = item.price * item.quantity;\n    return `${prefix} ${order.id}: ${currency} ${total}`;\n  }\n\n  return order.items.map(formatLine);\n}\n\nconsole.log(\n  formatOrder({\n    id: \"A-17\",\n    items: [{ price: 5, quantity: 2 }],\n  }),\n);\n```\n\nThe output is:\n\n```text\n[ 'Order A-17: USD 10' ]\n```\n\nResolve each name from inside `formatLine`:\n\n1. `item` and `total` are found in the current function environment.\n2. `prefix` and `order` are found in the enclosing `formatOrder` environment.\n3. `currency` is found in the surrounding script or module environment.\n4. `price`, `quantity`, `id`, and `items` are property names, not lexical searches.\n5. `map` calling `formatLine` does not insert its own internals into `formatLine`'s lexical chain.\n\n## Important Examples\n\n### Module scope is shared scope\n\n```js\n// request-id.js\nlet nextRequestId = 0;\n\nexport function createRequestId() {\n  nextRequestId += 1;\n  return `request-${nextRequestId}`;\n}\n```\n\nThe name is private to the module unless exported, but the binding is still shared by consumers of that evaluated module instance. In React it can be shared across components; in server code it can outlive a request. Name isolation and lifecycle isolation are different properties.\n\n## Common Misconceptions\n\n| Claim                                              | Better explanation                                                            |\n| -------------------------------------------------- | ----------------------------------------------------------------------------- |\n| “JavaScript checks the caller's variables.”        | Lookup follows lexical environments captured where the function was created.  |\n| “An unusable inner binding falls through outward.” | A matching uninitialized binding stops lookup and throws when read.           |\n| “`obj.key` resolves `key` through scope.”          | Only `obj` is resolved lexically; `key` is looked up as a property.           |\n| “`typeof` never throws for missing variables.”     | It handles unresolvable names specially but still throws for TDZ bindings.    |\n| “Module scope gives every importer a copy.”        | Importers normally share one evaluated module instance and its live bindings. |\n\n## React Connection\n\nReact code commonly uses three lexical layers:\n\n1. **Module scope:** imports, constants, helpers, and deliberately shared state.\n2. **Render scope:** props, state snapshots, derived values, and callbacks created during one component invocation.\n3. **Callback invocation scope:** event parameters and updater-function parameters created later.\n\n```jsx\nconst defaultLimit = 20;\n\nfunction Results({ limit = defaultLimit }) {\n  const [page, setPage] = React.useState(1);\n\n  function handleNextPage() {\n    setPage((currentPage) => currentPage + 1);\n  }\n\n  return (\n    \u003cbutton onClick={handleNextPage}>\n      Page {page}, limit {limit}\n    \u003c/button>\n  );\n}\n```\n\n`defaultLimit` comes from module scope. `limit`, `page`, and `handleNextPage` belong to this render. `currentPage` is created when React later invokes the updater.\n\nThe functional updater does not make `page` dynamically scoped. It avoids reading the captured `page` binding by accepting the latest queued state as an argument.\n\nMutable module bindings bypass React's state model, are shared across component instances, and do not trigger rendering. Use them only when that shared lifetime is intentional.\n\n## Practical Implications\n\nAt a breakpoint, **Call Stack** shows how execution arrived at the current function. **Scope** shows which bindings are visible from the selected frame. A caller's local variable can appear in another frame without belonging to the current function's lexical chain.\n\nWhen a name resolves unexpectedly:\n\n1. Find every declaration with the same name.\n2. Start from the use site and identify the nearest environment.\n3. Include parameters, imports, catch bindings, and destructuring aliases.\n4. Distinguish an absent binding from an uninitialized one.\n\nReproduce top-level bugs in the correct environment. Browser console snippets, classic scripts, ES modules, CommonJS, and bundler wrappers can have different global behavior.\n\nEngines statically analyze and optimize lexical access, so scope depth is not a useful microbenchmark. Prefer clear structure, avoid `with` and direct `eval`, and investigate retained closures or module state as lifetime issues rather than lookup-speed issues.\n\n## Interview Questions\n\n### Level 1 — Fundamentals\n\n**Question:** How does JavaScript resolve an identifier?\n\n**Model answer:** It checks the current environment record and follows lexical `[[OuterEnv]]` links until it finds the name or reaches the end. The chain is determined by where code was defined, not who called it. Resolution creates an internal reference, which is then used to read or write the binding.\n\n### Level 2 — Applied understanding\n\n**Question:** Why can shadowing cause a temporal-dead-zone error?\n\n**Model answer:** A lexical binding is created before its declaration initializes it. Resolution finds that nearer binding and stops, so an outer binding with the same name is not considered. Reading the uninitialized binding throws `ReferenceError`.\n\n### Level 3 — Senior reasoning\n\n**Question:** Why is mutable module state risky in React or server rendering?\n\n**Model answer:** Module scope isolates names from the global environment but normally shares one evaluated module's bindings among its consumers. State can therefore cross component instances or server requests, and mutation bypasses React's update mechanism. The correct storage depends on the intended lifecycle.\n\n### Level 4 — Deep follow-up\n\n**Question:** Why does `typeof missingName` return `'undefined'`, while `typeof value` can throw before `let value`?\n\n**Model answer:** `missingName` is unresolvable, and `typeof` has a special case for that result. A TDZ name resolves successfully to an existing but uninitialized binding. Reading that binding still throws.\n\n## Exercises\n\n### 1. Trace lexical lookup\n\n```js\nconst value = \"module\";\n\nfunction outer() {\n  const value = \"outer\";\n  return function inner() {\n    return value;\n  };\n}\n\nconsole.log(outer()());\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe output is `outer`. `inner` follows its lexical outer link to the retained `outer` invocation environment and finds that binding before module scope.\n\n\u003c/details>\n\n### 2. Distinguish binding and property\n\n```js\nconst timeout = 1000;\nconst options = { timeout: 5000 };\n\nconsole.log(options.timeout);\nconsole.log(timeout);\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe output is `5000` and `1000`. The first expression resolves `options` and then performs property lookup. The second resolves the lexical `timeout` binding.\n\n\u003c/details>\n\n### 3. Predict `typeof`\n\n```js\nconsole.log(typeof notDeclared);\n\ntry {\n  console.log(typeof later);\n  let later = 1;\n} catch (error) {\n  console.log(error.name);\n}\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe output is `undefined` followed by `ReferenceError`. `notDeclared` is unresolvable; `later` is resolved but uninitialized.\n\n\u003c/details>\n\n## Chapter Summary\n\n- Identifier resolution follows lexical environment links, not dynamic callers.\n- The nearest matching binding stops the search.\n- Unresolvable and resolved-but-uninitialized references fail for different reasons.\n- Identifier lookup and object property lookup are separate mechanisms.\n- Module scope isolates names but can still share mutable state across consumers.\n- React module, render, and callback scopes have different lifetimes.\n\n### Interview-ready explanation\n\nJavaScript is lexically scoped. Resolving an identifier starts in the current environment and follows `[[OuterEnv]]` links determined by where the code was defined. The nearest matching binding wins, even if it is still uninitialized. An unresolvable name normally throws, although `typeof` handles that case specially. Property access is separate: in `obj.key`, only `obj` is resolved lexically. In React, this model separates shared module bindings, per-render bindings, and parameters created when callbacks run later.\n\n## Further Reading\n\n- [ECMA-262: ResolveBinding](https://tc39.es/ecma262/#sec-resolvebinding)\n- [ECMA-262: GetIdentifierReference](https://tc39.es/ecma262/#sec-getidentifierreference)\n- [ECMA-262: Reference Record Specification Type](https://tc39.es/ecma262/#sec-reference-record-specification-type)\n- [ECMA-262: GetValue](https://tc39.es/ecma262/#sec-getvalue)\n- [ECMA-262: PutValue](https://tc39.es/ecma262/#sec-putvalue)\n- [ECMA-262: Strict Mode](https://tc39.es/ecma262/#sec-strict-mode-of-ecmascript)\n- [React: State as a Snapshot](https://react.dev/learn/state-as-a-snapshot)\n- [Chrome DevTools: JavaScript Debugging Reference](https://developer.chrome.com/docs/devtools/javascript/reference)",
    headings: [
      "Learning objectives",
      "Quick Refresher",
      "Why This Matters",
      "Core Mental Model",
      "Visual Model",
      "Formal Model",
      "Step-by-Step Runtime Walkthrough",
      "Important Examples",
      "Common Misconceptions",
      "React Connection",
      "Practical Implications",
      "Interview Questions",
      "Exercises",
      "Chapter Summary",
      "Further Reading",
    ],
    quickRefresher:
      "JavaScript is lexically scoped: where a function is defined determines its outer scope. Resolution searches the current environment and then follows [[OuterEnv]] links. The nearest matching binding wins. A caller's local variables do not enter the callee's scope. An unresolvable name and an uninitialized binding are different conditions. In obj.key , only obj is resolved lexically; key is a property name. Module bindings are isolated from global scope but shared by consumers of that module instance.",
    excerpt:
      "Senior scope questions test more than “inner code can access outer variables.” They combine callbacks, shadowing, modules, temporal dead zones, globals, and property access. The core skill is identifying which lookup mechanism applies.",
    readingMinutes: 8,
    searchText:
      "chapter 5 — scope and identifier resolution learning objectives after completing this chapter, you should be able to: trace identifier resolution through lexical environments; distinguish lexical scope from dynamic call order; explain shadowing and unresolvable references; distinguish binding lookup from object property lookup; reason about typeof , modules, and accidental globals; diagnose scope mistakes in react applications. quick refresher javascript is lexically scoped: where a function is defined determines its outer scope. resolution searches the current environment and then follows [[outerenv]] links. the nearest matching binding wins. a caller's local variables do not enter the callee's scope. an unresolvable name and an uninitialized binding are different conditions. in obj.key , only obj is resolved lexically; key is a property name. module bindings are isolated from global scope but shared by consumers of that module instance. why this matters senior scope questions test more than “inner code can access outer variables.” they combine callbacks, shadowing, modules, temporal dead zones, globals, and property access. the core skill is identifying which lookup mechanism applies. keep three structures separate: structure answers lexical environment chain which binding does this identifier mean? call stack which function is running, and which callers are waiting? prototype chain where can this object property be found? confusing these structures produces plausible but incorrect explanations. core mental model when javascript evaluates an identifier such as timeout : 1. check the current environment for a timeout binding. 2. if found, stop and create an internal reference to that binding. 3. otherwise, follow [[outerenv]] and repeat. 4. if the chain reaches null , the reference is unresolvable. finding a binding and reading its value are separate operations. resolution can therefore find an uninitialized binding and then throw when code tries to read it. the outer chain comes from lexical nesting—where code was created—not the sequence of functions that called it. visual model the lookup begins where timeout is used. because the current block does not contain that name, resolution follows [[outerenv]] . it stops at the function binding, creates a reference to it, and never reaches the same name in module scope. formal model resolve first, then read or write evaluating an identifier uses specification machinery commonly summarized as resolvebinding(name) . the search produces an internal reference record describing the binding or the fact that no binding was found. javascript code cannot inspect or store this record directly. the next operation depends on context: reading uses the reference to obtain a value; assignment uses it as a destination; a normal read through an unresolvable reference throws referenceerror ; reading a resolved but uninitialized binding also throws referenceerror . these errors have different causes: one name was not found; the other was found but is not ready to be read. lexical scope is not dynamic scope callwithlocalmode is the runtime caller, but its local mode is not part of readmode 's lexical chain. readmode searches the environment captured where it was defined and finds the global binding. shadowing stops the search an inner declaration shadows an outer declaration with the same name. resolution stops at the nearest environment containing the name, even if that binding is uninitialized. the block's status exists before its declaration initializes it. the outer status is never considered. binding lookup is not property lookup in settings.timeout , javascript resolves only settings through lexical scope. it then looks up the timeout property on the resulting object, potentially using its prototype chain. destructuring can connect the two mechanisms without making them identical: timeout is the property key; requesttimeout is the new lexical binding. the two important typeof cases typeof has special behavior for an unresolvable name and returns 'undefined' . it does not suppress the error for a resolved but uninitialized lexical binding. assignment to an undeclared name assigning through an unresolvable reference throws in strict code. legacy non strict script semantics may instead create a global object property: modules are always strict, so this mistake throws. depending on implicit global creation is unsafe and environment sensitive; declare the intended binding. scope categories block scope: let , const , class , and applicable block functions. function scope: parameters, var , and function body declarations. script global scope: top level declarations stored through the global environment's declarative or object backed side. module scope: module local declarations and live import bindings; module code is strict. module scope prevents global name collisions, but it does not provide per component or per request state. step by step runtime walkthrough the output is: resolve each name from inside formatline : 1. item and total are found in the current function environment. 2. prefix and order are found in the enclosing formatorder environment. 3. currency is found in the surrounding script or module environment. 4. price , quantity , id , and items are property names, not lexical searches. 5. map calling formatline does not insert its own internals into formatline 's lexical chain. important examples module scope is shared scope the name is private to the module unless exported, but the binding is still shared by consumers of that evaluated module instance. in react it can be shared across components; in server code it can outlive a request. name isolation and lifecycle isolation are different properties. common misconceptions claim better explanation “javascript checks the caller's variables.” lookup follows lexical environments captured where the function was created. “an unusable inner binding falls through outward.” a matching uninitialized binding stops lookup and throws when read. “ obj.key resolves key through scope.” only obj is resolved lexically; key is looked up as a property. “ typeof never throws for missing variables.” it handles unresolvable names specially but still throws for tdz bindings. “module scope gives every importer a copy.” importers normally share one evaluated module instance and its live bindings. react connection react code commonly uses three lexical layers: 1. module scope: imports, constants, helpers, and deliberately shared state. 2. render scope: props, state snapshots, derived values, and callbacks created during one component invocation. 3. callback invocation scope: event parameters and updater function parameters created later. defaultlimit comes from module scope. limit , page , and handlenextpage belong to this render. currentpage is created when react later invokes the updater. the functional updater does not make page dynamically scoped. it avoids reading the captured page binding by accepting the latest queued state as an argument. mutable module bindings bypass react's state model, are shared across component instances, and do not trigger rendering. use them only when that shared lifetime is intentional. practical implications at a breakpoint, call stack shows how execution arrived at the current function. scope shows which bindings are visible from the selected frame. a caller's local variable can appear in another frame without belonging to the current function's lexical chain. when a name resolves unexpectedly: 1. find every declaration with the same name. 2. start from the use site and identify the nearest environment. 3. include parameters, imports, catch bindings, and destructuring aliases. 4. distinguish an absent binding from an uninitialized one. reproduce top level bugs in the correct environment. browser console snippets, classic scripts, es modules, commonjs, and bundler wrappers can have different global behavior. engines statically analyze and optimize lexical access, so scope depth is not a useful microbenchmark. prefer clear structure, avoid with and direct eval , and investigate retained closures or module state as lifetime issues rather than lookup speed issues. interview questions level 1 — fundamentals question: how does javascript resolve an identifier? model answer: it checks the current environment record and follows lexical [[outerenv]] links until it finds the name or reaches the end. the chain is determined by where code was defined, not who called it. resolution creates an internal reference, which is then used to read or write the binding. level 2 — applied understanding question: why can shadowing cause a temporal dead zone error? model answer: a lexical binding is created before its declaration initializes it. resolution finds that nearer binding and stops, so an outer binding with the same name is not considered. reading the uninitialized binding throws referenceerror . level 3 — senior reasoning question: why is mutable module state risky in react or server rendering? model answer: module scope isolates names from the global environment but normally shares one evaluated module's bindings among its consumers. state can therefore cross component instances or server requests, and mutation bypasses react's update mechanism. the correct storage depends on the intended lifecycle. level 4 — deep follow up question: why does typeof missingname return 'undefined' , while typeof value can throw before let value ? model answer: missingname is unresolvable, and typeof has a special case for that result. a tdz name resolves successfully to an existing but uninitialized binding. reading that binding still throws. exercises 1. trace lexical lookup solution the output is outer . inner follows its lexical outer link to the retained outer invocation environment and finds that binding before module scope. 2. distinguish binding and property solution the output is 5000 and 1000 . the first expression resolves options and then performs property lookup. the second resolves the lexical timeout binding. 3. predict typeof solution the output is undefined followed by referenceerror . notdeclared is unresolvable; later is resolved but uninitialized. chapter summary identifier resolution follows lexical environment links, not dynamic callers. the nearest matching binding stops the search. unresolvable and resolved but uninitialized references fail for different reasons. identifier lookup and object property lookup are separate mechanisms. module scope isolates names but can still share mutable state across consumers. react module, render, and callback scopes have different lifetimes. interview ready explanation javascript is lexically scoped. resolving an identifier starts in the current environment and follows [[outerenv]] links determined by where the code was defined. the nearest matching binding wins, even if it is still uninitialized. an unresolvable name normally throws, although typeof handles that case specially. property access is separate: in obj.key , only obj is resolved lexically. in react, this model separates shared module bindings, per render bindings, and parameters created when callbacks run later. further reading ecma 262: resolvebinding ecma 262: getidentifierreference ecma 262: reference record specification type ecma 262: getvalue ecma 262: putvalue ecma 262: strict mode react: state as a snapshot chrome devtools: javascript debugging reference",
  },
  {
    number: 6,
    slug: "06-closures",
    title: "Closures",
    kind: "chapter",
    partNumber: 1,
    partName: "Runtime Foundations",
    markdown:
      '# Chapter 6 — Closures\n\n## Learning objectives\n\nAfter completing this chapter, you should be able to:\n\n- define a closure using lexical environments rather than vague “remembering” language;\n- explain why an outer function can return while captured bindings remain reachable;\n- distinguish capturing a binding from copying its current value;\n- predict shared and independent closure state;\n- explain the difference between `var` and `let` in loop-created callbacks;\n- identify closure-related memory retention and clean up long-lived callbacks;\n- diagnose stale closures in React without treating every closure as a bug;\n\n## Quick Refresher\n\n- A closure is a function together with access to the lexical environment where that function was created.\n- JavaScript functions close over bindings, not frozen copies of primitive values.\n- An outer call may finish while its environment remains reachable through a returned callback.\n- Closures created by the same outer invocation can share bindings; separate invocations normally create separate bindings.\n- `let` in a `for` loop creates a fresh per-iteration binding when required by the loop semantics; `var` uses one function-scoped binding.\n- A closure retains only what remains reachable through its environment graph; engines may optimize unobservable details.\n- A retained callback can keep DOM nodes, caches, responses, or other large object graphs alive.\n- In React, every render creates new bindings. A callback created during one render observes that render\'s state snapshot.\n- A stale closure is usually a synchronization or lifecycle mistake, not evidence that closures themselves are unreliable.\n\n## Why This Matters\n\nClosures power callbacks, event handlers, promises, memoization, factory functions, module APIs, and nearly every React component. They are also a frequent source of weak interview answers. Saying “the inner function remembers the outer variables” predicts some behavior, but it does not explain mutation, shared state, loop bindings, stale React callbacks, or memory retention.\n\nSenior engineers need a model that answers two production questions:\n\n1. **Which binding will this callback read when it eventually runs?**\n2. **What remains reachable for as long as this callback remains reachable?**\n\nThe first question is about correctness. The second is about lifetime and memory.\n\n## Core Mental Model\n\nWhen JavaScript creates an ordinary function, the function object receives an internal `[[Environment]]` slot referring to the lexical environment active at creation time. When the function is called later, the new function environment links outward to that saved environment.\n\n```js\nfunction createCounter() {\n  let count = 0;\n\n  return function increment() {\n    count += 1;\n    return count;\n  };\n}\n\nconst next = createCounter();\n\nconsole.log(next()); // 1\nconsole.log(next()); // 2\n```\n\nThe `createCounter` execution context is removed from the call stack after it returns. That does not make its bindings unreachable. The returned `increment` function still refers to the environment containing `count`, so that environment remains available.\n\nThree structures must remain separate:\n\n- the **call stack** records currently active calls;\n- the **lexical environment chain** determines identifier resolution;\n- the **reachability graph** determines which objects and environments can be reclaimed.\n\nA call can disappear from the stack while its environment remains reachable.\n\n## Visual Model\n\n![Closure retaining a binding after its outer call returns](/closure-lifetime.svg)\n\n`createCounter` creates the `count` binding and the `increment` function. After the outer call returns, its active frame is gone, but the returned function remains reachable through `next`. Its saved `[[Environment]]` path therefore keeps the same `count` binding available for later calls.\n\n## Formal Model\n\n### Function creation captures an environment\n\nThe specification does not define a user-visible `Closure` object. An ordinary function stores its creation environment in `[[Environment]]`. A later call uses that saved environment as its lexical outer environment, regardless of who calls the function.\n\n“Closure” is therefore a programming-language description of behavior produced by functions, environment records, and lexical lookup—not an object that application code can inspect.\n\n### Bindings are captured, not value snapshots\n\nConsider this factory:\n\n```js\nfunction createScoreboard() {\n  let score = 0;\n\n  return {\n    read() {\n      return score;\n    },\n    add(points) {\n      score += points;\n    },\n  };\n}\n\nconst board = createScoreboard();\nboard.add(10);\nconsole.log(board.read()); // 10\n```\n\nBoth methods resolve `score` to the same mutable binding from one `createScoreboard` invocation. `read` did not capture the number `0`; it retained a lookup path to the binding whose value later became `10`.\n\nYou can deliberately capture a snapshot by creating another binding:\n\n```js\nlet status = "pending";\nconst snapshot = status;\nconst readSnapshot = () => snapshot;\nconst readCurrent = () => status;\n\nstatus = "complete";\n\nconsole.log(readSnapshot()); // pending\nconsole.log(readCurrent()); // complete\n```\n\nThe distinction is not “closure versus no closure.” Both functions are closures. They close over different bindings.\n\n### Lifetime follows reachability\n\nReturning removes a call from the active stack. Garbage collection instead follows reachability: if a reachable function still needs an outer binding, the required state must remain available.\n\nThis is a semantic rule, not a required heap layout. Engines may omit unused bindings or represent captured state differently. Reason about binding identity and reachability, not a literal scope object containing every local.\n\n### Shared versus independent environments\n\nClosures created during one factory invocation can share its bindings. A second invocation normally creates a new environment with independent bindings. Calling the same returned closure repeatedly therefore updates its existing state; calling another factory result updates different state.\n\nUse one reliable interview rule: determine which invocation created the binding, then determine which closures can still reach it.\n\n## Step-by-Step Runtime Walkthrough\n\nPredict the output:\n\n```js\nfunction createQueue(name) {\n  const items = [];\n\n  return {\n    enqueue(item) {\n      items.push(item);\n      console.log(`${name}: added ${item}`);\n    },\n    flush() {\n      const snapshot = [...items];\n      items.length = 0;\n      return snapshot;\n    },\n  };\n}\n\nconst urgent = createQueue("urgent");\nconst normal = createQueue("normal");\n\nurgent.enqueue("fix-login");\nnormal.enqueue("update-copy");\nurgent.enqueue("restore-cache");\n\nconsole.log(urgent.flush());\nconsole.log(normal.flush());\n```\n\nExpected output:\n\n```text\nurgent: added fix-login\nnormal: added update-copy\nurgent: added restore-cache\n[ \'fix-login\', \'restore-cache\' ]\n[ \'update-copy\' ]\n```\n\nTrace it precisely:\n\n1. The first `createQueue` invocation creates bindings for `name` and `items`.\n2. Its returned methods retain that invocation\'s environment.\n3. The second invocation creates a separate `name` binding and a separate array referenced by its `items` binding.\n4. Calling `urgent.enqueue` creates a new `enqueue` call environment. `items` and `name` are not local there, so lookup follows the saved outer environment.\n5. Mutating the array does not replace the `items` binding; it changes the referenced object.\n6. `flush` copies the current array elements, then mutates the shared array\'s `length`.\n7. The two queue objects never share their captured arrays because they came from different factory invocations.\n\n## Important Examples\n\n### Encapsulated mutable state\n\n```js\nexport function createRequestDeduper() {\n  const inFlight = new Map();\n\n  return function requestOnce(key, load) {\n    if (inFlight.has(key)) {\n      return inFlight.get(key);\n    }\n\n    const request = Promise.resolve()\n      .then(load)\n      .finally(() => {\n        inFlight.delete(key);\n      });\n\n    inFlight.set(key, request);\n    return request;\n  };\n}\n```\n\nThe returned function provides controlled access to `inFlight` without exposing the map directly. The `finally` cleanup is essential: without it, settled promises and their keys remain reachable for as long as the deduper remains reachable.\n\nThis pattern is appropriate when the desired lifetime is the lifetime of one created deduper. A module-level map would instead be shared by every consumer of that module instance. A React component-local closure would be recreated with renders unless deliberately stabilized.\n\n### Interview edge case: `var` and per-iteration `let`\n\n```js\nconst withVar = [];\n\nfor (var i = 0; i \u003c 3; i += 1) {\n  withVar.push(() => i);\n}\n\nconsole.log(withVar.map((read) => read())); // [3, 3, 3]\n```\n\n`var i` creates one function- or global-scoped binding. Every callback closes over that binding. By the time they run, its value is `3`.\n\nNow compare `let`:\n\n```js\nconst withLet = [];\n\nfor (let i = 0; i \u003c 3; i += 1) {\n  withLet.push(() => i);\n}\n\nconsole.log(withLet.map((read) => read())); // [0, 1, 2]\n```\n\nFor a `for` loop with a lexical declaration, the specification creates per-iteration environments. Each callback closes over a different `i` binding.\n\nThe important difference is binding identity, not asynchronous timing or arrow-function syntax.\n\n## Common Misconceptions\n\n| Claim                                                | Better explanation                                                                                                |\n| ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |\n| “A closure exists only when a function is returned.” | Event listeners, promise handlers, and immediately invoked nested functions also use their creation environments. |\n| “Closures capture values.”                           | Functions retain access to bindings; later reads observe the value currently held by that binding.                |\n| “The outer call stays on the stack.”                 | The call finishes; required bindings can remain reachable independently.                                          |\n| “Every local is retained forever.”                   | Only reachable, semantically required state matters, and engines can optimize its representation.                 |\n| “Closures are memory leaks.”                         | A leak requires unintended retention beyond the desired lifetime.                                                 |\n| “`let` changes when callbacks run.”                  | It changes binding identity in the loop, not scheduling.                                                          |\n| “React hooks cause stale closures.”                  | Ordinary closures expose a lifecycle mismatch when old callbacks remain connected.                                |\n\n## React Connection\n\nEach React render calls the component again and creates a new lexical environment. Props, state variables, derived values, and inline callbacks belong to that render.\n\n```jsx\nfunction SearchStatus({ query }) {\n  const [count, setCount] = useState(0);\n\n  function handleClick() {\n    setTimeout(() => {\n      console.log({ query, count });\n    }, 1000);\n  }\n\n  return \u003cbutton onClick={handleClick}>Log current render\u003c/button>;\n}\n```\n\nWhen the user clicks, `handleClick` and the timer callback belong to that render. Updating state before the timer fires does not rewrite the captured `count` binding into a newer render\'s binding. The callback reports the snapshot associated with the click.\n\nThat behavior is often desirable: an event should retain the values relevant when it occurred. It becomes a stale-closure bug when long-lived logic is expected to synchronize with current reactive values but remains connected to an older render.\n\n### Effect dependencies\n\n```jsx\nfunction Chat({ roomId }) {\n  useEffect(() => {\n    const connection = createConnection(roomId);\n    connection.connect();\n\n    return () => connection.disconnect();\n  }, [roomId]);\n}\n```\n\nThe effect setup closes over one render\'s `roomId`. Including `roomId` in the dependency list tells React to clean up the previous synchronization and run a new setup when that reactive value changes.\n\nRemoving a dependency merely to silence the linter does not make the callback current. Strong fixes change the design:\n\n- include reactive values that genuinely control synchronization;\n- use a functional state update when the next state depends only on previous state;\n- move non-reactive logic outside an effect when it does not belong there;\n- use a ref when mutable current data must not trigger a render;\n- separate event-specific logic from synchronization logic.\n\nThe closure is behaving correctly; stale behavior indicates that the surrounding lifecycle or synchronization contract is wrong.\n\n## Memory and Debugging\n\nCreating small closures is normal. The more important question is whether a long-lived callback retains data beyond its intended lifetime:\n\n```js\nfunction mountPanel(button, model) {\n  function handleClick() {\n    renderDetails(model.selectedItem);\n  }\n\n  button.addEventListener("click", handleClick);\n\n  return () => {\n    button.removeEventListener("click", handleClick);\n  };\n}\n```\n\nThe likely path is: event target → `handleClick` → saved environment → `model` → its object graph. The cleanup removes the listener at the root of that path.\n\nCommon long-lived roots include:\n\n- event targets with registered listeners;\n- timers and intervals that have not been cancelled;\n- subscriptions and observer callbacks;\n- pending asynchronous operations;\n- caches and module-level collections;\n- framework roots holding mounted component state.\n\nThe existence of a closure does not prove which path retains an object. Confirm the actual path with memory tooling.\n\nFor incorrect values, inspect the callback\'s creation site: the invocation site explains when it ran, while the creation site explains which environment it retained. Ask separately:\n\n1. Is this the same callback function as before?\n2. Does it resolve the name to the same binding as before?\n\nRecreating a callback changes its identity and may capture new bindings. Mutating a shared binding instead changes what existing closures observe.\n\nFor suspected retention:\n\n1. create and then remove the UI or data expected to be released;\n2. force garbage collection when the tool supports it;\n3. compare heap snapshots;\n4. inspect retaining paths for unexpected listeners, timers, collections, or closures;\n5. fix the lifecycle root rather than deleting unrelated locals.\n\n## Interview Questions\n\n### Level 1 — Fundamentals\n\n**Question:** What is a closure?\n\n**Model answer:** A closure is a function with access to the lexical environment where it was created. In specification terms, an ordinary function stores that environment in its internal `[[Environment]]` slot. Later calls resolve outer identifiers through that saved environment, even if the creating function has already returned.\n\n### Level 2 — Applied understanding\n\n**Question:** Why do callbacks created with `var` in a loop often all log the final value, while callbacks created with `let` log different values?\n\n**Model answer:** `var` gives the callbacks one shared function-scoped binding. They run after the loop has updated that binding to its final value. A `for` loop with `let` creates per-iteration lexical bindings, so each callback closes over a different binding. The difference is binding identity, not asynchronous execution itself.\n\n### Level 3 — Senior reasoning\n\n**Question:** When does a closure cause a memory leak?\n\n**Model answer:** A closure is not inherently a leak. It becomes part of a leak when a long-lived root—such as an event listener, timer, subscription, or cache—retains the closure beyond its intended lifecycle, and the closure\'s environment keeps otherwise obsolete objects reachable. I confirm this with heap snapshots and retaining paths, then remove the root through cleanup or bounded storage.\n\n### Level 4 — Deep follow-up\n\n**Question:** Does a closure store a copy of every local variable from its outer function?\n\n**Model answer:** That is not a valid observable guarantee. Semantically, the function retains access through its saved lexical environment to bindings required by later identifier resolution. Engines can optimize storage, omit unused bindings, or represent captured state differently as long as behavior is preserved. I reason about binding identity and reachability, not a literal copied scope object.\n\n## Exercises\n\n### 1. Compare shared and independent state\n\n```js\nfunction pair() {\n  let value = 0;\n  return [() => ++value, () => --value];\n}\n\nconst [up, down] = pair();\nconsole.log(up(), up(), down());\n\nfunction counter() {\n  let value = 0;\n  return () => ++value;\n}\n\nconst a = counter();\nconst b = counter();\n\nconsole.log(a(), a(), b());\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nBoth lines output `1 2 1`, for different reasons. `up` and `down` share the binding created by one `pair` call. `a` and `b` use bindings created by separate `counter` calls.\n\n\u003c/details>\n\n### 2. Fix the loop\n\nExplain and fix this code without changing when the callbacks execute:\n\n```js\nconst readers = [];\n\nfor (var index = 0; index \u003c 3; index += 1) {\n  readers.push(() => index);\n}\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nUse `let index`. The loop then creates per-iteration bindings, producing `0`, `1`, and `2`. An extra factory invocation receiving `index` as a parameter also works because each call creates a new parameter binding.\n\n\u003c/details>\n\n### 3. Find the retention path\n\nA removed modal remains in a heap snapshot. Its close button registered a callback that references the modal\'s model. List the likely retention path and the cleanup you would verify.\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nA likely path is an active event target or application registry → listener function → saved lexical environment → model → modal data. Verify that unmounting removes the listener and that no subscription, timer, or registry still stores the callback. Use the actual retaining path in memory tools rather than assuming the closure is the root.\n\n\u003c/details>\n\n## Chapter Summary\n\n- **Essential model:** a function retains access to its creation environment through lexical environment links.\n- **Binding rule:** closures capture access to bindings, not automatic frozen value copies.\n- **Lifetime rule:** leaving the call stack does not imply that captured state is unreachable.\n- **Identity rule:** closures from one invocation may share bindings; different invocations normally create independent bindings.\n- **Loop rule:** `var` commonly shares one binding, while `let` can create per-iteration bindings.\n- **Memory rule:** leaks come from unintended long-lived reachability paths, not from closure syntax itself.\n- **React rule:** callbacks belong to render snapshots; stale behavior indicates a lifecycle or synchronization mismatch.\n\n### Interview-ready explanation\n\nA closure is a function that retains access to the lexical environment where it was created. Ordinary functions save that environment internally, so later identifier resolution follows the saved outer chain even after the creating call has returned. The function closes over bindings rather than frozen value copies, which explains shared mutable state and why separate factory invocations produce independent state. In loops, `var` callbacks share one binding, while `let` can create a binding per iteration. Closures affect memory only through reachability: a long-lived listener or timer can retain an environment and the object graph reachable from it. In React, each render creates new bindings, so a callback observes the state snapshot from the render that created it unless the design explicitly reconnects it to current data.\n\n## Further Reading\n\n- [ECMA-262: Lexical Environments](https://tc39.es/ecma262/multipage/executable-code-and-execution-contexts.html#sec-lexical-environments)\n- [ECMA-262: Ordinary Function Objects](https://tc39.es/ecma262/multipage/ecmascript-language-functions-and-classes.html#sec-ordinary-function-objects)\n- [ECMA-262: OrdinaryFunctionCreate](https://tc39.es/ecma262/multipage/ecmascript-language-functions-and-classes.html#sec-ordinaryfunctioncreate)\n- [ECMA-262: CreatePerIterationEnvironment](https://tc39.es/ecma262/multipage/ecmascript-language-statements-and-declarations.html#sec-createperiterationenvironment)\n- [React: State as a Snapshot](https://react.dev/learn/state-as-a-snapshot)\n- [React: Removing Effect Dependencies](https://react.dev/learn/removing-effect-dependencies)\n- [Chrome DevTools: Fix Memory Problems](https://developer.chrome.com/docs/devtools/memory-problems/)',
    headings: [
      "Learning objectives",
      "Quick Refresher",
      "Why This Matters",
      "Core Mental Model",
      "Visual Model",
      "Formal Model",
      "Step-by-Step Runtime Walkthrough",
      "Important Examples",
      "Common Misconceptions",
      "React Connection",
      "Memory and Debugging",
      "Interview Questions",
      "Exercises",
      "Chapter Summary",
      "Further Reading",
    ],
    quickRefresher:
      "A closure is a function together with access to the lexical environment where that function was created. JavaScript functions close over bindings, not frozen copies of primitive values. An outer call may finish while its environment remains reachable through a returned callback. Closures created by the same outer invocation can share bindings; separate invocations normally create separate bindings. let in a for loop creates a fresh per iteration binding when required by the loop semantics; var uses one function scoped binding. A closure retains only what remains reachable through its environment graph; engines may optimize unobservable details. A retained callback can keep DOM nodes, caches, responses, or other large object graphs alive. In React, every render creates new bindings. A callback created during one render observes that render's state snapshot. A stale closure is usually a synchronization or lifecycle mistake, not evidence that closures themselves are unreliable.",
    excerpt:
      "Closures power callbacks, event handlers, promises, memoization, factory functions, module APIs, and nearly every React component. They are also a frequent source of weak interview answers. Saying “the inner function remembers the outer variables” predicts some behavior, but it does not explain mutation, shared state, loop bindings, stale React callbacks, or memory retention.",
    readingMinutes: 11,
    searchText:
      "chapter 6 — closures learning objectives after completing this chapter, you should be able to: define a closure using lexical environments rather than vague “remembering” language; explain why an outer function can return while captured bindings remain reachable; distinguish capturing a binding from copying its current value; predict shared and independent closure state; explain the difference between var and let in loop created callbacks; identify closure related memory retention and clean up long lived callbacks; diagnose stale closures in react without treating every closure as a bug; quick refresher a closure is a function together with access to the lexical environment where that function was created. javascript functions close over bindings, not frozen copies of primitive values. an outer call may finish while its environment remains reachable through a returned callback. closures created by the same outer invocation can share bindings; separate invocations normally create separate bindings. let in a for loop creates a fresh per iteration binding when required by the loop semantics; var uses one function scoped binding. a closure retains only what remains reachable through its environment graph; engines may optimize unobservable details. a retained callback can keep dom nodes, caches, responses, or other large object graphs alive. in react, every render creates new bindings. a callback created during one render observes that render's state snapshot. a stale closure is usually a synchronization or lifecycle mistake, not evidence that closures themselves are unreliable. why this matters closures power callbacks, event handlers, promises, memoization, factory functions, module apis, and nearly every react component. they are also a frequent source of weak interview answers. saying “the inner function remembers the outer variables” predicts some behavior, but it does not explain mutation, shared state, loop bindings, stale react callbacks, or memory retention. senior engineers need a model that answers two production questions: 1. which binding will this callback read when it eventually runs? 2. what remains reachable for as long as this callback remains reachable? the first question is about correctness. the second is about lifetime and memory. core mental model when javascript creates an ordinary function, the function object receives an internal [[environment]] slot referring to the lexical environment active at creation time. when the function is called later, the new function environment links outward to that saved environment. the createcounter execution context is removed from the call stack after it returns. that does not make its bindings unreachable. the returned increment function still refers to the environment containing count , so that environment remains available. three structures must remain separate: the call stack records currently active calls; the lexical environment chain determines identifier resolution; the reachability graph determines which objects and environments can be reclaimed. a call can disappear from the stack while its environment remains reachable. visual model createcounter creates the count binding and the increment function. after the outer call returns, its active frame is gone, but the returned function remains reachable through next . its saved [[environment]] path therefore keeps the same count binding available for later calls. formal model function creation captures an environment the specification does not define a user visible closure object. an ordinary function stores its creation environment in [[environment]] . a later call uses that saved environment as its lexical outer environment, regardless of who calls the function. “closure” is therefore a programming language description of behavior produced by functions, environment records, and lexical lookup—not an object that application code can inspect. bindings are captured, not value snapshots consider this factory: both methods resolve score to the same mutable binding from one createscoreboard invocation. read did not capture the number 0 ; it retained a lookup path to the binding whose value later became 10 . you can deliberately capture a snapshot by creating another binding: the distinction is not “closure versus no closure.” both functions are closures. they close over different bindings. lifetime follows reachability returning removes a call from the active stack. garbage collection instead follows reachability: if a reachable function still needs an outer binding, the required state must remain available. this is a semantic rule, not a required heap layout. engines may omit unused bindings or represent captured state differently. reason about binding identity and reachability, not a literal scope object containing every local. shared versus independent environments closures created during one factory invocation can share its bindings. a second invocation normally creates a new environment with independent bindings. calling the same returned closure repeatedly therefore updates its existing state; calling another factory result updates different state. use one reliable interview rule: determine which invocation created the binding, then determine which closures can still reach it. step by step runtime walkthrough predict the output: expected output: trace it precisely: 1. the first createqueue invocation creates bindings for name and items . 2. its returned methods retain that invocation's environment. 3. the second invocation creates a separate name binding and a separate array referenced by its items binding. 4. calling urgent.enqueue creates a new enqueue call environment. items and name are not local there, so lookup follows the saved outer environment. 5. mutating the array does not replace the items binding; it changes the referenced object. 6. flush copies the current array elements, then mutates the shared array's length . 7. the two queue objects never share their captured arrays because they came from different factory invocations. important examples encapsulated mutable state the returned function provides controlled access to inflight without exposing the map directly. the finally cleanup is essential: without it, settled promises and their keys remain reachable for as long as the deduper remains reachable. this pattern is appropriate when the desired lifetime is the lifetime of one created deduper. a module level map would instead be shared by every consumer of that module instance. a react component local closure would be recreated with renders unless deliberately stabilized. interview edge case: var and per iteration let var i creates one function or global scoped binding. every callback closes over that binding. by the time they run, its value is 3 . now compare let : for a for loop with a lexical declaration, the specification creates per iteration environments. each callback closes over a different i binding. the important difference is binding identity, not asynchronous timing or arrow function syntax. common misconceptions claim better explanation “a closure exists only when a function is returned.” event listeners, promise handlers, and immediately invoked nested functions also use their creation environments. “closures capture values.” functions retain access to bindings; later reads observe the value currently held by that binding. “the outer call stays on the stack.” the call finishes; required bindings can remain reachable independently. “every local is retained forever.” only reachable, semantically required state matters, and engines can optimize its representation. “closures are memory leaks.” a leak requires unintended retention beyond the desired lifetime. “ let changes when callbacks run.” it changes binding identity in the loop, not scheduling. “react hooks cause stale closures.” ordinary closures expose a lifecycle mismatch when old callbacks remain connected. react connection each react render calls the component again and creates a new lexical environment. props, state variables, derived values, and inline callbacks belong to that render. when the user clicks, handleclick and the timer callback belong to that render. updating state before the timer fires does not rewrite the captured count binding into a newer render's binding. the callback reports the snapshot associated with the click. that behavior is often desirable: an event should retain the values relevant when it occurred. it becomes a stale closure bug when long lived logic is expected to synchronize with current reactive values but remains connected to an older render. effect dependencies the effect setup closes over one render's roomid . including roomid in the dependency list tells react to clean up the previous synchronization and run a new setup when that reactive value changes. removing a dependency merely to silence the linter does not make the callback current. strong fixes change the design: include reactive values that genuinely control synchronization; use a functional state update when the next state depends only on previous state; move non reactive logic outside an effect when it does not belong there; use a ref when mutable current data must not trigger a render; separate event specific logic from synchronization logic. the closure is behaving correctly; stale behavior indicates that the surrounding lifecycle or synchronization contract is wrong. memory and debugging creating small closures is normal. the more important question is whether a long lived callback retains data beyond its intended lifetime: the likely path is: event target → handleclick → saved environment → model → its object graph. the cleanup removes the listener at the root of that path. common long lived roots include: event targets with registered listeners; timers and intervals that have not been cancelled; subscriptions and observer callbacks; pending asynchronous operations; caches and module level collections; framework roots holding mounted component state. the existence of a closure does not prove which path retains an object. confirm the actual path with memory tooling. for incorrect values, inspect the callback's creation site: the invocation site explains when it ran, while the creation site explains which environment it retained. ask separately: 1. is this the same callback function as before? 2. does it resolve the name to the same binding as before? recreating a callback changes its identity and may capture new bindings. mutating a shared binding instead changes what existing closures observe. for suspected retention: 1. create and then remove the ui or data expected to be released; 2. force garbage collection when the tool supports it; 3. compare heap snapshots; 4. inspect retaining paths for unexpected listeners, timers, collections, or closures; 5. fix the lifecycle root rather than deleting unrelated locals. interview questions level 1 — fundamentals question: what is a closure? model answer: a closure is a function with access to the lexical environment where it was created. in specification terms, an ordinary function stores that environment in its internal [[environment]] slot. later calls resolve outer identifiers through that saved environment, even if the creating function has already returned. level 2 — applied understanding question: why do callbacks created with var in a loop often all log the final value, while callbacks created with let log different values? model answer: var gives the callbacks one shared function scoped binding. they run after the loop has updated that binding to its final value. a for loop with let creates per iteration lexical bindings, so each callback closes over a different binding. the difference is binding identity, not asynchronous execution itself. level 3 — senior reasoning question: when does a closure cause a memory leak? model answer: a closure is not inherently a leak. it becomes part of a leak when a long lived root—such as an event listener, timer, subscription, or cache—retains the closure beyond its intended lifecycle, and the closure's environment keeps otherwise obsolete objects reachable. i confirm this with heap snapshots and retaining paths, then remove the root through cleanup or bounded storage. level 4 — deep follow up question: does a closure store a copy of every local variable from its outer function? model answer: that is not a valid observable guarantee. semantically, the function retains access through its saved lexical environment to bindings required by later identifier resolution. engines can optimize storage, omit unused bindings, or represent captured state differently as long as behavior is preserved. i reason about binding identity and reachability, not a literal copied scope object. exercises 1. compare shared and independent state solution both lines output 1 2 1 , for different reasons. up and down share the binding created by one pair call. a and b use bindings created by separate counter calls. 2. fix the loop explain and fix this code without changing when the callbacks execute: solution use let index . the loop then creates per iteration bindings, producing 0 , 1 , and 2 . an extra factory invocation receiving index as a parameter also works because each call creates a new parameter binding. 3. find the retention path a removed modal remains in a heap snapshot. its close button registered a callback that references the modal's model. list the likely retention path and the cleanup you would verify. solution a likely path is an active event target or application registry → listener function → saved lexical environment → model → modal data. verify that unmounting removes the listener and that no subscription, timer, or registry still stores the callback. use the actual retaining path in memory tools rather than assuming the closure is the root. chapter summary essential model: a function retains access to its creation environment through lexical environment links. binding rule: closures capture access to bindings, not automatic frozen value copies. lifetime rule: leaving the call stack does not imply that captured state is unreachable. identity rule: closures from one invocation may share bindings; different invocations normally create independent bindings. loop rule: var commonly shares one binding, while let can create per iteration bindings. memory rule: leaks come from unintended long lived reachability paths, not from closure syntax itself. react rule: callbacks belong to render snapshots; stale behavior indicates a lifecycle or synchronization mismatch. interview ready explanation a closure is a function that retains access to the lexical environment where it was created. ordinary functions save that environment internally, so later identifier resolution follows the saved outer chain even after the creating call has returned. the function closes over bindings rather than frozen value copies, which explains shared mutable state and why separate factory invocations produce independent state. in loops, var callbacks share one binding, while let can create a binding per iteration. closures affect memory only through reachability: a long lived listener or timer can retain an environment and the object graph reachable from it. in react, each render creates new bindings, so a callback observes the state snapshot from the render that created it unless the design explicitly reconnects it to current data. further reading ecma 262: lexical environments ecma 262: ordinary function objects ecma 262: ordinaryfunctioncreate ecma 262: createperiterationenvironment react: state as a snapshot react: removing effect dependencies chrome devtools: fix memory problems",
  },
  {
    number: 7,
    slug: "07-this-binding",
    title: "`this` Binding",
    kind: "chapter",
    partNumber: 1,
    partName: "Runtime Foundations",
    markdown:
      '# Chapter 7 — `this` Binding\n\n## Learning objectives\n\nAfter completing this chapter, you should be able to:\n\n- determine `this` from a function\'s invocation form;\n- explain why extracting a method changes its receiver;\n- distinguish ordinary, arrow, bound, and constructor calls;\n- explain strict-mode and non-strict-mode fallback behavior;\n- use `call`, `apply`, and `bind` precisely;\n- diagnose lost-receiver bugs in callbacks, classes, DOM code, and React.\n\n## Quick Refresher\n\n- For an ordinary function, `this` is usually determined when the function is called.\n- `object.method()` calls `method` with `object` as the receiver.\n- Copying `object.method` into a variable does not permanently attach it to `object`.\n- A plain strict-mode call receives `undefined` as `this`.\n- Arrow functions do not create their own `this`; they use the surrounding lexical `this`.\n- `call` and `apply` invoke immediately with an explicit receiver; `bind` creates a new function.\n- `new` creates a receiver and invokes a constructable function as a constructor.\n- Passing a method as a callback often loses its original receiver.\n\n## Why This Matters\n\nWeak explanations say that `this` refers to “the current object” or “the object that owns the function.” Neither rule predicts detached methods, callbacks, arrows, bound functions, constructors, or strict mode. Senior interviews expect you to read the call expression and identify the receiver without guessing from where the function was defined.\n\nThe same skill prevents production failures such as an event handler losing a class instance, a private method throwing after extraction, or a callback silently writing to the wrong object.\n\n## Core Mental Model\n\nFor an ordinary function, start with the **call site**:\n\n| Invocation                  | Resulting `this`                               |\n| --------------------------- | ---------------------------------------------- |\n| `account.read()`            | `account`                                      |\n| `read()` in strict mode     | `undefined`                                    |\n| `read.call(account)`        | `account`                                      |\n| `read.apply(account, args)` | `account`                                      |\n| `read.bind(account)()`      | The bound `account`                            |\n| `new Account()`             | A newly created receiver                       |\n| Arrow function call         | No new `this`; use the enclosing lexical value |\n\n```js\n"use strict";\n\nconst account = {\n  balance: 100,\n  read() {\n    return this.balance;\n  },\n};\n\nconsole.log(account.read()); // 100\n\nconst detached = account.read;\nconsole.log(detached()); // TypeError: this is undefined\n```\n\nThe function did not change. The invocation changed from a property-reference call to a plain call, so the receiver was lost.\n\n## Visual Model\n\n![How different call sites select this for the same function](/this-call-site.svg)\n\nFor an ordinary function, the invocation selects the receiver: a method call preserves the property base, a strict plain call has no receiver, `call` supplies one explicitly, and `new` creates one. Arrow functions follow a separate rule because they read `this` lexically instead of receiving it from the call site.\n\n## Formal Model\n\n### A method call preserves a reference base\n\nEvaluating `account.read` produces an internal property Reference Record whose base is `account`. Calling that reference supplies the base object as `this`.\n\n```js\naccount.read();\n```\n\nExtraction first obtains the function value and discards the property reference:\n\n```js\nconst read = account.read;\nread();\n```\n\nThis is why “the function belongs to the object” is misleading. Functions are values; the call expression determines whether a receiver is supplied.\n\nParentheses alone do not normally detach a method:\n\n```js\naccount.read(); // this === account\n```\n\nBut expressions that produce only the function value do:\n\n```js\n(0, account.read)();\nconst { read } = account;\nread();\n```\n\n### Plain calls and strictness\n\nFor a plain call to an ordinary function:\n\n- strict-mode code keeps `this` as `undefined`;\n- legacy non-strict code substitutes the global object for `undefined` or `null` and boxes primitive receivers.\n\nModules and class bodies are strict. Modern code should not depend on global-object substitution.\n\n```js\nfunction strictReceiver() {\n  "use strict";\n  return this;\n}\n\nconsole.log(strictReceiver()); // undefined\n```\n\n### Explicit receivers: `call`, `apply`, and `bind`\n\n`call` and `apply` invoke a function immediately:\n\n```js\nfunction describe(prefix, suffix) {\n  return `${prefix}${this.name}${suffix}`;\n}\n\nconst user = { name: "Mina" };\n\ndescribe.call(user, "User: ", ".");\ndescribe.apply(user, ["User: ", "."]);\n```\n\n`bind` creates a new bound function and can also prefill arguments:\n\n```js\nconst describeUser = describe.bind(user, "User: ");\nconsole.log(describeUser(".")); // User: Mina.\n```\n\nCalling `call` or `apply` on a bound function cannot replace its bound receiver. If a bound function is used with `new`, construction creates the receiver instead; the stored bound `this` is ignored, while bound arguments still apply.\n\n### Arrow functions use lexical `this`\n\nAn arrow function has no own `this` binding. Reading `this` inside it continues through the surrounding lexical environment.\n\n```js\nconst timer = {\n  seconds: 0,\n  start() {\n    setInterval(() => {\n      this.seconds += 1;\n    }, 1000);\n  },\n};\n```\n\nThe arrow uses the `this` of `start`. Replacing it with an ordinary callback would give that callback a receiver according to how the host invokes it, not according to where it appears in the source.\n\nBecause arrows have no own `this`, `call`, `apply`, and `bind` cannot change the value they read. Arrow functions also cannot be used as constructors.\n\n### Constructor calls\n\nFor `new Constructor(...args)`, the language broadly:\n\n1. creates a new object linked to `Constructor.prototype`;\n2. invokes the constructor with that object as `this`;\n3. returns the explicit result if it is an object, otherwise returns the created object.\n\n```js\nfunction Account(balance) {\n  this.balance = balance;\n}\n\nconst account = new Account(100);\n```\n\nIn a derived class constructor, `this` remains uninitialized until `super()` completes. Accessing it earlier throws.\n\n### Class methods and private fields\n\nClass methods are strict but not automatically bound:\n\n```js\nclass Counter {\n  #count = 0;\n\n  increment() {\n    this.#count += 1;\n  }\n}\n\nconst counter = new Counter();\nconst increment = counter.increment;\nincrement(); // TypeError\n```\n\nThe detached call supplies `undefined`, and private-field access also requires a receiver carrying the correct private brand. Bind the method, wrap the call, or use an instance field arrow when its per-instance identity and allocation are intentional.\n\n## Step-by-Step Runtime Walkthrough\n\nPredict the output:\n\n```js\n"use strict";\n\nconst player = {\n  name: "Ada",\n  score: 10,\n  add(points) {\n    this.score += points;\n    return `${this.name}: ${this.score}`;\n  },\n};\n\nconst detached = player.add;\nconst bound = player.add.bind(player, 5);\n\nconsole.log(player.add(2));\n\ntry {\n  detached(3);\n} catch (error) {\n  console.log(error.name);\n}\n\nconsole.log(bound());\nconsole.log(player.score);\n```\n\nThe output is:\n\n```text\nAda: 12\nTypeError\nAda: 17\n17\n```\n\nTrace the call sites:\n\n1. `player.add(2)` preserves the property reference, so `this` is `player`.\n2. `detached(3)` is a plain strict-mode call, so `this` is `undefined`.\n3. `bound()` uses the receiver and first argument stored by `bind`.\n4. Both successful calls mutate the same `player.score` property.\n\n## Important Examples\n\n### Passing a method as a callback\n\n```js\nclass SearchTracker {\n  constructor() {\n    this.count = 0;\n  }\n\n  record() {\n    this.count += 1;\n  }\n}\n\nconst tracker = new SearchTracker();\n\nbutton.addEventListener("click", tracker.record); // receiver is not tracker\nbutton.addEventListener("click", () => tracker.record()); // receiver is tracker\n```\n\nA callback consumer controls how it invokes the function. Passing a method value does not preserve the original object. A wrapper creates a new call site that explicitly uses `tracker` as the receiver.\n\nFor DOM listeners registered as ordinary functions, browsers set `this` to the listener\'s `currentTarget`. Prefer the explicit `event.currentTarget`; it is clearer and also works with arrows.\n\n### Borrowing a method\n\n```js\nconst first = {\n  name: "first",\n  identify() {\n    return this.name;\n  },\n};\n\nconst second = { name: "second", identify: first.identify };\n\nconsole.log(second.identify()); // second\n```\n\nThe function was defined in `first`, but `second` is the receiver at this call site. Unlike lexical variables, ordinary-function `this` does not come from the function\'s creation environment.\n\n## Common Misconceptions\n\n| Claim                                               | Better explanation                                                             |\n| --------------------------------------------------- | ------------------------------------------------------------------------------ |\n| “`this` is the object that owns the function.”      | Ordinary-function `this` is generally determined by invocation, not ownership. |\n| “`this` refers to the current function.”            | It is a receiver value, not the function object.                               |\n| “Arrow functions bind `this` permanently.”          | Arrows have no own `this`; they read the enclosing lexical value.              |\n| “Methods are automatically bound.”                  | Extracting a method usually removes the property-reference receiver.           |\n| “`bind` changes the original function.”             | It returns a new bound function with different identity.                       |\n| “`call` can change an arrow\'s `this`.”              | Explicit-receiver operations cannot create a binding an arrow does not have.   |\n| “Callbacks keep the receiver from where they came.” | The callback consumer determines the later invocation form.                    |\n\n## React Connection\n\nFunction components do not use `this`; props and state are lexical bindings. This avoids an entire category of receiver bugs but does not remove closures or function identity concerns.\n\nLegacy class components do use instance methods:\n\n```jsx\nclass SaveButton extends React.Component {\n  handleClick() {\n    this.props.onSave();\n  }\n\n  render() {\n    return \u003cbutton onClick={() => this.handleClick()}>Save\u003c/button>;\n  }\n}\n```\n\nThe wrapper calls `this.handleClick()` as a method. Common alternatives are binding once in the constructor or using an instance field arrow:\n\n```jsx\nhandleClick = () => {\n  this.props.onSave();\n};\n```\n\nThese approaches produce different function identities and allocation patterns. Binding or creating wrappers during every render can also defeat shallow equality or memoization when identity matters. Correctness comes first; optimize identity only where measurement or component contracts justify it.\n\nWhen a function component passes `service.save` directly, the ordinary JavaScript detached-method rule still applies. React does not restore the service receiver.\n\n## Performance and Debugging\n\nReceiver selection itself is rarely a useful optimization target. More practical concerns are accidental function allocation and unstable callback identity from repeated `bind` or wrapper creation.\n\nWhen `this` is wrong:\n\n1. Find the exact call expression, not only the function definition.\n2. Classify it as method, plain, explicit, bound, constructor, or arrow behavior.\n3. Check whether destructuring or callback passing discarded a property reference.\n4. Confirm whether strict mode turns the missing receiver into `undefined`.\n5. Inspect transpiled code only after understanding the source-level call.\n\nAt a breakpoint, inspect `this` in the current frame and compare it with the expected object. For private-field errors, verify both receiver presence and private brand. Avoid “fixing” receiver bugs with a global arrow conversion: arrows are appropriate only when lexical `this` is the intended contract.\n\n## Interview Questions\n\n### Level 1 — Fundamentals\n\n**Question:** How is `this` determined in JavaScript?\n\n**Model answer:** For an ordinary function, start from the invocation. A property-reference call supplies its base object, a strict plain call supplies `undefined`, `call` and `apply` supply an explicit receiver, `bind` stores one for later calls, and `new` creates one. Arrow functions are different because they use lexical `this` from the surrounding environment.\n\n### Level 2 — Applied understanding\n\n**Question:** Why does an extracted method often fail?\n\n**Model answer:** `object.method()` preserves a property Reference Record whose base becomes `this`. Assigning `object.method` to a variable retrieves only the function value. Calling that variable is a plain call, so the original object is no longer supplied as the receiver.\n\n### Level 3 — Senior reasoning\n\n**Question:** When would you choose a wrapper, `bind`, or an arrow instance field?\n\n**Model answer:** A wrapper makes the receiver and arguments explicit at one call site. `bind` creates a reusable function with a fixed receiver and optional partial arguments. An arrow instance field provides lexical instance access but creates a function per instance and changes prototype-method semantics. I choose based on lifetime, identity requirements, and API expectations rather than style alone.\n\n### Level 4 — Deep follow-up\n\n**Question:** What happens when a bound constructor is called with `new`?\n\n**Model answer:** If the target is constructable, `new` creates the receiver and the bound `this` value is ignored. Bound arguments are still prepended. Construction behavior comes from the target function, and the resulting object follows the target\'s construction semantics.\n\n## Exercises\n\n### 1. Predict the receiver\n\n```js\n"use strict";\n\nconst box = {\n  value: 3,\n  read() {\n    return this?.value;\n  },\n};\n\nconst read = box.read;\n\nconsole.log(box.read());\nconsole.log(read());\nconsole.log(read.call({ value: 7 }));\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe output is `3`, `undefined`, and `7`. The optional chain inside `read` prevents an error for the strict plain call; it does not restore the lost receiver.\n\n\u003c/details>\n\n### 2. Explain the arrow\n\n```js\nconst panel = {\n  title: "Settings",\n  createReader() {\n    return () => this.title;\n  },\n};\n\nconst read = panel.createReader();\nconsole.log(read.call({ title: "Profile" }));\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe output is `Settings`. `createReader()` runs with `panel` as `this`, and the arrow reads that surrounding lexical value. `call` cannot provide the arrow with a different `this` binding.\n\n\u003c/details>\n\n### 3. Repair the callback\n\n```js\nclass Controller {\n  save() {\n    return this.repository.commit();\n  }\n}\n\nqueue.add(controller.save);\n```\n\nGive two valid repairs and explain their identity or lifetime trade-off.\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nPass `controller.save.bind(controller)` and retain that bound function if removal later requires the same identity, or pass `() => controller.save()` and retain the wrapper for the same reason. Binding once during initialization avoids creating a new callback at every registration or render.\n\n\u003c/details>\n\n### 4. Explain construction\n\nWhat receiver does `new BoundAccount(100)` use if `BoundAccount` was created with `Account.bind(existingAccount)`?\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nConstruction creates a new receiver; it does not use `existingAccount`. Any arguments stored by `bind` are prepended to the constructor arguments.\n\n\u003c/details>\n\n## Chapter Summary\n\n- Ordinary-function `this` is determined primarily by the invocation form.\n- Method syntax preserves a property-reference base; method extraction discards it.\n- Strict plain calls receive `undefined`; modern code should not rely on global fallback.\n- Arrows use lexical `this` and cannot be rebound or constructed.\n- `call` and `apply` invoke explicitly; `bind` creates a new function.\n- `new` creates a receiver and applies constructor return rules.\n- Callback APIs frequently expose lost-receiver bugs.\n- React function components avoid `this`, but passed service methods still follow JavaScript rules.\n\n### Interview-ready explanation\n\nFor an ordinary JavaScript function, I determine `this` from the call expression. `object.method()` supplies `object` because the property reference retains its base. Extracting the method leaves only a function value, so a strict plain call receives `undefined`. `call` and `apply` provide an explicit receiver, `bind` creates a function with a stored receiver, and `new` creates a receiver for construction. Arrow functions are the exception: they have no own `this` and read it lexically from the surrounding environment. This model explains detached callbacks, class-method errors, and why passing `service.save` through React does not preserve `service`.\n\n## Further Reading\n\n- [ECMA-262: ResolveThisBinding](https://tc39.es/ecma262/#sec-resolvethisbinding)\n- [ECMA-262: EvaluateCall](https://tc39.es/ecma262/#sec-evaluatecall)\n- [ECMA-262: Function Environment Records](https://tc39.es/ecma262/#sec-function-environment-records)\n- [ECMA-262: Bound Function Exotic Objects](https://tc39.es/ecma262/#sec-bound-function-exotic-objects)\n- [ECMA-262: The `new` Operator](https://tc39.es/ecma262/#sec-new-operator)\n- [MDN: `this`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/this)\n- [React: Responding to Events](https://react.dev/learn/responding-to-events)',
    headings: [
      "Learning objectives",
      "Quick Refresher",
      "Why This Matters",
      "Core Mental Model",
      "Visual Model",
      "Formal Model",
      "Step-by-Step Runtime Walkthrough",
      "Important Examples",
      "Common Misconceptions",
      "React Connection",
      "Performance and Debugging",
      "Interview Questions",
      "Exercises",
      "Chapter Summary",
      "Further Reading",
    ],
    quickRefresher:
      "For an ordinary function, this is usually determined when the function is called. object.method() calls method with object as the receiver. Copying object.method into a variable does not permanently attach it to object . A plain strict mode call receives undefined as this . Arrow functions do not create their own this ; they use the surrounding lexical this . call and apply invoke immediately with an explicit receiver; bind creates a new function. new creates a receiver and invokes a constructable function as a constructor. Passing a method as a callback often loses its original receiver.",
    excerpt:
      "Weak explanations say that this refers to “the current object” or “the object that owns the function.” Neither rule predicts detached methods, callbacks, arrows, bound functions, constructors, or strict mode. Senior interviews expect you to read the call expression and identify the receiver without guessing from where the function was defined.",
    readingMinutes: 9,
    searchText:
      "chapter 7 — this binding learning objectives after completing this chapter, you should be able to: determine this from a function's invocation form; explain why extracting a method changes its receiver; distinguish ordinary, arrow, bound, and constructor calls; explain strict mode and non strict mode fallback behavior; use call , apply , and bind precisely; diagnose lost receiver bugs in callbacks, classes, dom code, and react. quick refresher for an ordinary function, this is usually determined when the function is called. object.method() calls method with object as the receiver. copying object.method into a variable does not permanently attach it to object . a plain strict mode call receives undefined as this . arrow functions do not create their own this ; they use the surrounding lexical this . call and apply invoke immediately with an explicit receiver; bind creates a new function. new creates a receiver and invokes a constructable function as a constructor. passing a method as a callback often loses its original receiver. why this matters weak explanations say that this refers to “the current object” or “the object that owns the function.” neither rule predicts detached methods, callbacks, arrows, bound functions, constructors, or strict mode. senior interviews expect you to read the call expression and identify the receiver without guessing from where the function was defined. the same skill prevents production failures such as an event handler losing a class instance, a private method throwing after extraction, or a callback silently writing to the wrong object. core mental model for an ordinary function, start with the call site : invocation resulting this account.read() account read() in strict mode undefined read.call(account) account read.apply(account, args) account read.bind(account)() the bound account new account() a newly created receiver arrow function call no new this ; use the enclosing lexical value the function did not change. the invocation changed from a property reference call to a plain call, so the receiver was lost. visual model for an ordinary function, the invocation selects the receiver: a method call preserves the property base, a strict plain call has no receiver, call supplies one explicitly, and new creates one. arrow functions follow a separate rule because they read this lexically instead of receiving it from the call site. formal model a method call preserves a reference base evaluating account.read produces an internal property reference record whose base is account . calling that reference supplies the base object as this . extraction first obtains the function value and discards the property reference: this is why “the function belongs to the object” is misleading. functions are values; the call expression determines whether a receiver is supplied. parentheses alone do not normally detach a method: but expressions that produce only the function value do: plain calls and strictness for a plain call to an ordinary function: strict mode code keeps this as undefined ; legacy non strict code substitutes the global object for undefined or null and boxes primitive receivers. modules and class bodies are strict. modern code should not depend on global object substitution. explicit receivers: call , apply , and bind call and apply invoke a function immediately: bind creates a new bound function and can also prefill arguments: calling call or apply on a bound function cannot replace its bound receiver. if a bound function is used with new , construction creates the receiver instead; the stored bound this is ignored, while bound arguments still apply. arrow functions use lexical this an arrow function has no own this binding. reading this inside it continues through the surrounding lexical environment. the arrow uses the this of start . replacing it with an ordinary callback would give that callback a receiver according to how the host invokes it, not according to where it appears in the source. because arrows have no own this , call , apply , and bind cannot change the value they read. arrow functions also cannot be used as constructors. constructor calls for new constructor(...args) , the language broadly: 1. creates a new object linked to constructor.prototype ; 2. invokes the constructor with that object as this ; 3. returns the explicit result if it is an object, otherwise returns the created object. in a derived class constructor, this remains uninitialized until super() completes. accessing it earlier throws. class methods and private fields class methods are strict but not automatically bound: the detached call supplies undefined , and private field access also requires a receiver carrying the correct private brand. bind the method, wrap the call, or use an instance field arrow when its per instance identity and allocation are intentional. step by step runtime walkthrough predict the output: the output is: trace the call sites: 1. player.add(2) preserves the property reference, so this is player . 2. detached(3) is a plain strict mode call, so this is undefined . 3. bound() uses the receiver and first argument stored by bind . 4. both successful calls mutate the same player.score property. important examples passing a method as a callback a callback consumer controls how it invokes the function. passing a method value does not preserve the original object. a wrapper creates a new call site that explicitly uses tracker as the receiver. for dom listeners registered as ordinary functions, browsers set this to the listener's currenttarget . prefer the explicit event.currenttarget ; it is clearer and also works with arrows. borrowing a method the function was defined in first , but second is the receiver at this call site. unlike lexical variables, ordinary function this does not come from the function's creation environment. common misconceptions claim better explanation “ this is the object that owns the function.” ordinary function this is generally determined by invocation, not ownership. “ this refers to the current function.” it is a receiver value, not the function object. “arrow functions bind this permanently.” arrows have no own this ; they read the enclosing lexical value. “methods are automatically bound.” extracting a method usually removes the property reference receiver. “ bind changes the original function.” it returns a new bound function with different identity. “ call can change an arrow's this .” explicit receiver operations cannot create a binding an arrow does not have. “callbacks keep the receiver from where they came.” the callback consumer determines the later invocation form. react connection function components do not use this ; props and state are lexical bindings. this avoids an entire category of receiver bugs but does not remove closures or function identity concerns. legacy class components do use instance methods: the wrapper calls this.handleclick() as a method. common alternatives are binding once in the constructor or using an instance field arrow: these approaches produce different function identities and allocation patterns. binding or creating wrappers during every render can also defeat shallow equality or memoization when identity matters. correctness comes first; optimize identity only where measurement or component contracts justify it. when a function component passes service.save directly, the ordinary javascript detached method rule still applies. react does not restore the service receiver. performance and debugging receiver selection itself is rarely a useful optimization target. more practical concerns are accidental function allocation and unstable callback identity from repeated bind or wrapper creation. when this is wrong: 1. find the exact call expression, not only the function definition. 2. classify it as method, plain, explicit, bound, constructor, or arrow behavior. 3. check whether destructuring or callback passing discarded a property reference. 4. confirm whether strict mode turns the missing receiver into undefined . 5. inspect transpiled code only after understanding the source level call. at a breakpoint, inspect this in the current frame and compare it with the expected object. for private field errors, verify both receiver presence and private brand. avoid “fixing” receiver bugs with a global arrow conversion: arrows are appropriate only when lexical this is the intended contract. interview questions level 1 — fundamentals question: how is this determined in javascript? model answer: for an ordinary function, start from the invocation. a property reference call supplies its base object, a strict plain call supplies undefined , call and apply supply an explicit receiver, bind stores one for later calls, and new creates one. arrow functions are different because they use lexical this from the surrounding environment. level 2 — applied understanding question: why does an extracted method often fail? model answer: object.method() preserves a property reference record whose base becomes this . assigning object.method to a variable retrieves only the function value. calling that variable is a plain call, so the original object is no longer supplied as the receiver. level 3 — senior reasoning question: when would you choose a wrapper, bind , or an arrow instance field? model answer: a wrapper makes the receiver and arguments explicit at one call site. bind creates a reusable function with a fixed receiver and optional partial arguments. an arrow instance field provides lexical instance access but creates a function per instance and changes prototype method semantics. i choose based on lifetime, identity requirements, and api expectations rather than style alone. level 4 — deep follow up question: what happens when a bound constructor is called with new ? model answer: if the target is constructable, new creates the receiver and the bound this value is ignored. bound arguments are still prepended. construction behavior comes from the target function, and the resulting object follows the target's construction semantics. exercises 1. predict the receiver solution the output is 3 , undefined , and 7 . the optional chain inside read prevents an error for the strict plain call; it does not restore the lost receiver. 2. explain the arrow solution the output is settings . createreader() runs with panel as this , and the arrow reads that surrounding lexical value. call cannot provide the arrow with a different this binding. 3. repair the callback give two valid repairs and explain their identity or lifetime trade off. solution pass controller.save.bind(controller) and retain that bound function if removal later requires the same identity, or pass () = controller.save() and retain the wrapper for the same reason. binding once during initialization avoids creating a new callback at every registration or render. 4. explain construction what receiver does new boundaccount(100) use if boundaccount was created with account.bind(existingaccount) ? solution construction creates a new receiver; it does not use existingaccount . any arguments stored by bind are prepended to the constructor arguments. chapter summary ordinary function this is determined primarily by the invocation form. method syntax preserves a property reference base; method extraction discards it. strict plain calls receive undefined ; modern code should not rely on global fallback. arrows use lexical this and cannot be rebound or constructed. call and apply invoke explicitly; bind creates a new function. new creates a receiver and applies constructor return rules. callback apis frequently expose lost receiver bugs. react function components avoid this , but passed service methods still follow javascript rules. interview ready explanation for an ordinary javascript function, i determine this from the call expression. object.method() supplies object because the property reference retains its base. extracting the method leaves only a function value, so a strict plain call receives undefined . call and apply provide an explicit receiver, bind creates a function with a stored receiver, and new creates a receiver for construction. arrow functions are the exception: they have no own this and read it lexically from the surrounding environment. this model explains detached callbacks, class method errors, and why passing service.save through react does not preserve service . further reading ecma 262: resolvethisbinding ecma 262: evaluatecall ecma 262: function environment records ecma 262: bound function exotic objects ecma 262: the new operator mdn: this react: responding to events",
  },
  {
    number: 8,
    slug: "08-memory-management-and-garbage-collection",
    title: "Memory Management and Garbage Collection",
    kind: "chapter",
    partNumber: 1,
    partName: "Runtime Foundations",
    markdown:
      '# Chapter 8 — Memory Management and Garbage Collection\n\n## Learning objectives\n\nAfter completing this chapter, you should be able to:\n\n- explain garbage collection in terms of reachability;\n- distinguish allocation, retention, reclamation, and memory leaks;\n- identify roots and trace retaining paths;\n- explain when weak collections are useful;\n- recognize common browser and React retention bugs;\n- investigate memory growth with heap snapshots and allocation tools;\n- avoid relying on garbage-collection timing or finalizers for correctness.\n\n## Quick Refresher\n\n- JavaScript automatically manages memory, but applications still control object reachability.\n- An object remains alive while it is reachable from a root through strong references.\n- A leak is unwanted retention, not merely high allocation or growing memory.\n- Closures, listeners, timers, caches, and detached DOM trees can extend object lifetimes.\n- `WeakMap` and `WeakSet` do not keep their object keys strongly reachable.\n- Garbage-collection timing is not deterministic or observable as an application contract.\n- Heap snapshots explain retained state; CPU profiles explain execution cost.\n- React cleanup should remove subscriptions and other external retention paths.\n\n## Why This Matters\n\n“JavaScript has garbage collection” does not mean applications cannot leak memory. The collector can reclaim only state that is no longer reachable. A forgotten listener, unbounded cache, or module-level registry can keep an obsolete interface and its entire object graph alive indefinitely.\n\nSenior engineers must distinguish a real leak from temporary allocation pressure, caching, development tooling, and normal garbage-collector behavior. The correct response begins with evidence about what retains the object—not with deleting arbitrary variables or forcing collection.\n\n## Core Mental Model\n\nThink in terms of a directed graph:\n\n- **nodes** are objects, functions, environments, DOM nodes, and other managed values;\n- **edges** are references between them;\n- **roots** are starting points the runtime considers live;\n- anything reachable from a root must remain available;\n- unreachable state becomes eligible for reclamation.\n\n```js\nconst registry = new Map();\n\nfunction rememberView(id, viewModel) {\n  registry.set(id, viewModel);\n}\n```\n\nIf `registry` is module-scoped and remains reachable, every stored `viewModel` remains reachable too. Removing the UI alone does not release those entries.\n\nThe most useful debugging question is:\n\n> What strong reference path still connects this object to a root?\n\nGarbage collection answers whether memory **may** be reclaimed. It does not promise when reclamation will occur or whether the process will immediately return memory to the operating system.\n\n## Visual Model\n\n![Garbage collection reachability from roots and an unreachable object cycle](/garbage-collection-reachability.svg)\n\nThe module registry and browser listener are roots for this investigation. Their strong paths keep the map entry, callback, and detached DOM tree alive. The separate object cycle has no path from a root, so the cycle itself does not prevent collection.\n\n## Formal Model\n\n### Allocation, retention, and reclamation\n\nThese are separate events:\n\n1. **Allocation:** execution creates a value requiring managed storage.\n2. **Retention:** one or more reachable references keep that value alive.\n3. **Unreachability:** no strong path from a root reaches the value.\n4. **Reclamation:** the implementation eventually reuses or releases its storage.\n\nMemory can rise because allocation temporarily outpaces collection without indicating a leak. A leak produces continuing unwanted reachability across completed application lifecycles.\n\n### Roots and retaining paths\n\nExact roots are implementation- and host-dependent, but useful categories include:\n\n- active execution state;\n- reachable global and module state;\n- host registrations such as listeners and timers;\n- framework roots and mounted component state;\n- native or embedder references exposed through browser APIs.\n\nA retaining path is a chain from one of those roots to the object being investigated. The path matters more than the syntax that created the object.\n\n### Collection strategy is an implementation detail\n\nA conceptual mark-and-sweep explanation is useful:\n\n1. start from roots;\n2. mark everything reachable through strong references;\n3. reclaim unmarked storage.\n\nReal engines use more sophisticated strategies, often including generational, incremental, and concurrent work. These techniques reduce pause time and collection cost, but ECMAScript does not guarantee a particular algorithm, schedule, or heap layout.\n\nDo not infer liveness from whether DevTools happens to show an object after one operation. Tooling itself can retain inspected values, and collection may not have run yet.\n\n### Cycles are not inherently leaks\n\nReachability-based collectors can reclaim unreachable cycles:\n\n```js\nlet left = {};\nlet right = {};\n\nleft.other = right;\nright.other = left;\n\nleft = null;\nright = null;\n```\n\nAfter the external references are removed, the pair can become unreachable even though the objects still reference each other. Cycles become leaks only when some unwanted path from a root still reaches them.\n\n### Strong and weak associations\n\nA normal `Map` strongly retains both keys and values. `WeakMap` does not keep an object key alive solely because it is present in the collection.\n\n```js\nconst metadata = new WeakMap();\n\nfunction annotate(element, data) {\n  metadata.set(element, data);\n}\n```\n\nWhen an `element` becomes unreachable elsewhere, its weak association does not prevent collection. This makes `WeakMap` useful for metadata whose lifetime should follow an object.\n\nWeak collections deliberately cannot be enumerated. If entries could be listed, observable results would depend on nondeterministic garbage-collection timing.\n\nUse weak collections only when object-key lifetime matches the desired ownership model. They do not fix leaks when another strong path still retains the key, and they are not a replacement for bounded caches that must be enumerable.\n\n### `WeakRef` and finalization\n\n`WeakRef` allows a non-owning observation of an object, and `FinalizationRegistry` can request a callback after collection. Both are intentionally nondeterministic.\n\nDo not use them for essential cleanup, business logic, cache correctness, or timely release of scarce external resources. Prefer explicit lifecycle operations such as `close`, `dispose`, unsubscribe functions, and `AbortController`.\n\n## Step-by-Step Runtime Walkthrough\n\nConsider a modal controller:\n\n```js\nconst openModals = new Map();\n\nfunction mountModal(id, element, model) {\n  function handleClose() {\n    element.remove();\n  }\n\n  element.querySelector("[data-close]").addEventListener("click", handleClose);\n  openModals.set(id, { element, model, handleClose });\n}\n```\n\nClosing the modal removes its element from the document, but that does not necessarily make it collectible:\n\n1. `openModals` is reachable from module scope.\n2. Its entry strongly references `element`, `model`, and `handleClose`.\n3. The callback\'s closure also refers to `element`.\n4. Removing the element from the DOM changes document membership, not JavaScript reachability.\n5. The entry and its reachable graph remain alive until the registry releases them.\n\nA lifecycle-aware version removes both external registrations and application references:\n\n```js\nconst openModals = new Map();\n\nfunction mountModal(id, element, model) {\n  const closeButton = element.querySelector("[data-close]");\n\n  function dispose() {\n    closeButton.removeEventListener("click", dispose);\n    openModals.delete(id);\n    element.remove();\n  }\n\n  closeButton.addEventListener("click", dispose);\n  openModals.set(id, { element, model, dispose });\n\n  return dispose;\n}\n```\n\nThe key design improvement is explicit ownership: mounting establishes retention paths, and disposal removes them.\n\n## Important Leak Patterns\n\n### Unbounded caches and registries\n\n```js\nconst responseCache = new Map();\n\nexport async function loadProduct(id) {\n  if (!responseCache.has(id)) {\n    responseCache.set(\n      id,\n      fetch(`/api/products/${id}`).then((response) => response.json()),\n    );\n  }\n\n  return responseCache.get(id);\n}\n```\n\nThis may be intentional caching, but without eviction its lifetime is the module instance\'s lifetime. Choose a bound, expiration policy, explicit invalidation, or object-keyed weak association according to product requirements.\n\n### Listeners, observers, and subscriptions\n\nAn event target or external store may retain its callbacks. Those callbacks can retain component data through closures. Cleanup must use the same callback identity that was registered.\n\n```js\nconst handleResize = () => updateLayout(model);\n\nwindow.addEventListener("resize", handleResize);\n\n// Later\nwindow.removeEventListener("resize", handleResize);\n```\n\nCreating a new arrow during removal does not remove the original listener.\n\n### Timers and pending work\n\nIntervals remain registered until cleared. Long-delay timers, observers, queued work, and pending requests can also extend lifetimes. Cancellation is valuable both for memory and for preventing obsolete work from updating current UI.\n\n### Detached DOM trees\n\nA detached node is not automatically leaked. It becomes a problem when JavaScript, a listener registry, a framework, or tooling still retains it after the application expects it to disappear. Investigate the retaining path rather than the “Detached” label alone.\n\n## Common Misconceptions\n\n| Claim                                                    | Better explanation                                                                                   |\n| -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |\n| “Garbage collection prevents memory leaks.”              | It reclaims unreachable state; applications determine which references remain reachable.             |\n| “Setting a variable to `null` frees memory immediately.” | It removes one reference. Other paths may remain, and collection timing is nondeterministic.         |\n| “Circular references always leak.”                       | Unreachable cycles can be collected by reachability-based collectors.                                |\n| “A large heap proves a leak.”                            | Caches, allocation bursts, delayed collection, and tooling can also increase heap size.              |\n| “A detached DOM node is necessarily leaked.”             | It is a leak only if unwanted strong references keep it alive.                                       |\n| “`WeakMap` makes its values weak.”                       | Weakness concerns reachability through object keys; values remain available while their key is live. |\n| “Finalizers are reliable cleanup hooks.”                 | Their execution is delayed, nondeterministic, and may never occur before process termination.        |\n\n## React Connection\n\nReact releases its own references when components unmount, but it cannot automatically clean up every external system a component contacted.\n\n```jsx\nfunction Presence({ userId }) {\n  useEffect(() => {\n    const controller = new AbortController();\n    const unsubscribe = presenceStore.subscribe(userId, handlePresence);\n\n    loadPresence(userId, { signal: controller.signal });\n\n    return () => {\n      controller.abort();\n      unsubscribe();\n    };\n  }, [userId]);\n\n  return null;\n}\n```\n\nThe effect cleanup mirrors setup: it aborts obsolete work and removes the store\'s reference to the callback. When `userId` changes, React first cleans up the previous synchronization before establishing the next one.\n\nCommon React retention problems include:\n\n- effects that subscribe without unsubscribing;\n- timers or observers that survive unmount;\n- module-level maps keyed by component or request data;\n- external stores that retain abandoned listeners;\n- caches without bounds or invalidation;\n- development measurements misread as production behavior.\n\nReact Strict Mode may run an extra development setup-and-cleanup cycle to expose missing cleanup. It is a diagnostic signal, not the production lifecycle itself.\n\nState updates after unmount do not by themselves prove a memory leak. Ask whether some external root still retains the callback or data and whether that retention is unwanted.\n\n## Performance and Debugging\n\n### Distinguish allocation pressure from retention\n\n- **Allocation pressure:** many short-lived values increase collection work but become unreachable normally.\n- **Retention:** values remain reachable across completed lifecycles.\n- **Leak:** retention is unintended and continues to grow or preserve obsolete state.\n\nOptimize only after identifying which condition exists. A CPU profile or allocation timeline helps with allocation cost; heap snapshots and retaining paths help with retained state.\n\n### Use a repeatable investigation\n\n1. Define one lifecycle, such as opening and closing a modal.\n2. Record a baseline after the application settles.\n3. Repeat the lifecycle several times.\n4. Release the feature and allow or request collection in the diagnostic tool.\n5. Compare snapshots by constructor and retained size.\n6. Inspect retaining paths for objects that should have disappeared.\n7. Fix the root reference and repeat the same experiment.\n\nOne snapshot rarely proves growth. Repetition distinguishes one-time initialization from accumulation.\n\n### Read memory metrics carefully\n\n- **Shallow size** is storage directly owned by an object.\n- **Retained size** estimates memory that could become collectible if that object and its exclusive descendants were released.\n- **Dominator** relationships help identify objects responsible for retaining large subgraphs.\n\nThese are tooling models and estimates, not source-language guarantees. DevTools consoles, selected elements, framework extensions, and heap snapshots can retain values while debugging.\n\n## Interview Questions\n\n### Level 1 — Fundamentals\n\n**Question:** How does JavaScript know when an object can be garbage-collected?\n\n**Model answer:** The useful semantic model is reachability. Starting from runtime and host roots, the collector traces strong references. If no root can reach an object, it becomes eligible for reclamation. Eligibility does not guarantee immediate collection or memory return to the operating system.\n\n### Level 2 — Applied understanding\n\n**Question:** Why can a removed DOM node remain in memory?\n\n**Model answer:** Removing a node from the document only removes that document relationship. JavaScript variables, caches, event registrations, framework state, or debugging tools may still retain it. I inspect its retaining path to find the strong reference that conflicts with the intended lifecycle.\n\n### Level 3 — Senior reasoning\n\n**Question:** How would you prove that repeatedly opening a screen leaks memory?\n\n**Model answer:** I define a repeatable open-close lifecycle, establish a settled baseline, repeat the lifecycle, allow collection, and compare multiple heap snapshots. I look for instances that accumulate and inspect their retaining paths and dominators. Then I remove the responsible listener, cache entry, timer, subscription, or framework reference and repeat the same test.\n\n### Level 4 — Deep follow-up\n\n**Question:** When is `WeakMap` preferable to `Map`?\n\n**Model answer:** I use `WeakMap` for metadata associated with object keys when the collection should not own those keys and enumeration is unnecessary. It does not provide observable eviction, size, or iteration, and it does not help if another strong path retains the keys. Bounded or enumerable caches usually need an explicit `Map` policy instead.\n\n## Exercises\n\n### 1. Analyze a cycle\n\n```js\nlet parent = { name: "parent" };\nlet child = { name: "child", parent };\nparent.child = child;\n\nparent = null;\nchild = null;\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe cycle can become collectible because no remaining application reference reaches either object. The mutual references do not make the cycle a root.\n\n\u003c/details>\n\n### 2. Find the listener bug\n\n```js\nwindow.addEventListener("resize", () => updateLayout(model));\n\nreturn () => {\n  window.removeEventListener("resize", () => updateLayout(model));\n};\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe two arrows are different function objects, so removal does not match the registered listener. Store one callback in a binding and use the same identity for both operations.\n\n\u003c/details>\n\n### 3. Choose a cache\n\nYou need metadata for DOM elements that should disappear when each element is otherwise unreachable. Do you choose `Map` or `WeakMap`?\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nChoose `WeakMap` if the elements are object keys, the metadata should follow their lifetime, and enumeration is unnecessary. A `Map` would strongly retain each element until explicit deletion.\n\n\u003c/details>\n\n### 4. Diagnose React cleanup\n\nA component subscribes to a global store whenever `roomId` changes but never returns cleanup. What can accumulate, and what should the effect return?\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe store can retain one callback and its captured render data for every previous room. The effect should return the unsubscribe function—or a function that calls it—so React removes the old subscription before resynchronizing or unmounting.\n\n\u003c/details>\n\n## Chapter Summary\n\n- Garbage collection is best understood through strong reachability from roots.\n- A leak is unwanted retention across the application\'s intended lifecycle.\n- Removing one reference or DOM relationship does not prove an object is unreachable.\n- Cycles can be collected when no root reaches them.\n- Weak collections support non-owning object-key associations but are not general cache policies.\n- Finalization is nondeterministic and unsuitable for correctness-critical cleanup.\n- Listeners, subscriptions, timers, registries, and caches are common retention roots.\n- React effects should explicitly undo external synchronization.\n- Heap snapshots and retaining paths diagnose retention; repeated experiments establish accumulation.\n\n### Interview-ready explanation\n\nJavaScript garbage collection is best explained with reachability. Objects remain alive while a strong path connects them to a runtime or host root; unreachable objects become eligible for reclamation at an unspecified time. Memory leaks therefore come from unwanted references—often listeners, subscriptions, timers, caches, closures, or module registries—not from the absence of manual `free`. I diagnose them by repeating a lifecycle, comparing heap snapshots, and following retaining paths to the responsible root. `WeakMap` is useful when object-keyed metadata should not own the key, but explicit cleanup and bounded storage remain the primary lifecycle tools, especially in React effects.\n\n## Further Reading\n\n- [ECMA-262: Memory Model](https://tc39.es/ecma262/#sec-memory-model)\n- [ECMA-262: WeakMap Objects](https://tc39.es/ecma262/#sec-weakmap-objects)\n- [ECMA-262: WeakRef Objects](https://tc39.es/ecma262/#sec-weak-ref-objects)\n- [ECMA-262: FinalizationRegistry Objects](https://tc39.es/ecma262/#sec-finalization-registry-objects)\n- [MDN: Memory Management](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Memory_management)\n- [Chrome DevTools: Fix Memory Problems](https://developer.chrome.com/docs/devtools/memory-problems/)\n- [React: Synchronizing with Effects](https://react.dev/learn/synchronizing-with-effects)',
    headings: [
      "Learning objectives",
      "Quick Refresher",
      "Why This Matters",
      "Core Mental Model",
      "Visual Model",
      "Formal Model",
      "Step-by-Step Runtime Walkthrough",
      "Important Leak Patterns",
      "Common Misconceptions",
      "React Connection",
      "Performance and Debugging",
      "Interview Questions",
      "Exercises",
      "Chapter Summary",
      "Further Reading",
    ],
    quickRefresher:
      "JavaScript automatically manages memory, but applications still control object reachability. An object remains alive while it is reachable from a root through strong references. A leak is unwanted retention, not merely high allocation or growing memory. Closures, listeners, timers, caches, and detached DOM trees can extend object lifetimes. WeakMap and WeakSet do not keep their object keys strongly reachable. Garbage collection timing is not deterministic or observable as an application contract. Heap snapshots explain retained state; CPU profiles explain execution cost. React cleanup should remove subscriptions and other external retention paths.",
    excerpt:
      "“JavaScript has garbage collection” does not mean applications cannot leak memory. The collector can reclaim only state that is no longer reachable. A forgotten listener, unbounded cache, or module level registry can keep an obsolete interface and its entire object graph alive indefinitely.",
    readingMinutes: 11,
    searchText:
      "chapter 8 — memory management and garbage collection learning objectives after completing this chapter, you should be able to: explain garbage collection in terms of reachability; distinguish allocation, retention, reclamation, and memory leaks; identify roots and trace retaining paths; explain when weak collections are useful; recognize common browser and react retention bugs; investigate memory growth with heap snapshots and allocation tools; avoid relying on garbage collection timing or finalizers for correctness. quick refresher javascript automatically manages memory, but applications still control object reachability. an object remains alive while it is reachable from a root through strong references. a leak is unwanted retention, not merely high allocation or growing memory. closures, listeners, timers, caches, and detached dom trees can extend object lifetimes. weakmap and weakset do not keep their object keys strongly reachable. garbage collection timing is not deterministic or observable as an application contract. heap snapshots explain retained state; cpu profiles explain execution cost. react cleanup should remove subscriptions and other external retention paths. why this matters “javascript has garbage collection” does not mean applications cannot leak memory. the collector can reclaim only state that is no longer reachable. a forgotten listener, unbounded cache, or module level registry can keep an obsolete interface and its entire object graph alive indefinitely. senior engineers must distinguish a real leak from temporary allocation pressure, caching, development tooling, and normal garbage collector behavior. the correct response begins with evidence about what retains the object—not with deleting arbitrary variables or forcing collection. core mental model think in terms of a directed graph: nodes are objects, functions, environments, dom nodes, and other managed values; edges are references between them; roots are starting points the runtime considers live; anything reachable from a root must remain available; unreachable state becomes eligible for reclamation. if registry is module scoped and remains reachable, every stored viewmodel remains reachable too. removing the ui alone does not release those entries. the most useful debugging question is: what strong reference path still connects this object to a root? garbage collection answers whether memory may be reclaimed. it does not promise when reclamation will occur or whether the process will immediately return memory to the operating system. visual model the module registry and browser listener are roots for this investigation. their strong paths keep the map entry, callback, and detached dom tree alive. the separate object cycle has no path from a root, so the cycle itself does not prevent collection. formal model allocation, retention, and reclamation these are separate events: 1. allocation: execution creates a value requiring managed storage. 2. retention: one or more reachable references keep that value alive. 3. unreachability: no strong path from a root reaches the value. 4. reclamation: the implementation eventually reuses or releases its storage. memory can rise because allocation temporarily outpaces collection without indicating a leak. a leak produces continuing unwanted reachability across completed application lifecycles. roots and retaining paths exact roots are implementation and host dependent, but useful categories include: active execution state; reachable global and module state; host registrations such as listeners and timers; framework roots and mounted component state; native or embedder references exposed through browser apis. a retaining path is a chain from one of those roots to the object being investigated. the path matters more than the syntax that created the object. collection strategy is an implementation detail a conceptual mark and sweep explanation is useful: 1. start from roots; 2. mark everything reachable through strong references; 3. reclaim unmarked storage. real engines use more sophisticated strategies, often including generational, incremental, and concurrent work. these techniques reduce pause time and collection cost, but ecmascript does not guarantee a particular algorithm, schedule, or heap layout. do not infer liveness from whether devtools happens to show an object after one operation. tooling itself can retain inspected values, and collection may not have run yet. cycles are not inherently leaks reachability based collectors can reclaim unreachable cycles: after the external references are removed, the pair can become unreachable even though the objects still reference each other. cycles become leaks only when some unwanted path from a root still reaches them. strong and weak associations a normal map strongly retains both keys and values. weakmap does not keep an object key alive solely because it is present in the collection. when an element becomes unreachable elsewhere, its weak association does not prevent collection. this makes weakmap useful for metadata whose lifetime should follow an object. weak collections deliberately cannot be enumerated. if entries could be listed, observable results would depend on nondeterministic garbage collection timing. use weak collections only when object key lifetime matches the desired ownership model. they do not fix leaks when another strong path still retains the key, and they are not a replacement for bounded caches that must be enumerable. weakref and finalization weakref allows a non owning observation of an object, and finalizationregistry can request a callback after collection. both are intentionally nondeterministic. do not use them for essential cleanup, business logic, cache correctness, or timely release of scarce external resources. prefer explicit lifecycle operations such as close , dispose , unsubscribe functions, and abortcontroller . step by step runtime walkthrough consider a modal controller: closing the modal removes its element from the document, but that does not necessarily make it collectible: 1. openmodals is reachable from module scope. 2. its entry strongly references element , model , and handleclose . 3. the callback's closure also refers to element . 4. removing the element from the dom changes document membership, not javascript reachability. 5. the entry and its reachable graph remain alive until the registry releases them. a lifecycle aware version removes both external registrations and application references: the key design improvement is explicit ownership: mounting establishes retention paths, and disposal removes them. important leak patterns unbounded caches and registries this may be intentional caching, but without eviction its lifetime is the module instance's lifetime. choose a bound, expiration policy, explicit invalidation, or object keyed weak association according to product requirements. listeners, observers, and subscriptions an event target or external store may retain its callbacks. those callbacks can retain component data through closures. cleanup must use the same callback identity that was registered. creating a new arrow during removal does not remove the original listener. timers and pending work intervals remain registered until cleared. long delay timers, observers, queued work, and pending requests can also extend lifetimes. cancellation is valuable both for memory and for preventing obsolete work from updating current ui. detached dom trees a detached node is not automatically leaked. it becomes a problem when javascript, a listener registry, a framework, or tooling still retains it after the application expects it to disappear. investigate the retaining path rather than the “detached” label alone. common misconceptions claim better explanation “garbage collection prevents memory leaks.” it reclaims unreachable state; applications determine which references remain reachable. “setting a variable to null frees memory immediately.” it removes one reference. other paths may remain, and collection timing is nondeterministic. “circular references always leak.” unreachable cycles can be collected by reachability based collectors. “a large heap proves a leak.” caches, allocation bursts, delayed collection, and tooling can also increase heap size. “a detached dom node is necessarily leaked.” it is a leak only if unwanted strong references keep it alive. “ weakmap makes its values weak.” weakness concerns reachability through object keys; values remain available while their key is live. “finalizers are reliable cleanup hooks.” their execution is delayed, nondeterministic, and may never occur before process termination. react connection react releases its own references when components unmount, but it cannot automatically clean up every external system a component contacted. the effect cleanup mirrors setup: it aborts obsolete work and removes the store's reference to the callback. when userid changes, react first cleans up the previous synchronization before establishing the next one. common react retention problems include: effects that subscribe without unsubscribing; timers or observers that survive unmount; module level maps keyed by component or request data; external stores that retain abandoned listeners; caches without bounds or invalidation; development measurements misread as production behavior. react strict mode may run an extra development setup and cleanup cycle to expose missing cleanup. it is a diagnostic signal, not the production lifecycle itself. state updates after unmount do not by themselves prove a memory leak. ask whether some external root still retains the callback or data and whether that retention is unwanted. performance and debugging distinguish allocation pressure from retention allocation pressure: many short lived values increase collection work but become unreachable normally. retention: values remain reachable across completed lifecycles. leak: retention is unintended and continues to grow or preserve obsolete state. optimize only after identifying which condition exists. a cpu profile or allocation timeline helps with allocation cost; heap snapshots and retaining paths help with retained state. use a repeatable investigation 1. define one lifecycle, such as opening and closing a modal. 2. record a baseline after the application settles. 3. repeat the lifecycle several times. 4. release the feature and allow or request collection in the diagnostic tool. 5. compare snapshots by constructor and retained size. 6. inspect retaining paths for objects that should have disappeared. 7. fix the root reference and repeat the same experiment. one snapshot rarely proves growth. repetition distinguishes one time initialization from accumulation. read memory metrics carefully shallow size is storage directly owned by an object. retained size estimates memory that could become collectible if that object and its exclusive descendants were released. dominator relationships help identify objects responsible for retaining large subgraphs. these are tooling models and estimates, not source language guarantees. devtools consoles, selected elements, framework extensions, and heap snapshots can retain values while debugging. interview questions level 1 — fundamentals question: how does javascript know when an object can be garbage collected? model answer: the useful semantic model is reachability. starting from runtime and host roots, the collector traces strong references. if no root can reach an object, it becomes eligible for reclamation. eligibility does not guarantee immediate collection or memory return to the operating system. level 2 — applied understanding question: why can a removed dom node remain in memory? model answer: removing a node from the document only removes that document relationship. javascript variables, caches, event registrations, framework state, or debugging tools may still retain it. i inspect its retaining path to find the strong reference that conflicts with the intended lifecycle. level 3 — senior reasoning question: how would you prove that repeatedly opening a screen leaks memory? model answer: i define a repeatable open close lifecycle, establish a settled baseline, repeat the lifecycle, allow collection, and compare multiple heap snapshots. i look for instances that accumulate and inspect their retaining paths and dominators. then i remove the responsible listener, cache entry, timer, subscription, or framework reference and repeat the same test. level 4 — deep follow up question: when is weakmap preferable to map ? model answer: i use weakmap for metadata associated with object keys when the collection should not own those keys and enumeration is unnecessary. it does not provide observable eviction, size, or iteration, and it does not help if another strong path retains the keys. bounded or enumerable caches usually need an explicit map policy instead. exercises 1. analyze a cycle solution the cycle can become collectible because no remaining application reference reaches either object. the mutual references do not make the cycle a root. 2. find the listener bug solution the two arrows are different function objects, so removal does not match the registered listener. store one callback in a binding and use the same identity for both operations. 3. choose a cache you need metadata for dom elements that should disappear when each element is otherwise unreachable. do you choose map or weakmap ? solution choose weakmap if the elements are object keys, the metadata should follow their lifetime, and enumeration is unnecessary. a map would strongly retain each element until explicit deletion. 4. diagnose react cleanup a component subscribes to a global store whenever roomid changes but never returns cleanup. what can accumulate, and what should the effect return? solution the store can retain one callback and its captured render data for every previous room. the effect should return the unsubscribe function—or a function that calls it—so react removes the old subscription before resynchronizing or unmounting. chapter summary garbage collection is best understood through strong reachability from roots. a leak is unwanted retention across the application's intended lifecycle. removing one reference or dom relationship does not prove an object is unreachable. cycles can be collected when no root reaches them. weak collections support non owning object key associations but are not general cache policies. finalization is nondeterministic and unsuitable for correctness critical cleanup. listeners, subscriptions, timers, registries, and caches are common retention roots. react effects should explicitly undo external synchronization. heap snapshots and retaining paths diagnose retention; repeated experiments establish accumulation. interview ready explanation javascript garbage collection is best explained with reachability. objects remain alive while a strong path connects them to a runtime or host root; unreachable objects become eligible for reclamation at an unspecified time. memory leaks therefore come from unwanted references—often listeners, subscriptions, timers, caches, closures, or module registries—not from the absence of manual free . i diagnose them by repeating a lifecycle, comparing heap snapshots, and following retaining paths to the responsible root. weakmap is useful when object keyed metadata should not own the key, but explicit cleanup and bounded storage remain the primary lifecycle tools, especially in react effects. further reading ecma 262: memory model ecma 262: weakmap objects ecma 262: weakref objects ecma 262: finalizationregistry objects mdn: memory management chrome devtools: fix memory problems react: synchronizing with effects",
  },
  {
    number: 8.5,
    slug: "09-part-one-summary",
    title: "Part I Summary",
    kind: "summary",
    partNumber: 1,
    partName: "Runtime Foundations",
    markdown:
      "# Part I Summary — Runtime Foundations\n\nThis chapter is a compact interview-ready review of the runtime foundations in Part I.\nIt is designed to be sufficient for a quick prep before a senior frontend discussion.\n\n## Core runtime model\n\n- A JavaScript runtime is ECMAScript + engine + host.\n- ECMAScript defines observable language semantics, jobs, execution contexts, and scope.\n- The engine implements and optimizes ECMAScript, but it does not own browser APIs.\n- The host supplies globals, scheduling, rendering, and environment-specific APIs.\n- Frameworks like React add scheduling policy, not a new JavaScript runtime.\n\n## What matters most\n\n- In a browser agent, one callback runs to completion.\n- After a task ends, the browser drains microtasks, may render, and then selects another task.\n- Promise reactions and `queueMicrotask` callbacks are microtasks.\n- Timer callbacks, event dispatch, and user input handlers arrive as later tasks.\n- A DOM mutation updates state; paint waits for a rendering opportunity.\n- `async` / `await` and `Promise` do not make CPU work run in parallel.\n- React transitions change update priority, not where the work executes.\n\n## Layer distinctions\n\n- **ECMAScript:** `Promise`, `Array`, `globalThis`, language semantics, jobs, execution contexts, realms.\n- **Host (browser):** `document`, timers, fetch, event loops, rendering, microtasks/tasks scheduling.\n- **Host (Node):** `process.nextTick`, event-loop phases, filesystem, `setImmediate`.\n- **Engine:** V8, SpiderMonkey, JavaScriptCore, garbage collection, inline caching, hidden classes.\n- **Framework:** React render/commit phases, batching, transitions, hooks, component scheduling.\n\n## Execution and scope\n\n- A function call normally creates a fresh execution context and invocation-specific bindings in associated environments.\n- The call stack is LIFO for synchronous calls; returns and throws unwind it.\n- Lexical scope follows where code is defined, not where it is called.\n- Identifier lookup walks lexical environment links to the nearest binding.\n- The temporal dead zone applies to uninitialized `let`, `const`, and `class` bindings.\n\n## Closures and React callbacks\n\n- Closures retain the environment where they were created.\n- Captured bindings remain reachable until no live roots refer to them.\n- In React, each render creates new bindings; a callback retains the snapshot from its render.\n- Stale callback behavior means the callback is using a previous render’s environment.\n\n## `this` and invocation form\n\n- Ordinary function `this` depends on call form:\n  - method call: receiver is base object,\n  - plain call: `undefined` in strict mode,\n  - `.call`/`.apply` override it,\n  - `.bind` creates a bound function,\n  - `new` creates a receiver.\n- Arrow functions use lexical `this` and cannot be rebound.\n\n## Memory and GC\n\n- Garbage collection is based on reachability from roots.\n- A leak is unwanted retention; removing one reference does not prove collectability.\n- Cycles can be collected when no roots reach them.\n- Weak collections do not strongly retain their object keys and are not a general cache solution.\n- React effects should clean up subscriptions, listeners, and external registrations.\n\n## Interview-ready checklist\n\n- Did I name the layer owning the behavior?\n- Did I qualify the environment: browser or Node.js?\n- Did I separate ECMAScript scheduling from host scheduling?\n- Did I distinguish DOM updates from paint?\n- Did I explain `async` / `Promise` / React transitions without implying parallelism?\n- Did I avoid conflating lexical scope with the call stack?\n\n## Short answers for common topics\n\n**What is a JavaScript runtime?**\nA runtime is ECMAScript plus an engine plus a host. The host adds APIs and scheduling that ECMAScript does not define.\n\n**Why does `.then` run later?**\nPromise resolution schedules a job, and in browsers that job becomes a microtask. Microtasks run after the current task finishes.\n\n**Why might a spinner never appear?**\nA long-running handler blocks the main thread, so the browser cannot paint the DOM update until after the handler returns.\n\n**Does React `startTransition` make work parallel?**\nNo. It lowers the update priority and defers rendering, but the JavaScript still runs on the same main thread.\n\n**What is the difference between lexical scope and the call stack?**\nLexical scope is determined by where code is written; the call stack is determined by the order of function invocation.\n\n**What is an execution context?**\nIt is specification state for one active or suspended evaluation. It is related to, but not identical to, a physical engine stack frame or a lexical environment.\n\n**What does a closure retain?**\nA closure retains access to bindings through the lexical environment where its function was created. The creating call can leave the stack while those bindings remain reachable.\n\n**How is `this` selected?**\nFor an ordinary function, start with the invocation form: method, plain, explicit, bound, or constructor call. Arrow functions instead read `this` lexically.\n\n**When can an object be garbage-collected?**\nIt becomes eligible when no strong path from a runtime or host root reaches it. Eligibility does not guarantee immediate collection.",
    headings: [
      "Core runtime model",
      "What matters most",
      "Layer distinctions",
      "Execution and scope",
      "Closures and React callbacks",
      "`this` and invocation form",
      "Memory and GC",
      "Interview-ready checklist",
      "Short answers for common topics",
    ],
    quickRefresher:
      "A JavaScript runtime is ECMAScript + engine + host. ECMAScript defines observable language semantics, jobs, execution contexts, and scope. The engine implements and optimizes ECMAScript, but it does not own browser APIs. The host supplies globals, scheduling, rendering, and environment specific APIs. Frameworks like React add scheduling policy, not a new JavaScript runtime.",
    excerpt:
      "This chapter is a compact interview ready review of the runtime foundations in Part I. It is designed to be sufficient for a quick prep before a senior frontend discussion.",
    readingMinutes: 4,
    searchText:
      "part i summary — runtime foundations this chapter is a compact interview ready review of the runtime foundations in part i. it is designed to be sufficient for a quick prep before a senior frontend discussion. core runtime model a javascript runtime is ecmascript + engine + host. ecmascript defines observable language semantics, jobs, execution contexts, and scope. the engine implements and optimizes ecmascript, but it does not own browser apis. the host supplies globals, scheduling, rendering, and environment specific apis. frameworks like react add scheduling policy, not a new javascript runtime. what matters most in a browser agent, one callback runs to completion. after a task ends, the browser drains microtasks, may render, and then selects another task. promise reactions and queuemicrotask callbacks are microtasks. timer callbacks, event dispatch, and user input handlers arrive as later tasks. a dom mutation updates state; paint waits for a rendering opportunity. async / await and promise do not make cpu work run in parallel. react transitions change update priority, not where the work executes. layer distinctions ecmascript: promise , array , globalthis , language semantics, jobs, execution contexts, realms. host (browser): document , timers, fetch, event loops, rendering, microtasks/tasks scheduling. host (node): process.nexttick , event loop phases, filesystem, setimmediate . engine: v8, spidermonkey, javascriptcore, garbage collection, inline caching, hidden classes. framework: react render/commit phases, batching, transitions, hooks, component scheduling. execution and scope a function call normally creates a fresh execution context and invocation specific bindings in associated environments. the call stack is lifo for synchronous calls; returns and throws unwind it. lexical scope follows where code is defined, not where it is called. identifier lookup walks lexical environment links to the nearest binding. the temporal dead zone applies to uninitialized let , const , and class bindings. closures and react callbacks closures retain the environment where they were created. captured bindings remain reachable until no live roots refer to them. in react, each render creates new bindings; a callback retains the snapshot from its render. stale callback behavior means the callback is using a previous render’s environment. this and invocation form ordinary function this depends on call form: method call: receiver is base object, plain call: undefined in strict mode, .call / .apply override it, .bind creates a bound function, new creates a receiver. arrow functions use lexical this and cannot be rebound. memory and gc garbage collection is based on reachability from roots. a leak is unwanted retention; removing one reference does not prove collectability. cycles can be collected when no roots reach them. weak collections do not strongly retain their object keys and are not a general cache solution. react effects should clean up subscriptions, listeners, and external registrations. interview ready checklist did i name the layer owning the behavior? did i qualify the environment: browser or node.js? did i separate ecmascript scheduling from host scheduling? did i distinguish dom updates from paint? did i explain async / promise / react transitions without implying parallelism? did i avoid conflating lexical scope with the call stack? short answers for common topics what is a javascript runtime? a runtime is ecmascript plus an engine plus a host. the host adds apis and scheduling that ecmascript does not define. why does .then run later? promise resolution schedules a job, and in browsers that job becomes a microtask. microtasks run after the current task finishes. why might a spinner never appear? a long running handler blocks the main thread, so the browser cannot paint the dom update until after the handler returns. does react starttransition make work parallel? no. it lowers the update priority and defers rendering, but the javascript still runs on the same main thread. what is the difference between lexical scope and the call stack? lexical scope is determined by where code is written; the call stack is determined by the order of function invocation. what is an execution context? it is specification state for one active or suspended evaluation. it is related to, but not identical to, a physical engine stack frame or a lexical environment. what does a closure retain? a closure retains access to bindings through the lexical environment where its function was created. the creating call can leave the stack while those bindings remain reachable. how is this selected? for an ordinary function, start with the invocation form: method, plain, explicit, bound, or constructor call. arrow functions instead read this lexically. when can an object be garbage collected? it becomes eligible when no strong path from a runtime or host root reaches it. eligibility does not guarantee immediate collection.",
  },
  {
    number: 9,
    slug: "09-primitive-values-and-objects",
    title: "Primitive Values and Objects",
    kind: "chapter",
    partNumber: 2,
    partName: "Values And Objects",
    markdown:
      '# Chapter 9 — Primitive Values and Objects\n\n## Learning objectives\n\nAfter completing this chapter, you should be able to:\n\n- classify every ECMAScript value as primitive or object;\n- explain assignment and parameter passing without saying “objects pass by reference”;\n- distinguish value equality, object identity, mutation, and reassignment;\n- explain why primitives appear to have properties and methods;\n- use `typeof`, `Object.is`, and wrapper objects precisely;\n- connect object identity to React state, props, dependencies, and memoization.\n\n## Quick Refresher\n\n- ECMAScript has seven primitive types and one Object type.\n- Primitives are immutable values without object identity.\n- Objects have identity and can contain properties and internal state.\n- Assignment and function calls copy values; an object value still identifies the same object after being copied.\n- `const` prevents rebinding, not object mutation.\n- Property access on most primitives uses temporary object-like language machinery.\n- `typeof null` is `"object"`; callable objects normally produce `"function"`.\n- React compares state and dependency values with `Object.is`, so object identity matters.\n\n## Why This Matters\n\n“Primitives are copied by value; objects are copied by reference” often predicts simple output, but it teaches the wrong mechanism. JavaScript is pass-by-value: the copied value may be an object identity, allowing two bindings to designate the same mutable object without giving a function access to the caller\'s variable.\n\nThis distinction explains mutation bugs, failed React updates, dependency churn, shallow-copy mistakes, wrapper-object surprises, and interview questions involving `NaN`, `-0`, strings, arrays, and functions.\n\n## Core Mental Model\n\nEvery JavaScript value belongs to one of these specification types:\n\n| Category  | Types                                                                | Core property                                       |\n| --------- | -------------------------------------------------------------------- | --------------------------------------------------- |\n| Primitive | Undefined, Null, Boolean, Number, BigInt, String, Symbol             | Immutable value without object identity             |\n| Object    | Ordinary objects, arrays, functions, dates, maps, promises, and more | Value with identity, properties, and internal slots |\n\nVariables do not contain “primitive boxes” or “object boxes” defined by the language. A binding holds a value. Assignment copies that value into another binding.\n\n```js\nlet count = 1;\nlet copiedCount = count;\n\nlet profile = { name: "Ada" };\nlet copiedProfile = profile;\n```\n\n`count` and `copiedCount` hold equal Number values. `profile` and `copiedProfile` hold copies of the same Object value, so both designate one object identity.\n\nUse this interview-safe phrasing:\n\n> JavaScript passes values. When the value is an object, copying it preserves the identity of that object.\n\n## Formal Model\n\n### The seven primitive types\n\n| Type      | Representative values        | Important detail                                                             |\n| --------- | ---------------------------- | ---------------------------------------------------------------------------- |\n| Undefined | `undefined`                  | Default result for many missing values and uninitialized `var` bindings      |\n| Null      | `null`                       | Intentional null value; `typeof null` is historically `"object"`             |\n| Boolean   | `true`, `false`              | Logical values                                                               |\n| Number    | `3`, `NaN`, `Infinity`, `-0` | IEEE-754 binary64 numeric type                                               |\n| BigInt    | `3n`                         | Arbitrary-precision integers; does not mix implicitly with Number arithmetic |\n| String    | `"hello"`                    | Immutable sequence of UTF-16 code units                                      |\n| Symbol    | `Symbol("id")`               | Unique primitive commonly used as a property key                             |\n\n`undefined` and `null` are both nullish, but they are different values of different types. Their application meanings are conventions: JavaScript does not universally define `undefined` as “missing” and `null` as “intentionally empty.”\n\n### Primitive immutability\n\nA primitive value cannot be changed in place:\n\n```js\nlet label = "save";\n\nlabel[0] = "S";\nconsole.log(label); // "save"\n\nlabel = "Save";\n```\n\nThe final assignment replaces the value held by `label`; it does not mutate the original string. String methods likewise return new values when a different string is needed.\n\nImmutability does not mean a binding cannot change. A `let` binding can successively hold different immutable values.\n\n### Objects have identity\n\nTwo separately created objects have different identities even when their properties look identical:\n\n```js\nconst first = { enabled: true };\nconst second = { enabled: true };\nconst alias = first;\n\nconsole.log(first === second); // false\nconsole.log(first === alias); // true\n```\n\nObjects are often mutable, but mutability is not what defines Object type. An object can be frozen, sealed, or expose only non-writable properties and still retain its identity.\n\nFunctions and arrays are objects with specialized behavior:\n\n- a function is callable and may also have properties;\n- an array has indexed properties and Array-specific internal behavior;\n- built-ins such as `Map`, `Date`, and `Promise` use internal slots not reproduced by copying visible properties.\n\n### Assignment and function arguments\n\nJavaScript does not give a called function direct access to the caller\'s binding:\n\n```js\nfunction update(profile) {\n  profile.name = "Grace";\n  profile = { name: "Lin" };\n}\n\nconst user = { name: "Ada" };\nupdate(user);\n\nconsole.log(user); // { name: "Grace" }\n```\n\nThe parameter `profile` initially receives the same Object value as `user`:\n\n1. `profile.name = "Grace"` mutates their shared object.\n2. Reassigning `profile` changes only the local parameter binding.\n3. The caller\'s `user` binding still designates the original object.\n\nIf JavaScript passed the caller\'s variable by reference, assigning a new object to `profile` would also reassign `user`. It does not.\n\n### Binding immutability versus object mutation\n\n```js\nconst settings = { theme: "dark" };\n\nsettings.theme = "light"; // allowed\nsettings = { theme: "system" }; // TypeError\n```\n\n`const` makes the binding non-reassignable after initialization. It does not freeze the object. `Object.freeze(settings)` can prevent certain own-property changes, but freezing is shallow and does not make nested objects recursively immutable.\n\n### Why primitives appear to have methods\n\nExcept for `null` and `undefined`, primitives can participate in property access:\n\n```js\nconsole.log("frontend".toUpperCase()); // FRONTEND\nconsole.log((42).toFixed(2)); // 42.00\n```\n\nThe language performs object-coercion behavior so the operation can use methods such as `String.prototype.toUpperCase`. The primitive does not permanently become a wrapper object.\n\nThis is visible when attempting to store a property on a primitive:\n\n```js\n"use strict";\n\nlet name = "Ada";\nname.role = "engineer"; // TypeError in strict mode\n```\n\nAvoid explicitly constructed primitive wrappers:\n\n```js\nconst primitive = false;\nconst wrapped = new Boolean(false);\n\nconsole.log(Boolean(primitive)); // false\nconsole.log(Boolean(wrapped)); // true: every object is truthy\nconsole.log(primitive === wrapped); // false\n```\n\nUse `String(value)`, `Number(value)`, and `Boolean(value)` for explicit conversion without `new`.\n\n### `typeof` is a classifier with exceptions\n\n| Expression              | Result        |\n| ----------------------- | ------------- |\n| `typeof undefined`      | `"undefined"` |\n| `typeof null`           | `"object"`    |\n| `typeof true`           | `"boolean"`   |\n| `typeof 1`              | `"number"`    |\n| `typeof 1n`             | `"bigint"`    |\n| `typeof "x"`            | `"string"`    |\n| `typeof Symbol()`       | `"symbol"`    |\n| `typeof {}`             | `"object"`    |\n| `typeof function () {}` | `"function"`  |\n\n`"function"` is a `typeof` result for callable objects, not an additional ECMAScript value type. Use `value === null` to test for `null`, and `Array.isArray(value)` to identify arrays across realms.\n\n### Equality preview\n\nChapter 10 examines equality and coercion in depth. For this chapter, retain three facts:\n\n- strict equality compares object identity;\n- `NaN === NaN` is `false`;\n- `0 === -0` is `true`.\n\n`Object.is` differs on the last two cases:\n\n```js\nObject.is(NaN, NaN); // true\nObject.is(0, -0); // false\n```\n\nFor most other primitive comparisons, `Object.is` behaves like strict equality. For objects, it still compares identity.\n\n## Step-by-Step Runtime Walkthrough\n\nPredict the output:\n\n```js\nfunction revise(total, order) {\n  total += 10;\n  order.total += 10;\n  order = { total: 0 };\n\n  return { total, order };\n}\n\nlet total = 20;\nconst order = { total: 20 };\n\nconst result = revise(total, order);\n\nconsole.log(total);\nconsole.log(order.total);\nconsole.log(result.total);\nconsole.log(result.order.total);\n```\n\nThe output is:\n\n```text\n20\n30\n30\n0\n```\n\nTrace the values:\n\n1. The parameter `total` receives a copy of the Number value `20`.\n2. `total += 10` reassigns only the local parameter.\n3. The parameter `order` receives the same Object value as the outer binding.\n4. `order.total += 10` mutates that shared object, so the caller observes `30`.\n5. Reassigning the parameter to `{ total: 0 }` affects only the local binding.\n6. The returned object contains the local Number value `30` and the newly created Object value.\n\nThe language uses the same pass-by-value rule in every step. The different output follows from object identity and mutation.\n\n## Important Examples\n\n### Shallow copying creates a new outer identity\n\n```js\nconst original = {\n  user: { name: "Ada" },\n  active: true,\n};\n\nconst copy = { ...original };\n\nconsole.log(copy === original); // false\nconsole.log(copy.user === original.user); // true\n\ncopy.user.name = "Grace";\nconsole.log(original.user.name); // Grace\n```\n\nSpread creates a new ordinary object and copies enumerable own property values. It does not recursively clone nested objects, preserve every property descriptor, or reproduce specialized internal slots.\n\nChapter 16 develops copying and structural sharing in detail.\n\n### Symbols are primitive identity values\n\n```js\nconst first = Symbol("id");\nconst second = Symbol("id");\n\nconsole.log(first === second); // false\n```\n\nThe description is diagnostic text, not Symbol identity. Symbols are primitives, yet separately created symbols remain distinct. Along with strings, they can serve as property keys.\n\n## Common Misconceptions\n\n| Claim                                       | Better explanation                                                                     |\n| ------------------------------------------- | -------------------------------------------------------------------------------------- |\n| “Objects are passed by reference.”          | Arguments are values; copying an Object value preserves its object identity.           |\n| “Objects are defined by mutability.”        | Objects are identity-bearing values; some objects reject or restrict mutation.         |\n| “`const` makes an object immutable.”        | It prevents rebinding; the designated object can still mutate.                         |\n| “Methods prove strings are objects.”        | Primitive property access uses object-coercion behavior; the string remains primitive. |\n| “`typeof null` proves `null` is an object.” | The result is a historical language quirk; Null is a primitive type.                   |\n| “Functions form a separate value category.” | Functions are callable objects; `"function"` is a special `typeof` result.             |\n| “Spread makes a deep clone.”                | It creates a new outer object while normally preserving nested object identities.      |\n\n## React Connection\n\nReact uses `Object.is` when deciding whether a state value changed and when comparing dependency-array entries.\n\n```jsx\nfunction ProfileEditor() {\n  const [profile, setProfile] = useState({ name: "Ada" });\n\n  function renameIncorrectly() {\n    profile.name = "Grace";\n    setProfile(profile);\n  }\n\n  function renameCorrectly() {\n    setProfile({ ...profile, name: "Grace" });\n  }\n}\n```\n\nThe first function mutates the existing object and passes the same identity back. React may treat the state as unchanged. The second creates a new outer identity that represents the update.\n\nIdentity also affects memoization and effects:\n\n```jsx\nconst options = { limit: 20 };\n\nuseEffect(() => {\n  connect(options);\n}, [options]);\n```\n\nIf this object is created during every render, the dependency changes every time even when its properties are equal. The best fix is usually to create the object inside the effect or derive primitive dependencies—not to add memoization mechanically.\n\nStable identity is not always desirable. Reusing a mutated object can hide a real change, while creating new objects unnecessarily can invalidate memoization. Match identity changes to semantic state changes.\n\n## Performance and Debugging\n\nPrimitive-versus-object syntax does not reveal exact storage. Engines may represent values using tagged machine words, registers, stack slots, optimized objects, or heap allocations. “Primitives live on the stack and objects live on the heap” is not a JavaScript guarantee and is not a reliable performance explanation.\n\nWhen state changes unexpectedly:\n\n1. Identify which bindings contain primitive values and which designate objects.\n2. Check whether multiple bindings designate the same object.\n3. Separate property mutation from variable reassignment.\n4. Inspect shallow copies for shared nested identities.\n5. In React, compare the previous and next values with `Object.is`.\n\nUse allocation and memory tools only when allocation behavior is the actual performance question. Prefer clear data ownership over speculative avoidance of object creation.\n\n## Interview Questions\n\n### Level 1 — Fundamentals\n\n**Question:** What are JavaScript\'s primitive types?\n\n**Model answer:** Undefined, Null, Boolean, Number, BigInt, String, and Symbol. Every other ECMAScript value is an Object value. Functions and arrays are specialized objects, even though callable objects produce `"function"` from `typeof`.\n\n### Level 2 — Applied understanding\n\n**Question:** Is JavaScript pass-by-value or pass-by-reference?\n\n**Model answer:** JavaScript is pass-by-value. A parameter receives a copy of the argument value. When that value is an object, both bindings designate the same object, so property mutation is shared. Reassigning the parameter does not reassign the caller\'s binding.\n\n### Level 3 — Senior reasoning\n\n**Question:** Why can mutating React state and passing it back fail to render?\n\n**Model answer:** The mutation changes properties but preserves the state object\'s identity. React compares the previous and next state with `Object.is`, so passing the same object can bail out. A state update should create a new identity for the changed path while preserving identities for unchanged data.\n\n### Level 4 — Deep follow-up\n\n**Question:** Are primitives always stored on the stack and objects on the heap?\n\n**Model answer:** ECMAScript does not specify that layout. Engines choose representations and can optimize, box, scalar-replace, or move values while preserving observable behavior. I use primitive versus object to reason about semantics—immutability and identity—not physical storage.\n\n## Exercises\n\n### 1. Mutation versus reassignment\n\n```js\nfunction change(value) {\n  value.done = true;\n  value = { done: false };\n}\n\nconst task = { done: false };\nchange(task);\nconsole.log(task.done);\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe output is `true`. The property mutation affects the shared object. Reassigning the local parameter does not change the caller\'s `task` binding.\n\n\u003c/details>\n\n### 2. Primitive property access\n\nWhy does `"hello".length` work if strings are not objects?\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nProperty access applies object-coercion behavior to the primitive so String prototype behavior and indexed properties can be used. The original value remains a primitive and is not permanently converted into a wrapper object.\n\n\u003c/details>\n\n### 3. Find the shared identity\n\n```js\nconst first = { nested: { value: 1 } };\nconst second = { ...first };\n\nsecond.nested.value = 2;\n\nconsole.log(first.nested.value);\nconsole.log(first === second);\nconsole.log(first.nested === second.nested);\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe output is `2`, `false`, and `true`. Spread created a new outer object but copied the nested Object value, preserving that nested identity.\n\n\u003c/details>\n\n### 4. Explain the wrapper\n\n```js\nif (new Boolean(false)) {\n  console.log("runs");\n}\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nIt logs `runs`. `new Boolean(false)` creates an object, and objects are truthy regardless of the wrapped Boolean value. Use the primitive `false` or `Boolean(value)` instead.\n\n\u003c/details>\n\n## Chapter Summary\n\n- ECMAScript values are either primitives or objects.\n- Primitives are immutable values; objects have identity and may be mutable.\n- JavaScript passes values, including Object values that preserve object identity when copied.\n- Property mutation and binding reassignment are different operations.\n- `const` protects a binding, not the object it designates.\n- Primitive property access does not make the primitive a persistent wrapper object.\n- `typeof` has historical and callable-object exceptions.\n- Spread creates a new outer identity but normally preserves nested identities.\n- React uses `Object.is`, making identity changes part of state and dependency design.\n\n### Interview-ready explanation\n\nJavaScript has seven primitive types and one Object type. Primitives are immutable values without object identity; objects are identity-bearing values with properties and internal behavior. JavaScript is always pass-by-value: copying an Object value makes another binding designate the same object, which is why mutation is shared while parameter reassignment is not. `const` prevents rebinding rather than object mutation, and primitive method calls use temporary object-coercion behavior. In React, state and dependencies are compared with `Object.is`, so updates should change identity exactly where semantic data changed.\n\n## Further Reading\n\n- [ECMA-262: ECMAScript Data Types and Values](https://tc39.es/ecma262/#sec-ecmascript-data-types-and-values)\n- [ECMA-262: The `typeof` Operator](https://tc39.es/ecma262/#sec-typeof-operator)\n- [ECMA-262: ToObject](https://tc39.es/ecma262/#sec-toobject)\n- [ECMA-262: SameValue](https://tc39.es/ecma262/#sec-samevalue)\n- [MDN: JavaScript Data Types and Data Structures](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Data_structures)\n- [React: Updating Objects in State](https://react.dev/learn/updating-objects-in-state)\n- [React: Referencing Values with Refs](https://react.dev/learn/referencing-values-with-refs)',
    headings: [
      "Learning objectives",
      "Quick Refresher",
      "Why This Matters",
      "Core Mental Model",
      "Formal Model",
      "Step-by-Step Runtime Walkthrough",
      "Important Examples",
      "Common Misconceptions",
      "React Connection",
      "Performance and Debugging",
      "Interview Questions",
      "Exercises",
      "Chapter Summary",
      "Further Reading",
    ],
    quickRefresher:
      'ECMAScript has seven primitive types and one Object type. Primitives are immutable values without object identity. Objects have identity and can contain properties and internal state. Assignment and function calls copy values; an object value still identifies the same object after being copied. const prevents rebinding, not object mutation. Property access on most primitives uses temporary object like language machinery. typeof null is "object" ; callable objects normally produce "function" . React compares state and dependency values with Object.is , so object identity matters.',
    excerpt:
      "“Primitives are copied by value; objects are copied by reference” often predicts simple output, but it teaches the wrong mechanism. JavaScript is pass by value: the copied value may be an object identity, allowing two bindings to designate the same mutable object without giving a function access to the caller's variable.",
    readingMinutes: 10,
    searchText:
      'chapter 9 — primitive values and objects learning objectives after completing this chapter, you should be able to: classify every ecmascript value as primitive or object; explain assignment and parameter passing without saying “objects pass by reference”; distinguish value equality, object identity, mutation, and reassignment; explain why primitives appear to have properties and methods; use typeof , object.is , and wrapper objects precisely; connect object identity to react state, props, dependencies, and memoization. quick refresher ecmascript has seven primitive types and one object type. primitives are immutable values without object identity. objects have identity and can contain properties and internal state. assignment and function calls copy values; an object value still identifies the same object after being copied. const prevents rebinding, not object mutation. property access on most primitives uses temporary object like language machinery. typeof null is "object" ; callable objects normally produce "function" . react compares state and dependency values with object.is , so object identity matters. why this matters “primitives are copied by value; objects are copied by reference” often predicts simple output, but it teaches the wrong mechanism. javascript is pass by value: the copied value may be an object identity, allowing two bindings to designate the same mutable object without giving a function access to the caller\'s variable. this distinction explains mutation bugs, failed react updates, dependency churn, shallow copy mistakes, wrapper object surprises, and interview questions involving nan , 0 , strings, arrays, and functions. core mental model every javascript value belongs to one of these specification types: category types core property primitive undefined, null, boolean, number, bigint, string, symbol immutable value without object identity object ordinary objects, arrays, functions, dates, maps, promises, and more value with identity, properties, and internal slots variables do not contain “primitive boxes” or “object boxes” defined by the language. a binding holds a value. assignment copies that value into another binding. count and copiedcount hold equal number values. profile and copiedprofile hold copies of the same object value, so both designate one object identity. use this interview safe phrasing: javascript passes values. when the value is an object, copying it preserves the identity of that object. formal model the seven primitive types type representative values important detail undefined undefined default result for many missing values and uninitialized var bindings null null intentional null value; typeof null is historically "object" boolean true , false logical values number 3 , nan , infinity , 0 ieee 754 binary64 numeric type bigint 3n arbitrary precision integers; does not mix implicitly with number arithmetic string "hello" immutable sequence of utf 16 code units symbol symbol("id") unique primitive commonly used as a property key undefined and null are both nullish, but they are different values of different types. their application meanings are conventions: javascript does not universally define undefined as “missing” and null as “intentionally empty.” primitive immutability a primitive value cannot be changed in place: the final assignment replaces the value held by label ; it does not mutate the original string. string methods likewise return new values when a different string is needed. immutability does not mean a binding cannot change. a let binding can successively hold different immutable values. objects have identity two separately created objects have different identities even when their properties look identical: objects are often mutable, but mutability is not what defines object type. an object can be frozen, sealed, or expose only non writable properties and still retain its identity. functions and arrays are objects with specialized behavior: a function is callable and may also have properties; an array has indexed properties and array specific internal behavior; built ins such as map , date , and promise use internal slots not reproduced by copying visible properties. assignment and function arguments javascript does not give a called function direct access to the caller\'s binding: the parameter profile initially receives the same object value as user : 1. profile.name = "grace" mutates their shared object. 2. reassigning profile changes only the local parameter binding. 3. the caller\'s user binding still designates the original object. if javascript passed the caller\'s variable by reference, assigning a new object to profile would also reassign user . it does not. binding immutability versus object mutation const makes the binding non reassignable after initialization. it does not freeze the object. object.freeze(settings) can prevent certain own property changes, but freezing is shallow and does not make nested objects recursively immutable. why primitives appear to have methods except for null and undefined , primitives can participate in property access: the language performs object coercion behavior so the operation can use methods such as string.prototype.touppercase . the primitive does not permanently become a wrapper object. this is visible when attempting to store a property on a primitive: avoid explicitly constructed primitive wrappers: use string(value) , number(value) , and boolean(value) for explicit conversion without new . typeof is a classifier with exceptions expression result typeof undefined "undefined" typeof null "object" typeof true "boolean" typeof 1 "number" typeof 1n "bigint" typeof "x" "string" typeof symbol() "symbol" typeof {} "object" typeof function () {} "function" "function" is a typeof result for callable objects, not an additional ecmascript value type. use value === null to test for null , and array.isarray(value) to identify arrays across realms. equality preview chapter 10 examines equality and coercion in depth. for this chapter, retain three facts: strict equality compares object identity; nan === nan is false ; 0 === 0 is true . object.is differs on the last two cases: for most other primitive comparisons, object.is behaves like strict equality. for objects, it still compares identity. step by step runtime walkthrough predict the output: the output is: trace the values: 1. the parameter total receives a copy of the number value 20 . 2. total += 10 reassigns only the local parameter. 3. the parameter order receives the same object value as the outer binding. 4. order.total += 10 mutates that shared object, so the caller observes 30 . 5. reassigning the parameter to { total: 0 } affects only the local binding. 6. the returned object contains the local number value 30 and the newly created object value. the language uses the same pass by value rule in every step. the different output follows from object identity and mutation. important examples shallow copying creates a new outer identity spread creates a new ordinary object and copies enumerable own property values. it does not recursively clone nested objects, preserve every property descriptor, or reproduce specialized internal slots. chapter 16 develops copying and structural sharing in detail. symbols are primitive identity values the description is diagnostic text, not symbol identity. symbols are primitives, yet separately created symbols remain distinct. along with strings, they can serve as property keys. common misconceptions claim better explanation “objects are passed by reference.” arguments are values; copying an object value preserves its object identity. “objects are defined by mutability.” objects are identity bearing values; some objects reject or restrict mutation. “ const makes an object immutable.” it prevents rebinding; the designated object can still mutate. “methods prove strings are objects.” primitive property access uses object coercion behavior; the string remains primitive. “ typeof null proves null is an object.” the result is a historical language quirk; null is a primitive type. “functions form a separate value category.” functions are callable objects; "function" is a special typeof result. “spread makes a deep clone.” it creates a new outer object while normally preserving nested object identities. react connection react uses object.is when deciding whether a state value changed and when comparing dependency array entries. the first function mutates the existing object and passes the same identity back. react may treat the state as unchanged. the second creates a new outer identity that represents the update. identity also affects memoization and effects: if this object is created during every render, the dependency changes every time even when its properties are equal. the best fix is usually to create the object inside the effect or derive primitive dependencies—not to add memoization mechanically. stable identity is not always desirable. reusing a mutated object can hide a real change, while creating new objects unnecessarily can invalidate memoization. match identity changes to semantic state changes. performance and debugging primitive versus object syntax does not reveal exact storage. engines may represent values using tagged machine words, registers, stack slots, optimized objects, or heap allocations. “primitives live on the stack and objects live on the heap” is not a javascript guarantee and is not a reliable performance explanation. when state changes unexpectedly: 1. identify which bindings contain primitive values and which designate objects. 2. check whether multiple bindings designate the same object. 3. separate property mutation from variable reassignment. 4. inspect shallow copies for shared nested identities. 5. in react, compare the previous and next values with object.is . use allocation and memory tools only when allocation behavior is the actual performance question. prefer clear data ownership over speculative avoidance of object creation. interview questions level 1 — fundamentals question: what are javascript\'s primitive types? model answer: undefined, null, boolean, number, bigint, string, and symbol. every other ecmascript value is an object value. functions and arrays are specialized objects, even though callable objects produce "function" from typeof . level 2 — applied understanding question: is javascript pass by value or pass by reference? model answer: javascript is pass by value. a parameter receives a copy of the argument value. when that value is an object, both bindings designate the same object, so property mutation is shared. reassigning the parameter does not reassign the caller\'s binding. level 3 — senior reasoning question: why can mutating react state and passing it back fail to render? model answer: the mutation changes properties but preserves the state object\'s identity. react compares the previous and next state with object.is , so passing the same object can bail out. a state update should create a new identity for the changed path while preserving identities for unchanged data. level 4 — deep follow up question: are primitives always stored on the stack and objects on the heap? model answer: ecmascript does not specify that layout. engines choose representations and can optimize, box, scalar replace, or move values while preserving observable behavior. i use primitive versus object to reason about semantics—immutability and identity—not physical storage. exercises 1. mutation versus reassignment solution the output is true . the property mutation affects the shared object. reassigning the local parameter does not change the caller\'s task binding. 2. primitive property access why does "hello".length work if strings are not objects? solution property access applies object coercion behavior to the primitive so string prototype behavior and indexed properties can be used. the original value remains a primitive and is not permanently converted into a wrapper object. 3. find the shared identity solution the output is 2 , false , and true . spread created a new outer object but copied the nested object value, preserving that nested identity. 4. explain the wrapper solution it logs runs . new boolean(false) creates an object, and objects are truthy regardless of the wrapped boolean value. use the primitive false or boolean(value) instead. chapter summary ecmascript values are either primitives or objects. primitives are immutable values; objects have identity and may be mutable. javascript passes values, including object values that preserve object identity when copied. property mutation and binding reassignment are different operations. const protects a binding, not the object it designates. primitive property access does not make the primitive a persistent wrapper object. typeof has historical and callable object exceptions. spread creates a new outer identity but normally preserves nested identities. react uses object.is , making identity changes part of state and dependency design. interview ready explanation javascript has seven primitive types and one object type. primitives are immutable values without object identity; objects are identity bearing values with properties and internal behavior. javascript is always pass by value: copying an object value makes another binding designate the same object, which is why mutation is shared while parameter reassignment is not. const prevents rebinding rather than object mutation, and primitive method calls use temporary object coercion behavior. in react, state and dependencies are compared with object.is , so updates should change identity exactly where semantic data changed. further reading ecma 262: ecmascript data types and values ecma 262: the typeof operator ecma 262: toobject ecma 262: samevalue mdn: javascript data types and data structures react: updating objects in state react: referencing values with refs',
  },
  {
    number: 10,
    slug: "10-equality-identity-and-coercion",
    title: "Equality, Identity, and Coercion",
    kind: "chapter",
    partNumber: 2,
    partName: "Values And Objects",
    markdown:
      '# Chapter 10 — Equality, Identity, and Coercion\n\n## Learning objectives\n\nAfter completing this chapter, you should be able to:\n\n- distinguish strict equality, loose equality, `Object.is`, and SameValueZero;\n- predict when equality compares primitive values and when it compares object identity;\n- trace coercion through `ToPrimitive`, numeric conversion, and string conversion;\n- explain the special behavior of `NaN`, signed zero, `null`, and `undefined`;\n- choose comparison and validation strategies that make application intent explicit;\n- connect equality semantics to React state, dependency arrays, `Map`, and `Set`.\n\n## Quick Refresher\n\n- Equality operators do not perform deep structural comparison.\n- `===` avoids type conversion, but `NaN !== NaN` and `0 === -0`.\n- `Object.is` treats `NaN` as equal to itself and distinguishes `0` from `-0`.\n- `Map`, `Set`, and `Array.prototype.includes` use SameValueZero: `NaN` matches itself and signed zeros match.\n- `==` follows a defined coercion algorithm; it is not simply “compare after converting both sides.”\n- Objects are equal only when both operands identify the same object.\n- Objects may be converted to primitives before arithmetic or loose comparison.\n- Explicit conversion is usually clearer at system boundaries than relying on operator coercion.\n\n## Why This Matters\n\nSenior interviews rarely stop at “always use triple equals.” They ask why `NaN` behaves differently across APIs, why two identical-looking objects are unequal, why `[] == false` is true, or why a React update using `NaN` may bail out.\n\nThe useful skill is not memorizing surprising outputs. It is selecting the correct comparison algorithm and tracing only the conversions that algorithm requires.\n\n## Core Mental Model\n\nEvaluate a comparison in three steps:\n\n1. **Name the operation.** Is it strict equality, loose equality, `Object.is`, SameValueZero, or relational comparison?\n2. **Apply its conversion rules.** Some operations compare types directly; others convert one operand or convert an object to a primitive.\n3. **Compare the resulting values.** Primitive values compare according to the chosen algorithm. Objects compare by identity.\n\nThe common equality operations differ at a few important edges:\n\n| Operation                                | Coerces different types | `NaN` equals itself | `0` equals `-0` | Objects compare by identity |\n| ---------------------------------------- | ----------------------- | ------------------- | --------------- | --------------------------- |\n| `===`                                    | No                      | No                  | Yes             | Yes                         |\n| `==`                                     | Sometimes               | No                  | Yes             | Yes                         |\n| `Object.is`                              | No                      | Yes                 | No              | Yes                         |\n| SameValueZero (`Set`, `Map`, `includes`) | No                      | Yes                 | Yes             | Yes                         |\n\nNo entry performs recursive object comparison.\n\n## Formal Model\n\n### Strict equality\n\nStrict equality normally returns `false` when operand types differ:\n\n```js\n1 === "1"; // false\nfalse === 0; // false\nnull === undefined; // false\n```\n\nFor operands of the same type:\n\n- strings compare their sequences of code units;\n- booleans compare their logical value;\n- numbers compare numerically, except `NaN` never equals anything and signed zeros compare equal;\n- BigInts compare their mathematical integer values;\n- Symbols compare their Symbol identity;\n- objects compare their object identity.\n\n```js\nconst first = { role: "admin" };\nconst second = { role: "admin" };\nconst alias = first;\n\nfirst === second; // false\nfirst === alias; // true\n```\n\n“Strict” means no type coercion. It does not mean deep, safer in every context, or mathematically perfect.\n\n### `Object.is` and SameValue\n\n`Object.is` exposes the specification\'s SameValue comparison:\n\n```js\nObject.is(NaN, NaN); // true\nObject.is(0, -0); // false\nObject.is({}, {}); // false\n```\n\nThese signed-zero semantics matter in uncommon numeric domains. In ordinary application logic, the `NaN` behavior is usually the more useful distinction.\n\nReact uses `Object.is` semantics when comparing state values and dependency entries.\n\n### SameValueZero\n\nSameValueZero combines the convenient edges of strict equality and `Object.is`: `NaN` equals itself, while `0` and `-0` remain equal.\n\n```js\n[NaN].includes(NaN); // true\n[NaN].indexOf(NaN); // -1\n\nnew Set([NaN, NaN]).size; // 1\nnew Map([[0, "zero"]]).get(-0); // "zero"\n```\n\n`includes`, `Set`, and `Map` use SameValueZero. `indexOf` uses strict equality, which is why the two array searches differ for `NaN`.\n\n### Abstract equality\n\nLoose equality is a precise algorithm that permits selected conversions when types differ.\n\nImportant rules include:\n\n- `null == undefined` is `true`, and neither is loosely equal to other values;\n- a Number and a String comparison converts the String to Number;\n- a Boolean is converted to Number before comparison;\n- an Object compared with a primitive is first converted to a primitive;\n- a Number and BigInt can compare equal when they represent the same mathematical value;\n- `NaN` remains unequal to everything.\n\n```js\n"42" == 42; // true\nfalse == 0; // true\nnull == undefined; // true\nnull == 0; // false\n42n == 42; // true\n```\n\nDo not summarize this as “JavaScript converts both values to the same type.” The algorithm chooses a conversion based on the specific pair of types, and `null == 0` demonstrates that it does not simply convert everything to Number.\n\n### Object-to-primitive conversion\n\nWhen an operation requires a primitive from an object, JavaScript applies `ToPrimitive` with a hint such as `number`, `string`, or `default`.\n\nAt a high level, conversion can use:\n\n1. `obj[Symbol.toPrimitive](hint)`, when present;\n2. otherwise, ordinary conversion that tries `valueOf` and `toString` in an order determined by the hint.\n\n```js\nconst amount = {\n  value: 40,\n  [Symbol.toPrimitive](hint) {\n    return hint === "string" ? `$${this.value}` : this.value;\n  },\n};\n\nNumber(amount); // 40\nString(amount); // "$40"\namount == 40; // true\n```\n\nThe conversion must eventually produce a primitive. Returning another object from every attempted conversion method causes a `TypeError`.\n\nMost ordinary objects stringify as `"[object Object]"`, while arrays stringify by joining their elements:\n\n```js\nString({}); // "[object Object]"\nString([]); // ""\nString([1, 2]); // "1,2"\n```\n\nThese results explain many loose-equality puzzles without requiring special array rules.\n\n### The `+` operator is overloaded\n\nBinary `+` first converts objects to primitives. If either resulting primitive is a String, it performs string concatenation; otherwise it performs numeric addition.\n\n```js\n1 + 2; // 3\n1 + "2"; // "12"\n"1" + 2 + 3; // "123"\n1 + 2 + "3"; // "33"\n```\n\nEvaluation is left-to-right. Parenthesization therefore changes which intermediate value becomes a String.\n\nBigInt and Number cannot be mixed by arithmetic:\n\n```js\n1n + 1; // TypeError\n```\n\n### Boolean contexts are not equality comparisons\n\nConditions use truthiness through Boolean conversion:\n\n```js\nif ([]) {\n  // runs: every object is truthy\n}\n\nBoolean("false"); // true\nBoolean(0); // false\n```\n\nThe falsy values are `false`, `0`, `-0`, `0n`, `NaN`, `""`, `null`, and `undefined`. Every object—including `[]`, `{}`, and `new Boolean(false)`—is truthy.\n\nDo not use `value == true` as a truthiness test. Loose equality applies its own conversion rules and can disagree with Boolean conversion.\n\n## Step-by-Step Runtime Walkthrough\n\nExplain why this is true:\n\n```js\n[] == false; // true\n```\n\nTrace the abstract equality algorithm:\n\n1. The operands are Object and Boolean.\n2. The Boolean `false` converts to Number `0`.\n3. The array must be compared with the primitive Number, so it is converted to a primitive.\n4. An empty array\'s ordinary string conversion produces `""`.\n5. Comparing String `""` with Number `0` converts the String to Number `0`.\n6. Number `0` equals Number `0`.\n\nThis result does not mean an empty array is falsy:\n\n```js\nBoolean([]); // true\n```\n\nThe condition and the loose comparison invoke different abstract operations.\n\n## Practical Comparison Patterns\n\n### Treat input parsing and validation separately\n\nBrowser inputs and URL parameters usually provide strings. Convert once at the boundary, then validate the converted value:\n\n```js\nfunction parsePage(raw) {\n  const page = Number(raw);\n\n  if (!Number.isInteger(page) || page \u003c 1) {\n    return null;\n  }\n\n  return page;\n}\n```\n\nThis is clearer than repeatedly relying on comparisons such as `raw == 1` throughout business logic.\n\n`Number.isNaN(value)` tests whether a value is specifically the Number value `NaN`. Global `isNaN(value)` converts first and therefore answers a different question.\n\n### Use the nullish pair deliberately\n\nOne defensible use of loose equality is a concise nullish check:\n\n```js\nif (value == null) {\n  // value is null or undefined\n}\n```\n\nThis works because the abstract equality algorithm makes `null` and `undefined` equal only to each other. Use it only when the team recognizes the idiom; `value === null || value === undefined` is more explicit.\n\nFor defaults, distinguish nullish values from all falsy values:\n\n```js\nconst retries = configuredRetries ?? 3;\nconst label = suppliedLabel || "Untitled";\n```\n\n`??` falls back only for `null` or `undefined`. `||` falls back for any falsy value, including `0` and `""`.\n\n### Structural equality is application-defined\n\nJavaScript has no universal built-in deep-equality operator. A correct structural comparison must define policies for prototypes, property order, Symbols, non-enumerable properties, cycles, dates, maps, sets, typed arrays, and other specialized objects.\n\n`JSON.stringify(a) === JSON.stringify(b)` is not a general deep-equality solution. Serialization omits or transforms some values, fails on cycles and BigInt, and can produce different text for objects with different property order.\n\nPrefer domain-specific comparisons or a well-tested library whose semantics match the data.\n\n## Common Misconceptions\n\n| Claim                                        | Better explanation                                                                      |\n| -------------------------------------------- | --------------------------------------------------------------------------------------- |\n| “`===` compares object contents.”            | It compares object identity.                                                            |\n| “`Object.is` is always stricter than `===`.” | It differs specifically for `NaN` and signed zero.                                      |\n| “`==` converts both sides to Number.”        | It selects conversions based on the pair of operand types.                              |\n| “Empty arrays are falsy.”                    | Every object is truthy; `[] == false` follows equality coercion, not truthiness.        |\n| “`includes` and `indexOf` compare equally.”  | `includes` uses SameValueZero; `indexOf` uses strict equality.                          |\n| “Stringifying objects gives deep equality.”  | JSON serialization has narrower, configurable semantics and important information loss. |\n| “`                                           |                                                                                         | `and`??` provide the same default.” | `   |     | `reacts to all falsy values;`??` reacts only to nullish values. |\n\n## React Connection\n\nReact compares state updates and dependency-array entries using `Object.is` semantics:\n\n```jsx\nconst [value, setValue] = useState(NaN);\n\nsetValue(NaN); // React can bail out: Object.is(NaN, NaN) is true\n```\n\nFor objects, comparison remains identity-based:\n\n```jsx\nfunction Search({ query }) {\n  const options = { query };\n\n  useEffect(() => {\n    runSearch(options);\n  }, [options]);\n}\n```\n\n`options` is a new object after every render, so the dependency differs even when `query` is unchanged. Prefer depending on `query` directly or constructing the object inside the effect.\n\nDo not replace identity comparison with deep comparison by default. Deep comparison adds cost, hides unstable ownership, and requires semantics for every nested value. First design state and dependencies so identity changes represent meaningful changes.\n\n## Performance and Debugging\n\nEquality is usually cheap; repeated coercion, allocation, serialization, or deep traversal may not be. Optimize only after identifying the actual hot path.\n\nWhen a comparison is surprising:\n\n1. Record both operand values and types.\n2. Identify the operator or API and its comparison algorithm.\n3. If coercion occurs, write each intermediate primitive explicitly.\n4. For objects, check identity before inspecting properties.\n5. Handle `NaN`, signed zero, and nullish values deliberately when relevant.\n\nAvoid debugging solely with string interpolation because it can coerce values. Log the values separately and inspect them with `typeof`, `Object.is`, and appropriate type guards.\n\n## Interview Questions\n\n### Level 1 — Fundamentals\n\n**Question:** How do `===` and `Object.is` differ?\n\n**Model answer:** Both avoid coercion and compare objects by identity. They differ for two Number cases: `Object.is(NaN, NaN)` is true while `NaN === NaN` is false, and `Object.is(0, -0)` is false while `0 === -0` is true.\n\n### Level 2 — Applied understanding\n\n**Question:** Why does `[NaN].includes(NaN)` return true while `[NaN].indexOf(NaN)` returns `-1`?\n\n**Model answer:** `includes` uses SameValueZero, under which `NaN` equals itself. `indexOf` uses strict equality, under which `NaN` is unequal to every value, including itself.\n\n### Level 3 — Senior reasoning\n\n**Question:** Is using `==` always a bug?\n\n**Model answer:** No. It follows a specified algorithm, and `value == null` can intentionally test for both nullish values. However, mixed-type coercion is easy to misread at application boundaries. I normally convert input explicitly and use strict equality, allowing loose equality only for a narrow, documented idiom.\n\n### Level 4 — Deep follow-up\n\n**Question:** Why is there no simple universal deep-equality operation for JavaScript objects?\n\n**Model answer:** Structural equality requires policy decisions: prototypes, descriptors, Symbols, key order, cycles, and specialized internal state for collections, dates, typed arrays, and other built-ins. Different domains need different definitions, so I use identity by default and a domain-specific comparator when structural equivalence is genuinely required.\n\n## Exercises\n\n### 1. Choose the algorithm\n\nPredict each result:\n\n```js\nNaN === NaN;\nObject.is(NaN, NaN);\nObject.is(0, -0);\nnew Set([0, -0]).size;\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe results are `false`, `true`, `false`, and `1`. Strict equality rejects `NaN`; SameValue accepts it and distinguishes signed zero; SameValueZero used by `Set` treats signed zeros as equal.\n\n\u003c/details>\n\n### 2. Trace coercion\n\nExplain the result:\n\n```js\n[1] == 1;\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nIt is `true`. The array converts to the primitive String `"1"`; comparison between String and Number then converts the String to Number `1`, which equals the other Number.\n\n\u003c/details>\n\n### 3. Preserve valid falsy input\n\nRepair this default so a configured value of `0` is preserved:\n\n```js\nconst delay = config.delay || 250;\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nUse `const delay = config.delay ?? 250;`. Nullish coalescing uses the default only when the value is `null` or `undefined`.\n\n\u003c/details>\n\n### 4. Diagnose the effect\n\nWhy does this effect run after every render?\n\n```jsx\nuseEffect(() => subscribe({ topic }), [{ topic }]);\n```\n\n\u003cdetails>\n\u003csummary>Solution\u003c/summary>\n\nThe object in the dependency array is newly created on every render. `Object.is` compares object identity, so every instance differs. Depend on `topic` and create the options object inside the effect.\n\n\u003c/details>\n\n## Chapter Summary\n\n- JavaScript exposes several comparison algorithms with different `NaN` and signed-zero behavior.\n- Strict equality avoids coercion but compares objects only by identity.\n- Loose equality applies defined, type-directed conversions.\n- Object-to-primitive conversion explains many arithmetic and equality surprises.\n- Truthiness is Boolean conversion, not loose equality with `true` or `false`.\n- `Map`, `Set`, and `includes` use SameValueZero.\n- React uses `Object.is` for state and dependency comparisons.\n- Structural equality needs explicit domain semantics.\n- Convert and validate external input at boundaries instead of spreading implicit coercion through application logic.\n\n### Interview-ready explanation\n\nJavaScript does not have one universal equality rule. Strict equality avoids coercion, SameValue through `Object.is` additionally equates `NaN` and distinguishes signed zero, and SameValueZero equates `NaN` while treating signed zeros alike. Loose equality follows a type-directed coercion algorithm, including object-to-primitive conversion; it is not simply “convert both sides.” Objects compare by identity under all these operations. In production code, I normally normalize external input explicitly, choose the comparison that matches the domain, and rely on structural comparison only when its semantics are clearly defined.\n\n## Further Reading\n\n- [ECMA-262: Equality Operators](https://tc39.es/ecma262/#sec-equality-operators)\n- [ECMA-262: Abstract Equality Comparison](https://tc39.es/ecma262/#sec-islooselyequal)\n- [ECMA-262: Strict Equality Comparison](https://tc39.es/ecma262/#sec-isstrictlyequal)\n- [ECMA-262: SameValue and SameValueZero](https://tc39.es/ecma262/#sec-samevalue)\n- [ECMA-262: ToPrimitive](https://tc39.es/ecma262/#sec-toprimitive)\n- [MDN: Equality Comparisons and Sameness](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Equality_comparisons_and_sameness)\n- [React: Synchronizing with Effects](https://react.dev/learn/synchronizing-with-effects)',
    headings: [
      "Learning objectives",
      "Quick Refresher",
      "Why This Matters",
      "Core Mental Model",
      "Formal Model",
      "Step-by-Step Runtime Walkthrough",
      "Practical Comparison Patterns",
      "Common Misconceptions",
      "React Connection",
      "Performance and Debugging",
      "Interview Questions",
      "Exercises",
      "Chapter Summary",
      "Further Reading",
    ],
    quickRefresher:
      "Equality operators do not perform deep structural comparison. === avoids type conversion, but NaN !== NaN and 0 === 0 . Object.is treats NaN as equal to itself and distinguishes 0 from 0 . Map , Set , and Array.prototype.includes use SameValueZero: NaN matches itself and signed zeros match. == follows a defined coercion algorithm; it is not simply “compare after converting both sides.” Objects are equal only when both operands identify the same object. Objects may be converted to primitives before arithmetic or loose comparison. Explicit conversion is usually clearer at system boundaries than relying on operator coercion.",
    excerpt:
      "Senior interviews rarely stop at “always use triple equals.” They ask why NaN behaves differently across APIs, why two identical looking objects are unequal, why [] == false is true, or why a React update using NaN may bail out.",
    readingMinutes: 10,
    searchText:
      'chapter 10 — equality, identity, and coercion learning objectives after completing this chapter, you should be able to: distinguish strict equality, loose equality, object.is , and samevaluezero; predict when equality compares primitive values and when it compares object identity; trace coercion through toprimitive , numeric conversion, and string conversion; explain the special behavior of nan , signed zero, null , and undefined ; choose comparison and validation strategies that make application intent explicit; connect equality semantics to react state, dependency arrays, map , and set . quick refresher equality operators do not perform deep structural comparison. === avoids type conversion, but nan !== nan and 0 === 0 . object.is treats nan as equal to itself and distinguishes 0 from 0 . map , set , and array.prototype.includes use samevaluezero: nan matches itself and signed zeros match. == follows a defined coercion algorithm; it is not simply “compare after converting both sides.” objects are equal only when both operands identify the same object. objects may be converted to primitives before arithmetic or loose comparison. explicit conversion is usually clearer at system boundaries than relying on operator coercion. why this matters senior interviews rarely stop at “always use triple equals.” they ask why nan behaves differently across apis, why two identical looking objects are unequal, why [] == false is true, or why a react update using nan may bail out. the useful skill is not memorizing surprising outputs. it is selecting the correct comparison algorithm and tracing only the conversions that algorithm requires. core mental model evaluate a comparison in three steps: 1. name the operation. is it strict equality, loose equality, object.is , samevaluezero, or relational comparison? 2. apply its conversion rules. some operations compare types directly; others convert one operand or convert an object to a primitive. 3. compare the resulting values. primitive values compare according to the chosen algorithm. objects compare by identity. the common equality operations differ at a few important edges: operation coerces different types nan equals itself 0 equals 0 objects compare by identity === no no yes yes == sometimes no yes yes object.is no yes no yes samevaluezero ( set , map , includes ) no yes yes yes no entry performs recursive object comparison. formal model strict equality strict equality normally returns false when operand types differ: for operands of the same type: strings compare their sequences of code units; booleans compare their logical value; numbers compare numerically, except nan never equals anything and signed zeros compare equal; bigints compare their mathematical integer values; symbols compare their symbol identity; objects compare their object identity. “strict” means no type coercion. it does not mean deep, safer in every context, or mathematically perfect. object.is and samevalue object.is exposes the specification\'s samevalue comparison: these signed zero semantics matter in uncommon numeric domains. in ordinary application logic, the nan behavior is usually the more useful distinction. react uses object.is semantics when comparing state values and dependency entries. samevaluezero samevaluezero combines the convenient edges of strict equality and object.is : nan equals itself, while 0 and 0 remain equal. includes , set , and map use samevaluezero. indexof uses strict equality, which is why the two array searches differ for nan . abstract equality loose equality is a precise algorithm that permits selected conversions when types differ. important rules include: null == undefined is true , and neither is loosely equal to other values; a number and a string comparison converts the string to number; a boolean is converted to number before comparison; an object compared with a primitive is first converted to a primitive; a number and bigint can compare equal when they represent the same mathematical value; nan remains unequal to everything. do not summarize this as “javascript converts both values to the same type.” the algorithm chooses a conversion based on the specific pair of types, and null == 0 demonstrates that it does not simply convert everything to number. object to primitive conversion when an operation requires a primitive from an object, javascript applies toprimitive with a hint such as number , string , or default . at a high level, conversion can use: 1. objsymbol.toprimitive , when present; 2. otherwise, ordinary conversion that tries valueof and tostring in an order determined by the hint. the conversion must eventually produce a primitive. returning another object from every attempted conversion method causes a typeerror . most ordinary objects stringify as "[object object]" , while arrays stringify by joining their elements: these results explain many loose equality puzzles without requiring special array rules. the + operator is overloaded binary + first converts objects to primitives. if either resulting primitive is a string, it performs string concatenation; otherwise it performs numeric addition. evaluation is left to right. parenthesization therefore changes which intermediate value becomes a string. bigint and number cannot be mixed by arithmetic: boolean contexts are not equality comparisons conditions use truthiness through boolean conversion: the falsy values are false , 0 , 0 , 0n , nan , "" , null , and undefined . every object—including [] , {} , and new boolean(false) —is truthy. do not use value == true as a truthiness test. loose equality applies its own conversion rules and can disagree with boolean conversion. step by step runtime walkthrough explain why this is true: trace the abstract equality algorithm: 1. the operands are object and boolean. 2. the boolean false converts to number 0 . 3. the array must be compared with the primitive number, so it is converted to a primitive. 4. an empty array\'s ordinary string conversion produces "" . 5. comparing string "" with number 0 converts the string to number 0 . 6. number 0 equals number 0 . this result does not mean an empty array is falsy: the condition and the loose comparison invoke different abstract operations. practical comparison patterns treat input parsing and validation separately browser inputs and url parameters usually provide strings. convert once at the boundary, then validate the converted value: this is clearer than repeatedly relying on comparisons such as raw == 1 throughout business logic. number.isnan(value) tests whether a value is specifically the number value nan . global isnan(value) converts first and therefore answers a different question. use the nullish pair deliberately one defensible use of loose equality is a concise nullish check: this works because the abstract equality algorithm makes null and undefined equal only to each other. use it only when the team recognizes the idiom; value === null value === undefined is more explicit. for defaults, distinguish nullish values from all falsy values: ?? falls back only for null or undefined . falls back for any falsy value, including 0 and "" . structural equality is application defined javascript has no universal built in deep equality operator. a correct structural comparison must define policies for prototypes, property order, symbols, non enumerable properties, cycles, dates, maps, sets, typed arrays, and other specialized objects. json.stringify(a) === json.stringify(b) is not a general deep equality solution. serialization omits or transforms some values, fails on cycles and bigint, and can produce different text for objects with different property order. prefer domain specific comparisons or a well tested library whose semantics match the data. common misconceptions claim better explanation “ === compares object contents.” it compares object identity. “ object.is is always stricter than === .” it differs specifically for nan and signed zero. “ == converts both sides to number.” it selects conversions based on the pair of operand types. “empty arrays are falsy.” every object is truthy; [] == false follows equality coercion, not truthiness. “ includes and indexof compare equally.” includes uses samevaluezero; indexof uses strict equality. “stringifying objects gives deep equality.” json serialization has narrower, configurable semantics and important information loss. “ and ?? provide the same default.” reacts to all falsy values; ?? reacts only to nullish values. react connection react compares state updates and dependency array entries using object.is semantics: for objects, comparison remains identity based: options is a new object after every render, so the dependency differs even when query is unchanged. prefer depending on query directly or constructing the object inside the effect. do not replace identity comparison with deep comparison by default. deep comparison adds cost, hides unstable ownership, and requires semantics for every nested value. first design state and dependencies so identity changes represent meaningful changes. performance and debugging equality is usually cheap; repeated coercion, allocation, serialization, or deep traversal may not be. optimize only after identifying the actual hot path. when a comparison is surprising: 1. record both operand values and types. 2. identify the operator or api and its comparison algorithm. 3. if coercion occurs, write each intermediate primitive explicitly. 4. for objects, check identity before inspecting properties. 5. handle nan , signed zero, and nullish values deliberately when relevant. avoid debugging solely with string interpolation because it can coerce values. log the values separately and inspect them with typeof , object.is , and appropriate type guards. interview questions level 1 — fundamentals question: how do === and object.is differ? model answer: both avoid coercion and compare objects by identity. they differ for two number cases: object.is(nan, nan) is true while nan === nan is false, and object.is(0, 0) is false while 0 === 0 is true. level 2 — applied understanding question: why does [nan].includes(nan) return true while [nan].indexof(nan) returns 1 ? model answer: includes uses samevaluezero, under which nan equals itself. indexof uses strict equality, under which nan is unequal to every value, including itself. level 3 — senior reasoning question: is using == always a bug? model answer: no. it follows a specified algorithm, and value == null can intentionally test for both nullish values. however, mixed type coercion is easy to misread at application boundaries. i normally convert input explicitly and use strict equality, allowing loose equality only for a narrow, documented idiom. level 4 — deep follow up question: why is there no simple universal deep equality operation for javascript objects? model answer: structural equality requires policy decisions: prototypes, descriptors, symbols, key order, cycles, and specialized internal state for collections, dates, typed arrays, and other built ins. different domains need different definitions, so i use identity by default and a domain specific comparator when structural equivalence is genuinely required. exercises 1. choose the algorithm predict each result: solution the results are false , true , false , and 1 . strict equality rejects nan ; samevalue accepts it and distinguishes signed zero; samevaluezero used by set treats signed zeros as equal. 2. trace coercion explain the result: solution it is true . the array converts to the primitive string "1" ; comparison between string and number then converts the string to number 1 , which equals the other number. 3. preserve valid falsy input repair this default so a configured value of 0 is preserved: solution use const delay = config.delay ?? 250; . nullish coalescing uses the default only when the value is null or undefined . 4. diagnose the effect why does this effect run after every render? solution the object in the dependency array is newly created on every render. object.is compares object identity, so every instance differs. depend on topic and create the options object inside the effect. chapter summary javascript exposes several comparison algorithms with different nan and signed zero behavior. strict equality avoids coercion but compares objects only by identity. loose equality applies defined, type directed conversions. object to primitive conversion explains many arithmetic and equality surprises. truthiness is boolean conversion, not loose equality with true or false . map , set , and includes use samevaluezero. react uses object.is for state and dependency comparisons. structural equality needs explicit domain semantics. convert and validate external input at boundaries instead of spreading implicit coercion through application logic. interview ready explanation javascript does not have one universal equality rule. strict equality avoids coercion, samevalue through object.is additionally equates nan and distinguishes signed zero, and samevaluezero equates nan while treating signed zeros alike. loose equality follows a type directed coercion algorithm, including object to primitive conversion; it is not simply “convert both sides.” objects compare by identity under all these operations. in production code, i normally normalize external input explicitly, choose the comparison that matches the domain, and rely on structural comparison only when its semantics are clearly defined. further reading ecma 262: equality operators ecma 262: abstract equality comparison ecma 262: strict equality comparison ecma 262: samevalue and samevaluezero ecma 262: toprimitive mdn: equality comparisons and sameness react: synchronizing with effects',
  },
];

export const performanceChapters: Chapter[] = [
  {
    number: 1,
    slug: "01-what-frontend-performance-means",
    title: "What Frontend Performance Means",
    kind: "chapter",
    partNumber: 1,
    partName: "Performance Reasoning",
    markdown:
      "# Chapter 1 — What Frontend Performance Means\n\n## Learning objectives\n\nAfter completing this chapter, you should be able to:\n\n- define frontend performance in terms of user outcomes rather than technical scores;\n- distinguish loading, responsiveness, visual stability, rendering, and resource-health problems;\n- explain the difference between perceived and measured performance;\n- separate latency, throughput, and resource consumption;\n- choose a meaningful user journey and success criterion before profiling;\n- explain why averages, local tests, and single scores can misrepresent real experience;\n- describe a disciplined performance workflow in an interview.\n\n## Quick refresher\n\n- Performance is the quality of a user experience over time, not a single Lighthouse score.\n- A page may load quickly but respond slowly, remain visually unstable, or degrade during a long session.\n- Users experience complete journeys; tools often report isolated operations and metrics.\n- Field data describes real users. Laboratory data provides controlled reproduction and diagnosis.\n- Latency measures delay, throughput measures completed work over time, and resource consumption measures the cost of producing that work.\n- An optimization is successful only when it improves a defined outcome without causing a worse tradeoff elsewhere.\n\n## Why this matters\n\nPerformance conversations often begin too late and at the wrong level. A team sees a low score, a large bundle, or a component that rendered many times and immediately proposes a familiar optimization. The proposed change may be technically valid while doing nothing noticeable for users.\n\nA senior frontend engineer starts with a different question:\n\n> What experience is failing, for which users, under which conditions, and what evidence would show that it improved?\n\nThis framing prevents three common mistakes:\n\n1. optimizing code that is not on a user-critical path;\n2. treating a diagnostic metric as the product outcome;\n3. moving cost from one part of the system to another without recognizing the tradeoff.\n\nFrontend performance is therefore not merely browser speed. It is the discipline of connecting user experience to evidence, responsible work, and verified change.\n\n## Core mental model\n\nUse the following chain when reasoning about performance:\n\n```text\nUser experience\n      ↓\nObservable symptom\n      ↓\nRelevant metric\n      ↓\nBrowser or application work\n      ↓\nTargeted change\n      ↓\nVerified outcome\n```\n\nEach arrow matters. Jumping directly from a symptom to an optimization replaces investigation with guessing.\n\nSuppose users report that product search feels slow. Several distinct problems could produce that report:\n\n- the input event waits behind a long JavaScript task;\n- the handler performs expensive filtering;\n- React renders a large subtree;\n- the browser performs costly layout after the render;\n- the application waits for a network response before acknowledging input;\n- the result arrives quickly but visual feedback is missing;\n- only low-end mobile devices are affected.\n\n“Add `useMemo`” is not yet a justified answer. The correct response begins by making the interaction reproducible and locating where its time is spent.\n\n## Performance is multidimensional\n\nA product is not simply fast or slow. It has multiple performance dimensions that can fail independently.\n\n### Loading\n\nLoading performance describes how quickly useful content and capabilities become available.\n\nImportant questions include:\n\n- When does the first meaningful content appear?\n- When does the primary content appear?\n- When can the user perform the intended action?\n- Which resources delay those moments?\n- Does a repeat visit behave differently from a first visit?\n\nA server-rendered page may show useful content early while still requiring significant JavaScript before an interaction works. A client-rendered page may download little HTML but delay visible content until scripts and data complete.\n\nDo not reduce loading to the browser `load` event. That event may occur after the user already sees and uses the page, or before an application is meaningfully ready.\n\n### Responsiveness\n\nResponsiveness describes how promptly the interface acknowledges and presents the result of an interaction.\n\nAn interaction can be delayed in three broad places:\n\n1. **Before its handler runs** because the main thread is busy.\n2. **During application processing** because handlers and related JavaScript take too long.\n3. **Before the next paint** because rendering, style, layout, or paint work is expensive.\n\nA handler completing quickly does not guarantee a responsive interface. The user observes the next visual result, not the end of the JavaScript function.\n\n### Visual stability\n\nVisual stability describes whether content remains where the user expects it to be.\n\nUnexpected movement can be caused by:\n\n- media without reserved dimensions;\n- font replacement;\n- asynchronously inserted banners or advertisements;\n- content expanding above the current viewport;\n- animations that change layout;\n- client and server output differences.\n\nA page can load quickly and still feel poor if a button moves while the user is about to select it.\n\n### Rendering smoothness\n\nRendering smoothness concerns continuous visual updates such as animation, scrolling, dragging, resizing, and data visualization.\n\nDropped frames may come from JavaScript, style calculation, layout, paint, rasterization, or excessive compositing work. The remedy depends on the responsible stage.\n\n### Resource health\n\nApplications consume network bandwidth, CPU time, memory, battery, storage, and server capacity.\n\nResource problems may not immediately appear as a slow interaction. An unbounded cache can perform well at first and fail after an hour. Frequent background polling can preserve fresh data while consuming battery and network capacity. Large client bundles can be acceptable on a development laptop but harmful on constrained devices.\n\nPerformance work must consider the entire lifecycle of the application, not only its first few seconds.\n\n## Perceived and measured performance\n\nMeasured performance describes timings and resource costs captured by tools. Perceived performance describes how users interpret the experience.\n\nThey influence each other but are not identical.\n\nConsider two save operations that both require two seconds:\n\n```text\nExperience A\nClick → nothing changes → success appears after 2 seconds\n\nExperience B\nClick → button becomes pending immediately → optimistic result appears → confirmation after 2 seconds\n```\n\nThe underlying operation has the same duration. Experience B often feels faster because it acknowledges the action and communicates progress.\n\nPerception is not permission to hide real slowness. Optimistic feedback must account for failure, and a skeleton cannot compensate for an avoidable ten-second request. The useful principle is:\n\n> Reduce real delay where possible and communicate unavoidable delay honestly.\n\nPerformance improvements can therefore include:\n\n- doing less work;\n- doing work earlier;\n- doing work later;\n- doing work in parallel;\n- moving work away from the main thread;\n- prioritizing critical work;\n- preserving previously visible content;\n- providing immediate feedback.\n\nEach technique changes when, where, or whether work happens.\n\n## Latency, throughput, and resource consumption\n\nThese terms answer different questions.\n\n### Latency\n\nLatency is the time required for one operation or journey.\n\nExamples:\n\n- time from navigation to primary content;\n- time from key press to updated results;\n- time from opening a dialog to its next painted frame;\n- time for an API request to complete.\n\nUsers directly experience latency.\n\n### Throughput\n\nThroughput is the amount of work completed during a period.\n\nExamples:\n\n- items processed per second;\n- requests handled per second;\n- frames produced per second;\n- records rendered during a batch.\n\nIncreasing throughput does not always reduce latency for one high-priority interaction. Batching more work can improve total throughput while making the first result arrive later.\n\n### Resource consumption\n\nResource consumption is the amount of bandwidth, CPU, memory, battery, storage, or server capacity required.\n\nReducing resource use can improve latency, but the relationship is not automatic. A compressed resource uses less bandwidth but requires decompression. A cache reduces repeated work but consumes memory and creates invalidation responsibilities.\n\nSenior-level performance reasoning makes these tradeoffs explicit instead of describing every reduction as an unconditional win.\n\n## Users experience journeys, not isolated functions\n\nA microbenchmark may prove that one function is faster than another. It does not prove that the application journey improved.\n\nSuppose a team replaces a 4-millisecond transformation with a 1-millisecond version. The checkout interaction still takes 900 milliseconds because it waits for a request and then performs 300 milliseconds of layout work. The transformation improved by 75 percent but the user-visible journey barely changed.\n\nDefine a journey before measuring:\n\n```text\nJourney: Search for a customer with a large account\n\nStart: A character is entered into the search field\nEnd: Updated results are painted and usable\nConditions: Mid-range mobile device, 4G network, 5,000 cached records\nSuccess: Most interactions complete within the agreed responsiveness budget\n```\n\nThis definition establishes:\n\n- the user action;\n- the observable completion point;\n- representative conditions;\n- the population or data size;\n- the success criterion.\n\nOnly then should the team decide which tools and metrics are relevant.\n\n## Field data and laboratory data\n\nField and laboratory measurements answer complementary questions.\n\n| Measurement | Best for | Limitations |\n| --- | --- | --- |\n| Field data | Understanding real users, devices, networks, routes, and long-tail experience | Less controlled; requires attribution and segmentation |\n| Laboratory data | Reproducing a problem, inspecting timelines, testing changes before release | Simulated conditions may not represent the full user population |\n\nField data can reveal that interaction latency is poor only on certain devices or routes. A laboratory trace can then explain why a reproducible interaction is slow.\n\nDo not ask which form of data is universally better. Use field data to find and prioritize real problems, then controlled measurements to diagnose and verify them.\n\n## Distributions matter more than averages\n\nAn average compresses different experiences into one value.\n\nImagine these five interaction times:\n\n```text\n80 ms, 90 ms, 100 ms, 110 ms, 1,200 ms\n```\n\nThe average is 316 milliseconds, but no user experienced 316 milliseconds. Four interactions were relatively fast and one was extremely slow.\n\nPerformance data is commonly examined through percentiles because they preserve information about the distribution. The 75th or 95th percentile can reveal whether a meaningful portion of users receive a poor experience.\n\nAlways segment carefully. A global percentile may hide:\n\n- a slow geographic region;\n- low-memory devices;\n- authenticated pages with much larger data sets;\n- first-time visitors with cold caches;\n- a particular application release;\n- one browser or operating system.\n\nSegmentation should follow a plausible hypothesis. Producing hundreds of arbitrary segments creates noise and false conclusions.\n\n## Performance budgets\n\nA performance budget converts an aspiration such as “keep the application fast” into a constraint that can guide decisions.\n\nBudgets may describe:\n\n- user-facing metrics;\n- JavaScript transferred or executed;\n- image weight;\n- request count;\n- main-thread time;\n- memory growth;\n- latency for a critical journey.\n\nA strong budget has:\n\n1. **A reason:** which user or business outcome it protects.\n2. **A scope:** which route, journey, device class, or population it applies to.\n3. **A measurement method:** how it will be reproduced or collected.\n4. **An owner:** who responds when it fails.\n5. **An action:** warning, investigation, or release prevention.\n\nA budget without an agreed response becomes decorative reporting. A strict build failure without reliable measurement becomes noise that teams learn to bypass.\n\n## A disciplined performance workflow\n\nUse this workflow for both production incidents and planned optimization.\n\n### 1. Observe\n\nRecord the user-visible symptom without prematurely naming the cause.\n\nWeak:\n\n> React is rendering too much.\n\nStronger:\n\n> After selecting a filter, the results panel does not visibly update for approximately half a second on mid-range mobile devices.\n\n### 2. Reproduce\n\nDefine the exact journey, data set, cache state, device, and network conditions.\n\n### 3. Measure\n\nCapture a baseline using a tool appropriate to the symptom. Preserve the trace or result so the same journey can be compared later.\n\n### 4. Locate\n\nDetermine where the delay occurs: network, JavaScript, React, style, layout, paint, server, or another system.\n\n### 5. Hypothesize\n\nState a falsifiable explanation:\n\n> Filtering 20,000 records and rendering every matching row creates a long main-thread task after each key press.\n\n### 6. Change\n\nApply the smallest change that tests the hypothesis. Possible changes include reducing work, changing priority, virtualizing output, moving computation, or avoiding a request waterfall.\n\n### 7. Verify\n\nRepeat the same measurement. Confirm the user-visible outcome and check that the change did not harm correctness, accessibility, memory, or another performance dimension.\n\n### 8. Monitor\n\nIf the journey matters in production, add a budget, field metric, or regression check appropriate to its risk.\n\n## Common misconceptions\n\n| Claim | Better explanation |\n| --- | --- |\n| “A high Lighthouse score means the application is fast.” | A synthetic audit is one source of evidence under specific conditions; real journeys and users may differ. |\n| “Every render is a performance bug.” | Rendering is normal React work. Investigate expensive or unnecessarily broad work that affects an outcome. |\n| “The bundle is small, so loading is fast.” | Server latency, resource discovery, priority, images, fonts, execution, and rendering also matter. |\n| “Memoization always improves performance.” | Memoization adds comparison, memory, and complexity; it helps only when avoided work is more expensive. |\n| “The average is good, so users are fine.” | Averages can hide a slow segment or long tail. Examine distributions and meaningful cohorts. |\n| “Perceived performance is fake performance.” | Immediate feedback and progressive disclosure improve communication, but should complement rather than conceal real improvements. |\n| “One local profile represents production.” | Hardware, network, data volume, browser state, and user behavior vary. |\n| “Performance work ends after optimization.” | Important journeys need ongoing monitoring and regression protection. |\n\n## Practical example: a slow search interaction\n\nUsers report that typing into a customer search field feels delayed.\n\nStart with the experience:\n\n```text\nAction: Type one character\nExpected result: Input updates immediately and results follow without blocking continued typing\nAffected users: Accounts with more than 10,000 customers\n```\n\nPossible evidence:\n\n- field monitoring shows poor interaction latency for large accounts;\n- a controlled trace shows a long task after each input event;\n- the task contains filtering followed by a large React render;\n- layout cost grows with the number of rendered rows.\n\nPossible changes:\n\n- keep the input update urgent;\n- defer result rendering;\n- move or index expensive filtering;\n- virtualize the result list;\n- avoid rendering unchanged surrounding UI.\n\nVerification must repeat the original journey with the same data and device conditions. Counting fewer renders alone is insufficient; the result must become visibly more responsive.\n\n## Performance and product decisions\n\nPerformance work competes with features, reliability, accessibility, and engineering capacity. A technically measurable improvement may not justify its cost.\n\nPrioritize using:\n\n- number of affected users;\n- severity and frequency of the delay;\n- importance of the journey;\n- business or accessibility impact;\n- confidence in the diagnosis;\n- cost and risk of the change;\n- likelihood of regression.\n\nA useful performance proposal states:\n\n```text\nProblem\nAffected population\nEvidence\nProposed change\nExpected outcome\nTradeoffs\nVerification plan\nRegression protection\n```\n\nThis turns performance from an aesthetic preference into an engineering decision.\n\n## Interview questions\n\n### Level 1 — Fundamentals\n\n**Question:** What does frontend performance mean?\n\n**Model answer:** Frontend performance is the quality of the user experience over time: how quickly useful content appears, how promptly interactions produce visual feedback, how stable the interface remains, and how responsibly the application uses device and network resources. I evaluate it through user journeys and evidence rather than one score.\n\n### Level 2 — Applied understanding\n\n**Question:** What is the difference between field and laboratory performance data?\n\n**Model answer:** Field data describes real users across their actual devices, networks, routes, and behaviors, so it is strong for identifying and prioritizing problems. Lab data provides controlled, repeatable conditions and detailed traces, so it is strong for diagnosis and before-and-after testing. I normally use them together.\n\n### Level 3 — Senior reasoning\n\n**Question:** A team reduced its JavaScript bundle by 30 percent, but users report no improvement. Why might that happen?\n\n**Model answer:** Transfer size may not have been the limiting part of the important journey. The page may be dominated by server latency, late resource discovery, image loading, JavaScript execution that remained unchanged, hydration, or post-interaction rendering. I would compare the original user-facing metric and timeline rather than treating bundle reduction as the outcome.\n\n### Level 4 — Deep follow-up\n\n**Question:** How would you decide whether a performance optimization is worth its maintenance cost?\n\n**Model answer:** I would quantify the affected population, severity, frequency, and importance of the journey; establish evidence linking the proposed change to the bottleneck; estimate implementation and regression risk; and define a measurable expected outcome. I would keep the change only if the verified benefit justifies its ongoing complexity.\n\n## Exercises\n\n### 1. Rewrite the symptom\n\nRewrite this statement without assuming a cause:\n\n> The dashboard is slow because React renders too much.\n\n\u003cdetails>\n\u003csummary>Suggested answer\u003c/summary>\n\n> After changing the date range, the dashboard does not show updated charts for approximately 800 milliseconds on representative mobile hardware.\n\nThe revised statement describes the action, visible result, timing, and conditions. Profiling can now determine whether React work is actually responsible.\n\n\u003c/details>\n\n### 2. Choose the evidence\n\nA product page performs well in local tests, but field data reports poor loading for mobile users in one region. What should you do next?\n\n\u003cdetails>\n\u003csummary>Suggested answer\u003c/summary>\n\nSegment the field data by route, device, connection, and region; identify which loading phase is slow; then reproduce representative latency and device constraints in a controlled environment. A local desktop Lighthouse run alone cannot explain the affected population.\n\n\u003c/details>\n\n### 3. Identify the tradeoff\n\nA team caches every search result indefinitely to make repeat queries instant. What new performance risks does this create?\n\n\u003cdetails>\n\u003csummary>Suggested answer\u003c/summary>\n\nThe cache can grow without bound, consume memory, retain sensitive or stale data, and increase lookup or serialization costs. The team needs a freshness policy, size limit, eviction strategy, and lifecycle appropriate to the data.\n\n\u003c/details>\n\n### 4. Define a journey\n\nChoose one important interaction in an application you know and define:\n\n- start event;\n- visible completion point;\n- representative conditions;\n- affected population;\n- success criterion.\n\nThen list the first two measurements you would collect and explain why they fit the symptom.\n\n## Chapter summary\n\n- Frontend performance is a user-experience quality, not a single score.\n- Loading, responsiveness, visual stability, smoothness, and resource health can fail independently.\n- Perceived performance and measured performance are related but not identical.\n- Latency, throughput, and resource consumption describe different dimensions.\n- Field data finds real problems; laboratory data reproduces and diagnoses them.\n- User journeys and distributions provide more useful context than isolated functions and averages.\n- A strong workflow moves from observation to reproduction, measurement, diagnosis, targeted change, verification, and monitoring.\n- Performance changes must justify their complexity and avoid regressions in correctness, accessibility, or other performance dimensions.\n\n### Interview-ready explanation\n\nFrontend performance is the quality of the experience users receive over time: how quickly useful content appears, how promptly interactions produce a visible result, how stable and smooth the interface remains, and how efficiently it uses constrained resources. I start with a specific user journey and affected population, use field data to establish whether the problem is real, use controlled traces to locate the responsible work, apply the smallest targeted change, and repeat the original measurement. An optimization is successful only when it improves the defined outcome without creating a worse tradeoff elsewhere.\n\n## Further reading\n\n- [Web Vitals](https://web.dev/articles/vitals)\n- [Core Web Vitals workflows](https://web.dev/articles/vitals-tools)\n- [Chrome DevTools Performance panel](https://developer.chrome.com/docs/devtools/performance/overview)\n- [React Performance Tracks](https://react.dev/reference/dev-tools/react-performance-tracks)",
    headings: [
      "Learning objectives",
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Performance is multidimensional",
      "Perceived and measured performance",
      "Latency, throughput, and resource consumption",
      "Users experience journeys, not isolated functions",
      "Field data and laboratory data",
      "Distributions matter more than averages",
      "Performance budgets",
      "A disciplined performance workflow",
      "Common misconceptions",
      "Practical example: a slow search interaction",
      "Performance and product decisions",
      "Interview questions",
      "Exercises",
      "Chapter summary",
      "Further reading",
    ],
    quickRefresher:
      "Performance is the quality of a user experience over time, not a single Lighthouse score. A page may load quickly but respond slowly, remain visually unstable, or degrade during a long session. Users experience complete journeys; tools often report isolated operations and metrics. Field data describes real users. Laboratory data provides controlled reproduction and diagnosis. Latency measures delay, throughput measures completed work over time, and resource consumption measures the cost of producing that work. An optimization is successful only when it improves a defined outcome without causing a worse tradeoff elsewhere.",
    excerpt:
      "Performance conversations often begin too late and at the wrong level. A team sees a low score, a large bundle, or a component that rendered many times and immediately proposes a familiar optimization. The proposed change may be technically valid while doing nothing noticeable for users.",
    readingMinutes: 14,
    searchText:
      "chapter 1 — what frontend performance means learning objectives after completing this chapter, you should be able to: define frontend performance in terms of user outcomes rather than technical scores; distinguish loading, responsiveness, visual stability, rendering, and resource health problems; explain the difference between perceived and measured performance; separate latency, throughput, and resource consumption; choose a meaningful user journey and success criterion before profiling; explain why averages, local tests, and single scores can misrepresent real experience; describe a disciplined performance workflow in an interview. quick refresher performance is the quality of a user experience over time, not a single lighthouse score. a page may load quickly but respond slowly, remain visually unstable, or degrade during a long session. users experience complete journeys; tools often report isolated operations and metrics. field data describes real users. laboratory data provides controlled reproduction and diagnosis. latency measures delay, throughput measures completed work over time, and resource consumption measures the cost of producing that work. an optimization is successful only when it improves a defined outcome without causing a worse tradeoff elsewhere. why this matters performance conversations often begin too late and at the wrong level. a team sees a low score, a large bundle, or a component that rendered many times and immediately proposes a familiar optimization. the proposed change may be technically valid while doing nothing noticeable for users. a senior frontend engineer starts with a different question: what experience is failing, for which users, under which conditions, and what evidence would show that it improved? this framing prevents three common mistakes: 1. optimizing code that is not on a user critical path; 2. treating a diagnostic metric as the product outcome; 3. moving cost from one part of the system to another without recognizing the tradeoff. frontend performance is therefore not merely browser speed. it is the discipline of connecting user experience to evidence, responsible work, and verified change. core mental model use the following chain when reasoning about performance: each arrow matters. jumping directly from a symptom to an optimization replaces investigation with guessing. suppose users report that product search feels slow. several distinct problems could produce that report: the input event waits behind a long javascript task; the handler performs expensive filtering; react renders a large subtree; the browser performs costly layout after the render; the application waits for a network response before acknowledging input; the result arrives quickly but visual feedback is missing; only low end mobile devices are affected. “add usememo ” is not yet a justified answer. the correct response begins by making the interaction reproducible and locating where its time is spent. performance is multidimensional a product is not simply fast or slow. it has multiple performance dimensions that can fail independently. loading loading performance describes how quickly useful content and capabilities become available. important questions include: when does the first meaningful content appear? when does the primary content appear? when can the user perform the intended action? which resources delay those moments? does a repeat visit behave differently from a first visit? a server rendered page may show useful content early while still requiring significant javascript before an interaction works. a client rendered page may download little html but delay visible content until scripts and data complete. do not reduce loading to the browser load event. that event may occur after the user already sees and uses the page, or before an application is meaningfully ready. responsiveness responsiveness describes how promptly the interface acknowledges and presents the result of an interaction. an interaction can be delayed in three broad places: 1. before its handler runs because the main thread is busy. 2. during application processing because handlers and related javascript take too long. 3. before the next paint because rendering, style, layout, or paint work is expensive. a handler completing quickly does not guarantee a responsive interface. the user observes the next visual result, not the end of the javascript function. visual stability visual stability describes whether content remains where the user expects it to be. unexpected movement can be caused by: media without reserved dimensions; font replacement; asynchronously inserted banners or advertisements; content expanding above the current viewport; animations that change layout; client and server output differences. a page can load quickly and still feel poor if a button moves while the user is about to select it. rendering smoothness rendering smoothness concerns continuous visual updates such as animation, scrolling, dragging, resizing, and data visualization. dropped frames may come from javascript, style calculation, layout, paint, rasterization, or excessive compositing work. the remedy depends on the responsible stage. resource health applications consume network bandwidth, cpu time, memory, battery, storage, and server capacity. resource problems may not immediately appear as a slow interaction. an unbounded cache can perform well at first and fail after an hour. frequent background polling can preserve fresh data while consuming battery and network capacity. large client bundles can be acceptable on a development laptop but harmful on constrained devices. performance work must consider the entire lifecycle of the application, not only its first few seconds. perceived and measured performance measured performance describes timings and resource costs captured by tools. perceived performance describes how users interpret the experience. they influence each other but are not identical. consider two save operations that both require two seconds: the underlying operation has the same duration. experience b often feels faster because it acknowledges the action and communicates progress. perception is not permission to hide real slowness. optimistic feedback must account for failure, and a skeleton cannot compensate for an avoidable ten second request. the useful principle is: reduce real delay where possible and communicate unavoidable delay honestly. performance improvements can therefore include: doing less work; doing work earlier; doing work later; doing work in parallel; moving work away from the main thread; prioritizing critical work; preserving previously visible content; providing immediate feedback. each technique changes when, where, or whether work happens. latency, throughput, and resource consumption these terms answer different questions. latency latency is the time required for one operation or journey. examples: time from navigation to primary content; time from key press to updated results; time from opening a dialog to its next painted frame; time for an api request to complete. users directly experience latency. throughput throughput is the amount of work completed during a period. examples: items processed per second; requests handled per second; frames produced per second; records rendered during a batch. increasing throughput does not always reduce latency for one high priority interaction. batching more work can improve total throughput while making the first result arrive later. resource consumption resource consumption is the amount of bandwidth, cpu, memory, battery, storage, or server capacity required. reducing resource use can improve latency, but the relationship is not automatic. a compressed resource uses less bandwidth but requires decompression. a cache reduces repeated work but consumes memory and creates invalidation responsibilities. senior level performance reasoning makes these tradeoffs explicit instead of describing every reduction as an unconditional win. users experience journeys, not isolated functions a microbenchmark may prove that one function is faster than another. it does not prove that the application journey improved. suppose a team replaces a 4 millisecond transformation with a 1 millisecond version. the checkout interaction still takes 900 milliseconds because it waits for a request and then performs 300 milliseconds of layout work. the transformation improved by 75 percent but the user visible journey barely changed. define a journey before measuring: this definition establishes: the user action; the observable completion point; representative conditions; the population or data size; the success criterion. only then should the team decide which tools and metrics are relevant. field data and laboratory data field and laboratory measurements answer complementary questions. measurement best for limitations field data understanding real users, devices, networks, routes, and long tail experience less controlled; requires attribution and segmentation laboratory data reproducing a problem, inspecting timelines, testing changes before release simulated conditions may not represent the full user population field data can reveal that interaction latency is poor only on certain devices or routes. a laboratory trace can then explain why a reproducible interaction is slow. do not ask which form of data is universally better. use field data to find and prioritize real problems, then controlled measurements to diagnose and verify them. distributions matter more than averages an average compresses different experiences into one value. imagine these five interaction times: the average is 316 milliseconds, but no user experienced 316 milliseconds. four interactions were relatively fast and one was extremely slow. performance data is commonly examined through percentiles because they preserve information about the distribution. the 75th or 95th percentile can reveal whether a meaningful portion of users receive a poor experience. always segment carefully. a global percentile may hide: a slow geographic region; low memory devices; authenticated pages with much larger data sets; first time visitors with cold caches; a particular application release; one browser or operating system. segmentation should follow a plausible hypothesis. producing hundreds of arbitrary segments creates noise and false conclusions. performance budgets a performance budget converts an aspiration such as “keep the application fast” into a constraint that can guide decisions. budgets may describe: user facing metrics; javascript transferred or executed; image weight; request count; main thread time; memory growth; latency for a critical journey. a strong budget has: 1. a reason: which user or business outcome it protects. 2. a scope: which route, journey, device class, or population it applies to. 3. a measurement method: how it will be reproduced or collected. 4. an owner: who responds when it fails. 5. an action: warning, investigation, or release prevention. a budget without an agreed response becomes decorative reporting. a strict build failure without reliable measurement becomes noise that teams learn to bypass. a disciplined performance workflow use this workflow for both production incidents and planned optimization. 1. observe record the user visible symptom without prematurely naming the cause. weak: react is rendering too much. stronger: after selecting a filter, the results panel does not visibly update for approximately half a second on mid range mobile devices. 2. reproduce define the exact journey, data set, cache state, device, and network conditions. 3. measure capture a baseline using a tool appropriate to the symptom. preserve the trace or result so the same journey can be compared later. 4. locate determine where the delay occurs: network, javascript, react, style, layout, paint, server, or another system. 5. hypothesize state a falsifiable explanation: filtering 20,000 records and rendering every matching row creates a long main thread task after each key press. 6. change apply the smallest change that tests the hypothesis. possible changes include reducing work, changing priority, virtualizing output, moving computation, or avoiding a request waterfall. 7. verify repeat the same measurement. confirm the user visible outcome and check that the change did not harm correctness, accessibility, memory, or another performance dimension. 8. monitor if the journey matters in production, add a budget, field metric, or regression check appropriate to its risk. common misconceptions claim better explanation “a high lighthouse score means the application is fast.” a synthetic audit is one source of evidence under specific conditions; real journeys and users may differ. “every render is a performance bug.” rendering is normal react work. investigate expensive or unnecessarily broad work that affects an outcome. “the bundle is small, so loading is fast.” server latency, resource discovery, priority, images, fonts, execution, and rendering also matter. “memoization always improves performance.” memoization adds comparison, memory, and complexity; it helps only when avoided work is more expensive. “the average is good, so users are fine.” averages can hide a slow segment or long tail. examine distributions and meaningful cohorts. “perceived performance is fake performance.” immediate feedback and progressive disclosure improve communication, but should complement rather than conceal real improvements. “one local profile represents production.” hardware, network, data volume, browser state, and user behavior vary. “performance work ends after optimization.” important journeys need ongoing monitoring and regression protection. practical example: a slow search interaction users report that typing into a customer search field feels delayed. start with the experience: possible evidence: field monitoring shows poor interaction latency for large accounts; a controlled trace shows a long task after each input event; the task contains filtering followed by a large react render; layout cost grows with the number of rendered rows. possible changes: keep the input update urgent; defer result rendering; move or index expensive filtering; virtualize the result list; avoid rendering unchanged surrounding ui. verification must repeat the original journey with the same data and device conditions. counting fewer renders alone is insufficient; the result must become visibly more responsive. performance and product decisions performance work competes with features, reliability, accessibility, and engineering capacity. a technically measurable improvement may not justify its cost. prioritize using: number of affected users; severity and frequency of the delay; importance of the journey; business or accessibility impact; confidence in the diagnosis; cost and risk of the change; likelihood of regression. a useful performance proposal states: this turns performance from an aesthetic preference into an engineering decision. interview questions level 1 — fundamentals question: what does frontend performance mean? model answer: frontend performance is the quality of the user experience over time: how quickly useful content appears, how promptly interactions produce visual feedback, how stable the interface remains, and how responsibly the application uses device and network resources. i evaluate it through user journeys and evidence rather than one score. level 2 — applied understanding question: what is the difference between field and laboratory performance data? model answer: field data describes real users across their actual devices, networks, routes, and behaviors, so it is strong for identifying and prioritizing problems. lab data provides controlled, repeatable conditions and detailed traces, so it is strong for diagnosis and before and after testing. i normally use them together. level 3 — senior reasoning question: a team reduced its javascript bundle by 30 percent, but users report no improvement. why might that happen? model answer: transfer size may not have been the limiting part of the important journey. the page may be dominated by server latency, late resource discovery, image loading, javascript execution that remained unchanged, hydration, or post interaction rendering. i would compare the original user facing metric and timeline rather than treating bundle reduction as the outcome. level 4 — deep follow up question: how would you decide whether a performance optimization is worth its maintenance cost? model answer: i would quantify the affected population, severity, frequency, and importance of the journey; establish evidence linking the proposed change to the bottleneck; estimate implementation and regression risk; and define a measurable expected outcome. i would keep the change only if the verified benefit justifies its ongoing complexity. exercises 1. rewrite the symptom rewrite this statement without assuming a cause: the dashboard is slow because react renders too much. suggested answer after changing the date range, the dashboard does not show updated charts for approximately 800 milliseconds on representative mobile hardware. the revised statement describes the action, visible result, timing, and conditions. profiling can now determine whether react work is actually responsible. 2. choose the evidence a product page performs well in local tests, but field data reports poor loading for mobile users in one region. what should you do next? suggested answer segment the field data by route, device, connection, and region; identify which loading phase is slow; then reproduce representative latency and device constraints in a controlled environment. a local desktop lighthouse run alone cannot explain the affected population. 3. identify the tradeoff a team caches every search result indefinitely to make repeat queries instant. what new performance risks does this create? suggested answer the cache can grow without bound, consume memory, retain sensitive or stale data, and increase lookup or serialization costs. the team needs a freshness policy, size limit, eviction strategy, and lifecycle appropriate to the data. 4. define a journey choose one important interaction in an application you know and define: start event; visible completion point; representative conditions; affected population; success criterion. then list the first two measurements you would collect and explain why they fit the symptom. chapter summary frontend performance is a user experience quality, not a single score. loading, responsiveness, visual stability, smoothness, and resource health can fail independently. perceived performance and measured performance are related but not identical. latency, throughput, and resource consumption describe different dimensions. field data finds real problems; laboratory data reproduces and diagnoses them. user journeys and distributions provide more useful context than isolated functions and averages. a strong workflow moves from observation to reproduction, measurement, diagnosis, targeted change, verification, and monitoring. performance changes must justify their complexity and avoid regressions in correctness, accessibility, or other performance dimensions. interview ready explanation frontend performance is the quality of the experience users receive over time: how quickly useful content appears, how promptly interactions produce a visible result, how stable and smooth the interface remains, and how efficiently it uses constrained resources. i start with a specific user journey and affected population, use field data to establish whether the problem is real, use controlled traces to locate the responsible work, apply the smallest targeted change, and repeat the original measurement. an optimization is successful only when it improves the defined outcome without creating a worse tradeoff elsewhere. further reading web vitals core web vitals workflows chrome devtools performance panel react performance tracks",
  },
];

export const reactChapters: Chapter[] = [
  {
    number: 1,
    slug: "01-render-and-commit-phases",
    title: "Render and Commit Phases",
    kind: "chapter",
    partNumber: 1,
    partName: "Rendering Model",
    markdown:
      '# Chapter 1 — Render and Commit Phases\n\n## Quick refresher\n\nReact updates the screen in two main phases:\n\n1. **Render:** React calls components and calculates the next UI tree.\n2. **Commit:** React applies the required changes to the host environment, such as the DOM.\n\nA render is a calculation. It does **not** guarantee that the DOM changes. React may render a component, compare the result with the previous tree, and conclude that no host mutation is necessary.\n\n```text\ntrigger update → render → reconcile → commit → browser paint\n```\n\n## Why this matters\n\nThis distinction explains many common React questions:\n\n- Why did the component function run when the DOM did not change?\n- Why must rendering be pure?\n- When is it safe to read or modify the DOM?\n- Why can development logs appear twice in Strict Mode?\n- When do `useLayoutEffect` and `useEffect` run?\n- How can React pause or abandon work without corrupting the application?\n\nInterviewers are usually testing your mental model, not whether you remember internal implementation details.\n\n## Core mental model\n\n### 1. An update triggers rendering\n\nThe initial root render, a state update, a parent render, a changed context value, or an external-store notification can schedule work. React begins calculating what the UI should look like next.\n\n### 2. Render calculates the next tree\n\nDuring render, React calls function components and evaluates their Hooks. The returned React elements are descriptions of UI, not DOM nodes.\n\n```tsx\ntype PriceProps = {\n  value: number;\n};\n\nfunction Price({ value }: PriceProps) {\n  const formatted = new Intl.NumberFormat("en-US", {\n    style: "currency",\n    currency: "USD",\n  }).format(value);\n\n  return \u003cspan>{formatted}\u003c/span>;\n}\n```\n\nFormatting the value is safe because it only calculates output from the current props. Rendering must be **pure and idempotent**: given the same props, state, and context, the component should return the same result without changing anything outside itself.\n\nDo not perform these operations during render:\n\n- mutate the DOM;\n- modify props, state objects, or shared module state;\n- start subscriptions or timers;\n- make uncontrolled network requests;\n- call a state setter unconditionally;\n- depend on how many times the component function has run.\n\nReact may prioritize, pause, restart, or discard render work. None of those attempts should leave observable side effects behind.\n\n### 3. Reconciliation determines what changed\n\nReact compares the next element tree with the previous one. Component type, position, and keys help React decide whether to preserve an existing component instance and its state or replace it.\n\nReconciliation belongs to the render work. It produces the set of host changes required for the commit. It is not the same thing as directly comparing every DOM node.\n\n```tsx\nfunction Status({ online }: { online: boolean }) {\n  return \u003cp className="status">{online ? "Online" : "Offline"}\u003c/p>;\n}\n```\n\nWhen `online` changes, React can reuse the existing `\u003cp>` and update only its text. If a render produces the same relevant output as before, React may have nothing to mutate in the DOM.\n\n### 4. Commit makes the result observable\n\nOnce React has a completed tree, it commits the result. Commit work is synchronous for that tree because the user must not see a partially applied UI.\n\nDuring commit, React can:\n\n- insert, update, or remove DOM nodes;\n- detach and attach refs;\n- run `useLayoutEffect` cleanup and setup at the appropriate points;\n- make the committed tree the current UI.\n\n`useLayoutEffect` runs after DOM mutations but before the browser paints. It is useful when code must measure layout and synchronously adjust the UI before the user sees it.\n\n```tsx\nfunction Tooltip() {\n  const tooltipRef = useRef\u003cHTMLDivElement>(null);\n\n  useLayoutEffect(() => {\n    const rect = tooltipRef.current?.getBoundingClientRect();\n    // Measure the committed DOM and position the tooltip.\n  }, []);\n\n  return \u003cdiv ref={tooltipRef}>Details\u003c/div>;\n}\n```\n\nUse layout Effects sparingly because synchronous work there delays painting.\n\n### 5. The browser paints, then passive Effects normally run\n\nAfter the commit, the browser can paint the updated screen. React generally runs `useEffect` after paint, although interaction-related scheduling can affect the exact timing. The important interview distinction is that an Effect runs **after a commit**, never during render.\n\n```tsx\nfunction ChatRoom({ roomId }: { roomId: string }) {\n  useEffect(() => {\n    const connection = createConnection(roomId);\n    connection.connect();\n\n    return () => connection.disconnect();\n  }, [roomId]);\n\n  return \u003ch1>Room: {roomId}\u003c/h1>;\n}\n```\n\nThis Effect synchronizes the committed component with an external system. Its cleanup runs before the Effect is set up again for changed dependencies and when the component unmounts.\n\n## Render versus commit\n\n| Question | Render phase | Commit phase |\n| --- | --- | --- |\n| Main purpose | Calculate the next UI | Apply the finished result |\n| Calls component functions | Yes | No |\n| May be interrupted or discarded | Yes | No, not for a commit already in progress |\n| May mutate the DOM | No | Yes |\n| Must remain pure | Yes | Commit-related Effects may synchronize externally |\n| User can observe the intermediate work | No | The committed result becomes observable |\n\n## Effect timing at a glance\n\n```text\nrender\n  ↓\nDOM mutations and ref updates\n  ↓\nuseLayoutEffect cleanup/setup\n  ↓\nbrowser paint\n  ↓\nuseEffect cleanup/setup (generally)\n```\n\nDo not reach for an Effect merely because some calculation follows a render. If a value can be derived from props and state, calculate it during render. Use an Effect when the component must synchronize with something outside React, such as a network connection, browser API, timer, or third-party widget.\n\n## Mount, update, and unmount\n\n- **Mount:** React renders a component for the first time and commits its host nodes.\n- **Update:** React renders an existing component again and commits only the necessary differences.\n- **Unmount:** React removes the component, runs relevant cleanup, and clears its refs.\n\nThe component function running is not itself a mount. A function can run multiple times for one eventual committed update, and an attempted render may never commit.\n\n## Strict Mode in development\n\nIn development, Strict Mode intentionally performs extra work to expose impure rendering and missing Effect cleanup. You may see component functions called more than once, or an Effect setup followed by cleanup and setup again.\n\nThis does not mean production renders everything twice. It means the code should remain correct when render logic is repeated and Effects are properly cleaned up.\n\n```tsx\nlet nextId = 0;\n\nfunction BadExample() {\n  // Bug: rendering mutates module state, so repeated renders change behavior.\n  const id = nextId++;\n  return \u003cp>Item {id}\u003c/p>;\n}\n```\n\nGenerate stable identifiers through React APIs such as `useId`, or create data identifiers when the data itself is created—not as a side effect of rendering.\n\n## Common traps\n\n- Saying “React re-rendered the DOM.” React renders components, then may commit DOM changes.\n- Assuming a component render always leads to a commit.\n- Mutating an object or global value during render.\n- Reading a ref during render to make UI decisions about the current DOM.\n- Starting requests, timers, or subscriptions in the component body.\n- Using `useEffect` for derived data and causing an avoidable second render.\n- Saying `useEffect` is guaranteed to run immediately after paint. Exact scheduling is more nuanced.\n- Treating extra Strict Mode development calls as a production bug.\n- Confusing reconciliation with the commit phase.\n\n## Interview answer\n\nReact processes an update in a render phase and a commit phase. During render, it calls components to calculate the next element tree and reconciles that tree with the previous one. Render work must stay pure because React may repeat, pause, or discard it. Once React has a completed result, the commit phase applies the required DOM mutations, updates refs, and runs layout Effects. The browser can then paint, and passive Effects generally run afterward. Therefore, a component rendering does not necessarily mean the DOM changed, and side effects must not happen during render.\n\n## Follow-up questions\n\n### Does every render update the DOM?\n\nNo. React may render and reconcile a component but find that its host output does not require a DOM mutation.\n\n### Why can React safely discard a render?\n\nRendering is expected to be pure. Until commit, React has calculated a possible next tree without making that result visible through host mutations.\n\n### Where should an API request happen?\n\nIf a user action directly causes it, an event handler is often the clearest place. If the component must synchronize with an external resource because it is mounted or an input changed, use an Effect or a framework data-fetching mechanism. Never start an uncontrolled request during render.\n\n### When would you choose `useLayoutEffect` over `useEffect`?\n\nUse `useLayoutEffect` when code must read the committed layout and synchronously adjust the UI before paint, such as positioning a tooltip. Prefer `useEffect` when the synchronization does not need to block paint.\n\n### Are Effects part of rendering?\n\nNo. Effects run only after React commits. A render that is abandoned does not run its Effects.\n\n## Check yourself\n\n1. Why may a `console.log` in a component run even when the DOM remains unchanged?\n2. What makes a calculation safe to perform during render?\n3. Why must subscriptions return cleanup from an Effect?\n4. Which Hook would you use to measure layout before paint, and what is its cost?\n5. What does Strict Mode reveal by repeating development-only work?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Render versus commit",
      "Effect timing at a glance",
      "Mount, update, and unmount",
      "Strict Mode in development",
      "Common traps",
      "Interview answer",
      "Follow-up questions",
      "Check yourself",
    ],
    quickRefresher:
      "React updates the screen in two main phases: 1. Render: React calls components and calculates the next UI tree. 2. Commit: React applies the required changes to the host environment, such as the DOM. A render is a calculation. It does not guarantee that the DOM changes. React may render a component, compare the result with the previous tree, and conclude that no host mutation is necessary.",
    excerpt: "This distinction explains many common React questions:",
    readingMinutes: 6,
    searchText:
      "chapter 1 — render and commit phases quick refresher react updates the screen in two main phases: 1. render: react calls components and calculates the next ui tree. 2. commit: react applies the required changes to the host environment, such as the dom. a render is a calculation. it does not guarantee that the dom changes. react may render a component, compare the result with the previous tree, and conclude that no host mutation is necessary. why this matters this distinction explains many common react questions: why did the component function run when the dom did not change? why must rendering be pure? when is it safe to read or modify the dom? why can development logs appear twice in strict mode? when do uselayouteffect and useeffect run? how can react pause or abandon work without corrupting the application? interviewers are usually testing your mental model, not whether you remember internal implementation details. core mental model 1. an update triggers rendering the initial root render, a state update, a parent render, a changed context value, or an external store notification can schedule work. react begins calculating what the ui should look like next. 2. render calculates the next tree during render, react calls function components and evaluates their hooks. the returned react elements are descriptions of ui, not dom nodes. formatting the value is safe because it only calculates output from the current props. rendering must be pure and idempotent : given the same props, state, and context, the component should return the same result without changing anything outside itself. do not perform these operations during render: mutate the dom; modify props, state objects, or shared module state; start subscriptions or timers; make uncontrolled network requests; call a state setter unconditionally; depend on how many times the component function has run. react may prioritize, pause, restart, or discard render work. none of those attempts should leave observable side effects behind. 3. reconciliation determines what changed react compares the next element tree with the previous one. component type, position, and keys help react decide whether to preserve an existing component instance and its state or replace it. reconciliation belongs to the render work. it produces the set of host changes required for the commit. it is not the same thing as directly comparing every dom node. when online changes, react can reuse the existing and update only its text. if a render produces the same relevant output as before, react may have nothing to mutate in the dom. 4. commit makes the result observable once react has a completed tree, it commits the result. commit work is synchronous for that tree because the user must not see a partially applied ui. during commit, react can: insert, update, or remove dom nodes; detach and attach refs; run uselayouteffect cleanup and setup at the appropriate points; make the committed tree the current ui. uselayouteffect runs after dom mutations but before the browser paints. it is useful when code must measure layout and synchronously adjust the ui before the user sees it. use layout effects sparingly because synchronous work there delays painting. 5. the browser paints, then passive effects normally run after the commit, the browser can paint the updated screen. react generally runs useeffect after paint, although interaction related scheduling can affect the exact timing. the important interview distinction is that an effect runs after a commit , never during render. this effect synchronizes the committed component with an external system. its cleanup runs before the effect is set up again for changed dependencies and when the component unmounts. render versus commit question render phase commit phase main purpose calculate the next ui apply the finished result calls component functions yes no may be interrupted or discarded yes no, not for a commit already in progress may mutate the dom no yes must remain pure yes commit related effects may synchronize externally user can observe the intermediate work no the committed result becomes observable effect timing at a glance do not reach for an effect merely because some calculation follows a render. if a value can be derived from props and state, calculate it during render. use an effect when the component must synchronize with something outside react, such as a network connection, browser api, timer, or third party widget. mount, update, and unmount mount: react renders a component for the first time and commits its host nodes. update: react renders an existing component again and commits only the necessary differences. unmount: react removes the component, runs relevant cleanup, and clears its refs. the component function running is not itself a mount. a function can run multiple times for one eventual committed update, and an attempted render may never commit. strict mode in development in development, strict mode intentionally performs extra work to expose impure rendering and missing effect cleanup. you may see component functions called more than once, or an effect setup followed by cleanup and setup again. this does not mean production renders everything twice. it means the code should remain correct when render logic is repeated and effects are properly cleaned up. generate stable identifiers through react apis such as useid , or create data identifiers when the data itself is created—not as a side effect of rendering. common traps saying “react re rendered the dom.” react renders components, then may commit dom changes. assuming a component render always leads to a commit. mutating an object or global value during render. reading a ref during render to make ui decisions about the current dom. starting requests, timers, or subscriptions in the component body. using useeffect for derived data and causing an avoidable second render. saying useeffect is guaranteed to run immediately after paint. exact scheduling is more nuanced. treating extra strict mode development calls as a production bug. confusing reconciliation with the commit phase. interview answer react processes an update in a render phase and a commit phase. during render, it calls components to calculate the next element tree and reconciles that tree with the previous one. render work must stay pure because react may repeat, pause, or discard it. once react has a completed result, the commit phase applies the required dom mutations, updates refs, and runs layout effects. the browser can then paint, and passive effects generally run afterward. therefore, a component rendering does not necessarily mean the dom changed, and side effects must not happen during render. follow up questions does every render update the dom? no. react may render and reconcile a component but find that its host output does not require a dom mutation. why can react safely discard a render? rendering is expected to be pure. until commit, react has calculated a possible next tree without making that result visible through host mutations. where should an api request happen? if a user action directly causes it, an event handler is often the clearest place. if the component must synchronize with an external resource because it is mounted or an input changed, use an effect or a framework data fetching mechanism. never start an uncontrolled request during render. when would you choose uselayouteffect over useeffect ? use uselayouteffect when code must read the committed layout and synchronously adjust the ui before paint, such as positioning a tooltip. prefer useeffect when the synchronization does not need to block paint. are effects part of rendering? no. effects run only after react commits. a render that is abandoned does not run its effects. check yourself 1. why may a console.log in a component run even when the dom remains unchanged? 2. what makes a calculation safe to perform during render? 3. why must subscriptions return cleanup from an effect? 4. which hook would you use to measure layout before paint, and what is its cost? 5. what does strict mode reveal by repeating development only work?",
  },
  {
    number: 2,
    slug: "02-what-causes-a-component-to-render",
    title: "What Causes a Component to Render",
    kind: "chapter",
    partNumber: 1,
    partName: "Rendering Model",
    markdown:
      '# Chapter 2 — What Causes a Component to Render\n\n## Quick refresher\n\nA component function can run because:\n\n- React is mounting it for the first time;\n- its own state was updated;\n- an ancestor rendered and React continued rendering through this child;\n- a context value it consumes changed;\n- an external store it subscribes to produced a new snapshot.\n\n“A component renders when its props change” is not a complete mental model. Props are values supplied during a parent render. By default, rendering a parent also renders its child components whether or not their props changed.\n\n## Why this matters\n\nInterview questions about unnecessary renders, `memo`, Context, referential equality, and React performance all depend on understanding what actually schedules or propagates rendering.\n\nYou should be able to separate three different events:\n\n1. An update is scheduled.\n2. React calls a component to calculate its next output.\n3. React commits a visible DOM change if reconciliation finds one.\n\nThese events are related, but they are not equivalent.\n\n## Core mental model\n\n```text\nupdate source\n  ↓\nReact renders the affected component\n  ↓\nReact normally continues through its descendants\n  ↓\nmemoization or another bailout may skip some work\n  ↓\nReact commits only the required host changes\n```\n\n## 1. Initial render\n\nWhen a root is rendered for the first time, React calls the components needed to construct its tree and then commits their host output.\n\n```tsx\nconst root = createRoot(document.getElementById("root")!);\nroot.render(\u003cApp />);\n```\n\nThis is the component’s initial render or mount. An update renders an already mounted component again; it does not necessarily remount it.\n\n## 2. State updates\n\nCalling a state setter requests an update. React queues the update and calculates the next state during rendering.\n\n```tsx\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  return (\n    \u003cbutton onClick={() => setCount(current => current + 1)}>\n      Count: {count}\n    \u003c/button>\n  );\n}\n```\n\nThe setter does not mutate the `count` variable in the currently running event handler. State behaves like a snapshot for that render. React normally batches multiple updates from the same interaction before rendering.\n\nReact compares the next state with the current state using `Object.is`. If they are equal, React can skip committing the update and rendering descendants. React may still need to call the component while processing an update before determining that it can bail out, so correctness must never depend on the exact call count.\n\n```tsx\nsetCount(0); // May bail out when count is already 0.\n```\n\nMutating an object and passing the same reference back is a bug because React sees the same state identity:\n\n```tsx\n// Wrong\nuser.name = "Ada";\nsetUser(user);\n\n// Correct\nsetUser(current => ({ ...current, name: "Ada" }));\n```\n\nThe same principles apply to `useReducer`: dispatching schedules an update, and returning the current state object can allow React to bail out.\n\n## 3. Parent renders\n\nBy default, when a parent renders, React evaluates the child components returned by that parent too.\n\n```tsx\nfunction Parent() {\n  const [count, setCount] = useState(0);\n\n  return (\n    \u003c>\n      \u003cbutton onClick={() => setCount(value => value + 1)}>\n        Parent count: {count}\n      \u003c/button>\n      \u003cChild label="Stable" />\n    \u003c/>\n  );\n}\n\nfunction Child({ label }: { label: string }) {\n  console.log("Child rendered");\n  return \u003cp>{label}\u003c/p>;\n}\n```\n\nUpdating `count` renders `Parent`. React then normally calls `Child`, even though `label` is still the primitive string `"Stable"`. The child rendering still may produce no DOM mutation.\n\nA prop change does not independently notify a child. The child receives new props because its parent rendered and produced a new child element description.\n\n## 4. Context changes\n\nA component that reads a context is subscribed to that context. When the nearest provider receives a different `value` according to `Object.is`, React renders its consumers.\n\n```tsx\ntype Theme = "light" | "dark";\n\nconst ThemeContext = createContext\u003cTheme>("light");\n\nfunction ThemeLabel() {\n  const theme = useContext(ThemeContext);\n  return \u003cspan>Theme: {theme}\u003c/span>;\n}\n```\n\nContext propagation is independent of a `memo` props comparison. A memoized component must still render when a context value it consumes changes.\n\nObject and function provider values deserve attention because a new reference is unequal on every render:\n\n```tsx\n// A new object is created whenever App renders.\n\u003cSettingsContext.Provider value={{ locale, setLocale }}>\n  {children}\n\u003c/SettingsContext.Provider>\n```\n\nThis is not automatically a performance problem. Measure first. If the provider has broad, expensive consumers, split contexts by responsibility or stabilize the value where that provides a demonstrated benefit.\n\n## 5. External-store subscriptions\n\nData outside React can request rendering through a subscription. The supported low-level integration is `useSyncExternalStore`.\n\n```tsx\nfunction OnlineStatus() {\n  const online = useSyncExternalStore(\n    subscribeToNetwork,\n    getNetworkSnapshot,\n    getServerNetworkSnapshot,\n  );\n\n  return \u003cp>{online ? "Online" : "Offline"}\u003c/p>;\n}\n```\n\nReact calls `getSnapshot` and compares the returned value with the previous snapshot using `Object.is`. The snapshot must be stable while the store has not changed. Returning a fresh object every time can create repeated updates.\n\nState-management libraries commonly implement equivalent subscription behavior for you.\n\n## Rendering descendants and bailouts\n\nReact normally walks from an updated component into the elements it returns. It can skip some work when identity and inputs prove that recalculation is unnecessary.\n\nCommon bailout mechanisms include:\n\n- setting state to a value equal to the current value;\n- `memo` skipping a component when its props compare equal;\n- preserving an already created React element object;\n- framework or compiler optimizations that safely reuse previous work.\n\nThese are optimizations, not new application semantics. Your component must behave correctly even if React renders it again.\n\n## What `memo` actually does\n\n`memo` can skip a child render when its parent renders and the child’s props are equal.\n\n```tsx\nconst Child = memo(function Child({ label }: { label: string }) {\n  return \u003cp>{label}\u003c/p>;\n});\n```\n\nBy default, React compares each prop using `Object.is`. Primitive props often remain equal naturally. Inline objects, arrays, and functions create new references:\n\n```tsx\n// These props are new references on each Parent render.\n\u003cChart options={{ showLegend: true }} onSelect={() => selectItem(id)} />\n```\n\n`useMemo` and `useCallback` can stabilize those references, but adding them everywhere increases complexity and may provide no measurable benefit. Use the React Profiler to find expensive render paths first.\n\n`memo` does not prevent rendering caused by:\n\n- the component’s own state update;\n- a context change the component consumes;\n- an external-store subscription update;\n- props that are genuinely different.\n\nIt is also a performance optimization rather than a correctness guarantee.\n\n## Render trigger comparison\n\n| Source | What changed? | Important nuance |\n| --- | --- | --- |\n| Initial mount | The component enters the tree | Produces its first committed output |\n| Local state | A queued state value | Equal state can allow a bailout |\n| Parent render | An ancestor is recalculating its subtree | Child props do not have to change |\n| Context | A consumed provider value | Can render a memoized consumer |\n| External store | The subscribed snapshot | Snapshot identity must be stable |\n\n## How to diagnose an unexpected render\n\nUse a repeatable process instead of adding memoization immediately:\n\n1. Confirm the render with React DevTools Profiler.\n2. Identify whether state, a parent, context, or an external store initiated it.\n3. Inspect which props or context values changed identity.\n4. Decide whether the render is actually expensive or user-visible.\n5. Reduce state scope, split context, stabilize an input, or memoize only when the measurement justifies it.\n6. Profile again to verify the result.\n\nA frequent improvement is moving rapidly changing state closer to the components that need it. This reduces the size of the subtree reached by each update without adding comparison overhead.\n\n## Common traps\n\n- Saying a component renders only when its props change.\n- Treating changed props as an independent notification mechanism.\n- Assuming every component render changes the DOM.\n- Assuming `memo` blocks state or context updates.\n- Mutating state and returning the same object reference.\n- Returning a fresh object from `useSyncExternalStore` on every read.\n- Memoizing every value without measuring the render cost.\n- Using a custom `memo` comparison that ignores a function prop and captures stale state.\n- Confusing a re-render with a remount and state reset.\n- Using console logs alone to evaluate development behavior under Strict Mode.\n\n## Interview answer\n\nA component renders when it mounts and when React processes an update from its own state, an ancestor render, a context it consumes, or a subscribed external store. Props do not independently trigger rendering; a component receives new props when its parent renders. By default React continues rendering through descendants, although equal state or memoization can create bailouts. `memo` only compares props and does not block the component’s own state, consumed context, or store updates. Finally, rendering is only calculation—the DOM changes only if reconciliation produces something to commit.\n\n## Follow-up questions\n\n### If a parent renders, does every descendant render?\n\nReact normally follows the elements returned by the parent and renders their component children. Bailouts such as `memo`, equal state, or reused element identity can skip parts of the tree.\n\n### Does changing a regular variable cause a render?\n\nNo. React does not track arbitrary local variables or ref mutations. A render must be scheduled through state, context, an external subscription, or a new root render.\n\n### Does changing a ref cause a render?\n\nNo. Updating `ref.current` is intentionally mutable and does not notify React. Use state when a value must affect rendered output.\n\n### Can `memo` stop a context-driven render?\n\nNot when the memoized component itself consumes the changed context. The props comparison and context subscription are separate.\n\n### Why can an inline function defeat `memo`?\n\nThe parent creates a new function object on every render. Since `Object.is` sees a different reference, the memoized child’s props are not equal.\n\n### Is an extra render always a performance problem?\n\nNo. Many renders are cheap, and React may commit no DOM changes. Optimize a measured bottleneck rather than render counts in isolation.\n\n## Check yourself\n\n1. Why can a child render when all its primitive props have the same values?\n2. What is the difference between a component re-rendering and remounting?\n3. Why can returning the same mutated state object prevent an expected update?\n4. Which updates can still render a component wrapped in `memo`?\n5. Why must an external-store snapshot have stable identity?\n6. What should you measure before introducing `useMemo` or `useCallback`?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "1. Initial render",
      "2. State updates",
      "3. Parent renders",
      "4. Context changes",
      "5. External-store subscriptions",
      "Rendering descendants and bailouts",
      "What `memo` actually does",
      "Render trigger comparison",
      "How to diagnose an unexpected render",
      "Common traps",
      "Interview answer",
      "Follow-up questions",
      "Check yourself",
    ],
    quickRefresher:
      "A component function can run because: React is mounting it for the first time; its own state was updated; an ancestor rendered and React continued rendering through this child; a context value it consumes changed; an external store it subscribes to produced a new snapshot. “A component renders when its props change” is not a complete mental model. Props are values supplied during a parent render. By default, rendering a parent also renders its child components whether or not their props changed.",
    excerpt:
      "Interview questions about unnecessary renders, memo , Context, referential equality, and React performance all depend on understanding what actually schedules or propagates rendering.",
    readingMinutes: 7,
    searchText:
      'chapter 2 — what causes a component to render quick refresher a component function can run because: react is mounting it for the first time; its own state was updated; an ancestor rendered and react continued rendering through this child; a context value it consumes changed; an external store it subscribes to produced a new snapshot. “a component renders when its props change” is not a complete mental model. props are values supplied during a parent render. by default, rendering a parent also renders its child components whether or not their props changed. why this matters interview questions about unnecessary renders, memo , context, referential equality, and react performance all depend on understanding what actually schedules or propagates rendering. you should be able to separate three different events: 1. an update is scheduled. 2. react calls a component to calculate its next output. 3. react commits a visible dom change if reconciliation finds one. these events are related, but they are not equivalent. core mental model 1. initial render when a root is rendered for the first time, react calls the components needed to construct its tree and then commits their host output. this is the component’s initial render or mount. an update renders an already mounted component again; it does not necessarily remount it. 2. state updates calling a state setter requests an update. react queues the update and calculates the next state during rendering. the setter does not mutate the count variable in the currently running event handler. state behaves like a snapshot for that render. react normally batches multiple updates from the same interaction before rendering. react compares the next state with the current state using object.is . if they are equal, react can skip committing the update and rendering descendants. react may still need to call the component while processing an update before determining that it can bail out, so correctness must never depend on the exact call count. mutating an object and passing the same reference back is a bug because react sees the same state identity: the same principles apply to usereducer : dispatching schedules an update, and returning the current state object can allow react to bail out. 3. parent renders by default, when a parent renders, react evaluates the child components returned by that parent too. updating count renders parent . react then normally calls child , even though label is still the primitive string "stable" . the child rendering still may produce no dom mutation. a prop change does not independently notify a child. the child receives new props because its parent rendered and produced a new child element description. 4. context changes a component that reads a context is subscribed to that context. when the nearest provider receives a different value according to object.is , react renders its consumers. context propagation is independent of a memo props comparison. a memoized component must still render when a context value it consumes changes. object and function provider values deserve attention because a new reference is unequal on every render: this is not automatically a performance problem. measure first. if the provider has broad, expensive consumers, split contexts by responsibility or stabilize the value where that provides a demonstrated benefit. 5. external store subscriptions data outside react can request rendering through a subscription. the supported low level integration is usesyncexternalstore . react calls getsnapshot and compares the returned value with the previous snapshot using object.is . the snapshot must be stable while the store has not changed. returning a fresh object every time can create repeated updates. state management libraries commonly implement equivalent subscription behavior for you. rendering descendants and bailouts react normally walks from an updated component into the elements it returns. it can skip some work when identity and inputs prove that recalculation is unnecessary. common bailout mechanisms include: setting state to a value equal to the current value; memo skipping a component when its props compare equal; preserving an already created react element object; framework or compiler optimizations that safely reuse previous work. these are optimizations, not new application semantics. your component must behave correctly even if react renders it again. what memo actually does memo can skip a child render when its parent renders and the child’s props are equal. by default, react compares each prop using object.is . primitive props often remain equal naturally. inline objects, arrays, and functions create new references: usememo and usecallback can stabilize those references, but adding them everywhere increases complexity and may provide no measurable benefit. use the react profiler to find expensive render paths first. memo does not prevent rendering caused by: the component’s own state update; a context change the component consumes; an external store subscription update; props that are genuinely different. it is also a performance optimization rather than a correctness guarantee. render trigger comparison source what changed? important nuance initial mount the component enters the tree produces its first committed output local state a queued state value equal state can allow a bailout parent render an ancestor is recalculating its subtree child props do not have to change context a consumed provider value can render a memoized consumer external store the subscribed snapshot snapshot identity must be stable how to diagnose an unexpected render use a repeatable process instead of adding memoization immediately: 1. confirm the render with react devtools profiler. 2. identify whether state, a parent, context, or an external store initiated it. 3. inspect which props or context values changed identity. 4. decide whether the render is actually expensive or user visible. 5. reduce state scope, split context, stabilize an input, or memoize only when the measurement justifies it. 6. profile again to verify the result. a frequent improvement is moving rapidly changing state closer to the components that need it. this reduces the size of the subtree reached by each update without adding comparison overhead. common traps saying a component renders only when its props change. treating changed props as an independent notification mechanism. assuming every component render changes the dom. assuming memo blocks state or context updates. mutating state and returning the same object reference. returning a fresh object from usesyncexternalstore on every read. memoizing every value without measuring the render cost. using a custom memo comparison that ignores a function prop and captures stale state. confusing a re render with a remount and state reset. using console logs alone to evaluate development behavior under strict mode. interview answer a component renders when it mounts and when react processes an update from its own state, an ancestor render, a context it consumes, or a subscribed external store. props do not independently trigger rendering; a component receives new props when its parent renders. by default react continues rendering through descendants, although equal state or memoization can create bailouts. memo only compares props and does not block the component’s own state, consumed context, or store updates. finally, rendering is only calculation—the dom changes only if reconciliation produces something to commit. follow up questions if a parent renders, does every descendant render? react normally follows the elements returned by the parent and renders their component children. bailouts such as memo , equal state, or reused element identity can skip parts of the tree. does changing a regular variable cause a render? no. react does not track arbitrary local variables or ref mutations. a render must be scheduled through state, context, an external subscription, or a new root render. does changing a ref cause a render? no. updating ref.current is intentionally mutable and does not notify react. use state when a value must affect rendered output. can memo stop a context driven render? not when the memoized component itself consumes the changed context. the props comparison and context subscription are separate. why can an inline function defeat memo ? the parent creates a new function object on every render. since object.is sees a different reference, the memoized child’s props are not equal. is an extra render always a performance problem? no. many renders are cheap, and react may commit no dom changes. optimize a measured bottleneck rather than render counts in isolation. check yourself 1. why can a child render when all its primitive props have the same values? 2. what is the difference between a component re rendering and remounting? 3. why can returning the same mutated state object prevent an expected update? 4. which updates can still render a component wrapped in memo ? 5. why must an external store snapshot have stable identity? 6. what should you measure before introducing usememo or usecallback ?',
  },
  {
    number: 3,
    slug: "03-reconciliation-and-element-identity",
    title: "Reconciliation and Element Identity",
    kind: "chapter",
    partNumber: 1,
    partName: "Rendering Model",
    markdown:
      '# Chapter 3 — Reconciliation and Element Identity\n\n## Quick refresher\n\nReconciliation is how React matches the next element tree with the previously committed tree. The result determines whether React:\n\n- updates an existing component and preserves its state;\n- mounts a new component with fresh state;\n- unmounts an old component and runs its cleanup;\n- reuses, updates, inserts, moves, or removes host nodes.\n\nReact primarily reasons about a component’s **type**, **position in the tree**, and **key**. State belongs to a component identity at a tree position—not to a JSX tag or a variable name by itself.\n\n## Why this matters\n\nThis mental model explains several frequent interview problems:\n\n- Why did form state unexpectedly reset?\n- Why did state remain when conditional content changed?\n- Why are list keys important?\n- How can a key intentionally reset a component?\n- Why is defining a component inside another component dangerous?\n- Why did an Effect clean up and run again?\n\nThe useful interview answer is not “React compares the virtual DOM.” You should explain what React uses to decide whether two elements represent the same component identity.\n\n## Core mental model\n\n```text\nsame tree position + same element type + same key\n  → preserve component identity and local state\n\ndifferent type or different key at that position\n  → unmount old subtree and mount a new subtree\n```\n\nKeys are part of position. Without an explicit key, React primarily uses the element’s order among its siblings.\n\n## Elements, components, and instances\n\nThese terms are related but distinct:\n\n- A **React element** is an immutable description such as `\u003cUserCard user={user} />`.\n- A **component type** is the function or class referenced by that element, such as `UserCard`.\n- A mounted **component instance** is React’s preserved identity for that type at a particular tree position. Function components do not expose a JavaScript instance object, but React still preserves their Hook state and lifecycle identity.\n- A **host node** is a platform element such as a DOM `\u003cdiv>`.\n\nCalling a component function again normally updates the same component identity. It does not imply a remount.\n\n## Rule 1: Different types replace the subtree\n\nWhen the element type at a position changes, React treats it as a different subtree.\n\n```tsx\nfunction Profile({ compact }: { compact: boolean }) {\n  return compact ? \u003cCompactProfile /> : \u003cFullProfile />;\n}\n```\n\nSwitching `compact` changes the root component type at that position. React unmounts the old profile and mounts the other one.\n\nConsequences include:\n\n- local state is discarded;\n- Effect cleanup runs for the removed subtree;\n- refs are cleared;\n- old DOM nodes may be removed and new ones created;\n- the new subtree starts with fresh state and Effects.\n\nThe same rule applies to host elements:\n\n```tsx\nreturn emphasized ? \u003cstrong>{label}\u003c/strong> : \u003cspan>{label}\u003c/span>;\n```\n\nChanging from `strong` to `span` replaces that host subtree rather than updating one tag into another.\n\n## Rule 2: The same type at the same position preserves state\n\nDifferent JSX branches do not automatically create different component identities.\n\n```tsx\nfunction Profile({ compact }: { compact: boolean }) {\n  return compact ? (\n    \u003cUserForm layout="compact" />\n  ) : (\n    \u003cUserForm layout="wide" />\n  );\n}\n```\n\nBoth branches place `UserForm` in the same tree position with the same type and no differing key. React updates its props and preserves its local state.\n\nThis remains true even though the JSX appears on different lines. React reasons about the returned tree, not the source-code branch that produced it.\n\nOften the clearest version is to keep the stable structure explicit:\n\n```tsx\nfunction Profile({ compact }: { compact: boolean }) {\n  return \u003cUserForm layout={compact ? "compact" : "wide"} />;\n}\n```\n\n## Rule 3: Keys refine sibling identity\n\nA key tells React which sibling an element represents across renders. Keys are most visible in lists, but they can define identity anywhere siblings may change.\n\n```tsx\nfunction UserList({ users }: { users: readonly User[] }) {\n  return (\n    \u003cul>\n      {users.map(user => (\n        \u003cUserRow key={user.id} user={user} />\n      ))}\n    \u003c/ul>\n  );\n}\n```\n\nIf the list is reordered, `user.id` lets React associate each `UserRow` with the same logical user. Its state moves with that identity instead of staying attached to the old array index.\n\nA good key is:\n\n- unique among the current siblings;\n- stable for the lifetime of the logical item;\n- derived from the item’s identity, usually a database or domain ID.\n\nKeys do not need to be globally unique. They only need to distinguish siblings in the same collection.\n\n## Position without explicit keys\n\nWhen siblings have no keys, their order acts as identity. This can be safe for fixed, non-reordered content, but it becomes risky for dynamic lists.\n\n```tsx\n{tasks.map((task, index) => (\n  \u003cTaskEditor key={index} task={task} />\n))}\n```\n\nIf a task is inserted at the beginning, the item previously at index `0` moves to index `1`. React instead matches the new first item with the old identity at index `0`, which can make uncontrolled input values or local state appear under the wrong task.\n\nAn index key is acceptable only when the list is truly static: items are never reordered, inserted, removed, or filtered, and they have no stable identity of their own.\n\n## Keys can intentionally reset state\n\nChanging a key tells React that the element represents a different identity, even if its component type and visual position are unchanged.\n\n```tsx\nfunction Chat({ recipient }: { recipient: User }) {\n  return \u003cMessageComposer key={recipient.id} recipient={recipient} />;\n}\n```\n\nWhen the recipient changes, React unmounts the previous composer and mounts a fresh one. A draft intended for one person cannot leak into another conversation.\n\nUse this deliberately. A key is a clear reset boundary when all local state below it should be recreated. If only one value should change, updating that state directly may be more precise than remounting the entire subtree.\n\n## State follows tree identity, not visual placement\n\nState is attached to the returned React tree. CSS that moves an element visually does not change its React identity. Conversely, moving JSX to a different parent or sibling position can change identity even if the result looks identical on screen.\n\n```tsx\nfunction Dashboard({ showSidebar }: { showSidebar: boolean }) {\n  return (\n    \u003cmain>\n      {showSidebar && \u003cSidebar />}\n      \u003cEditor />\n    \u003c/main>\n  );\n}\n```\n\nBecause `Sidebar` and `Editor` occupy distinct structural positions, toggling the conditional does not necessarily make `Editor` the same kind of unkeyed list item as the sidebar. React uses the actual child structure and element types when matching. For dynamic homogeneous sibling collections, explicit stable keys remove ambiguity.\n\nThe safest interview habit is to reason from the complete returned tree: parent, sibling slot, type, and key.\n\n## Never define component types during render\n\nDefining a component inside another component creates a new function object on every parent render. React sees a different component type and remounts it.\n\n```tsx\nfunction ProfilePage() {\n  const [name, setName] = useState("");\n\n  function NameInput() {\n    return \u003cinput value={name} onChange={event => setName(event.target.value)} />;\n  }\n\n  return \u003cNameInput />;\n}\n```\n\nEvery `ProfilePage` render creates a new `NameInput` function identity. This can reset the input subtree and rerun its Effects.\n\nMove the component definition to module scope:\n\n```tsx\ntype NameInputProps = {\n  value: string;\n  onChange: (value: string) => void;\n};\n\nfunction NameInput({ value, onChange }: NameInputProps) {\n  return \u003cinput value={value} onChange={event => onChange(event.target.value)} />;\n}\n\nfunction ProfilePage() {\n  const [name, setName] = useState("");\n  return \u003cNameInput value={name} onChange={setName} />;\n}\n```\n\nPassing a new callback prop may cause an update, but it does not change the component type and therefore does not reset its state.\n\n## Conditional rendering cases\n\n### Same type, different props\n\n```tsx\n{isAdmin ? \u003cPanel mode="admin" /> : \u003cPanel mode="member" />}\n```\n\n`Panel` is preserved because its type, position, and key are the same.\n\n### Same type, different keys\n\n```tsx\n{isAdmin ? (\n  \u003cPanel key="admin" mode="admin" />\n) : (\n  \u003cPanel key="member" mode="member" />\n)}\n```\n\nThe changed key intentionally resets `Panel`.\n\n### Different types\n\n```tsx\n{isAdmin ? \u003cAdminPanel /> : \u003cMemberPanel />}\n```\n\nThe types differ, so React replaces the subtree.\n\n### Component removed from the tree\n\n```tsx\n{showForm && \u003cCheckoutForm />}\n```\n\nWhen `showForm` becomes false, the form unmounts and its local state is lost. Rendering it again creates a new identity. Lift the state to a preserved parent if it must survive being hidden, or hide it without removing it when that tradeoff is appropriate.\n\n## Identity and Effects\n\nIdentity determines whether an Effect is updated or completely discarded:\n\n- Preserved component: Effect cleanup and setup depend on its dependency changes.\n- Replaced or removed component: all of its Effect cleanup runs as it unmounts.\n- Newly mounted component: its Effects start fresh after commit.\n\nUnexpected repeated Effect setup can therefore indicate either changing dependencies or an unintended remount caused by type, key, or position.\n\n## Identity decision table\n\n| Type | Position | Key | Result |\n| --- | --- | --- | --- |\n| Same | Same | Same | Preserve identity and state |\n| Different | Same | Any | Replace subtree and reset state |\n| Same | Same | Different | Replace subtree and reset state |\n| Same | Different parent or slot | Any | Usually a different identity |\n| Same list item | Reordered | Stable domain key | Preserve logical item identity |\n| Same list item | Reordered | Index key | State may attach to the wrong item |\n\n## Common traps\n\n- Saying React preserves state because the JSX “looks the same.”\n- Believing separate conditional branches always create separate identities.\n- Confusing a prop update with a component replacement.\n- Defining a component function inside another component.\n- Generating keys with `Math.random()` or `Date.now()` during render.\n- Using array indexes as keys in a list that can change order.\n- Expecting a `key` to appear inside the child’s props.\n- Assuming keys must be globally unique.\n- Adding a key without realizing that changing it resets every stateful descendant.\n- Treating a re-render and a remount as synonyms.\n\n## Interview answer\n\nDuring reconciliation, React matches the next element tree with the previous one. It generally preserves a component and its local state when the same element type and key remain at the same tree position. If the type or key changes, React treats it as a new identity: the old subtree unmounts, its Effects clean up, and a new subtree mounts with fresh state. In lists, stable domain keys let identity follow logical items through insertion and reordering. This is also why defining components inside another component is unsafe—the nested function is a new type on every render.\n\n## Follow-up questions\n\n### If only a component’s props change, does its state reset?\n\nNo. If its type, position, and key remain the same, React normally preserves the component identity and updates it with new props.\n\n### Why are random keys harmful?\n\nA new random key is produced on every render. React therefore remounts every item, losing local state and DOM state and repeating setup work.\n\n### Is an array index always a bad key?\n\nNo. It can be acceptable for a static list whose order and membership never change. It is unsafe when items can be inserted, deleted, filtered, or reordered.\n\n### Can a key be used outside a list?\n\nYes. A changed key can distinguish alternatives at the same position or deliberately reset a stateful subtree.\n\n### Can a child read its own key from props?\n\nNo. `key` is metadata used by React. Pass the same value as a separate prop if the component needs it.\n\n### What is the difference between a re-render and a remount?\n\nA re-render recalculates an existing identity and normally preserves its state. A remount creates a new identity with fresh state after cleaning up the previous one.\n\n## Check yourself\n\n1. Why does switching `\u003cForm theme="light" />` to `\u003cForm theme="dark" />` normally preserve form state?\n2. What lifecycle work happens when a component’s key changes?\n3. Why can an index key put input state on the wrong list item after reordering?\n4. How would you intentionally clear a chat draft when the recipient changes?\n5. Why does moving a component definition to module scope prevent accidental resets?\n6. How would you tell whether repeated Effect setup comes from dependency changes or a remount?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Elements, components, and instances",
      "Rule 1: Different types replace the subtree",
      "Rule 2: The same type at the same position preserves state",
      "Rule 3: Keys refine sibling identity",
      "Position without explicit keys",
      "Keys can intentionally reset state",
      "State follows tree identity, not visual placement",
      "Never define component types during render",
      "Conditional rendering cases",
      "Identity and Effects",
      "Identity decision table",
      "Common traps",
      "Interview answer",
      "Follow-up questions",
      "Check yourself",
    ],
    quickRefresher:
      "Reconciliation is how React matches the next element tree with the previously committed tree. The result determines whether React: updates an existing component and preserves its state; mounts a new component with fresh state; unmounts an old component and runs its cleanup; reuses, updates, inserts, moves, or removes host nodes. React primarily reasons about a component’s type , position in the tree , and key . State belongs to a component identity at a tree position—not to a JSX tag or a variable name by itself.",
    excerpt: "This mental model explains several frequent interview problems:",
    readingMinutes: 8,
    searchText:
      "chapter 3 — reconciliation and element identity quick refresher reconciliation is how react matches the next element tree with the previously committed tree. the result determines whether react: updates an existing component and preserves its state; mounts a new component with fresh state; unmounts an old component and runs its cleanup; reuses, updates, inserts, moves, or removes host nodes. react primarily reasons about a component’s type , position in the tree , and key . state belongs to a component identity at a tree position—not to a jsx tag or a variable name by itself. why this matters this mental model explains several frequent interview problems: why did form state unexpectedly reset? why did state remain when conditional content changed? why are list keys important? how can a key intentionally reset a component? why is defining a component inside another component dangerous? why did an effect clean up and run again? the useful interview answer is not “react compares the virtual dom.” you should explain what react uses to decide whether two elements represent the same component identity. core mental model keys are part of position. without an explicit key, react primarily uses the element’s order among its siblings. elements, components, and instances these terms are related but distinct: a react element is an immutable description such as . a component type is the function or class referenced by that element, such as usercard . a mounted component instance is react’s preserved identity for that type at a particular tree position. function components do not expose a javascript instance object, but react still preserves their hook state and lifecycle identity. a host node is a platform element such as a dom . calling a component function again normally updates the same component identity. it does not imply a remount. rule 1: different types replace the subtree when the element type at a position changes, react treats it as a different subtree. switching compact changes the root component type at that position. react unmounts the old profile and mounts the other one. consequences include: local state is discarded; effect cleanup runs for the removed subtree; refs are cleared; old dom nodes may be removed and new ones created; the new subtree starts with fresh state and effects. the same rule applies to host elements: changing from strong to span replaces that host subtree rather than updating one tag into another. rule 2: the same type at the same position preserves state different jsx branches do not automatically create different component identities. both branches place userform in the same tree position with the same type and no differing key. react updates its props and preserves its local state. this remains true even though the jsx appears on different lines. react reasons about the returned tree, not the source code branch that produced it. often the clearest version is to keep the stable structure explicit: rule 3: keys refine sibling identity a key tells react which sibling an element represents across renders. keys are most visible in lists, but they can define identity anywhere siblings may change. if the list is reordered, user.id lets react associate each userrow with the same logical user. its state moves with that identity instead of staying attached to the old array index. a good key is: unique among the current siblings; stable for the lifetime of the logical item; derived from the item’s identity, usually a database or domain id. keys do not need to be globally unique. they only need to distinguish siblings in the same collection. position without explicit keys when siblings have no keys, their order acts as identity. this can be safe for fixed, non reordered content, but it becomes risky for dynamic lists. if a task is inserted at the beginning, the item previously at index 0 moves to index 1 . react instead matches the new first item with the old identity at index 0 , which can make uncontrolled input values or local state appear under the wrong task. an index key is acceptable only when the list is truly static: items are never reordered, inserted, removed, or filtered, and they have no stable identity of their own. keys can intentionally reset state changing a key tells react that the element represents a different identity, even if its component type and visual position are unchanged. when the recipient changes, react unmounts the previous composer and mounts a fresh one. a draft intended for one person cannot leak into another conversation. use this deliberately. a key is a clear reset boundary when all local state below it should be recreated. if only one value should change, updating that state directly may be more precise than remounting the entire subtree. state follows tree identity, not visual placement state is attached to the returned react tree. css that moves an element visually does not change its react identity. conversely, moving jsx to a different parent or sibling position can change identity even if the result looks identical on screen. because sidebar and editor occupy distinct structural positions, toggling the conditional does not necessarily make editor the same kind of unkeyed list item as the sidebar. react uses the actual child structure and element types when matching. for dynamic homogeneous sibling collections, explicit stable keys remove ambiguity. the safest interview habit is to reason from the complete returned tree: parent, sibling slot, type, and key. never define component types during render defining a component inside another component creates a new function object on every parent render. react sees a different component type and remounts it. every profilepage render creates a new nameinput function identity. this can reset the input subtree and rerun its effects. move the component definition to module scope: passing a new callback prop may cause an update, but it does not change the component type and therefore does not reset its state. conditional rendering cases same type, different props panel is preserved because its type, position, and key are the same. same type, different keys the changed key intentionally resets panel . different types the types differ, so react replaces the subtree. component removed from the tree when showform becomes false, the form unmounts and its local state is lost. rendering it again creates a new identity. lift the state to a preserved parent if it must survive being hidden, or hide it without removing it when that tradeoff is appropriate. identity and effects identity determines whether an effect is updated or completely discarded: preserved component: effect cleanup and setup depend on its dependency changes. replaced or removed component: all of its effect cleanup runs as it unmounts. newly mounted component: its effects start fresh after commit. unexpected repeated effect setup can therefore indicate either changing dependencies or an unintended remount caused by type, key, or position. identity decision table type position key result same same same preserve identity and state different same any replace subtree and reset state same same different replace subtree and reset state same different parent or slot any usually a different identity same list item reordered stable domain key preserve logical item identity same list item reordered index key state may attach to the wrong item common traps saying react preserves state because the jsx “looks the same.” believing separate conditional branches always create separate identities. confusing a prop update with a component replacement. defining a component function inside another component. generating keys with math.random() or date.now() during render. using array indexes as keys in a list that can change order. expecting a key to appear inside the child’s props. assuming keys must be globally unique. adding a key without realizing that changing it resets every stateful descendant. treating a re render and a remount as synonyms. interview answer during reconciliation, react matches the next element tree with the previous one. it generally preserves a component and its local state when the same element type and key remain at the same tree position. if the type or key changes, react treats it as a new identity: the old subtree unmounts, its effects clean up, and a new subtree mounts with fresh state. in lists, stable domain keys let identity follow logical items through insertion and reordering. this is also why defining components inside another component is unsafe—the nested function is a new type on every render. follow up questions if only a component’s props change, does its state reset? no. if its type, position, and key remain the same, react normally preserves the component identity and updates it with new props. why are random keys harmful? a new random key is produced on every render. react therefore remounts every item, losing local state and dom state and repeating setup work. is an array index always a bad key? no. it can be acceptable for a static list whose order and membership never change. it is unsafe when items can be inserted, deleted, filtered, or reordered. can a key be used outside a list? yes. a changed key can distinguish alternatives at the same position or deliberately reset a stateful subtree. can a child read its own key from props? no. key is metadata used by react. pass the same value as a separate prop if the component needs it. what is the difference between a re render and a remount? a re render recalculates an existing identity and normally preserves its state. a remount creates a new identity with fresh state after cleaning up the previous one. check yourself 1. why does switching to normally preserve form state? 2. what lifecycle work happens when a component’s key changes? 3. why can an index key put input state on the wrong list item after reordering? 4. how would you intentionally clear a chat draft when the recipient changes? 5. why does moving a component definition to module scope prevent accidental resets? 6. how would you tell whether repeated effect setup comes from dependency changes or a remount?",
  },
  {
    number: 4,
    slug: "04-keys-and-list-rendering",
    title: "Keys and List Rendering",
    kind: "chapter",
    partNumber: 1,
    partName: "Rendering Model",
    markdown:
      '# Chapter 4 — Keys and List Rendering\n\n## Quick refresher\n\nReact renders collections by transforming data into elements, commonly with `map`. Each element in a dynamic sibling collection needs a key that identifies the same logical item across renders.\n\n```tsx\nfunction TodoList({ todos }: { todos: readonly Todo[] }) {\n  return (\n    \u003cul>\n      {todos.map(todo => (\n        \u003cTodoRow key={todo.id} todo={todo} />\n      ))}\n    \u003c/ul>\n  );\n}\n```\n\nA good key is:\n\n- **stable:** the same item receives the same key on every render;\n- **unique among its siblings:** no two items in that collection share it;\n- **derived from data identity:** usually a database ID or domain identifier.\n\n## Why this matters\n\nKeys are not primarily about removing a console warning. They determine how React matches siblings during reconciliation.\n\nIncorrect keys can cause:\n\n- input values or validation state to appear on the wrong row;\n- focus and text selection to jump unexpectedly;\n- expanded or selected state to follow a position instead of an item;\n- Effects to clean up and restart unnecessarily;\n- animations to attach to the wrong element;\n- avoidable DOM replacement and lost user input.\n\nInterviewers expect you to explain both what keys do and when an index key is unsafe.\n\n## Core mental model\n\n```text\nkey identifies the logical sibling\n  ↓\nReact matches the old and new element with that key\n  ↓\ncomponent state and DOM identity follow the item\n```\n\nWithout explicit keys, React primarily matches siblings by their order. A stable key changes the question from “what is at position 2?” to “where is the item with ID `todo-42` now?”\n\n## Rendering a collection\n\nKeep the data transformation separate from the row’s rendering responsibility:\n\n```tsx\ntype Product = {\n  id: string;\n  name: string;\n  price: number;\n  available: boolean;\n};\n\ntype ProductListProps = {\n  products: readonly Product[];\n};\n\nfunction ProductList({ products }: ProductListProps) {\n  const availableProducts = products.filter(product => product.available);\n\n  return (\n    \u003cul>\n      {availableProducts.map(product => (\n        \u003cProductRow key={product.id} product={product} />\n      ))}\n    \u003c/ul>\n  );\n}\n```\n\n`filter` and `map` return new arrays without mutating the input. Rendering should not sort or modify the received array in place:\n\n```tsx\n// Wrong: mutates a prop during render.\nproducts.sort((a, b) => a.price - b.price);\n\n// Correct: creates a sorted copy.\nconst sortedProducts = products.toSorted((a, b) => a.price - b.price);\n```\n\nIf the environment does not support `toSorted`, use `[...products].sort(...)`.\n\n## How stable keys preserve identity\n\nSuppose these editable rows are rendered:\n\n```text\nkey a → Buy milk\nkey b → Book tickets\nkey c → Read notes\n```\n\nAfter sorting, the order becomes:\n\n```text\nkey b → Book tickets\nkey a → Buy milk\nkey c → Read notes\n```\n\nWith stable IDs, React knows that `b` moved before `a`. The local state, focus, and DOM associated with each row can remain with the corresponding task.\n\nKeys are hints for matching React elements. They do not guarantee that React literally moves every existing DOM node, but they give React the identity information needed to preserve the correct component instances.\n\n## Why index keys fail in changing lists\n\nAn index identifies a current position rather than the underlying item.\n\n```tsx\n// Risky when the collection can change.\ntodos.map((todo, index) => (\n  \u003cTodoRow key={index} todo={todo} />\n));\n```\n\nImagine row `0` has local edit text for todo A. If A is removed, todo B becomes row `0`. React sees the same key, `0`, and may reuse A’s previous component identity for B. The edit state can now appear beside the wrong data.\n\nIndex keys are usually acceptable only when all of these are true:\n\n- the items have no stable IDs;\n- the list’s order never changes;\n- items are never inserted, removed, or filtered;\n- rows do not contain identity-sensitive local or DOM state.\n\nEven then, an explicit domain key is clearer when one exists.\n\n## Why random keys are worse\n\nNever generate keys during render:\n\n```tsx\n// Wrong\nitems.map(item => \u003cRow key={Math.random()} item={item} />);\n```\n\nEvery render creates different keys. React treats every row as a new identity, unmounts the old rows, and mounts new ones. Local state, focus, and uncontrolled form values are lost, while setup and cleanup work repeats.\n\nGenerate an ID once when an item is created, then store that ID in the data:\n\n```tsx\nfunction createTodo(title: string): Todo {\n  return {\n    id: crypto.randomUUID(),\n    title,\n    completed: false,\n  };\n}\n```\n\nRandomness is not the problem when generating a persistent data ID. Regenerating identity during every render is the problem.\n\n## Put the key where the array is created\n\nThe key belongs on the outermost element returned directly from `map`:\n\n```tsx\nfunction TodoList({ todos }: { todos: readonly Todo[] }) {\n  return todos.map(todo => (\n    \u003cTodoRow key={todo.id} todo={todo} />\n  ));\n}\n\nfunction TodoRow({ todo }: { todo: Todo }) {\n  return \u003carticle>{todo.title}\u003c/article>;\n}\n```\n\nPutting the key on `\u003carticle>` inside `TodoRow` is too late. React needs the key while matching the `TodoRow` siblings created by the parent.\n\n## Keys and fragments\n\nWhen each item must return multiple sibling DOM nodes, use the explicit `Fragment` form because the shorthand syntax cannot receive a key:\n\n```tsx\nimport { Fragment } from "react";\n\nfunction DefinitionList({ entries }: { entries: readonly Entry[] }) {\n  return (\n    \u003cdl>\n      {entries.map(entry => (\n        \u003cFragment key={entry.id}>\n          \u003cdt>{entry.term}\u003c/dt>\n          \u003cdd>{entry.definition}\u003c/dd>\n        \u003c/Fragment>\n      ))}\n    \u003c/dl>\n  );\n}\n```\n\n`\u003c>...\u003c/>` cannot accept `key`.\n\n## Key uniqueness is local\n\nKeys need to be unique only among siblings in the same array or collection.\n\n```tsx\n\u003cActiveUsers users={users} />\n\u003cArchivedUsers users={users} />\n```\n\nBoth lists may use `user.id`. They are separate sibling scopes, so keys do not need to be globally unique across the application.\n\nDuplicate keys within one collection are ambiguous. React cannot reliably know which previous sibling corresponds to which next sibling.\n\n## Keys are not component props\n\nReact consumes `key` as reconciliation metadata. The child cannot read `props.key`.\n\n```tsx\n\u003cTodoRow key={todo.id} todoId={todo.id} todo={todo} />\n```\n\nIf the component needs the identifier, pass it separately as a normal prop.\n\n## Keys can intentionally reset state\n\nA key is useful outside a rendered list when two alternatives should be treated as different identities.\n\n```tsx\nfunction DocumentWorkspace({ documentId }: { documentId: string }) {\n  return \u003cEditor key={documentId} documentId={documentId} />;\n}\n```\n\nWhen `documentId` changes, React unmounts the previous editor and mounts a fresh one. All local state below `Editor` resets.\n\nThis is appropriate when state belongs exclusively to one logical entity, such as:\n\n- a message draft for a particular recipient;\n- an editor history for one document;\n- a form initialized for a different record.\n\nDo not use a changing key as a general way to “force a re-render.” It forces a remount, which is more destructive and also repeats Effect setup and DOM creation.\n\n## Keys and memoization solve different problems\n\n- A **key** identifies which sibling an element represents across renders.\n- `memo` may skip recalculating a preserved component when its props compare equal.\n\nA stable key does not prevent a row from rendering when its parent renders. An unstable key prevents memoization from helping because React sees an entirely new component identity.\n\n## Updating list state immutably\n\nStable keys work best with immutable state updates that preserve unchanged item references.\n\n```tsx\nfunction toggleTodo(id: string) {\n  setTodos(currentTodos =>\n    currentTodos.map(todo =>\n      todo.id === id\n        ? { ...todo, completed: !todo.completed }\n        : todo,\n    ),\n  );\n}\n```\n\nThe array is new, the changed todo is new, and unchanged todo objects keep their references. This makes the update predictable and allows row memoization to be effective if profiling shows it is useful.\n\n## Key strategy table\n\n| Key source | Stable identity? | Suitable? |\n| --- | --- | --- |\n| Database or domain ID | Yes | Usually the best choice |\n| Composite stable fields | Sometimes | Valid if the combination is unique and immutable |\n| Array index | Identifies position | Only for truly static lists |\n| `Math.random()` during render | No | Never |\n| `crypto.randomUUID()` during render | No | Never |\n| ID generated once when data is created | Yes | Good when the backend provides no ID |\n\n## Common traps\n\n- Treating keys as a warning-suppression requirement rather than identity.\n- Using an array index for editable, filterable, or reorderable data.\n- Generating a key during rendering.\n- Putting the key inside the extracted row component.\n- Expecting `key` to be available through props.\n- Assuming keys must be globally unique.\n- Using a mutable field such as an editable title as the key.\n- Expecting stable keys to prevent child renders.\n- Changing a key to force an update when a normal state or prop update is sufficient.\n- Mutating the list with `sort`, `splice`, or direct item assignment.\n\n## Interview answer\n\nKeys give React stable identity among sibling elements during reconciliation. A key should be unique among those siblings, stable across renders, and derived from the logical item—usually its domain ID. This lets state and DOM identity follow an item through insertion, deletion, and reordering. An index key identifies a position, so in a changing list it can associate existing row state with the wrong item. Random keys are worse because every render remounts every row. Keys can also be used deliberately outside lists to reset a subtree when its logical entity changes.\n\n## Follow-up questions\n\n### Why can an index key move input state to the wrong row?\n\nAfter insertion, removal, or sorting, the same index refers to a different item. React reuses the component identity for that position, including its local and DOM state.\n\n### When is an index key acceptable?\n\nFor a genuinely static collection with fixed order and membership and no identity-sensitive row state. Use a real ID whenever one exists.\n\n### Why is `Math.random()` a bad key?\n\nIt changes on every render, so React cannot match old and new rows. Every row unmounts and mounts again.\n\n### Where should a key be placed after extracting a row component?\n\nOn the row component returned directly from `map`, because that is the sibling collection React is reconciling.\n\n### Does changing a key re-render or remount a component?\n\nIt remounts it. The previous identity unmounts and loses its state; a new identity mounts with fresh state.\n\n### Do stable keys make list operations constant time?\n\nNo. Keys improve matching correctness and help React reuse identities, but they do not make reconciliation or DOM work free.\n\n## Check yourself\n\n1. What three properties make a good key?\n2. Why is an editable title usually a poor key?\n3. How would you assign an ID to client-created data that has no backend ID?\n4. Why must the explicit `Fragment` form be used for a keyed fragment?\n5. What is the difference between a key and `memo`?\n6. When would changing a key be the clearest way to reset state?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Rendering a collection",
      "How stable keys preserve identity",
      "Why index keys fail in changing lists",
      "Why random keys are worse",
      "Put the key where the array is created",
      "Keys and fragments",
      "Key uniqueness is local",
      "Keys are not component props",
      "Keys can intentionally reset state",
      "Keys and memoization solve different problems",
      "Updating list state immutably",
      "Key strategy table",
      "Common traps",
      "Interview answer",
      "Follow-up questions",
      "Check yourself",
    ],
    quickRefresher:
      "React renders collections by transforming data into elements, commonly with map . Each element in a dynamic sibling collection needs a key that identifies the same logical item across renders. A good key is: stable: the same item receives the same key on every render; unique among its siblings: no two items in that collection share it; derived from data identity: usually a database ID or domain identifier.",
    excerpt:
      "Keys are not primarily about removing a console warning. They determine how React matches siblings during reconciliation.",
    readingMinutes: 7,
    searchText:
      "chapter 4 — keys and list rendering quick refresher react renders collections by transforming data into elements, commonly with map . each element in a dynamic sibling collection needs a key that identifies the same logical item across renders. a good key is: stable: the same item receives the same key on every render; unique among its siblings: no two items in that collection share it; derived from data identity: usually a database id or domain identifier. why this matters keys are not primarily about removing a console warning. they determine how react matches siblings during reconciliation. incorrect keys can cause: input values or validation state to appear on the wrong row; focus and text selection to jump unexpectedly; expanded or selected state to follow a position instead of an item; effects to clean up and restart unnecessarily; animations to attach to the wrong element; avoidable dom replacement and lost user input. interviewers expect you to explain both what keys do and when an index key is unsafe. core mental model without explicit keys, react primarily matches siblings by their order. a stable key changes the question from “what is at position 2?” to “where is the item with id todo 42 now?” rendering a collection keep the data transformation separate from the row’s rendering responsibility: filter and map return new arrays without mutating the input. rendering should not sort or modify the received array in place: if the environment does not support tosorted , use [...products].sort(...) . how stable keys preserve identity suppose these editable rows are rendered: after sorting, the order becomes: with stable ids, react knows that b moved before a . the local state, focus, and dom associated with each row can remain with the corresponding task. keys are hints for matching react elements. they do not guarantee that react literally moves every existing dom node, but they give react the identity information needed to preserve the correct component instances. why index keys fail in changing lists an index identifies a current position rather than the underlying item. imagine row 0 has local edit text for todo a. if a is removed, todo b becomes row 0 . react sees the same key, 0 , and may reuse a’s previous component identity for b. the edit state can now appear beside the wrong data. index keys are usually acceptable only when all of these are true: the items have no stable ids; the list’s order never changes; items are never inserted, removed, or filtered; rows do not contain identity sensitive local or dom state. even then, an explicit domain key is clearer when one exists. why random keys are worse never generate keys during render: every render creates different keys. react treats every row as a new identity, unmounts the old rows, and mounts new ones. local state, focus, and uncontrolled form values are lost, while setup and cleanup work repeats. generate an id once when an item is created, then store that id in the data: randomness is not the problem when generating a persistent data id. regenerating identity during every render is the problem. put the key where the array is created the key belongs on the outermost element returned directly from map : putting the key on inside todorow is too late. react needs the key while matching the todorow siblings created by the parent. keys and fragments when each item must return multiple sibling dom nodes, use the explicit fragment form because the shorthand syntax cannot receive a key: \u003c ... cannot accept key . key uniqueness is local keys need to be unique only among siblings in the same array or collection. both lists may use user.id . they are separate sibling scopes, so keys do not need to be globally unique across the application. duplicate keys within one collection are ambiguous. react cannot reliably know which previous sibling corresponds to which next sibling. keys are not component props react consumes key as reconciliation metadata. the child cannot read props.key . if the component needs the identifier, pass it separately as a normal prop. keys can intentionally reset state a key is useful outside a rendered list when two alternatives should be treated as different identities. when documentid changes, react unmounts the previous editor and mounts a fresh one. all local state below editor resets. this is appropriate when state belongs exclusively to one logical entity, such as: a message draft for a particular recipient; an editor history for one document; a form initialized for a different record. do not use a changing key as a general way to “force a re render.” it forces a remount, which is more destructive and also repeats effect setup and dom creation. keys and memoization solve different problems a key identifies which sibling an element represents across renders. memo may skip recalculating a preserved component when its props compare equal. a stable key does not prevent a row from rendering when its parent renders. an unstable key prevents memoization from helping because react sees an entirely new component identity. updating list state immutably stable keys work best with immutable state updates that preserve unchanged item references. the array is new, the changed todo is new, and unchanged todo objects keep their references. this makes the update predictable and allows row memoization to be effective if profiling shows it is useful. key strategy table key source stable identity? suitable? database or domain id yes usually the best choice composite stable fields sometimes valid if the combination is unique and immutable array index identifies position only for truly static lists math.random() during render no never crypto.randomuuid() during render no never id generated once when data is created yes good when the backend provides no id common traps treating keys as a warning suppression requirement rather than identity. using an array index for editable, filterable, or reorderable data. generating a key during rendering. putting the key inside the extracted row component. expecting key to be available through props. assuming keys must be globally unique. using a mutable field such as an editable title as the key. expecting stable keys to prevent child renders. changing a key to force an update when a normal state or prop update is sufficient. mutating the list with sort , splice , or direct item assignment. interview answer keys give react stable identity among sibling elements during reconciliation. a key should be unique among those siblings, stable across renders, and derived from the logical item—usually its domain id. this lets state and dom identity follow an item through insertion, deletion, and reordering. an index key identifies a position, so in a changing list it can associate existing row state with the wrong item. random keys are worse because every render remounts every row. keys can also be used deliberately outside lists to reset a subtree when its logical entity changes. follow up questions why can an index key move input state to the wrong row? after insertion, removal, or sorting, the same index refers to a different item. react reuses the component identity for that position, including its local and dom state. when is an index key acceptable? for a genuinely static collection with fixed order and membership and no identity sensitive row state. use a real id whenever one exists. why is math.random() a bad key? it changes on every render, so react cannot match old and new rows. every row unmounts and mounts again. where should a key be placed after extracting a row component? on the row component returned directly from map , because that is the sibling collection react is reconciling. does changing a key re render or remount a component? it remounts it. the previous identity unmounts and loses its state; a new identity mounts with fresh state. do stable keys make list operations constant time? no. keys improve matching correctness and help react reuse identities, but they do not make reconciliation or dom work free. check yourself 1. what three properties make a good key? 2. why is an editable title usually a poor key? 3. how would you assign an id to client created data that has no backend id? 4. why must the explicit fragment form be used for a keyed fragment? 5. what is the difference between a key and memo ? 6. when would changing a key be the clearest way to reset state?",
  },
  {
    number: 5,
    slug: "05-state-as-a-snapshot",
    title: "State as a Snapshot",
    kind: "chapter",
    partNumber: 1,
    partName: "Rendering Model",
    markdown:
      '# Chapter 5 — State as a Snapshot\n\n## Quick refresher\n\nState is not a mutable variable that changes inside an existing render. Each render receives a fixed snapshot of props, state, and context. Calling a state setter queues an update and asks React to produce a later render with the next snapshot.\n\n```text\nrender N receives state N\n  ↓\nevent handler from render N queues an update\n  ↓\nrender N still sees state N\n  ↓\nReact produces render N + 1 with the next state\n```\n\nEvent handlers and asynchronous callbacks close over the snapshot from the render that created them.\n\n## Why this matters\n\nThis model explains common interview questions about:\n\n- why logging state immediately after a setter shows the old value;\n- why three direct updates may increment only once;\n- why delayed callbacks see older values;\n- when to use a functional state updater;\n- when a stale closure is a bug and when it is intentional;\n- why refs can expose the latest mutable value without rendering.\n\n“State updates are asynchronous” is an imprecise answer. The stronger explanation is that the running JavaScript closure has a snapshot, while the setter queues work for a future render.\n\n## Core mental model\n\n```tsx\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  function increment() {\n    setCount(count + 1);\n    console.log(count); // The current render\'s snapshot.\n  }\n\n  return \u003cbutton onClick={increment}>Count: {count}\u003c/button>;\n}\n```\n\nIf `count` is `0` when the button is rendered, the `increment` function created by that render closes over `0`. Calling `setCount(1)` queues the next state; it does not reassign the `count` constant inside the handler. The log therefore prints `0`.\n\nOn the next render, React calls `Counter` again and supplies `count` as `1`. A new `increment` closure is created with that new snapshot.\n\n## A render produces its own closures\n\nEvery render creates a new version of the component’s local variables and functions.\n\n```tsx\nfunction MessageForm() {\n  const [message, setMessage] = useState("");\n\n  function submit() {\n    const submittedMessage = message;\n\n    setTimeout(() => {\n      alert(`Sent: ${submittedMessage}`);\n    }, 1000);\n  }\n\n  return (\n    \u003c>\n      \u003cinput\n        value={message}\n        onChange={event => setMessage(event.target.value)}\n      />\n      \u003cbutton onClick={submit}>Send\u003c/button>\n    \u003c/>\n  );\n}\n```\n\nIf the user submits “Hello” and then edits the input, the timeout still reports “Hello.” That is often correct: the callback represents the specific submission that created it.\n\nA captured value is not automatically a bug. Ask whether the callback should observe the historical value from its initiating interaction or the latest value at execution time.\n\n## Repeated direct updates use the same snapshot\n\nAll reads of `count` inside one handler refer to the same render snapshot:\n\n```tsx\nfunction addThree() {\n  setCount(count + 1);\n  setCount(count + 1);\n  setCount(count + 1);\n}\n```\n\nIf `count` is `0`, each call queues “replace with `1`.” The calls do not become `1`, `2`, and `3` because the local `count` variable never changes during the handler.\n\nUse functional updaters when the next state depends on previously queued state:\n\n```tsx\nfunction addThree() {\n  setCount(current => current + 1);\n  setCount(current => current + 1);\n  setCount(current => current + 1);\n}\n```\n\nReact processes the updater queue in order:\n\n```text\n0 → 1 → 2 → 3\n```\n\nThe parameter name `current`, `previous`, or `value` is only a local name. React supplies the result of the previous queued update.\n\n## Replacement updates and functional updates\n\nA setter accepts either a next value or an updater function.\n\n```tsx\nsetCount(5);                    // Queue a replacement value.\nsetCount(current => current + 1); // Calculate from queued state.\n```\n\nOrder matters:\n\n```tsx\nsetCount(count + 5);\nsetCount(current => current + 1);\n```\n\nIf the snapshot is `0`, React first queues replacement with `5`, then applies the updater to produce `6`.\n\nIn the reverse order:\n\n```tsx\nsetCount(current => current + 1);\nsetCount(5);\n```\n\nThe final replacement makes the result `5`.\n\nFunctional updaters should be pure. In development Strict Mode, React may call updater functions more than once to expose accidental side effects.\n\n## Batching and snapshots\n\nReact normally batches state updates made during the same interaction or task. It waits until the running code finishes before processing the queued updates and rendering.\n\n```tsx\nfunction handleCheckout() {\n  setSubmitting(true);\n  setError(null);\n  setAttemptCount(count => count + 1);\n}\n```\n\nThese setters can produce one render rather than three separate intermediate screens. Batching is why the handler finishes with one consistent snapshot.\n\nDo not depend on a specific number of renders for correctness. Think in terms of queued state transitions and the final UI React should calculate.\n\nReact keeps separate intentional user events separate. For example, two distinct clicks are not accidentally merged into one logical click.\n\n## State setters do not return the new state\n\nA setter returns `undefined`. If later code in the same handler needs the next value, calculate it locally:\n\n```tsx\nfunction incrementAndReport() {\n  const nextCount = count + 1;\n  setCount(nextCount);\n  analytics.track("count_changed", { count: nextCount });\n}\n```\n\nIf the operation depends on the latest queued state and also causes an external side effect, reconsider the design. Updater functions must remain pure; external work belongs in the event handler or an appropriate synchronization boundary.\n\n## Object state is also a snapshot\n\nObjects stored in state should be treated as immutable snapshots.\n\n```tsx\ntype Profile = {\n  name: string;\n  city: string;\n};\n\nconst [profile, setProfile] = useState\u003cProfile>({\n  name: "Ada",\n  city: "London",\n});\n```\n\nMutating the current object changes data that belongs to an existing render and keeps the same reference:\n\n```tsx\n// Wrong\nprofile.city = "Paris";\nsetProfile(profile);\n```\n\nCreate the next snapshot instead:\n\n```tsx\nsetProfile(current => ({\n  ...current,\n  city: "Paris",\n}));\n```\n\nThis preserves the history of previous render snapshots and gives React a new identity to compare.\n\n## Stale closures: expected versus problematic\n\n### Expected snapshot behavior\n\nAn asynchronous callback should remember the value associated with the action that created it:\n\n```tsx\nfunction confirmOrder() {\n  const orderToSubmit = order;\n  submitOrder(orderToSubmit);\n}\n```\n\nThe user editing a later order should not change the already submitted payload.\n\n### Problematic stale state\n\nA long-lived callback may need to update from whatever state is current when it runs:\n\n```tsx\nuseEffect(() => {\n  const id = window.setInterval(() => {\n    setCount(count + 1); // Captures the Effect\'s count snapshot.\n  }, 1000);\n\n  return () => window.clearInterval(id);\n}, []);\n```\n\nThe interval repeatedly queues the same next value. Use a functional updater:\n\n```tsx\nuseEffect(() => {\n  const id = window.setInterval(() => {\n    setCount(current => current + 1);\n  }, 1000);\n\n  return () => window.clearInterval(id);\n}, []);\n```\n\nThis avoids reading `count`, so the Effect does not need it as a dependency.\n\nDo not omit a dependency merely to suppress Effect reruns. First decide whether the logic needs a snapshot, a reactive dependency, a functional updater, or a latest-value escape hatch.\n\n## When a ref should hold the latest value\n\nSometimes an asynchronous callback genuinely needs the latest value rather than its historical snapshot. A ref can provide mutable storage that does not schedule rendering.\n\n```tsx\nfunction SearchStatus({ query }: { query: string }) {\n  const latestQueryRef = useRef(query);\n\n  useEffect(() => {\n    latestQueryRef.current = query;\n  }, [query]);\n\n  function reportLater() {\n    setTimeout(() => {\n      console.log(latestQueryRef.current);\n    }, 1000);\n  }\n\n  return \u003cbutton onClick={reportLater}>Report latest query\u003c/button>;\n}\n```\n\nUse this deliberately:\n\n- state drives rendered output and schedules rendering;\n- a ref stores mutable information that rendering does not need to observe.\n\nDo not replace ordinary state with refs just to avoid understanding closures. The UI will not update when `ref.current` changes.\n\n## Props and context are snapshots too\n\nThe same closure reasoning applies to props and context values. An event handler belongs to one render and sees the props and context from that render.\n\n```tsx\nfunction SaveButton({ documentId }: { documentId: string }) {\n  function handleSave() {\n    saveDocument(documentId);\n  }\n\n  return \u003cbutton onClick={handleSave}>Save\u003c/button>;\n}\n```\n\nWhen `documentId` changes, React creates a new render with a new handler. A handler already running does not have its captured variables silently rewritten.\n\n## Choosing the right update form\n\n| Situation | Preferred form |\n| --- | --- |\n| Replace state with a value independent of previous state | `setOpen(true)` |\n| Toggle or increment from previous state | `setOpen(open => !open)` |\n| Apply several updates cumulatively | Functional updaters |\n| Replace an object while preserving other fields | Functional updater plus immutable copy |\n| Async callback should retain action-time value | Use its captured snapshot |\n| Long-lived callback should update from latest queued state | Functional updater when possible |\n| Non-rendering callback must read latest value | Ref, used deliberately |\n\n## Common traps\n\n- Saying a setter immediately changes the state variable.\n- Explaining the behavior only as “asynchronous state.”\n- Logging after a setter and treating the snapshot as a failed update.\n- Calling `setCount(count + 1)` repeatedly and expecting cumulative results.\n- Mutating an object or array from the current state snapshot.\n- Calling every captured value a stale-closure bug.\n- Omitting Effect dependencies to force an old closure to remain.\n- Performing side effects inside functional updater functions.\n- Using refs for values that should update the UI.\n- Expecting batching details or render counts to be application semantics.\n\n## Interview answer\n\nReact state behaves like a snapshot associated with a particular render. Event handlers and callbacks close over that render’s props and state. Calling a setter queues an update for a future render; it does not mutate the variable in the currently running closure. That is why reading state immediately after a setter returns the old snapshot. When the next state depends on previously queued state, I use a pure functional updater. For asynchronous code, I first decide whether it should preserve the initiating render’s value or intentionally read the latest value; a ref is an escape hatch for the latter when the value does not drive rendering.\n\n## Follow-up questions\n\n### Why does logging state after a setter show the old value?\n\nThe handler is still running with the snapshot from the render that created it. The setter queued another render rather than mutating that local variable.\n\n### Why do three `setCount(count + 1)` calls usually add only one?\n\nAll three calls read the same `count` snapshot and queue the same replacement value. Functional updaters consume the queued result in sequence.\n\n### Are stale closures always bugs?\n\nNo. Capturing the value associated with a particular click or submission provides useful consistency. It is a bug only when the requirement is to observe a newer value.\n\n### When should you use a functional updater?\n\nWhen the next state is calculated from previous or already queued state, especially for increments, toggles, async callbacks, and multiple updates in one batch.\n\n### Does a ref solve stale state?\n\nA ref can expose the latest mutable value to a callback, but changing it does not render the UI. Prefer a functional updater or correct dependencies when those express the requirement.\n\n### Can a functional updater contain a network request?\n\nNo. Updaters must be pure and may be invoked more than once in development. Perform external side effects in an event handler or Effect as appropriate.\n\n## Check yourself\n\n1. What does `count` contain immediately after `setCount(count + 1)` in the same handler?\n2. What result follows from queuing a replacement with `5` and then an increment updater?\n3. Why do functional updaters compose while repeated direct replacements do not?\n4. When is a callback’s captured historical value the desired behavior?\n5. Why does mutating a state object violate the snapshot model?\n6. When should a ref hold the latest value instead of state?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "A render produces its own closures",
      "Repeated direct updates use the same snapshot",
      "Replacement updates and functional updates",
      "Batching and snapshots",
      "State setters do not return the new state",
      "Object state is also a snapshot",
      "Stale closures: expected versus problematic",
      "When a ref should hold the latest value",
      "Props and context are snapshots too",
      "Choosing the right update form",
      "Common traps",
      "Interview answer",
      "Follow-up questions",
      "Check yourself",
    ],
    quickRefresher:
      "State is not a mutable variable that changes inside an existing render. Each render receives a fixed snapshot of props, state, and context. Calling a state setter queues an update and asks React to produce a later render with the next snapshot. Event handlers and asynchronous callbacks close over the snapshot from the render that created them.",
    excerpt: "This model explains common interview questions about:",
    readingMinutes: 7,
    searchText:
      "chapter 5 — state as a snapshot quick refresher state is not a mutable variable that changes inside an existing render. each render receives a fixed snapshot of props, state, and context. calling a state setter queues an update and asks react to produce a later render with the next snapshot. event handlers and asynchronous callbacks close over the snapshot from the render that created them. why this matters this model explains common interview questions about: why logging state immediately after a setter shows the old value; why three direct updates may increment only once; why delayed callbacks see older values; when to use a functional state updater; when a stale closure is a bug and when it is intentional; why refs can expose the latest mutable value without rendering. “state updates are asynchronous” is an imprecise answer. the stronger explanation is that the running javascript closure has a snapshot, while the setter queues work for a future render. core mental model if count is 0 when the button is rendered, the increment function created by that render closes over 0 . calling setcount(1) queues the next state; it does not reassign the count constant inside the handler. the log therefore prints 0 . on the next render, react calls counter again and supplies count as 1 . a new increment closure is created with that new snapshot. a render produces its own closures every render creates a new version of the component’s local variables and functions. if the user submits “hello” and then edits the input, the timeout still reports “hello.” that is often correct: the callback represents the specific submission that created it. a captured value is not automatically a bug. ask whether the callback should observe the historical value from its initiating interaction or the latest value at execution time. repeated direct updates use the same snapshot all reads of count inside one handler refer to the same render snapshot: if count is 0 , each call queues “replace with 1 .” the calls do not become 1 , 2 , and 3 because the local count variable never changes during the handler. use functional updaters when the next state depends on previously queued state: react processes the updater queue in order: the parameter name current , previous , or value is only a local name. react supplies the result of the previous queued update. replacement updates and functional updates a setter accepts either a next value or an updater function. order matters: if the snapshot is 0 , react first queues replacement with 5 , then applies the updater to produce 6 . in the reverse order: the final replacement makes the result 5 . functional updaters should be pure. in development strict mode, react may call updater functions more than once to expose accidental side effects. batching and snapshots react normally batches state updates made during the same interaction or task. it waits until the running code finishes before processing the queued updates and rendering. these setters can produce one render rather than three separate intermediate screens. batching is why the handler finishes with one consistent snapshot. do not depend on a specific number of renders for correctness. think in terms of queued state transitions and the final ui react should calculate. react keeps separate intentional user events separate. for example, two distinct clicks are not accidentally merged into one logical click. state setters do not return the new state a setter returns undefined . if later code in the same handler needs the next value, calculate it locally: if the operation depends on the latest queued state and also causes an external side effect, reconsider the design. updater functions must remain pure; external work belongs in the event handler or an appropriate synchronization boundary. object state is also a snapshot objects stored in state should be treated as immutable snapshots. mutating the current object changes data that belongs to an existing render and keeps the same reference: create the next snapshot instead: this preserves the history of previous render snapshots and gives react a new identity to compare. stale closures: expected versus problematic expected snapshot behavior an asynchronous callback should remember the value associated with the action that created it: the user editing a later order should not change the already submitted payload. problematic stale state a long lived callback may need to update from whatever state is current when it runs: the interval repeatedly queues the same next value. use a functional updater: this avoids reading count , so the effect does not need it as a dependency. do not omit a dependency merely to suppress effect reruns. first decide whether the logic needs a snapshot, a reactive dependency, a functional updater, or a latest value escape hatch. when a ref should hold the latest value sometimes an asynchronous callback genuinely needs the latest value rather than its historical snapshot. a ref can provide mutable storage that does not schedule rendering. use this deliberately: state drives rendered output and schedules rendering; a ref stores mutable information that rendering does not need to observe. do not replace ordinary state with refs just to avoid understanding closures. the ui will not update when ref.current changes. props and context are snapshots too the same closure reasoning applies to props and context values. an event handler belongs to one render and sees the props and context from that render. when documentid changes, react creates a new render with a new handler. a handler already running does not have its captured variables silently rewritten. choosing the right update form situation preferred form replace state with a value independent of previous state setopen(true) toggle or increment from previous state setopen(open = !open) apply several updates cumulatively functional updaters replace an object while preserving other fields functional updater plus immutable copy async callback should retain action time value use its captured snapshot long lived callback should update from latest queued state functional updater when possible non rendering callback must read latest value ref, used deliberately common traps saying a setter immediately changes the state variable. explaining the behavior only as “asynchronous state.” logging after a setter and treating the snapshot as a failed update. calling setcount(count + 1) repeatedly and expecting cumulative results. mutating an object or array from the current state snapshot. calling every captured value a stale closure bug. omitting effect dependencies to force an old closure to remain. performing side effects inside functional updater functions. using refs for values that should update the ui. expecting batching details or render counts to be application semantics. interview answer react state behaves like a snapshot associated with a particular render. event handlers and callbacks close over that render’s props and state. calling a setter queues an update for a future render; it does not mutate the variable in the currently running closure. that is why reading state immediately after a setter returns the old snapshot. when the next state depends on previously queued state, i use a pure functional updater. for asynchronous code, i first decide whether it should preserve the initiating render’s value or intentionally read the latest value; a ref is an escape hatch for the latter when the value does not drive rendering. follow up questions why does logging state after a setter show the old value? the handler is still running with the snapshot from the render that created it. the setter queued another render rather than mutating that local variable. why do three setcount(count + 1) calls usually add only one? all three calls read the same count snapshot and queue the same replacement value. functional updaters consume the queued result in sequence. are stale closures always bugs? no. capturing the value associated with a particular click or submission provides useful consistency. it is a bug only when the requirement is to observe a newer value. when should you use a functional updater? when the next state is calculated from previous or already queued state, especially for increments, toggles, async callbacks, and multiple updates in one batch. does a ref solve stale state? a ref can expose the latest mutable value to a callback, but changing it does not render the ui. prefer a functional updater or correct dependencies when those express the requirement. can a functional updater contain a network request? no. updaters must be pure and may be invoked more than once in development. perform external side effects in an event handler or effect as appropriate. check yourself 1. what does count contain immediately after setcount(count + 1) in the same handler? 2. what result follows from queuing a replacement with 5 and then an increment updater? 3. why do functional updaters compose while repeated direct replacements do not? 4. when is a callback’s captured historical value the desired behavior? 5. why does mutating a state object violate the snapshot model? 6. when should a ref hold the latest value instead of state?",
  },
  {
    number: 6,
    slug: "06-state-preservation-and-reset",
    title: "State Preservation and Reset",
    kind: "chapter",
    partNumber: 1,
    partName: "Rendering Model",
    markdown:
      '# Chapter 6 — State Preservation and Reset\n\n## Quick refresher\n\nReact associates local state with a component identity at a position in the rendered tree. State is preserved while the component’s type, position, and key continue to describe the same identity.\n\n```text\nsame type + same tree position + same key\n  → preserve local state\n\ndifferent type or key, or removed position\n  → discard old state and mount fresh state\n```\n\nChanging props alone normally updates an existing component; it does not reset that component’s state.\n\n## Why this matters\n\nInterview tasks often include a product requirement hidden behind the word “state”:\n\n- Should a chat draft reset when the recipient changes?\n- Should each tab remember its unfinished form?\n- Should closing a modal discard or retain entered values?\n- Should switching records reinitialize a form?\n- Should a multi-step flow preserve progress after a step is hidden?\n\nThe first question is not “Which Hook should I use?” It is “Which logical entity owns this state, and how long should it live?”\n\n## Core mental model\n\nReact does not store state inside the component function. The function can run many times. React stores state against the component’s place and identity in its tree.\n\n```tsx\nfunction App() {\n  const [compact, setCompact] = useState(false);\n\n  return \u003cEditor compact={compact} />;\n}\n```\n\nWhen `compact` changes, `Editor` remains the same type at the same position with the same key. Its props update, but its local state is preserved.\n\n## Preservation: same identity, new props\n\n```tsx\ntype ChatProps = {\n  contact: Contact;\n};\n\nfunction App({ selectedContact }: { selectedContact: Contact }) {\n  return \u003cChat contact={selectedContact} />;\n}\n```\n\nSwitching `selectedContact` does not reset `Chat`. This may be correct if the draft is shared across contacts, but it may be dangerous if a message intended for Alice remains visible after switching to Bob.\n\nReact cannot infer the product meaning of the state. The developer must decide whether the component still represents the same logical thing.\n\n## Reset with a key\n\nWhen local state belongs exclusively to one entity, include that entity in the component identity:\n\n```tsx\nfunction App({ selectedContact }: { selectedContact: Contact }) {\n  return (\n    \u003cChat\n      key={selectedContact.id}\n      contact={selectedContact}\n    />\n  );\n}\n```\n\nChanging the contact changes the key. React therefore:\n\n1. unmounts the previous `Chat` subtree;\n2. runs its Effect cleanup and clears its refs;\n3. removes or replaces its host nodes as needed;\n4. mounts a new `Chat` with fresh initial state;\n5. runs the new subtree’s Effects after commit.\n\nA keyed reset applies to the entire subtree, not just one state variable.\n\nUse it when the requirement is “this is a different entity, so all local UI state below this boundary should restart.”\n\n## Reset by changing component type\n\nA different component type at the same position also creates a different identity:\n\n```tsx\nfunction AccountPanel({ mode }: { mode: "personal" | "business" }) {\n  return mode === "personal" ? \u003cPersonalForm /> : \u003cBusinessForm />;\n}\n```\n\nSwitching modes unmounts one form and mounts the other. This is appropriate when they are truly different concepts and implementations.\n\nDo not create artificial wrapper component types merely to force resets. A deliberate key usually communicates an entity-based reset more clearly.\n\n## Removing a component discards its local state\n\nConditional rendering can remove a component from the tree:\n\n```tsx\nfunction Checkout() {\n  const [showAddress, setShowAddress] = useState(false);\n\n  return (\n    \u003c>\n      \u003cbutton onClick={() => setShowAddress(show => !show)}>\n        Toggle address\n      \u003c/button>\n      {showAddress && \u003cAddressForm />}\n    \u003c/>\n  );\n}\n```\n\nWhen `showAddress` becomes false, `AddressForm` unmounts and loses its local state. Showing it again mounts a fresh form.\n\nIf the UI should disappear but its state must survive, choose one of these strategies:\n\n- keep the component mounted and hide it visually where appropriate;\n- lift the state to a parent that remains mounted;\n- store the state by entity ID in a parent or external store;\n- persist it to browser or server storage when it must outlive the page.\n\nEach strategy has a cost. Keeping a hidden subtree mounted retains DOM, memory, subscriptions, and Effects. Lifting state increases the parent’s responsibility. Persistence requires synchronization and error handling.\n\n## Preserve independent state in independent positions\n\nIf two components should remember state simultaneously, render both identities and control only their visibility:\n\n```tsx\nfunction Settings({ activeTab }: { activeTab: "profile" | "security" }) {\n  return (\n    \u003c>\n      \u003csection hidden={activeTab !== "profile"}>\n        \u003cProfileSettings />\n      \u003c/section>\n      \u003csection hidden={activeTab !== "security"}>\n        \u003cSecuritySettings />\n      \u003c/section>\n    \u003c/>\n  );\n}\n```\n\nBoth settings components remain mounted in separate tree positions, so both retain local state.\n\nThis is reasonable for small forms. It may be wasteful for many heavy tabs. In that case, lift or normalize the state and mount only the active view.\n\nAlso consider accessibility: `hidden` removes the inactive section from layout and the accessibility tree. A CSS-only visually hidden technique may not do so.\n\n## Store drafts by logical identity\n\nSometimes state should survive switching away and later return to the correct entity.\n\n```tsx\ntype Drafts = Record\u003cstring, string>;\n\nfunction ChatWorkspace({ contact }: { contact: Contact }) {\n  const [drafts, setDrafts] = useState\u003cDrafts>({});\n  const draft = drafts[contact.id] ?? "";\n\n  function updateDraft(message: string) {\n    setDrafts(current => ({\n      ...current,\n      [contact.id]: message,\n    }));\n  }\n\n  return (\n    \u003cChatComposer\n      recipient={contact}\n      value={draft}\n      onChange={updateDraft}\n    />\n  );\n}\n```\n\nThe draft state belongs to `ChatWorkspace` and is indexed by contact identity. Switching contacts shows each contact’s own preserved draft.\n\nThis is different from a keyed reset:\n\n- keyed reset discards the previous local state;\n- state stored by ID preserves multiple entity-specific values.\n\n## Resetting forms from changing records\n\nA common interview scenario initializes a form from a selected record.\n\n```tsx\nfunction EditUser({ user }: { user: User }) {\n  const [name, setName] = useState(user.name);\n  // ...\n}\n```\n\nThe `useState` initializer runs only when that component identity mounts. Changing `user` does not re-run it.\n\nIf each user should receive a fresh form, key the form by user ID:\n\n```tsx\nfunction UserEditor({ selectedUser }: { selectedUser: User }) {\n  return \u003cEditUser key={selectedUser.id} user={selectedUser} />;\n}\n```\n\nIf unsaved edits should survive user switching, move drafts into state keyed by user ID instead.\n\nIf the form should always mirror the server record and never own an editable draft, it may not need local duplicated state at all.\n\n## Avoid resetting state in an Effect by default\n\nThis pattern renders once with stale state and then schedules another render:\n\n```tsx\nfunction EditUser({ user }: { user: User }) {\n  const [name, setName] = useState(user.name);\n\n  useEffect(() => {\n    setName(user.name);\n  }, [user]);\n\n  // ...\n}\n```\n\nIt also requires remembering every field that must reset and can accidentally overwrite a user edit when object identity changes.\n\nPrefer:\n\n- a key when the entire local subtree should restart;\n- lifted state when the parent owns the draft;\n- derived values when no independent local state is needed;\n- an explicit event-driven reset when the user invokes a reset action.\n\nAn Effect can be justified when synchronizing with a genuinely external system, but prop-to-state copying is usually a state-modeling issue rather than synchronization.\n\n## Explicit user-initiated reset\n\nWhen a reset is a user action, update the state in that event:\n\n```tsx\nfunction SearchFilters() {\n  const initialFilters = { query: "", category: "all" } as const;\n  const [filters, setFilters] = useState({ ...initialFilters });\n\n  function resetFilters() {\n    setFilters({ ...initialFilters });\n  }\n\n  return (\n    \u003cform>\n      {/* controlled fields */}\n      \u003cbutton type="button" onClick={resetFilters}>\n        Reset filters\n      \u003c/button>\n    \u003c/form>\n  );\n}\n```\n\nUse a key at a higher boundary only if the reset should recreate all descendant state and DOM state, including uncontrolled inputs.\n\n## State lifetime decision table\n\n| Requirement | Typical design |\n| --- | --- |\n| Preserve state while props change | Keep the same type, position, and key |\n| Reset all state for a different entity | Key the subtree by entity ID |\n| Preserve separate state for several visible views | Render them in separate stable positions |\n| Preserve state after a view unmounts | Lift it or store it externally |\n| Preserve a draft per entity | Store drafts keyed by entity ID |\n| Reset selected fields from a button | Update them in the event handler |\n| Recompute a value from current props | Derive it during render instead of storing it |\n| Persist across reloads or devices | Browser or server persistence, as required |\n\n## Common traps\n\n- Assuming a prop change re-runs a `useState` initializer.\n- Clearing several local state fields in an Effect after a prop changes.\n- Adding a key without realizing the whole subtree will remount.\n- Using a key when the user expects unsaved state to survive.\n- Storing important data only inside a component that can unmount.\n- Confusing hiding a mounted component with conditionally removing it.\n- Keeping many expensive hidden trees mounted without considering memory and Effects.\n- Using an array index as an entity identity.\n- Defining components inside another component and accidentally resetting state.\n- Treating persistence and preservation as the same requirement.\n\n## Interview answer\n\nReact preserves local state for the same component type and key at the same tree position. Changing props alone does not reset state, and a `useState` initializer does not run again on an ordinary update. If a new logical entity should start with fresh local state, I key the subtree by that entity’s stable ID. If state must survive unmounting or be restored per entity, I lift it to a stable owner or store it by ID. I avoid copying props into state and resetting them in an Effect unless there is a real synchronization requirement; the correct choice follows the intended state lifetime.\n\n## Follow-up questions\n\n### When should a chat be keyed by recipient ID?\n\nWhen a draft belongs only to the active recipient and should be discarded upon switching. The new key mounts a fresh chat subtree.\n\n### What if every recipient’s draft must be preserved?\n\nStore drafts in a parent or external store keyed by recipient ID. A key alone resets state; it does not preserve previous instances indefinitely.\n\n### Why does `useState(user.name)` not update when `user` changes?\n\nThe initializer is used for the component’s initial mount. If React preserves the same identity, later prop changes do not reinitialize its state.\n\n### What is the cost of hiding instead of unmounting?\n\nThe component retains state, DOM, memory, and potentially active Effects or subscriptions. It may be appropriate for small frequently switched views but expensive at scale.\n\n### Why is an Effect often the wrong reset mechanism?\n\nIt commits one render with the previous state and then schedules another. It also duplicates state-management logic and can overwrite user input. Identity or ownership usually expresses the requirement more directly.\n\n### Does changing a key merely re-render a component?\n\nNo. It creates a new identity: the old subtree unmounts and cleans up, while a new subtree mounts with fresh state.\n\n## Check yourself\n\n1. Why does changing a component’s props normally preserve its local state?\n2. When should a form be keyed by a record ID?\n3. How would you preserve a separate draft for every chat contact?\n4. What resources remain active when a component is hidden but still mounted?\n5. Why can resetting prop-derived state in an Effect produce a stale intermediate render?\n6. What is the difference between preserving state, resetting state, and persisting state?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Preservation: same identity, new props",
      "Reset with a key",
      "Reset by changing component type",
      "Removing a component discards its local state",
      "Preserve independent state in independent positions",
      "Store drafts by logical identity",
      "Resetting forms from changing records",
      "Avoid resetting state in an Effect by default",
      "Explicit user-initiated reset",
      "State lifetime decision table",
      "Common traps",
      "Interview answer",
      "Follow-up questions",
      "Check yourself",
    ],
    quickRefresher:
      "React associates local state with a component identity at a position in the rendered tree. State is preserved while the component’s type, position, and key continue to describe the same identity. Changing props alone normally updates an existing component; it does not reset that component’s state.",
    excerpt: "Interview tasks often include a product requirement hidden behind the word “state”:",
    readingMinutes: 7,
    searchText:
      "chapter 6 — state preservation and reset quick refresher react associates local state with a component identity at a position in the rendered tree. state is preserved while the component’s type, position, and key continue to describe the same identity. changing props alone normally updates an existing component; it does not reset that component’s state. why this matters interview tasks often include a product requirement hidden behind the word “state”: should a chat draft reset when the recipient changes? should each tab remember its unfinished form? should closing a modal discard or retain entered values? should switching records reinitialize a form? should a multi step flow preserve progress after a step is hidden? the first question is not “which hook should i use?” it is “which logical entity owns this state, and how long should it live?” core mental model react does not store state inside the component function. the function can run many times. react stores state against the component’s place and identity in its tree. when compact changes, editor remains the same type at the same position with the same key. its props update, but its local state is preserved. preservation: same identity, new props switching selectedcontact does not reset chat . this may be correct if the draft is shared across contacts, but it may be dangerous if a message intended for alice remains visible after switching to bob. react cannot infer the product meaning of the state. the developer must decide whether the component still represents the same logical thing. reset with a key when local state belongs exclusively to one entity, include that entity in the component identity: changing the contact changes the key. react therefore: 1. unmounts the previous chat subtree; 2. runs its effect cleanup and clears its refs; 3. removes or replaces its host nodes as needed; 4. mounts a new chat with fresh initial state; 5. runs the new subtree’s effects after commit. a keyed reset applies to the entire subtree, not just one state variable. use it when the requirement is “this is a different entity, so all local ui state below this boundary should restart.” reset by changing component type a different component type at the same position also creates a different identity: switching modes unmounts one form and mounts the other. this is appropriate when they are truly different concepts and implementations. do not create artificial wrapper component types merely to force resets. a deliberate key usually communicates an entity based reset more clearly. removing a component discards its local state conditional rendering can remove a component from the tree: when showaddress becomes false, addressform unmounts and loses its local state. showing it again mounts a fresh form. if the ui should disappear but its state must survive, choose one of these strategies: keep the component mounted and hide it visually where appropriate; lift the state to a parent that remains mounted; store the state by entity id in a parent or external store; persist it to browser or server storage when it must outlive the page. each strategy has a cost. keeping a hidden subtree mounted retains dom, memory, subscriptions, and effects. lifting state increases the parent’s responsibility. persistence requires synchronization and error handling. preserve independent state in independent positions if two components should remember state simultaneously, render both identities and control only their visibility: both settings components remain mounted in separate tree positions, so both retain local state. this is reasonable for small forms. it may be wasteful for many heavy tabs. in that case, lift or normalize the state and mount only the active view. also consider accessibility: hidden removes the inactive section from layout and the accessibility tree. a css only visually hidden technique may not do so. store drafts by logical identity sometimes state should survive switching away and later return to the correct entity. the draft state belongs to chatworkspace and is indexed by contact identity. switching contacts shows each contact’s own preserved draft. this is different from a keyed reset: keyed reset discards the previous local state; state stored by id preserves multiple entity specific values. resetting forms from changing records a common interview scenario initializes a form from a selected record. the usestate initializer runs only when that component identity mounts. changing user does not re run it. if each user should receive a fresh form, key the form by user id: if unsaved edits should survive user switching, move drafts into state keyed by user id instead. if the form should always mirror the server record and never own an editable draft, it may not need local duplicated state at all. avoid resetting state in an effect by default this pattern renders once with stale state and then schedules another render: it also requires remembering every field that must reset and can accidentally overwrite a user edit when object identity changes. prefer: a key when the entire local subtree should restart; lifted state when the parent owns the draft; derived values when no independent local state is needed; an explicit event driven reset when the user invokes a reset action. an effect can be justified when synchronizing with a genuinely external system, but prop to state copying is usually a state modeling issue rather than synchronization. explicit user initiated reset when a reset is a user action, update the state in that event: use a key at a higher boundary only if the reset should recreate all descendant state and dom state, including uncontrolled inputs. state lifetime decision table requirement typical design preserve state while props change keep the same type, position, and key reset all state for a different entity key the subtree by entity id preserve separate state for several visible views render them in separate stable positions preserve state after a view unmounts lift it or store it externally preserve a draft per entity store drafts keyed by entity id reset selected fields from a button update them in the event handler recompute a value from current props derive it during render instead of storing it persist across reloads or devices browser or server persistence, as required common traps assuming a prop change re runs a usestate initializer. clearing several local state fields in an effect after a prop changes. adding a key without realizing the whole subtree will remount. using a key when the user expects unsaved state to survive. storing important data only inside a component that can unmount. confusing hiding a mounted component with conditionally removing it. keeping many expensive hidden trees mounted without considering memory and effects. using an array index as an entity identity. defining components inside another component and accidentally resetting state. treating persistence and preservation as the same requirement. interview answer react preserves local state for the same component type and key at the same tree position. changing props alone does not reset state, and a usestate initializer does not run again on an ordinary update. if a new logical entity should start with fresh local state, i key the subtree by that entity’s stable id. if state must survive unmounting or be restored per entity, i lift it to a stable owner or store it by id. i avoid copying props into state and resetting them in an effect unless there is a real synchronization requirement; the correct choice follows the intended state lifetime. follow up questions when should a chat be keyed by recipient id? when a draft belongs only to the active recipient and should be discarded upon switching. the new key mounts a fresh chat subtree. what if every recipient’s draft must be preserved? store drafts in a parent or external store keyed by recipient id. a key alone resets state; it does not preserve previous instances indefinitely. why does usestate(user.name) not update when user changes? the initializer is used for the component’s initial mount. if react preserves the same identity, later prop changes do not reinitialize its state. what is the cost of hiding instead of unmounting? the component retains state, dom, memory, and potentially active effects or subscriptions. it may be appropriate for small frequently switched views but expensive at scale. why is an effect often the wrong reset mechanism? it commits one render with the previous state and then schedules another. it also duplicates state management logic and can overwrite user input. identity or ownership usually expresses the requirement more directly. does changing a key merely re render a component? no. it creates a new identity: the old subtree unmounts and cleans up, while a new subtree mounts with fresh state. check yourself 1. why does changing a component’s props normally preserve its local state? 2. when should a form be keyed by a record id? 3. how would you preserve a separate draft for every chat contact? 4. what resources remain active when a component is hidden but still mounted? 5. why can resetting prop derived state in an effect produce a stale intermediate render? 6. what is the difference between preserving state, resetting state, and persisting state?",
  },
  {
    number: 7,
    slug: "07-batching-and-functional-updates",
    title: "Batching and Functional Updates",
    kind: "chapter",
    partNumber: 1,
    partName: "Rendering Model",
    markdown:
      '# Chapter 7 — Batching and Functional Updates\n\n## Quick refresher\n\nReact queues state updates and normally batches related updates before rendering. Batching avoids exposing partially updated UI and reduces unnecessary render and commit work.\n\nA setter can queue either:\n\n- a **replacement value**, such as `setCount(5)`;\n- an **updater function**, such as `setCount(current => current + 1)`.\n\nFunctional updaters are required when the next state depends on previously queued state.\n\n## Why this matters\n\nInterviewers use this topic to test whether you can predict update results rather than treating setters as immediate assignments.\n\nYou should be able to answer:\n\n- Why do three `setCount(count + 1)` calls usually add only one?\n- How are updater functions processed?\n- What happens when replacement values and updaters are mixed?\n- Which updates does modern React batch?\n- Does batching merge different state variables?\n- When is `flushSync` justified?\n\n## Core mental model\n\n```text\nevent or callback runs\n  ↓\nsetters enqueue state transitions\n  ↓\nReact processes each Hook\'s queue in order\n  ↓\nReact renders with the resulting state\n  ↓\nReact commits the required changes\n```\n\nThe current handler keeps its existing render snapshot throughout this process. Queuing an update does not modify the local state variable already captured by that handler.\n\n## Replacement updates use the current snapshot\n\n```tsx\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  function addThree() {\n    setCount(count + 1);\n    setCount(count + 1);\n    setCount(count + 1);\n  }\n\n  return \u003cbutton onClick={addThree}>Count: {count}\u003c/button>;\n}\n```\n\nWhen `count` is `0`, all three expressions evaluate immediately to `1`. Conceptually, React receives three requests to replace the state with `1`. The final state is therefore `1`, not `3`.\n\nThis is not because React “lost” two updates. The application queued the same replacement value three times.\n\n## Functional updaters compose\n\n```tsx\nfunction addThree() {\n  setCount(current => current + 1);\n  setCount(current => current + 1);\n  setCount(current => current + 1);\n}\n```\n\nReact processes these functions in queue order. Each updater receives the result produced by the previous update:\n\n| Queued update | Input | Output |\n| --- | ---: | ---: |\n| `current => current + 1` | 0 | 1 |\n| `current => current + 1` | 1 | 2 |\n| `current => current + 1` | 2 | 3 |\n\nThe next render receives `3`.\n\nUse a functional updater when the next state depends on the previous state:\n\n```tsx\nsetCount(count => count + 1);\nsetOpen(open => !open);\nsetItems(items => [...items, newItem]);\nsetSelection(selection => selection.filter(id => id !== removedId));\n```\n\n## Replacement and updater ordering\n\nReact processes queued updates in the order they were added.\n\n### Replacement, then updater\n\n```tsx\nsetCount(count + 5);\nsetCount(current => current + 1);\n```\n\nIf the snapshot is `0`, the replacement queues `5`, then the updater receives `5` and returns `6`.\n\n### Updater, then replacement\n\n```tsx\nsetCount(current => current + 1);\nsetCount(5);\n```\n\nThe updater produces `1`, but the later replacement makes the final queued result `5`.\n\n### Several mixed updates\n\n```tsx\nsetCount(5);\nsetCount(current => current * 2);\nsetCount(current => current + 3);\n```\n\nReact calculates:\n\n```text\nreplace with 5 → 10 → 13\n```\n\nDo not memorize isolated examples. Walk through the queue in order.\n\n## Updater functions must be pure\n\nAn updater calculates next state from its argument. It must not perform external work:\n\n```tsx\n// Wrong\nsetItems(items => {\n  analytics.track("item_added");\n  return [...items, newItem];\n});\n```\n\nReact may call updater functions more than once in development Strict Mode to detect impurity. Put the side effect in the event handler and keep the transition pure:\n\n```tsx\nfunction addItem(newItem: Item) {\n  analytics.track("item_added");\n  setItems(items => [...items, newItem]);\n}\n```\n\nAn updater must also return new objects or arrays rather than mutating its argument.\n\n```tsx\n// Wrong\nsetItems(items => {\n  items.push(newItem);\n  return items;\n});\n\n// Correct\nsetItems(items => [...items, newItem]);\n```\n\n## What batching guarantees\n\nBatching lets React process several queued updates before producing the next visible UI.\n\n```tsx\nfunction submit() {\n  setSubmitting(true);\n  setError(null);\n  setAttemptCount(count => count + 1);\n}\n```\n\nReact can render once with the combined result instead of committing intermediate combinations such as “submitting but old error still visible.”\n\nBatching does **not** mean:\n\n- all state is merged into one object;\n- setters mutate variables synchronously;\n- updates can be reordered arbitrarily;\n- React will always call the component exactly once;\n- separate intentional user interactions become one event.\n\nCode should depend on final state semantics, not on counting renders.\n\n## Automatic batching in modern React\n\nModern React batches updates from more sources than only React event handlers. Updates in callbacks such as promises, timers, and native event listeners are generally batched when the application uses a modern root.\n\n```tsx\nfunction loadProfile() {\n  fetchProfile().then(profile => {\n    setProfile(profile);\n    setLoading(false);\n  });\n}\n```\n\nThese updates can result in one render with both changes applied.\n\nAn asynchronous function can cross scheduling boundaries:\n\n```tsx\nasync function save() {\n  setSaving(true);\n\n  await saveDocument();\n\n  setSaving(false);\n  setSavedAt(Date.now());\n}\n```\n\nThe update before `await` and the updates after it do not form one uninterrupted synchronous batch. The post-`await` updates can be batched together. The user can therefore see the intended intermediate saving state.\n\nAvoid designing logic around subtle batching boundaries. Express correct state transitions regardless of how many render attempts React uses.\n\n## Separate user events stay separate\n\nReact does not combine distinct intentional interactions in a way that breaks event semantics. If a user clicks a submit button twice, React processes the first event’s update before the next click where necessary—for example, so a disabled button can prevent the second submission.\n\nBatching groups related updates; it does not erase the boundaries of meaningful user actions.\n\n## Multiple state variables have separate queues\n\nEach state Hook has its own update queue, but React can process their results in one render.\n\n```tsx\nfunction openDialog() {\n  setSelectedItem(item);\n  setDialogOpen(true);\n}\n```\n\nThe next render can receive both the selected item and the open state. React does not merge them into one value.\n\nIf several state variables always change together and represent one transition, consider whether they should be one object or managed by `useReducer`:\n\n```tsx\ntype FormState =\n  | { status: "editing"; error: null }\n  | { status: "submitting"; error: null }\n  | { status: "error"; error: string }\n  | { status: "success"; error: null };\n```\n\nThis is a modeling choice, not a batching requirement. Separate state values are fine when they change independently.\n\n## Functional updates avoid stale async writes\n\nSuppose several asynchronous responses add notifications:\n\n```tsx\nfunction handleNotification(notification: Notification) {\n  setNotifications([...notifications, notification]);\n}\n```\n\nThe callback may capture an older `notifications` array, causing later writes to overwrite one another. Calculate from the queued current state:\n\n```tsx\nfunction handleNotification(notification: Notification) {\n  setNotifications(current => [...current, notification]);\n}\n```\n\nThis solves state-transition staleness. It does not solve all async problems: stale requests can still finish out of order, so request identity, cancellation, or response validation may also be required.\n\n## When direct replacement is clearer\n\nNot every setter needs an updater. Direct replacement is appropriate when the next value does not depend on previous state:\n\n```tsx\nsetOpen(true);\nsetQuery(event.target.value);\nsetSelectedUser(user);\nsetError(null);\n```\n\nWriting `setOpen(() => true)` adds no value. Choose the form that expresses the transition.\n\nIf you reference a state variable only to calculate its next value, an updater is usually safer:\n\n```tsx\n// Better than setOpen(!open) when updates may queue.\nsetOpen(open => !open);\n```\n\n## The rare role of `flushSync`\n\n`flushSync` forces React to process updates and synchronously update the DOM before the callback returns.\n\n```tsx\nimport { flushSync } from "react-dom";\n\nfunction addAndScroll(item: Item) {\n  flushSync(() => {\n    setItems(items => [...items, item]);\n  });\n\n  listRef.current?.lastElementChild?.scrollIntoView();\n}\n```\n\nWithout the forced commit, the new DOM node might not exist when the imperative measurement runs.\n\nThis is an escape hatch for integrations that require the DOM immediately, such as certain browser APIs or third-party widgets. It can hurt performance, force pending work to run, and undermine batching. Do not use it to make ordinary state logic feel synchronous.\n\nOften a ref callback, layout Effect, or declarative design expresses the requirement better.\n\n## Update strategy table\n\n| Requirement | Appropriate update |\n| --- | --- |\n| Set a value independent of old state | `setValue(nextValue)` |\n| Increment or toggle | Functional updater |\n| Apply several cumulative updates | Functional updaters in queue order |\n| Update an object from its current fields | Functional updater returning a new object |\n| Update several independent Hooks | Queue each; React can batch the render |\n| Model one complex transition | Consider `useReducer` |\n| Read DOM immediately after a required commit | Rare `flushSync`, after considering alternatives |\n\n## Common traps\n\n- Treating a setter as a synchronous assignment.\n- Expecting repeated replacement updates from one snapshot to accumulate.\n- Assuming functional updaters run immediately when queued.\n- Mutating the updater argument.\n- Performing logging, requests, or analytics inside an updater.\n- Using direct replacement when an async callback depends on current state.\n- Assuming batching merges separate state Hooks.\n- Depending on an exact number of component calls.\n- Claiming every update across an `await` belongs to one batch.\n- Using `flushSync` for normal application flows.\n- Believing a functional updater solves request race conditions by itself.\n\n## Interview answer\n\nReact queues state updates and batches related work before rendering. A direct value is a replacement calculated from the current render’s snapshot, so repeated `setCount(count + 1)` calls queue the same value. A functional updater receives the latest value produced by the queue, which makes cumulative updates compose correctly. Modern React generally batches updates from React events and asynchronous callbacks, but application correctness should not depend on render counts or subtle batch boundaries. Updaters must remain pure, and `flushSync` is reserved for rare imperative integrations that need the DOM committed immediately.\n\n## Follow-up questions\n\n### What does three `setCount(count + 1)` calls produce from zero?\n\nUsually `1`, because each expression reads the same zero snapshot and queues replacement with `1`.\n\n### What does three increment updater functions produce?\n\n`3`. React passes each updater the result of the previous queued update.\n\n### Are state updates asynchronous?\n\nIt is more precise to say setters enqueue updates for a future render. The current closure keeps its snapshot, and React schedules and batches the queued work.\n\n### Does automatic batching mean the loading state never appears?\n\nNo. Updates separated by asynchronous boundaries can produce separate renders. A pre-`await` loading update can commit before the post-`await` result updates.\n\n### When should state values be combined?\n\nWhen they represent one coherent state machine, must maintain invariants together, or are easier to transition atomically. Do not combine them merely because React batches their updates.\n\n### When is `flushSync` acceptable?\n\nWhen an unavoidable imperative integration must read or manipulate DOM that depends on a state update immediately. It should remain localized and rare.\n\n## Check yourself\n\n1. Starting from `2`, what is the result of replacement with `5`, multiplication by `2`, then addition of `3`?\n2. Why must an updater function be pure?\n3. Which direct state updates remain clearer than functional updaters?\n4. How does batching differ from merging state?\n5. Why might updates before and after `await` produce separate renders?\n6. What problem does `flushSync` solve, and what is its cost?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Replacement updates use the current snapshot",
      "Functional updaters compose",
      "Replacement and updater ordering",
      "Updater functions must be pure",
      "What batching guarantees",
      "Automatic batching in modern React",
      "Separate user events stay separate",
      "Multiple state variables have separate queues",
      "Functional updates avoid stale async writes",
      "When direct replacement is clearer",
      "The rare role of `flushSync`",
      "Update strategy table",
      "Common traps",
      "Interview answer",
      "Follow-up questions",
      "Check yourself",
    ],
    quickRefresher:
      "React queues state updates and normally batches related updates before rendering. Batching avoids exposing partially updated UI and reduces unnecessary render and commit work. A setter can queue either: a replacement value , such as setCount(5) ; an updater function , such as setCount(current = current + 1) . Functional updaters are required when the next state depends on previously queued state.",
    excerpt:
      "Interviewers use this topic to test whether you can predict update results rather than treating setters as immediate assignments.",
    readingMinutes: 7,
    searchText:
      "chapter 7 — batching and functional updates quick refresher react queues state updates and normally batches related updates before rendering. batching avoids exposing partially updated ui and reduces unnecessary render and commit work. a setter can queue either: a replacement value , such as setcount(5) ; an updater function , such as setcount(current = current + 1) . functional updaters are required when the next state depends on previously queued state. why this matters interviewers use this topic to test whether you can predict update results rather than treating setters as immediate assignments. you should be able to answer: why do three setcount(count + 1) calls usually add only one? how are updater functions processed? what happens when replacement values and updaters are mixed? which updates does modern react batch? does batching merge different state variables? when is flushsync justified? core mental model the current handler keeps its existing render snapshot throughout this process. queuing an update does not modify the local state variable already captured by that handler. replacement updates use the current snapshot when count is 0 , all three expressions evaluate immediately to 1 . conceptually, react receives three requests to replace the state with 1 . the final state is therefore 1 , not 3 . this is not because react “lost” two updates. the application queued the same replacement value three times. functional updaters compose react processes these functions in queue order. each updater receives the result produced by the previous update: queued update input output : : current = current + 1 0 1 current = current + 1 1 2 current = current + 1 2 3 the next render receives 3 . use a functional updater when the next state depends on the previous state: replacement and updater ordering react processes queued updates in the order they were added. replacement, then updater if the snapshot is 0 , the replacement queues 5 , then the updater receives 5 and returns 6 . updater, then replacement the updater produces 1 , but the later replacement makes the final queued result 5 . several mixed updates react calculates: do not memorize isolated examples. walk through the queue in order. updater functions must be pure an updater calculates next state from its argument. it must not perform external work: react may call updater functions more than once in development strict mode to detect impurity. put the side effect in the event handler and keep the transition pure: an updater must also return new objects or arrays rather than mutating its argument. what batching guarantees batching lets react process several queued updates before producing the next visible ui. react can render once with the combined result instead of committing intermediate combinations such as “submitting but old error still visible.” batching does not mean: all state is merged into one object; setters mutate variables synchronously; updates can be reordered arbitrarily; react will always call the component exactly once; separate intentional user interactions become one event. code should depend on final state semantics, not on counting renders. automatic batching in modern react modern react batches updates from more sources than only react event handlers. updates in callbacks such as promises, timers, and native event listeners are generally batched when the application uses a modern root. these updates can result in one render with both changes applied. an asynchronous function can cross scheduling boundaries: the update before await and the updates after it do not form one uninterrupted synchronous batch. the post await updates can be batched together. the user can therefore see the intended intermediate saving state. avoid designing logic around subtle batching boundaries. express correct state transitions regardless of how many render attempts react uses. separate user events stay separate react does not combine distinct intentional interactions in a way that breaks event semantics. if a user clicks a submit button twice, react processes the first event’s update before the next click where necessary—for example, so a disabled button can prevent the second submission. batching groups related updates; it does not erase the boundaries of meaningful user actions. multiple state variables have separate queues each state hook has its own update queue, but react can process their results in one render. the next render can receive both the selected item and the open state. react does not merge them into one value. if several state variables always change together and represent one transition, consider whether they should be one object or managed by usereducer : this is a modeling choice, not a batching requirement. separate state values are fine when they change independently. functional updates avoid stale async writes suppose several asynchronous responses add notifications: the callback may capture an older notifications array, causing later writes to overwrite one another. calculate from the queued current state: this solves state transition staleness. it does not solve all async problems: stale requests can still finish out of order, so request identity, cancellation, or response validation may also be required. when direct replacement is clearer not every setter needs an updater. direct replacement is appropriate when the next value does not depend on previous state: writing setopen(() = true) adds no value. choose the form that expresses the transition. if you reference a state variable only to calculate its next value, an updater is usually safer: the rare role of flushsync flushsync forces react to process updates and synchronously update the dom before the callback returns. without the forced commit, the new dom node might not exist when the imperative measurement runs. this is an escape hatch for integrations that require the dom immediately, such as certain browser apis or third party widgets. it can hurt performance, force pending work to run, and undermine batching. do not use it to make ordinary state logic feel synchronous. often a ref callback, layout effect, or declarative design expresses the requirement better. update strategy table requirement appropriate update set a value independent of old state setvalue(nextvalue) increment or toggle functional updater apply several cumulative updates functional updaters in queue order update an object from its current fields functional updater returning a new object update several independent hooks queue each; react can batch the render model one complex transition consider usereducer read dom immediately after a required commit rare flushsync , after considering alternatives common traps treating a setter as a synchronous assignment. expecting repeated replacement updates from one snapshot to accumulate. assuming functional updaters run immediately when queued. mutating the updater argument. performing logging, requests, or analytics inside an updater. using direct replacement when an async callback depends on current state. assuming batching merges separate state hooks. depending on an exact number of component calls. claiming every update across an await belongs to one batch. using flushsync for normal application flows. believing a functional updater solves request race conditions by itself. interview answer react queues state updates and batches related work before rendering. a direct value is a replacement calculated from the current render’s snapshot, so repeated setcount(count + 1) calls queue the same value. a functional updater receives the latest value produced by the queue, which makes cumulative updates compose correctly. modern react generally batches updates from react events and asynchronous callbacks, but application correctness should not depend on render counts or subtle batch boundaries. updaters must remain pure, and flushsync is reserved for rare imperative integrations that need the dom committed immediately. follow up questions what does three setcount(count + 1) calls produce from zero? usually 1 , because each expression reads the same zero snapshot and queues replacement with 1 . what does three increment updater functions produce? 3 . react passes each updater the result of the previous queued update. are state updates asynchronous? it is more precise to say setters enqueue updates for a future render. the current closure keeps its snapshot, and react schedules and batches the queued work. does automatic batching mean the loading state never appears? no. updates separated by asynchronous boundaries can produce separate renders. a pre await loading update can commit before the post await result updates. when should state values be combined? when they represent one coherent state machine, must maintain invariants together, or are easier to transition atomically. do not combine them merely because react batches their updates. when is flushsync acceptable? when an unavoidable imperative integration must read or manipulate dom that depends on a state update immediately. it should remain localized and rare. check yourself 1. starting from 2 , what is the result of replacement with 5 , multiplication by 2 , then addition of 3 ? 2. why must an updater function be pure? 3. which direct state updates remain clearer than functional updaters? 4. how does batching differ from merging state? 5. why might updates before and after await produce separate renders? 6. what problem does flushsync solve, and what is its cost?",
  },
  {
    number: 8,
    slug: "08-choosing-a-state-structure",
    title: "Choosing a State Structure",
    kind: "chapter",
    partNumber: 2,
    partName: "State and Hooks",
    markdown:
      '# Chapter 8 — Choosing a State Structure\n\n## Quick refresher\n\n- Keep state minimal and represent each fact once.\n- Group values that change together; separate values that change independently.\n- Avoid contradictory, redundant, deeply nested, or duplicated state.\n- Prefer IDs over duplicating complete objects when the object already exists in a collection.\n\n## Why this matters\n\nMany React bugs are state-modeling bugs rather than rendering bugs. A good model makes invalid combinations difficult to represent and reduces synchronization Effects.\n\n## Core mental model\n\nAvoid independent booleans that can contradict one another:\n\n```tsx\nconst [isLoading, setIsLoading] = useState(false);\nconst [hasError, setHasError] = useState(false);\nconst [isSuccess, setIsSuccess] = useState(false);\n```\n\nModel the mutually exclusive state directly:\n\n```tsx\ntype Status = "idle" | "loading" | "success" | "error";\nconst [status, setStatus] = useState\u003cStatus>("idle");\n```\n\nFor collections, store stable identity rather than a second object copy:\n\n```tsx\nconst [selectedId, setSelectedId] = useState\u003cstring | null>(null);\nconst selectedItem = items.find(item => item.id === selectedId) ?? null;\n```\n\nWhen updating nested data, treat state as immutable and copy only the changed path. If updates become difficult to express or many transitions must remain consistent, consider a reducer.\n\n## Common traps\n\n- Mirroring props in state without a clear ownership reason.\n- Storing both a selected object and the collection that contains it.\n- Using several booleans for one state machine.\n- Flattening everything even when related values always change together.\n\n## Interview answer\n\nI keep state minimal, normalize it around stable IDs, and represent mutually exclusive modes with one status rather than contradictory booleans. I group values by how they change and derive anything that can be calculated from props or state. This reduces invalid states and removes synchronization code.\n\n## Check yourself\n\nWhy is `selectedId` often safer than storing a duplicate `selectedItem` object?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Keep state minimal and represent each fact once. Group values that change together; separate values that change independently. Avoid contradictory, redundant, deeply nested, or duplicated state. Prefer IDs over duplicating complete objects when the object already exists in a collection.",
    excerpt:
      "Many React bugs are state modeling bugs rather than rendering bugs. A good model makes invalid combinations difficult to represent and reduces synchronization Effects.",
    readingMinutes: 2,
    searchText:
      "chapter 8 — choosing a state structure quick refresher keep state minimal and represent each fact once. group values that change together; separate values that change independently. avoid contradictory, redundant, deeply nested, or duplicated state. prefer ids over duplicating complete objects when the object already exists in a collection. why this matters many react bugs are state modeling bugs rather than rendering bugs. a good model makes invalid combinations difficult to represent and reduces synchronization effects. core mental model avoid independent booleans that can contradict one another: model the mutually exclusive state directly: for collections, store stable identity rather than a second object copy: when updating nested data, treat state as immutable and copy only the changed path. if updates become difficult to express or many transitions must remain consistent, consider a reducer. common traps mirroring props in state without a clear ownership reason. storing both a selected object and the collection that contains it. using several booleans for one state machine. flattening everything even when related values always change together. interview answer i keep state minimal, normalize it around stable ids, and represent mutually exclusive modes with one status rather than contradictory booleans. i group values by how they change and derive anything that can be calculated from props or state. this reduces invalid states and removes synchronization code. check yourself why is selectedid often safer than storing a duplicate selecteditem object?",
  },
  {
    number: 9,
    slug: "09-derived-and-redundant-state",
    title: "Derived and Redundant State",
    kind: "chapter",
    partNumber: 2,
    partName: "State and Hooks",
    markdown:
      '# Chapter 9 — Derived and Redundant State\n\n## Quick refresher\n\nIf a value can be calculated from current props and state during render, it usually should not be stored as state.\n\n## Why this matters\n\nRedundant state creates an additional source of truth. It can become stale, requires extra updates, and often causes an unnecessary render through an Effect.\n\n## Core mental model\n\nDo not synchronize a value React can calculate:\n\n```tsx\nfunction Name({ firstName, lastName }: Props) {\n  const [fullName, setFullName] = useState("");\n\n  useEffect(() => {\n    setFullName(`${firstName} ${lastName}`);\n  }, [firstName, lastName]);\n}\n```\n\nDerive it while rendering:\n\n```tsx\nfunction Name({ firstName, lastName }: Props) {\n  const fullName = `${firstName} ${lastName}`;\n  return \u003cspan>{fullName}\u003c/span>;\n}\n```\n\nExpensive derivation is still derivation. Measure first, then use `useMemo` only when avoiding the recalculation is valuable.\n\nState is appropriate when the value represents independent information: user input, a server result, a chosen mode, or a snapshot intentionally captured at a particular time. Initializing state from a prop can be valid when the prop is explicitly only an initial value, such as `initialColor`.\n\n## Common traps\n\n- Using an Effect to synchronize two pieces of local state.\n- Assuming every calculated value needs `useMemo`.\n- Copying a prop into state and expecting later prop changes to synchronize automatically.\n- Removing state that intentionally represents a historical snapshot.\n\n## Interview answer\n\nI derive values during render whenever current props and state already contain the required information. Storing the result would duplicate a source of truth and introduce synchronization risk. I use state only when the value changes independently or intentionally captures information over time, and I memoize expensive derivations only after identifying a real cost.\n\n## Check yourself\n\nWhen is `useState(props.value)` intentional, and when does it create a synchronization bug?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "If a value can be calculated from current props and state during render, it usually should not be stored as state.",
    excerpt:
      "Redundant state creates an additional source of truth. It can become stale, requires extra updates, and often causes an unnecessary render through an Effect.",
    readingMinutes: 2,
    searchText:
      "chapter 9 — derived and redundant state quick refresher if a value can be calculated from current props and state during render, it usually should not be stored as state. why this matters redundant state creates an additional source of truth. it can become stale, requires extra updates, and often causes an unnecessary render through an effect. core mental model do not synchronize a value react can calculate: derive it while rendering: expensive derivation is still derivation. measure first, then use usememo only when avoiding the recalculation is valuable. state is appropriate when the value represents independent information: user input, a server result, a chosen mode, or a snapshot intentionally captured at a particular time. initializing state from a prop can be valid when the prop is explicitly only an initial value, such as initialcolor . common traps using an effect to synchronize two pieces of local state. assuming every calculated value needs usememo . copying a prop into state and expecting later prop changes to synchronize automatically. removing state that intentionally represents a historical snapshot. interview answer i derive values during render whenever current props and state already contain the required information. storing the result would duplicate a source of truth and introduce synchronization risk. i use state only when the value changes independently or intentionally captures information over time, and i memoize expensive derivations only after identifying a real cost. check yourself when is usestate(props.value) intentional, and when does it create a synchronization bug?",
  },
  {
    number: 10,
    slug: "10-rules-of-hooks",
    title: "Rules of Hooks",
    kind: "chapter",
    partNumber: 2,
    partName: "State and Hooks",
    markdown:
      "# Chapter 10 — Rules of Hooks\n\n## Quick refresher\n\n- Call Hooks only at the top level of a component or custom Hook.\n- Call Hooks only from React components or custom Hooks.\n- Do not call Hooks conditionally, in loops, event handlers, or ordinary utility functions.\n\n## Why this matters\n\nReact associates Hook state with call order. The rules preserve that order across renders and allow React and its tooling to reason about component behavior.\n\n## Core mental model\n\nConceptually, React matches Hook calls by position:\n\n```text\nfirst call  → first state slot\nsecond call → second Effect slot\nthird call  → third ref slot\n```\n\nThis is unsafe because the call order changes:\n\n```tsx\nif (enabled) {\n  const [value, setValue] = useState(0);\n}\n```\n\nMove the condition inside the Hook’s behavior instead:\n\n```tsx\nuseEffect(() => {\n  if (!enabled) return;\n  return subscribe();\n}, [enabled]);\n```\n\nCustom Hooks can call other Hooks because they participate in the same ordered component execution. Their names start with `use` so lint tooling can recognize and enforce Hook semantics.\n\nModern lint rules also enforce purity and dependency requirements beyond the two foundational rules. The core reason remains predictable render-time composition.\n\n## Common traps\n\n- Calling a Hook after an early return that occurs only on some renders.\n- Calling Hooks from event handlers or module-level helpers.\n- Assuming a custom Hook creates a separate component or state store.\n- Naming a normal function `useSomething` even though it is not a Hook.\n\n## Interview answer\n\nHooks must be called unconditionally at the top level of components or custom Hooks because React tracks Hook state by call order. If a conditional changes that order, later state slots no longer correspond to the same calls. Conditions should normally move inside the Hook or into a separate component.\n\n## Check yourself\n\nWhy is a Hook after a conditional early return unsafe even though it is not visibly inside an `if` block?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Call Hooks only at the top level of a component or custom Hook. Call Hooks only from React components or custom Hooks. Do not call Hooks conditionally, in loops, event handlers, or ordinary utility functions.",
    excerpt:
      "React associates Hook state with call order. The rules preserve that order across renders and allow React and its tooling to reason about component behavior.",
    readingMinutes: 2,
    searchText:
      "chapter 10 — rules of hooks quick refresher call hooks only at the top level of a component or custom hook. call hooks only from react components or custom hooks. do not call hooks conditionally, in loops, event handlers, or ordinary utility functions. why this matters react associates hook state with call order. the rules preserve that order across renders and allow react and its tooling to reason about component behavior. core mental model conceptually, react matches hook calls by position: this is unsafe because the call order changes: move the condition inside the hook’s behavior instead: custom hooks can call other hooks because they participate in the same ordered component execution. their names start with use so lint tooling can recognize and enforce hook semantics. modern lint rules also enforce purity and dependency requirements beyond the two foundational rules. the core reason remains predictable render time composition. common traps calling a hook after an early return that occurs only on some renders. calling hooks from event handlers or module level helpers. assuming a custom hook creates a separate component or state store. naming a normal function usesomething even though it is not a hook. interview answer hooks must be called unconditionally at the top level of components or custom hooks because react tracks hook state by call order. if a conditional changes that order, later state slots no longer correspond to the same calls. conditions should normally move inside the hook or into a separate component. check yourself why is a hook after a conditional early return unsafe even though it is not visibly inside an if block?",
  },
  {
    number: 11,
    slug: "11-closures-and-state-updates",
    title: "Closures and State Updates",
    kind: "chapter",
    partNumber: 2,
    partName: "State and Hooks",
    markdown:
      "# Chapter 11 — Closures and State Updates\n\n## Quick refresher\n\nEvery render creates new variables and functions. A callback closes over the props and state from the render in which it was created.\n\n## Why this matters\n\nClosures explain stale asynchronous callbacks, missing Effect dependencies, repeated state updates, and many incorrect attempts to “fix” React with refs.\n\n## Core mental model\n\nThis interval captures the initial `count` because the Effect never reruns:\n\n```tsx\nuseEffect(() => {\n  const id = setInterval(() => setCount(count + 1), 1000);\n  return () => clearInterval(id);\n}, []);\n```\n\nIf the update only needs previous state, remove the captured dependency:\n\n```tsx\nuseEffect(() => {\n  const id = setInterval(() => setCount(value => value + 1), 1000);\n  return () => clearInterval(id);\n}, []);\n```\n\nIf an Effect needs a reactive value, include it as a dependency and accept the corresponding resynchronization. If a long-lived external callback truly needs the latest value without causing resubscription, use the appropriate modern Effect-event pattern when available, or a carefully synchronized ref.\n\nA stale closure is not inherently wrong. A submission handler should often retain the values submitted at that moment. The question is whether snapshot or latest-value semantics match the requirement.\n\n## Common traps\n\n- Removing dependencies to silence the linter.\n- Calling all captured values stale bugs.\n- Using refs to hide reactive data from React.\n- Using a functional updater when the callback also needs other changing props.\n\n## Interview answer\n\nEach render creates a snapshot, and its callbacks close over that snapshot. I first decide whether the operation should use the value from that render or the latest value. Functional updates solve previous-state calculations; correct dependencies resynchronize Effects; refs are reserved for mutable values whose changes should not render.\n\n## Check yourself\n\nWhy does a functional updater fix an interval that increments state but not one that also reads a changing `step` prop?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Every render creates new variables and functions. A callback closes over the props and state from the render in which it was created.",
    excerpt:
      "Closures explain stale asynchronous callbacks, missing Effect dependencies, repeated state updates, and many incorrect attempts to “fix” React with refs.",
    readingMinutes: 2,
    searchText:
      "chapter 11 — closures and state updates quick refresher every render creates new variables and functions. a callback closes over the props and state from the render in which it was created. why this matters closures explain stale asynchronous callbacks, missing effect dependencies, repeated state updates, and many incorrect attempts to “fix” react with refs. core mental model this interval captures the initial count because the effect never reruns: if the update only needs previous state, remove the captured dependency: if an effect needs a reactive value, include it as a dependency and accept the corresponding resynchronization. if a long lived external callback truly needs the latest value without causing resubscription, use the appropriate modern effect event pattern when available, or a carefully synchronized ref. a stale closure is not inherently wrong. a submission handler should often retain the values submitted at that moment. the question is whether snapshot or latest value semantics match the requirement. common traps removing dependencies to silence the linter. calling all captured values stale bugs. using refs to hide reactive data from react. using a functional updater when the callback also needs other changing props. interview answer each render creates a snapshot, and its callbacks close over that snapshot. i first decide whether the operation should use the value from that render or the latest value. functional updates solve previous state calculations; correct dependencies resynchronize effects; refs are reserved for mutable values whose changes should not render. check yourself why does a functional updater fix an interval that increments state but not one that also reads a changing step prop?",
  },
  {
    number: 12,
    slug: "12-useeffect-as-synchronization",
    title: "useEffect as Synchronization",
    kind: "chapter",
    partNumber: 2,
    partName: "State and Hooks",
    markdown:
      "# Chapter 12 — useEffect as Synchronization\n\n## Quick refresher\n\nAn Effect synchronizes a component with something outside React: a network connection, browser API, third-party widget, timer, or subscription.\n\n## Why this matters\n\nTreating Effects as generic “run after render” callbacks produces redundant state, tangled data flow, race conditions, and unnecessary renders.\n\n## Core mental model\n\n```text\nrender describes UI\nevent handlers respond to interactions\nEffects synchronize with external systems\n```\n\n```tsx\nuseEffect(() => {\n  const connection = createConnection(roomId);\n  connection.connect();\n  return () => connection.disconnect();\n}, [roomId]);\n```\n\nThe Effect establishes synchronization for the current `roomId`. When it changes, React cleans up the old connection and establishes the new one.\n\nDo not use an Effect for calculations:\n\n```tsx\n// Derive during render instead.\nconst visibleItems = items.filter(item => item.name.includes(query));\n```\n\nDo not move interaction-specific work into an Effect merely because state changed. If submitting a form causes a POST request, perform it in the submit handler; the user event is the cause.\n\nFramework data APIs or server-state libraries may be better than hand-written fetching Effects because they provide caching, deduplication, server rendering, and loading coordination.\n\n## Common traps\n\n- Using Effects to derive local data.\n- Triggering user-event side effects indirectly through state.\n- Thinking `[]` means “run once” rather than “uses no reactive values.”\n- Fetching without accounting for stale responses or framework capabilities.\n\n## Interview answer\n\nI use `useEffect` to keep an external system synchronized with the current rendered state. The setup describes the synchronization and cleanup undoes it. Derived values stay in render, and interaction-caused work stays in event handlers. This framing makes dependencies and cleanup consequences easier to reason about.\n\n## Check yourself\n\nShould analytics for “the user clicked Buy” run in an Effect watching purchase state or in the click/submission flow?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "An Effect synchronizes a component with something outside React: a network connection, browser API, third party widget, timer, or subscription.",
    excerpt:
      "Treating Effects as generic “run after render” callbacks produces redundant state, tangled data flow, race conditions, and unnecessary renders.",
    readingMinutes: 2,
    searchText:
      "chapter 12 — useeffect as synchronization quick refresher an effect synchronizes a component with something outside react: a network connection, browser api, third party widget, timer, or subscription. why this matters treating effects as generic “run after render” callbacks produces redundant state, tangled data flow, race conditions, and unnecessary renders. core mental model the effect establishes synchronization for the current roomid . when it changes, react cleans up the old connection and establishes the new one. do not use an effect for calculations: do not move interaction specific work into an effect merely because state changed. if submitting a form causes a post request, perform it in the submit handler; the user event is the cause. framework data apis or server state libraries may be better than hand written fetching effects because they provide caching, deduplication, server rendering, and loading coordination. common traps using effects to derive local data. triggering user event side effects indirectly through state. thinking [] means “run once” rather than “uses no reactive values.” fetching without accounting for stale responses or framework capabilities. interview answer i use useeffect to keep an external system synchronized with the current rendered state. the setup describes the synchronization and cleanup undoes it. derived values stay in render, and interaction caused work stays in event handlers. this framing makes dependencies and cleanup consequences easier to reason about. check yourself should analytics for “the user clicked buy” run in an effect watching purchase state or in the click/submission flow?",
  },
  {
    number: 13,
    slug: "13-effect-dependencies-and-cleanup",
    title: "Effect Dependencies and Cleanup",
    kind: "chapter",
    partNumber: 2,
    partName: "State and Hooks",
    markdown:
      "# Chapter 13 — Effect Dependencies and Cleanup\n\n## Quick refresher\n\nDependencies list every reactive value read by an Effect. Cleanup stops or reverses the previous synchronization before the Effect reruns and when the component unmounts.\n\n## Why this matters\n\nCorrect dependencies prevent stale behavior; correct cleanup prevents leaked subscriptions and obsolete asynchronous work.\n\n## Core mental model\n\n```tsx\nuseEffect(() => {\n  const controller = new AbortController();\n  let ignore = false;\n\n  loadUser(userId, controller.signal).then(user => {\n    if (!ignore) setUser(user);\n  });\n\n  return () => {\n    ignore = true;\n    controller.abort();\n  };\n}, [userId]);\n```\n\n`userId` is reactive because it comes from props or state and is read inside the Effect. When it changes, cleanup invalidates the previous request before the next synchronization starts.\n\nObjects and functions created during render have new identities. Before memoizing them, ask whether they can be created inside the Effect or whether the Effect can depend on primitive inputs instead.\n\nCleanup must be symmetrical: unsubscribe what was subscribed, disconnect what was connected, or cancel work that no longer belongs to the current render. Development Strict Mode may run an extra setup-cleanup cycle to expose missing symmetry.\n\n## Common traps\n\n- Suppressing dependency lint rules.\n- Omitting a changing value because rerunning feels inconvenient.\n- Adding memoization before simplifying the Effect.\n- Relying only on request order and allowing old responses to overwrite new state.\n\n## Interview answer\n\nAn Effect’s dependency list is determined by the reactive values its setup reads; it is not a manually chosen schedule. Cleanup ends the synchronization created by that particular execution before replacement or unmounting. For async work, I cancel when possible and also prevent obsolete executions from committing state.\n\n## Check yourself\n\nWhy can moving an options object inside an Effect be better than wrapping it in `useMemo`?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Dependencies list every reactive value read by an Effect. Cleanup stops or reverses the previous synchronization before the Effect reruns and when the component unmounts.",
    excerpt:
      "Correct dependencies prevent stale behavior; correct cleanup prevents leaked subscriptions and obsolete asynchronous work.",
    readingMinutes: 2,
    searchText:
      "chapter 13 — effect dependencies and cleanup quick refresher dependencies list every reactive value read by an effect. cleanup stops or reverses the previous synchronization before the effect reruns and when the component unmounts. why this matters correct dependencies prevent stale behavior; correct cleanup prevents leaked subscriptions and obsolete asynchronous work. core mental model userid is reactive because it comes from props or state and is read inside the effect. when it changes, cleanup invalidates the previous request before the next synchronization starts. objects and functions created during render have new identities. before memoizing them, ask whether they can be created inside the effect or whether the effect can depend on primitive inputs instead. cleanup must be symmetrical: unsubscribe what was subscribed, disconnect what was connected, or cancel work that no longer belongs to the current render. development strict mode may run an extra setup cleanup cycle to expose missing symmetry. common traps suppressing dependency lint rules. omitting a changing value because rerunning feels inconvenient. adding memoization before simplifying the effect. relying only on request order and allowing old responses to overwrite new state. interview answer an effect’s dependency list is determined by the reactive values its setup reads; it is not a manually chosen schedule. cleanup ends the synchronization created by that particular execution before replacement or unmounting. for async work, i cancel when possible and also prevent obsolete executions from committing state. check yourself why can moving an options object inside an effect be better than wrapping it in usememo ?",
  },
  {
    number: 14,
    slug: "14-useref-and-mutable-values",
    title: "useRef and Mutable Values",
    kind: "chapter",
    partNumber: 2,
    partName: "State and Hooks",
    markdown:
      "# Chapter 14 — useRef and Mutable Values\n\n## Quick refresher\n\nA ref is a stable object whose `.current` value persists between renders. Changing it does not trigger a render.\n\n## Why this matters\n\nRefs are the correct escape hatch for DOM nodes and mutable values that participate in behavior but not in the rendered output.\n\n## Core mental model\n\nUse state for information that affects rendering. Use a ref for information React does not need to display:\n\n```tsx\nfunction SearchInput() {\n  const inputRef = useRef\u003cHTMLInputElement>(null);\n  return (\n    \u003c>\n      \u003cinput ref={inputRef} />\n      \u003cbutton onClick={() => inputRef.current?.focus()}>Focus\u003c/button>\n    \u003c/>\n  );\n}\n```\n\nRefs also store timer IDs, previous integration instances, or mutable values required by external callbacks:\n\n```tsx\nconst timerRef = useRef\u003cReturnType\u003ctypeof setTimeout> | null>(null);\n```\n\nDo not read or write refs during render except for predictable initialization patterns. Render must remain pure, and ref mutations are invisible to React. DOM access belongs in event handlers or Effects; layout measurement that must happen before paint typically belongs in `useLayoutEffect`.\n\nPassing refs through component boundaries is part of API design. Expose the smallest imperative surface necessary rather than leaking an entire internal DOM structure.\n\n## Common traps\n\n- Storing visible UI state in a ref and wondering why the UI does not update.\n- Using a ref to avoid correct Effect dependencies.\n- Mutating or reading DOM nodes during render.\n- Exposing more imperative methods than consumers require.\n\n## Interview answer\n\n`useRef` returns a stable mutable container that survives renders without scheduling new ones. I use it for DOM access and non-rendering values such as timer handles or external instances. If a value affects what the component displays, it belongs in state instead. Refs are an escape hatch, not an alternative state system.\n\n## Check yourself\n\nShould the current search query live in state or a ref, and why?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "A ref is a stable object whose .current value persists between renders. Changing it does not trigger a render.",
    excerpt:
      "Refs are the correct escape hatch for DOM nodes and mutable values that participate in behavior but not in the rendered output.",
    readingMinutes: 2,
    searchText:
      "chapter 14 — useref and mutable values quick refresher a ref is a stable object whose .current value persists between renders. changing it does not trigger a render. why this matters refs are the correct escape hatch for dom nodes and mutable values that participate in behavior but not in the rendered output. core mental model use state for information that affects rendering. use a ref for information react does not need to display: refs also store timer ids, previous integration instances, or mutable values required by external callbacks: do not read or write refs during render except for predictable initialization patterns. render must remain pure, and ref mutations are invisible to react. dom access belongs in event handlers or effects; layout measurement that must happen before paint typically belongs in uselayouteffect . passing refs through component boundaries is part of api design. expose the smallest imperative surface necessary rather than leaking an entire internal dom structure. common traps storing visible ui state in a ref and wondering why the ui does not update. using a ref to avoid correct effect dependencies. mutating or reading dom nodes during render. exposing more imperative methods than consumers require. interview answer useref returns a stable mutable container that survives renders without scheduling new ones. i use it for dom access and non rendering values such as timer handles or external instances. if a value affects what the component displays, it belongs in state instead. refs are an escape hatch, not an alternative state system. check yourself should the current search query live in state or a ref, and why?",
  },
  {
    number: 15,
    slug: "15-usereducer",
    title: "useReducer",
    kind: "chapter",
    partNumber: 2,
    partName: "State and Hooks",
    markdown:
      '# Chapter 15 — useReducer\n\n## Quick refresher\n\n`useReducer` centralizes state transitions in a pure reducer: `(state, action) => nextState`.\n\n## Why this matters\n\nReducers make complex transitions explicit, testable, and resistant to invalid partial updates. They do not automatically improve performance or replace application-wide state tools.\n\n## Core mental model\n\n```tsx\ntype State = { status: "idle" | "saving" | "error"; message: string };\ntype Action =\n  | { type: "submitted" }\n  | { type: "succeeded" }\n  | { type: "failed"; message: string };\n\nfunction reducer(state: State, action: Action): State {\n  switch (action.type) {\n    case "submitted": return { ...state, status: "saving", message: "" };\n    case "succeeded": return { ...state, status: "idle" };\n    case "failed": return { status: "error", message: action.message };\n  }\n}\n\nconst [state, dispatch] = useReducer(reducer, initialState);\n```\n\nActions describe what happened, while the reducer decides how state changes. Keep reducers pure: no requests, timers, random values, mutation, or other side effects. Perform effects outside the reducer and dispatch the resulting event.\n\nChoose a reducer when multiple fields change together, transitions are numerous, or centralizing state logic improves clarity. Several independent `useState` calls are often simpler for small components.\n\n## Common traps\n\n- Using a reducer for every component by default.\n- Performing side effects inside the reducer.\n- Creating actions that merely expose setters, such as `setField` for everything.\n- Mutating and returning the existing state object.\n\n## Interview answer\n\nI use `useReducer` when state transitions are related enough that explicit events and one transition function make the model clearer. The reducer stays pure and returns new state; event handlers or Effects perform external work and dispatch outcomes. It improves transition design, not automatically rendering performance.\n\n## Check yourself\n\nWhy is `dispatch({ type: "saveSucceeded" })` usually more meaningful than `dispatch({ type: "setLoading", value: false })`?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "useReducer centralizes state transitions in a pure reducer: (state, action) = nextState .",
    excerpt:
      "Reducers make complex transitions explicit, testable, and resistant to invalid partial updates. They do not automatically improve performance or replace application wide state tools.",
    readingMinutes: 1,
    searchText:
      'chapter 15 — usereducer quick refresher usereducer centralizes state transitions in a pure reducer: (state, action) = nextstate . why this matters reducers make complex transitions explicit, testable, and resistant to invalid partial updates. they do not automatically improve performance or replace application wide state tools. core mental model actions describe what happened, while the reducer decides how state changes. keep reducers pure: no requests, timers, random values, mutation, or other side effects. perform effects outside the reducer and dispatch the resulting event. choose a reducer when multiple fields change together, transitions are numerous, or centralizing state logic improves clarity. several independent usestate calls are often simpler for small components. common traps using a reducer for every component by default. performing side effects inside the reducer. creating actions that merely expose setters, such as setfield for everything. mutating and returning the existing state object. interview answer i use usereducer when state transitions are related enough that explicit events and one transition function make the model clearer. the reducer stays pure and returns new state; event handlers or effects perform external work and dispatch outcomes. it improves transition design, not automatically rendering performance. check yourself why is dispatch({ type: "savesucceeded" }) usually more meaningful than dispatch({ type: "setloading", value: false }) ?',
  },
  {
    number: 16,
    slug: "16-designing-custom-hooks",
    title: "Designing Custom Hooks",
    kind: "chapter",
    partNumber: 2,
    partName: "State and Hooks",
    markdown:
      '# Chapter 16 — Designing Custom Hooks\n\n## Quick refresher\n\nA custom Hook extracts reusable stateful behavior. It shares logic, not state: each call receives its own Hook state unless the Hook connects to a shared external source.\n\n## Why this matters\n\nInterviewers look for abstractions with clear ownership and useful contracts—not merely code moved into a function.\n\n## Core mental model\n\n```tsx\nfunction useOnlineStatus() {\n  const [online, setOnline] = useState(() => navigator.onLine);\n\n  useEffect(() => {\n    const update = () => setOnline(navigator.onLine);\n    window.addEventListener("online", update);\n    window.addEventListener("offline", update);\n    return () => {\n      window.removeEventListener("online", update);\n      window.removeEventListener("offline", update);\n    };\n  }, []);\n\n  return online;\n}\n```\n\nThe component consumes a meaningful value without owning browser subscription details. The Hook still follows all Hook rules and must provide correct cleanup.\n\nDesign around a behavior or capability, not a lifecycle wrapper. Prefer a focused API with explicit inputs and outputs. Return the smallest stable contract callers need, and avoid hiding important application ownership or turning every Effect into a generic `useMount` abstraction.\n\nNot all reused code needs a Hook. Pure calculations should remain ordinary functions. A function becomes a Hook when it needs React Hooks or should compose other stateful behavior.\n\n## Common traps\n\n- Assuming two calls to a custom Hook share state.\n- Creating vague wrappers such as `useEffectOnce` that hide dependency semantics.\n- Returning a large unstable object without considering consumers.\n- Using a Hook for a pure formatting or transformation function.\n\n## Interview answer\n\nA custom Hook packages reusable stateful behavior behind a domain-focused contract. It should make ownership clearer, expose explicit inputs and useful outputs, and correctly handle synchronization and cleanup. Calls share implementation but normally not state. Pure reusable logic remains an ordinary function.\n\n## Check yourself\n\nWhen should filtering data be a normal function, and when might a `useFilteredResults` Hook be justified?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "A custom Hook extracts reusable stateful behavior. It shares logic, not state: each call receives its own Hook state unless the Hook connects to a shared external source.",
    excerpt:
      "Interviewers look for abstractions with clear ownership and useful contracts—not merely code moved into a function.",
    readingMinutes: 2,
    searchText:
      "chapter 16 — designing custom hooks quick refresher a custom hook extracts reusable stateful behavior. it shares logic, not state: each call receives its own hook state unless the hook connects to a shared external source. why this matters interviewers look for abstractions with clear ownership and useful contracts—not merely code moved into a function. core mental model the component consumes a meaningful value without owning browser subscription details. the hook still follows all hook rules and must provide correct cleanup. design around a behavior or capability, not a lifecycle wrapper. prefer a focused api with explicit inputs and outputs. return the smallest stable contract callers need, and avoid hiding important application ownership or turning every effect into a generic usemount abstraction. not all reused code needs a hook. pure calculations should remain ordinary functions. a function becomes a hook when it needs react hooks or should compose other stateful behavior. common traps assuming two calls to a custom hook share state. creating vague wrappers such as useeffectonce that hide dependency semantics. returning a large unstable object without considering consumers. using a hook for a pure formatting or transformation function. interview answer a custom hook packages reusable stateful behavior behind a domain focused contract. it should make ownership clearer, expose explicit inputs and useful outputs, and correctly handle synchronization and cleanup. calls share implementation but normally not state. pure reusable logic remains an ordinary function. check yourself when should filtering data be a normal function, and when might a usefilteredresults hook be justified?",
  },
  {
    number: 17,
    slug: "17-state-ownership-and-lifting-state",
    title: "State Ownership and Lifting State",
    kind: "chapter",
    partNumber: 3,
    partName: "Component Design",
    markdown:
      "# Chapter 17 — State Ownership and Lifting State\n\n## Quick refresher\n\n- Every piece of state should have one clear owner.\n- Keep state as close as possible to the components that use it.\n- Lift it to the nearest common ancestor when multiple descendants must coordinate.\n- Pass data down and communicate changes upward through callbacks.\n\n## Why this matters\n\nState placement determines coupling, render scope, and whether components can disagree. Interviewers often test whether you can find the owner without immediately reaching for context or a global store.\n\n## Core mental model\n\nTwo inputs that must stay synchronized need a shared owner:\n\n```tsx\nfunction TemperatureCalculator() {\n  const [celsius, setCelsius] = useState(0);\n\n  return (\n    \u003c>\n      \u003cTemperatureInput value={celsius} onChange={setCelsius} />\n      \u003cTemperatureInput\n        value={toFahrenheit(celsius)}\n        onChange={value => setCelsius(toCelsius(value))}\n      />\n    \u003c/>\n  );\n}\n```\n\nThe parent owns the source of truth; each child receives a value and reports intent. Do not lift state higher than necessary: distant ownership increases prop plumbing and can enlarge the subtree affected by updates.\n\nBefore choosing context or an external store, ask:\n\n1. Which components read the value?\n2. Which events change it?\n3. What is their closest stable common owner?\n4. Must it survive that owner unmounting?\n\n## Common traps\n\n- Keeping synchronized copies in multiple children.\n- Moving all state to the application root.\n- Using context solely to avoid passing a prop through one or two layers.\n- Confusing server cache data with client UI state.\n\n## Interview answer\n\nI place state at the lowest component that owns all reads and writes. If siblings must coordinate, I lift one source of truth to their closest common ancestor and pass values and event callbacks down. I move beyond that boundary only when lifetime, sharing, or update requirements justify context, URL state, a server cache, or an external store.\n\n## Check yourself\n\nWhen does lifting state improve consistency but worsen performance or maintainability?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Every piece of state should have one clear owner. Keep state as close as possible to the components that use it. Lift it to the nearest common ancestor when multiple descendants must coordinate. Pass data down and communicate changes upward through callbacks.",
    excerpt:
      "State placement determines coupling, render scope, and whether components can disagree. Interviewers often test whether you can find the owner without immediately reaching for context or a global store.",
    readingMinutes: 2,
    searchText:
      "chapter 17 — state ownership and lifting state quick refresher every piece of state should have one clear owner. keep state as close as possible to the components that use it. lift it to the nearest common ancestor when multiple descendants must coordinate. pass data down and communicate changes upward through callbacks. why this matters state placement determines coupling, render scope, and whether components can disagree. interviewers often test whether you can find the owner without immediately reaching for context or a global store. core mental model two inputs that must stay synchronized need a shared owner: the parent owns the source of truth; each child receives a value and reports intent. do not lift state higher than necessary: distant ownership increases prop plumbing and can enlarge the subtree affected by updates. before choosing context or an external store, ask: 1. which components read the value? 2. which events change it? 3. what is their closest stable common owner? 4. must it survive that owner unmounting? common traps keeping synchronized copies in multiple children. moving all state to the application root. using context solely to avoid passing a prop through one or two layers. confusing server cache data with client ui state. interview answer i place state at the lowest component that owns all reads and writes. if siblings must coordinate, i lift one source of truth to their closest common ancestor and pass values and event callbacks down. i move beyond that boundary only when lifetime, sharing, or update requirements justify context, url state, a server cache, or an external store. check yourself when does lifting state improve consistency but worsen performance or maintainability?",
  },
  {
    number: 18,
    slug: "18-controlled-and-uncontrolled-components",
    title: "Controlled and Uncontrolled Components",
    kind: "chapter",
    partNumber: 3,
    partName: "Component Design",
    markdown:
      "# Chapter 18 — Controlled and Uncontrolled Components\n\n## Quick refresher\n\nA controlled component receives its important value from a parent and reports changes. An uncontrolled component owns the value internally, often accepting an initial default.\n\n## Why this matters\n\nThis distinction affects API design, reset behavior, validation, coordination, and integration with native forms or third-party code.\n\n## Core mental model\n\n```tsx\n// Controlled\n\u003cAccordion expanded={open} onExpandedChange={setOpen} />\n\n// Uncontrolled\n\u003cAccordion defaultExpanded />\n```\n\nA reusable component can support both modes, but must define a consistent contract:\n\n```tsx\nfunction Accordion({ expanded, defaultExpanded = false, onExpandedChange }: Props) {\n  const [internal, setInternal] = useState(defaultExpanded);\n  const isControlled = expanded !== undefined;\n  const isExpanded = isControlled ? expanded : internal;\n\n  function update(next: boolean) {\n    if (!isControlled) setInternal(next);\n    onExpandedChange?.(next);\n  }\n}\n```\n\n`defaultExpanded` is read as an initial value; changing it later should not normally control the component. Avoid switching between controlled and uncontrolled modes during a component’s lifetime.\n\nNative form controls are uncontrolled when read through form submission or refs, and controlled when React supplies their current `value` or `checked`.\n\n## Common traps\n\n- Copying a controlled prop into state and creating two owners.\n- Expecting changes to a `defaultValue` prop to update current state.\n- Switching modes after mount.\n- Exposing both APIs without defining which value wins.\n\n## Interview answer\n\nA controlled component delegates state ownership to its parent through a value and change callback, which enables coordination and validation. An uncontrolled component owns state and may accept an initial default, which can simplify isolated use. A component supporting both must choose the current value consistently and never silently switch ownership modes.\n\n## Check yourself\n\nWhy does `defaultValue` describe initialization rather than ongoing synchronization?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "A controlled component receives its important value from a parent and reports changes. An uncontrolled component owns the value internally, often accepting an initial default.",
    excerpt:
      "This distinction affects API design, reset behavior, validation, coordination, and integration with native forms or third party code.",
    readingMinutes: 1,
    searchText:
      "chapter 18 — controlled and uncontrolled components quick refresher a controlled component receives its important value from a parent and reports changes. an uncontrolled component owns the value internally, often accepting an initial default. why this matters this distinction affects api design, reset behavior, validation, coordination, and integration with native forms or third party code. core mental model a reusable component can support both modes, but must define a consistent contract: defaultexpanded is read as an initial value; changing it later should not normally control the component. avoid switching between controlled and uncontrolled modes during a component’s lifetime. native form controls are uncontrolled when read through form submission or refs, and controlled when react supplies their current value or checked . common traps copying a controlled prop into state and creating two owners. expecting changes to a defaultvalue prop to update current state. switching modes after mount. exposing both apis without defining which value wins. interview answer a controlled component delegates state ownership to its parent through a value and change callback, which enables coordination and validation. an uncontrolled component owns state and may accept an initial default, which can simplify isolated use. a component supporting both must choose the current value consistently and never silently switch ownership modes. check yourself why does defaultvalue describe initialization rather than ongoing synchronization?",
  },
  {
    number: 19,
    slug: "19-composition-over-configuration",
    title: "Composition Over Configuration",
    kind: "chapter",
    partNumber: 3,
    partName: "Component Design",
    markdown:
      '# Chapter 19 — Composition Over Configuration\n\n## Quick refresher\n\nComposition lets consumers supply elements or behavior through children and slots. Configuration asks one component to understand every possible variation through props.\n\n## Why this matters\n\nSenior interviews assess whether component APIs can grow without becoming a large collection of conditional branches and conflicting boolean props.\n\n## Core mental model\n\nConfiguration becomes difficult when combinations multiply:\n\n```tsx\n\u003cCard showHeader compact hasFooter elevated loading />\n```\n\nComposition gives structure to the caller:\n\n```tsx\n\u003cCard>\n  \u003cCard.Header>\u003cUserSummary />\u003c/Card.Header>\n  \u003cCard.Body>\u003cActivity />\u003c/Card.Body>\n  \u003cCard.Footer>\u003cActions />\u003c/Card.Footer>\n\u003c/Card>\n```\n\nUse ordinary `children` for one flexible content region. Use named props such as `header` and `footer` for a few explicit slots. Use compound components when related pieces need coordinated state and semantics.\n\nComposition is not automatically better. A small closed set of variants is clearer as a typed prop. The goal is to expose meaningful choices while preventing invalid combinations.\n\nPrefer elements or components over render callbacks unless the consumer needs internal state to produce its content.\n\n## Common traps\n\n- Replacing every prop with a compound component.\n- Creating many boolean props that produce invalid combinations.\n- Using a render prop when static children are sufficient.\n- Allowing arbitrary composition that breaks required semantics or accessibility.\n\n## Interview answer\n\nI use configuration for a small, stable set of variants and composition when consumers need structural flexibility. Children, named slots, or compound components let callers assemble UI without teaching one component every possible layout. The API should still enforce important semantic and accessibility constraints.\n\n## Check yourself\n\nWhen is `variant="danger"` clearer than supplying a custom component through a slot?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Composition lets consumers supply elements or behavior through children and slots. Configuration asks one component to understand every possible variation through props.",
    excerpt:
      "Senior interviews assess whether component APIs can grow without becoming a large collection of conditional branches and conflicting boolean props.",
    readingMinutes: 2,
    searchText:
      'chapter 19 — composition over configuration quick refresher composition lets consumers supply elements or behavior through children and slots. configuration asks one component to understand every possible variation through props. why this matters senior interviews assess whether component apis can grow without becoming a large collection of conditional branches and conflicting boolean props. core mental model configuration becomes difficult when combinations multiply: composition gives structure to the caller: use ordinary children for one flexible content region. use named props such as header and footer for a few explicit slots. use compound components when related pieces need coordinated state and semantics. composition is not automatically better. a small closed set of variants is clearer as a typed prop. the goal is to expose meaningful choices while preventing invalid combinations. prefer elements or components over render callbacks unless the consumer needs internal state to produce its content. common traps replacing every prop with a compound component. creating many boolean props that produce invalid combinations. using a render prop when static children are sufficient. allowing arbitrary composition that breaks required semantics or accessibility. interview answer i use configuration for a small, stable set of variants and composition when consumers need structural flexibility. children, named slots, or compound components let callers assemble ui without teaching one component every possible layout. the api should still enforce important semantic and accessibility constraints. check yourself when is variant="danger" clearer than supplying a custom component through a slot?',
  },
  {
    number: 20,
    slug: "20-designing-reusable-component-apis",
    title: "Designing Reusable Component APIs",
    kind: "chapter",
    partNumber: 3,
    partName: "Component Design",
    markdown:
      "# Chapter 20 — Designing Reusable Component APIs\n\n## Quick refresher\n\nA reusable component API defines ownership, allowed variants, event semantics, accessibility behavior, defaults, and escape hatches.\n\n## Why this matters\n\nReusability is not maximum flexibility. Good APIs make common correct usage easy and invalid or inaccessible combinations difficult.\n\n## Core mental model\n\nDesign from consumer intent rather than internal implementation:\n\n```tsx\ntype DialogProps = {\n  open: boolean;\n  onOpenChange(open: boolean): void;\n  title: ReactNode;\n  children: ReactNode;\n};\n```\n\n`onOpenChange` describes the proposed state transition without exposing which internal button was clicked. The component owns focus management, Escape handling, and dialog semantics; consumers own whether the dialog is open.\n\nUseful API questions:\n\n- Who owns the state?\n- What are the valid states and combinations?\n- Which semantics must the component guarantee?\n- Which HTML props should be forwarded?\n- Does the escape hatch preserve the contract?\n- Can the API evolve without breaking callers?\n\nPrefer explicit names such as `onValueChange` when the callback reports a semantic value. Keep callbacks stable only when performance evidence or an external subscription requires it—not as a blanket API promise.\n\n## Common traps\n\n- Passing internal setters directly to consumers.\n- Adding props for isolated product-specific exceptions.\n- Accepting arbitrary markup that breaks keyboard or ARIA relationships.\n- Hiding ownership by maintaining a second internal copy of controlled state.\n\n## Interview answer\n\nI start a reusable API from the consumer’s states and events. I define one owner for each value, encode closed variants in types, provide composition where structure must vary, and keep accessibility behavior inside the primitive. Escape hatches should extend presentation without invalidating semantic guarantees.\n\n## Check yourself\n\nWhy is `onOpenChange(nextOpen)` often a better public contract than exposing `setOpen`?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "A reusable component API defines ownership, allowed variants, event semantics, accessibility behavior, defaults, and escape hatches.",
    excerpt:
      "Reusability is not maximum flexibility. Good APIs make common correct usage easy and invalid or inaccessible combinations difficult.",
    readingMinutes: 2,
    searchText:
      "chapter 20 — designing reusable component apis quick refresher a reusable component api defines ownership, allowed variants, event semantics, accessibility behavior, defaults, and escape hatches. why this matters reusability is not maximum flexibility. good apis make common correct usage easy and invalid or inaccessible combinations difficult. core mental model design from consumer intent rather than internal implementation: onopenchange describes the proposed state transition without exposing which internal button was clicked. the component owns focus management, escape handling, and dialog semantics; consumers own whether the dialog is open. useful api questions: who owns the state? what are the valid states and combinations? which semantics must the component guarantee? which html props should be forwarded? does the escape hatch preserve the contract? can the api evolve without breaking callers? prefer explicit names such as onvaluechange when the callback reports a semantic value. keep callbacks stable only when performance evidence or an external subscription requires it—not as a blanket api promise. common traps passing internal setters directly to consumers. adding props for isolated product specific exceptions. accepting arbitrary markup that breaks keyboard or aria relationships. hiding ownership by maintaining a second internal copy of controlled state. interview answer i start a reusable api from the consumer’s states and events. i define one owner for each value, encode closed variants in types, provide composition where structure must vary, and keep accessibility behavior inside the primitive. escape hatches should extend presentation without invalidating semantic guarantees. check yourself why is onopenchange(nextopen) often a better public contract than exposing setopen ?",
  },
  {
    number: 21,
    slug: "21-compound-and-headless-components",
    title: "Compound and Headless Components",
    kind: "chapter",
    partNumber: 3,
    partName: "Component Design",
    markdown:
      '# Chapter 21 — Compound and Headless Components\n\n## Quick refresher\n\nCompound components expose cooperating pieces under one conceptual API. Headless components provide state and behavior while leaving most rendering and styling to consumers.\n\n## Why this matters\n\nThese patterns support flexible design systems, but they require careful state sharing, semantic constraints, and documentation.\n\n## Core mental model\n\n```tsx\n\u003cTabs defaultValue="profile">\n  \u003cTabs.List aria-label="Account">\n    \u003cTabs.Trigger value="profile">Profile\u003c/Tabs.Trigger>\n    \u003cTabs.Trigger value="security">Security\u003c/Tabs.Trigger>\n  \u003c/Tabs.List>\n  \u003cTabs.Panel value="profile">...\u003c/Tabs.Panel>\n  \u003cTabs.Panel value="security">...\u003c/Tabs.Panel>\n\u003c/Tabs>\n```\n\nThe root owns or receives the active value. Descendants communicate through a focused context. Triggers and panels remain composable while the implementation coordinates IDs, selection, keyboard navigation, and ARIA relationships.\n\nA headless API might instead expose a Hook or render function that returns state and prop-getters. This offers more markup control but transfers more responsibility to consumers and can make correct accessibility harder.\n\nKeep context private to the component family, fail clearly when a child is outside its provider, and split contexts when frequently changing state should not rerender unrelated consumers.\n\n## Common traps\n\n- Treating compound components as styling-only namespaces.\n- Exposing context internals as a public API.\n- Allowing flexible markup that breaks required DOM relationships.\n- Using a headless abstraction when a styled semantic primitive would be safer.\n\n## Interview answer\n\nCompound components provide flexible structure while a shared owner coordinates state and semantics. Headless components separate behavior from presentation more aggressively. I use them when consumers need real structural control, but keep accessibility invariants, ownership, and context scope explicit so flexibility does not make correct usage fragile.\n\n## Check yourself\n\nWhat behavior should `Tabs` keep internal even when its visual presentation is fully customizable?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Compound components expose cooperating pieces under one conceptual API. Headless components provide state and behavior while leaving most rendering and styling to consumers.",
    excerpt:
      "These patterns support flexible design systems, but they require careful state sharing, semantic constraints, and documentation.",
    readingMinutes: 2,
    searchText:
      "chapter 21 — compound and headless components quick refresher compound components expose cooperating pieces under one conceptual api. headless components provide state and behavior while leaving most rendering and styling to consumers. why this matters these patterns support flexible design systems, but they require careful state sharing, semantic constraints, and documentation. core mental model the root owns or receives the active value. descendants communicate through a focused context. triggers and panels remain composable while the implementation coordinates ids, selection, keyboard navigation, and aria relationships. a headless api might instead expose a hook or render function that returns state and prop getters. this offers more markup control but transfers more responsibility to consumers and can make correct accessibility harder. keep context private to the component family, fail clearly when a child is outside its provider, and split contexts when frequently changing state should not rerender unrelated consumers. common traps treating compound components as styling only namespaces. exposing context internals as a public api. allowing flexible markup that breaks required dom relationships. using a headless abstraction when a styled semantic primitive would be safer. interview answer compound components provide flexible structure while a shared owner coordinates state and semantics. headless components separate behavior from presentation more aggressively. i use them when consumers need real structural control, but keep accessibility invariants, ownership, and context scope explicit so flexibility does not make correct usage fragile. check yourself what behavior should tabs keep internal even when its visual presentation is fully customizable?",
  },
  {
    number: 22,
    slug: "22-context-and-provider-design",
    title: "Context and Provider Design",
    kind: "chapter",
    partNumber: 3,
    partName: "Component Design",
    markdown:
      '# Chapter 22 — Context and Provider Design\n\n## Quick refresher\n\nContext lets descendants read a value from the nearest matching provider without passing it through every intermediate component.\n\n## Why this matters\n\nContext solves value distribution, not state modeling, caching, or high-frequency update performance by itself.\n\n## Core mental model\n\n```tsx\nconst ThemeContext = createContext\u003cThemeContextValue | null>(null);\n\nfunction useTheme() {\n  const value = useContext(ThemeContext);\n  if (!value) throw new Error("useTheme must be used within ThemeProvider");\n  return value;\n}\n```\n\nThe provider establishes ownership and lifetime; context distributes access. Good candidates include theme, locale, authenticated identity, or state shared by a compound component.\n\nAll consumers are notified when a provider’s `value` changes according to `Object.is`. Avoid creating needless identities and avoid combining unrelated, frequently changing values into one large context:\n\n```tsx\nconst value = useMemo(() => ({ theme, setTheme }), [theme]);\n```\n\nMemoization helps only if the provider’s parent can render without `theme` changing and consumer work matters. Splitting state and actions or separate domains can produce clearer boundaries. For frequent selective subscriptions, an external store with selectors may fit better.\n\n## Common traps\n\n- Using context as a universal global state solution.\n- Providing a fake default that hides a missing provider.\n- Putting unrelated values into one provider object.\n- Memoizing the value without measuring or understanding update fan-out.\n\n## Interview answer\n\nContext distributes a value through a subtree; it does not decide how that value is modeled. I scope providers by ownership and lifetime, expose a focused custom Hook, and keep values cohesive. Because provider changes notify consumers, I split high-frequency domains or use selector-based external stores when update granularity matters.\n\n## Check yourself\n\nWhy can one large application context cause broad rerenders even when a consumer reads only one field?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Context lets descendants read a value from the nearest matching provider without passing it through every intermediate component.",
    excerpt:
      "Context solves value distribution, not state modeling, caching, or high frequency update performance by itself.",
    readingMinutes: 2,
    searchText:
      "chapter 22 — context and provider design quick refresher context lets descendants read a value from the nearest matching provider without passing it through every intermediate component. why this matters context solves value distribution, not state modeling, caching, or high frequency update performance by itself. core mental model the provider establishes ownership and lifetime; context distributes access. good candidates include theme, locale, authenticated identity, or state shared by a compound component. all consumers are notified when a provider’s value changes according to object.is . avoid creating needless identities and avoid combining unrelated, frequently changing values into one large context: memoization helps only if the provider’s parent can render without theme changing and consumer work matters. splitting state and actions or separate domains can produce clearer boundaries. for frequent selective subscriptions, an external store with selectors may fit better. common traps using context as a universal global state solution. providing a fake default that hides a missing provider. putting unrelated values into one provider object. memoizing the value without measuring or understanding update fan out. interview answer context distributes a value through a subtree; it does not decide how that value is modeled. i scope providers by ownership and lifetime, expose a focused custom hook, and keep values cohesive. because provider changes notify consumers, i split high frequency domains or use selector based external stores when update granularity matters. check yourself why can one large application context cause broad rerenders even when a consumer reads only one field?",
  },
  {
    number: 23,
    slug: "23-error-boundaries",
    title: "Error Boundaries",
    kind: "chapter",
    partNumber: 3,
    partName: "Component Design",
    markdown:
      "# Chapter 23 — Error Boundaries\n\n## Quick refresher\n\nAn error boundary catches rendering errors in its descendant tree and displays fallback UI instead of unmounting the entire application.\n\n## Why this matters\n\nProduction React applications need deliberate failure isolation. Interviewers expect you to know both what boundaries catch and what they do not.\n\n## Core mental model\n\n```tsx\n\u003cAppShell>\n  \u003cErrorBoundary fallback={\u003cPanelError />}>\n    \u003cAccountPanel />\n  \u003c/ErrorBoundary>\n\u003c/AppShell>\n```\n\nPlace boundaries around independently recoverable regions, routes, or risky integrations. A boundary can log component-stack information and offer retry or navigation. Changing its `key` is a common way to reset boundary state for a new resource or retry attempt.\n\nTraditional error boundaries are class components using `getDerivedStateFromError` and optionally `componentDidCatch`; applications commonly consume a framework or library wrapper.\n\nBoundaries catch errors thrown while rendering descendants and in relevant descendant lifecycle work. They do not generally catch errors in event handlers, arbitrary asynchronous callbacks, server rendering, or errors thrown by the boundary itself. Handle expected request failures as explicit data states rather than throwing every failure into a boundary.\n\n## Common traps\n\n- Wrapping only the entire application and losing all UI for one panel failure.\n- Expecting a boundary to catch rejected event-handler promises.\n- Treating validation or an expected 404 as an exceptional render failure.\n- Logging an error without providing recovery or useful context.\n\n## Interview answer\n\nError boundaries isolate unexpected failures in descendant rendering so the rest of the interface can remain usable. I place them at meaningful recovery boundaries, log diagnostic context, and provide retry or navigation. Expected data errors remain explicit UI state, and event-handler or unrelated async errors require their own handling.\n\n## Check yourself\n\nWhy should a dashboard often use panel-level boundaries in addition to a route-level boundary?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "An error boundary catches rendering errors in its descendant tree and displays fallback UI instead of unmounting the entire application.",
    excerpt:
      "Production React applications need deliberate failure isolation. Interviewers expect you to know both what boundaries catch and what they do not.",
    readingMinutes: 2,
    searchText:
      "chapter 23 — error boundaries quick refresher an error boundary catches rendering errors in its descendant tree and displays fallback ui instead of unmounting the entire application. why this matters production react applications need deliberate failure isolation. interviewers expect you to know both what boundaries catch and what they do not. core mental model place boundaries around independently recoverable regions, routes, or risky integrations. a boundary can log component stack information and offer retry or navigation. changing its key is a common way to reset boundary state for a new resource or retry attempt. traditional error boundaries are class components using getderivedstatefromerror and optionally componentdidcatch ; applications commonly consume a framework or library wrapper. boundaries catch errors thrown while rendering descendants and in relevant descendant lifecycle work. they do not generally catch errors in event handlers, arbitrary asynchronous callbacks, server rendering, or errors thrown by the boundary itself. handle expected request failures as explicit data states rather than throwing every failure into a boundary. common traps wrapping only the entire application and losing all ui for one panel failure. expecting a boundary to catch rejected event handler promises. treating validation or an expected 404 as an exceptional render failure. logging an error without providing recovery or useful context. interview answer error boundaries isolate unexpected failures in descendant rendering so the rest of the interface can remain usable. i place them at meaningful recovery boundaries, log diagnostic context, and provide retry or navigation. expected data errors remain explicit ui state, and event handler or unrelated async errors require their own handling. check yourself why should a dashboard often use panel level boundaries in addition to a route level boundary?",
  },
  {
    number: 24,
    slug: "24-accessible-interactive-components",
    title: "Accessible Interactive Components",
    kind: "chapter",
    partNumber: 3,
    partName: "Component Design",
    markdown:
      '# Chapter 24 — Accessible Interactive Components\n\n## Quick refresher\n\nStart with native HTML semantics. Add ARIA only when native elements cannot express the required interaction, and implement the complete keyboard and focus behavior of the chosen pattern.\n\n## Why this matters\n\nAccessibility is part of component correctness. A component that works only with a pointer or only visually is incomplete, even if its React state is well designed.\n\n## Core mental model\n\nBuild in this order:\n\n```text\nsemantic element → accessible name → keyboard behavior → focus → state announcement → testing\n```\n\nUse a button instead of a clickable `div`:\n\n```tsx\n\u003cbutton type="button" aria-expanded={open} aria-controls={menuId} onClick={toggle}>\n  Options\n\u003c/button>\n```\n\nThe browser provides focusability, Enter and Space activation, and button semantics. `aria-expanded` communicates component state; `aria-controls` identifies the controlled region.\n\nFor composite widgets such as tabs, listboxes, menus, dialogs, or comboboxes, follow the established interaction pattern. Decide where DOM focus lives, how arrow keys work, how Escape behaves, and where focus returns after dismissal. Do not add an ARIA role without implementing its expected behavior.\n\nTest with keyboard-only navigation, visible focus, accessible-name queries, automated checks, and representative screen-reader testing. Automation cannot verify the complete experience.\n\n## Common traps\n\n- Using `div` click handlers instead of native controls.\n- Adding `tabIndex={0}` without keyboard activation semantics.\n- Treating ARIA as a substitute for semantic HTML.\n- Moving focus unexpectedly or failing to restore it after a dialog closes.\n\n## Interview answer\n\nI begin with native semantics because they provide behavior and accessibility for free. For custom widgets, I implement the recognized keyboard, focus, role, state, and announcement contract—not only ARIA attributes. I verify user-observable behavior with keyboard, accessibility-tree queries, automation, and assistive-technology testing.\n\n## Check yourself\n\nWhy does adding `role="button"` to a `div` still leave important work unfinished?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Start with native HTML semantics. Add ARIA only when native elements cannot express the required interaction, and implement the complete keyboard and focus behavior of the chosen pattern.",
    excerpt:
      "Accessibility is part of component correctness. A component that works only with a pointer or only visually is incomplete, even if its React state is well designed.",
    readingMinutes: 2,
    searchText:
      'chapter 24 — accessible interactive components quick refresher start with native html semantics. add aria only when native elements cannot express the required interaction, and implement the complete keyboard and focus behavior of the chosen pattern. why this matters accessibility is part of component correctness. a component that works only with a pointer or only visually is incomplete, even if its react state is well designed. core mental model build in this order: use a button instead of a clickable div : the browser provides focusability, enter and space activation, and button semantics. aria expanded communicates component state; aria controls identifies the controlled region. for composite widgets such as tabs, listboxes, menus, dialogs, or comboboxes, follow the established interaction pattern. decide where dom focus lives, how arrow keys work, how escape behaves, and where focus returns after dismissal. do not add an aria role without implementing its expected behavior. test with keyboard only navigation, visible focus, accessible name queries, automated checks, and representative screen reader testing. automation cannot verify the complete experience. common traps using div click handlers instead of native controls. adding tabindex={0} without keyboard activation semantics. treating aria as a substitute for semantic html. moving focus unexpectedly or failing to restore it after a dialog closes. interview answer i begin with native semantics because they provide behavior and accessibility for free. for custom widgets, i implement the recognized keyboard, focus, role, state, and announcement contract—not only aria attributes. i verify user observable behavior with keyboard, accessibility tree queries, automation, and assistive technology testing. check yourself why does adding role="button" to a div still leave important work unfinished?',
  },
  {
    number: 25,
    slug: "25-diagnosing-unnecessary-renders",
    title: "Diagnosing Unnecessary Renders",
    kind: "chapter",
    partNumber: 4,
    partName: "Performance",
    markdown:
      '# Chapter 25 — Diagnosing Unnecessary Renders\n\n## Quick refresher\n\n- A render is not automatically a performance problem.\n- Find a slow user interaction before counting renders.\n- Use the React Profiler to identify expensive commits and components.\n- Fix state ownership and component boundaries before adding memoization.\n- Measure again to verify that the change improved user-visible behavior.\n\n## Why this matters\n\n“This component rerenders too much” is a common but incomplete diagnosis. Rendering is how React calculates UI. A fast render that produces no expensive commit may be harmless, while one infrequent render of a large tree may block an important interaction.\n\nSenior candidates should connect a user-visible symptom to measured React work and then choose the smallest justified change.\n\n## Core mental model\n\nUse this investigation chain:\n\n```text\nslow interaction\n      ↓\nrecord a profile\n      ↓\nfind the expensive commit\n      ↓\nidentify why components rendered\n      ↓\nchange ownership, work, or boundaries\n      ↓\nprofile again\n```\n\nStart with a reproducible interaction, representative data, and a production-like build. In React DevTools Profiler, inspect:\n\n- which commit overlaps the delay;\n- which components consumed the most render time;\n- why they rendered;\n- whether the cost is React rendering or browser layout, paint, or scripting outside React.\n\n## Common causes\n\n### State is owned too high\n\n```tsx\nfunction Page() {\n  const [query, setQuery] = useState("");\n\n  return (\n    \u003c>\n      \u003cSearchBox value={query} onChange={setQuery} />\n      \u003cExpensiveDashboard />\n    \u003c/>\n  );\n}\n```\n\nEvery keystroke renders `Page` and normally evaluates `ExpensiveDashboard`. If only search needs the query, move the state into a smaller subtree. Colocation can eliminate work without adding memoization.\n\n### Expensive work runs during every render\n\n```tsx\nconst visibleRows = sortAndFilter(rows, query);\n```\n\nFirst improve the algorithm or reduce the data processed. If the calculation is genuinely expensive and its inputs often remain unchanged, `useMemo` may avoid repeating it.\n\n### A large subtree receives a frequently changing value\n\nBroad context providers, animation state, pointer coordinates, or controlled input state can notify more consumers than necessary. Split ownership or subscription boundaries so frequently changing data reaches only the components that need it.\n\n### Child props change identity\n\nNew objects and functions can defeat `memo`:\n\n```tsx\n\u003cResults filters={{ query }} onSelect={item => selectItem(item)} />\n```\n\nDo not stabilize every value automatically. First confirm that `Results` is expensive, memoized, and frequently receives otherwise unchanged inputs. Then simplify the API, move value creation, or stabilize the relevant props.\n\n## Choosing the fix\n\nPrefer fixes in this order:\n\n1. colocate state and narrow update scope;\n2. remove unnecessary Effects and derived-state updates;\n3. reduce expensive work or improve its algorithm;\n4. split large components at meaningful boundaries;\n5. virtualize large collections;\n6. apply `memo`, `useMemo`, or `useCallback` where profiling justifies them;\n7. use transitions when non-urgent work cannot be avoided synchronously.\n\nMemoization trades computation for comparisons, retained values, dependency management, and API complexity. It is useful when skipped work is more expensive than those costs.\n\n## Common traps\n\n- Treating every parent-to-child render as waste.\n- Measuring only in development or with tiny test data.\n- Adding `useCallback` when the callback is not passed to a memoized consumer or used as a dependency.\n- Ignoring browser layout and paint because the React profile looks busy.\n- Comparing render counts instead of interaction latency.\n- Optimizing a component that is not on a user-critical path.\n\n## Interview answer\n\nI start from a slow user interaction, reproduce it with representative data, and record a React profile. I identify the expensive commit, the components that consumed time, and why they rendered, while checking whether the real cost is outside React. I first narrow state ownership, remove redundant updates, and reduce the work performed. I add memoization only at an expensive boundary with stable inputs, then profile the same interaction again to verify the improvement.\n\n## Check yourself\n\n1. Why is a high render count not sufficient evidence of a performance problem?\n2. When can moving state downward outperform wrapping a large subtree in `memo`?\n3. What evidence would justify adding `useCallback`?\n\n## Related chapters\n\n- [What causes a component to render](/react/handbook/chapters/02-what-causes-a-component-to-render)\n- [State ownership and lifting state](/react/handbook/chapters/17-state-ownership-and-lifting-state)\n- [React performance investigations](/performance/investigations)',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common causes",
      "Choosing the fix",
      "Common traps",
      "Interview answer",
      "Check yourself",
      "Related chapters",
    ],
    quickRefresher:
      "A render is not automatically a performance problem. Find a slow user interaction before counting renders. Use the React Profiler to identify expensive commits and components. Fix state ownership and component boundaries before adding memoization. Measure again to verify that the change improved user visible behavior.",
    excerpt:
      "“This component rerenders too much” is a common but incomplete diagnosis. Rendering is how React calculates UI. A fast render that produces no expensive commit may be harmless, while one infrequent render of a large tree may block an important interaction.",
    readingMinutes: 3,
    searchText:
      "chapter 25 — diagnosing unnecessary renders quick refresher a render is not automatically a performance problem. find a slow user interaction before counting renders. use the react profiler to identify expensive commits and components. fix state ownership and component boundaries before adding memoization. measure again to verify that the change improved user visible behavior. why this matters “this component rerenders too much” is a common but incomplete diagnosis. rendering is how react calculates ui. a fast render that produces no expensive commit may be harmless, while one infrequent render of a large tree may block an important interaction. senior candidates should connect a user visible symptom to measured react work and then choose the smallest justified change. core mental model use this investigation chain: start with a reproducible interaction, representative data, and a production like build. in react devtools profiler, inspect: which commit overlaps the delay; which components consumed the most render time; why they rendered; whether the cost is react rendering or browser layout, paint, or scripting outside react. common causes state is owned too high every keystroke renders page and normally evaluates expensivedashboard . if only search needs the query, move the state into a smaller subtree. colocation can eliminate work without adding memoization. expensive work runs during every render first improve the algorithm or reduce the data processed. if the calculation is genuinely expensive and its inputs often remain unchanged, usememo may avoid repeating it. a large subtree receives a frequently changing value broad context providers, animation state, pointer coordinates, or controlled input state can notify more consumers than necessary. split ownership or subscription boundaries so frequently changing data reaches only the components that need it. child props change identity new objects and functions can defeat memo : do not stabilize every value automatically. first confirm that results is expensive, memoized, and frequently receives otherwise unchanged inputs. then simplify the api, move value creation, or stabilize the relevant props. choosing the fix prefer fixes in this order: 1. colocate state and narrow update scope; 2. remove unnecessary effects and derived state updates; 3. reduce expensive work or improve its algorithm; 4. split large components at meaningful boundaries; 5. virtualize large collections; 6. apply memo , usememo , or usecallback where profiling justifies them; 7. use transitions when non urgent work cannot be avoided synchronously. memoization trades computation for comparisons, retained values, dependency management, and api complexity. it is useful when skipped work is more expensive than those costs. common traps treating every parent to child render as waste. measuring only in development or with tiny test data. adding usecallback when the callback is not passed to a memoized consumer or used as a dependency. ignoring browser layout and paint because the react profile looks busy. comparing render counts instead of interaction latency. optimizing a component that is not on a user critical path. interview answer i start from a slow user interaction, reproduce it with representative data, and record a react profile. i identify the expensive commit, the components that consumed time, and why they rendered, while checking whether the real cost is outside react. i first narrow state ownership, remove redundant updates, and reduce the work performed. i add memoization only at an expensive boundary with stable inputs, then profile the same interaction again to verify the improvement. check yourself 1. why is a high render count not sufficient evidence of a performance problem? 2. when can moving state downward outperform wrapping a large subtree in memo ? 3. what evidence would justify adding usecallback ? related chapters what causes a component to render state ownership and lifting state react performance investigations",
  },
  {
    number: 26,
    slug: "26-referential-equality",
    title: "Referential Equality",
    kind: "chapter",
    partNumber: 4,
    partName: "Performance",
    markdown:
      "# Chapter 26 — Referential Equality\n\n## Quick refresher\n\n- Primitives compare by value; objects, arrays, and functions compare by identity.\n- React uses `Object.is` for state bailouts, context values, and Hook dependencies.\n- New object and function literals have new identities on every render.\n- Referential stability matters only at a boundary that compares identity.\n\n## Why this matters\n\nIdentity explains why Effects rerun, memoized children render, and context consumers update even when values look structurally equal.\n\n## Core mental model\n\n```tsx\nconst options = { roomId };\n\nuseEffect(() => {\n  const connection = connect(options);\n  return () => connection.disconnect();\n}, [options]);\n```\n\n`options` is new on every render, so the Effect resynchronizes. The simplest fix is often to create it inside the Effect and depend on the primitive:\n\n```tsx\nuseEffect(() => {\n  const connection = connect({ roomId });\n  return () => connection.disconnect();\n}, [roomId]);\n```\n\nIdentity is not inherently a problem. New values are cheap unless they defeat a meaningful cache, retrigger synchronization, or notify consumers. Preserve identity deliberately at those boundaries through simpler APIs, moving creation, or memoization.\n\nState updates must also use new identities when data changes. Mutating an object and passing the same reference can let React bail out even though its contents changed.\n\n## Common traps\n\n- Memoizing every object and callback.\n- Comparing objects by reference when structural equality is required.\n- Mutating state and reusing its reference.\n- Ignoring identity in Effect dependencies or context provider values.\n\n## Interview answer\n\nReact frequently uses `Object.is`, so equal-looking objects are different when their references differ. This matters at dependency arrays, memoized props, context values, and state updates. I stabilize identity only where a consumer compares it and the skipped work matters; otherwise new values are normal JavaScript behavior.\n\n## Check yourself\n\nWhy can moving an object inside an Effect be better than memoizing it?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Primitives compare by value; objects, arrays, and functions compare by identity. React uses Object.is for state bailouts, context values, and Hook dependencies. New object and function literals have new identities on every render. Referential stability matters only at a boundary that compares identity.",
    excerpt:
      "Identity explains why Effects rerun, memoized children render, and context consumers update even when values look structurally equal.",
    readingMinutes: 2,
    searchText:
      "chapter 26 — referential equality quick refresher primitives compare by value; objects, arrays, and functions compare by identity. react uses object.is for state bailouts, context values, and hook dependencies. new object and function literals have new identities on every render. referential stability matters only at a boundary that compares identity. why this matters identity explains why effects rerun, memoized children render, and context consumers update even when values look structurally equal. core mental model options is new on every render, so the effect resynchronizes. the simplest fix is often to create it inside the effect and depend on the primitive: identity is not inherently a problem. new values are cheap unless they defeat a meaningful cache, retrigger synchronization, or notify consumers. preserve identity deliberately at those boundaries through simpler apis, moving creation, or memoization. state updates must also use new identities when data changes. mutating an object and passing the same reference can let react bail out even though its contents changed. common traps memoizing every object and callback. comparing objects by reference when structural equality is required. mutating state and reusing its reference. ignoring identity in effect dependencies or context provider values. interview answer react frequently uses object.is , so equal looking objects are different when their references differ. this matters at dependency arrays, memoized props, context values, and state updates. i stabilize identity only where a consumer compares it and the skipped work matters; otherwise new values are normal javascript behavior. check yourself why can moving an object inside an effect be better than memoizing it?",
  },
  {
    number: 27,
    slug: "27-memo-usememo-and-usecallback",
    title: "memo, useMemo, and useCallback",
    kind: "chapter",
    partNumber: 4,
    partName: "Performance",
    markdown:
      "# Chapter 27 — memo, useMemo, and useCallback\n\n## Quick refresher\n\n- `memo` may skip rendering a component when its props compare equal.\n- `useMemo` caches a calculated value between renders.\n- `useCallback` caches a function identity; it does not prevent creating the function expression.\n- All three are performance optimizations, not correctness tools.\n\n## Why this matters\n\nThese APIs are frequently overused in interviews. A strong answer explains the boundary, cost, and evidence that make memoization valuable.\n\n## Core mental model\n\n```tsx\nconst Results = memo(function Results({ items, onSelect }: Props) {\n  return items.map(item => (\n    \u003cbutton key={item.id} onClick={() => onSelect(item)}>{item.name}\u003c/button>\n  ));\n});\n\nconst visibleItems = useMemo(\n  () => expensiveFilter(items, query),\n  [items, query],\n);\n\nconst handleSelect = useCallback(\n  (item: Item) => onSelect(item.id),\n  [onSelect],\n);\n```\n\nThis combination is useful only if `Results` is expensive, its inputs often remain unchanged, and the cache costs less than the skipped work. One always-new prop breaks the `memo` bailout.\n\n`useMemo` is not a semantic guarantee. Code should remain correct if React discards the cached value. Use state or refs for information whose persistence is part of behavior.\n\nPrefer structural fixes first: colocate state, accept JSX children, remove unnecessary Effects, and reduce the underlying computation.\n\n## Common traps\n\n- Using `useMemo` for correctness or side effects.\n- Adding `useCallback` when no consumer benefits from stable identity.\n- Writing expensive custom prop comparators that omit function closures.\n- Ignoring memory retention and dependency complexity.\n\n## Interview answer\n\n`memo` caches a component render by props, `useMemo` caches a calculation, and `useCallback` caches a function identity. I use them together only at a measured expensive boundary whose inputs often stay equal. I first improve state placement and the work itself, because memoization adds comparisons, retained values, and dependency maintenance.\n\n## Check yourself\n\nWhen does `useCallback` make no observable performance difference?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "memo may skip rendering a component when its props compare equal. useMemo caches a calculated value between renders. useCallback caches a function identity; it does not prevent creating the function expression. All three are performance optimizations, not correctness tools.",
    excerpt:
      "These APIs are frequently overused in interviews. A strong answer explains the boundary, cost, and evidence that make memoization valuable.",
    readingMinutes: 2,
    searchText:
      "chapter 27 — memo, usememo, and usecallback quick refresher memo may skip rendering a component when its props compare equal. usememo caches a calculated value between renders. usecallback caches a function identity; it does not prevent creating the function expression. all three are performance optimizations, not correctness tools. why this matters these apis are frequently overused in interviews. a strong answer explains the boundary, cost, and evidence that make memoization valuable. core mental model this combination is useful only if results is expensive, its inputs often remain unchanged, and the cache costs less than the skipped work. one always new prop breaks the memo bailout. usememo is not a semantic guarantee. code should remain correct if react discards the cached value. use state or refs for information whose persistence is part of behavior. prefer structural fixes first: colocate state, accept jsx children, remove unnecessary effects, and reduce the underlying computation. common traps using usememo for correctness or side effects. adding usecallback when no consumer benefits from stable identity. writing expensive custom prop comparators that omit function closures. ignoring memory retention and dependency complexity. interview answer memo caches a component render by props, usememo caches a calculation, and usecallback caches a function identity. i use them together only at a measured expensive boundary whose inputs often stay equal. i first improve state placement and the work itself, because memoization adds comparisons, retained values, and dependency maintenance. check yourself when does usecallback make no observable performance difference?",
  },
  {
    number: 28,
    slug: "28-context-performance",
    title: "Context Performance",
    kind: "chapter",
    partNumber: 4,
    partName: "Performance",
    markdown:
      "# Chapter 28 — Context Performance\n\n## Quick refresher\n\nWhen a provider value changes, React notifies components that consume that context. `memo` does not block a context update inside a consumer.\n\n## Why this matters\n\nA broad provider containing frequently changing data can create large update fan-out even when each consumer reads only one field.\n\n## Core mental model\n\n```tsx\n\u003cAppContext.Provider value={{ user, theme, notifications, setTheme }}>\n  \u003cApp />\n\u003c/AppContext.Provider>\n```\n\nThe object is new whenever the provider renders, and any changed field changes the complete context value. Improve the model before optimizing identities:\n\n```tsx\n\u003cAuthContext.Provider value={user}>\n  \u003cThemeContext.Provider value={theme}>\n    \u003cNotificationsProvider>{children}\u003c/NotificationsProvider>\n  \u003c/ThemeContext.Provider>\n\u003c/AuthContext.Provider>\n```\n\nSplit providers by domain and update frequency, place them near the subtree that needs them, and keep state and dispatch APIs focused. Memoizing a provider value can prevent notifications caused only by a parent render, but cannot prevent legitimate updates when a dependency changes.\n\nFor high-frequency data or many consumers that need slices, use an external store with selector-based subscriptions. Also consider whether composition or ordinary props would make dependencies clearer.\n\n## Common traps\n\n- Assuming consumers subscribe only to fields they read.\n- Wrapping consumers in `memo` and expecting context updates to stop.\n- Putting unrelated application state into one provider.\n- Replacing prop passing with context without an ownership reason.\n\n## Interview answer\n\nContext updates are based on the provider value’s identity, and consumers do not select individual object fields automatically. I reduce fan-out by splitting cohesive domains, scoping providers, and avoiding needless value changes. For high-frequency selective subscriptions, I prefer a store designed around selectors rather than forcing context to be a full state-management system.\n\n## Check yourself\n\nWhy does memoizing a provider object not help when one of its dependencies changes on every keystroke?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "When a provider value changes, React notifies components that consume that context. memo does not block a context update inside a consumer.",
    excerpt:
      "A broad provider containing frequently changing data can create large update fan out even when each consumer reads only one field.",
    readingMinutes: 2,
    searchText:
      "chapter 28 — context performance quick refresher when a provider value changes, react notifies components that consume that context. memo does not block a context update inside a consumer. why this matters a broad provider containing frequently changing data can create large update fan out even when each consumer reads only one field. core mental model the object is new whenever the provider renders, and any changed field changes the complete context value. improve the model before optimizing identities: split providers by domain and update frequency, place them near the subtree that needs them, and keep state and dispatch apis focused. memoizing a provider value can prevent notifications caused only by a parent render, but cannot prevent legitimate updates when a dependency changes. for high frequency data or many consumers that need slices, use an external store with selector based subscriptions. also consider whether composition or ordinary props would make dependencies clearer. common traps assuming consumers subscribe only to fields they read. wrapping consumers in memo and expecting context updates to stop. putting unrelated application state into one provider. replacing prop passing with context without an ownership reason. interview answer context updates are based on the provider value’s identity, and consumers do not select individual object fields automatically. i reduce fan out by splitting cohesive domains, scoping providers, and avoiding needless value changes. for high frequency selective subscriptions, i prefer a store designed around selectors rather than forcing context to be a full state management system. check yourself why does memoizing a provider object not help when one of its dependencies changes on every keystroke?",
  },
  {
    number: 29,
    slug: "29-code-splitting-and-lazy-loading",
    title: "Code Splitting and Lazy Loading",
    kind: "chapter",
    partNumber: 4,
    partName: "Performance",
    markdown:
      '# Chapter 29 — Code Splitting and Lazy Loading\n\n## Quick refresher\n\nCode splitting divides JavaScript into chunks loaded when needed. `lazy` defers loading a component module, while `Suspense` provides a fallback during loading.\n\n## Why this matters\n\nShipping less initial JavaScript can improve loading and responsiveness, but excessive splitting creates request overhead, waterfalls, and disruptive fallback transitions.\n\n## Core mental model\n\n```tsx\nconst SettingsPanel = lazy(() => import("./SettingsPanel"));\n\nfunction SettingsRoute() {\n  return (\n    \u003cSuspense fallback={\u003cSettingsSkeleton />}>\n      \u003cSettingsPanel />\n    \u003c/Suspense>\n  );\n}\n```\n\nChoose boundaries around routes, large optional features, editors, charts, or rarely opened dialogs—not every small component. The fallback should preserve layout and appear at a boundary that matches the user experience.\n\nLoading code only after a click can create a visible delay. Prefetch likely next routes or modules on intent, idle time, or viewport proximity when the bandwidth tradeoff is justified. Framework route-level splitting is usually the starting point.\n\nSplitting code does not reduce total application code by itself. Remove unused dependencies, import narrowly, and inspect chunk duplication. Measure transferred bytes, parse and execution time, and the interaction that first needs the lazy chunk.\n\n## Common traps\n\n- Lazy-loading tiny components and increasing coordination overhead.\n- Creating sequential code and data waterfalls.\n- Showing a full-page spinner for a small deferred region.\n- Measuring chunk count instead of user-visible loading behavior.\n\n## Interview answer\n\nI split at meaningful route or feature boundaries so the initial path ships less JavaScript. I place Suspense fallbacks around independently loading regions, avoid code-data waterfalls, and prefetch likely next work when appropriate. I verify both initial improvement and the latency introduced when the deferred feature is first used.\n\n## Check yourself\n\nWhy can a smaller initial bundle still produce a worse first interaction with a lazy feature?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Code splitting divides JavaScript into chunks loaded when needed. lazy defers loading a component module, while Suspense provides a fallback during loading.",
    excerpt:
      "Shipping less initial JavaScript can improve loading and responsiveness, but excessive splitting creates request overhead, waterfalls, and disruptive fallback transitions.",
    readingMinutes: 2,
    searchText:
      "chapter 29 — code splitting and lazy loading quick refresher code splitting divides javascript into chunks loaded when needed. lazy defers loading a component module, while suspense provides a fallback during loading. why this matters shipping less initial javascript can improve loading and responsiveness, but excessive splitting creates request overhead, waterfalls, and disruptive fallback transitions. core mental model choose boundaries around routes, large optional features, editors, charts, or rarely opened dialogs—not every small component. the fallback should preserve layout and appear at a boundary that matches the user experience. loading code only after a click can create a visible delay. prefetch likely next routes or modules on intent, idle time, or viewport proximity when the bandwidth tradeoff is justified. framework route level splitting is usually the starting point. splitting code does not reduce total application code by itself. remove unused dependencies, import narrowly, and inspect chunk duplication. measure transferred bytes, parse and execution time, and the interaction that first needs the lazy chunk. common traps lazy loading tiny components and increasing coordination overhead. creating sequential code and data waterfalls. showing a full page spinner for a small deferred region. measuring chunk count instead of user visible loading behavior. interview answer i split at meaningful route or feature boundaries so the initial path ships less javascript. i place suspense fallbacks around independently loading regions, avoid code data waterfalls, and prefetch likely next work when appropriate. i verify both initial improvement and the latency introduced when the deferred feature is first used. check yourself why can a smaller initial bundle still produce a worse first interaction with a lazy feature?",
  },
  {
    number: 30,
    slug: "30-list-virtualization",
    title: "List Virtualization",
    kind: "chapter",
    partNumber: 4,
    partName: "Performance",
    markdown:
      "# Chapter 30 — List Virtualization\n\n## Quick refresher\n\nVirtualization renders only the visible window of a large collection plus a small overscan region, while preserving the scrollable size.\n\n## Why this matters\n\nLarge DOM trees increase React rendering, browser style and layout work, memory use, and update cost. Virtualization bounds that work, but adds scrolling and accessibility complexity.\n\n## Core mental model\n\n```text\n10,000 data items\n      ↓\nscroll position + viewport size\n      ↓\nrender items 240–280\n      ↓\noffset them within a full-height container\n```\n\nUse a proven library unless the interview explicitly asks for implementation. The essential inputs are item count, viewport size, scroll offset, estimated or measured row sizes, overscan, and stable item identity.\n\nFixed-height rows are simpler. Variable heights require measurement, cache invalidation, and scroll-position correction. Overscan reduces blank flashes during fast scrolling but increases rendered work.\n\nVirtualization affects keyboard navigation, focus, browser find, screen-reader navigation, printing, and scroll restoration. Focused items should not disappear unexpectedly, and list position can be communicated with appropriate semantics such as `aria-posinset` and `aria-setsize` when needed.\n\nPagination or incremental rendering may be better when users need shareable pages, complete document semantics, or server-side query boundaries.\n\n## Common traps\n\n- Virtualizing small lists without evidence.\n- Using array indices as identities for changing data.\n- Ignoring variable row height and focus behavior.\n- Optimizing React rendering while expensive cell work remains unchanged.\n\n## Interview answer\n\nVirtualization limits rendering and DOM work to the visible range plus overscan. I use it for genuinely large collections, preferably through a tested library, and account for stable keys, variable measurement, focus, accessibility, and scroll restoration. I compare it with pagination because virtualization is not automatically the best product behavior.\n\n## Check yourself\n\nWhat new correctness problems appear when the focused row scrolls outside the virtualized window?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Virtualization renders only the visible window of a large collection plus a small overscan region, while preserving the scrollable size.",
    excerpt:
      "Large DOM trees increase React rendering, browser style and layout work, memory use, and update cost. Virtualization bounds that work, but adds scrolling and accessibility complexity.",
    readingMinutes: 2,
    searchText:
      "chapter 30 — list virtualization quick refresher virtualization renders only the visible window of a large collection plus a small overscan region, while preserving the scrollable size. why this matters large dom trees increase react rendering, browser style and layout work, memory use, and update cost. virtualization bounds that work, but adds scrolling and accessibility complexity. core mental model use a proven library unless the interview explicitly asks for implementation. the essential inputs are item count, viewport size, scroll offset, estimated or measured row sizes, overscan, and stable item identity. fixed height rows are simpler. variable heights require measurement, cache invalidation, and scroll position correction. overscan reduces blank flashes during fast scrolling but increases rendered work. virtualization affects keyboard navigation, focus, browser find, screen reader navigation, printing, and scroll restoration. focused items should not disappear unexpectedly, and list position can be communicated with appropriate semantics such as aria posinset and aria setsize when needed. pagination or incremental rendering may be better when users need shareable pages, complete document semantics, or server side query boundaries. common traps virtualizing small lists without evidence. using array indices as identities for changing data. ignoring variable row height and focus behavior. optimizing react rendering while expensive cell work remains unchanged. interview answer virtualization limits rendering and dom work to the visible range plus overscan. i use it for genuinely large collections, preferably through a tested library, and account for stable keys, variable measurement, focus, accessibility, and scroll restoration. i compare it with pagination because virtualization is not automatically the best product behavior. check yourself what new correctness problems appear when the focused row scrolls outside the virtualized window?",
  },
  {
    number: 31,
    slug: "31-profiling-react-applications",
    title: "Profiling React Applications",
    kind: "chapter",
    partNumber: 4,
    partName: "Performance",
    markdown:
      "# Chapter 31 — Profiling React Applications\n\n## Quick refresher\n\nThe React Profiler records commits, component render durations, and update causes. Browser performance tools show the wider main-thread, layout, paint, and network picture.\n\n## Why this matters\n\nOptimization without a reproducible profile is guessing. React timing must also be connected to the user interaction and browser work that users experience.\n\n## Core mental model\n\nUse a controlled workflow:\n\n1. Define the slow interaction and success measure.\n2. Reproduce with realistic data and hardware.\n3. Record a baseline in a production-like build.\n4. Locate the relevant commit.\n5. Inspect the flamegraph or ranked view and render reasons.\n6. Form one hypothesis and make one targeted change.\n7. repeat the same recording and compare.\n\nThe Profiler distinguishes **actual duration**, the work performed for a commit, from **base duration**, an estimate of rendering the subtree without memoization. A component appearing in a flamegraph does not prove it is the bottleneck; inspect its duration and whether it lies on the delayed interaction path.\n\nUse the browser Performance panel when React finishes quickly but the frame remains slow. Layout, paint, garbage collection, event handlers, third-party scripts, or network waits may dominate.\n\nThe `\u003cProfiler>` API can collect targeted timings in code, but production instrumentation should be sampled carefully because profiling itself has overhead.\n\n## Common traps\n\n- Profiling only development behavior.\n- Recording many unrelated interactions in one trace.\n- Comparing runs with different inputs or cache state.\n- Optimizing the tallest-looking component without checking duration.\n- Declaring success without repeating the user-visible measurement.\n\n## Interview answer\n\nI profile a specific slow interaction with representative data, identify the relevant commit, and inspect component duration and render causes. I correlate React work with the browser timeline so I do not mistake layout or scripting for React cost. After one targeted change, I repeat the same scenario and compare both the profile and user-facing latency.\n\n## Check yourself\n\nWhat should you investigate when React commits are short but the browser still misses frames?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "The React Profiler records commits, component render durations, and update causes. Browser performance tools show the wider main thread, layout, paint, and network picture.",
    excerpt:
      "Optimization without a reproducible profile is guessing. React timing must also be connected to the user interaction and browser work that users experience.",
    readingMinutes: 2,
    searchText:
      "chapter 31 — profiling react applications quick refresher the react profiler records commits, component render durations, and update causes. browser performance tools show the wider main thread, layout, paint, and network picture. why this matters optimization without a reproducible profile is guessing. react timing must also be connected to the user interaction and browser work that users experience. core mental model use a controlled workflow: 1. define the slow interaction and success measure. 2. reproduce with realistic data and hardware. 3. record a baseline in a production like build. 4. locate the relevant commit. 5. inspect the flamegraph or ranked view and render reasons. 6. form one hypothesis and make one targeted change. 7. repeat the same recording and compare. the profiler distinguishes actual duration , the work performed for a commit, from base duration , an estimate of rendering the subtree without memoization. a component appearing in a flamegraph does not prove it is the bottleneck; inspect its duration and whether it lies on the delayed interaction path. use the browser performance panel when react finishes quickly but the frame remains slow. layout, paint, garbage collection, event handlers, third party scripts, or network waits may dominate. the api can collect targeted timings in code, but production instrumentation should be sampled carefully because profiling itself has overhead. common traps profiling only development behavior. recording many unrelated interactions in one trace. comparing runs with different inputs or cache state. optimizing the tallest looking component without checking duration. declaring success without repeating the user visible measurement. interview answer i profile a specific slow interaction with representative data, identify the relevant commit, and inspect component duration and render causes. i correlate react work with the browser timeline so i do not mistake layout or scripting for react cost. after one targeted change, i repeat the same scenario and compare both the profile and user facing latency. check yourself what should you investigate when react commits are short but the browser still misses frames?",
  },
  {
    number: 32,
    slug: "32-transitions-and-deferred-updates",
    title: "Transitions and Deferred Updates",
    kind: "chapter",
    partNumber: 4,
    partName: "Performance",
    markdown:
      '# Chapter 32 — Transitions and Deferred Updates\n\n## Quick refresher\n\nTransitions mark updates as non-urgent so urgent interactions can remain responsive. `useDeferredValue` lets a subtree lag behind a rapidly changing value.\n\n## Why this matters\n\nConcurrent scheduling can improve responsiveness when expensive UI updates compete with typing or navigation. It does not make the expensive work faster or replace debouncing.\n\n## Core mental model\n\nKeep the controlled input urgent and defer the expensive result update:\n\n```tsx\nconst [query, setQuery] = useState("");\nconst deferredQuery = useDeferredValue(query);\n\nreturn (\n  \u003c>\n    \u003cinput value={query} onChange={event => setQuery(event.target.value)} />\n    \u003cResults query={deferredQuery} />\n  \u003c/>\n);\n```\n\nThe input reflects every key immediately while `Results` may temporarily render an older query. Indicate stale content visually when that distinction matters.\n\nUse `startTransition` when you control the state update:\n\n```tsx\nconst [isPending, startTransition] = useTransition();\n\nfunction selectTab(tab: Tab) {\n  startTransition(() => setActiveTab(tab));\n}\n```\n\nTransition work is interruptible and can be restarted by urgent updates. Therefore rendering must remain pure. Do not use transitions to control text inputs, and do not wrap work that must complete immediately.\n\nDebouncing waits for inactivity and reduces how often work starts. Deferral allows work to begin but gives urgent updates priority. They solve different problems and may sometimes be combined.\n\n## Common traps\n\n- Expecting transitions to reduce computation time.\n- Marking the controlled input update as non-urgent.\n- Confusing `isPending` with a network loading state.\n- Using deferral instead of fixing an unnecessarily expensive tree.\n- Hiding stale content without communicating pending state.\n\n## Interview answer\n\nTransitions separate urgent updates, such as input feedback, from non-urgent rendering that may be interrupted. `startTransition` marks an update I control; `useDeferredValue` gives a subtree a deferred version of a value. They improve scheduling rather than computation, so I still reduce expensive work and use debouncing separately when I need to limit external requests.\n\n## Check yourself\n\nHow does `useDeferredValue(query)` differ from debouncing `query` before a network request?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Transitions mark updates as non urgent so urgent interactions can remain responsive. useDeferredValue lets a subtree lag behind a rapidly changing value.",
    excerpt:
      "Concurrent scheduling can improve responsiveness when expensive UI updates compete with typing or navigation. It does not make the expensive work faster or replace debouncing.",
    readingMinutes: 2,
    searchText:
      "chapter 32 — transitions and deferred updates quick refresher transitions mark updates as non urgent so urgent interactions can remain responsive. usedeferredvalue lets a subtree lag behind a rapidly changing value. why this matters concurrent scheduling can improve responsiveness when expensive ui updates compete with typing or navigation. it does not make the expensive work faster or replace debouncing. core mental model keep the controlled input urgent and defer the expensive result update: the input reflects every key immediately while results may temporarily render an older query. indicate stale content visually when that distinction matters. use starttransition when you control the state update: transition work is interruptible and can be restarted by urgent updates. therefore rendering must remain pure. do not use transitions to control text inputs, and do not wrap work that must complete immediately. debouncing waits for inactivity and reduces how often work starts. deferral allows work to begin but gives urgent updates priority. they solve different problems and may sometimes be combined. common traps expecting transitions to reduce computation time. marking the controlled input update as non urgent. confusing ispending with a network loading state. using deferral instead of fixing an unnecessarily expensive tree. hiding stale content without communicating pending state. interview answer transitions separate urgent updates, such as input feedback, from non urgent rendering that may be interrupted. starttransition marks an update i control; usedeferredvalue gives a subtree a deferred version of a value. they improve scheduling rather than computation, so i still reduce expensive work and use debouncing separately when i need to limit external requests. check yourself how does usedeferredvalue(query) differ from debouncing query before a network request?",
  },
  {
    number: 33,
    slug: "33-client-and-server-rendering",
    title: "Client and Server Rendering",
    kind: "chapter",
    partNumber: 5,
    partName: "Modern React Architecture",
    markdown:
      "# Chapter 33 — Client and Server Rendering\n\n## Quick refresher\n\nClient rendering builds UI in the browser. Server rendering produces initial HTML on the server; client JavaScript can then hydrate it. Modern applications commonly combine strategies by route and component.\n\n## Why this matters\n\nRendering location affects startup cost, SEO, caching, data access, interactivity, and operational complexity.\n\n## Core mental model\n\n```text\nCSR: shell → JavaScript → data → UI\nSSR: request → server HTML → browser display → hydration\n```\n\nCSR supports rich navigation but can delay useful content behind JavaScript and data waterfalls. SSR can show content earlier and access server resources directly, but adds server work, hydration cost, and consistency requirements. Static generation moves rendering to build or revalidation time.\n\nChoose per route: public content may favor server or static output; highly interactive authenticated screens may rely more on client behavior. Measure complete navigation and interaction, not only HTML arrival.\n\n## Common traps\n\n- Calling SSR automatically faster.\n- Treating the application as entirely client- or server-rendered.\n- Ignoring hydration and client JavaScript cost.\n- Fetching the same data again unnecessarily after hydration.\n\n## Interview answer\n\nI choose rendering boundaries from user journeys, data location, cacheability, and interaction needs. Server rendering can improve initial delivery, while client rendering supports continued interaction. Hybrid architectures usually work best, provided we measure HTML, JavaScript, hydration, and navigation together.\n\n## Check yourself\n\nWhy can fast server HTML still lead to a slow interactive experience?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Client rendering builds UI in the browser. Server rendering produces initial HTML on the server; client JavaScript can then hydrate it. Modern applications commonly combine strategies by route and component.",
    excerpt:
      "Rendering location affects startup cost, SEO, caching, data access, interactivity, and operational complexity.",
    readingMinutes: 1,
    searchText:
      "chapter 33 — client and server rendering quick refresher client rendering builds ui in the browser. server rendering produces initial html on the server; client javascript can then hydrate it. modern applications commonly combine strategies by route and component. why this matters rendering location affects startup cost, seo, caching, data access, interactivity, and operational complexity. core mental model csr supports rich navigation but can delay useful content behind javascript and data waterfalls. ssr can show content earlier and access server resources directly, but adds server work, hydration cost, and consistency requirements. static generation moves rendering to build or revalidation time. choose per route: public content may favor server or static output; highly interactive authenticated screens may rely more on client behavior. measure complete navigation and interaction, not only html arrival. common traps calling ssr automatically faster. treating the application as entirely client or server rendered. ignoring hydration and client javascript cost. fetching the same data again unnecessarily after hydration. interview answer i choose rendering boundaries from user journeys, data location, cacheability, and interaction needs. server rendering can improve initial delivery, while client rendering supports continued interaction. hybrid architectures usually work best, provided we measure html, javascript, hydration, and navigation together. check yourself why can fast server html still lead to a slow interactive experience?",
  },
  {
    number: 34,
    slug: "34-hydration",
    title: "Hydration",
    kind: "chapter",
    partNumber: 5,
    partName: "Modern React Architecture",
    markdown:
      "# Chapter 34 — Hydration\n\n## Quick refresher\n\nHydration attaches React behavior to server-rendered HTML by rendering the same initial tree on the client and connecting it to existing DOM.\n\n## Why this matters\n\nThe page may look ready before it can respond. Hydration mismatches can cause warnings, discarded work, incorrect UI, and layout shifts.\n\n## Core mental model\n\nThe server and first client render must agree. Avoid nondeterministic render output:\n\n```tsx\n// Risky during initial render\nreturn \u003cspan>{new Date().toLocaleTimeString()}\u003c/span>;\n```\n\nDifferences can come from time, random values, locale, browser-only APIs, invalid HTML, or data changing between server and client. Provide deterministic initial data, defer browser-only synchronization to an Effect, or isolate client-only UI deliberately.\n\nHydration also has a performance cost because React must load code and recreate the component tree. Suspense boundaries can allow selective hydration and prioritize interaction with ready regions.\n\n## Common traps\n\n- Using `typeof window` branches that change initial markup.\n- Suppressing mismatch warnings instead of fixing the cause.\n- Confusing visible HTML with usable UI.\n- Rendering the whole page as client-only to avoid one mismatch.\n\n## Interview answer\n\nHydration connects client React to server HTML, so the initial output must be deterministic and equivalent. I eliminate time, random, locale, and browser-only differences or isolate them behind intentional boundaries. I also measure hydration as JavaScript and main-thread work, not merely correctness.\n\n## Check yourself\n\nHow would you render a browser-local preference without producing a hydration mismatch?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Hydration attaches React behavior to server rendered HTML by rendering the same initial tree on the client and connecting it to existing DOM.",
    excerpt:
      "The page may look ready before it can respond. Hydration mismatches can cause warnings, discarded work, incorrect UI, and layout shifts.",
    readingMinutes: 2,
    searchText:
      "chapter 34 — hydration quick refresher hydration attaches react behavior to server rendered html by rendering the same initial tree on the client and connecting it to existing dom. why this matters the page may look ready before it can respond. hydration mismatches can cause warnings, discarded work, incorrect ui, and layout shifts. core mental model the server and first client render must agree. avoid nondeterministic render output: differences can come from time, random values, locale, browser only apis, invalid html, or data changing between server and client. provide deterministic initial data, defer browser only synchronization to an effect, or isolate client only ui deliberately. hydration also has a performance cost because react must load code and recreate the component tree. suspense boundaries can allow selective hydration and prioritize interaction with ready regions. common traps using typeof window branches that change initial markup. suppressing mismatch warnings instead of fixing the cause. confusing visible html with usable ui. rendering the whole page as client only to avoid one mismatch. interview answer hydration connects client react to server html, so the initial output must be deterministic and equivalent. i eliminate time, random, locale, and browser only differences or isolate them behind intentional boundaries. i also measure hydration as javascript and main thread work, not merely correctness. check yourself how would you render a browser local preference without producing a hydration mismatch?",
  },
  {
    number: 35,
    slug: "35-server-components",
    title: "Server Components",
    kind: "chapter",
    partNumber: 5,
    partName: "Modern React Architecture",
    markdown:
      "# Chapter 35 — Server Components\n\n## Quick refresher\n\nServer Components execute on the server and send a serialized UI representation rather than their component JavaScript to the browser. Client Components provide state, Effects, event handlers, and browser APIs.\n\n## Why this matters\n\nThe boundary changes bundle composition, data access, component APIs, and where interactivity belongs.\n\n## Core mental model\n\nServer Components can access server data directly and compose Client Components:\n\n```tsx\nasync function ProductPage({ id }: { id: string }) {\n  const product = await db.product.find(id);\n  return \u003cAddToCart productId={product.id} />;\n}\n```\n\nValues crossing into a Client Component must be serializable. A client boundary pulls its imported client-side dependency graph into the browser, so keep it as narrow as practical. Server Components are not the same as SSR: SSR produces HTML, while Server Components define a server/client module and data boundary; frameworks may use both.\n\nThey cannot use client Hooks or handle browser events. Mutations require an explicit server action or API mechanism with authorization and validation.\n\n## Common traps\n\n- Marking large trees as client components for one interactive leaf.\n- Passing functions or nonserializable objects across the boundary.\n- Treating Server Components as secure without authorization checks.\n- Equating Server Components with server-rendered HTML.\n\n## Interview answer\n\nServer Components keep data access and noninteractive rendering on the server while Client Components define interactive islands. I keep client boundaries narrow, pass serializable props, and compose server content through them where possible. They complement rather than replace SSR, caching, and secure mutation design.\n\n## Check yourself\n\nWhy can moving a client boundary upward increase the browser bundle?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Server Components execute on the server and send a serialized UI representation rather than their component JavaScript to the browser. Client Components provide state, Effects, event handlers, and browser APIs.",
    excerpt:
      "The boundary changes bundle composition, data access, component APIs, and where interactivity belongs.",
    readingMinutes: 2,
    searchText:
      "chapter 35 — server components quick refresher server components execute on the server and send a serialized ui representation rather than their component javascript to the browser. client components provide state, effects, event handlers, and browser apis. why this matters the boundary changes bundle composition, data access, component apis, and where interactivity belongs. core mental model server components can access server data directly and compose client components: values crossing into a client component must be serializable. a client boundary pulls its imported client side dependency graph into the browser, so keep it as narrow as practical. server components are not the same as ssr: ssr produces html, while server components define a server/client module and data boundary; frameworks may use both. they cannot use client hooks or handle browser events. mutations require an explicit server action or api mechanism with authorization and validation. common traps marking large trees as client components for one interactive leaf. passing functions or nonserializable objects across the boundary. treating server components as secure without authorization checks. equating server components with server rendered html. interview answer server components keep data access and noninteractive rendering on the server while client components define interactive islands. i keep client boundaries narrow, pass serializable props, and compose server content through them where possible. they complement rather than replace ssr, caching, and secure mutation design. check yourself why can moving a client boundary upward increase the browser bundle?",
  },
  {
    number: 36,
    slug: "36-suspense-and-streaming",
    title: "Suspense and Streaming",
    kind: "chapter",
    partNumber: 5,
    partName: "Modern React Architecture",
    markdown:
      "# Chapter 36 — Suspense and Streaming\n\n## Quick refresher\n\nSuspense coordinates a fallback while a supported child is not ready. Streaming lets the server send completed parts of a response progressively.\n\n## Why this matters\n\nBoundary placement controls reveal order, perceived loading, layout stability, and failure isolation.\n\n## Core mental model\n\n```tsx\n\u003cSuspense fallback={\u003cProfileSkeleton />}>\n  \u003cProfile />\n  \u003cSuspense fallback={\u003cFeedSkeleton />}>\n    \u003cFeed />\n  \u003c/Suspense>\n\u003c/Suspense>\n```\n\nPlace boundaries around regions that can load meaningfully and independently. Too few boundaries block large sections; too many create flicker and visual fragmentation. Skeletons should preserve approximate layout.\n\nSuspense does not make arbitrary fetching code Suspense-aware. Use a framework or data source designed to integrate with it. Pair boundaries with error boundaries because loading and failure are separate states.\n\nStreaming improves progressive delivery but does not remove network, database, JavaScript, or hydration costs.\n\n## Common traps\n\n- Wrapping every component in Suspense.\n- Assuming Suspense catches errors.\n- Causing sequential data waterfalls inside nested components.\n- Replacing already visible content with a disruptive fallback during an update.\n\n## Interview answer\n\nSuspense coordinates loading UI; streaming allows ready server output to arrive incrementally. I choose boundaries from UX reveal order and independent recovery regions, avoid data waterfalls, preserve layout, and pair loading boundaries with error handling. I use framework-supported data integration rather than ad hoc promises.\n\n## Check yourself\n\nWhen should two loading regions share a Suspense boundary rather than reveal independently?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Suspense coordinates a fallback while a supported child is not ready. Streaming lets the server send completed parts of a response progressively.",
    excerpt:
      "Boundary placement controls reveal order, perceived loading, layout stability, and failure isolation.",
    readingMinutes: 1,
    searchText:
      "chapter 36 — suspense and streaming quick refresher suspense coordinates a fallback while a supported child is not ready. streaming lets the server send completed parts of a response progressively. why this matters boundary placement controls reveal order, perceived loading, layout stability, and failure isolation. core mental model place boundaries around regions that can load meaningfully and independently. too few boundaries block large sections; too many create flicker and visual fragmentation. skeletons should preserve approximate layout. suspense does not make arbitrary fetching code suspense aware. use a framework or data source designed to integrate with it. pair boundaries with error boundaries because loading and failure are separate states. streaming improves progressive delivery but does not remove network, database, javascript, or hydration costs. common traps wrapping every component in suspense. assuming suspense catches errors. causing sequential data waterfalls inside nested components. replacing already visible content with a disruptive fallback during an update. interview answer suspense coordinates loading ui; streaming allows ready server output to arrive incrementally. i choose boundaries from ux reveal order and independent recovery regions, avoid data waterfalls, preserve layout, and pair loading boundaries with error handling. i use framework supported data integration rather than ad hoc promises. check yourself when should two loading regions share a suspense boundary rather than reveal independently?",
  },
  {
    number: 37,
    slug: "37-data-fetching-architecture",
    title: "Data-Fetching Architecture",
    kind: "chapter",
    partNumber: 5,
    partName: "Modern React Architecture",
    markdown:
      "# Chapter 37 — Data-Fetching Architecture\n\n## Quick refresher\n\nData architecture defines where fetching begins, who owns cache state, how duplicate requests are avoided, and how freshness and errors are represented.\n\n## Why this matters\n\nFetching in leaf Effects often creates waterfalls, duplicate requests, poor server rendering, and manual race-condition handling.\n\n## Core mental model\n\nSeparate three concerns:\n\n```text\nresource identity → cache/freshness policy → UI boundary\n```\n\nStart requests as high and early as the route permits, run independent requests in parallel, and use framework or server-state primitives for caching and deduplication. A cache key must include every input that changes the resource.\n\nDistinguish server state from client UI state. Server state has remote ownership, freshness, errors, and invalidation; copying it into local state usually creates synchronization problems.\n\nEffects remain appropriate for imperative external synchronization, but raw fetching Effects need cancellation and stale-response protection.\n\n## Common traps\n\n- Fetching sequentially through nested component Effects.\n- Using one global loading boolean for independent resources.\n- Treating cached data as permanently fresh.\n- Invalidating an entire cache after every mutation.\n\n## Interview answer\n\nI model resource identity and freshness explicitly, start independent work in parallel, and use the framework or a server-state cache for deduplication, retries, and SSR integration. Components consume resource states at meaningful loading and error boundaries. Local state remains for client-owned interaction state rather than duplicating server data.\n\n## Check yourself\n\nWhy does lifting a request to a route often remove a component data waterfall?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Data architecture defines where fetching begins, who owns cache state, how duplicate requests are avoided, and how freshness and errors are represented.",
    excerpt:
      "Fetching in leaf Effects often creates waterfalls, duplicate requests, poor server rendering, and manual race condition handling.",
    readingMinutes: 2,
    searchText:
      "chapter 37 — data fetching architecture quick refresher data architecture defines where fetching begins, who owns cache state, how duplicate requests are avoided, and how freshness and errors are represented. why this matters fetching in leaf effects often creates waterfalls, duplicate requests, poor server rendering, and manual race condition handling. core mental model separate three concerns: start requests as high and early as the route permits, run independent requests in parallel, and use framework or server state primitives for caching and deduplication. a cache key must include every input that changes the resource. distinguish server state from client ui state. server state has remote ownership, freshness, errors, and invalidation; copying it into local state usually creates synchronization problems. effects remain appropriate for imperative external synchronization, but raw fetching effects need cancellation and stale response protection. common traps fetching sequentially through nested component effects. using one global loading boolean for independent resources. treating cached data as permanently fresh. invalidating an entire cache after every mutation. interview answer i model resource identity and freshness explicitly, start independent work in parallel, and use the framework or a server state cache for deduplication, retries, and ssr integration. components consume resource states at meaningful loading and error boundaries. local state remains for client owned interaction state rather than duplicating server data. check yourself why does lifting a request to a route often remove a component data waterfall?",
  },
  {
    number: 38,
    slug: "38-mutations-and-optimistic-updates",
    title: "Mutations and Optimistic Updates",
    kind: "chapter",
    partNumber: 5,
    partName: "Modern React Architecture",
    markdown:
      "# Chapter 38 — Mutations and Optimistic Updates\n\n## Quick refresher\n\nA mutation changes server-owned data. An optimistic update shows the expected result before confirmation, then reconciles success or rolls back failure.\n\n## Why this matters\n\nOptimism improves perceived responsiveness but introduces concurrency, rollback, identity, and error-handling problems.\n\n## Core mental model\n\n```text\ncapture previous state → apply optimistic state → send mutation\n                                      ↙ success     ↘ failure\n                               reconcile result    rollback\n```\n\nGive optimistic records temporary IDs and track pending state. Prefer operation-specific rollback over replacing an entire cache snapshot, which could erase newer successful changes. Server responses remain authoritative.\n\nOptimism is best for likely-successful, understandable, reversible actions. Destructive, financial, permission-sensitive, or conflict-heavy actions may require confirmation or pessimistic UI.\n\nPrevent duplicate submissions, make server operations idempotent when possible, and announce pending or failed state accessibly.\n\n## Common traps\n\n- Assuming requests finish in submission order.\n- Losing later edits during rollback.\n- Leaving temporary and server IDs duplicated.\n- Showing success for an operation that cannot safely be reversed.\n\n## Interview answer\n\nFor an optimistic mutation I record enough information to reconcile or roll back that operation, update the cache immediately, prevent accidental duplicates, and replace temporary data with the authoritative response. I handle concurrent mutations explicitly and reserve optimism for actions with high success probability and understandable recovery.\n\n## Check yourself\n\nWhy is restoring one old cache snapshot unsafe when multiple optimistic mutations overlap?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "A mutation changes server owned data. An optimistic update shows the expected result before confirmation, then reconciles success or rolls back failure.",
    excerpt:
      "Optimism improves perceived responsiveness but introduces concurrency, rollback, identity, and error handling problems.",
    readingMinutes: 1,
    searchText:
      "chapter 38 — mutations and optimistic updates quick refresher a mutation changes server owned data. an optimistic update shows the expected result before confirmation, then reconciles success or rolls back failure. why this matters optimism improves perceived responsiveness but introduces concurrency, rollback, identity, and error handling problems. core mental model give optimistic records temporary ids and track pending state. prefer operation specific rollback over replacing an entire cache snapshot, which could erase newer successful changes. server responses remain authoritative. optimism is best for likely successful, understandable, reversible actions. destructive, financial, permission sensitive, or conflict heavy actions may require confirmation or pessimistic ui. prevent duplicate submissions, make server operations idempotent when possible, and announce pending or failed state accessibly. common traps assuming requests finish in submission order. losing later edits during rollback. leaving temporary and server ids duplicated. showing success for an operation that cannot safely be reversed. interview answer for an optimistic mutation i record enough information to reconcile or roll back that operation, update the cache immediately, prevent accidental duplicates, and replace temporary data with the authoritative response. i handle concurrent mutations explicitly and reserve optimism for actions with high success probability and understandable recovery. check yourself why is restoring one old cache snapshot unsafe when multiple optimistic mutations overlap?",
  },
  {
    number: 39,
    slug: "39-forms-and-actions",
    title: "Forms and Actions",
    kind: "chapter",
    partNumber: 5,
    partName: "Modern React Architecture",
    markdown:
      '# Chapter 39 — Forms and Actions\n\n## Quick refresher\n\nForms combine field ownership, validation, submission, pending state, errors, and accessibility. Native form semantics provide submission, keyboard behavior, and progressive enhancement.\n\n## Why this matters\n\nForm architecture is a frequent interview topic because naive controlled state can create complexity without improving the user experience.\n\n## Core mental model\n\nUse the platform first:\n\n```tsx\n\u003cform action={saveProfile}>\n  \u003clabel htmlFor="name">Name\u003c/label>\n  \u003cinput id="name" name="name" required />\n  \u003cbutton type="submit">Save\u003c/button>\n\u003c/form>\n```\n\nUncontrolled native fields are often sufficient when values are needed at submission. Use controlled fields when rendering depends on each change or values must coordinate live. Validate at appropriate layers: native constraints for immediate basics, client logic for UX, and server validation as the authority.\n\nActions can coordinate submission and pending state with framework support, but authorization, validation, and idempotency remain server responsibilities. Preserve user input on failure and connect errors to fields through accessible descriptions.\n\n## Common traps\n\n- Controlling every field by default.\n- Validating only in the browser.\n- Disabling submission without explaining pending state.\n- Clearing input after a failed request.\n\n## Interview answer\n\nI start from semantic HTML forms, choose controlled state only when live coordination requires it, and keep the server authoritative for validation and permissions. The UI represents idle, pending, success, and error states, prevents duplicate submissions, preserves input on failure, and associates messages with their fields.\n\n## Check yourself\n\nWhen is an uncontrolled input simpler and more robust than controlled state?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Forms combine field ownership, validation, submission, pending state, errors, and accessibility. Native form semantics provide submission, keyboard behavior, and progressive enhancement.",
    excerpt:
      "Form architecture is a frequent interview topic because naive controlled state can create complexity without improving the user experience.",
    readingMinutes: 1,
    searchText:
      "chapter 39 — forms and actions quick refresher forms combine field ownership, validation, submission, pending state, errors, and accessibility. native form semantics provide submission, keyboard behavior, and progressive enhancement. why this matters form architecture is a frequent interview topic because naive controlled state can create complexity without improving the user experience. core mental model use the platform first: uncontrolled native fields are often sufficient when values are needed at submission. use controlled fields when rendering depends on each change or values must coordinate live. validate at appropriate layers: native constraints for immediate basics, client logic for ux, and server validation as the authority. actions can coordinate submission and pending state with framework support, but authorization, validation, and idempotency remain server responsibilities. preserve user input on failure and connect errors to fields through accessible descriptions. common traps controlling every field by default. validating only in the browser. disabling submission without explaining pending state. clearing input after a failed request. interview answer i start from semantic html forms, choose controlled state only when live coordination requires it, and keep the server authoritative for validation and permissions. the ui represents idle, pending, success, and error states, prevents duplicate submissions, preserves input on failure, and associates messages with their fields. check yourself when is an uncontrolled input simpler and more robust than controlled state?",
  },
  {
    number: 40,
    slug: "40-choosing-state-management-tools",
    title: "Choosing State-Management Tools",
    kind: "chapter",
    partNumber: 5,
    partName: "Modern React Architecture",
    markdown:
      "# Chapter 40 — Choosing State-Management Tools\n\n## Quick refresher\n\nChoose a state tool from ownership, lifetime, sharing, update frequency, persistence, and synchronization requirements—not popularity.\n\n## Why this matters\n\nApplications contain different state categories that should not be forced into one global store.\n\n## Core mental model\n\n```text\nlocal UI state      → useState / useReducer\nsubtree dependency  → props / context\nshareable location  → URL\nremote server state → framework cache / query library\nexternal live state → external store subscription\n```\n\nKeep transient input, disclosure, and selection near their owner. Put filters and navigation state in the URL when they should survive refresh or be shareable. Use a server-state cache for remote freshness and invalidation. Use an external client store when many distant consumers need selective high-frequency updates or state must outlive component trees.\n\nEvaluate debugging, SSR, persistence, selectors, bundle cost, team familiarity, and migration—not only API convenience.\n\n## Common traps\n\n- Putting server responses into a generic global store.\n- Using context for high-frequency selective subscriptions.\n- Keeping shareable filters only in memory.\n- Adopting a library before defining state ownership.\n\n## Interview answer\n\nI classify state first. Local interaction state stays local, subtree dependencies use props or context, shareable navigation state belongs in the URL, and remote data uses a server-state cache. I introduce an external store when lifetime or subscription granularity requires it, then compare SSR, debugging, performance, and team costs.\n\n## Check yourself\n\nWhere should product-search filters live if users must share and restore the result page?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Choose a state tool from ownership, lifetime, sharing, update frequency, persistence, and synchronization requirements—not popularity.",
    excerpt:
      "Applications contain different state categories that should not be forced into one global store.",
    readingMinutes: 1,
    searchText:
      "chapter 40 — choosing state management tools quick refresher choose a state tool from ownership, lifetime, sharing, update frequency, persistence, and synchronization requirements—not popularity. why this matters applications contain different state categories that should not be forced into one global store. core mental model keep transient input, disclosure, and selection near their owner. put filters and navigation state in the url when they should survive refresh or be shareable. use a server state cache for remote freshness and invalidation. use an external client store when many distant consumers need selective high frequency updates or state must outlive component trees. evaluate debugging, ssr, persistence, selectors, bundle cost, team familiarity, and migration—not only api convenience. common traps putting server responses into a generic global store. using context for high frequency selective subscriptions. keeping shareable filters only in memory. adopting a library before defining state ownership. interview answer i classify state first. local interaction state stays local, subtree dependencies use props or context, shareable navigation state belongs in the url, and remote data uses a server state cache. i introduce an external store when lifetime or subscription granularity requires it, then compare ssr, debugging, performance, and team costs. check yourself where should product search filters live if users must share and restore the result page?",
  },
  {
    number: 41,
    slug: "41-testing-user-observable-behavior",
    title: "Testing User-Observable Behavior",
    kind: "chapter",
    partNumber: 6,
    partName: "Testing and Production Behavior",
    markdown:
      '# Chapter 41 — Testing User-Observable Behavior\n\n## Quick refresher\n\nTest what users can perceive and do: rendered content, accessible roles and names, interactions, navigation, and outcomes.\n\n## Why this matters\n\nTests coupled to component internals resist refactoring while missing real accessibility and integration problems.\n\n## Core mental model\n\n```tsx\nrender(\u003cLogin />);\nawait user.type(screen.getByLabelText(/email/i), "a@example.com");\nawait user.click(screen.getByRole("button", { name: /sign in/i }));\nexpect(await screen.findByText(/welcome/i)).toBeVisible();\n```\n\nPrefer role, label, and visible-text queries because they resemble user access and expose missing semantics. Mock at system boundaries such as network requests, not internal Hooks. Assert outcomes rather than state variables or function call sequences.\n\nImplementation tests remain appropriate for pure algorithms or stable library contracts, but UI confidence comes primarily from behavior.\n\n## Common traps\n\n- Selecting elements by CSS class or test ID first.\n- Calling event handlers directly.\n- Asserting internal component state.\n- Mocking so much that no real integration remains.\n\n## Interview answer\n\nI test UI through accessible queries and realistic interactions, then assert user-visible outcomes. I mock external boundaries rather than internal implementation and keep pure logic tests separate. This produces refactor-resistant tests and makes accessibility failures more visible.\n\n## Check yourself\n\nWhy is `getByRole("button", { name: "Save" })` usually better than a test ID?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Test what users can perceive and do: rendered content, accessible roles and names, interactions, navigation, and outcomes.",
    excerpt:
      "Tests coupled to component internals resist refactoring while missing real accessibility and integration problems.",
    readingMinutes: 1,
    searchText:
      'chapter 41 — testing user observable behavior quick refresher test what users can perceive and do: rendered content, accessible roles and names, interactions, navigation, and outcomes. why this matters tests coupled to component internals resist refactoring while missing real accessibility and integration problems. core mental model prefer role, label, and visible text queries because they resemble user access and expose missing semantics. mock at system boundaries such as network requests, not internal hooks. assert outcomes rather than state variables or function call sequences. implementation tests remain appropriate for pure algorithms or stable library contracts, but ui confidence comes primarily from behavior. common traps selecting elements by css class or test id first. calling event handlers directly. asserting internal component state. mocking so much that no real integration remains. interview answer i test ui through accessible queries and realistic interactions, then assert user visible outcomes. i mock external boundaries rather than internal implementation and keep pure logic tests separate. this produces refactor resistant tests and makes accessibility failures more visible. check yourself why is getbyrole("button", { name: "save" }) usually better than a test id?',
  },
  {
    number: 42,
    slug: "42-test-boundaries",
    title: "Unit, Integration, and E2E Boundaries",
    kind: "chapter",
    partNumber: 6,
    partName: "Testing and Production Behavior",
    markdown:
      "# Chapter 42 — Unit, Integration, and E2E Boundaries\n\n## Quick refresher\n\nUnit tests isolate small logic, integration tests combine cooperating UI and services, and end-to-end tests exercise critical journeys in a real browser and deployed-like system.\n\n## Why this matters\n\nConfidence comes from choosing the cheapest boundary that can reveal the relevant failure.\n\n## Core mental model\n\nUse many fast tests for pure complex logic, integration tests for component behavior and data flow, and a smaller E2E suite for critical cross-system journeys. Avoid rigid ratios; risk determines coverage.\n\n```text\nformatting rule → unit\nautocomplete request and keyboard behavior → integration\nsign-in through redirected dashboard → E2E\n```\n\nContract tests can verify boundaries between frontend and APIs. Visual regression tests help with appearance but do not replace behavioral assertions.\n\n## Common traps\n\n- Testing every component in isolation.\n- Recreating browser behavior in unit mocks.\n- Covering all edge cases only through slow E2E tests.\n- Treating line coverage as confidence.\n\n## Interview answer\n\nI choose boundaries by failure risk. Pure decision logic gets unit tests, most React behavior gets integration tests through public UI, and a focused E2E suite protects critical journeys and infrastructure integration. I avoid duplicating identical assertions at every layer.\n\n## Check yourself\n\nWhich test boundary best verifies that a form displays a server validation error beside the correct field?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Unit tests isolate small logic, integration tests combine cooperating UI and services, and end to end tests exercise critical journeys in a real browser and deployed like system.",
    excerpt:
      "Confidence comes from choosing the cheapest boundary that can reveal the relevant failure.",
    readingMinutes: 1,
    searchText:
      "chapter 42 — unit, integration, and e2e boundaries quick refresher unit tests isolate small logic, integration tests combine cooperating ui and services, and end to end tests exercise critical journeys in a real browser and deployed like system. why this matters confidence comes from choosing the cheapest boundary that can reveal the relevant failure. core mental model use many fast tests for pure complex logic, integration tests for component behavior and data flow, and a smaller e2e suite for critical cross system journeys. avoid rigid ratios; risk determines coverage. contract tests can verify boundaries between frontend and apis. visual regression tests help with appearance but do not replace behavioral assertions. common traps testing every component in isolation. recreating browser behavior in unit mocks. covering all edge cases only through slow e2e tests. treating line coverage as confidence. interview answer i choose boundaries by failure risk. pure decision logic gets unit tests, most react behavior gets integration tests through public ui, and a focused e2e suite protects critical journeys and infrastructure integration. i avoid duplicating identical assertions at every layer. check yourself which test boundary best verifies that a form displays a server validation error beside the correct field?",
  },
  {
    number: 43,
    slug: "43-testing-asynchronous-ui",
    title: "Testing Asynchronous UI",
    kind: "chapter",
    partNumber: 6,
    partName: "Testing and Production Behavior",
    markdown:
      '# Chapter 43 — Testing Asynchronous UI\n\n## Quick refresher\n\nAsync UI tests trigger behavior, wait for an observable outcome, and control external nondeterminism without depending on arbitrary time delays.\n\n## Why this matters\n\nIncorrect waiting produces flaky tests, missed race conditions, and false positives wrapped in React update warnings.\n\n## Core mental model\n\n```tsx\nawait user.click(screen.getByRole("button", { name: /load/i }));\nexpect(screen.getByRole("status")).toHaveTextContent(/loading/i);\nexpect(await screen.findByText("Ada Lovelace")).toBeVisible();\n```\n\nUse `findBy` for elements that will appear and `waitFor` for retrying an assertion. Mock network behavior at the request boundary and test success, empty, error, cancellation, and out-of-order responses where relevant.\n\nUse fake timers only for timer-owned behavior such as debounce, and advance them intentionally. Do not combine timer control and unresolved promises without understanding both queues.\n\n## Common traps\n\n- Adding sleeps to make a test pass.\n- Wrapping everything in `waitFor`.\n- Forgetting to await user interactions.\n- Testing only the successful response.\n\n## Interview answer\n\nI trigger async UI through user interactions, assert immediate pending feedback, and wait for a specific observable outcome. I control network and timer boundaries deterministically, test failure and stale-response cases, and avoid arbitrary sleeps or implementation-specific state assertions.\n\n## Check yourself\n\nHow would you test that an older autocomplete response cannot replace a newer query?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Async UI tests trigger behavior, wait for an observable outcome, and control external nondeterminism without depending on arbitrary time delays.",
    excerpt:
      "Incorrect waiting produces flaky tests, missed race conditions, and false positives wrapped in React update warnings.",
    readingMinutes: 1,
    searchText:
      "chapter 43 — testing asynchronous ui quick refresher async ui tests trigger behavior, wait for an observable outcome, and control external nondeterminism without depending on arbitrary time delays. why this matters incorrect waiting produces flaky tests, missed race conditions, and false positives wrapped in react update warnings. core mental model use findby for elements that will appear and waitfor for retrying an assertion. mock network behavior at the request boundary and test success, empty, error, cancellation, and out of order responses where relevant. use fake timers only for timer owned behavior such as debounce, and advance them intentionally. do not combine timer control and unresolved promises without understanding both queues. common traps adding sleeps to make a test pass. wrapping everything in waitfor . forgetting to await user interactions. testing only the successful response. interview answer i trigger async ui through user interactions, assert immediate pending feedback, and wait for a specific observable outcome. i control network and timer boundaries deterministically, test failure and stale response cases, and avoid arbitrary sleeps or implementation specific state assertions. check yourself how would you test that an older autocomplete response cannot replace a newer query?",
  },
  {
    number: 44,
    slug: "44-strict-mode-behavior",
    title: "Strict Mode Behavior",
    kind: "chapter",
    partNumber: 6,
    partName: "Testing and Production Behavior",
    markdown:
      "# Chapter 44 — Strict Mode Behavior\n\n## Quick refresher\n\nStrict Mode enables development-only checks that may repeat rendering and Effect setup-cleanup cycles to expose impure rendering and missing cleanup.\n\n## Why this matters\n\nCode that relies on “this runs once” can duplicate subscriptions, requests, or mutations and fail under remounting or concurrent behavior.\n\n## Core mental model\n\n```tsx\nuseEffect(() => {\n  const subscription = source.subscribe(handleValue);\n  return () => subscription.unsubscribe();\n}, [source]);\n```\n\nCorrect synchronization survives setup, cleanup, and setup again. Rendering must also be pure: mutating props or module state during render becomes visible when React calls the component again.\n\nThe checks do not mean production always performs the same sequence. They reveal code that is unsafe under legitimate lifecycle changes. Fix the underlying symmetry rather than disabling Strict Mode or using a ref to hide duplicate setup.\n\n## Common traps\n\n- Treating repeated development logs as a React bug.\n- Guarding an Effect with `hasRun.current` while leaking its resource.\n- Performing irreversible work during render.\n- Assuming an empty dependency array guarantees one lifetime execution.\n\n## Interview answer\n\nStrict Mode intentionally stresses render purity and Effect cleanup in development. I make rendering idempotent and every Effect a symmetric setup-cleanup process. I do not suppress repeated setup with a ref because that hides the lifecycle bug rather than making synchronization safe.\n\n## Check yourself\n\nWhy is a `hasRun` ref usually the wrong fix for an Effect that sends duplicate development requests?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Strict Mode enables development only checks that may repeat rendering and Effect setup cleanup cycles to expose impure rendering and missing cleanup.",
    excerpt:
      "Code that relies on “this runs once” can duplicate subscriptions, requests, or mutations and fail under remounting or concurrent behavior.",
    readingMinutes: 1,
    searchText:
      "chapter 44 — strict mode behavior quick refresher strict mode enables development only checks that may repeat rendering and effect setup cleanup cycles to expose impure rendering and missing cleanup. why this matters code that relies on “this runs once” can duplicate subscriptions, requests, or mutations and fail under remounting or concurrent behavior. core mental model correct synchronization survives setup, cleanup, and setup again. rendering must also be pure: mutating props or module state during render becomes visible when react calls the component again. the checks do not mean production always performs the same sequence. they reveal code that is unsafe under legitimate lifecycle changes. fix the underlying symmetry rather than disabling strict mode or using a ref to hide duplicate setup. common traps treating repeated development logs as a react bug. guarding an effect with hasrun.current while leaking its resource. performing irreversible work during render. assuming an empty dependency array guarantees one lifetime execution. interview answer strict mode intentionally stresses render purity and effect cleanup in development. i make rendering idempotent and every effect a symmetric setup cleanup process. i do not suppress repeated setup with a ref because that hides the lifecycle bug rather than making synchronization safe. check yourself why is a hasrun ref usually the wrong fix for an effect that sends duplicate development requests?",
  },
  {
    number: 45,
    slug: "45-race-conditions-and-cancellation",
    title: "Race Conditions and Cancellation",
    kind: "chapter",
    partNumber: 6,
    partName: "Testing and Production Behavior",
    markdown:
      "# Chapter 45 — Race Conditions and Cancellation\n\n## Quick refresher\n\nAsync operations can finish out of order. Cleanup should cancel obsolete work when possible and prevent obsolete results from updating state.\n\n## Why this matters\n\nWithout ownership rules, an older request can replace newer UI, or an unmounted feature can continue consuming resources.\n\n## Core mental model\n\n```tsx\nuseEffect(() => {\n  const controller = new AbortController();\n  let ignore = false;\n\n  search(query, controller.signal).then(result => {\n    if (!ignore) setResult(result);\n  });\n\n  return () => {\n    ignore = true;\n    controller.abort();\n  };\n}, [query]);\n```\n\nCancellation stops unnecessary supported work. The ignore guard ensures that this Effect execution cannot commit after losing ownership, even if the abstraction does not honor abort fully.\n\nServer-state libraries often implement cancellation, deduplication, and request identity. Mutations additionally require idempotency and conflict strategy; blindly cancelling a client request does not guarantee the server did not apply it.\n\n## Common traps\n\n- Assuming request completion order matches start order.\n- Treating abort as proof that no server mutation occurred.\n- Reporting cancellation as an error.\n- Updating shared state without checking which request owns it.\n\n## Interview answer\n\nI associate async work with the render or operation that started it. Cleanup aborts supported work and invalidates that execution so stale results cannot commit. For mutations I also design idempotency and reconciliation because cancelling transport does not necessarily cancel server-side effects.\n\n## Check yourself\n\nWhy might an ignore guard remain useful when using `AbortController`?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Async operations can finish out of order. Cleanup should cancel obsolete work when possible and prevent obsolete results from updating state.",
    excerpt:
      "Without ownership rules, an older request can replace newer UI, or an unmounted feature can continue consuming resources.",
    readingMinutes: 1,
    searchText:
      "chapter 45 — race conditions and cancellation quick refresher async operations can finish out of order. cleanup should cancel obsolete work when possible and prevent obsolete results from updating state. why this matters without ownership rules, an older request can replace newer ui, or an unmounted feature can continue consuming resources. core mental model cancellation stops unnecessary supported work. the ignore guard ensures that this effect execution cannot commit after losing ownership, even if the abstraction does not honor abort fully. server state libraries often implement cancellation, deduplication, and request identity. mutations additionally require idempotency and conflict strategy; blindly cancelling a client request does not guarantee the server did not apply it. common traps assuming request completion order matches start order. treating abort as proof that no server mutation occurred. reporting cancellation as an error. updating shared state without checking which request owns it. interview answer i associate async work with the render or operation that started it. cleanup aborts supported work and invalidates that execution so stale results cannot commit. for mutations i also design idempotency and reconciliation because cancelling transport does not necessarily cancel server side effects. check yourself why might an ignore guard remain useful when using abortcontroller ?",
  },
  {
    number: 46,
    slug: "46-loading-empty-and-error-states",
    title: "Loading, Empty, and Error States",
    kind: "chapter",
    partNumber: 6,
    partName: "Testing and Production Behavior",
    markdown:
      '# Chapter 46 — Loading, Empty, and Error States\n\n## Quick refresher\n\nAsync UI should represent distinct idle, pending, success, empty, and error states. Refreshing existing data differs from loading it for the first time.\n\n## Why this matters\n\nBoolean combinations create impossible states and poor transitions. Explicit states make rendering, testing, and accessibility clearer.\n\n## Core mental model\n\n```tsx\ntype Resource\u003cT> =\n  | { status: "idle" }\n  | { status: "loading" }\n  | { status: "success"; data: T }\n  | { status: "error"; message: string };\n```\n\nKeep useful previous data visible during background refresh when appropriate, with a subtle pending indicator. Empty state is a successful result with no items, not an error. Errors should explain impact and offer a relevant retry or alternative.\n\nUse `aria-live` or status semantics for important asynchronous changes without repeatedly announcing noisy updates. Skeletons should preserve layout; spinners should not erase useful context.\n\n## Common traps\n\n- Modeling loading and error as contradictory booleans.\n- Showing the same empty UI before a request and after an empty success.\n- Replacing existing content with a spinner during every refresh.\n- Displaying an error without recovery.\n\n## Interview answer\n\nI model async state explicitly and distinguish initial loading, background refresh, empty success, and failure. I preserve useful content when possible, provide relevant recovery, and announce important transitions accessibly. This avoids impossible boolean combinations and makes tests match actual user states.\n\n## Check yourself\n\nWhy should an empty search result not use the same UI as an unsubmitted search?',
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Async UI should represent distinct idle, pending, success, empty, and error states. Refreshing existing data differs from loading it for the first time.",
    excerpt:
      "Boolean combinations create impossible states and poor transitions. Explicit states make rendering, testing, and accessibility clearer.",
    readingMinutes: 1,
    searchText:
      "chapter 46 — loading, empty, and error states quick refresher async ui should represent distinct idle, pending, success, empty, and error states. refreshing existing data differs from loading it for the first time. why this matters boolean combinations create impossible states and poor transitions. explicit states make rendering, testing, and accessibility clearer. core mental model keep useful previous data visible during background refresh when appropriate, with a subtle pending indicator. empty state is a successful result with no items, not an error. errors should explain impact and offer a relevant retry or alternative. use aria live or status semantics for important asynchronous changes without repeatedly announcing noisy updates. skeletons should preserve layout; spinners should not erase useful context. common traps modeling loading and error as contradictory booleans. showing the same empty ui before a request and after an empty success. replacing existing content with a spinner during every refresh. displaying an error without recovery. interview answer i model async state explicitly and distinguish initial loading, background refresh, empty success, and failure. i preserve useful content when possible, provide relevant recovery, and announce important transitions accessibly. this avoids impossible boolean combinations and makes tests match actual user states. check yourself why should an empty search result not use the same ui as an unsubmitted search?",
  },
  {
    number: 47,
    slug: "47-debugging-react-applications",
    title: "Debugging React Applications",
    kind: "chapter",
    partNumber: 6,
    partName: "Testing and Production Behavior",
    markdown:
      "# Chapter 47 — Debugging React Applications\n\n## Quick refresher\n\nDebugging starts with reproduction, scope reduction, evidence collection, and a falsifiable hypothesis—not immediate code changes.\n\n## Why this matters\n\nReact symptoms often originate from identity, ownership, stale closures, mutation, browser behavior, or external data rather than React itself.\n\n## Core mental model\n\n```text\nreproduce → minimize → observe → hypothesize → test one change → verify\n```\n\nRead the first meaningful error and component stack. Use React DevTools to inspect props, state, context, owners, and render activity. Use the Profiler for timing and browser DevTools for network, console, DOM, accessibility, and main-thread evidence.\n\nFor unexpected state, trace who owns it and which event or Effect updates it. For unexpected remounts, inspect type, position, and keys. For loops, find an Effect that updates a dependency or an unstable dependency recreated each render. Build a minimal reproduction when framework or library interaction is unclear.\n\n## Common traps\n\n- Changing multiple things before testing the hypothesis.\n- Debugging minified symptoms without source maps or context.\n- Treating logs during render as committed UI.\n- Silencing warnings instead of finding the violated invariant.\n\n## Interview answer\n\nI make the failure reproducible, reduce it to the smallest responsible boundary, and collect evidence with React and browser tools. I trace ownership, identity, update sources, and Effect dependencies, then test one falsifiable hypothesis. Finally I verify the original user scenario and add a regression test at the appropriate boundary.\n\n## Check yourself\n\nWhich identity questions would you ask when a child’s state resets unexpectedly?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Debugging starts with reproduction, scope reduction, evidence collection, and a falsifiable hypothesis—not immediate code changes.",
    excerpt:
      "React symptoms often originate from identity, ownership, stale closures, mutation, browser behavior, or external data rather than React itself.",
    readingMinutes: 2,
    searchText:
      "chapter 47 — debugging react applications quick refresher debugging starts with reproduction, scope reduction, evidence collection, and a falsifiable hypothesis—not immediate code changes. why this matters react symptoms often originate from identity, ownership, stale closures, mutation, browser behavior, or external data rather than react itself. core mental model read the first meaningful error and component stack. use react devtools to inspect props, state, context, owners, and render activity. use the profiler for timing and browser devtools for network, console, dom, accessibility, and main thread evidence. for unexpected state, trace who owns it and which event or effect updates it. for unexpected remounts, inspect type, position, and keys. for loops, find an effect that updates a dependency or an unstable dependency recreated each render. build a minimal reproduction when framework or library interaction is unclear. common traps changing multiple things before testing the hypothesis. debugging minified symptoms without source maps or context. treating logs during render as committed ui. silencing warnings instead of finding the violated invariant. interview answer i make the failure reproducible, reduce it to the smallest responsible boundary, and collect evidence with react and browser tools. i trace ownership, identity, update sources, and effect dependencies, then test one falsifiable hypothesis. finally i verify the original user scenario and add a regression test at the appropriate boundary. check yourself which identity questions would you ask when a child’s state resets unexpectedly?",
  },
  {
    number: 48,
    slug: "48-production-resilience",
    title: "Production Resilience",
    kind: "chapter",
    partNumber: 6,
    partName: "Testing and Production Behavior",
    markdown:
      "# Chapter 48 — Production Resilience\n\n## Quick refresher\n\nResilient React applications isolate failures, expose useful diagnostics, tolerate partial outages, and provide safe recovery without losing user work.\n\n## Why this matters\n\nProduction behavior includes slow networks, stale deployments, third-party failures, malformed data, unsupported browsers, and long-running sessions—not only successful local rendering.\n\n## Core mental model\n\nDesign resilience in layers:\n\n```text\nvalidation → explicit resource states → timeout/retry policy\n→ error boundaries → observability → recovery or degradation\n```\n\nPlace error and Suspense boundaries around independently recoverable regions. Preserve form drafts and useful cached data during transient failure. Retry only safe operations with bounded backoff and jitter; avoid retry storms and non-idempotent mutations.\n\nCollect actionable client errors, component context, release version, performance signals, and user journey information while respecting privacy. Source maps and release correlation make reports diagnosable. Provide recovery for stale chunk failures after deployments and degrade optional third-party features rather than blocking core journeys.\n\n## Common traps\n\n- Retrying every failure indefinitely.\n- Sending sensitive state in error reports.\n- Using one application-wide fallback for local failures.\n- Losing unsaved user input during recovery.\n- Monitoring errors without ownership or alert thresholds.\n\n## Interview answer\n\nI design failure isolation with explicit async states and scoped boundaries, preserve valuable user state, and define safe timeout and retry policies. Observability includes release and journey context without sensitive data. Recovery may retry, refresh stale code, use cached data, or degrade optional functionality, and every alert has clear ownership.\n\n## Check yourself\n\nHow should an application recover when a deployed lazy chunk no longer exists on the server?",
    headings: [
      "Quick refresher",
      "Why this matters",
      "Core mental model",
      "Common traps",
      "Interview answer",
      "Check yourself",
    ],
    quickRefresher:
      "Resilient React applications isolate failures, expose useful diagnostics, tolerate partial outages, and provide safe recovery without losing user work.",
    excerpt:
      "Production behavior includes slow networks, stale deployments, third party failures, malformed data, unsupported browsers, and long running sessions—not only successful local rendering.",
    readingMinutes: 2,
    searchText:
      "chapter 48 — production resilience quick refresher resilient react applications isolate failures, expose useful diagnostics, tolerate partial outages, and provide safe recovery without losing user work. why this matters production behavior includes slow networks, stale deployments, third party failures, malformed data, unsupported browsers, and long running sessions—not only successful local rendering. core mental model design resilience in layers: place error and suspense boundaries around independently recoverable regions. preserve form drafts and useful cached data during transient failure. retry only safe operations with bounded backoff and jitter; avoid retry storms and non idempotent mutations. collect actionable client errors, component context, release version, performance signals, and user journey information while respecting privacy. source maps and release correlation make reports diagnosable. provide recovery for stale chunk failures after deployments and degrade optional third party features rather than blocking core journeys. common traps retrying every failure indefinitely. sending sensitive state in error reports. using one application wide fallback for local failures. losing unsaved user input during recovery. monitoring errors without ownership or alert thresholds. interview answer i design failure isolation with explicit async states and scoped boundaries, preserve valuable user state, and define safe timeout and retry policies. observability includes release and journey context without sensitive data. recovery may retry, refresh stale code, use cached data, or degrade optional functionality, and every alert has clear ownership. check yourself how should an application recover when a deployed lazy chunk no longer exists on the server?",
  },
];

export const reactPracticeArticles: Chapter[] = [
  {
    number: 1,
    slug: "01-accessible-autocomplete",
    title: "Accessible Autocomplete",
    kind: "chapter",
    partNumber: 1,
    partName: "React Live Coding",
    markdown:
      '# Accessible Autocomplete\n\n## The interview prompt\n\nBuild a reusable autocomplete component that lets a user search for and select one suggested record. The caller supplies the record type, asynchronous search function, stable identity, label, and optional rendering.\n\nThe component should:\n\n- update suggestions as the user types;\n- fetch results from an asynchronous data source;\n- display loading, empty, and error states;\n- support pointer and keyboard selection;\n- close when a result is selected or the user presses Escape;\n- expose the selected option to its parent.\n\nAssume the interview lasts 45–60 minutes. Styling is secondary to behavior, state design, and clear reasoning.\n\n## What the interviewer is evaluating\n\nAutocomplete looks like a text input and a list, but it exposes several important frontend skills at once:\n\n- translating an ambiguous product prompt into explicit behavior;\n- separating input state, selected value, and highlighted option;\n- synchronizing React with an asynchronous external system;\n- preventing stale responses from replacing current results;\n- preserving immediate typing while requests are delayed;\n- managing composite-widget keyboard behavior;\n- using stable identities and semantic relationships;\n- deciding what belongs in the interview solution and what belongs in production hardening.\n\nA senior candidate is not expected to build a production design-system combobox from memory. They are expected to identify the difficult parts, establish a coherent model, build the critical behavior, and explain the remaining risks.\n\n## Clarifying questions to ask\n\nBefore writing JSX, clarify the contract.\n\n1. Are suggestions local or fetched from a server?\n2. How many characters should trigger a search?\n3. Is arbitrary text allowed, or must the user select a result?\n4. Should the search be debounced?\n5. What information does each suggestion display?\n6. What happens when no result matches?\n7. Which keyboard interactions are required?\n8. Should the component be controlled by its parent?\n9. Does selecting an item submit, navigate, or only update state?\n10. Are external component libraries permitted?\n\nFor this article, use the following decisions:\n\n- results come from an asynchronous caller-provided search function;\n- two characters trigger a request;\n- arbitrary text may remain in the input;\n- requests are debounced by 250 milliseconds;\n- selecting a result calls `onSelect(option)`;\n- DOM focus remains in the input while arrow keys change the active suggestion;\n- the first version uses no external component library.\n\n## Requirements and deliberate non-requirements\n\n### Required in the interview version\n\n- Controlled text input\n- Debounced query\n- Loading, error, empty, and success states\n- Stale-request protection\n- Arrow Up and Arrow Down navigation\n- Enter to select\n- Escape to close\n- Pointer selection\n- Correct combobox, listbox, and option relationships\n- Stable option IDs\n\n### Explain, but do not necessarily finish\n\n- Result caching\n- Request deduplication across component instances\n- Windowing thousands of results\n- Mobile screen-reader testing\n- Internationalized matching\n- Analytics\n- Server rendering\n- Full browser and assistive-technology compatibility testing\n\nThis boundary is important. A live-coding solution should be small enough to complete and rich enough to demonstrate judgment.\n\n## Type contract\n\nDefine the reusable boundary before state or JSX. Every progressive checkpoint and the assembled implementation use the same generic contract:\n\n```tsx\nimport type { ReactNode } from "react";\n\ntype OptionId = string | number;\n\ntype SearchState\u003cT> =\n  | { status: "idle"; results: T[] }\n  | { status: "loading"; results: T[] }\n  | { status: "success"; results: T[] }\n  | { status: "error"; results: T[]; message: string };\n\ntype AutocompleteProps\u003cT> = {\n  label: string;\n  searchOptions: (\n    query: string,\n    signal: AbortSignal,\n  ) => Promise\u003cT[]>;\n  getOptionId: (option: T) => OptionId;\n  getOptionLabel: (option: T) => string;\n  renderOption?: (option: T) => ReactNode;\n  onSelect: (option: T) => void;\n  minimumQueryLength?: number;\n  debounceDelay?: number;\n  suggestionsLabel?: string;\n};\n```\n\nThe responsibilities are explicit:\n\n- `T` is the caller\'s result type, such as `Person`, `Product`, or `City`.\n- `searchOptions` owns data access and accepts an `AbortSignal`.\n- `getOptionId` provides stable identity without assuming an `id` field.\n- `getOptionLabel` provides input text and an accessible option label.\n- `renderOption` customizes visible content without changing combobox behavior.\n- `onSelect` returns the original typed record to the parent.\n- `debounceDelay` controls search timing without delaying the input value.\n\nFor example, one caller may use:\n\n```tsx\ntype Product = {\n  sku: string;\n  title: string;\n  price: number;\n};\n```\n\n## State model and invariants\n\nStart by naming the independent pieces of state.\n\n```ts\nconst [searchState, setSearchState] = useState\u003cSearchState\u003cT>>({\n  status: "idle",\n  results: [],\n});\n```\n\nThe component also needs:\n\n```ts\nconst [inputValue, setInputValue] = useState("");\nconst [activeIndex, setActiveIndex] = useState(-1);\nconst [dismissed, setDismissed] = useState(false);\n```\n\nDo not combine these concepts accidentally:\n\n- `inputValue` is the editable text.\n- `results` are suggestions for the current debounced query.\n- `activeIndex` identifies the suggestion currently navigated by keyboard.\n- `dismissed` records an intentional Escape or selection close that cannot be derived.\n- the selected option belongs to the parent through `onSelect`.\n- whether the popup is visible can normally be derived.\n\nUseful invariants:\n\n1. `activeIndex` is `-1` or identifies an existing result.\n2. Results displayed after a request belong to the latest effective query.\n3. Selecting an option uses `getOptionLabel`, returns the original record, and closes the popup.\n4. Escape closes the popup without selecting the active option.\n5. DOM focus remains on the input while assistive-technology focus follows the active option.\n\nAvoid a collection of loosely related booleans such as `isLoading`, `hasError`, `isEmpty`, and `isOpen` when they can contradict one another. A status union makes the possible request states explicit.\n\n## Component and Hook design\n\nKeep the interview architecture small:\n\n```text\nAutocomplete\u003cT>\n├── input and label\n├── status message\n└── suggestion list\n```\n\nOne component is acceptable at this size. Extract behavior only when the boundary becomes useful.\n\nA reasonable public API is:\n\n```ts\ntype ProductAutocompleteProps = AutocompleteProps\u003cProduct>;\n```\n\nThis API makes the data source injectable. The component does not know the endpoint, authentication mechanism, or application store. That makes it easier to test and reuse.\n\nDo not extract `useAutocomplete` merely to show that you know custom Hooks. Extract it when multiple renderers need the behavior or when the component becomes difficult to reason about. A custom Hook shares behavior, not markup or accessibility semantics.\n\n## Step-by-step React solution\n\nBuild one working vertical slice at a time. Every step below has a visible result and introduces only one new source of complexity.\n\nThe sequence is intentional:\n\n```text\nControlled input\n    ↓\nLocal suggestions\n    ↓\nAsynchronous search\n    ↓\nRace protection\n    ↓\nDebounced requests\n    ↓\nKeyboard navigation\n    ↓\nCombobox semantics\n```\n\nIf the interview ends early, you still have a working checkpoint and can explain the next step.\n\n### Step 1: build a synchronous generic version\n\nStart with caller-supplied local options and the type contract defined above:\n\n```tsx\ntype LocalAutocompleteProps\u003cT> = Pick\u003c\n  AutocompleteProps\u003cT>,\n  "label" | "getOptionId" | "getOptionLabel" | "renderOption" | "onSelect"\n> & {\n  options: readonly T[];\n};\n\nexport function LocalAutocomplete\u003cT>({\n  label,\n  options,\n  getOptionId,\n  getOptionLabel,\n  renderOption,\n  onSelect,\n}: LocalAutocompleteProps\u003cT>) {\n  const [inputValue, setInputValue] = useState("");\n  const query = inputValue.trim().toLocaleLowerCase();\n  const results = query\n    ? options.filter(option =>\n        getOptionLabel(option).toLocaleLowerCase().includes(query),\n      )\n    : [];\n\n  function selectOption(option: T) {\n    setInputValue(getOptionLabel(option));\n    onSelect(option);\n  }\n\n  return (\n    \u003cdiv>\n      \u003clabel>\n        {label}\n        \u003cinput\n          value={inputValue}\n          onChange={event => setInputValue(event.target.value)}\n        />\n      \u003c/label>\n      \u003cul>\n        {results.map(option => (\n          \u003cli key={getOptionId(option)}>\n            \u003cbutton type="button" onClick={() => selectOption(option)}>\n              {renderOption?.(option) ?? getOptionLabel(option)}\n            \u003c/button>\n          \u003c/li>\n        ))}\n      \u003c/ul>\n    \u003c/div>\n  );\n}\n```\n\nThe component assumes nothing about the shape of T. Identity, searchable labels, visible content, and the selected record all come from typed caller props.\n\n### Step 2: replace local options with asynchronous search\n\nUse the generic request state from the opening contract:\n\n```tsx\nconst [searchState, setSearchState] = useState\u003cSearchState\u003cT>>({\n  status: "idle",\n  results: [],\n});\n\nuseEffect(() => {\n  const query = inputValue.trim();\n\n  if (query.length \u003c minimumQueryLength) {\n    setSearchState({ status: "idle", results: [] });\n    return;\n  }\n\n  const signal = new AbortController().signal;\n  setSearchState({ status: "loading", results: [] });\n\n  searchOptions(query, signal)\n    .then(results => {\n      setSearchState({ status: "success", results });\n    })\n    .catch(() => {\n      setSearchState({\n        status: "error",\n        results: [],\n        message: "Suggestions could not be loaded.",\n      });\n    });\n}, [inputValue, minimumQueryLength, searchOptions]);\n```\n\nRender loading, empty, and error states independently from generic option rendering:\n\n```tsx\n{searchState.status === "loading" && \u003cp>Loading…\u003c/p>}\n{searchState.status === "error" && \u003cp role="alert">{searchState.message}\u003c/p>}\n{searchState.status === "success" && searchState.results.length === 0 && (\n  \u003cp>No suggestions found.\u003c/p>\n)}\n```\n\n### Step 3: protect against stale responses\n\nEach Effect execution owns one request:\n\n```tsx\nuseEffect(() => {\n  const query = inputValue.trim();\n\n  if (query.length \u003c minimumQueryLength) {\n    setSearchState({ status: "idle", results: [] });\n    return;\n  }\n\n  const controller = new AbortController();\n  let ignore = false;\n\n  setSearchState(previous => ({\n    status: "loading",\n    results: previous.results,\n  }));\n\n  searchOptions(query, controller.signal)\n    .then(results => {\n      if (!ignore) setSearchState({ status: "success", results });\n    })\n    .catch(error => {\n      const aborted =\n        error instanceof DOMException && error.name === "AbortError";\n\n      if (!ignore && !aborted) {\n        setSearchState({\n          status: "error",\n          results: [],\n          message: "Suggestions could not be loaded.",\n        });\n      }\n    });\n\n  return () => {\n    ignore = true;\n    controller.abort();\n  };\n}, [inputValue, minimumQueryLength, searchOptions]);\n```\n\nCancellation reduces work; the ignore guard prevents obsolete results even when a supplied search implementation does not fully honor abort.\n\n### Step 4: debounce the query, not the input\n\nExtract timing into a reusable value Hook:\n\n```tsx\nfunction useDebounce\u003cT>(value: T, delay: number): T {\n  const [debouncedValue, setDebouncedValue] = useState(value);\n\n  useEffect(() => {\n    const timer = window.setTimeout(() => {\n      setDebouncedValue(value);\n    }, delay);\n\n    return () => window.clearTimeout(timer);\n  }, [value, delay]);\n\n  return debouncedValue;\n}\n\nconst debouncedQuery = useDebounce(\n  inputValue.trim(),\n  debounceDelay,\n);\n```\n\nThe input still displays inputValue immediately. Only the Effect that starts external work changes from inputValue to debouncedQuery.\n\n### Step 5: add generic keyboard navigation\n\nThe active index navigates the current typed result array:\n\n```tsx\nconst results = searchState.results;\nconst activeOption =\n  activeIndex >= 0 ? results[activeIndex] : undefined;\n\nfunction handleKeyDown(event: KeyboardEvent\u003cHTMLInputElement>) {\n  if (event.key === "Escape") {\n    setDismissed(true);\n    setActiveIndex(-1);\n    return;\n  }\n\n  if (results.length === 0) return;\n\n  if (event.key === "ArrowDown") {\n    event.preventDefault();\n    setActiveIndex(index =>\n      index \u003c results.length - 1 ? index + 1 : 0,\n    );\n  } else if (event.key === "ArrowUp") {\n    event.preventDefault();\n    setActiveIndex(index =>\n      index > 0 ? index - 1 : results.length - 1,\n    );\n  } else if (event.key === "Enter" && activeOption) {\n    event.preventDefault();\n    selectOption(activeOption);\n  }\n}\n```\n\nSelection uses getOptionLabel for input text and returns the original T:\n\n```tsx\nfunction selectOption(option: T) {\n  setInputValue(getOptionLabel(option));\n  setDismissed(true);\n  setActiveIndex(-1);\n  onSelect(option);\n}\n```\n\n### Step 6: add generic combobox semantics\n\nCaller-provided identity creates stable React keys and active-descendant IDs:\n\n```tsx\nconst activeOptionId = activeOption\n  ? `\\${reactId}-option-\\${getOptionId(activeOption)}`\n  : undefined;\n\n\u003cinput\n  id={inputId}\n  role="combobox"\n  aria-autocomplete="list"\n  aria-expanded={canShowPopup}\n  aria-controls={listboxId}\n  aria-activedescendant={activeOptionId}\n  aria-describedby={statusId}\n  value={inputValue}\n  onChange={event => handleInputChange(event.target.value)}\n  onKeyDown={handleKeyDown}\n/>\n\n\u003cul id={listboxId} role="listbox" aria-label={suggestionsLabel}>\n  {results.map((option, index) => {\n    const optionId = getOptionId(option);\n    const active = index === activeIndex;\n\n    return (\n      \u003cli\n        id={`\\${reactId}-option-\\${optionId}`}\n        role="option"\n        aria-selected={active}\n        key={optionId}\n        ref={node => {\n          optionRefs.current[index] = node;\n        }}\n        onMouseDown={event => event.preventDefault()}\n        onClick={() => selectOption(option)}\n      >\n        {renderOption?.(option) ?? getOptionLabel(option)}\n      \u003c/li>\n    );\n  })}\n\u003c/ul>\n```\n\nThe autocomplete owns listbox semantics and keyboard behavior. Callers customize only typed data access and visible option content.\n\nThis completes the generic vertical slice. The assembled component below repeats the contract so it is self-contained and ready to copy.\n\n## Complete interview-sized implementation\n\nThe following version combines all six steps in one component.\n\n```tsx\nimport {\n  useEffect,\n  useId,\n  useRef,\n  useState,\n  type KeyboardEvent,\n  type ReactNode,\n} from "react";\n\ntype OptionId = string | number;\n\ntype SearchState\u003cT> =\n  | { status: "idle"; results: T[] }\n  | { status: "loading"; results: T[] }\n  | { status: "success"; results: T[] }\n  | { status: "error"; results: T[]; message: string };\n\ntype AutocompleteProps\u003cT> = {\n  label: string;\n  searchOptions: (\n    query: string,\n    signal: AbortSignal,\n  ) => Promise\u003cT[]>;\n  getOptionId: (option: T) => OptionId;\n  getOptionLabel: (option: T) => string;\n  renderOption?: (option: T) => ReactNode;\n  onSelect: (option: T) => void;\n  minimumQueryLength?: number;\n  debounceDelay?: number;\n  suggestionsLabel?: string;\n};\n\nfunction useDebounce\u003cT>(value: T, delay: number): T {\n  const [debouncedValue, setDebouncedValue] = useState(value);\n\n  useEffect(() => {\n    const timer = window.setTimeout(() => {\n      setDebouncedValue(value);\n    }, delay);\n\n    return () => window.clearTimeout(timer);\n  }, [value, delay]);\n\n  return debouncedValue;\n}\n\nexport function Autocomplete\u003cT>({\n  label,\n  searchOptions,\n  getOptionId,\n  getOptionLabel,\n  renderOption,\n  onSelect,\n  minimumQueryLength = 2,\n  debounceDelay = 250,\n  suggestionsLabel = `${label} suggestions`,\n}: AutocompleteProps\u003cT>) {\n  const reactId = useId();\n  const inputId = `${reactId}-input`;\n  const listboxId = `${reactId}-listbox`;\n  const statusId = `${reactId}-status`;\n\n  const [inputValue, setInputValue] = useState("");\n  const debouncedQuery = useDebounce(inputValue.trim(), debounceDelay);\n  const [activeIndex, setActiveIndex] = useState(-1);\n  const [dismissed, setDismissed] = useState(false);\n  const [searchState, setSearchState] = useState\u003cSearchState\u003cT>>({\n    status: "idle",\n    results: [],\n  });\n\n  const optionRefs = useRef\u003cArray\u003cHTMLLIElement | null>>([]);\n\n  useEffect(() => {\n    if (debouncedQuery.length \u003c minimumQueryLength) {\n      setSearchState({ status: "idle", results: [] });\n      setActiveIndex(-1);\n      return;\n    }\n\n    const controller = new AbortController();\n    let ignore = false;\n\n    setSearchState(previous => ({\n      status: "loading",\n      results: previous.results,\n    }));\n\n    searchOptions(debouncedQuery, controller.signal)\n      .then(results => {\n        if (ignore) return;\n        setSearchState({ status: "success", results });\n        setActiveIndex(-1);\n      })\n      .catch(error => {\n        if (ignore || error instanceof DOMException && error.name === "AbortError") {\n          return;\n        }\n\n        setSearchState({\n          status: "error",\n          results: [],\n          message: "Suggestions could not be loaded.",\n        });\n        setActiveIndex(-1);\n      });\n\n    return () => {\n      ignore = true;\n      controller.abort();\n    };\n  }, [debouncedQuery, minimumQueryLength, searchOptions]);\n\n  const results = searchState.results;\n  const canShowPopup =\n    !dismissed &&\n    inputValue.trim().length >= minimumQueryLength &&\n    (searchState.status === "loading" ||\n      searchState.status === "error" ||\n      searchState.status === "success");\n\n  const activeOption = activeIndex >= 0 ? results[activeIndex] : undefined;\n  const activeOptionId = activeOption\n    ? `${reactId}-option-${getOptionId(activeOption)}`\n    : undefined;\n\n  useEffect(() => {\n    if (activeIndex >= 0) {\n      optionRefs.current[activeIndex]?.scrollIntoView({\n        block: "nearest",\n      });\n    }\n  }, [activeIndex]);\n\n  function selectOption(option: T) {\n    setInputValue(getOptionLabel(option));\n    setDismissed(true);\n    setActiveIndex(-1);\n    onSelect(option);\n  }\n\n  function handleKeyDown(event: KeyboardEvent\u003cHTMLInputElement>) {\n    if (event.key === "Escape") {\n      if (canShowPopup) {\n        event.preventDefault();\n        setDismissed(true);\n        setActiveIndex(-1);\n      }\n      return;\n    }\n\n    if (results.length === 0) return;\n\n    if (event.key === "ArrowDown") {\n      event.preventDefault();\n      setDismissed(false);\n      setActiveIndex(index =>\n        index \u003c results.length - 1 ? index + 1 : 0,\n      );\n      return;\n    }\n\n    if (event.key === "ArrowUp") {\n      event.preventDefault();\n      setDismissed(false);\n      setActiveIndex(index =>\n        index > 0 ? index - 1 : results.length - 1,\n      );\n      return;\n    }\n\n    if (event.key === "Enter" && activeOption) {\n      event.preventDefault();\n      selectOption(activeOption);\n    }\n  }\n\n  function handleInputChange(value: string) {\n    setInputValue(value);\n    setDismissed(false);\n    setActiveIndex(-1);\n  }\n\n  let statusMessage = "";\n\n  if (inputValue.trim().length > 0 && inputValue.trim().length \u003c minimumQueryLength) {\n    statusMessage = `Enter at least ${minimumQueryLength} characters.`;\n  } else if (searchState.status === "loading") {\n    statusMessage = "Loading suggestions.";\n  } else if (searchState.status === "error") {\n    statusMessage = searchState.message;\n  } else if (searchState.status === "success") {\n    statusMessage = results.length === 0\n      ? "No suggestions found."\n      : `${results.length} suggestions available.`;\n  }\n\n  return (\n    \u003cdiv className="autocomplete">\n      \u003clabel htmlFor={inputId}>{label}\u003c/label>\n      \u003cinput\n        id={inputId}\n        role="combobox"\n        aria-autocomplete="list"\n        aria-expanded={canShowPopup}\n        aria-controls={listboxId}\n        aria-activedescendant={activeOptionId}\n        aria-describedby={statusId}\n        autoComplete="off"\n        value={inputValue}\n        onChange={event => handleInputChange(event.target.value)}\n        onKeyDown={handleKeyDown}\n        onFocus={() => setDismissed(false)}\n      />\n\n      \u003cdiv id={statusId} role="status" className="sr-only">\n        {statusMessage}\n      \u003c/div>\n\n      {canShowPopup && (\n        \u003cdiv className="autocomplete-popup">\n          {searchState.status === "loading" && results.length === 0 && (\n            \u003cp>Loading…\u003c/p>\n          )}\n\n          {searchState.status === "error" && (\n            \u003cp role="alert">{searchState.message}\u003c/p>\n          )}\n\n          {searchState.status === "success" && results.length === 0 && (\n            \u003cp>No suggestions found.\u003c/p>\n          )}\n\n          {results.length > 0 && (\n            \u003cul id={listboxId} role="listbox" aria-label={suggestionsLabel}>\n              {results.map((option, index) => {\n                const optionId = getOptionId(option);\n                const active = index === activeIndex;\n\n                return (\n                  \u003cli\n                    id={`${reactId}-option-${optionId}`}\n                    role="option"\n                    aria-selected={active}\n                    key={optionId}\n                    ref={node => {\n                      optionRefs.current[index] = node;\n                    }}\n                    onMouseDown={event => event.preventDefault()}\n                    onClick={() => selectOption(option)}\n                  >\n                    {renderOption?.(option) ?? getOptionLabel(option)}\n                  \u003c/li>\n                );\n              })}\n            \u003c/ul>\n          )}\n        \u003c/div>\n      )}\n    \u003c/div>\n  );\n}\n```\n\nHere is one product configuration. A city, repository, or employee search supplies different accessors and rendering without changing `Autocomplete`:\n\n```tsx\n\u003cAutocomplete\u003cProduct>\n  label="Product"\n  searchOptions={searchProducts}\n  getOptionId={product => product.sku}\n  getOptionLabel={product => product.title}\n  renderOption={product => (\n    \u003c>\n      \u003cstrong>{product.title}\u003c/strong>\n      \u003cspan>{formatCurrency(product.price)}\u003c/span>\n    \u003c/>\n  )}\n  onSelect={product => addToCart(product)}\n  minimumQueryLength={2}\n  debounceDelay={250}\n/>\n```\n\n## Why the solution is structured this way\n\n### The raw input and debounced query are separate\n\nThe input must update immediately on every key press. Debouncing the state bound to `value` would make typing feel delayed. Instead, the raw input is urgent UI state and the debounced query controls only external synchronization.\n\n`useDebounce` owns only the timer lifecycle and remains reusable for other delayed values. The autocomplete still owns query normalization (`trim`), the delay choice, minimum-length rules, networking, and rendering. Keeping those responsibilities outside the hook avoids turning a small timing primitive into a feature-specific abstraction.\n\n### The Effect synchronizes with the current query\n\nFetching belongs in an Effect in this standalone example because results must remain synchronized with the effective query regardless of whether it came from typing, restored URL state, or a parent update.\n\nProduction frameworks and server-state libraries may provide a better loading boundary, cache, deduplication, and server-rendering strategy. The interview implementation keeps the mechanism explicit so it can be discussed.\n\n### Cancellation and ignoring are both present\n\n`AbortController` asks the underlying request to stop. The `ignore` flag protects state even if the supplied data function does not fully honor cancellation or resolves during cleanup.\n\nThis prevents the classic race:\n\n```text\nRequest "rea" starts\nRequest "react" starts\nRequest "react" finishes\nRequest "rea" finishes later and overwrites the correct results\n```\n\n### Popup visibility is mostly derived\n\nThe component does not maintain an independent `isOpen` value that must be synchronized with query length, request state, and available results. It derives visibility and keeps only `dismissed`, the user decision that cannot be inferred from the other state.\n\n### Input focus does not move into the list\n\nDOM focus remains on the editable input so standard text-editing keys continue to work. `aria-activedescendant` communicates which listbox option is active. This follows the editable combobox focus model.\n\n## Accessibility and keyboard behavior\n\nThe semantic relationships are as important as the rendered list.\n\nThe input exposes:\n\n- `role="combobox"`;\n- `aria-expanded` for popup visibility;\n- `aria-controls` pointing to the listbox;\n- `aria-autocomplete="list"`;\n- `aria-activedescendant` pointing to the active option.\n\nThe popup exposes:\n\n- a `listbox` containing `option` elements;\n- `aria-selected` on the active option;\n- stable IDs derived from stable result IDs.\n\nKeyboard contract:\n\n| Key | Behavior |\n| --- | --- |\n| Arrow Down | Open the popup if necessary and move to the next option |\n| Arrow Up | Open the popup if necessary and move to the previous option |\n| Enter | Select the active option |\n| Escape | Close without selecting |\n| Tab | Leave the widget normally |\n| Printable keys | Continue editing the input using native behavior |\n\nDo not intercept Home, End, Left Arrow, or Right Arrow unless the chosen combobox behavior explicitly requires it. They are native text-editing keys while focus remains in the input.\n\nPointer selection uses `onMouseDown={event => event.preventDefault()}` so the input does not lose focus before the click completes. A production component must test pointer, touch, and mobile assistive-technology behavior rather than assuming mouse behavior covers every input modality.\n\n## Async, performance, and edge cases\n\n### Minimum query length\n\nShort queries often produce noisy results and unnecessary server load. Make the threshold explicit and communicate it to the user.\n\n### Debouncing\n\nDebouncing reduces requests during continuous typing. It does not make a slow endpoint faster, and it adds intentional delay. Choose the duration based on request cost and product behavior rather than copying 300 milliseconds automatically.\n\n### Caching\n\nA small cache can make repeated queries instant:\n\n```ts\nfunction createSearchCache\u003cT>() {\n  return new Map\u003cstring, T[]>();\n}\n```\n\nBut a cache creates questions:\n\n- How long are results fresh?\n- Is the data safe to retain?\n- Is the cache shared across component instances?\n- Is its size bounded?\n- Can user permissions change?\n\nFor production, a server-state library or framework cache may be more appropriate than a component-local map.\n\n### Result identity\n\nUse `getOptionId(option)` for React keys and option IDs. An array index changes meaning when results are inserted, removed, or reordered.\n\n### Selected value versus input text\n\nThis implementation allows arbitrary text. If selection is required, the component needs an explicit validity rule and must decide what blur, form submission, and later editing do to the selected object.\n\n### Internationalized matching\n\nClient filtering through lowercase string comparison is not universally correct. Locale, accents, tokenization, transliteration, and server ranking may affect the intended behavior.\n\n### Large result sets\n\nAutocomplete should usually return a small ranked result set. Virtualizing hundreds of suggestions can complicate `aria-activedescendant`, scrolling, and screen-reader behavior. Prefer a better result limit and ranking before adding virtualization.\n\n## Testing strategy\n\nTest observable behavior, not internal state variables.\n\n### Essential component tests\n\n1. Typing fewer than the minimum characters does not request results.\n2. Typing a valid query eventually requests it.\n3. Loading, success, empty, and error states are rendered.\n4. A stale request cannot replace the newest results.\n5. Arrow Down and Arrow Up change the active option.\n6. Enter selects the active option.\n7. Escape closes without selecting.\n8. Pointer selection calls `onSelect` with the original typed option.\n9. The input exposes the expected combobox state.\n10. The active descendant points to an existing option.\n\nExample test outline:\n\n```tsx\nit("does not display an older response", async () => {\n  const requests = createControllableSearch();\n  const user = userEvent.setup();\n\n  render(\n    \u003cAutocomplete\u003cProduct>\n      label="Product"\n      searchOptions={requests.search}\n      getOptionId={product => product.sku}\n      getOptionLabel={product => product.title}\n      onSelect={() => {}}\n    />,\n  );\n\n  const input = screen.getByRole("combobox", {\n    name: "Product",\n  });\n\n  await user.type(input, "rea");\n  await advanceDebounce();\n  await user.type(input, "ct");\n  await advanceDebounce();\n\n  requests.resolve("react", [reactProduct]);\n  expect(await screen.findByText("React Product")).toBeVisible();\n\n  requests.resolve("rea", [olderProduct]);\n  expect(screen.queryByText("Older Product")).not.toBeInTheDocument();\n});\n```\n\nAutomated accessibility checks are useful but insufficient. Manually test keyboard behavior, visible focus, zoom, pointer interaction, and representative screen readers.\n\n## Common candidate mistakes\n\n### Debouncing the controlled input\n\nThis makes typing visibly lag. Debounce the query that triggers work, not the value rendered by the input.\n\n### Storing filtered results through another Effect\n\nLocal filtered results are derived data and can be calculated during rendering. Effects are needed only when synchronizing with an external source.\n\n### Fetching without cleanup\n\nResponses can arrive out of order. A loading implementation is incomplete until it prevents stale results.\n\n### Using array indexes as option identity\n\nIndexes change when the result set changes and can make active-descendant relationships point at the wrong item.\n\n### Moving DOM focus into every suggestion\n\nEditable comboboxes normally keep focus in the input. Moving focus can interfere with continued typing and standard editing keys.\n\n### Treating ARIA attributes as complete accessibility\n\nRoles do not implement keyboard behavior, focus management, scrolling, announcements, or touch interaction.\n\n### Closing on blur without understanding pointer order\n\nThe input can blur before a suggestion click fires, unmounting the option before selection. Handle the interaction deliberately and test it.\n\n### Overengineering before a vertical slice works\n\nBuilding a generic reducer, cache, compound API, and virtualization layer before selection works is a poor interview tradeoff. Establish the critical journey first.\n\n## Senior-level improvements\n\nAfter the interview-sized version works, discuss these production decisions:\n\n- integrate with a framework loader or server-state library;\n- share request caching and deduplication;\n- expose controlled input and selected-value APIs;\n- add configurable result rendering without losing semantics;\n- support required-selection and free-text modes explicitly;\n- preserve results during non-urgent updates;\n- add retry behavior and richer errors;\n- test with multiple browsers and assistive technologies;\n- collect query latency, selection, abandonment, and error metrics;\n- protect private result data from inappropriate caching;\n- document API invariants for design-system consumers.\n\nDo not claim every improvement belongs in every product. A reusable design-system combobox and a single product search field have different complexity budgets.\n\n## A 60-second solution explanation\n\nThe component is generic over `T`: callers provide search, identity, label, and optional rendering while the component owns asynchronous and accessible combobox behavior. I separate the immediate input from the configurable debounced query, and the request Effect uses `AbortController` plus an ignore guard to prevent stale results. Request state is an explicit status, popup visibility is mostly derived, and DOM focus stays in the input while `aria-activedescendant` tracks keyboard navigation. In production I would evaluate a framework or server-state cache, test across assistive technologies, and make controlled selection and free-text behavior explicit.\n\n## Likely interview follow-ups\n\n### Why not use `useDeferredValue` instead of debouncing?\n\nDeferring changes rendering priority; it does not inherently reduce network requests. Debouncing intentionally waits for a quiet period before starting work. They solve different problems and can be combined when appropriate.\n\n### Would you use an Effect for production data fetching?\n\nNot automatically. A framework loader or server-state library may provide caching, deduplication, server rendering, prefetching, and better error boundaries. The Effect implementation demonstrates the lifecycle requirements when the component owns synchronization directly.\n\n### How would you support controlled selection?\n\nAccept `selectedOption` and `onSelectedOptionChange`, define how input text relates to the selected value, and avoid copying the controlled prop into independent state without an intentional editing model.\n\n### How would you avoid request waterfalls?\n\nLoad data at an appropriate route or feature boundary, prefetch likely data, and avoid waiting for a child to mount before discovering a request that the parent already knows will be needed.\n\n### How would you render rich suggestions?\n\nAccept a render function or structured slots while keeping listbox and option semantics owned by the autocomplete. Consumers should customize content without needing to rebuild keyboard and accessibility behavior.\n\n## Article summary\n\n- Clarify whether text is free-form or selection is required.\n- Keep record-specific identity, labels, and rendering in typed caller props.\n- Keep raw input, debounced query, active option, and selected value conceptually separate.\n- Debounce external work rather than the controlled input.\n- Cancel or ignore stale requests.\n- Derive popup visibility where possible.\n- Keep DOM focus in an editable combobox input.\n- Implement keyboard behavior in addition to ARIA relationships.\n- Use stable domain identity for keys and option IDs.\n- Build a complete vertical slice before production hardening.',
    headings: [
      "The interview prompt",
      "What the interviewer is evaluating",
      "Clarifying questions to ask",
      "Requirements and deliberate non-requirements",
      "Type contract",
      "State model and invariants",
      "Component and Hook design",
      "Step-by-step React solution",
      "Complete interview-sized implementation",
      "Why the solution is structured this way",
      "Accessibility and keyboard behavior",
      "Async, performance, and edge cases",
      "Testing strategy",
      "Common candidate mistakes",
      "Senior-level improvements",
      "A 60-second solution explanation",
      "Likely interview follow-ups",
      "Article summary",
    ],
    quickRefresher: "",
    excerpt:
      "Build a reusable autocomplete component that lets a user search for and select one suggested record. The caller supplies the record type, asynchronous search function, stable identity, label, and optional rendering.",
    readingMinutes: 13,
    searchText:
      'accessible autocomplete the interview prompt build a reusable autocomplete component that lets a user search for and select one suggested record. the caller supplies the record type, asynchronous search function, stable identity, label, and optional rendering. the component should: update suggestions as the user types; fetch results from an asynchronous data source; display loading, empty, and error states; support pointer and keyboard selection; close when a result is selected or the user presses escape; expose the selected option to its parent. assume the interview lasts 45–60 minutes. styling is secondary to behavior, state design, and clear reasoning. what the interviewer is evaluating autocomplete looks like a text input and a list, but it exposes several important frontend skills at once: translating an ambiguous product prompt into explicit behavior; separating input state, selected value, and highlighted option; synchronizing react with an asynchronous external system; preventing stale responses from replacing current results; preserving immediate typing while requests are delayed; managing composite widget keyboard behavior; using stable identities and semantic relationships; deciding what belongs in the interview solution and what belongs in production hardening. a senior candidate is not expected to build a production design system combobox from memory. they are expected to identify the difficult parts, establish a coherent model, build the critical behavior, and explain the remaining risks. clarifying questions to ask before writing jsx, clarify the contract. 1. are suggestions local or fetched from a server? 2. how many characters should trigger a search? 3. is arbitrary text allowed, or must the user select a result? 4. should the search be debounced? 5. what information does each suggestion display? 6. what happens when no result matches? 7. which keyboard interactions are required? 8. should the component be controlled by its parent? 9. does selecting an item submit, navigate, or only update state? 10. are external component libraries permitted? for this article, use the following decisions: results come from an asynchronous caller provided search function; two characters trigger a request; arbitrary text may remain in the input; requests are debounced by 250 milliseconds; selecting a result calls onselect(option) ; dom focus remains in the input while arrow keys change the active suggestion; the first version uses no external component library. requirements and deliberate non requirements required in the interview version controlled text input debounced query loading, error, empty, and success states stale request protection arrow up and arrow down navigation enter to select escape to close pointer selection correct combobox, listbox, and option relationships stable option ids explain, but do not necessarily finish result caching request deduplication across component instances windowing thousands of results mobile screen reader testing internationalized matching analytics server rendering full browser and assistive technology compatibility testing this boundary is important. a live coding solution should be small enough to complete and rich enough to demonstrate judgment. type contract define the reusable boundary before state or jsx. every progressive checkpoint and the assembled implementation use the same generic contract: the responsibilities are explicit: t is the caller\'s result type, such as person , product , or city . searchoptions owns data access and accepts an abortsignal . getoptionid provides stable identity without assuming an id field. getoptionlabel provides input text and an accessible option label. renderoption customizes visible content without changing combobox behavior. onselect returns the original typed record to the parent. debouncedelay controls search timing without delaying the input value. for example, one caller may use: state model and invariants start by naming the independent pieces of state. the component also needs: do not combine these concepts accidentally: inputvalue is the editable text. results are suggestions for the current debounced query. activeindex identifies the suggestion currently navigated by keyboard. dismissed records an intentional escape or selection close that cannot be derived. the selected option belongs to the parent through onselect . whether the popup is visible can normally be derived. useful invariants: 1. activeindex is 1 or identifies an existing result. 2. results displayed after a request belong to the latest effective query. 3. selecting an option uses getoptionlabel , returns the original record, and closes the popup. 4. escape closes the popup without selecting the active option. 5. dom focus remains on the input while assistive technology focus follows the active option. avoid a collection of loosely related booleans such as isloading , haserror , isempty , and isopen when they can contradict one another. a status union makes the possible request states explicit. component and hook design keep the interview architecture small: one component is acceptable at this size. extract behavior only when the boundary becomes useful. a reasonable public api is: this api makes the data source injectable. the component does not know the endpoint, authentication mechanism, or application store. that makes it easier to test and reuse. do not extract useautocomplete merely to show that you know custom hooks. extract it when multiple renderers need the behavior or when the component becomes difficult to reason about. a custom hook shares behavior, not markup or accessibility semantics. step by step react solution build one working vertical slice at a time. every step below has a visible result and introduces only one new source of complexity. the sequence is intentional: if the interview ends early, you still have a working checkpoint and can explain the next step. step 1: build a synchronous generic version start with caller supplied local options and the type contract defined above: the component assumes nothing about the shape of t. identity, searchable labels, visible content, and the selected record all come from typed caller props. step 2: replace local options with asynchronous search use the generic request state from the opening contract: render loading, empty, and error states independently from generic option rendering: step 3: protect against stale responses each effect execution owns one request: cancellation reduces work; the ignore guard prevents obsolete results even when a supplied search implementation does not fully honor abort. step 4: debounce the query, not the input extract timing into a reusable value hook: the input still displays inputvalue immediately. only the effect that starts external work changes from inputvalue to debouncedquery. step 5: add generic keyboard navigation the active index navigates the current typed result array: selection uses getoptionlabel for input text and returns the original t: step 6: add generic combobox semantics caller provided identity creates stable react keys and active descendant ids: the autocomplete owns listbox semantics and keyboard behavior. callers customize only typed data access and visible option content. this completes the generic vertical slice. the assembled component below repeats the contract so it is self contained and ready to copy. complete interview sized implementation the following version combines all six steps in one component. here is one product configuration. a city, repository, or employee search supplies different accessors and rendering without changing autocomplete : why the solution is structured this way the raw input and debounced query are separate the input must update immediately on every key press. debouncing the state bound to value would make typing feel delayed. instead, the raw input is urgent ui state and the debounced query controls only external synchronization. usedebounce owns only the timer lifecycle and remains reusable for other delayed values. the autocomplete still owns query normalization ( trim ), the delay choice, minimum length rules, networking, and rendering. keeping those responsibilities outside the hook avoids turning a small timing primitive into a feature specific abstraction. the effect synchronizes with the current query fetching belongs in an effect in this standalone example because results must remain synchronized with the effective query regardless of whether it came from typing, restored url state, or a parent update. production frameworks and server state libraries may provide a better loading boundary, cache, deduplication, and server rendering strategy. the interview implementation keeps the mechanism explicit so it can be discussed. cancellation and ignoring are both present abortcontroller asks the underlying request to stop. the ignore flag protects state even if the supplied data function does not fully honor cancellation or resolves during cleanup. this prevents the classic race: popup visibility is mostly derived the component does not maintain an independent isopen value that must be synchronized with query length, request state, and available results. it derives visibility and keeps only dismissed , the user decision that cannot be inferred from the other state. input focus does not move into the list dom focus remains on the editable input so standard text editing keys continue to work. aria activedescendant communicates which listbox option is active. this follows the editable combobox focus model. accessibility and keyboard behavior the semantic relationships are as important as the rendered list. the input exposes: role="combobox" ; aria expanded for popup visibility; aria controls pointing to the listbox; aria autocomplete="list" ; aria activedescendant pointing to the active option. the popup exposes: a listbox containing option elements; aria selected on the active option; stable ids derived from stable result ids. keyboard contract: key behavior arrow down open the popup if necessary and move to the next option arrow up open the popup if necessary and move to the previous option enter select the active option escape close without selecting tab leave the widget normally printable keys continue editing the input using native behavior do not intercept home, end, left arrow, or right arrow unless the chosen combobox behavior explicitly requires it. they are native text editing keys while focus remains in the input. pointer selection uses onmousedown={event = event.preventdefault()} so the input does not lose focus before the click completes. a production component must test pointer, touch, and mobile assistive technology behavior rather than assuming mouse behavior covers every input modality. async, performance, and edge cases minimum query length short queries often produce noisy results and unnecessary server load. make the threshold explicit and communicate it to the user. debouncing debouncing reduces requests during continuous typing. it does not make a slow endpoint faster, and it adds intentional delay. choose the duration based on request cost and product behavior rather than copying 300 milliseconds automatically. caching a small cache can make repeated queries instant: but a cache creates questions: how long are results fresh? is the data safe to retain? is the cache shared across component instances? is its size bounded? can user permissions change? for production, a server state library or framework cache may be more appropriate than a component local map. result identity use getoptionid(option) for react keys and option ids. an array index changes meaning when results are inserted, removed, or reordered. selected value versus input text this implementation allows arbitrary text. if selection is required, the component needs an explicit validity rule and must decide what blur, form submission, and later editing do to the selected object. internationalized matching client filtering through lowercase string comparison is not universally correct. locale, accents, tokenization, transliteration, and server ranking may affect the intended behavior. large result sets autocomplete should usually return a small ranked result set. virtualizing hundreds of suggestions can complicate aria activedescendant , scrolling, and screen reader behavior. prefer a better result limit and ranking before adding virtualization. testing strategy test observable behavior, not internal state variables. essential component tests 1. typing fewer than the minimum characters does not request results. 2. typing a valid query eventually requests it. 3. loading, success, empty, and error states are rendered. 4. a stale request cannot replace the newest results. 5. arrow down and arrow up change the active option. 6. enter selects the active option. 7. escape closes without selecting. 8. pointer selection calls onselect with the original typed option. 9. the input exposes the expected combobox state. 10. the active descendant points to an existing option. example test outline: automated accessibility checks are useful but insufficient. manually test keyboard behavior, visible focus, zoom, pointer interaction, and representative screen readers. common candidate mistakes debouncing the controlled input this makes typing visibly lag. debounce the query that triggers work, not the value rendered by the input. storing filtered results through another effect local filtered results are derived data and can be calculated during rendering. effects are needed only when synchronizing with an external source. fetching without cleanup responses can arrive out of order. a loading implementation is incomplete until it prevents stale results. using array indexes as option identity indexes change when the result set changes and can make active descendant relationships point at the wrong item. moving dom focus into every suggestion editable comboboxes normally keep focus in the input. moving focus can interfere with continued typing and standard editing keys. treating aria attributes as complete accessibility roles do not implement keyboard behavior, focus management, scrolling, announcements, or touch interaction. closing on blur without understanding pointer order the input can blur before a suggestion click fires, unmounting the option before selection. handle the interaction deliberately and test it. overengineering before a vertical slice works building a generic reducer, cache, compound api, and virtualization layer before selection works is a poor interview tradeoff. establish the critical journey first. senior level improvements after the interview sized version works, discuss these production decisions: integrate with a framework loader or server state library; share request caching and deduplication; expose controlled input and selected value apis; add configurable result rendering without losing semantics; support required selection and free text modes explicitly; preserve results during non urgent updates; add retry behavior and richer errors; test with multiple browsers and assistive technologies; collect query latency, selection, abandonment, and error metrics; protect private result data from inappropriate caching; document api invariants for design system consumers. do not claim every improvement belongs in every product. a reusable design system combobox and a single product search field have different complexity budgets. a 60 second solution explanation the component is generic over t : callers provide search, identity, label, and optional rendering while the component owns asynchronous and accessible combobox behavior. i separate the immediate input from the configurable debounced query, and the request effect uses abortcontroller plus an ignore guard to prevent stale results. request state is an explicit status, popup visibility is mostly derived, and dom focus stays in the input while aria activedescendant tracks keyboard navigation. in production i would evaluate a framework or server state cache, test across assistive technologies, and make controlled selection and free text behavior explicit. likely interview follow ups why not use usedeferredvalue instead of debouncing? deferring changes rendering priority; it does not inherently reduce network requests. debouncing intentionally waits for a quiet period before starting work. they solve different problems and can be combined when appropriate. would you use an effect for production data fetching? not automatically. a framework loader or server state library may provide caching, deduplication, server rendering, prefetching, and better error boundaries. the effect implementation demonstrates the lifecycle requirements when the component owns synchronization directly. how would you support controlled selection? accept selectedoption and onselectedoptionchange , define how input text relates to the selected value, and avoid copying the controlled prop into independent state without an intentional editing model. how would you avoid request waterfalls? load data at an appropriate route or feature boundary, prefetch likely data, and avoid waiting for a child to mount before discovering a request that the parent already knows will be needed. how would you render rich suggestions? accept a render function or structured slots while keeping listbox and option semantics owned by the autocomplete. consumers should customize content without needing to rebuild keyboard and accessibility behavior. article summary clarify whether text is free form or selection is required. keep record specific identity, labels, and rendering in typed caller props. keep raw input, debounced query, active option, and selected value conceptually separate. debounce external work rather than the controlled input. cancel or ignore stale requests. derive popup visibility where possible. keep dom focus in an editable combobox input. implement keyboard behavior in addition to aria relationships. use stable domain identity for keys and option ids. build a complete vertical slice before production hardening.',
  },
  {
    number: 2,
    slug: "02-data-table",
    title: "Sortable Data Table",
    kind: "chapter",
    partNumber: 1,
    partName: "React Live Coding",
    markdown:
      '# Sortable Data Table\n\n## The interview prompt\n\nBuild a reusable, typed data table that can display any record shape. The caller supplies rows, stable row identity, and column definitions. Users must be able to:\n\n- sort columns that opt into sorting;\n- filter rows with a text query;\n- select individual rows and select all visible rows;\n- move between pages;\n- use the table with a keyboard and assistive technology.\n\nKeep the implementation small enough for a 45–60 minute interview and explain how it would evolve for remote data or thousands of rows.\n\n## What the interviewer is evaluating\n\nThe task is less about drawing rows than modeling transformations correctly. A strong solution demonstrates:\n\n- one source of truth for input data and interaction state;\n- derived filtering, sorting, and pagination rather than synchronized copies;\n- immutable selection based on stable row IDs;\n- a deterministic comparator that does not mutate props;\n- correct native table and sort-button semantics;\n- clear empty, selection, and pagination behavior;\n- awareness of performance and server-side alternatives.\n\n## Clarifying questions to ask\n\nBefore coding, establish the contract:\n\n1. Which columns are sortable, and what is their default direction?\n2. Does a repeated header click toggle ascending and descending, or also return to unsorted?\n3. Which fields does filtering search, and is it case-sensitive?\n4. Does “select all” mean all rows in the dataset, all filtered rows, or only the current page?\n5. Should selection survive filtering and page changes?\n6. Is pagination local or server-driven?\n7. How should missing values sort?\n8. Is a single-column sort sufficient?\n\nFor this walkthrough:\n\n- sorting uses one column and toggles ascending/descending;\n- filtering searches columns that opt into filtering;\n- select all affects the current page;\n- selection survives filtering and pagination;\n- pagination and all transformations are local;\n- page size is five rows.\n\n## Requirements and deliberate non-requirements\n\n### Required in the interview version\n\n- Semantic `\u003ctable>`, `\u003cthead>`, `\u003ctbody>`, header cells, and captions.\n- Configurable rendering plus opt-in sorting and filtering per column.\n- Text filtering.\n- Stable row selection and select-all-visible behavior.\n- Local pagination whose page remains valid after filtering.\n- Empty results and a concise result summary.\n\n### Explain, but do not necessarily finish\n\n- Server-side filtering, sorting, and cursor pagination.\n- Column resizing, reordering, and visibility.\n- Multi-column sorting.\n- Virtualization for very large collections.\n- URL synchronization.\n- Persisted selection across unloaded server pages.\n\n## Type contract\n\nDefine the reusable boundary before writing component logic. Every later snippet uses these types:\n\n```tsx\nimport type { ReactNode } from "react";\n\ntype RowId = string | number;\ntype SortValue = string | number | Date;\ntype SortDirection = "ascending" | "descending";\n\ntype SortState = {\n  columnId: string;\n  direction: SortDirection;\n};\n\ntype ColumnDef\u003cT> = {\n  id: string;\n  header: string;\n  cell: (row: T) => ReactNode;\n  sortValue?: (row: T) => SortValue;\n  searchValue?: (row: T) => string;\n  rowHeader?: boolean;\n};\n\ntype DataTableProps\u003cT> = {\n  rows: readonly T[];\n  columns: readonly ColumnDef\u003cT>[];\n  getRowId: (row: T) => RowId;\n  getRowLabel: (row: T) => string;\n  caption: string;\n  initialSort?: SortState;\n  pageSize?: number;\n  filterDelay?: number;\n};\n```\n\nThe roles are deliberately separate:\n\n- `T` is the caller\'s record type, such as `Employee`, `Product`, or `Invoice`.\n- `ColumnDef\u003cT>` contains domain-specific accessors and cell rendering.\n- `DataTableProps\u003cT>` contains generic table configuration.\n- `getRowId` avoids assuming that every record has an `id` property.\n- `getRowLabel` supplies an accessible name for its selection checkbox.\n- `SortValue` limits sorting to values the shared comparator understands.\n\nFor example, one caller could supply this model without changing the table:\n\n```tsx\ntype Product = {\n  sku: string;\n  title: string;\n  price: number;\n  available: boolean;\n};\n```\n\n## State model and invariants\n\nKeep only user decisions in state:\n\n```tsx\nconst [query, setQuery] = useState("");\nconst [sort, setSort] = useState\u003cSortState | null>(initialSort ?? null);\nconst [selectedIds, setSelectedIds] = useState\u003cSet\u003cRowId>>(() => new Set());\nconst [page, setPage] = useState(1);\n```\n\nDo not store `filteredRows`, `sortedRows`, or `pageRows`. They are derivations of current props and state:\n\n```text\nrows + query\n      ↓\nfiltered rows + sort\n      ↓\nsorted rows + page\n      ↓\nvisible rows\n```\n\nImportant invariants:\n\n- input `rows` are never mutated;\n- selection is identified by `getRowId(row)`, not by a hard-coded field or array position;\n- page is always between `1` and `pageCount`;\n- the header checkbox describes only visible rows;\n- `aria-sort` exists only on the active sortable header.\n\n## Component and helper design\n\nFor interview scope, one component plus pure helpers is enough:\n\n```text\nDataTable\n├── filtering and sorting derivations\n├── selection transitions\n├── semantic table markup\n└── pagination controls\n```\n\nThe shared comparator supports the `SortValue` union from the type contract:\n\n```tsx\nfunction compareValues(left: SortValue, right: SortValue) {\n  if (left instanceof Date && right instanceof Date) {\n    return left.getTime() - right.getTime();\n  }\n\n  if (typeof left === "number" && typeof right === "number") {\n    return left - right;\n  }\n\n  return String(left).localeCompare(String(right), undefined, {\n    sensitivity: "base",\n    numeric: true,\n  });\n}\n```\n\nThe caller owns domain knowledge—how to display a value, which value should be compared, and which text should be searched. The table owns only generic transformations, selection, pagination, and semantics.\n\n## Step-by-step React solution\n\n### Step 1: render a semantic table\n\nStart with the generic contract and correct HTML before adding state:\n\n```tsx\nfunction DataTable\u003cT>({\n  rows,\n  columns,\n  getRowId,\n  caption,\n}: DataTableProps\u003cT>) {\n  return (\n    \u003ctable>\n      \u003ccaption>{caption}\u003c/caption>\n      \u003cthead>\n        \u003ctr>\n          {columns.map(column => (\n            \u003cth scope="col" key={column.id}>{column.header}\u003c/th>\n          ))}\n        \u003c/tr>\n      \u003c/thead>\n      \u003ctbody>\n        {rows.map(row => (\n          \u003ctr key={getRowId(row)}>\n            {columns.map(column =>\n              column.rowHeader ? (\n                \u003cth scope="row" key={column.id}>{column.cell(row)}\u003c/th>\n              ) : (\n                \u003ctd key={column.id}>{column.cell(row)}\u003c/td>\n              ),\n            )}\n          \u003c/tr>\n        ))}\n      \u003c/tbody>\n    \u003c/table>\n  );\n}\n```\n\nUse stable IDs for keys. A table already gives assistive technologies relationships between cells and headers; replacing it with a grid of `\u003cdiv>` elements would require rebuilding those semantics.\n\n### Step 2: add sorting without mutating props\n\nPut a real button inside each sortable header. The button provides keyboard activation without custom handlers:\n\n```tsx\nconst [sort, setSort] = useState\u003cSortState | null>(initialSort ?? null);\n\nfunction requestSort(columnId: string) {\n  setSort(current => ({\n    columnId,\n    direction:\n      current?.columnId === columnId && current.direction === "ascending"\n        ? "descending"\n        : "ascending",\n  }));\n}\n```\n\nDerive a copied, sorted array:\n\n```tsx\nconst sortColumn = sort\n  ? columns.find(column => column.id === sort.columnId)\n  : undefined;\n\nconst sortedRows =\n  sort && sortColumn?.sortValue\n    ? [...rows].sort((left, right) => {\n        const result = compareValues(\n          sortColumn.sortValue!(left),\n          sortColumn.sortValue!(right),\n        );\n        return sort.direction === "ascending" ? result : -result;\n      })\n    : [...rows];\n```\n\nNever call `rows.sort(...)`: `sort` mutates the array supplied by the parent.\n\nExpose the active direction through `aria-sort` on the `\u003cth>`:\n\n```tsx\n\u003cth scope="col" aria-sort={sort?.columnId === column.id ? sort.direction : undefined}>\n  \u003cbutton type="button" onClick={() => requestSort(column.id)}>\n    {column.header}\n  \u003c/button>\n\u003c/th>\n```\n\n### Step 3: debounce the filter query, then derive rows\n\nKeep `query` immediate so the controlled input never lags. A reusable Hook produces a delayed value for expensive filtering:\n\n```tsx\nfunction useDebounce\u003cT>(value: T, delay: number): T {\n  const [debouncedValue, setDebouncedValue] = useState(value);\n\n  useEffect(() => {\n    const timer = window.setTimeout(() => {\n      setDebouncedValue(value);\n    }, delay);\n\n    return () => window.clearTimeout(timer);\n  }, [value, delay]);\n\n  return debouncedValue;\n}\n```\n\nDebounce only the value that triggers filtering—not the state bound to the input:\n\n```tsx\nconst [query, setQuery] = useState("");\nconst debouncedQuery = useDebounce(query, 200);\nconst normalizedQuery = debouncedQuery.trim().toLocaleLowerCase();\n\nconst filteredRows = rows.filter(row => {\n  if (!normalizedQuery) return true;\n\n  return columns.some(column => {\n    const value = column.searchValue?.(row);\n    return value?.toLocaleLowerCase().includes(normalizedQuery) ?? false;\n  });\n});\n\nconst sortedRows = [...filteredRows].sort(/* comparator */);\n```\n\n```tsx\n\u003clabel htmlFor="table-filter">Filter rows\u003c/label>\n\u003cinput\n  id="table-filter"\n  type="search"\n  value={query}\n  onChange={event => {\n    setQuery(event.target.value);\n    setPage(1);\n  }}\n/>\n```\n\nResetting the page belongs in the filter event because the event changes the result set. No Effect is needed to synchronize page with query.\n\nFor a small local collection, filtering on every keystroke is usually fast enough and debounce may be unnecessary. It is included here because interview datasets can be large and the same state model transfers to remote filtering. In production, choose the delay from measured behavior rather than treating `200` milliseconds as universal.\n\n### Step 4: paginate the derived result\n\nCalculate the page range after filtering and sorting:\n\n```tsx\nconst pageCount = Math.max(1, Math.ceil(sortedRows.length / pageSize));\nconst currentPage = Math.min(page, pageCount);\nconst startIndex = (currentPage - 1) * pageSize;\nconst pageRows = sortedRows.slice(startIndex, startIndex + pageSize);\n```\n\nUse `currentPage` rather than setting state during render. It keeps the rendered page valid if the parent replaces `rows` with a smaller collection.\n\n```tsx\n\u003cbutton type="button" disabled={currentPage === 1} onClick={() => setPage(value => value - 1)}>\n  Previous\n\u003c/button>\n\u003cspan>Page {currentPage} of {pageCount}\u003c/span>\n\u003cbutton type="button" disabled={currentPage === pageCount} onClick={() => setPage(value => value + 1)}>\n  Next\n\u003c/button>\n```\n\n### Step 5: add row selection using stable IDs\n\nNever mutate the existing `Set`; return a new one so React receives a new state identity:\n\n```tsx\nfunction toggleRow(id: RowId) {\n  setSelectedIds(current => {\n    const next = new Set(current);\n    next.has(id) ? next.delete(id) : next.add(id);\n    return next;\n  });\n}\n```\n\nDerive header checkbox state from the current page:\n\n```tsx\nconst pageIds = pageRows.map(getRowId);\nconst allPageRowsSelected =\n  pageIds.length > 0 && pageIds.every(id => selectedIds.has(id));\nconst somePageRowsSelected =\n  pageIds.some(id => selectedIds.has(id)) && !allPageRowsSelected;\n```\n\nHTML checkboxes have an `indeterminate` DOM property rather than an attribute, so synchronize it through a ref:\n\n```tsx\nconst selectAllRef = useRef\u003cHTMLInputElement>(null);\n\nuseEffect(() => {\n  if (selectAllRef.current) {\n    selectAllRef.current.indeterminate = somePageRowsSelected;\n  }\n}, [somePageRowsSelected]);\n```\n\nThis is a valid Effect: it synchronizes React state with a DOM property that JSX cannot express declaratively.\n\n### Step 6: finish empty states and announcements\n\nKeep the table structure when there are no matching rows:\n\n```tsx\n\u003ctbody>\n  {pageRows.length ? rowsMarkup : (\n    \u003ctr>\n      \u003ctd colSpan={columns.length + 1}>No rows match “{query}”.\u003c/td>\n    \u003c/tr>\n  )}\n\u003c/tbody>\n```\n\nProvide concise status text outside the table:\n\n```tsx\n\u003cp role="status" aria-live="polite">\n  {sortedRows.length} rows. {selectedIds.size} selected.\n\u003c/p>\n```\n\nDo not announce every visual detail. Native button, checkbox, table, and `aria-sort` semantics already communicate most behavior.\n\n## Complete interview-sized implementation\n\nThe component below knows nothing about employees, products, invoices, or any other domain. All domain behavior enters through typed props.\n\n```tsx\nimport {\n  useEffect,\n  useId,\n  useRef,\n  useState,\n  type ReactNode,\n} from "react";\n\ntype RowId = string | number;\ntype SortValue = string | number | Date;\ntype SortDirection = "ascending" | "descending";\ntype SortState = { columnId: string; direction: SortDirection };\n\nexport type ColumnDef\u003cT> = {\n  id: string;\n  header: string;\n  cell: (row: T) => ReactNode;\n  sortValue?: (row: T) => SortValue;\n  searchValue?: (row: T) => string;\n  rowHeader?: boolean;\n};\n\ntype DataTableProps\u003cT> = {\n  rows: readonly T[];\n  columns: readonly ColumnDef\u003cT>[];\n  getRowId: (row: T) => RowId;\n  getRowLabel: (row: T) => string;\n  caption: string;\n  initialSort?: SortState;\n  pageSize?: number;\n  filterDelay?: number;\n};\n\nfunction compareValues(left: SortValue, right: SortValue) {\n  if (left instanceof Date && right instanceof Date) {\n    return left.getTime() - right.getTime();\n  }\n\n  if (typeof left === "number" && typeof right === "number") {\n    return left - right;\n  }\n\n  return String(left).localeCompare(String(right), undefined, {\n    sensitivity: "base",\n    numeric: true,\n  });\n}\n\nfunction useDebounce\u003cT>(value: T, delay: number): T {\n  const [debouncedValue, setDebouncedValue] = useState(value);\n\n  useEffect(() => {\n    const timer = window.setTimeout(() => {\n      setDebouncedValue(value);\n    }, delay);\n\n    return () => window.clearTimeout(timer);\n  }, [value, delay]);\n\n  return debouncedValue;\n}\n\nexport function DataTable\u003cT>({\n  rows,\n  columns,\n  getRowId,\n  getRowLabel,\n  caption,\n  initialSort,\n  pageSize = 5,\n  filterDelay = 200,\n}: DataTableProps\u003cT>) {\n  const filterId = useId();\n  const selectAllRef = useRef\u003cHTMLInputElement>(null);\n  const [query, setQuery] = useState("");\n  const [sort, setSort] = useState\u003cSortState | null>(initialSort ?? null);\n  const [selectedIds, setSelectedIds] = useState\u003cSet\u003cRowId>>(\n    () => new Set(),\n  );\n  const [page, setPage] = useState(1);\n\n  const debouncedQuery = useDebounce(query, filterDelay);\n  const normalizedQuery = debouncedQuery.trim().toLocaleLowerCase();\n  const isFiltering = query !== debouncedQuery;\n  const filteredRows = rows.filter(row => {\n    if (!normalizedQuery) return true;\n\n    return columns.some(column => {\n      const value = column.searchValue?.(row);\n      return value?.toLocaleLowerCase().includes(normalizedQuery) ?? false;\n    });\n  });\n\n  const sortColumn = sort\n    ? columns.find(column => column.id === sort.columnId)\n    : undefined;\n\n  const sortedRows =\n    sort && sortColumn?.sortValue\n      ? [...filteredRows].sort((left, right) => {\n          const result = compareValues(\n            sortColumn.sortValue!(left),\n            sortColumn.sortValue!(right),\n          );\n          return sort.direction === "ascending" ? result : -result;\n        })\n      : [...filteredRows];\n\n  const pageCount = Math.max(1, Math.ceil(sortedRows.length / pageSize));\n  const currentPage = Math.min(page, pageCount);\n  const startIndex = (currentPage - 1) * pageSize;\n  const pageRows = sortedRows.slice(startIndex, startIndex + pageSize);\n  const pageIds = pageRows.map(getRowId);\n  const allPageRowsSelected =\n    pageIds.length > 0 && pageIds.every(id => selectedIds.has(id));\n  const somePageRowsSelected =\n    pageIds.some(id => selectedIds.has(id)) && !allPageRowsSelected;\n\n  useEffect(() => {\n    if (selectAllRef.current) {\n      selectAllRef.current.indeterminate = somePageRowsSelected;\n    }\n  }, [somePageRowsSelected]);\n\n  function requestSort(columnId: string) {\n    setSort(current => ({\n      columnId,\n      direction:\n        current?.columnId === columnId &&\n        current.direction === "ascending"\n          ? "descending"\n          : "ascending",\n    }));\n    setPage(1);\n  }\n\n  function toggleRow(id: RowId) {\n    setSelectedIds(current => {\n      const next = new Set(current);\n      next.has(id) ? next.delete(id) : next.add(id);\n      return next;\n    });\n  }\n\n  function togglePage() {\n    setSelectedIds(current => {\n      const next = new Set(current);\n      for (const id of pageIds) {\n        allPageRowsSelected ? next.delete(id) : next.add(id);\n      }\n      return next;\n    });\n  }\n\n  return (\n    \u003csection aria-labelledby={`\\${filterId}-title`}>\n      \u003ch2 id={`\\${filterId}-title`}>{caption}\u003c/h2>\n\n      \u003clabel htmlFor={filterId}>Filter rows\u003c/label>\n      \u003cinput\n        id={filterId}\n        type="search"\n        value={query}\n        onChange={event => {\n          setQuery(event.target.value);\n          setPage(1);\n        }}\n      />\n\n      \u003cp role="status" aria-live="polite">\n        {isFiltering\n          ? "Filtering rows…"\n          : `${sortedRows.length} rows. ${selectedIds.size} selected.`}\n      \u003c/p>\n\n      \u003ctable>\n        \u003ccaption>{caption}\u003c/caption>\n        \u003cthead>\n          \u003ctr>\n            \u003cth scope="col">\n              \u003cinput\n                ref={selectAllRef}\n                type="checkbox"\n                checked={allPageRowsSelected}\n                disabled={pageRows.length === 0}\n                onChange={togglePage}\n                aria-label="Select all rows on this page"\n              />\n            \u003c/th>\n            {columns.map(column => {\n              const active = sort?.columnId === column.id;\n              return (\n                \u003cth\n                  scope="col"\n                  key={column.id}\n                  aria-sort={\n                    active && column.sortValue\n                      ? sort.direction\n                      : undefined\n                  }\n                >\n                  {column.sortValue ? (\n                    \u003cbutton\n                      type="button"\n                      onClick={() => requestSort(column.id)}\n                    >\n                      {column.header}\n                      \u003cspan aria-hidden="true">\n                        {active\n                          ? sort.direction === "ascending"\n                            ? " ↑"\n                            : " ↓"\n                          : " ↕"}\n                      \u003c/span>\n                    \u003c/button>\n                  ) : (\n                    column.header\n                  )}\n                \u003c/th>\n              );\n            })}\n          \u003c/tr>\n        \u003c/thead>\n        \u003ctbody>\n          {pageRows.length > 0 ? (\n            pageRows.map(row => {\n              const rowId = getRowId(row);\n              return (\n                \u003ctr key={rowId}>\n                  \u003ctd>\n                    \u003cinput\n                      type="checkbox"\n                      checked={selectedIds.has(rowId)}\n                      onChange={() => toggleRow(rowId)}\n                      aria-label={`Select \\${getRowLabel(row)}`}\n                    />\n                  \u003c/td>\n                  {columns.map(column =>\n                    column.rowHeader ? (\n                      \u003cth scope="row" key={column.id}>\n                        {column.cell(row)}\n                      \u003c/th>\n                    ) : (\n                      \u003ctd key={column.id}>{column.cell(row)}\u003c/td>\n                    ),\n                  )}\n                \u003c/tr>\n              );\n            })\n          ) : (\n            \u003ctr>\n              \u003ctd colSpan={columns.length + 1}>\n                No rows match “{query}”.\n              \u003c/td>\n            \u003c/tr>\n          )}\n        \u003c/tbody>\n      \u003c/table>\n\n      \u003cnav aria-label={`\\${caption} pages`}>\n        \u003cbutton\n          type="button"\n          disabled={currentPage === 1}\n          onClick={() => setPage(value => Math.max(1, value - 1))}\n        >\n          Previous\n        \u003c/button>\n        \u003cspan>Page {currentPage} of {pageCount}\u003c/span>\n        \u003cbutton\n          type="button"\n          disabled={currentPage === pageCount}\n          onClick={() =>\n            setPage(value => Math.min(pageCount, value + 1))\n          }\n        >\n          Next\n        \u003c/button>\n      \u003c/nav>\n    \u003c/section>\n  );\n}\n```\n\nHere is one employee configuration. A product or invoice table supplies a different row type and column array without changing `DataTable`:\n\n```tsx\ntype Employee = {\n  id: string;\n  name: string;\n  role: string;\n  startedAt: string;\n};\n\nconst employeeColumns = [\n  {\n    id: "name",\n    header: "Name",\n    cell: (employee: Employee) => employee.name,\n    sortValue: (employee: Employee) => employee.name,\n    searchValue: (employee: Employee) => employee.name,\n    rowHeader: true,\n  },\n  {\n    id: "role",\n    header: "Role",\n    cell: (employee: Employee) => employee.role,\n    sortValue: (employee: Employee) => employee.role,\n    searchValue: (employee: Employee) => employee.role,\n  },\n  {\n    id: "startedAt",\n    header: "Start date",\n    cell: (employee: Employee) => (\n      \u003ctime dateTime={employee.startedAt}>\n        {new Intl.DateTimeFormat(undefined, {\n          dateStyle: "medium",\n        }).format(new Date(employee.startedAt))}\n      \u003c/time>\n    ),\n    sortValue: (employee: Employee) => new Date(employee.startedAt),\n  },\n] satisfies readonly ColumnDef\u003cEmployee>[];\n\n\u003cDataTable\n  rows={employees}\n  columns={employeeColumns}\n  getRowId={employee => employee.id}\n  getRowLabel={employee => employee.name}\n  caption="Employee directory"\n  initialSort={{ columnId: "name", direction: "ascending" }}\n/>;\n```\n\n## Why the solution is structured this way\n\n### Transformation order is explicit\n\nThe component filters, then sorts, then paginates. Changing that order changes behavior: paginating before filtering would search only the current slice, while paginating before sorting would sort each page independently.\n\n### Derived collections are not state\n\nEvery displayed collection follows from `rows`, `query`, `sort`, and `page`. Storing intermediate arrays would add synchronization paths and stale-data bugs.\n\n### Selection is independent of visibility\n\nThe `Set` holds stable IDs across pages and filters. The header checkbox operates only on `pageRows`, matching the stated requirement without erasing hidden selections.\n\n### The only Effect synchronizes a DOM property\n\nFiltering, sorting, and pagination require no Effects. The indeterminate checkbox is different: its DOM property cannot be represented as a normal JSX attribute.\n\n## Accessibility and keyboard behavior\n\n- Preserve native table markup and header scopes.\n- Put sorting interactions in `\u003cbutton>` elements inside headers.\n- Set `aria-sort="ascending"` or `"descending"` only on the active header.\n- Give every checkbox an accessible name.\n- Use native disabled pagination buttons.\n- Keep a caption, even if it is visually hidden in the finished design.\n- Announce result and selection summaries without making every row update noisy.\n- Do not add `role="grid"` unless implementing grid-specific focus and keyboard behavior.\n\n## Performance and scale\n\nFor dozens or hundreds of local rows, direct derivation is normally adequate. If profiling finds expensive transformations, memoize the complete pipeline based on `rows`, `query`, and `sort` rather than storing its output.\n\nFor large datasets:\n\n- move filtering, sorting, and pagination to the server;\n- encode sort, filters, and page or cursor in the request and possibly URL;\n- use request cancellation and cache keys that include every parameter;\n- define whether select-all means loaded rows, filtered rows, or the entire server result;\n- virtualize only when DOM size is the demonstrated bottleneck;\n- consider `useDeferredValue` for expensive local filtering while keeping input urgent.\n- debounce remote filters to avoid starting a request for every keystroke.\n\n## Testing strategy\n\nEssential integration tests:\n\n1. The configured initial sort is applied through the column\'s `sortValue`.\n2. Clicking a sortable header toggles direction and updates `aria-sort`.\n3. Clicking another header activates its ascending sort.\n4. Filtering waits for the configured debounce delay, searches only columns with `searchValue`, and resets to page one.\n5. Empty filtering retains the table and shows the empty message.\n6. Pagination displays the correct slice and disables boundary buttons.\n7. Row selection survives page and filter changes.\n8. Select-all selects and clears only visible rows.\n9. The header checkbox becomes indeterminate for partial page selection.\n10. Sort buttons and checkboxes work using keyboard interaction.\n\nUse fake timers for the debounce test: type a query, confirm the input updates immediately, advance by `filterDelay`, and then assert the rows change.\n\nUnit-test `compareValues` and domain-specific column accessors separately when null handling, locale rules, or custom ordering becomes complex.\n\n## Common candidate mistakes\n\n### Mutating the input array\n\n`rows.sort()` changes parent-owned data. Sort a copy with `[...rows]` or `toSorted()` when supported by the target environment.\n\n### Synchronizing derived arrays through Effects\n\nThis adds extra renders and permits intermediate stale UI. Calculate the pipeline during render.\n\n### Using array indices as row identity\n\nSorting and filtering change positions. Selection and local row state must follow the record ID.\n\n### Making the entire header clickable\n\nA `\u003cth onClick>` is not automatically keyboard operable. Use a button inside the header.\n\n### Ambiguous select-all behavior\n\nState the selection scope before implementation. “All” may mean page, filtered result, loaded records, or every server record.\n\n### Overengineering the generic abstraction\n\nThe useful generic boundary is small: identity plus column accessors and renderers. Avoid recreating a full table library with plugins, nested headers, and dozens of options during the interview.\n\n## Senior-level improvements\n\n- Add reusable column factories for repeated domain patterns such as currency, dates, and status badges.\n- Synchronize filters, sorting, and page with URL search parameters.\n- Add server-state caching and cancellation for remote transformations.\n- Support explicit null ordering and locale-sensitive collators.\n- Add column visibility and persisted user preferences.\n- Use cursor pagination when offset pagination becomes unstable or expensive.\n- Model whole-result selection as “all matching except excluded IDs” rather than enumerating unloaded IDs.\n- Add virtualization with an accessibility and focus strategy.\n\n## A 60-second solution explanation\n\nThe table is generic over `T`: callers provide columns plus functions for row identity and accessible labels, so the component contains no domain field names. Each column independently defines rendering, sorting, filtering, and row-header behavior. The controlled input updates immediately while `useDebounce` delays only the query used by the filter. I store only user decisions—query, sort descriptor, selected IDs, and page—and derive rows through filter, sort, and pagination. Selection uses stable caller-provided IDs, native table semantics remain intact, and the other Effect synchronizes the checkbox’s indeterminate DOM property. At server scale I would make these controls report debounced intent to a cached remote query instead of transforming all rows locally.\n\n## Likely interview follow-ups\n\n### Why not put filtered rows in state?\n\nThey contain no independent information. Storing them duplicates `rows` and `query`, creates synchronization work, and can display stale results.\n\n### When would you use `useMemo`?\n\nAfter profiling shows that repeated filtering or sorting is meaningful and inputs often remain unchanged. It is an optimization, not a source of truth.\n\n### Why debounce instead of `useDeferredValue`?\n\nDebounce waits for a quiet period and reduces how often filtering or a remote request starts. `useDeferredValue` allows React to begin rendering a lagging version with lower priority, so it improves scheduling but does not guarantee fewer calculations or requests. For remote search, debounce is usually the relevant first tool; for expensive local rendering, deferral may provide a more responsive experience.\n\n### How would server-side sorting change the component?\n\nThe sort descriptor becomes part of the resource key. A parent, route, or data Hook owns requests, cancellation, caching, loading, and errors; the table reports sort intent and renders the supplied page.\n\n### How would you support all filtered rows across server pages?\n\nRepresent selection as an all-matching mode plus excluded IDs, tied to the active filter definition. The server action must interpret the same filter snapshot.\n\n### Should this use `role="grid"`?\n\nNot for ordinary sorting, checkboxes, and links. A grid role implies a more complex composite-widget keyboard model. Native table semantics are preferable unless spreadsheet-like cell navigation is required.\n\n## Article summary\n\n- Clarify transformation and selection semantics before coding.\n- Keep domain knowledge in typed column definitions and caller-provided row identity.\n- Store interaction choices; derive displayed collections.\n- Keep the input immediate and debounce only the value that triggers filtering.\n- Filter, sort, and paginate in an explicit order.\n- Never mutate input arrays.\n- Identify rows by stable IDs.\n- Preserve native table semantics and use buttons for sorting.\n- Treat local and server-scale tables as different architectures.\n- Measure before memoizing or virtualizing.\n\n## Further reading\n\n- [Derived and redundant state](/react/handbook/chapters/09-derived-and-redundant-state)\n- [State ownership and lifting state](/react/handbook/chapters/17-state-ownership-and-lifting-state)\n- [Accessible interactive components](/react/handbook/chapters/24-accessible-interactive-components)\n- [List virtualization](/react/handbook/chapters/30-list-virtualization)\n- [Choosing state-management tools](/react/handbook/chapters/40-choosing-state-management-tools)',
    headings: [
      "The interview prompt",
      "What the interviewer is evaluating",
      "Clarifying questions to ask",
      "Requirements and deliberate non-requirements",
      "Type contract",
      "State model and invariants",
      "Component and helper design",
      "Step-by-step React solution",
      "Complete interview-sized implementation",
      "Why the solution is structured this way",
      "Accessibility and keyboard behavior",
      "Performance and scale",
      "Testing strategy",
      "Common candidate mistakes",
      "Senior-level improvements",
      "A 60-second solution explanation",
      "Likely interview follow-ups",
      "Article summary",
      "Further reading",
    ],
    quickRefresher: "",
    excerpt:
      "Build a reusable, typed data table that can display any record shape. The caller supplies rows, stable row identity, and column definitions. Users must be able to:",
    readingMinutes: 10,
    searchText:
      'sortable data table the interview prompt build a reusable, typed data table that can display any record shape. the caller supplies rows, stable row identity, and column definitions. users must be able to: sort columns that opt into sorting; filter rows with a text query; select individual rows and select all visible rows; move between pages; use the table with a keyboard and assistive technology. keep the implementation small enough for a 45–60 minute interview and explain how it would evolve for remote data or thousands of rows. what the interviewer is evaluating the task is less about drawing rows than modeling transformations correctly. a strong solution demonstrates: one source of truth for input data and interaction state; derived filtering, sorting, and pagination rather than synchronized copies; immutable selection based on stable row ids; a deterministic comparator that does not mutate props; correct native table and sort button semantics; clear empty, selection, and pagination behavior; awareness of performance and server side alternatives. clarifying questions to ask before coding, establish the contract: 1. which columns are sortable, and what is their default direction? 2. does a repeated header click toggle ascending and descending, or also return to unsorted? 3. which fields does filtering search, and is it case sensitive? 4. does “select all” mean all rows in the dataset, all filtered rows, or only the current page? 5. should selection survive filtering and page changes? 6. is pagination local or server driven? 7. how should missing values sort? 8. is a single column sort sufficient? for this walkthrough: sorting uses one column and toggles ascending/descending; filtering searches columns that opt into filtering; select all affects the current page; selection survives filtering and pagination; pagination and all transformations are local; page size is five rows. requirements and deliberate non requirements required in the interview version semantic , , , header cells, and captions. configurable rendering plus opt in sorting and filtering per column. text filtering. stable row selection and select all visible behavior. local pagination whose page remains valid after filtering. empty results and a concise result summary. explain, but do not necessarily finish server side filtering, sorting, and cursor pagination. column resizing, reordering, and visibility. multi column sorting. virtualization for very large collections. url synchronization. persisted selection across unloaded server pages. type contract define the reusable boundary before writing component logic. every later snippet uses these types: the roles are deliberately separate: t is the caller\'s record type, such as employee , product , or invoice . columndef contains domain specific accessors and cell rendering. datatableprops contains generic table configuration. getrowid avoids assuming that every record has an id property. getrowlabel supplies an accessible name for its selection checkbox. sortvalue limits sorting to values the shared comparator understands. for example, one caller could supply this model without changing the table: state model and invariants keep only user decisions in state: do not store filteredrows , sortedrows , or pagerows . they are derivations of current props and state: important invariants: input rows are never mutated; selection is identified by getrowid(row) , not by a hard coded field or array position; page is always between 1 and pagecount ; the header checkbox describes only visible rows; aria sort exists only on the active sortable header. component and helper design for interview scope, one component plus pure helpers is enough: the shared comparator supports the sortvalue union from the type contract: the caller owns domain knowledge—how to display a value, which value should be compared, and which text should be searched. the table owns only generic transformations, selection, pagination, and semantics. step by step react solution step 1: render a semantic table start with the generic contract and correct html before adding state: use stable ids for keys. a table already gives assistive technologies relationships between cells and headers; replacing it with a grid of elements would require rebuilding those semantics. step 2: add sorting without mutating props put a real button inside each sortable header. the button provides keyboard activation without custom handlers: derive a copied, sorted array: never call rows.sort(...) : sort mutates the array supplied by the parent. expose the active direction through aria sort on the : step 3: debounce the filter query, then derive rows keep query immediate so the controlled input never lags. a reusable hook produces a delayed value for expensive filtering: debounce only the value that triggers filtering—not the state bound to the input: resetting the page belongs in the filter event because the event changes the result set. no effect is needed to synchronize page with query. for a small local collection, filtering on every keystroke is usually fast enough and debounce may be unnecessary. it is included here because interview datasets can be large and the same state model transfers to remote filtering. in production, choose the delay from measured behavior rather than treating 200 milliseconds as universal. step 4: paginate the derived result calculate the page range after filtering and sorting: use currentpage rather than setting state during render. it keeps the rendered page valid if the parent replaces rows with a smaller collection. step 5: add row selection using stable ids never mutate the existing set ; return a new one so react receives a new state identity: derive header checkbox state from the current page: html checkboxes have an indeterminate dom property rather than an attribute, so synchronize it through a ref: this is a valid effect: it synchronizes react state with a dom property that jsx cannot express declaratively. step 6: finish empty states and announcements keep the table structure when there are no matching rows: provide concise status text outside the table: do not announce every visual detail. native button, checkbox, table, and aria sort semantics already communicate most behavior. complete interview sized implementation the component below knows nothing about employees, products, invoices, or any other domain. all domain behavior enters through typed props. here is one employee configuration. a product or invoice table supplies a different row type and column array without changing datatable : why the solution is structured this way transformation order is explicit the component filters, then sorts, then paginates. changing that order changes behavior: paginating before filtering would search only the current slice, while paginating before sorting would sort each page independently. derived collections are not state every displayed collection follows from rows , query , sort , and page . storing intermediate arrays would add synchronization paths and stale data bugs. selection is independent of visibility the set holds stable ids across pages and filters. the header checkbox operates only on pagerows , matching the stated requirement without erasing hidden selections. the only effect synchronizes a dom property filtering, sorting, and pagination require no effects. the indeterminate checkbox is different: its dom property cannot be represented as a normal jsx attribute. accessibility and keyboard behavior preserve native table markup and header scopes. put sorting interactions in elements inside headers. set aria sort="ascending" or "descending" only on the active header. give every checkbox an accessible name. use native disabled pagination buttons. keep a caption, even if it is visually hidden in the finished design. announce result and selection summaries without making every row update noisy. do not add role="grid" unless implementing grid specific focus and keyboard behavior. performance and scale for dozens or hundreds of local rows, direct derivation is normally adequate. if profiling finds expensive transformations, memoize the complete pipeline based on rows , query , and sort rather than storing its output. for large datasets: move filtering, sorting, and pagination to the server; encode sort, filters, and page or cursor in the request and possibly url; use request cancellation and cache keys that include every parameter; define whether select all means loaded rows, filtered rows, or the entire server result; virtualize only when dom size is the demonstrated bottleneck; consider usedeferredvalue for expensive local filtering while keeping input urgent. debounce remote filters to avoid starting a request for every keystroke. testing strategy essential integration tests: 1. the configured initial sort is applied through the column\'s sortvalue . 2. clicking a sortable header toggles direction and updates aria sort . 3. clicking another header activates its ascending sort. 4. filtering waits for the configured debounce delay, searches only columns with searchvalue , and resets to page one. 5. empty filtering retains the table and shows the empty message. 6. pagination displays the correct slice and disables boundary buttons. 7. row selection survives page and filter changes. 8. select all selects and clears only visible rows. 9. the header checkbox becomes indeterminate for partial page selection. 10. sort buttons and checkboxes work using keyboard interaction. use fake timers for the debounce test: type a query, confirm the input updates immediately, advance by filterdelay , and then assert the rows change. unit test comparevalues and domain specific column accessors separately when null handling, locale rules, or custom ordering becomes complex. common candidate mistakes mutating the input array rows.sort() changes parent owned data. sort a copy with [...rows] or tosorted() when supported by the target environment. synchronizing derived arrays through effects this adds extra renders and permits intermediate stale ui. calculate the pipeline during render. using array indices as row identity sorting and filtering change positions. selection and local row state must follow the record id. making the entire header clickable a is not automatically keyboard operable. use a button inside the header. ambiguous select all behavior state the selection scope before implementation. “all” may mean page, filtered result, loaded records, or every server record. overengineering the generic abstraction the useful generic boundary is small: identity plus column accessors and renderers. avoid recreating a full table library with plugins, nested headers, and dozens of options during the interview. senior level improvements add reusable column factories for repeated domain patterns such as currency, dates, and status badges. synchronize filters, sorting, and page with url search parameters. add server state caching and cancellation for remote transformations. support explicit null ordering and locale sensitive collators. add column visibility and persisted user preferences. use cursor pagination when offset pagination becomes unstable or expensive. model whole result selection as “all matching except excluded ids” rather than enumerating unloaded ids. add virtualization with an accessibility and focus strategy. a 60 second solution explanation the table is generic over t : callers provide columns plus functions for row identity and accessible labels, so the component contains no domain field names. each column independently defines rendering, sorting, filtering, and row header behavior. the controlled input updates immediately while usedebounce delays only the query used by the filter. i store only user decisions—query, sort descriptor, selected ids, and page—and derive rows through filter, sort, and pagination. selection uses stable caller provided ids, native table semantics remain intact, and the other effect synchronizes the checkbox’s indeterminate dom property. at server scale i would make these controls report debounced intent to a cached remote query instead of transforming all rows locally. likely interview follow ups why not put filtered rows in state? they contain no independent information. storing them duplicates rows and query , creates synchronization work, and can display stale results. when would you use usememo ? after profiling shows that repeated filtering or sorting is meaningful and inputs often remain unchanged. it is an optimization, not a source of truth. why debounce instead of usedeferredvalue ? debounce waits for a quiet period and reduces how often filtering or a remote request starts. usedeferredvalue allows react to begin rendering a lagging version with lower priority, so it improves scheduling but does not guarantee fewer calculations or requests. for remote search, debounce is usually the relevant first tool; for expensive local rendering, deferral may provide a more responsive experience. how would server side sorting change the component? the sort descriptor becomes part of the resource key. a parent, route, or data hook owns requests, cancellation, caching, loading, and errors; the table reports sort intent and renders the supplied page. how would you support all filtered rows across server pages? represent selection as an all matching mode plus excluded ids, tied to the active filter definition. the server action must interpret the same filter snapshot. should this use role="grid" ? not for ordinary sorting, checkboxes, and links. a grid role implies a more complex composite widget keyboard model. native table semantics are preferable unless spreadsheet like cell navigation is required. article summary clarify transformation and selection semantics before coding. keep domain knowledge in typed column definitions and caller provided row identity. store interaction choices; derive displayed collections. keep the input immediate and debounce only the value that triggers filtering. filter, sort, and paginate in an explicit order. never mutate input arrays. identify rows by stable ids. preserve native table semantics and use buttons for sorting. treat local and server scale tables as different architectures. measure before memoizing or virtualizing. further reading derived and redundant state state ownership and lifting state accessible interactive components list virtualization choosing state management tools',
  },
  {
    number: 3,
    slug: "03-accessible-modal-dialog",
    title: "Accessible Modal Dialog",
    kind: "chapter",
    partNumber: 1,
    partName: "React Live Coding",
    markdown:
      '# Accessible Modal Dialog\n\n## The interview prompt\n\nBuild a reusable modal dialog that renders above the application and accepts arbitrary content. It must open and close predictably, support keyboard users, move focus into the dialog, keep focus inside while open, and restore focus when it closes.\n\nAssume a 35–50 minute interview. Styling is secondary to interaction design, cleanup, accessible semantics, and a clear reusable API.\n\n## What the interviewer is evaluating\n\nA modal appears simple, but a correct implementation exposes several senior frontend skills:\n\n- translating “show an overlay” into an explicit interaction contract;\n- separating state ownership from dialog behavior;\n- using a portal without confusing DOM and React-tree behavior;\n- managing Escape, outside interaction, and event propagation;\n- moving, containing, and restoring focus;\n- synchronizing React with the DOM through refs and Effects;\n- making the background unavailable while the modal is active;\n- cleaning up global listeners, scroll locking, and temporary DOM nodes;\n- recognizing when a production-tested primitive is safer than custom code.\n\nA strong interview solution does not reproduce every browser and assistive-technology edge case. It implements the critical behavior, explains its invariants, and identifies what requires production hardening.\n\n## Clarifying questions to ask\n\nBefore coding, establish the expected behavior.\n\n1. Is the dialog modal, or may users interact with the background?\n2. Is `open` controlled by the parent?\n3. Should Escape always close it?\n4. Should clicking the overlay close it?\n5. What receives initial focus?\n6. Where should focus return after closing?\n7. Are nested dialogs or popovers required?\n8. Should the content remain mounted while closed?\n9. Are animations required?\n10. May we use the native `\u003cdialog>` element or a headless component library?\n\nFor this walkthrough:\n\n- the parent controls `open`;\n- Escape and the close button dismiss the dialog;\n- overlay dismissal is configurable;\n- focus moves to an explicit initial target or the first focusable element;\n- Tab and Shift+Tab remain within the dialog;\n- focus returns to the element active before opening;\n- the dialog renders in a portal;\n- background content becomes inert and body scrolling is locked;\n- nested dialogs and exit animations are production extensions.\n\n## Requirements and deliberate non-requirements\n\n### Required in the interview version\n\n- Controlled `open` and `onOpenChange` API\n- Portal rendering\n- Accessible dialog name and optional description\n- Explicit close button\n- Escape dismissal\n- Safe overlay dismissal\n- Initial focus\n- Tab and Shift+Tab containment\n- Focus restoration\n- Background inertness\n- Body scroll locking and cleanup\n\n### Explain, but do not necessarily finish\n\n- Nested modal stacks\n- Exit animations that delay unmounting\n- Shadow DOM and iframe focus\n- Mobile virtual-keyboard behavior\n- Complex scroll-lock compensation\n- Alert-dialog semantics\n- Server-rendered initially open dialogs\n- Full browser and screen-reader compatibility testing\n\nThis boundary keeps the exercise achievable while showing that a design-system dialog requires more hardening than an interview implementation.\n\n## Type contract\n\nDefine the public contract before implementing DOM behavior:\n\n```tsx\nimport type {\n  ReactNode,\n  RefObject,\n} from "react";\n\ntype ModalDialogProps = {\n  open: boolean;\n  onOpenChange: (open: boolean) => void;\n  title: string;\n  description?: string;\n  children: ReactNode;\n  initialFocusRef?: RefObject\u003cHTMLElement | null>;\n  closeOnEscape?: boolean;\n  closeOnOverlayClick?: boolean;\n  closeLabel?: string;\n};\n```\n\nThe responsibilities are explicit:\n\n- the parent owns whether the dialog is open;\n- `onOpenChange(false)` is the only dismissal request;\n- `title` always gives the dialog an accessible name;\n- `description` supplies optional supporting context;\n- `children` keeps body content composable;\n- `initialFocusRef` lets a form choose its first invalid or primary field;\n- dismissal behavior is configured without exposing internal event handlers.\n\nDo not accept an arbitrary `role` in this version. A modal dialog and an alert dialog have different interaction expectations and should not be switched accidentally by a styling prop.\n\n## State model and invariants\n\nThe dialog does not need local open state because the parent owns it:\n\n```tsx\nconst [open, setOpen] = useState(false);\n\n\u003cModalDialog\n  open={open}\n  onOpenChange={setOpen}\n  title="Edit profile"\n>\n  \u003cProfileForm />\n\u003c/ModalDialog>\n```\n\nThe component needs refs rather than additional rendering state:\n\n```tsx\nconst dialogRef = useRef\u003cHTMLDivElement>(null);\nconst previouslyFocusedRef = useRef\u003cHTMLElement | null>(null);\n```\n\nUseful invariants:\n\n1. A closed dialog renders no interactive modal surface.\n2. An open dialog has an accessible name.\n3. Focus is inside the dialog after it opens.\n4. Tab navigation cannot move into inert background content.\n5. Escape and overlay dismissal request the same controlled state change.\n6. Clicking inside the dialog never counts as an overlay click.\n7. Every global mutation is restored during cleanup.\n8. Closing restores focus to a meaningful element when it still exists.\n\nAvoid storing `isMounted`, `hasFocus`, and `isClosing` unless animation or server-rendering requirements make those states real. Most behavior is synchronized from the controlled `open` prop.\n\n## Component and Hook design\n\nKeep the interview architecture small:\n\n```text\nModalDialog\n├── usePortalContainer\n├── focus and keyboard Effect\n├── inert and scroll-lock Effect\n└── portal\n    └── overlay\n        └── dialog surface\n```\n\nExtract `usePortalContainer` because temporary DOM-node ownership has a clear setup and cleanup lifecycle. Keep the initial focus and dismissal logic in the component until a second overlay primitive needs the same behavior.\n\nDo not extract a generic `useModal` Hook merely to make the component shorter. Focus containment, labeling, portal structure, and dismissal are one interaction contract; splitting them prematurely can make it harder to verify that they stay coordinated.\n\n## Step-by-step React solution\n\nBuild one behavior at a time:\n\n```text\nControlled dialog shell\n    ↓\nPortal\n    ↓\nEscape and safe overlay dismissal\n    ↓\nInitial focus and restoration\n    ↓\nTab containment\n    ↓\nBackground inertness and scroll lock\n```\n\nEach checkpoint remains explainable if the interview ends early.\n\n### Step 1: build a controlled semantic dialog\n\nStart with state ownership and accessible structure:\n\n```tsx\nfunction ModalDialog({\n  open,\n  onOpenChange,\n  title,\n  description,\n  children,\n}: ModalDialogProps) {\n  const titleId = useId();\n  const descriptionId = useId();\n\n  if (!open) return null;\n\n  return (\n    \u003cdiv className="modal-overlay">\n      \u003cdiv\n        role="dialog"\n        aria-modal="true"\n        aria-labelledby={titleId}\n        aria-describedby={description ? descriptionId : undefined}\n        className="modal-surface"\n      >\n        \u003ch2 id={titleId}>{title}\u003c/h2>\n        {description && (\n          \u003cp id={descriptionId}>{description}\u003c/p>\n        )}\n        {children}\n        \u003cbutton type="button" onClick={() => onOpenChange(false)}>\n          Close\n        \u003c/button>\n      \u003c/div>\n    \u003c/div>\n  );\n}\n```\n\n`aria-modal="true"` communicates modal intent, but it does not make the background inert or contain focus. Those behaviors still need implementation.\n\nDo not omit a visible title merely because the visual design has an icon. If a visible heading is genuinely inappropriate, the API still needs an explicit accessible label strategy.\n\n### Step 2: render through a portal\n\nA portal lets the overlay escape ancestor clipping and stacking contexts while remaining in the same React tree.\n\nCreate and own one portal container:\n\n```tsx\nfunction usePortalContainer() {\n  const [container, setContainer] =\n    useState\u003cHTMLDivElement | null>(null);\n\n  useEffect(() => {\n    const node = document.createElement("div");\n    node.dataset.modalPortal = "";\n    document.body.append(node);\n    setContainer(node);\n\n    return () => {\n      node.remove();\n    };\n  }, []);\n\n  return container;\n}\n```\n\nRender only after the container exists:\n\n```tsx\nconst portalContainer = usePortalContainer();\n\nif (!open || !portalContainer) return null;\n\nreturn createPortal(dialogMarkup, portalContainer);\n```\n\nThe Effect avoids reading `document` during server rendering. In a production application, a shared overlay root may be preferable to creating one container per instance.\n\nPortal events follow the React tree, not only the DOM tree. A dialog click can still bubble to React ancestors of the component that returned the portal.\n\n### Step 3: add Escape and safe overlay dismissal\n\nHandle Escape only while open and remove the listener during cleanup:\n\n```tsx\nuseEffect(() => {\n  if (!open || !closeOnEscape) return;\n\n  function handleKeyDown(event: KeyboardEvent) {\n    if (event.key === "Escape") {\n      event.preventDefault();\n      onOpenChange(false);\n    }\n  }\n\n  document.addEventListener("keydown", handleKeyDown);\n  return () => document.removeEventListener("keydown", handleKeyDown);\n}, [open, closeOnEscape, onOpenChange]);\n```\n\nFor overlay dismissal, check that the overlay itself was the event target:\n\n```tsx\nfunction handleOverlayMouseDown(\n  event: React.MouseEvent\u003cHTMLDivElement>,\n) {\n  if (\n    closeOnOverlayClick &&\n    event.target === event.currentTarget\n  ) {\n    onOpenChange(false);\n  }\n}\n```\n\n```tsx\n\u003cdiv className="modal-overlay" onMouseDown={handleOverlayMouseDown}>\n  \u003cdiv className="modal-surface">...\u003c/div>\n\u003c/div>\n```\n\nDo not call `stopPropagation` on every event inside the dialog. It can break legitimate React-tree event behavior. The target equality check distinguishes the overlay from its descendants.\n\nA production primitive should also decide how pointer-down inside followed by pointer-up outside behaves and how nested overlays coordinate dismissal.\n\n### Step 4: move and restore focus\n\nWhen opening, remember the active element, then focus the requested target or the first focusable descendant:\n\n```tsx\nconst dialogRef = useRef\u003cHTMLDivElement>(null);\nconst previouslyFocusedRef = useRef\u003cHTMLElement | null>(null);\n\nuseEffect(() => {\n  if (!open || !portalContainer) return;\n\n  previouslyFocusedRef.current =\n    document.activeElement instanceof HTMLElement\n      ? document.activeElement\n      : null;\n\n  const target =\n    initialFocusRef?.current ??\n    getFocusableElements(dialogRef.current)[0] ??\n    dialogRef.current;\n\n  target?.focus();\n\n  return () => {\n    const previous = previouslyFocusedRef.current;\n    if (previous?.isConnected) previous.focus();\n  };\n}, [open, portalContainer, initialFocusRef]);\n```\n\nMake the dialog surface programmatically focusable as a fallback:\n\n```tsx\n\u003cdiv ref={dialogRef} role="dialog" tabIndex={-1}>\n```\n\nInitial focus is a product decision. A destructive confirmation should normally focus the least destructive action, while a long informational dialog may focus its heading or container.\n\n### Step 5: contain Tab navigation\n\nCollect currently focusable descendants when Tab is pressed:\n\n```tsx\nconst FOCUSABLE_SELECTOR = [\n  "a[href]",\n  "button:not([disabled])",\n  "input:not([disabled])",\n  "select:not([disabled])",\n  "textarea:not([disabled])",\n  "[tabindex]:not([tabindex=\'-1\'])",\n].join(",");\n\nfunction getFocusableElements(root: HTMLElement | null) {\n  if (!root) return [];\n\n  return Array.from(\n    root.querySelectorAll\u003cHTMLElement>(FOCUSABLE_SELECTOR),\n  ).filter(element => !element.hidden && element.getClientRects().length > 0);\n}\n```\n\nCycle from the last item to the first and from the first to the last:\n\n```tsx\nfunction containTabKey(event: KeyboardEvent) {\n  if (event.key !== "Tab") return;\n\n  const focusable = getFocusableElements(dialogRef.current);\n  if (!focusable.length) {\n    event.preventDefault();\n    dialogRef.current?.focus();\n    return;\n  }\n\n  const first = focusable[0];\n  const last = focusable[focusable.length - 1];\n\n  if (event.shiftKey && document.activeElement === first) {\n    event.preventDefault();\n    last.focus();\n  } else if (!event.shiftKey && document.activeElement === last) {\n    event.preventDefault();\n    first.focus();\n  }\n}\n```\n\nAttach this inside the same open-dialog keyboard Effect. Recalculate on each key press because form validation or conditional content can change which elements are focusable.\n\nThis selector is interview-sized, not a complete focus-management engine. Production implementations must account for radio groups, Shadow DOM, iframes, hidden ancestors, disabled fieldsets, and nested overlays.\n\n### Step 6: make the background inert and lock scrolling\n\nWhen the dialog opens, make every body child except its portal container inert and preserve the previous values:\n\n```tsx\nuseEffect(() => {\n  if (!open || !portalContainer) return;\n\n  const siblings = Array.from(document.body.children)\n    .filter(element => element !== portalContainer)\n    .filter((element): element is HTMLElement => element instanceof HTMLElement);\n\n  const previousInert = siblings.map(element => [\n    element,\n    element.inert,\n  ] as const);\n  const previousOverflow = document.body.style.overflow;\n\n  siblings.forEach(element => {\n    element.inert = true;\n  });\n  document.body.style.overflow = "hidden";\n\n  return () => {\n    previousInert.forEach(([element, inert]) => {\n      element.inert = inert;\n    });\n    document.body.style.overflow = previousOverflow;\n  };\n}, [open, portalContainer]);\n```\n\n`inert` prevents focus and interaction with the background. It does not stop React Effects, timers, requests, or subscriptions in that background subtree.\n\nSaving prior values is essential. Cleanup must restore the environment it found rather than assuming `false` and an empty overflow style. Nested modal stacks need a shared overlay manager or reference counting so one dialog does not undo another dialog’s lock.\n\n## Complete interview-sized implementation\n\n```tsx\n"use client";\n\nimport {\n  useEffect,\n  useId,\n  useRef,\n  useState,\n  type MouseEvent,\n  type ReactNode,\n  type RefObject,\n} from "react";\nimport { createPortal } from "react-dom";\n\ntype ModalDialogProps = {\n  open: boolean;\n  onOpenChange: (open: boolean) => void;\n  title: string;\n  description?: string;\n  children: ReactNode;\n  initialFocusRef?: RefObject\u003cHTMLElement | null>;\n  closeOnEscape?: boolean;\n  closeOnOverlayClick?: boolean;\n  closeLabel?: string;\n};\n\nconst FOCUSABLE_SELECTOR = [\n  "a[href]",\n  "button:not([disabled])",\n  "input:not([disabled])",\n  "select:not([disabled])",\n  "textarea:not([disabled])",\n  "[tabindex]:not([tabindex=\'-1\'])",\n].join(",");\n\nfunction getFocusableElements(root: HTMLElement | null) {\n  if (!root) return [];\n\n  return Array.from(\n    root.querySelectorAll\u003cHTMLElement>(FOCUSABLE_SELECTOR),\n  ).filter(element => !element.hidden && element.getClientRects().length > 0);\n}\n\nfunction usePortalContainer() {\n  const [container, setContainer] =\n    useState\u003cHTMLDivElement | null>(null);\n\n  useEffect(() => {\n    const node = document.createElement("div");\n    node.dataset.modalPortal = "";\n    document.body.append(node);\n    setContainer(node);\n\n    return () => node.remove();\n  }, []);\n\n  return container;\n}\n\nexport function ModalDialog({\n  open,\n  onOpenChange,\n  title,\n  description,\n  children,\n  initialFocusRef,\n  closeOnEscape = true,\n  closeOnOverlayClick = true,\n  closeLabel = "Close dialog",\n}: ModalDialogProps) {\n  const portalContainer = usePortalContainer();\n  const dialogRef = useRef\u003cHTMLDivElement>(null);\n  const previouslyFocusedRef = useRef\u003cHTMLElement | null>(null);\n  const titleId = useId();\n  const descriptionId = useId();\n\n  useEffect(() => {\n    if (!open || !portalContainer) return;\n\n    previouslyFocusedRef.current =\n      document.activeElement instanceof HTMLElement\n        ? document.activeElement\n        : null;\n\n    const target =\n      initialFocusRef?.current ??\n      getFocusableElements(dialogRef.current)[0] ??\n      dialogRef.current;\n\n    target?.focus();\n\n    function handleKeyDown(event: KeyboardEvent) {\n      if (event.key === "Escape" && closeOnEscape) {\n        event.preventDefault();\n        onOpenChange(false);\n        return;\n      }\n\n      if (event.key !== "Tab") return;\n\n      const focusable = getFocusableElements(dialogRef.current);\n      if (!focusable.length) {\n        event.preventDefault();\n        dialogRef.current?.focus();\n        return;\n      }\n\n      const first = focusable[0];\n      const last = focusable[focusable.length - 1];\n\n      if (event.shiftKey && document.activeElement === first) {\n        event.preventDefault();\n        last.focus();\n      } else if (!event.shiftKey && document.activeElement === last) {\n        event.preventDefault();\n        first.focus();\n      }\n    }\n\n    document.addEventListener("keydown", handleKeyDown);\n\n    return () => {\n      document.removeEventListener("keydown", handleKeyDown);\n\n      const previous = previouslyFocusedRef.current;\n      if (previous?.isConnected) previous.focus();\n    };\n  }, [\n    open,\n    portalContainer,\n    initialFocusRef,\n    closeOnEscape,\n    onOpenChange,\n  ]);\n\n  useEffect(() => {\n    if (!open || !portalContainer) return;\n\n    const siblings = Array.from(document.body.children)\n      .filter(element => element !== portalContainer)\n      .filter(\n        (element): element is HTMLElement =>\n          element instanceof HTMLElement,\n      );\n\n    const previousInert = siblings.map(element => [\n      element,\n      element.inert,\n    ] as const);\n    const previousOverflow = document.body.style.overflow;\n\n    siblings.forEach(element => {\n      element.inert = true;\n    });\n    document.body.style.overflow = "hidden";\n\n    return () => {\n      previousInert.forEach(([element, inert]) => {\n        element.inert = inert;\n      });\n      document.body.style.overflow = previousOverflow;\n    };\n  }, [open, portalContainer]);\n\n  if (!open || !portalContainer) return null;\n\n  function handleOverlayMouseDown(event: MouseEvent\u003cHTMLDivElement>) {\n    if (\n      closeOnOverlayClick &&\n      event.target === event.currentTarget\n    ) {\n      onOpenChange(false);\n    }\n  }\n\n  return createPortal(\n    \u003cdiv className="modal-overlay" onMouseDown={handleOverlayMouseDown}>\n      \u003cdiv\n        ref={dialogRef}\n        role="dialog"\n        aria-modal="true"\n        aria-labelledby={titleId}\n        aria-describedby={description ? descriptionId : undefined}\n        tabIndex={-1}\n        className="modal-surface"\n      >\n        \u003cheader className="modal-header">\n          \u003cdiv>\n            \u003ch2 id={titleId}>{title}\u003c/h2>\n            {description && (\n              \u003cp id={descriptionId}>{description}\u003c/p>\n            )}\n          \u003c/div>\n          \u003cbutton\n            type="button"\n            aria-label={closeLabel}\n            onClick={() => onOpenChange(false)}\n          >\n            ×\n          \u003c/button>\n        \u003c/header>\n        \u003cdiv className="modal-content">{children}\u003c/div>\n      \u003c/div>\n    \u003c/div>,\n    portalContainer,\n  );\n}\n```\n\nExample usage:\n\n```tsx\nfunction EditProfile() {\n  const [open, setOpen] = useState(false);\n  const nameRef = useRef\u003cHTMLInputElement>(null);\n\n  return (\n    \u003c>\n      \u003cbutton type="button" onClick={() => setOpen(true)}>\n        Edit profile\n      \u003c/button>\n\n      \u003cModalDialog\n        open={open}\n        onOpenChange={setOpen}\n        title="Edit profile"\n        description="Changes are visible to your team."\n        initialFocusRef={nameRef}\n      >\n        \u003cform>\n          \u003clabel htmlFor="profile-name">Display name\u003c/label>\n          \u003cinput ref={nameRef} id="profile-name" name="name" />\n          \u003cbutton type="submit">Save changes\u003c/button>\n        \u003c/form>\n      \u003c/ModalDialog>\n    \u003c/>\n  );\n}\n```\n\nMinimal styling:\n\n```css\n.modal-overlay {\n  position: fixed;\n  inset: 0;\n  z-index: 1000;\n  display: grid;\n  place-items: center;\n  padding: 1rem;\n  background: rgb(0 0 0 / 60%);\n}\n\n.modal-surface {\n  width: min(34rem, 100%);\n  max-height: min(42rem, calc(100dvh - 2rem));\n  overflow: auto;\n  border-radius: 0.75rem;\n  background: white;\n  color: #171717;\n  box-shadow: 0 1.5rem 4rem rgb(0 0 0 / 35%);\n}\n\n.modal-header {\n  display: flex;\n  align-items: start;\n  justify-content: space-between;\n  gap: 1rem;\n  padding: 1.25rem;\n}\n\n.modal-content {\n  padding: 0 1.25rem 1.25rem;\n}\n\n@media (prefers-reduced-motion: no-preference) {\n  .modal-surface {\n    animation: modal-enter 160ms ease-out;\n  }\n}\n```\n\n## Accessibility and keyboard behavior\n\nThe required interaction contract is:\n\n| Input | Expected behavior |\n|---|---|\n| Open | Focus moves to the deliberate initial target |\n| Tab | Focus moves to the next focusable dialog descendant |\n| Shift+Tab | Focus moves to the previous focusable descendant |\n| Tab at the last item | Focus wraps to the first item |\n| Shift+Tab at the first item | Focus wraps to the last item |\n| Escape | Requests close when enabled |\n| Close button | Requests close |\n| Overlay pointer action | Requests close only when configured and truly outside |\n| Close | Focus returns to the trigger or logical successor |\n\nImportant semantic rules:\n\n- the dialog has `role="dialog"` and `aria-modal="true"`;\n- `aria-labelledby` references a meaningful title;\n- `aria-describedby` is omitted when no useful description exists;\n- the close button has an accessible name;\n- background content is inert rather than merely visually obscured;\n- focus remains visible throughout the workflow.\n\nDo not put every paragraph into `aria-describedby`; screen readers may announce an overwhelming block when the dialog receives focus. Use it for concise supporting context.\n\n## Portals, events, and edge cases\n\n### React events still follow the React tree\n\nPortals change DOM placement, not React ownership. A click inside the modal can bubble to React ancestors outside the portal container. Use precise dismissal checks rather than assuming DOM separation stops propagation.\n\n### Focus can disappear during updates\n\nValidation may remove the currently focused element. After conditional updates, preserve it when possible or move focus to the replacement or error summary. A focus trap should not continually force focus back on every render.\n\n### Nested overlays require coordination\n\nTwo independent dialogs can fight over Escape, inert state, scroll locking, and focus restoration. A production overlay manager should track a stack so only the topmost modal handles dismissal and locks are reference-counted.\n\n### Exit animations require presence state\n\nImmediate `open === false` unmounting cannot display an exit animation. Production code may separate logical open state from temporary presence state. During exit, the surface should no longer accept interaction, and focus restoration must happen at a deliberate time.\n\n### Native `\u003cdialog>` is a valid alternative\n\nThe native element provides top-layer rendering, modal behavior through `showModal()`, Escape handling, and focus support. It still requires a controlled React integration, accessible naming, form behavior decisions, animation handling, and cross-browser testing. In production, compare native support with a mature headless primitive before maintaining a custom focus manager.\n\n## Testing strategy\n\nTest the public behavior rather than internal refs.\n\n### Component tests\n\n1. The dialog is absent while closed.\n2. Opening renders the title, description, and body.\n3. Initial focus moves to `initialFocusRef`.\n4. The close button requests `onOpenChange(false)`.\n5. Escape closes only when enabled.\n6. Clicking the overlay closes only when enabled.\n7. Clicking dialog content does not close it.\n8. Tab and Shift+Tab wrap at the boundaries.\n9. Focus returns to the original trigger.\n10. Unmounting restores inert and scroll styles.\n\nExample:\n\n```tsx\ntest("moves focus into the dialog and restores it", async () => {\n  render(\u003cEditProfile />);\n  const trigger = screen.getByRole("button", {\n    name: "Edit profile",\n  });\n\n  await user.click(trigger);\n  expect(screen.getByLabelText("Display name")).toHaveFocus();\n\n  await user.keyboard("{Escape}");\n  expect(trigger).toHaveFocus();\n});\n```\n\n### Browser and assistive-technology tests\n\nDOM test environments do not reproduce every focus, layout, inert, scroll, or accessibility behavior. Verify critical dialog flows in a real browser:\n\n- keyboard-only opening, traversal, dismissal, and restoration;\n- screen-reader title and description announcement;\n- zoom and mobile viewport behavior;\n- long scrolling content;\n- reduced motion and forced colors;\n- nested popovers or dialogs;\n- removal of the trigger while open.\n\n## Common candidate mistakes\n\n### Treating the overlay as the dialog\n\nThe overlay is visual and receives outside interaction. The inner surface owns dialog semantics and labeling.\n\n### Adding `role="dialog"` without focus behavior\n\nARIA does not move focus, trap Tab, inert the background, or restore the trigger.\n\n### Closing on every overlay click\n\nAn event from dialog content bubbles to the overlay. Check `event.target === event.currentTarget`.\n\n### Forgetting cleanup\n\nLeaked document listeners, `body.style.overflow = "hidden"`, or `inert` state can break the application after the dialog closes.\n\n### Focusing the first element blindly\n\nThe first element may be a destructive action. Choose initial focus from task intent.\n\n### Defining a new portal container during render\n\nDOM mutation during render is impure and can leak nodes when rendering restarts. Create and remove the node in an Effect.\n\n### Building a production focus manager in an interview\n\nImplement the common path, state the limitations, and explain when a tested primitive is the responsible production choice.\n\n## Senior-level production improvements\n\nAfter the interview version, consider:\n\n- a shared overlay stack for nested dialogs and popovers;\n- reference-counted inertness and scroll locking;\n- scrollbar compensation and iOS scroll behavior;\n- exit-presence state with reduced-motion support;\n- robust tabbable-element discovery;\n- alert-dialog and non-modal variants as separate contracts;\n- a shared portal root with server-rendering integration;\n- focus recovery when the trigger disappears;\n- analytics that do not interfere with dismissal;\n- visual-regression, browser, and assistive-technology coverage.\n\nThe strongest production improvement may be replacing the custom implementation with a maintained headless dialog primitive while preserving the public application API.\n\n## A 60-second solution explanation\n\n> The parent owns `open`, and every dismissal path calls `onOpenChange(false)`. The dialog renders through an Effect-owned portal so it escapes clipping without mutating the DOM during render. When it opens, I remember the active element, move focus to an explicit target or the first focusable child, contain Tab navigation, listen for Escape, make background siblings inert, and lock body scrolling. Cleanup removes the listener, restores inert and scroll state, and returns focus. The overlay closes only when it is the original event target, so clicks inside are safe. For production I would use a tested primitive or add an overlay stack, robust tabbable discovery, animation presence, and browser and screen-reader coverage.\n\n## Likely interview follow-ups\n\n### Why use a portal?\n\nTo escape ancestor clipping and stacking contexts while keeping Context and React event propagation connected to the original React tree.\n\n### Why not stop propagation inside the dialog?\n\nIt can break legitimate ancestor behavior. Checking whether the overlay itself was targeted solves outside dismissal more precisely.\n\n### Why is `aria-modal` insufficient?\n\nIt communicates semantics but does not implement focus movement, Tab containment, background inertness, or restoration.\n\n### When would you use `\u003cdialog>`?\n\nWhen its platform behavior and browser support match the product requirements. I would still wrap it in a controlled component and test focus, forms, animation, and assistive technology.\n\n### What changes for nested dialogs?\n\nA shared manager should track the topmost overlay, coordinate Escape and outside interaction, reference-count global locks, and restore focus through the stack.\n\n### Should overlay click always close?\n\nNo. It may be appropriate for lightweight dismissible content, but destructive, blocking, or multi-step workflows may require an explicit decision.\n\n### Why is the focus selector incomplete?\n\nReal tabbability depends on visibility, radio groups, disabled fieldsets, Shadow DOM, iframes, and browser behavior. The interview version covers common controls; production should use a well-tested focus-management utility.',
    headings: [
      "The interview prompt",
      "What the interviewer is evaluating",
      "Clarifying questions to ask",
      "Requirements and deliberate non-requirements",
      "Type contract",
      "State model and invariants",
      "Component and Hook design",
      "Step-by-step React solution",
      "Complete interview-sized implementation",
      "Accessibility and keyboard behavior",
      "Portals, events, and edge cases",
      "Testing strategy",
      "Common candidate mistakes",
      "Senior-level production improvements",
      "A 60-second solution explanation",
      "Likely interview follow-ups",
    ],
    quickRefresher: "",
    excerpt:
      "Build a reusable modal dialog that renders above the application and accepts arbitrary content. It must open and close predictably, support keyboard users, move focus into the dialog, keep focus inside while open, and restore focus when it closes.",
    readingMinutes: 11,
    searchText:
      'accessible modal dialog the interview prompt build a reusable modal dialog that renders above the application and accepts arbitrary content. it must open and close predictably, support keyboard users, move focus into the dialog, keep focus inside while open, and restore focus when it closes. assume a 35–50 minute interview. styling is secondary to interaction design, cleanup, accessible semantics, and a clear reusable api. what the interviewer is evaluating a modal appears simple, but a correct implementation exposes several senior frontend skills: translating “show an overlay” into an explicit interaction contract; separating state ownership from dialog behavior; using a portal without confusing dom and react tree behavior; managing escape, outside interaction, and event propagation; moving, containing, and restoring focus; synchronizing react with the dom through refs and effects; making the background unavailable while the modal is active; cleaning up global listeners, scroll locking, and temporary dom nodes; recognizing when a production tested primitive is safer than custom code. a strong interview solution does not reproduce every browser and assistive technology edge case. it implements the critical behavior, explains its invariants, and identifies what requires production hardening. clarifying questions to ask before coding, establish the expected behavior. 1. is the dialog modal, or may users interact with the background? 2. is open controlled by the parent? 3. should escape always close it? 4. should clicking the overlay close it? 5. what receives initial focus? 6. where should focus return after closing? 7. are nested dialogs or popovers required? 8. should the content remain mounted while closed? 9. are animations required? 10. may we use the native element or a headless component library? for this walkthrough: the parent controls open ; escape and the close button dismiss the dialog; overlay dismissal is configurable; focus moves to an explicit initial target or the first focusable element; tab and shift+tab remain within the dialog; focus returns to the element active before opening; the dialog renders in a portal; background content becomes inert and body scrolling is locked; nested dialogs and exit animations are production extensions. requirements and deliberate non requirements required in the interview version controlled open and onopenchange api portal rendering accessible dialog name and optional description explicit close button escape dismissal safe overlay dismissal initial focus tab and shift+tab containment focus restoration background inertness body scroll locking and cleanup explain, but do not necessarily finish nested modal stacks exit animations that delay unmounting shadow dom and iframe focus mobile virtual keyboard behavior complex scroll lock compensation alert dialog semantics server rendered initially open dialogs full browser and screen reader compatibility testing this boundary keeps the exercise achievable while showing that a design system dialog requires more hardening than an interview implementation. type contract define the public contract before implementing dom behavior: the responsibilities are explicit: the parent owns whether the dialog is open; onopenchange(false) is the only dismissal request; title always gives the dialog an accessible name; description supplies optional supporting context; children keeps body content composable; initialfocusref lets a form choose its first invalid or primary field; dismissal behavior is configured without exposing internal event handlers. do not accept an arbitrary role in this version. a modal dialog and an alert dialog have different interaction expectations and should not be switched accidentally by a styling prop. state model and invariants the dialog does not need local open state because the parent owns it: the component needs refs rather than additional rendering state: useful invariants: 1. a closed dialog renders no interactive modal surface. 2. an open dialog has an accessible name. 3. focus is inside the dialog after it opens. 4. tab navigation cannot move into inert background content. 5. escape and overlay dismissal request the same controlled state change. 6. clicking inside the dialog never counts as an overlay click. 7. every global mutation is restored during cleanup. 8. closing restores focus to a meaningful element when it still exists. avoid storing ismounted , hasfocus , and isclosing unless animation or server rendering requirements make those states real. most behavior is synchronized from the controlled open prop. component and hook design keep the interview architecture small: extract useportalcontainer because temporary dom node ownership has a clear setup and cleanup lifecycle. keep the initial focus and dismissal logic in the component until a second overlay primitive needs the same behavior. do not extract a generic usemodal hook merely to make the component shorter. focus containment, labeling, portal structure, and dismissal are one interaction contract; splitting them prematurely can make it harder to verify that they stay coordinated. step by step react solution build one behavior at a time: each checkpoint remains explainable if the interview ends early. step 1: build a controlled semantic dialog start with state ownership and accessible structure: aria modal="true" communicates modal intent, but it does not make the background inert or contain focus. those behaviors still need implementation. do not omit a visible title merely because the visual design has an icon. if a visible heading is genuinely inappropriate, the api still needs an explicit accessible label strategy. step 2: render through a portal a portal lets the overlay escape ancestor clipping and stacking contexts while remaining in the same react tree. create and own one portal container: render only after the container exists: the effect avoids reading document during server rendering. in a production application, a shared overlay root may be preferable to creating one container per instance. portal events follow the react tree, not only the dom tree. a dialog click can still bubble to react ancestors of the component that returned the portal. step 3: add escape and safe overlay dismissal handle escape only while open and remove the listener during cleanup: for overlay dismissal, check that the overlay itself was the event target: do not call stoppropagation on every event inside the dialog. it can break legitimate react tree event behavior. the target equality check distinguishes the overlay from its descendants. a production primitive should also decide how pointer down inside followed by pointer up outside behaves and how nested overlays coordinate dismissal. step 4: move and restore focus when opening, remember the active element, then focus the requested target or the first focusable descendant: make the dialog surface programmatically focusable as a fallback: initial focus is a product decision. a destructive confirmation should normally focus the least destructive action, while a long informational dialog may focus its heading or container. step 5: contain tab navigation collect currently focusable descendants when tab is pressed: cycle from the last item to the first and from the first to the last: attach this inside the same open dialog keyboard effect. recalculate on each key press because form validation or conditional content can change which elements are focusable. this selector is interview sized, not a complete focus management engine. production implementations must account for radio groups, shadow dom, iframes, hidden ancestors, disabled fieldsets, and nested overlays. step 6: make the background inert and lock scrolling when the dialog opens, make every body child except its portal container inert and preserve the previous values: inert prevents focus and interaction with the background. it does not stop react effects, timers, requests, or subscriptions in that background subtree. saving prior values is essential. cleanup must restore the environment it found rather than assuming false and an empty overflow style. nested modal stacks need a shared overlay manager or reference counting so one dialog does not undo another dialog’s lock. complete interview sized implementation example usage: minimal styling: accessibility and keyboard behavior the required interaction contract is: input expected behavior open focus moves to the deliberate initial target tab focus moves to the next focusable dialog descendant shift+tab focus moves to the previous focusable descendant tab at the last item focus wraps to the first item shift+tab at the first item focus wraps to the last item escape requests close when enabled close button requests close overlay pointer action requests close only when configured and truly outside close focus returns to the trigger or logical successor important semantic rules: the dialog has role="dialog" and aria modal="true" ; aria labelledby references a meaningful title; aria describedby is omitted when no useful description exists; the close button has an accessible name; background content is inert rather than merely visually obscured; focus remains visible throughout the workflow. do not put every paragraph into aria describedby ; screen readers may announce an overwhelming block when the dialog receives focus. use it for concise supporting context. portals, events, and edge cases react events still follow the react tree portals change dom placement, not react ownership. a click inside the modal can bubble to react ancestors outside the portal container. use precise dismissal checks rather than assuming dom separation stops propagation. focus can disappear during updates validation may remove the currently focused element. after conditional updates, preserve it when possible or move focus to the replacement or error summary. a focus trap should not continually force focus back on every render. nested overlays require coordination two independent dialogs can fight over escape, inert state, scroll locking, and focus restoration. a production overlay manager should track a stack so only the topmost modal handles dismissal and locks are reference counted. exit animations require presence state immediate open === false unmounting cannot display an exit animation. production code may separate logical open state from temporary presence state. during exit, the surface should no longer accept interaction, and focus restoration must happen at a deliberate time. native is a valid alternative the native element provides top layer rendering, modal behavior through showmodal() , escape handling, and focus support. it still requires a controlled react integration, accessible naming, form behavior decisions, animation handling, and cross browser testing. in production, compare native support with a mature headless primitive before maintaining a custom focus manager. testing strategy test the public behavior rather than internal refs. component tests 1. the dialog is absent while closed. 2. opening renders the title, description, and body. 3. initial focus moves to initialfocusref . 4. the close button requests onopenchange(false) . 5. escape closes only when enabled. 6. clicking the overlay closes only when enabled. 7. clicking dialog content does not close it. 8. tab and shift+tab wrap at the boundaries. 9. focus returns to the original trigger. 10. unmounting restores inert and scroll styles. example: browser and assistive technology tests dom test environments do not reproduce every focus, layout, inert, scroll, or accessibility behavior. verify critical dialog flows in a real browser: keyboard only opening, traversal, dismissal, and restoration; screen reader title and description announcement; zoom and mobile viewport behavior; long scrolling content; reduced motion and forced colors; nested popovers or dialogs; removal of the trigger while open. common candidate mistakes treating the overlay as the dialog the overlay is visual and receives outside interaction. the inner surface owns dialog semantics and labeling. adding role="dialog" without focus behavior aria does not move focus, trap tab, inert the background, or restore the trigger. closing on every overlay click an event from dialog content bubbles to the overlay. check event.target === event.currenttarget . forgetting cleanup leaked document listeners, body.style.overflow = "hidden" , or inert state can break the application after the dialog closes. focusing the first element blindly the first element may be a destructive action. choose initial focus from task intent. defining a new portal container during render dom mutation during render is impure and can leak nodes when rendering restarts. create and remove the node in an effect. building a production focus manager in an interview implement the common path, state the limitations, and explain when a tested primitive is the responsible production choice. senior level production improvements after the interview version, consider: a shared overlay stack for nested dialogs and popovers; reference counted inertness and scroll locking; scrollbar compensation and ios scroll behavior; exit presence state with reduced motion support; robust tabbable element discovery; alert dialog and non modal variants as separate contracts; a shared portal root with server rendering integration; focus recovery when the trigger disappears; analytics that do not interfere with dismissal; visual regression, browser, and assistive technology coverage. the strongest production improvement may be replacing the custom implementation with a maintained headless dialog primitive while preserving the public application api. a 60 second solution explanation the parent owns open , and every dismissal path calls onopenchange(false) . the dialog renders through an effect owned portal so it escapes clipping without mutating the dom during render. when it opens, i remember the active element, move focus to an explicit target or the first focusable child, contain tab navigation, listen for escape, make background siblings inert, and lock body scrolling. cleanup removes the listener, restores inert and scroll state, and returns focus. the overlay closes only when it is the original event target, so clicks inside are safe. for production i would use a tested primitive or add an overlay stack, robust tabbable discovery, animation presence, and browser and screen reader coverage. likely interview follow ups why use a portal? to escape ancestor clipping and stacking contexts while keeping context and react event propagation connected to the original react tree. why not stop propagation inside the dialog? it can break legitimate ancestor behavior. checking whether the overlay itself was targeted solves outside dismissal more precisely. why is aria modal insufficient? it communicates semantics but does not implement focus movement, tab containment, background inertness, or restoration. when would you use ? when its platform behavior and browser support match the product requirements. i would still wrap it in a controlled component and test focus, forms, animation, and assistive technology. what changes for nested dialogs? a shared manager should track the topmost overlay, coordinate escape and outside interaction, reference count global locks, and restore focus through the stack. should overlay click always close? no. it may be appropriate for lightweight dismissible content, but destructive, blocking, or multi step workflows may require an explicit decision. why is the focus selector incomplete? real tabbability depends on visibility, radio groups, disabled fieldsets, shadow dom, iframes, and browser behavior. the interview version covers common controls; production should use a well tested focus management utility.',
  },
  {
    number: 4,
    slug: "04-reusable-tabs",
    title: "Reusable Tabs",
    kind: "chapter",
    partNumber: 1,
    partName: "React Live Coding",
    markdown:
      '# Reusable Tabs\n\n## The interview prompt\n\nBuild a reusable tabs component that receives arbitrary tab data, displays one panel at a time, and supports mouse and keyboard interaction.\n\nAssume a 30–40 minute live-coding interview. Prioritize a clear data model, correct selection behavior, basic accessibility, and code you can explain while writing it.\n\n## What the interviewer is evaluating\n\n- Can you turn data into reusable UI instead of hard-coding tabs?\n- Can you keep one source of truth for selection?\n- Do you understand derived state?\n- Can you add keyboard behavior without overengineering it?\n- Do you know the essential tabs ARIA relationships?\n- Can you explain what you would improve in production?\n\nThe interview solution does not need to be a complete design-system primitive.\n\n## Clarifying questions\n\nAsk a few questions before coding:\n\n1. Is selection controlled by the parent?\n2. Should disabled tabs be supported?\n3. Should Left and Right Arrow navigate between tabs?\n4. Should navigation wrap at the ends?\n5. Should inactive panels preserve their local state?\n6. Are vertical tabs or route-synchronized tabs required?\n\nFor this walkthrough:\n\n- the parent controls the selected tab;\n- tabs are provided as data;\n- disabled tabs are supported;\n- Left/Right Arrow, Home, and End navigate enabled tabs;\n- moving focus also selects the tab;\n- only the selected panel is rendered;\n- vertical orientation and state-preserving panels are follow-up improvements.\n\n## Keep the scope interview-sized\n\n### Implement now\n\n- Generic tab data and prop types\n- Controlled selection\n- Click interaction\n- Tab and panel semantics\n- Stable IDs\n- Arrow, Home, and End navigation\n- Disabled-tab handling\n- Empty-state handling\n\n### Discuss afterward\n\n- Controlled and uncontrolled modes in one API\n- Compound components and Context\n- Vertical tabs\n- Manual activation with Enter/Space\n- Preserving every panel’s local state\n- Dynamic insertion and removal\n- URL synchronization\n- Animated indicators\n- RTL navigation\n\nThis distinction is important in a live interview: finish the required behavior first, then show senior judgment by identifying production concerns.\n\n## Step 1 — Define the data and props\n\nStart with the public contract so the component is not tied to one content type:\n\n```tsx\nimport type { ReactNode } from "react";\n\nexport type TabItem = {\n  id: string;\n  label: ReactNode;\n  content: ReactNode;\n  disabled?: boolean;\n};\n\ntype TabsProps = {\n  items: readonly TabItem[];\n  value: string;\n  onValueChange: (value: string) => void;\n  ariaLabel: string;\n};\n```\n\nWhy this shape works:\n\n- `id` is the stable logical identity and selected value;\n- `label` accepts text, an icon, or a badge;\n- `content` accepts any React content;\n- `disabled` is optional;\n- `value` and `onValueChange` make selection controlled;\n- `ariaLabel` gives the tab list an accessible name.\n\nDo not store the selected tab object or index as additional state. Both are derived from `value` and `items`.\n\n## Step 2 — Render selectable tabs\n\nFind the selected item during render:\n\n```tsx\nconst selectedItem = items.find(item => item.id === value);\n```\n\nRender the triggers from data:\n\n```tsx\n\u003cdiv role="tablist" aria-label={ariaLabel}>\n  {items.map(item => {\n    const selected = item.id === value;\n\n    return (\n      \u003cbutton\n        key={item.id}\n        type="button"\n        role="tab"\n        aria-selected={selected}\n        disabled={item.disabled}\n        onClick={() => onValueChange(item.id)}\n      >\n        {item.label}\n      \u003c/button>\n    );\n  })}\n\u003c/div>\n```\n\nThe parent remains the source of truth. Clicking requests a value change; the component does not copy the prop into local state.\n\n## First working implementation\n\nAt this point, assemble the types and rendering logic into a complete clickable version:\n\n```tsx\nimport type { ReactNode } from "react";\n\nexport type TabItem = {\n  id: string;\n  label: ReactNode;\n  content: ReactNode;\n  disabled?: boolean;\n};\n\ntype TabsProps = {\n  items: readonly TabItem[];\n  value: string;\n  onValueChange: (value: string) => void;\n  ariaLabel: string;\n};\n\nexport function Tabs({\n  items,\n  value,\n  onValueChange,\n  ariaLabel,\n}: TabsProps) {\n  const selectedItem = items.find(item => item.id === value);\n\n  return (\n    \u003cdiv className="tabs">\n      \u003cdiv className="tabs__list" role="tablist" aria-label={ariaLabel}>\n        {items.map(item => {\n          const selected = item.id === value;\n\n          return (\n            \u003cbutton\n              key={item.id}\n              className="tabs__tab"\n              type="button"\n              role="tab"\n              aria-selected={selected}\n              disabled={item.disabled}\n              onClick={() => onValueChange(item.id)}\n            >\n              {item.label}\n            \u003c/button>\n          );\n        })}\n      \u003c/div>\n\n      {selectedItem ? (\n        \u003cdiv className="tabs__panel" role="tabpanel">\n          {selectedItem.content}\n        \u003c/div>\n      ) : (\n        \u003cp role="status">No tab is selected.\u003c/p>\n      )}\n    \u003c/div>\n  );\n}\n```\n\nThis is a valid first checkpoint:\n\n- it works with arbitrary data and content;\n- selection has one owner;\n- clicking and native button activation work;\n- disabled tabs cannot be selected;\n- an invalid value has an explicit fallback.\n\nIt is not finished yet. Every button remains in the normal Tab order, and the tabs and panel do not have explicit ID relationships. Say that aloud, then improve this working version rather than trying to write the final component in one pass.\n\n## Step 3 — Connect the selected tab and panel\n\nUse `useId` to create a stable prefix, then derive matching IDs from each item ID:\n\n```tsx\nconst instanceId = useId();\n\nfunction getTabId(instanceId: string, itemId: string) {\n  return `${instanceId}-tab-${encodeURIComponent(itemId)}`;\n}\n\nfunction getPanelId(instanceId: string, itemId: string) {\n  return `${instanceId}-panel-${encodeURIComponent(itemId)}`;\n}\n```\n\nEach tab controls its matching panel:\n\n```tsx\n\u003cbutton\n  id={getTabId(instanceId, item.id)}\n  aria-controls={getPanelId(instanceId, item.id)}\n  // ...\n>\n```\n\nRender the selected panel and point it back to the selected tab:\n\n```tsx\n{selectedItem && (\n  \u003cdiv\n    key={selectedItem.id}\n    role="tabpanel"\n    id={getPanelId(instanceId, selectedItem.id)}\n    aria-labelledby={getTabId(instanceId, selectedItem.id)}\n    tabIndex={0}\n  >\n    {selectedItem.content}\n  \u003c/div>\n)}\n```\n\n`useId` is stable across renders and safe for server rendering. Do not generate IDs with `Math.random()` during render.\n\nThis version renders only the selected panel. The `key` deliberately gives each panel distinct identity, so panel-local state resets after switching away. If state must survive, render all panels and hide inactive ones instead.\n\n## Step 4 — Add keyboard navigation\n\nTabs are a composite widget, so only the selected tab should be in the normal Tab order. Add roving `tabIndex` while implementing arrow navigation:\n\n```tsx\ntabIndex={selected ? 0 : -1}\n```\n\nExpected horizontal-tab behavior:\n\n| Key | Result |\n| --- | --- |\n| Right Arrow | Select the next enabled tab |\n| Left Arrow | Select the previous enabled tab |\n| Home | Select the first enabled tab |\n| End | Select the last enabled tab |\n| Tab | Enter or leave the tab widget |\n\nCreate the enabled list and calculate the destination:\n\n```tsx\nfunction handleKeyDown(event: KeyboardEvent\u003cHTMLButtonElement>) {\n  const enabledItems = items.filter(item => !item.disabled);\n  const currentIndex = enabledItems.findIndex(item => item.id === value);\n\n  if (currentIndex === -1 || enabledItems.length === 0) return;\n\n  let nextIndex: number | null = null;\n\n  if (event.key === "ArrowRight") {\n    nextIndex = (currentIndex + 1) % enabledItems.length;\n  }\n\n  if (event.key === "ArrowLeft") {\n    nextIndex =\n      (currentIndex - 1 + enabledItems.length) % enabledItems.length;\n  }\n\n  if (event.key === "Home") nextIndex = 0;\n  if (event.key === "End") nextIndex = enabledItems.length - 1;\n\n  if (nextIndex === null) return;\n\n  event.preventDefault();\n  const nextItem = enabledItems[nextIndex];\n  onValueChange(nextItem.id);\n  tabRefs.current.get(nextItem.id)?.focus();\n}\n```\n\nStore button elements in a ref map so keyboard navigation can focus its destination:\n\n```tsx\nconst tabRefs = useRef(new Map\u003cstring, HTMLButtonElement>());\n```\n\nThe destination button already exists, so focus can move during the keyboard interaction. Do not focus from an Effect on every controlled value change; that could steal focus on initial mount or when routing changes the selection externally.\n\nAttach each button to the map:\n\n```tsx\nref={node => {\n  if (node) tabRefs.current.set(item.id, node);\n  else tabRefs.current.delete(item.id);\n}}\n```\n\nIn an interview, explain that automatic activation is suitable when panels switch immediately. If selection triggers slow requests, manual activation—move focus with arrows, select with Enter or Space—may be preferable.\n\n## Complete solution\n\n```tsx\n"use client";\n\nimport { useId, useRef } from "react";\nimport type { KeyboardEvent, ReactNode } from "react";\n\nexport type TabItem = {\n  id: string;\n  label: ReactNode;\n  content: ReactNode;\n  disabled?: boolean;\n};\n\ntype TabsProps = {\n  items: readonly TabItem[];\n  value: string;\n  onValueChange: (value: string) => void;\n  ariaLabel: string;\n};\n\nfunction getTabId(instanceId: string, itemId: string) {\n  return `${instanceId}-tab-${encodeURIComponent(itemId)}`;\n}\n\nfunction getPanelId(instanceId: string, itemId: string) {\n  return `${instanceId}-panel-${encodeURIComponent(itemId)}`;\n}\n\nexport function Tabs({\n  items,\n  value,\n  onValueChange,\n  ariaLabel,\n}: TabsProps) {\n  const instanceId = useId();\n  const tabRefs = useRef(new Map\u003cstring, HTMLButtonElement>());\n  const selectedItem = items.find(item => item.id === value);\n\n  function handleKeyDown(event: KeyboardEvent\u003cHTMLButtonElement>) {\n    const enabledItems = items.filter(item => !item.disabled);\n    const currentIndex = enabledItems.findIndex(item => item.id === value);\n\n    if (currentIndex === -1 || enabledItems.length === 0) return;\n\n    let nextIndex: number | null = null;\n\n    if (event.key === "ArrowRight") {\n      nextIndex = (currentIndex + 1) % enabledItems.length;\n    }\n\n    if (event.key === "ArrowLeft") {\n      nextIndex =\n        (currentIndex - 1 + enabledItems.length) % enabledItems.length;\n    }\n\n    if (event.key === "Home") nextIndex = 0;\n    if (event.key === "End") nextIndex = enabledItems.length - 1;\n\n    if (nextIndex === null) return;\n\n    event.preventDefault();\n    const nextItem = enabledItems[nextIndex];\n    onValueChange(nextItem.id);\n    tabRefs.current.get(nextItem.id)?.focus();\n  }\n\n  return (\n    \u003cdiv className="tabs">\n      \u003cdiv className="tabs__list" role="tablist" aria-label={ariaLabel}>\n        {items.map(item => {\n          const selected = item.id === value;\n\n          return (\n            \u003cbutton\n              key={item.id}\n              ref={node => {\n                if (node) tabRefs.current.set(item.id, node);\n                else tabRefs.current.delete(item.id);\n              }}\n              className="tabs__tab"\n              type="button"\n              role="tab"\n              id={getTabId(instanceId, item.id)}\n              aria-selected={selected}\n              aria-controls={getPanelId(instanceId, item.id)}\n              tabIndex={selected ? 0 : -1}\n              disabled={item.disabled}\n              onClick={() => onValueChange(item.id)}\n              onKeyDown={handleKeyDown}\n            >\n              {item.label}\n            \u003c/button>\n          );\n        })}\n      \u003c/div>\n\n      {selectedItem ? (\n        \u003cdiv\n          key={selectedItem.id}\n          className="tabs__panel"\n          role="tabpanel"\n          id={getPanelId(instanceId, selectedItem.id)}\n          aria-labelledby={getTabId(instanceId, selectedItem.id)}\n          tabIndex={0}\n        >\n          {selectedItem.content}\n        \u003c/div>\n      ) : (\n        \u003cp role="status">No tab is selected.\u003c/p>\n      )}\n    \u003c/div>\n  );\n}\n```\n\nThe component is generic, accessible, and short enough to build while explaining each decision.\n\n## Example usage\n\n```tsx\nimport { useState } from "react";\nimport { Tabs } from "./Tabs";\nimport type { TabItem } from "./Tabs";\n\nconst settingsTabs: readonly TabItem[] = [\n  {\n    id: "profile",\n    label: "Profile",\n    content: \u003cProfileSettings />,\n  },\n  {\n    id: "security",\n    label: "Security",\n    content: \u003cSecuritySettings />,\n  },\n  {\n    id: "billing",\n    label: "Billing",\n    content: \u003cBillingSettings />,\n    disabled: true,\n  },\n];\n\nexport function SettingsPage() {\n  const [selectedTab, setSelectedTab] = useState("profile");\n\n  return (\n    \u003cTabs\n      items={settingsTabs}\n      value={selectedTab}\n      onValueChange={setSelectedTab}\n      ariaLabel="Account settings"\n    />\n  );\n}\n```\n\nThe same component can display product information, dashboard views, documentation sections, or any other React content.\n\n## Minimal CSS\n\n```css\n.tabs__list {\n  display: flex;\n  gap: 0.25rem;\n  border-bottom: 1px solid #cbd5e1;\n}\n\n.tabs__tab {\n  border: 0;\n  border-bottom: 3px solid transparent;\n  padding: 0.75rem 1rem;\n  color: #475569;\n  background: transparent;\n  font: inherit;\n  cursor: pointer;\n}\n\n.tabs__tab[aria-selected="true"] {\n  border-color: #2563eb;\n  color: #0f172a;\n  font-weight: 700;\n}\n\n.tabs__tab:focus-visible,\n.tabs__panel:focus-visible {\n  outline: 3px solid rgb(37 99 235 / 40%);\n  outline-offset: 3px;\n}\n\n.tabs__tab:disabled {\n  opacity: 0.45;\n  cursor: not-allowed;\n}\n\n.tabs__panel {\n  padding-block: 1.25rem;\n}\n```\n\nSelection and focus are different states. Keep both the selected styling and a visible focus indicator.\n\n## Complexity\n\nLet `n` be the number of tabs:\n\n- rendering is `O(n)`;\n- finding the selected item is `O(n)`;\n- an arrow-key operation filters and searches in `O(n)`;\n- ref lookup for focusing is `O(1)`.\n\nTabs collections are normally small, so clarity matters more than maintaining extra lookup structures. If hundreds of tabs exist, the interaction design likely needs reconsideration before micro-optimization.\n\n## Testing strategy\n\nTest the public behavior:\n\n1. The selected tab has `aria-selected="true"` and `tabIndex={0}`.\n2. The matching panel is visible and labelled by its tab.\n3. Clicking an enabled tab calls `onValueChange`.\n4. Disabled tabs cannot be clicked.\n5. Right and Left Arrow navigation wraps.\n6. Keyboard navigation skips disabled tabs.\n7. Home and End select the boundary tabs.\n8. Keyboard navigation moves focus to the newly selected tab.\n9. An invalid selected value renders the empty state.\n\n```tsx\nit("selects the next enabled tab with ArrowRight", async () => {\n  const user = userEvent.setup();\n  render(\u003cSettingsTabs />);\n\n  const profile = screen.getByRole("tab", { name: "Profile" });\n  const security = screen.getByRole("tab", { name: "Security" });\n\n  profile.focus();\n  await user.keyboard("{ArrowRight}");\n\n  expect(security).toHaveFocus();\n  expect(security).toHaveAttribute("aria-selected", "true");\n  expect(\n    screen.getByRole("tabpanel", { name: "Security" }),\n  ).toBeVisible();\n});\n```\n\nQuery by role and accessible name rather than class names or component state.\n\n## Common mistakes\n\n- Hard-coding each tab instead of rendering data.\n- Storing both selected ID and selected index.\n- Using the array index as the item identity.\n- Copying the controlled value into local state.\n- Rendering clickable `\u003cdiv>` elements instead of buttons.\n- Omitting `tablist`, `tab`, or `tabpanel` roles.\n- Forgetting `aria-controls` and `aria-labelledby`.\n- Putting every tab in the normal Tab order.\n- Navigating to disabled tabs.\n- Generating random IDs during render.\n- Removing the focus outline.\n- Adding Context and compound components before the requirements need them.\n\n## Production improvements to discuss\n\nAfter the core solution works, mention—not necessarily implement—the following:\n\n- Add uncontrolled mode with `defaultValue`.\n- Add `orientation="vertical"` and use Up/Down Arrow.\n- Add manual activation for panels that load slowly.\n- Render all panels with `hidden` when their local state must survive.\n- Recover selection if the active tab is removed or disabled.\n- Synchronize controlled selection with the URL.\n- Support RTL-aware horizontal navigation.\n- Expose refs and merge consumer handlers in a design-system API.\n- Add compound components only when consumers need custom tab/panel structure.\n- Validate duplicate IDs in development.\n- Test with representative screen readers.\n\nThis is where seniority appears: complete the essential task first, then describe how requirements would change the design.\n\n## How to explain the solution in 60 seconds\n\nI model each tab with a stable ID, arbitrary label, arbitrary content, and an optional disabled state. The parent controls one selected ID, so the selected item and index remain derived rather than duplicated state. I render native buttons with tab semantics and connect the active tab and panel using stable IDs from `useId`. Roving `tabIndex` puts only the selected tab in the page’s Tab order. Left, Right, Home, and End calculate the next enabled item, request selection, and focus its existing button during the keyboard interaction. This interview version renders one panel and resets its local state when switching; if preservation were required, I would keep all panels mounted and hide inactive ones.\n\n## Likely interview follow-ups\n\n### Why use an ID rather than the array index as the value?\n\nThe ID represents logical identity after insertion, removal, or reordering. An index represents only the current position.\n\n### Why is selection controlled?\n\nThe parent may need to coordinate tabs with routing, analytics, or other UI. A controlled API also keeps the interview implementation focused on one state owner.\n\n### Why focus inside the keyboard handler?\n\nThe destination button already exists, and focus movement is caused directly by that keyboard event. An Effect tied to `value` could steal focus when selection changes for another reason.\n\n### Why not use Context?\n\nOne component does not need it. Context becomes useful when a production API separates the root, list, triggers, and panels into compound components.\n\n### How would you preserve panel state?\n\nRender every panel with a stable key and apply `hidden` to inactive panels instead of unmounting them. This trades memory and Effect cost for preservation.\n\n### When would you use manual activation?\n\nWhen selecting a panel starts slow work. Arrow keys would move focus, while Enter or Space would update selection.',
    headings: [
      "The interview prompt",
      "What the interviewer is evaluating",
      "Clarifying questions",
      "Keep the scope interview-sized",
      "Step 1 — Define the data and props",
      "Step 2 — Render selectable tabs",
      "First working implementation",
      "Step 3 — Connect the selected tab and panel",
      "Step 4 — Add keyboard navigation",
      "Complete solution",
      "Example usage",
      "Minimal CSS",
      "Complexity",
      "Testing strategy",
      "Common mistakes",
      "Production improvements to discuss",
      "How to explain the solution in 60 seconds",
      "Likely interview follow-ups",
    ],
    quickRefresher: "",
    excerpt:
      "Build a reusable tabs component that receives arbitrary tab data, displays one panel at a time, and supports mouse and keyboard interaction.",
    readingMinutes: 7,
    searchText:
      'reusable tabs the interview prompt build a reusable tabs component that receives arbitrary tab data, displays one panel at a time, and supports mouse and keyboard interaction. assume a 30–40 minute live coding interview. prioritize a clear data model, correct selection behavior, basic accessibility, and code you can explain while writing it. what the interviewer is evaluating can you turn data into reusable ui instead of hard coding tabs? can you keep one source of truth for selection? do you understand derived state? can you add keyboard behavior without overengineering it? do you know the essential tabs aria relationships? can you explain what you would improve in production? the interview solution does not need to be a complete design system primitive. clarifying questions ask a few questions before coding: 1. is selection controlled by the parent? 2. should disabled tabs be supported? 3. should left and right arrow navigate between tabs? 4. should navigation wrap at the ends? 5. should inactive panels preserve their local state? 6. are vertical tabs or route synchronized tabs required? for this walkthrough: the parent controls the selected tab; tabs are provided as data; disabled tabs are supported; left/right arrow, home, and end navigate enabled tabs; moving focus also selects the tab; only the selected panel is rendered; vertical orientation and state preserving panels are follow up improvements. keep the scope interview sized implement now generic tab data and prop types controlled selection click interaction tab and panel semantics stable ids arrow, home, and end navigation disabled tab handling empty state handling discuss afterward controlled and uncontrolled modes in one api compound components and context vertical tabs manual activation with enter/space preserving every panel’s local state dynamic insertion and removal url synchronization animated indicators rtl navigation this distinction is important in a live interview: finish the required behavior first, then show senior judgment by identifying production concerns. step 1 — define the data and props start with the public contract so the component is not tied to one content type: why this shape works: id is the stable logical identity and selected value; label accepts text, an icon, or a badge; content accepts any react content; disabled is optional; value and onvaluechange make selection controlled; arialabel gives the tab list an accessible name. do not store the selected tab object or index as additional state. both are derived from value and items . step 2 — render selectable tabs find the selected item during render: render the triggers from data: the parent remains the source of truth. clicking requests a value change; the component does not copy the prop into local state. first working implementation at this point, assemble the types and rendering logic into a complete clickable version: this is a valid first checkpoint: it works with arbitrary data and content; selection has one owner; clicking and native button activation work; disabled tabs cannot be selected; an invalid value has an explicit fallback. it is not finished yet. every button remains in the normal tab order, and the tabs and panel do not have explicit id relationships. say that aloud, then improve this working version rather than trying to write the final component in one pass. step 3 — connect the selected tab and panel use useid to create a stable prefix, then derive matching ids from each item id: each tab controls its matching panel: render the selected panel and point it back to the selected tab: useid is stable across renders and safe for server rendering. do not generate ids with math.random() during render. this version renders only the selected panel. the key deliberately gives each panel distinct identity, so panel local state resets after switching away. if state must survive, render all panels and hide inactive ones instead. step 4 — add keyboard navigation tabs are a composite widget, so only the selected tab should be in the normal tab order. add roving tabindex while implementing arrow navigation: expected horizontal tab behavior: key result right arrow select the next enabled tab left arrow select the previous enabled tab home select the first enabled tab end select the last enabled tab tab enter or leave the tab widget create the enabled list and calculate the destination: store button elements in a ref map so keyboard navigation can focus its destination: the destination button already exists, so focus can move during the keyboard interaction. do not focus from an effect on every controlled value change; that could steal focus on initial mount or when routing changes the selection externally. attach each button to the map: in an interview, explain that automatic activation is suitable when panels switch immediately. if selection triggers slow requests, manual activation—move focus with arrows, select with enter or space—may be preferable. complete solution the component is generic, accessible, and short enough to build while explaining each decision. example usage the same component can display product information, dashboard views, documentation sections, or any other react content. minimal css selection and focus are different states. keep both the selected styling and a visible focus indicator. complexity let n be the number of tabs: rendering is o(n) ; finding the selected item is o(n) ; an arrow key operation filters and searches in o(n) ; ref lookup for focusing is o(1) . tabs collections are normally small, so clarity matters more than maintaining extra lookup structures. if hundreds of tabs exist, the interaction design likely needs reconsideration before micro optimization. testing strategy test the public behavior: 1. the selected tab has aria selected="true" and tabindex={0} . 2. the matching panel is visible and labelled by its tab. 3. clicking an enabled tab calls onvaluechange . 4. disabled tabs cannot be clicked. 5. right and left arrow navigation wraps. 6. keyboard navigation skips disabled tabs. 7. home and end select the boundary tabs. 8. keyboard navigation moves focus to the newly selected tab. 9. an invalid selected value renders the empty state. query by role and accessible name rather than class names or component state. common mistakes hard coding each tab instead of rendering data. storing both selected id and selected index. using the array index as the item identity. copying the controlled value into local state. rendering clickable elements instead of buttons. omitting tablist , tab , or tabpanel roles. forgetting aria controls and aria labelledby . putting every tab in the normal tab order. navigating to disabled tabs. generating random ids during render. removing the focus outline. adding context and compound components before the requirements need them. production improvements to discuss after the core solution works, mention—not necessarily implement—the following: add uncontrolled mode with defaultvalue . add orientation="vertical" and use up/down arrow. add manual activation for panels that load slowly. render all panels with hidden when their local state must survive. recover selection if the active tab is removed or disabled. synchronize controlled selection with the url. support rtl aware horizontal navigation. expose refs and merge consumer handlers in a design system api. add compound components only when consumers need custom tab/panel structure. validate duplicate ids in development. test with representative screen readers. this is where seniority appears: complete the essential task first, then describe how requirements would change the design. how to explain the solution in 60 seconds i model each tab with a stable id, arbitrary label, arbitrary content, and an optional disabled state. the parent controls one selected id, so the selected item and index remain derived rather than duplicated state. i render native buttons with tab semantics and connect the active tab and panel using stable ids from useid . roving tabindex puts only the selected tab in the page’s tab order. left, right, home, and end calculate the next enabled item, request selection, and focus its existing button during the keyboard interaction. this interview version renders one panel and resets its local state when switching; if preservation were required, i would keep all panels mounted and hide inactive ones. likely interview follow ups why use an id rather than the array index as the value? the id represents logical identity after insertion, removal, or reordering. an index represents only the current position. why is selection controlled? the parent may need to coordinate tabs with routing, analytics, or other ui. a controlled api also keeps the interview implementation focused on one state owner. why focus inside the keyboard handler? the destination button already exists, and focus movement is caused directly by that keyboard event. an effect tied to value could steal focus when selection changes for another reason. why not use context? one component does not need it. context becomes useful when a production api separates the root, list, triggers, and panels into compound components. how would you preserve panel state? render every panel with a stable key and apply hidden to inactive panels instead of unmounting them. this trades memory and effect cost for preservation. when would you use manual activation? when selecting a panel starts slow work. arrow keys would move focus, while enter or space would update selection.',
  },
];

export const roadmap: RoadmapPart[] = [
  {
    label: "Part I",
    title: "Runtime Foundations",
    chapters: [
      {
        number: 1,
        title: "JavaScript Runtime Model",
        slug: "01-javascript-runtime-model",
      },
      {
        number: 2,
        title: "Execution Contexts",
        slug: "02-execution-contexts",
      },
      {
        number: 3,
        title: "The Call Stack",
        slug: "03-the-call-stack",
      },
      {
        number: 4,
        title: "Lexical Environments and Environment Records",
        slug: "04-lexical-environments-and-environment-records",
      },
      {
        number: 5,
        title: "Scope and Identifier Resolution",
        slug: "05-scope-and-identifier-resolution",
      },
      {
        number: 6,
        title: "Closures",
        slug: "06-closures",
      },
      {
        number: 7,
        title: "this Binding",
        slug: "07-this-binding",
      },
      {
        number: 8,
        title: "Memory Management and Garbage Collection",
        slug: "08-memory-management-and-garbage-collection",
      },
    ],
  },
  {
    label: "Part II",
    title: "Values and Objects",
    chapters: [
      {
        number: 9,
        title: "Primitive Values and Objects",
        slug: "09-primitive-values-and-objects",
      },
      {
        number: 10,
        title: "Equality, Identity, and Coercion",
        slug: "10-equality-identity-and-coercion",
      },
      {
        number: 11,
        title: "Property Access and Property Descriptors",
        slug: null,
      },
      {
        number: 12,
        title: "The Prototype Chain",
        slug: null,
      },
      {
        number: 13,
        title: "Classes and Inheritance",
        slug: null,
      },
      {
        number: 14,
        title: "Symbols",
        slug: null,
      },
      {
        number: 15,
        title: "Maps, Sets, WeakMaps, and WeakSets",
        slug: null,
      },
      {
        number: 16,
        title: "Copying, Mutation, and Structural Sharing",
        slug: null,
      },
    ],
  },
  {
    label: "Part III",
    title: "Functions and Abstractions",
    chapters: [
      {
        number: 17,
        title: "Function Creation and Invocation",
        slug: null,
      },
      {
        number: 18,
        title: "Arrow Functions",
        slug: null,
      },
      {
        number: 19,
        title: "Higher-Order Functions",
        slug: null,
      },
      {
        number: 20,
        title: "Partial Application and Currying",
        slug: null,
      },
      {
        number: 21,
        title: "Function Composition",
        slug: null,
      },
      {
        number: 22,
        title: "Recursion and Stack Limits",
        slug: null,
      },
      {
        number: 23,
        title: "Iterators and Iterables",
        slug: null,
      },
      {
        number: 24,
        title: "Generators",
        slug: null,
      },
    ],
  },
  {
    label: "Part IV",
    title: "Asynchronous JavaScript",
    chapters: [
      {
        number: 25,
        title: "The Event Loop",
        slug: null,
      },
      {
        number: 26,
        title: "Tasks, Microtasks, and Rendering Opportunities",
        slug: null,
      },
      {
        number: 27,
        title: "Promises",
        slug: null,
      },
      {
        number: 28,
        title: "Promise Resolution and Thenable Assimilation",
        slug: null,
      },
      {
        number: 29,
        title: "Async/Await",
        slug: null,
      },
      {
        number: 30,
        title: "Error Propagation in Asynchronous Code",
        slug: null,
      },
      {
        number: 31,
        title: "Fetch and HTTP Cancellation",
        slug: null,
      },
      {
        number: 32,
        title: "AbortController and AbortSignal",
        slug: null,
      },
      {
        number: 33,
        title: "Concurrency Control",
        slug: null,
      },
      {
        number: 34,
        title: "Web Workers and Worker Communication",
        slug: null,
      },
    ],
  },
  {
    label: "Part V",
    title: "Modules and Code Loading",
    chapters: [
      {
        number: 35,
        title: "ES Modules",
        slug: null,
      },
      {
        number: 36,
        title: "CommonJS",
        slug: null,
      },
      {
        number: 37,
        title: "Module Evaluation and Caching",
        slug: null,
      },
      {
        number: 38,
        title: "Circular Dependencies",
        slug: null,
      },
      {
        number: 39,
        title: "Dynamic Imports",
        slug: null,
      },
      {
        number: 40,
        title: "Bundlers and Module Graphs",
        slug: null,
      },
      {
        number: 41,
        title: "Tree Shaking",
        slug: null,
      },
      {
        number: 42,
        title: "Code Splitting",
        slug: null,
      },
    ],
  },
  {
    label: "Part VI",
    title: "Browser Runtime",
    chapters: [
      {
        number: 43,
        title: "DOM Architecture",
        slug: null,
      },
      {
        number: 44,
        title: "Browser Event Dispatch",
        slug: null,
      },
      {
        number: 45,
        title: "Capturing, Bubbling, and Delegation",
        slug: null,
      },
      {
        number: 46,
        title: "Rendering Pipeline",
        slug: null,
      },
      {
        number: 47,
        title: "Style Calculation, Layout, Paint, and Compositing",
        slug: null,
      },
      {
        number: 48,
        title: "Forced Synchronous Layout",
        slug: null,
      },
      {
        number: 49,
        title: "Browser Scheduling APIs",
        slug: null,
      },
      {
        number: 50,
        title: "Storage and Cross-Context Communication",
        slug: null,
      },
    ],
  },
  {
    label: "Part VII",
    title: "Performance and Engine Behavior",
    chapters: [
      {
        number: 51,
        title: "Measuring JavaScript Performance",
        slug: null,
      },
      {
        number: 52,
        title: "Memory Leaks and Retained References",
        slug: null,
      },
      {
        number: 53,
        title: "Hidden Classes and Object Shapes",
        slug: null,
      },
      {
        number: 54,
        title: "Inline Caching",
        slug: null,
      },
      {
        number: 55,
        title: "Optimization and Deoptimization",
        slug: null,
      },
      {
        number: 56,
        title: "Allocation Patterns",
        slug: null,
      },
      {
        number: 57,
        title: "Long Tasks and Main-Thread Contention",
        slug: null,
      },
      {
        number: 58,
        title: "Performance Trade-offs in Data Structures",
        slug: null,
      },
    ],
  },
  {
    label: "Part VIII",
    title: "React Connections",
    chapters: [
      {
        number: 59,
        title: "Closures in React Components",
        slug: null,
      },
      {
        number: 60,
        title: "Stale Closures",
        slug: null,
      },
      {
        number: 61,
        title: "Referential Equality",
        slug: null,
      },
      {
        number: 62,
        title: "Dependency Arrays",
        slug: null,
      },
      {
        number: 63,
        title: "State Updates and Batching",
        slug: null,
      },
      {
        number: 64,
        title: "React Scheduling",
        slug: null,
      },
      {
        number: 65,
        title: "Render and Commit Phases",
        slug: null,
      },
      {
        number: 66,
        title: "Concurrent Rendering",
        slug: null,
      },
      {
        number: 67,
        title: "External Stores and Subscriptions",
        slug: null,
      },
      {
        number: 68,
        title: "JavaScript Performance Patterns in React",
        slug: null,
      },
    ],
  },
  {
    label: "Part IX",
    title: "Interview Preparation",
    chapters: [
      {
        number: 69,
        title: "How to Explain JavaScript Internals",
        slug: null,
      },
      {
        number: 70,
        title: "Common JavaScript Interview Misconceptions",
        slug: null,
      },
      {
        number: 71,
        title: "Output-Prediction Questions",
        slug: null,
      },
      {
        number: 72,
        title: "Debugging Questions",
        slug: null,
      },
      {
        number: 73,
        title: "Performance Questions",
        slug: null,
      },
      {
        number: 74,
        title: "Senior-Level Follow-up Questions",
        slug: null,
      },
      {
        number: 75,
        title: "Full JavaScript Mock Interview",
        slug: null,
      },
      {
        number: 76,
        title: "Final Review and Cheat Sheets",
        slug: null,
      },
    ],
  },
];

export const questionRoadmap: QuestionRoadmapSection[] = [
  {
    title: "JavaScript fundamentals",
    questions: [
      {
        number: 1,
        title: "What is the difference between var, let, and const?",
      },
      {
        number: 2,
        title: "What are JavaScript’s primitive data types?",
      },
      {
        number: 3,
        title: "What is the difference between null and undefined?",
      },
      {
        number: 4,
        title: "What is the difference between == and ===?",
      },
      {
        number: 5,
        title: "What are truthy and falsy values?",
      },
      {
        number: 6,
        title: "What is type coercion?",
      },
      {
        number: 7,
        title: "What is hoisting?",
      },
      {
        number: 8,
        title: "What is the temporal dead zone?",
      },
      {
        number: 9,
        title: "What is lexical scope?",
      },
      {
        number: 10,
        title: "What is a closure?",
      },
      {
        number: 11,
        title: "What is variable shadowing?",
      },
      {
        number: 12,
        title: "What is the difference between passing values by value and by reference?",
      },
      {
        number: 13,
        title: "How do shallow and deep copies differ?",
      },
      {
        number: 14,
        title: "What is destructuring?",
      },
      {
        number: 15,
        title: "What are rest and spread syntax, and how do they differ?",
      },
      {
        number: 16,
        title: "What are template literals?",
      },
      {
        number: 17,
        title: "What are JavaScript modules, and how do import and export work?",
      },
    ],
  },
  {
    title: "Functions and objects",
    questions: [
      {
        number: 18,
        title: "What is the difference between a function declaration and a function expression?",
      },
      {
        number: 19,
        title: "How do arrow functions differ from regular functions?",
      },
      {
        number: 20,
        title: "How does the this keyword work?",
      },
      {
        number: 21,
        title: "What do call, apply, and bind do?",
      },
      {
        number: 22,
        title: "What is a higher-order function?",
      },
      {
        number: 23,
        title: "What is a callback function?",
      },
      {
        number: 24,
        title: "What are pure functions and side effects?",
      },
      {
        number: 25,
        title: "What is an immediately invoked function expression (IIFE)?",
      },
      {
        number: 26,
        title: "How do default parameters work?",
      },
      {
        number: 27,
        title: "How do JavaScript objects inherit properties?",
      },
      {
        number: 28,
        title: "What is the prototype chain?",
      },
      {
        number: 29,
        title: "What is the difference between a class and constructor function?",
      },
      {
        number: 30,
        title: "What is the difference between static and instance methods?",
      },
      {
        number: 31,
        title: "What are getters and setters?",
      },
      {
        number: 32,
        title: "What is the difference between mutable and immutable operations?",
      },
    ],
  },
  {
    title: "Arrays and collections",
    questions: [
      {
        number: 33,
        title: "What is the difference between map, filter, and reduce?",
      },
      {
        number: 34,
        title: "What is the difference between forEach and map?",
      },
      {
        number: 35,
        title: "What is the difference between find and filter?",
      },
      {
        number: 36,
        title: "What is the difference between some and every?",
      },
      {
        number: 37,
        title: "Which array methods mutate the original array?",
      },
      {
        number: 38,
        title: "How can you remove duplicate values from an array?",
      },
      {
        number: 39,
        title: "When would you use Map instead of an object?",
      },
      {
        number: 40,
        title: "When would you use Set?",
      },
      {
        number: 41,
        title: "What are WeakMap and WeakSet?",
      },
    ],
  },
  {
    title: "Asynchronous JavaScript",
    questions: [
      {
        number: 42,
        title: "What is the JavaScript event loop?",
      },
      {
        number: 43,
        title: "What is the call stack?",
      },
      {
        number: 44,
        title: "What is the difference between synchronous and asynchronous code?",
      },
      {
        number: 45,
        title: "What are microtasks and macrotasks?",
      },
      {
        number: 46,
        title: "In what order do synchronous code, promises, and timers execute?",
      },
      {
        number: 47,
        title: "What is a Promise?",
      },
      {
        number: 48,
        title: "What states can a Promise have?",
      },
      {
        number: 49,
        title: "How do async and await work?",
      },
      {
        number: 50,
        title: "How do Promise.all, Promise.allSettled, Promise.race, and Promise.any differ?",
      },
      {
        number: 51,
        title: "How should errors be handled in asynchronous code?",
      },
      {
        number: 52,
        title: "How can an HTTP request be cancelled?",
      },
      {
        number: 53,
        title: "How can multiple asynchronous operations be executed in parallel?",
      },
    ],
  },
  {
    title: "Browser and DOM",
    questions: [
      {
        number: 54,
        title: "What is the DOM?",
      },
      {
        number: 55,
        title: "What is the difference between the DOM and the HTML source?",
      },
      {
        number: 56,
        title: "How do you select, create, update, and remove DOM elements?",
      },
      {
        number: 57,
        title: "What is event bubbling?",
      },
      {
        number: 58,
        title: "What is event capturing?",
      },
      {
        number: 59,
        title: "What is event delegation?",
      },
      {
        number: 60,
        title: "What is the difference between event.target and event.currentTarget?",
      },
      {
        number: 61,
        title: "What do preventDefault and stopPropagation do?",
      },
      {
        number: 62,
        title: "What is the difference between DOMContentLoaded and load?",
      },
      {
        number: 63,
        title: "What is the difference between innerHTML, innerText, and textContent?",
      },
      {
        number: 64,
        title: "What are custom events?",
      },
      {
        number: 65,
        title: "What are the differences between cookies, localStorage, and sessionStorage?",
      },
      {
        number: 66,
        title: "What is the same-origin policy?",
      },
      {
        number: 67,
        title: "What is CORS?",
      },
      {
        number: 68,
        title: "How does the Fetch API work?",
      },
      {
        number: 69,
        title: "What is the difference between Fetch and XMLHttpRequest?",
      },
      {
        number: 70,
        title: "What is the History API?",
      },
      {
        number: 71,
        title: "What are Web Workers, and when should they be used?",
      },
    ],
  },
  {
    title: "Performance and optimization",
    questions: [
      {
        number: 72,
        title: "What are debounce and throttle?",
      },
      {
        number: 73,
        title: "What causes memory leaks in frontend applications?",
      },
      {
        number: 74,
        title: "How does JavaScript garbage collection work?",
      },
      {
        number: 75,
        title: "What are reflow and repaint?",
      },
      {
        number: 76,
        title: "What is the difference between async and defer on script elements?",
      },
      {
        number: 77,
        title: "What is lazy loading?",
      },
      {
        number: 78,
        title: "What is code splitting?",
      },
      {
        number: 79,
        title: "What is tree shaking?",
      },
      {
        number: 80,
        title: "What is memoization?",
      },
      {
        number: 81,
        title: "How can frontend JavaScript performance be measured and improved?",
      },
      {
        number: 82,
        title: "What is the difference between requestAnimationFrame and setTimeout?",
      },
    ],
  },
  {
    title: "Errors, security, and code quality",
    questions: [
      {
        number: 83,
        title: "How do try, catch, finally, and throw work?",
      },
      {
        number: 84,
        title: "What is the difference between syntax, runtime, and logical errors?",
      },
      {
        number: 85,
        title: "What is cross-site scripting (XSS), and how can it be prevented?",
      },
      {
        number: 86,
        title: "What is cross-site request forgery (CSRF), and how can it be prevented?",
      },
      {
        number: 87,
        title: "Why is using eval dangerous?",
      },
      {
        number: 88,
        title: "What is strict mode?",
      },
      {
        number: 89,
        title: "What is optional chaining?",
      },
      {
        number: 90,
        title: "What is nullish coalescing?",
      },
      {
        number: 91,
        title: "What are common ways to test JavaScript code?",
      },
      {
        number: 92,
        title: "What is the difference between unit, integration, and end-to-end testing?",
      },
    ],
  },
  {
    title: "Practical interview questions",
    questions: [
      {
        number: 93,
        title: "How would you reverse a string?",
      },
      {
        number: 94,
        title: "How would you check whether a string is a palindrome?",
      },
      {
        number: 95,
        title: "How would you flatten a nested array?",
      },
      {
        number: 96,
        title: "How would you group an array of objects by a property?",
      },
      {
        number: 97,
        title: "How would you implement debounce?",
      },
      {
        number: 98,
        title: "How would you implement throttle?",
      },
      {
        number: 99,
        title: "How would you implement a deep clone?",
      },
      {
        number: 100,
        title: "How would you implement a simplified version of Promise.all?",
      },
      {
        number: 101,
        title: "How would you retry a failed asynchronous operation?",
      },
      {
        number: 102,
        title: "How would you build an event emitter?",
      },
      {
        number: 103,
        title: "How would you cache the result of a function?",
      },
      {
        number: 104,
        title: "How would you diagnose and improve a slow frontend application?",
      },
    ],
  },
];

export const questionAnswers: QuestionAnswer[] = [
  {
    number: 1,
    question: "What is the difference between var, let, and const?",
    answer:
      "`var`, `let`, and `const` declare variables, but they differ in scope, redeclaration, reassignment, and initialization.\n\n- `var` is function-scoped and can be redeclared and reassigned.\n- `let` is block-scoped, cannot be redeclared in the same scope, and can be reassigned.\n- `const` is block-scoped and cannot be redeclared or reassigned.",
    explanation:
      'Prefer `const` by default and use `let` when a variable must change. Avoid `var` in modern JavaScript because its function scope can cause unexpected behavior.\n\n```js\nconst name = "Alex";\nlet count = 0;\ncount++;\n```',
    details:
      'Variables declared with `let` and `const` belong to the nearest block, while `var` belongs to the nearest function:\n\n```js\nif (true) {\n  var functionScoped = "available outside";\n  let blockScoped = "available only inside";\n}\n\nconsole.log(functionScoped); // "available outside"\nconsole.log(blockScoped);    // ReferenceError\n```\n\nA `var` declaration can be repeated in the same scope:\n\n```js\nvar score = 10;\nvar score = 20; // Allowed\n```\n\n`let` and `const` cannot be redeclared in the same scope:\n\n```js\nlet age = 30;\nlet age = 31; // SyntaxError\n```\n\n`const` prevents reassignment, but it does not make objects immutable:\n\n```js\nconst user = { name: "Alex" };\n\nuser.name = "Sam";       // Allowed\nuser = { name: "Lee" };  // TypeError\n```\n\nAll three declarations are hoisted. However, `var` is initialized with `undefined`, while `let` and `const` remain inaccessible in the temporal dead zone until their declarations execute.',
  },
  {
    number: 2,
    question: "What are JavaScript’s primitive data types?",
    answer:
      "JavaScript has seven primitive data types:\n\n- `string`\n- `number`\n- `bigint`\n- `boolean`\n- `undefined`\n- `symbol`\n- `null`\n\nA primitive represents a single immutable value. Everything that is not a primitive is an object, including arrays and functions.",
    explanation:
      'Primitive values cannot be modified in place and are compared by value:\n\n```js\nconst first = "hello";\nconst second = "hello";\n\nfirst === second; // true\n```',
    details:
      'Each primitive has a particular purpose:\n\n- `string` represents text.\n- `number` represents floating-point numbers, including `NaN` and `Infinity`.\n- `bigint` represents integers larger than the safe range supported by `number`.\n- `boolean` represents `true` or `false`.\n- `undefined` commonly represents a missing or uninitialized value.\n- `symbol` creates unique identifiers.\n- `null` represents an intentional absence of a value.\n\n```js\nconst title = "Developer";\nconst price = 19.99;\nconst largeNumber = 9007199254740993n;\nconst isActive = true;\nconst missing = undefined;\nconst id = Symbol("id");\nconst selectedUser = null;\n```\n\nPrimitive values are immutable:\n\n```js\nlet text = "hello";\ntext.toUpperCase();\n\nconsole.log(text); // "hello"\n```\n\nString methods return new values rather than modifying the original string.\n\nThe `typeof` operator identifies most primitive types:\n\n```js\ntypeof "hello";    // "string"\ntypeof 42;         // "number"\ntypeof 42n;        // "bigint"\ntypeof true;       // "boolean"\ntypeof undefined;  // "undefined"\ntypeof Symbol();   // "symbol"\n```\n\nOne historical JavaScript behavior is:\n\n```js\ntypeof null; // "object"\n```\n\nDespite this result, `null` is a primitive.',
  },
  {
    number: 3,
    question: "What is the difference between null and undefined?",
    answer:
      "`undefined` usually means that a value has not been assigned or does not exist. `null` is an explicit value used to represent the intentional absence of a value.",
    explanation:
      "JavaScript frequently produces `undefined` automatically, while developers normally assign `null` deliberately.\n\n```js\nlet name;          // undefined\nconst user = null; // intentionally empty\n```",
    details:
      'Common situations that produce `undefined` include:\n\n- A declared but uninitialized variable\n- A missing object property\n- A function parameter that was not provided\n- A function with no explicit return value\n\n```js\nlet value;\nconsole.log(value); // undefined\n\nconst user = {};\nconsole.log(user.name); // undefined\n\nfunction greet(name) {\n  console.log(name);\n}\n\ngreet(); // undefined\n\nfunction doSomething() {}\ndoSomething(); // returns undefined\n```\n\n`null` is commonly used when a value is intentionally empty:\n\n```js\nconst state = {\n  selectedUser: null\n};\n```\n\nTheir types behave differently:\n\n```js\ntypeof undefined; // "undefined"\ntypeof null;      // "object"\n```\n\nLoose equality considers them equal, but strict equality does not:\n\n```js\nnull == undefined;  // true\nnull === undefined; // false\n```\n\nA useful nullish check is:\n\n```js\nif (value == null) {\n  // Runs when value is either null or undefined\n}\n```\n\nIn most other comparisons, strict equality should be preferred.',
  },
  {
    number: 4,
    question: "What is the difference between == and ===?",
    answer:
      "The loose equality operator `==` compares values after performing type coercion when necessary. The strict equality operator `===` compares values without converting their types.",
    explanation:
      'Strict equality is more predictable and should generally be preferred:\n\n```js\n5 == "5";  // true\n5 === "5"; // false\n```',
    details:
      'Loose equality follows type-conversion rules that can create surprising results:\n\n```js\n0 == false;          // true\n"" == false;         // true\nnull == undefined;   // true\n" \\t" == 0;          // true\n```\n\nStrict equality requires matching types:\n\n```js\n0 === false;          // false\n"" === false;         // false\nnull === undefined;   // false\n```\n\n`NaN` is not equal to itself with either operator:\n\n```js\nNaN == NaN;  // false\nNaN === NaN; // false\n```\n\nUse `Number.isNaN()` to test for it:\n\n```js\nNumber.isNaN(NaN); // true\n```\n\nObjects are compared by identity rather than by their contents:\n\n```js\n{} === {}; // false\n\nconst user = { name: "Alex" };\nconst sameUser = user;\n\nuser === sameUser; // true\n```\n\nJavaScript also provides `Object.is()`, which differs from `===` in two notable cases:\n\n```js\nObject.is(NaN, NaN); // true\nObject.is(0, -0);    // false\n```',
  },
  {
    number: 5,
    question: "What are truthy and falsy values?",
    answer:
      'A truthy value becomes `true` when evaluated in a Boolean context. A falsy value becomes `false`.\n\nJavaScript’s falsy values are:\n\n- `false`\n- `0`\n- `-0`\n- `0n`\n- `""`\n- `null`\n- `undefined`\n- `NaN`\n\nEvery other value is truthy.',
    explanation:
      'JavaScript performs Boolean coercion in conditions and logical expressions:\n\n```js\nif ("hello") {\n  // Runs because a non-empty string is truthy\n}\n```',
    details:
      'Values that sometimes look empty can still be truthy:\n\n```js\nBoolean([]);      // true\nBoolean({});      // true\nBoolean("false"); // true\nBoolean("0");     // true\n```\n\nLogical operators return operands rather than always returning Boolean values:\n\n```js\n"hello" && 42; // 42\n"" && 42;      // ""\n"" || "guest"; // "guest"\n```\n\nThe `&&` operator returns the first falsy operand or the final operand if all are truthy. The `||` operator returns the first truthy operand or the final operand if all are falsy.\n\nThis can cause problems when `0` or an empty string is a valid value:\n\n```js\nconst count = 0;\nconst result = count || 10;\n\nconsole.log(result); // 10\n```\n\nNullish coalescing only uses the fallback for `null` or `undefined`:\n\n```js\nconst result = count ?? 10;\n\nconsole.log(result); // 0\n```\n\nUse `Boolean(value)` or double negation when an actual Boolean is required:\n\n```js\nBoolean("hello"); // true\n!!"hello";        // true\n```',
  },
  {
    number: 6,
    question: "What is type coercion?",
    answer:
      "Type coercion is the conversion of a value from one data type to another. It can happen automatically through implicit coercion or intentionally through explicit conversion.",
    explanation:
      'JavaScript may automatically convert values when operators receive different types:\n\n```js\n"5" + 2; // "52"\n"5" - 2; // 3\n```\n\nExplicit conversion is usually easier to understand:\n\n```js\nNumber("5"); // 5\nString(5);   // "5"\nBoolean(0);  // false\n```',
    details:
      'Implicit coercion depends on the operator and the values involved.\n\nThe `+` operator performs addition when both operands are numbers. If either operand becomes a string, it performs string concatenation:\n\n```js\n5 + 2;    // 7\n"5" + 2;  // "52"\n5 + "2";  // "52"\n```\n\nMost other arithmetic operators attempt to convert their operands to numbers:\n\n```js\n"10" - 4; // 6\n"10" * 2; // 20\n"10" / 2; // 5\n```\n\nIf a value cannot be converted into a valid number, the result is `NaN`:\n\n```js\nNumber("hello"); // NaN\n"hello" * 2;     // NaN\n```\n\nBoolean coercion occurs in conditions and logical expressions:\n\n```js\nBoolean("");      // false\nBoolean("false"); // true\nBoolean([]);      // true\nBoolean({});      // true\n```\n\nLoose equality performs coercion before comparison, while strict equality does not:\n\n```js\n5 == "5";  // true\n5 === "5"; // false\n```\n\nExplicit conversion communicates intent more clearly and reduces unexpected behavior:\n\n```js\nconst input = "42";\nconst total = Number(input) + 8;\n\nconsole.log(total); // 50\n```',
  },
  {
    number: 7,
    question: "What is hoisting?",
    answer:
      "Hoisting describes how JavaScript creates declarations before executing the code in a scope.\n\nDifferent declarations behave differently:\n\n- Function declarations are initialized with their function definitions.\n- `var` variables are initialized with `undefined`.\n- `let`, `const`, and `class` declarations remain inaccessible until their declarations execute.\n- Function expressions follow the rules of the variables that contain them.",
    explanation:
      'JavaScript does not physically move declarations. Hoisting describes the observable result of declarations being processed before code execution.\n\n```js\nsayHello(); // Works\n\nfunction sayHello() {\n  console.log("Hello");\n}\n```',
    details:
      'A `var` variable can be accessed before its declaration, but its value is initially `undefined`:\n\n```js\nconsole.log(score); // undefined\nvar score = 10;\n```\n\nConceptually, the declaration is processed before execution, but the assignment remains in place:\n\n```js\nvar score;\nconsole.log(score);\nscore = 10;\n```\n\n`let` and `const` are also registered before execution, but accessing them early throws a `ReferenceError`:\n\n```js\nconsole.log(age); // ReferenceError\nlet age = 30;\n```\n\nFunction expressions are not fully initialized early:\n\n```js\ngreet(); // TypeError: greet is not a function\n\nvar greet = function () {\n  console.log("Hello");\n};\n```\n\nWith `let` or `const`, calling the function expression early produces a `ReferenceError`:\n\n```js\ngreet(); // ReferenceError\n\nconst greet = function () {\n  console.log("Hello");\n};\n```\n\nUnderstanding hoisting helps explain why declaration style affects when a variable or function can be used.',
  },
  {
    number: 8,
    question: "What is the temporal dead zone?",
    answer:
      "The temporal dead zone, or TDZ, is the period between entering a scope and executing the declaration of a `let`, `const`, or `class` binding.\n\nDuring this period, the binding belongs to the scope but has not been initialized. Accessing it throws a `ReferenceError`.",
    explanation:
      "The TDZ ends when JavaScript reaches the declaration:\n\n```js\nconsole.log(count); // ReferenceError\n\nlet count = 1;\n\nconsole.log(count); // 1\n```",
    details:
      'A variable in the TDZ can also shadow a variable from an outer scope:\n\n```js\nconst status = "global";\n\n{\n  console.log(status); // ReferenceError\n  const status = "local";\n}\n```\n\nJavaScript does not use the global `status` inside the block because the local binding belongs to that entire block. It remains inaccessible until its declaration.\n\nEven `typeof`, which is normally safe for undeclared variables, throws an error for a variable in the TDZ:\n\n```js\ntypeof unknownVariable; // "undefined"\n\ntypeof value; // ReferenceError\nlet value = 10;\n```\n\nA `const` variable must be initialized at its declaration:\n\n```js\nconst name; // SyntaxError\n```\n\nA `let` variable can be declared without an initial value. It becomes `undefined` after the declaration executes:\n\n```js\nlet name;\nconsole.log(name); // undefined\n```\n\nThe TDZ encourages variables to be declared before use and helps reveal initialization-order mistakes.',
  },
  {
    number: 9,
    question: "What is lexical scope?",
    answer:
      "Lexical scope means that a variable’s accessibility is determined by where variables and functions are written in the source code.\n\nWhen JavaScript resolves a variable, it searches the current scope and then moves outward through the surrounding scopes. This sequence is called the scope chain.",
    explanation:
      'Inner functions can access variables from outer scopes, but outer scopes cannot access variables declared only inside inner scopes.\n\n```js\nconst message = "Hello";\n\nfunction greet() {\n  console.log(message); // Accessible\n}\n```',
    details:
      'JavaScript has several kinds of scope:\n\n- Global scope\n- Module scope\n- Function scope\n- Block scope\n\nA function retains the scope in which it was defined, regardless of where it is called:\n\n```js\nconst name = "Global";\n\nfunction printName() {\n  console.log(name);\n}\n\nfunction run() {\n  const name = "Local";\n  printName();\n}\n\nrun(); // "Global"\n```\n\n`printName` was defined in the global lexical environment, so it uses the global `name`. The fact that it is called inside `run` does not change its scope.\n\nNested functions can search through multiple outer scopes:\n\n```js\nconst globalValue = "global";\n\nfunction outer() {\n  const outerValue = "outer";\n\n  function inner() {\n    const innerValue = "inner";\n\n    console.log(innerValue);\n    console.log(outerValue);\n    console.log(globalValue);\n  }\n\n  inner();\n}\n```\n\nScope lookup moves outward only. `outer` cannot access `innerValue`.\n\nLexical scope is also the foundation of closures because a function retains access to the environment where it was created.',
  },
  {
    number: 10,
    question: "What is a closure?",
    answer:
      "A closure is the combination of a function and the lexical environment in which that function was created.\n\nIt allows a function to retain access to variables from its outer scope even after the outer function has finished executing.",
    explanation:
      "Closures are commonly used for private state, callbacks, function factories, memoization, and partial application.\n\n```js\nfunction createCounter() {\n  let count = 0;\n\n  return () => ++count;\n}\n```",
    details:
      'The returned function continues to access `count` after `createCounter` has completed:\n\n```js\nconst counter = createCounter();\n\ncounter(); // 1\ncounter(); // 2\ncounter(); // 3\n```\n\nEach invocation of the outer function creates a separate lexical environment:\n\n```js\nconst firstCounter = createCounter();\nconst secondCounter = createCounter();\n\nfirstCounter();  // 1\nfirstCounter();  // 2\nsecondCounter(); // 1\n```\n\nClosures can create private state:\n\n```js\nfunction createAccount(initialBalance) {\n  let balance = initialBalance;\n\n  return {\n    deposit(amount) {\n      balance += amount;\n    },\n    getBalance() {\n      return balance;\n    }\n  };\n}\n\nconst account = createAccount(100);\naccount.deposit(50);\naccount.getBalance(); // 150\n```\n\nThe `balance` variable cannot be accessed directly from outside the returned methods.\n\nClosures are also created in event handlers:\n\n```js\nfunction attachHandler(button, message) {\n  button.addEventListener("click", () => {\n    console.log(message);\n  });\n}\n```\n\nThe handler retains access to `message`.\n\nClosures may keep referenced data in memory for as long as the closure remains reachable. Unnecessary long-lived closures can therefore contribute to memory usage.',
  },
  {
    number: 11,
    question: "What is variable shadowing?",
    answer:
      "Variable shadowing occurs when an inner scope declares a variable with the same name as a variable in an outer scope.\n\nInside the inner scope, that name resolves to the inner variable. The outer variable still exists and becomes accessible again after execution leaves the inner scope.",
    explanation:
      'Shadowing is valid, but excessive shadowing can make code confusing because the same name represents different values in nearby scopes.\n\n```js\nconst name = "Alex";\n\nfunction greet() {\n  const name = "Sam";\n  console.log(name); // "Sam"\n}\n```',
    details:
      'Shadowing can occur in functions and blocks:\n\n```js\nconst status = "offline";\n\n{\n  const status = "online";\n  console.log(status); // "online"\n}\n\nconsole.log(status); // "offline"\n```\n\nFunction parameters can also shadow outer variables:\n\n```js\nconst user = "Alex";\n\nfunction greet(user) {\n  console.log(user);\n}\n\ngreet("Sam"); // "Sam"\n```\n\nNot every combination of declarations is allowed. For example, a `let` variable cannot be redeclared with `var` in the same scope:\n\n```js\nlet count = 1;\nvar count = 2; // SyntaxError\n```\n\nShadowing is different from reassignment:\n\n```js\nlet score = 10;\n\n{\n  score = 20; // Reassigns the outer variable\n}\n\nconsole.log(score); // 20\n```\n\nDeclaring `let score = 20` inside the block would create a separate shadowing variable instead.\n\nClear, specific names are usually preferable when shadowing could make the code difficult to follow.',
  },
  {
    number: 12,
    question: "What is the difference between passing values by value and by reference?",
    answer:
      "JavaScript always passes function arguments by value.\n\n- For a primitive, the copied value is the primitive itself.\n- For an object, array, or function, the copied value is a reference identifying the same object.\n\nA function can mutate an object through the copied reference, but reassigning the parameter does not change the caller’s variable.",
    explanation:
      'JavaScript is accurately described as pass-by-value, including when the value being copied is an object reference.\n\n```js\nfunction update(user) {\n  user.name = "Sam";\n  user = { name: "Lee" };\n}\n```',
    details:
      'Reassigning a primitive parameter does not affect the original variable:\n\n```js\nfunction change(value) {\n  value = 20;\n}\n\nlet score = 10;\nchange(score);\n\nconsole.log(score); // 10\n```\n\nThe function receives its own copy of the number.\n\nWith an object, the parameter and the caller’s variable initially contain copies of the same reference:\n\n```js\nfunction update(user) {\n  user.name = "Sam";\n}\n\nconst person = { name: "Alex" };\nupdate(person);\n\nconsole.log(person.name); // "Sam"\n```\n\nBoth references identify the same object, so mutation is visible to the caller.\n\nReassigning the parameter only replaces the function’s local reference:\n\n```js\nfunction replace(user) {\n  user = { name: "Lee" };\n}\n\nconst person = { name: "Alex" };\nreplace(person);\n\nconsole.log(person.name); // "Alex"\n```\n\nThis distinction also applies to arrays:\n\n```js\nfunction addItem(items) {\n  items.push("new"); // Mutates the original array\n}\n\nfunction replaceItems(items) {\n  items = []; // Reassigns only the local parameter\n}\n```\n\nTo avoid unwanted mutation, create a new object or array:\n\n```js\nfunction rename(user, name) {\n  return { ...user, name };\n}\n```',
  },
  {
    number: 13,
    question: "How do shallow and deep copies differ?",
    answer:
      "A shallow copy creates a new top-level object or array but reuses references to nested objects. A deep copy creates independent copies of nested data as well.\n\nModifying a nested value in a shallow copy may affect the original. Modifying a deep copy does not.",
    explanation:
      "Spread syntax, `Object.assign()`, `Array.from()`, and `slice()` create shallow copies. `structuredClone()` can deeply copy many supported values.\n\n```js\nconst copy = { ...original };\n```",
    details:
      'A shallow copy duplicates only the first level:\n\n```js\nconst original = {\n  name: "Alex",\n  settings: {\n    theme: "light"\n  }\n};\n\nconst copy = { ...original };\n\ncopy.name = "Sam";\ncopy.settings.theme = "dark";\n\nconsole.log(original.name);           // "Alex"\nconsole.log(original.settings.theme); // "dark"\n```\n\n`name` is independent because it is a primitive top-level property. The nested `settings` object remains shared.\n\n`structuredClone()` creates a deep copy:\n\n```js\nconst deepCopy = structuredClone(original);\n\ndeepCopy.settings.theme = "blue";\n\nconsole.log(original.settings.theme); // "dark"\n```\n\nIt supports many types, including:\n\n- Objects and arrays\n- `Date`\n- `Map` and `Set`\n- Typed arrays\n- Circular references\n\nIt cannot clone every value. Functions, DOM nodes, and some platform-specific objects are not supported.\n\nConverting through JSON is sometimes used as a simple deep-copy technique:\n\n```js\nconst copy = JSON.parse(JSON.stringify(original));\n```\n\nHowever, this approach loses or changes unsupported data such as `undefined`, functions, symbols, `Date`, `Map`, `Set`, `BigInt`, and circular references. It should not be treated as a general-purpose cloning solution.',
  },
  {
    number: 14,
    question: "What is destructuring?",
    answer:
      "Destructuring is syntax that extracts values from arrays or properties from objects and assigns them to variables.\n\nObject destructuring matches values by property name, while array destructuring matches values by position.",
    explanation:
      'Destructuring provides a concise way to access selected data:\n\n```js\nconst user = { name: "Alex", age: 30 };\nconst { name, age } = user;\n\nconst colors = ["red", "blue"];\nconst [primary, secondary] = colors;\n```',
    details:
      'Object properties can be renamed:\n\n```js\nconst user = { name: "Alex" };\nconst { name: displayName } = user;\n\nconsole.log(displayName); // "Alex"\n```\n\nDefault values are used when the extracted value is `undefined`:\n\n```js\nconst user = {};\nconst { role = "guest" } = user;\n\nconsole.log(role); // "guest"\n```\n\nA default does not replace `null`:\n\n```js\nconst user = { role: null };\nconst { role = "guest" } = user;\n\nconsole.log(role); // null\n```\n\nArray elements can be skipped:\n\n```js\nconst colors = ["red", "green", "blue"];\nconst [primary, , tertiary] = colors;\n\nconsole.log(tertiary); // "blue"\n```\n\nNested values can be destructured:\n\n```js\nconst user = {\n  profile: {\n    email: "alex@example.com"\n  }\n};\n\nconst {\n  profile: { email }\n} = user;\n```\n\nRest syntax collects the remaining values:\n\n```js\nconst [first, ...remaining] = [1, 2, 3, 4];\n// first: 1\n// remaining: [2, 3, 4]\n```\n\nDestructuring is also useful in function parameters:\n\n```js\nfunction printUser({ name, role = "guest" }) {\n  console.log(`${name}: ${role}`);\n}\n```\n\nNested destructuring can throw an error if an expected parent value is missing, so defaults or optional access may be necessary when working with uncertain data.',
  },
  {
    number: 15,
    question: "What are rest and spread syntax, and how do they differ?",
    answer:
      "Rest and spread both use `...`, but they perform opposite operations:\n\n- Spread expands an iterable or object into individual values or properties.\n- Rest collects multiple remaining values or properties into a new array or object.\n\nTheir meaning is determined by where the syntax appears.",
    explanation:
      "Spread expands values, while rest collects them:\n\n```js\nconst numbers = [1, 2, 3];\nconst copy = [...numbers]; // Spread\n\nfunction sum(...values) {  // Rest\n  return values.reduce((total, value) => total + value, 0);\n}\n```",
    details:
      'Array spread can copy or combine arrays:\n\n```js\nconst first = [1, 2];\nconst second = [3, 4];\n\nconst combined = [...first, ...second];\n// [1, 2, 3, 4]\n```\n\nFunction-call spread passes iterable values as separate arguments:\n\n```js\nconst numbers = [4, 8, 2];\n\nMath.max(...numbers); // 8\n```\n\nObject spread copies enumerable own properties:\n\n```js\nconst defaults = {\n  theme: "light",\n  language: "en"\n};\n\nconst settings = {\n  ...defaults,\n  theme: "dark"\n};\n```\n\nLater properties override earlier properties, so `settings.theme` becomes `"dark"`.\n\nRest parameters collect arguments into a real array:\n\n```js\nfunction average(...numbers) {\n  const total = numbers.reduce(\n    (sum, number) => sum + number,\n    0\n  );\n\n  return total / numbers.length;\n}\n```\n\nRest syntax can collect remaining destructured values:\n\n```js\nconst user = {\n  id: 1,\n  name: "Alex",\n  role: "admin"\n};\n\nconst { id, ...details } = user;\n```\n\nSpread creates shallow copies. Nested objects remain shared:\n\n```js\nconst original = {\n  settings: { dark: false }\n};\n\nconst copy = { ...original };\ncopy.settings.dark = true;\n\nconsole.log(original.settings.dark); // true\n```',
  },
  {
    number: 16,
    question: "What are template literals?",
    answer:
      "Template literals are strings enclosed by backticks.\n\nThey support:\n\n- Expression interpolation with `${expression}`\n- Multiline strings\n- Tagged templates that process the literal through a function",
    explanation:
      'Template literals make dynamic strings more readable:\n\n```js\nconst name = "Alex";\nconst message = `Hello, ${name}!`;\n```',
    details:
      'Any valid expression can appear inside `${...}`:\n\n```js\nconst price = 20;\nconst quantity = 3;\n\nconst message = `Total: $${price * quantity}`;\n// "Total: $60"\n```\n\nProperties and function calls can also be interpolated:\n\n```js\nconst user = { name: "Alex" };\n\nconst text = `Welcome, ${user.name.toUpperCase()}!`;\n```\n\nTemplate literals can span multiple lines:\n\n```js\nconst content = `First line\nSecond line\nThird line`;\n```\n\nTagged templates pass the literal’s static strings and evaluated expressions to a function:\n\n```js\nfunction inspect(strings, ...values) {\n  console.log(strings);\n  console.log(values);\n}\n\nconst name = "Alex";\nconst count = 3;\n\ninspect`${name} has ${count} messages`;\n```\n\nTagged templates can be used for escaping content, localization, formatting, or domain-specific syntax.\n\nInterpolation does not automatically make untrusted content safe. When inserting user-controlled values into HTML, developers must still use appropriate escaping or safe DOM APIs to prevent cross-site scripting.',
  },
  {
    number: 17,
    question: "What are JavaScript modules, and how do import and export work?",
    answer:
      "JavaScript modules divide an application into separate files with explicit dependencies and their own top-level scope.\n\n`export` exposes values from a module, and `import` makes those values available in another module.\n\nA module may have multiple named exports but only one default export.",
    explanation:
      'Modules improve code organization, reuse, dependency management, and encapsulation.\n\n```js\n// math.js\nexport const add = (a, b) => a + b;\n\n// app.js\nimport { add } from "./math.js";\n```',
    details:
      'Named exports use the exported names:\n\n```js\n// math.js\nexport const add = (a, b) => a + b;\nexport const subtract = (a, b) => a - b;\n```\n\nThey are imported with braces:\n\n```js\nimport { add, subtract } from "./math.js";\n```\n\nA named import can be aliased:\n\n```js\nimport { add as sum } from "./math.js";\n```\n\nA default export can be imported using any local name:\n\n```js\n// logger.js\nexport default function log(message) {\n  console.log(message);\n}\n```\n\n```js\nimport writeLog from "./logger.js";\n```\n\nNamed and default imports can be combined:\n\n```js\n// math.js\nexport default function multiply(a, b) {\n  return a * b;\n}\n\nexport const add = (a, b) => a + b;\n```\n\n```js\nimport multiply, { add } from "./math.js";\n```\n\nStatic imports are resolved before the module executes and normally appear at the top level. Dynamic `import()` loads a module asynchronously:\n\n```js\nconst module = await import("./analytics.js");\nmodule.trackPageView();\n```\n\nDynamic imports are useful for conditional loading and code splitting.\n\nImported bindings are live bindings. If the exporting module updates an exported variable, importing modules observe its current value:\n\n```js\n// counter.js\nexport let count = 0;\n\nexport function increment() {\n  count++;\n}\n```\n\nModules are evaluated once, and repeated imports reuse the same module instance.\n\nIn browsers, modules are loaded with:\n\n```html\n\u003cscript type="module" src="./app.js">\u003c/script>\n```\n\nBrowser modules use strict mode automatically, have their own scope, and are deferred by default.',
  },
  {
    number: 18,
    question: "What is the difference between a function declaration and a function expression?",
    answer:
      "A function declaration defines a named function as a standalone statement. A function expression creates a function as part of an expression and usually assigns it to a variable.\n\nFunction declarations are fully initialized during hoisting. Function expressions follow the initialization rules of the variables that contain them.",
    explanation:
      'A function declaration can be called before it appears in the code. A function expression normally cannot.\n\n```js\ngreet(); // Works\n\nfunction greet() {\n  console.log("Hello");\n}\n```',
    details:
      'A function declaration uses this syntax:\n\n```js\nfunction add(a, b) {\n  return a + b;\n}\n```\n\nA function expression can be anonymous:\n\n```js\nconst add = function (a, b) {\n  return a + b;\n};\n```\n\nIt can also have an internal name:\n\n```js\nconst calculate = function add(a, b) {\n  return a + b;\n};\n```\n\nWhen a function expression is stored in `const`, the variable is in the temporal dead zone before its declaration:\n\n```js\ngreet(); // ReferenceError\n\nconst greet = function () {\n  console.log("Hello");\n};\n```\n\nWith `var`, the variable is initialized with `undefined`, so calling it early produces a `TypeError`:\n\n```js\ngreet(); // TypeError: greet is not a function\n\nvar greet = function () {\n  console.log("Hello");\n};\n```\n\nFunction expressions are useful when functions are passed as values, used as callbacks, or created conditionally.',
  },
  {
    number: 19,
    question: "How do arrow functions differ from regular functions?",
    answer:
      "Arrow functions provide shorter syntax and do not create their own `this`, `arguments`, `super`, or `new.target`.\n\nTheir `this` value is inherited lexically from the surrounding scope. Arrow functions also cannot be used as constructors and do not have a `prototype` property for constructing instances.",
    explanation:
      "Arrow functions are useful for callbacks and functions that should preserve the surrounding `this`. Regular functions are usually more appropriate for object methods and constructors.\n\n```js\nconst double = number => number * 2;\n```",
    details:
      'Arrow functions can use an implicit return when the body contains one expression:\n\n```js\nconst add = (a, b) => a + b;\n```\n\nCurly braces require an explicit `return`:\n\n```js\nconst add = (a, b) => {\n  return a + b;\n};\n```\n\nReturning an object implicitly requires parentheses:\n\n```js\nconst createUser = name => ({ name });\n```\n\nArrow functions inherit `this` from their surrounding scope:\n\n```js\nconst timer = {\n  seconds: 0,\n\n  start() {\n    setInterval(() => {\n      this.seconds++;\n    }, 1000);\n  }\n};\n```\n\nThe callback uses the same `this` as `start`.\n\nAn arrow function is usually unsuitable as an object method when dynamic `this` is required:\n\n```js\nconst user = {\n  name: "Alex",\n  greet: () => {\n    console.log(this.name);\n  }\n};\n```\n\nThe arrow function does not receive `user` as its `this`.\n\nArrow functions cannot be called with `new`:\n\n```js\nconst User = name => {\n  this.name = name;\n};\n\nnew User("Alex"); // TypeError\n```\n\nThey also do not have their own `arguments` object. Rest parameters should be used instead:\n\n```js\nconst sum = (...numbers) =>\n  numbers.reduce((total, number) => total + number, 0);\n```',
  },
  {
    number: 20,
    question: "How does the this keyword work?",
    answer:
      "In a regular function, `this` is usually determined by how the function is called, not where it is defined.\n\nIts value can come from:\n\n- A method call\n- A constructor call with `new`\n- An explicit call using `call`, `apply`, or `bind`\n- The default function-calling rule\n- The surrounding lexical scope for an arrow function",
    explanation:
      'When a function is called as an object method, `this` normally refers to the object before the dot.\n\n```js\nconst user = {\n  name: "Alex",\n  greet() {\n    console.log(this.name);\n  }\n};\n\nuser.greet(); // "Alex"\n```',
    details:
      'A standalone function call uses the default binding:\n\n```js\n"use strict";\n\nfunction showThis() {\n  console.log(this);\n}\n\nshowThis(); // undefined\n```\n\nIn older non-strict browser scripts, the default value may be the global object.\n\nA method can lose its receiver when it is separated from its object:\n\n```js\nconst greet = user.greet;\ngreet(); // `this` is no longer `user`\n```\n\nExplicit binding can provide a value:\n\n```js\ngreet.call(user); // "Alex"\n```\n\nConstructor calls create a new object and bind it to `this`:\n\n```js\nfunction User(name) {\n  this.name = name;\n}\n\nconst user = new User("Alex");\n```\n\nArrow functions do not create their own `this`:\n\n```js\nconst user = {\n  name: "Alex",\n\n  delayedGreeting() {\n    setTimeout(() => {\n      console.log(this.name);\n    }, 100);\n  }\n};\n```\n\nBinding precedence is generally:\n\n1. Constructor binding with `new`\n2. Explicit binding with `bind`, `call`, or `apply`\n3. Method binding\n4. Default binding\n\nA function created by `bind` keeps its bound `this`, although calling that bound function with `new` uses the newly created instance.',
  },
  {
    number: 21,
    question: "What do call, apply, and bind do?",
    answer:
      "`call`, `apply`, and `bind` let developers control the `this` value used by a regular function.\n\n- `call` invokes the function immediately with arguments listed separately.\n- `apply` invokes the function immediately with arguments provided as an array or array-like value.\n- `bind` returns a new function with a fixed `this` value and optional preset arguments.",
    explanation:
      '`call` and `apply` execute a function immediately. `bind` creates a function that can be executed later.\n\n```js\ngreet.call(user, "Hello");\ngreet.apply(user, ["Hello"]);\nconst boundGreet = greet.bind(user);\n```',
    details:
      'Consider this function:\n\n```js\nfunction introduce(greeting, punctuation) {\n  return `${greeting}, I am ${this.name}${punctuation}`;\n}\n\nconst user = { name: "Alex" };\n```\n\n`call` accepts individual arguments:\n\n```js\nintroduce.call(user, "Hello", "!");\n// "Hello, I am Alex!"\n```\n\n`apply` accepts them as a collection:\n\n```js\nintroduce.apply(user, ["Hello", "!"]);\n// "Hello, I am Alex!"\n```\n\n`bind` creates a new function:\n\n```js\nconst introduceAlex = introduce.bind(user);\n\nintroduceAlex("Hello", "!");\n// "Hello, I am Alex!"\n```\n\n`bind` can also preset arguments, which is called partial application:\n\n```js\nconst sayHelloToAlex = introduce.bind(\n  user,\n  "Hello"\n);\n\nsayHelloToAlex("!");\n```\n\nThese methods cannot replace the lexical `this` of an arrow function:\n\n```js\nconst showThis = () => this;\n\nshowThis.call(user); // Does not change arrow `this`\n```',
  },
  {
    number: 22,
    question: "What is a higher-order function?",
    answer:
      "A higher-order function is a function that accepts one or more functions as arguments, returns a function, or does both.\n\nHigher-order functions support composition and abstraction by allowing behavior to be treated as data.",
    explanation:
      "Common array methods such as `map`, `filter`, and `reduce` are higher-order functions because they receive callback functions.\n\n```js\nconst doubled = [1, 2, 3].map(\n  number => number * 2\n);\n```",
    details:
      'A function that receives another function is higher-order:\n\n```js\nfunction repeat(times, action) {\n  for (let index = 0; index \u003c times; index++) {\n    action(index);\n  }\n}\n\nrepeat(3, index => {\n  console.log(index);\n});\n```\n\nA function that returns a function is also higher-order:\n\n```js\nfunction multiplyBy(multiplier) {\n  return number => number * multiplier;\n}\n\nconst double = multiplyBy(2);\nconst triple = multiplyBy(3);\n\ndouble(5); // 10\ntriple(5); // 15\n```\n\nHigher-order functions can separate reusable control flow from changing behavior:\n\n```js\nfunction withLogging(action) {\n  return (...argumentsList) => {\n    console.log("Starting");\n    const result = action(...argumentsList);\n    console.log("Finished");\n    return result;\n  };\n}\n```\n\nThis pattern is useful for validation, authorization, caching, logging, retries, and event handling.',
  },
  {
    number: 23,
    question: "What is a callback function?",
    answer:
      "A callback is a function passed to another function so that the receiving function can execute it at an appropriate time.\n\nCallbacks may run synchronously, such as callbacks passed to `map`, or asynchronously, such as event handlers and timer callbacks.",
    explanation:
      'A callback allows one function to receive customizable behavior.\n\n```js\nfunction processUser(user, callback) {\n  callback(user);\n}\n\nprocessUser({ name: "Alex" }, user => {\n  console.log(user.name);\n});\n```',
    details:
      'Synchronous callbacks execute before the surrounding function returns:\n\n```js\nconst numbers = [1, 2, 3];\n\nconst doubled = numbers.map(number => {\n  return number * 2;\n});\n```\n\nAsynchronous callbacks execute later:\n\n```js\nsetTimeout(() => {\n  console.log("Executed later");\n}, 1000);\n```\n\nEvent listeners also use callbacks:\n\n```js\nbutton.addEventListener("click", event => {\n  console.log(event.target);\n});\n```\n\nTraditional asynchronous code can become deeply nested:\n\n```js\ngetUser(userId, user => {\n  getOrders(user.id, orders => {\n    getOrderDetails(orders[0].id, details => {\n      console.log(details);\n    });\n  });\n});\n```\n\nThis is often called callback hell. Promises and `async`/`await` make many asynchronous workflows easier to compose and handle.\n\nError-first callbacks are common in older Node.js APIs:\n\n```js\noperation((error, result) => {\n  if (error) {\n    console.error(error);\n    return;\n  }\n\n  console.log(result);\n});\n```',
  },
  {
    number: 24,
    question: "What are pure functions and side effects?",
    answer:
      "A pure function always returns the same output for the same inputs and does not modify or depend on external mutable state.\n\nA side effect is any observable interaction with something outside the function’s returned value.",
    explanation:
      "Pure functions are easier to test, reuse, cache, and reason about.\n\n```js\nfunction add(a, b) {\n  return a + b;\n}\n```",
    details:
      "Common side effects include:\n\n- Modifying an external variable\n- Mutating an argument\n- Updating the DOM\n- Writing to storage\n- Sending a network request\n- Logging to the console\n- Reading the current time\n- Generating a random value\n\nThis function is impure because it changes external state:\n\n```js\nlet total = 0;\n\nfunction addToTotal(amount) {\n  total += amount;\n  return total;\n}\n```\n\nThis version is pure:\n\n```js\nfunction addToTotal(total, amount) {\n  return total + amount;\n}\n```\n\nMutation can also make a function impure:\n\n```js\nfunction addUser(users, user) {\n  users.push(user);\n  return users;\n}\n```\n\nA non-mutating version returns a new array:\n\n```js\nfunction addUser(users, user) {\n  return [...users, user];\n}\n```\n\nReal applications require side effects. The goal is usually to isolate them from pure business logic rather than eliminate them entirely.\n\nFor example, data transformation can remain pure while a separate function handles the network request or DOM update.",
  },
  {
    number: 25,
    question: "What is an immediately invoked function expression (IIFE)?",
    answer:
      "An IIFE is a function expression that executes immediately after it is created.\n\nIt was traditionally used to create a private scope and prevent variables from leaking into the global scope.",
    explanation:
      'Parentheses turn the function declaration-like syntax into an expression, and the final parentheses invoke it.\n\n```js\n(function () {\n  const message = "Private";\n  console.log(message);\n})();\n```',
    details:
      'An arrow function can also be used:\n\n```js\n(() => {\n  const value = 42;\n  console.log(value);\n})();\n```\n\nAn IIFE can return a value:\n\n```js\nconst result = (() => {\n  const first = 10;\n  const second = 20;\n\n  return first + second;\n})();\n\nconsole.log(result); // 30\n```\n\nIIFEs were commonly used in the module pattern:\n\n```js\nconst counter = (() => {\n  let count = 0;\n\n  return {\n    increment() {\n      count++;\n    },\n    getCount() {\n      return count;\n    }\n  };\n})();\n```\n\nThe `count` variable remains private.\n\nModern JavaScript modules and block-scoped variables have reduced the need for IIFEs. However, they can still be useful for one-time initialization or creating an isolated scope in older code.\n\nAn async IIFE allows `await` in environments where top-level `await` is unavailable:\n\n```js\n(async () => {\n  const response = await fetch("/api/users");\n  const users = await response.json();\n\n  console.log(users);\n})();\n```',
  },
  {
    number: 26,
    question: "How do default parameters work?",
    answer:
      "Default parameters provide fallback values for function parameters when the corresponding argument is missing or explicitly set to `undefined`.\n\nThe default expression is evaluated when the function is called, not when it is defined.",
    explanation:
      'A default value is not used when the argument is `null`, `false`, `0`, or an empty string.\n\n```js\nfunction greet(name = "Guest") {\n  return `Hello, ${name}`;\n}\n\ngreet();          // "Hello, Guest"\ngreet(undefined); // "Hello, Guest"\ngreet(null);      // "Hello, null"\n```',
    details:
      'Default expressions can reference earlier parameters:\n\n```js\nfunction createRange(start, end = start + 10) {\n  return { start, end };\n}\n\ncreateRange(5); // { start: 5, end: 15 }\n```\n\nA later parameter cannot safely be referenced before it has been initialized:\n\n```js\nfunction example(first = second, second = 2) {\n  return first + second;\n}\n\nexample(); // ReferenceError\n```\n\nDefault expressions can call functions:\n\n```js\nfunction generateId() {\n  return crypto.randomUUID();\n}\n\nfunction createUser(name, id = generateId()) {\n  return { name, id };\n}\n```\n\n`generateId()` runs each time the default is needed.\n\nDestructured parameters can have defaults:\n\n```js\nfunction configure({\n  theme = "light",\n  language = "en"\n} = {}) {\n  return { theme, language };\n}\n\nconfigure(); // Works because the object defaults to {}\n```\n\nWithout `= {}`, calling `configure()` would attempt to destructure `undefined` and throw an error.',
  },
  {
    number: 27,
    question: "How do JavaScript objects inherit properties?",
    answer:
      "JavaScript objects inherit properties and methods through prototypes.\n\nEvery ordinary object has an internal prototype link to another object or to `null`. When a property is not found directly on an object, JavaScript follows this link through the prototype chain.",
    explanation:
      'Inheritance allows multiple objects to share behavior without copying the same methods onto every instance.\n\n```js\nconst animal = {\n  speak() {\n    console.log("Sound");\n  }\n};\n\nconst dog = Object.create(animal);\ndog.speak();\n```',
    details:
      'An object can have both own properties and inherited properties:\n\n```js\nconst animal = {\n  type: "animal"\n};\n\nconst dog = Object.create(animal);\ndog.name = "Rex";\n\ndog.name; // Own property\ndog.type; // Inherited property\n```\n\n`Object.hasOwn()` checks whether a property belongs directly to the object:\n\n```js\nObject.hasOwn(dog, "name"); // true\nObject.hasOwn(dog, "type"); // false\n```\n\nConstructor functions typically place shared methods on their `prototype` object:\n\n```js\nfunction User(name) {\n  this.name = name;\n}\n\nUser.prototype.greet = function () {\n  return `Hello, ${this.name}`;\n};\n\nconst user = new User("Alex");\nuser.greet();\n```\n\nThe `new` operator creates an object whose prototype points to `User.prototype`.\n\nClasses use the same prototype-based mechanism:\n\n```js\nclass User {\n  constructor(name) {\n    this.name = name;\n  }\n\n  greet() {\n    return `Hello, ${this.name}`;\n  }\n}\n```\n\nClass syntax makes the pattern more readable but does not replace prototype-based inheritance with a separate inheritance model.',
  },
  {
    number: 28,
    question: "What is the prototype chain?",
    answer:
      "The prototype chain is the sequence of objects JavaScript searches when resolving a property.\n\nJavaScript first checks the object itself. If the property is not found, it checks the object’s prototype and continues upward until it finds the property or reaches `null`.",
    explanation:
      "The prototype chain enables inherited properties and shared methods.\n\n```js\nconst user = {};\nuser.toString();\n```\n\n`toString` is found through `Object.prototype`.",
    details:
      'Consider an array:\n\n```js\nconst numbers = [1, 2, 3];\n```\n\nA simplified version of its prototype chain is:\n\n```text\nnumbers\n  → Array.prototype\n  → Object.prototype\n  → null\n```\n\n`numbers.map` is found on `Array.prototype`, while `numbers.toString` can ultimately be found through the chain.\n\nAn own property shadows an inherited property with the same name:\n\n```js\nconst parent = {\n  role: "parent"\n};\n\nconst child = Object.create(parent);\nchild.role = "child";\n\nconsole.log(child.role); // "child"\n```\n\nThe prototype can be inspected using:\n\n```js\nObject.getPrototypeOf(child);\n```\n\nAn object without `Object.prototype` can be created with:\n\n```js\nconst dictionary = Object.create(null);\n```\n\nThis object does not inherit methods such as `toString` or `hasOwnProperty`.\n\nDirectly changing prototypes with `Object.setPrototypeOf()` can harm performance. Prototypes are usually established when objects are created through classes, constructor functions, object literals, or `Object.create()`.',
  },
  {
    number: 29,
    question: "What is the difference between a class and a constructor function?",
    answer:
      "Classes and constructor functions can both create objects that share methods through prototypes.\n\nJavaScript classes provide clearer syntax for constructors, instance methods, static methods, inheritance, and private fields. Internally, they still use prototype-based inheritance.",
    explanation:
      "Classes are primarily a more structured syntax over JavaScript’s existing constructor and prototype mechanisms.\n\n```js\nclass User {\n  constructor(name) {\n    this.name = name;\n  }\n\n  greet() {\n    return `Hello, ${this.name}`;\n  }\n}\n```",
    details:
      'The constructor-function equivalent is:\n\n```js\nfunction User(name) {\n  this.name = name;\n}\n\nUser.prototype.greet = function () {\n  return `Hello, ${this.name}`;\n};\n```\n\nBoth versions allow this:\n\n```js\nconst user = new User("Alex");\nuser.greet();\n```\n\nImportant differences include:\n\n- Class declarations are in the temporal dead zone.\n- Class constructors must be called with `new`.\n- Class bodies run in strict mode.\n- Class methods are non-enumerable.\n- Classes support `extends`, `super`, static members, and private fields with concise syntax.\n\nCalling a class without `new` throws an error:\n\n```js\nUser("Alex"); // TypeError\n```\n\nA traditional constructor function may be called without `new`, although doing so is usually a bug.\n\nClasses support private fields:\n\n```js\nclass Account {\n  #balance = 0;\n\n  deposit(amount) {\n    this.#balance += amount;\n  }\n\n  getBalance() {\n    return this.#balance;\n  }\n}\n```\n\nPrivate fields cannot be accessed directly outside the class.',
  },
  {
    number: 30,
    question: "What is the difference between static and instance methods?",
    answer:
      "An instance method is called on an object created by a class and can access that instance through `this`.\n\nA static method belongs to the class itself rather than to individual instances. It is called using the class name.",
    explanation:
      'Instance methods operate on instance-specific data. Static methods usually provide utilities or class-level behavior.\n\n```js\nclass User {\n  greet() {\n    return "Hello";\n  }\n\n  static createGuest() {\n    return new User();\n  }\n}\n```',
    details:
      'Instance methods are stored on the class prototype:\n\n```js\nclass User {\n  constructor(name) {\n    this.name = name;\n  }\n\n  greet() {\n    return `Hello, ${this.name}`;\n  }\n}\n\nconst user = new User("Alex");\nuser.greet();\n```\n\nThe instance method is shared rather than recreated for every instance:\n\n```js\nuser.greet === User.prototype.greet; // true\n```\n\nStatic methods are called on the class:\n\n```js\nclass User {\n  static fromJSON(json) {\n    const data = JSON.parse(json);\n    return new User(data.name);\n  }\n\n  constructor(name) {\n    this.name = name;\n  }\n}\n\nconst user = User.fromJSON(\'{"name":"Alex"}\');\n```\n\nAn instance does not inherit static methods:\n\n```js\nuser.fromJSON(); // TypeError\n```\n\nInside a static method, `this` normally refers to the class on which the method was called:\n\n```js\nclass Base {\n  static create() {\n    return new this();\n  }\n}\n\nclass Child extends Base {}\n\nChild.create(); // Creates a Child instance\n```\n\nStatic properties are also useful for class-level constants or shared state, although shared mutable state should be used carefully.',
  },
  {
    number: 31,
    question: "What are getters and setters?",
    answer:
      "Getters and setters are methods that control how an object property is read or assigned while allowing it to be used with ordinary property syntax.\n\nA getter runs when the property is read. A setter runs when a value is assigned to the property.",
    explanation:
      'Getters can compute values, while setters can validate or normalize assignments.\n\n```js\nconst user = {\n  firstName: "Alex",\n  lastName: "Smith",\n\n  get fullName() {\n    return `${this.firstName} ${this.lastName}`;\n  }\n};\n```',
    details:
      'The getter is accessed like a property, not called like a method:\n\n```js\nuser.fullName; // "Alex Smith"\n```\n\nA setter can validate data:\n\n```js\nclass Temperature {\n  #celsius = 0;\n\n  get celsius() {\n    return this.#celsius;\n  }\n\n  set celsius(value) {\n    if (!Number.isFinite(value)) {\n      throw new TypeError("Temperature must be a number");\n    }\n\n    this.#celsius = value;\n  }\n}\n```\n\nIt is used with assignment syntax:\n\n```js\nconst temperature = new Temperature();\n\ntemperature.celsius = 25;\nconsole.log(temperature.celsius); // 25\n```\n\nA getter can expose a derived value:\n\n```js\nclass Rectangle {\n  constructor(width, height) {\n    this.width = width;\n    this.height = height;\n  }\n\n  get area() {\n    return this.width * this.height;\n  }\n}\n```\n\nGetters should generally avoid surprising side effects because property access looks like a simple read.\n\nSetters accept exactly one parameter. If only a getter is defined, the property is read-only through that accessor.\n\nAccessors can be defined in object literals, classes, or with `Object.defineProperty()`.',
  },
  {
    number: 32,
    question: "What is the difference between mutable and immutable operations?",
    answer:
      "A mutable operation changes an existing object or array. An immutable operation creates and returns a new value without changing the original.\n\nJavaScript primitives are immutable, but objects and arrays are mutable by default.",
    explanation:
      "Immutable updates make state changes easier to track and are especially useful in frontend state management.\n\n```js\nconst original = [1, 2];\nconst updated = [...original, 3];\n```",
    details:
      'Common mutating array methods include:\n\n- `push`\n- `pop`\n- `shift`\n- `unshift`\n- `splice`\n- `sort`\n- `reverse`\n- `fill`\n\n```js\nconst numbers = [3, 1, 2];\nnumbers.sort();\n\nconsole.log(numbers); // [1, 2, 3]\n```\n\nCommon non-mutating array methods include:\n\n- `map`\n- `filter`\n- `slice`\n- `concat`\n- `toSorted`\n- `toReversed`\n- `toSpliced`\n\n```js\nconst numbers = [3, 1, 2];\nconst sorted = numbers.toSorted();\n\nconsole.log(numbers); // [3, 1, 2]\nconsole.log(sorted);  // [1, 2, 3]\n```\n\nObjects can be updated immutably with spread syntax:\n\n```js\nconst user = {\n  name: "Alex",\n  role: "user"\n};\n\nconst updatedUser = {\n  ...user,\n  role: "admin"\n};\n```\n\nNested updates require copying every changed level:\n\n```js\nconst state = {\n  user: {\n    settings: {\n      theme: "light"\n    }\n  }\n};\n\nconst updatedState = {\n  ...state,\n  user: {\n    ...state.user,\n    settings: {\n      ...state.user.settings,\n      theme: "dark"\n    }\n  }\n};\n```\n\n`const` does not make an object immutable:\n\n```js\nconst user = { name: "Alex" };\nuser.name = "Sam"; // Allowed\n```\n\n`Object.freeze()` prevents direct changes to an object’s own properties, but it is shallow:\n\n```js\nconst settings = Object.freeze({\n  nested: {\n    theme: "light"\n  }\n});\n\nsettings.nested.theme = "dark"; // Nested object is not frozen\n```\n\nImmutability can improve predictability, change detection, undo functionality, memoization, and debugging. However, copying large structures also has a performance and memory cost, so the appropriate strategy depends on the application.',
  },
  {
    number: 33,
    question: "What is the difference between map, filter, and reduce?",
    answer:
      "`map`, `filter`, and `reduce` iterate over arrays without modifying the original array by themselves.\n\n- `map` transforms every element and returns an array of the same length.\n- `filter` keeps elements that pass a condition and returns an array of equal or shorter length.\n- `reduce` combines all elements into a single accumulated result.",
    explanation:
      "Choose the method based on the desired result:\n\n```js\nconst numbers = [1, 2, 3, 4];\n\nnumbers.map(number => number * 2);\n// [2, 4, 6, 8]\n\nnumbers.filter(number => number % 2 === 0);\n// [2, 4]\n\nnumbers.reduce((sum, number) => sum + number, 0);\n// 10\n```",
    details:
      '`map` calls its callback for every existing element and stores each returned value:\n\n```js\nconst users = [\n  { id: 1, name: "Alex" },\n  { id: 2, name: "Sam" }\n];\n\nconst names = users.map(user => user.name);\n// ["Alex", "Sam"]\n```\n\n`filter` includes an element when the callback returns a truthy value:\n\n```js\nconst activeUsers = users.filter(user => user.active);\n```\n\n`reduce` receives an accumulator, the current value, the current index, and the original array:\n\n```js\nconst total = numbers.reduce(\n  (accumulator, number) => accumulator + number,\n  0\n);\n```\n\nThe second argument is the accumulator’s initial value. Providing it is generally safer:\n\n```js\n[].reduce((sum, number) => sum + number);\n// TypeError\n\n[].reduce((sum, number) => sum + number, 0);\n// 0\n```\n\n`reduce` can build arrays, objects, maps, or other structures:\n\n```js\nconst grouped = users.reduce((groups, user) => {\n  const role = user.role;\n  groups[role] ??= [];\n  groups[role].push(user);\n  return groups;\n}, {});\n```\n\nAlthough these methods do not mutate the source array directly, their callbacks can still mutate objects contained in the array. Avoid such side effects when predictable transformations are important.',
  },
  {
    number: 34,
    question: "What is the difference between forEach and map?",
    answer:
      "Both methods execute a callback for each array element.\n\n- `map` returns a new array containing the callback’s return values.\n- `forEach` returns `undefined` and is intended for side effects.",
    explanation:
      "Use `map` to transform data. Use `forEach` when performing an action such as logging, updating the DOM, or modifying external state.\n\n```js\nconst numbers = [1, 2, 3];\n\nconst doubled = numbers.map(number => number * 2);\n// [2, 4, 6]\n\nnumbers.forEach(number => console.log(number));\n```",
    details:
      "Returning a value from a `forEach` callback does not create a result array:\n\n```js\nconst result = [1, 2, 3].forEach(number => {\n  return number * 2;\n});\n\nconsole.log(result); // undefined\n```\n\nBoth methods receive the same callback arguments:\n\n```js\narray.map((element, index, originalArray) => {\n  // ...\n});\n\narray.forEach((element, index, originalArray) => {\n  // ...\n});\n```\n\nNeither method supports early termination with `break` or `return`:\n\n```js\n[1, 2, 3].forEach(number => {\n  if (number === 2) {\n    return; // Skips only the rest of this callback\n  }\n});\n```\n\nUse `for...of`, `find`, `some`, or `every` when early termination is required.\n\nAvoid using `forEach` directly with asynchronous callbacks when the operations must be awaited:\n\n```js\n// Does not wait for all callbacks to finish\nusers.forEach(async user => {\n  await saveUser(user);\n});\n```\n\nSequential processing can use `for...of`:\n\n```js\nfor (const user of users) {\n  await saveUser(user);\n}\n```\n\nParallel processing can use `map` with `Promise.all`:\n\n```js\nawait Promise.all(\n  users.map(user => saveUser(user))\n);\n```",
  },
  {
    number: 35,
    question: "What is the difference between find and filter?",
    answer:
      "Both methods test array elements using a callback.\n\n- `find` returns the first matching element and stops searching.\n- `filter` returns a new array containing every matching element.\n\nIf no element matches, `find` returns `undefined`, while `filter` returns an empty array.",
    explanation:
      "Use `find` when expecting one result and `filter` when collecting multiple results.\n\n```js\nconst numbers = [1, 2, 3, 4];\n\nnumbers.find(number => number > 2);\n// 3\n\nnumbers.filter(number => number > 2);\n// [3, 4]\n```",
    details:
      '`find` returns the element itself, not its index:\n\n```js\nconst users = [\n  { id: 1, name: "Alex" },\n  { id: 2, name: "Sam" }\n];\n\nconst user = users.find(user => user.id === 2);\n// { id: 2, name: "Sam" }\n```\n\nUse `findIndex` when the position is needed:\n\n```js\nconst index = users.findIndex(user => user.id === 2);\n// 1\n```\n\nNewer JavaScript also provides `findLast` and `findLastIndex` for searching from the end:\n\n```js\nconst values = [1, 4, 2, 4];\n\nvalues.findLast(value => value === 4);      // 4\nvalues.findLastIndex(value => value === 4); // 3\n```\n\n`filter` always returns a new array, even when nothing matches:\n\n```js\nconst matches = users.filter(user => user.id === 99);\n\nconsole.log(matches); // []\nconsole.log(Boolean(matches)); // true\n```\n\nBecause an empty array is truthy, check its length when determining whether results exist:\n\n```js\nif (matches.length === 0) {\n  console.log("No users found");\n}\n```',
  },
  {
    number: 36,
    question: "What is the difference between some and every?",
    answer:
      "`some` and `every` test array elements and return Boolean values.\n\n- `some` returns `true` when at least one element passes the test.\n- `every` returns `true` only when all elements pass the test.\n\nBoth methods stop as soon as the final result is known.",
    explanation:
      "`some` answers “Does any element match?” while `every` answers “Do all elements match?”\n\n```js\nconst numbers = [2, 4, 5];\n\nnumbers.some(number => number % 2 !== 0);\n// true\n\nnumbers.every(number => number > 0);\n// true\n```",
    details:
      '`some` stops after finding the first truthy callback result:\n\n```js\nconst hasAdmin = users.some(user => {\n  return user.role === "admin";\n});\n```\n\n`every` stops after finding the first falsy result:\n\n```js\nconst allActive = users.every(user => {\n  return user.active;\n});\n```\n\nTheir results for an empty array are important:\n\n```js\n[].some(() => true);  // false\n[].every(() => false); // true\n```\n\n`some` returns `false` because no element satisfies the condition. `every` returns `true` because no element violates the condition. This is known as vacuous truth.\n\n`some` can be used as an existence check:\n\n```js\nconst hasPermission = permissions.some(\n  permission => permission === "edit"\n);\n```\n\n`every` is useful for validation:\n\n```js\nconst formIsValid = fields.every(field => {\n  return field.value.trim() !== "";\n});\n```\n\nBecause both short-circuit, they may be more efficient than `filter` when only a Boolean result is required.',
  },
  {
    number: 37,
    question: "Which array methods mutate the original array?",
    answer:
      "Common mutating array methods include:\n\n- `push`\n- `pop`\n- `shift`\n- `unshift`\n- `splice`\n- `sort`\n- `reverse`\n- `fill`\n- `copyWithin`\n\nMethods such as `map`, `filter`, `slice`, `concat`, `toSorted`, `toReversed`, and `toSpliced` return new arrays instead.",
    explanation:
      "Mutation changes the existing array reference, which can create unexpected state changes in frontend applications.\n\n```js\nconst numbers = [3, 1, 2];\nnumbers.sort();\n\nconsole.log(numbers); // [1, 2, 3]\n```",
    details:
      'Methods that add or remove elements mutate the array:\n\n```js\nconst items = ["a", "b"];\n\nitems.push("c");    // ["a", "b", "c"]\nitems.pop();        // ["a", "b"]\nitems.unshift("z"); // ["z", "a", "b"]\nitems.shift();      // ["a", "b"]\n```\n\n`splice` can remove, replace, or insert elements:\n\n```js\nconst items = ["a", "b", "c"];\n\nitems.splice(1, 1, "x");\n// items is ["a", "x", "c"]\n```\n\n`sort` and `reverse` mutate the original:\n\n```js\nconst numbers = [3, 1, 2];\n\nconst result = numbers.sort();\n\nresult === numbers; // true\n```\n\nModern non-mutating alternatives include:\n\n```js\nconst sorted = numbers.toSorted();\nconst reversed = numbers.toReversed();\nconst changed = numbers.toSpliced(1, 1, 10);\nconst replaced = numbers.with(0, 99);\n```\n\nOlder code can copy before applying a mutating method:\n\n```js\nconst sorted = [...numbers].sort((a, b) => a - b);\n```\n\nAssignment by index also mutates an array:\n\n```js\nnumbers[0] = 100;\n```\n\nA method can return a new array while its callback still mutates contained objects:\n\n```js\nconst users = [{ name: "Alex" }];\n\nconst result = users.map(user => {\n  user.name = "Sam";\n  return user;\n});\n\nconsole.log(users[0].name); // "Sam"\n```',
  },
  {
    number: 38,
    question: "How can you remove duplicate values from an array?",
    answer:
      "For primitive values, the most common solution is to create a `Set` and convert it back into an array.\n\nA `Set` stores only unique values and preserves insertion order.",
    explanation:
      "Spread syntax provides a concise solution:\n\n```js\nconst values = [1, 2, 2, 3, 3];\nconst uniqueValues = [...new Set(values)];\n\n// [1, 2, 3]\n```",
    details:
      '`Array.from()` can also convert a `Set` into an array:\n\n```js\nconst uniqueValues = Array.from(new Set(values));\n```\n\n`Set` works well for duplicate primitive values:\n\n```js\n[...new Set(["a", "a", "b"])];\n// ["a", "b"]\n```\n\nObject values are compared by identity, not by contents:\n\n```js\nconst users = [\n  { id: 1 },\n  { id: 1 }\n];\n\n[...new Set(users)].length; // 2\n```\n\nAlthough the objects contain the same data, they are different object references.\n\nObjects can be deduplicated by a property using a `Map`:\n\n```js\nconst users = [\n  { id: 1, name: "Alex" },\n  { id: 2, name: "Sam" },\n  { id: 1, name: "Alexander" }\n];\n\nconst uniqueUsers = [\n  ...new Map(\n    users.map(user => [user.id, user])\n  ).values()\n];\n```\n\nBecause later entries replace earlier entries with the same key, this keeps the final object for each `id`.\n\nTo keep the first occurrence:\n\n```js\nconst seen = new Set();\n\nconst uniqueUsers = users.filter(user => {\n  if (seen.has(user.id)) {\n    return false;\n  }\n\n  seen.add(user.id);\n  return true;\n});\n```\n\nThe right solution depends on whether uniqueness is based on primitive equality, object identity, or a selected property.',
  },
  {
    number: 39,
    question: "When would you use Map instead of an object?",
    answer:
      "Use `Map` when keys are dynamic, keys are not limited to strings and symbols, insertion order matters, or the collection is frequently updated and iterated.\n\nUse an object when representing a record with known property names, especially when the data naturally maps to JSON.",
    explanation:
      'A `Map` accepts values of any type as keys and provides dedicated collection methods.\n\n```js\nconst settings = new Map();\n\nsettings.set("theme", "dark");\nsettings.set(42, "numeric key");\nsettings.set(document.body, "element key");\n```',
    details:
      'Object property keys are strings or symbols. Other values are converted to strings:\n\n```js\nconst object = {};\n\nobject[1] = "number";\nobject["1"] = "string";\n\nconsole.log(object[1]); // "string"\n```\n\nA `Map` distinguishes these keys:\n\n```js\nconst map = new Map();\n\nmap.set(1, "number");\nmap.set("1", "string");\n\nmap.get(1);   // "number"\nmap.get("1"); // "string"\n```\n\nObjects can also be used as keys:\n\n```js\nconst user = { id: 1 };\nconst permissions = new Map();\n\npermissions.set(user, ["read", "write"]);\npermissions.get(user); // ["read", "write"]\n```\n\nA `Map` provides:\n\n```js\nmap.set(key, value);\nmap.get(key);\nmap.has(key);\nmap.delete(key);\nmap.clear();\nmap.size;\n```\n\nIt is directly iterable:\n\n```js\nfor (const [key, value] of map) {\n  console.log(key, value);\n}\n```\n\nObjects are often more convenient for structured records:\n\n```js\nconst user = {\n  id: 1,\n  name: "Alex",\n  role: "admin"\n};\n```\n\nObjects work directly with `JSON.stringify()`. A `Map` requires explicit conversion before JSON serialization.',
  },
  {
    number: 40,
    question: "When would you use Set?",
    answer:
      "Use a `Set` when a collection should contain only unique values or when efficient membership checks, additions, and deletions are important.\n\nA `Set` preserves insertion order and can contain values of any type.",
    explanation:
      "A `Set` is useful for deduplication and checking whether a value has already been seen.\n\n```js\nconst selectedIds = new Set();\n\nselectedIds.add(1);\nselectedIds.add(1);\nselectedIds.add(2);\n\nconsole.log(selectedIds.size); // 2\n```",
    details:
      'The main `Set` operations are:\n\n```js\nconst roles = new Set();\n\nroles.add("admin");\nroles.has("admin");    // true\nroles.delete("admin"); // true\nroles.clear();\nroles.size;            // 0\n```\n\nA `Set` can be constructed from any iterable:\n\n```js\nconst uniqueLetters = new Set("hello");\n// Set { "h", "e", "l", "o" }\n```\n\nIt can be converted into an array:\n\n```js\nconst values = [...uniqueLetters];\n```\n\nSets are directly iterable:\n\n```js\nfor (const role of roles) {\n  console.log(role);\n}\n```\n\nObject values are unique by identity:\n\n```js\nconst first = { id: 1 };\nconst second = { id: 1 };\n\nconst set = new Set([first, second]);\n\nconsole.log(set.size); // 2\n```\n\nA `Set` is often clearer than repeatedly using `array.includes()` when many membership checks are required:\n\n```js\nconst allowedRoles = new Set([\n  "admin",\n  "editor",\n  "author"\n]);\n\nif (allowedRoles.has(user.role)) {\n  // Allow access\n}\n```\n\nModern JavaScript environments also provide set-composition methods such as `union`, `intersection`, `difference`, and `isSubsetOf`. Compatibility should be checked when supporting older browsers.',
  },
  {
    number: 41,
    question: "What are WeakMap and WeakSet?",
    answer:
      "`WeakMap` and `WeakSet` are collections that hold their object entries weakly. Their presence in the collection does not prevent those objects from being garbage-collected when no other reachable references remain.\n\nA `WeakMap` associates values with object or non-registered symbol keys. A `WeakSet` tracks object or non-registered symbol values.",
    explanation:
      'They are useful for attaching metadata or tracking objects without extending those objects’ lifetimes.\n\n```js\nconst metadata = new WeakMap();\n\nconst element = document.querySelector("button");\nmetadata.set(element, { clicks: 0 });\n```',
    details:
      'A regular `Map` strongly retains its keys:\n\n```js\nconst cache = new Map();\n\nlet user = { id: 1 };\ncache.set(user, "cached");\n\nuser = null;\n```\n\nThe object remains reachable through `cache`, so it cannot be garbage-collected.\n\nA `WeakMap` does not keep its keys alive:\n\n```js\nconst cache = new WeakMap();\n\nlet user = { id: 1 };\ncache.set(user, "cached");\n\nuser = null;\n```\n\nIf no other references remain, the object and its associated entry may be collected.\n\nCommon `WeakMap` operations are:\n\n```js\nweakMap.set(object, value);\nweakMap.get(object);\nweakMap.has(object);\nweakMap.delete(object);\n```\n\nCommon `WeakSet` operations are:\n\n```js\nweakSet.add(object);\nweakSet.has(object);\nweakSet.delete(object);\n```\n\nWeak collections are not iterable and do not expose `size` or `clear`. Garbage collection is unpredictable, so JavaScript cannot provide a reliable list of their current entries.\n\nA `WeakMap` can store private metadata associated with an object:\n\n```js\nconst privateData = new WeakMap();\n\nclass User {\n  constructor(name) {\n    privateData.set(this, { name });\n  }\n\n  getName() {\n    return privateData.get(this).name;\n  }\n}\n```\n\nA `WeakSet` can track whether particular objects have been processed:\n\n```js\nconst processed = new WeakSet();\n\nfunction process(object) {\n  if (processed.has(object)) {\n    return;\n  }\n\n  processed.add(object);\n  // Process the object\n}\n```\n\nWeak collections are specialized tools. Use `Map` or `Set` when iteration, primitive keys, or collection size is required.',
  },
  {
    number: 42,
    question: "What is the JavaScript event loop?",
    answer:
      "The event loop coordinates synchronous JavaScript execution with asynchronous work.\n\nIt monitors the call stack and task queues. When the call stack is empty, it allows queued callbacks to run according to their priority and scheduling rules.",
    explanation:
      'JavaScript executes one piece of JavaScript code at a time on its main thread, but browser APIs can perform operations such as timers and network requests outside the call stack.\n\n```js\nconsole.log("First");\n\nsetTimeout(() => {\n  console.log("Third");\n}, 0);\n\nconsole.log("Second");\n```\n\nOutput:\n\n```text\nFirst\nSecond\nThird\n```',
    details:
      "The browser environment includes several cooperating components:\n\n- The JavaScript engine\n- The call stack\n- Browser APIs\n- The microtask queue\n- Task queues\n- The event loop\n- The rendering system\n\nWhen a timer is created, the browser tracks it outside the JavaScript call stack. After the delay has elapsed, its callback becomes eligible to enter a task queue.\n\nThe event loop generally performs work in this order:\n\n1. Execute the current task.\n2. Empty the microtask queue.\n3. Give the browser an opportunity to render.\n4. Begin another task.\n\nA zero-millisecond timeout does not run immediately. It specifies a minimum delay before the callback can be queued:\n\n```js\nsetTimeout(callback, 0);\n```\n\nThe callback must still wait for the current code and all queued microtasks to complete.\n\nLong-running synchronous code blocks the event loop:\n\n```js\nconst start = Date.now();\n\nwhile (Date.now() - start \u003c 5000) {\n  // Blocks the main thread\n}\n```\n\nDuring this period, the page may not respond to input, process callbacks, or update its display.",
  },
  {
    number: 43,
    question: "What is the call stack?",
    answer:
      "The call stack is the data structure JavaScript uses to track active function executions.\n\nWhen a function is called, a stack frame is added. When the function returns or throws an error, its frame is removed.",
    explanation:
      'JavaScript executes the function at the top of the stack before returning to the function below it.\n\n```js\nfunction first() {\n  second();\n}\n\nfunction second() {\n  console.log("Hello");\n}\n\nfirst();\n```',
    details:
      "A simplified execution sequence is:\n\n```text\nAdd global execution context\nAdd first()\nAdd second()\nAdd console.log()\nRemove console.log()\nRemove second()\nRemove first()\n```\n\nEach stack frame stores information such as:\n\n- Function arguments\n- Local variables\n- The current execution position\n- The function’s `this` value\n- A reference to its lexical environment\n\nRecursive functions add a new frame for every call:\n\n```js\nfunction countDown(number) {\n  if (number === 0) {\n    return;\n  }\n\n  countDown(number - 1);\n}\n\ncountDown(5);\n```\n\nRecursion without a valid stopping condition eventually exceeds the available stack:\n\n```js\nfunction recurse() {\n  recurse();\n}\n\nrecurse(); // RangeError: Maximum call stack size exceeded\n```\n\nStack traces show the chain of active function calls at the time an error occurred, making them important for debugging.",
  },
  {
    number: 44,
    question: "What is the difference between synchronous and asynchronous code?",
    answer:
      "Synchronous code executes sequentially, with each operation completing before the next one begins.\n\nAsynchronous code starts an operation and allows other JavaScript to continue while waiting. Its result is handled later through a callback, Promise, event, or `async` function.",
    explanation:
      'Asynchronous programming prevents waiting operations such as network requests and timers from unnecessarily blocking other work.\n\n```js\nconsole.log("Before");\n\nsetTimeout(() => {\n  console.log("Timer finished");\n}, 1000);\n\nconsole.log("After");\n```',
    details:
      'Synchronous code blocks further JavaScript execution until it finishes:\n\n```js\nconst result = performCalculation();\nconsole.log(result);\n```\n\nAsynchronous APIs return control before their work completes:\n\n```js\nfetch("/api/users")\n  .then(response => response.json())\n  .then(users => console.log(users));\n\nconsole.log("Request started");\n```\n\n`async`/`await` makes asynchronous code look sequential, but `await` does not block the entire JavaScript thread:\n\n```js\nasync function loadUsers() {\n  const response = await fetch("/api/users");\n  const users = await response.json();\n\n  return users;\n}\n```\n\nThe async function pauses at `await`, while other eligible work can continue.\n\nAsynchronous code is helpful for:\n\n- Network requests\n- Timers\n- User interactions\n- File operations in supported environments\n- Background work\n- Streams and animations\n\nCPU-intensive JavaScript is still blocking unless it is divided into smaller tasks or moved to a worker.',
  },
  {
    number: 45,
    question: "What are microtasks and macrotasks?",
    answer:
      "Microtasks and tasks—often informally called macrotasks—are scheduled work with different priorities.\n\nAfter the current synchronous code finishes, JavaScript empties the microtask queue before starting the next task.",
    explanation:
      'Promise callbacks are microtasks, while timer callbacks are tasks. Therefore, a resolved Promise usually runs before a zero-delay timer.\n\n```js\nsetTimeout(() => console.log("Timer"), 0);\n\nPromise.resolve().then(() => {\n  console.log("Promise");\n});\n\nconsole.log("Synchronous");\n```\n\nOutput:\n\n```text\nSynchronous\nPromise\nTimer\n```',
    details:
      'Common microtask sources include:\n\n- Promise reaction callbacks\n- Code after `await`\n- `queueMicrotask()`\n- `MutationObserver` callbacks\n\nCommon task sources include:\n\n- `setTimeout`\n- `setInterval`\n- User-interface events\n- Message events\n- Some network and platform callbacks\n\nMicrotasks added while processing microtasks run before the browser moves to the next task:\n\n```js\nqueueMicrotask(() => {\n  console.log("Microtask 1");\n\n  queueMicrotask(() => {\n    console.log("Microtask 2");\n  });\n});\n```\n\nExcessive microtasks can delay rendering and other tasks. This is called microtask starvation:\n\n```js\nfunction repeat() {\n  queueMicrotask(repeat);\n}\n\nrepeat();\n```\n\nBrowser scheduling details are defined in terms of tasks rather than “macrotasks,” but “macrotask” is commonly used in interviews to distinguish task queues from the microtask queue.',
  },
  {
    number: 46,
    question: "In what order do synchronous code, promises, and timers execute?",
    answer:
      "The usual order is:\n\n1. Synchronous code\n2. Microtasks, including Promise callbacks and continuations after `await`\n3. The next task, such as a timer callback\n\nMicrotasks are emptied after the current task completes and before another task begins.",
    explanation:
      'Promise callbacks therefore run before timer callbacks when both become eligible during the same task.\n\n```js\nconsole.log("A");\n\nsetTimeout(() => console.log("B"), 0);\n\nPromise.resolve().then(() => console.log("C"));\n\nconsole.log("D");\n```\n\nOutput:\n\n```text\nA\nD\nC\nB\n```',
    details:
      'Nested scheduling changes the order:\n\n```js\nconsole.log("1");\n\nsetTimeout(() => {\n  console.log("2");\n\n  Promise.resolve().then(() => {\n    console.log("3");\n  });\n}, 0);\n\nPromise.resolve().then(() => {\n  console.log("4");\n\n  setTimeout(() => {\n    console.log("5");\n  }, 0);\n});\n\nconsole.log("6");\n```\n\nOutput:\n\n```text\n1\n6\n4\n2\n3\n5\n```\n\nThe reasoning is:\n\n1. Synchronous code prints `1` and `6`.\n2. The first Promise callback runs and prints `4`.\n3. The first timer task prints `2`.\n4. Its Promise callback runs as a microtask and prints `3`.\n5. The timer created inside the earlier Promise callback prints `5`.\n\nAn `await` continuation is also scheduled through the microtask mechanism:\n\n```js\nasync function run() {\n  console.log("Inside 1");\n  await Promise.resolve();\n  console.log("Inside 2");\n}\n\nconsole.log("Outside 1");\nrun();\nconsole.log("Outside 2");\n```\n\nOutput:\n\n```text\nOutside 1\nInside 1\nOutside 2\nInside 2\n```',
  },
  {
    number: 47,
    question: "What is a Promise?",
    answer:
      "A Promise is an object representing the eventual completion or failure of an asynchronous operation.\n\nIt provides a structured way to register success and failure handlers and to compose asynchronous operations without deeply nested callbacks.",
    explanation:
      '`then` handles fulfillment, `catch` handles rejection, and `finally` runs after settlement regardless of the outcome.\n\n```js\nfetch("/api/users")\n  .then(response => response.json())\n  .then(users => console.log(users))\n  .catch(error => console.error(error))\n  .finally(() => console.log("Finished"));\n```',
    details:
      'A Promise can be created with an executor function:\n\n```js\nconst promise = new Promise((resolve, reject) => {\n  const succeeded = true;\n\n  if (succeeded) {\n    resolve("Completed");\n  } else {\n    reject(new Error("Failed"));\n  }\n});\n```\n\nThe executor runs synchronously:\n\n```js\nconsole.log("Before");\n\nnew Promise(resolve => {\n  console.log("Executor");\n  resolve();\n});\n\nconsole.log("After");\n```\n\nOutput:\n\n```text\nBefore\nExecutor\nAfter\n```\n\nPromise handlers always run asynchronously as microtasks, even when the Promise is already settled:\n\n```js\nPromise.resolve("Done").then(console.log);\nconsole.log("Synchronous");\n```\n\nOutput:\n\n```text\nSynchronous\nDone\n```\n\n`then` returns a new Promise, enabling chaining:\n\n```js\nPromise.resolve(2)\n  .then(value => value * 2)\n  .then(value => value + 1)\n  .then(console.log); // 5\n```\n\nReturning a Promise from a handler causes the next step to wait for it:\n\n```js\nfetch("/api/user")\n  .then(response => response.json())\n  .then(user => fetch(`/api/orders/${user.id}`))\n  .then(response => response.json());\n```\n\nThrowing inside a handler rejects the Promise returned by that handler:\n\n```js\nPromise.resolve()\n  .then(() => {\n    throw new Error("Failed");\n  })\n  .catch(error => console.error(error));\n```',
  },
  {
    number: 48,
    question: "What states can a Promise have?",
    answer:
      "A Promise has three possible states:\n\n- **Pending:** The operation has not settled.\n- **Fulfilled:** The operation completed successfully and produced a value.\n- **Rejected:** The operation failed and produced a reason, usually an `Error`.\n\nA fulfilled or rejected Promise is described as settled.",
    explanation:
      'A Promise can move from pending to fulfilled or rejected only once. Its final state and result cannot later be changed.\n\n```js\nconst promise = new Promise(resolve => {\n  resolve("First");\n  resolve("Second"); // Ignored\n});\n```',
    details:
      'These transitions are possible:\n\n```text\npending → fulfilled\npending → rejected\n```\n\nThese transitions are not possible:\n\n```text\nfulfilled → rejected\nrejected → fulfilled\nsettled → pending\n```\n\nResolving a Promise with another Promise makes the outer Promise adopt the other Promise’s eventual state:\n\n```js\nconst inner = new Promise(resolve => {\n  setTimeout(() => resolve("Done"), 1000);\n});\n\nconst outer = new Promise(resolve => {\n  resolve(inner);\n});\n\nouter.then(console.log); // "Done"\n```\n\nA Promise may be resolved but still pending while it follows another Promise. For everyday interviews, “resolved” is often treated as synonymous with “fulfilled,” but the specification distinguishes them.\n\nA rejection should normally use an `Error` object:\n\n```js\nreject(new Error("Unable to load user"));\n```\n\nThis preserves a useful message and stack trace.\n\nAn unhandled rejection occurs when a rejected Promise has no rejection handler. Browsers may report it in the console and emit an `unhandledrejection` event.',
  },
  {
    number: 49,
    question: "How do async and await work?",
    answer:
      "Declaring a function with `async` makes it always return a Promise.\n\n`await` pauses that async function until a value or Promise settles. If it fulfills, `await` produces its value. If it rejects, `await` throws the rejection reason inside the async function.",
    explanation:
      '`async`/`await` provides syntax for writing Promise-based workflows in a readable, sequential style.\n\n```js\nasync function loadUser() {\n  const response = await fetch("/api/user");\n  const user = await response.json();\n\n  return user;\n}\n```',
    details:
      'Returning a normal value from an async function creates a fulfilled Promise:\n\n```js\nasync function getNumber() {\n  return 42;\n}\n\ngetNumber().then(console.log); // 42\n```\n\nThrowing creates a rejected Promise:\n\n```js\nasync function fail() {\n  throw new Error("Failed");\n}\n\nfail().catch(console.error);\n```\n\n`await` accepts any value. A non-Promise value behaves like an already fulfilled Promise, but continuation still occurs asynchronously:\n\n```js\nasync function run() {\n  console.log("A");\n  await 42;\n  console.log("C");\n}\n\nrun();\nconsole.log("B");\n```\n\nOutput:\n\n```text\nA\nB\nC\n```\n\nErrors can be handled with `try` and `catch`:\n\n```js\nasync function loadUser() {\n  try {\n    const response = await fetch("/api/user");\n\n    if (!response.ok) {\n      throw new Error(`HTTP ${response.status}`);\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error(error);\n    throw error;\n  }\n}\n```\n\nIndependent operations should not be awaited one after another unnecessarily:\n\n```js\nconst [user, products] = await Promise.all([\n  fetchUser(),\n  fetchProducts()\n]);\n```\n\nThis starts both operations before waiting for them together.',
  },
  {
    number: 50,
    question: "How do Promise.all, Promise.allSettled, Promise.race, and Promise.any differ?",
    answer:
      "These static methods combine multiple asynchronous values:\n\n- `Promise.all` fulfills when all inputs fulfill and rejects when the first input rejects.\n- `Promise.allSettled` waits for every input and always fulfills with their outcomes.\n- `Promise.race` settles with the first input to settle.\n- `Promise.any` fulfills with the first input to fulfill and rejects only if every input rejects.",
    explanation:
      "Choose the method based on whether every result is required, partial failures are acceptable, or only the first outcome matters.",
    details:
      '`Promise.all` preserves input order, regardless of completion order:\n\n```js\nconst results = await Promise.all([\n  fetchUser(),\n  fetchProducts(),\n  fetchSettings()\n]);\n```\n\nIf any input rejects, the combined Promise rejects immediately. Other operations are not automatically cancelled.\n\n`Promise.allSettled` returns status objects:\n\n```js\nconst results = await Promise.allSettled([\n  Promise.resolve("A"),\n  Promise.reject(new Error("Failed"))\n]);\n```\n\nResult shape:\n\n```js\n[\n  {\n    status: "fulfilled",\n    value: "A"\n  },\n  {\n    status: "rejected",\n    reason: Error("Failed")\n  }\n]\n```\n\n`Promise.race` reacts to the first settlement, whether successful or failed:\n\n```js\nconst result = await Promise.race([\n  fetchData(),\n  timeoutPromise(5000)\n]);\n```\n\n`Promise.any` ignores rejections until one input fulfills:\n\n```js\nconst fastestSuccess = await Promise.any([\n  fetchFromServerA(),\n  fetchFromServerB()\n]);\n```\n\nIf every input rejects, `Promise.any` rejects with an `AggregateError`.\n\nEmpty iterables behave differently:\n\n```js\nPromise.all([]);        // Fulfilled with []\nPromise.allSettled([]); // Fulfilled with []\nPromise.any([]);        // Rejected with AggregateError\nPromise.race([]);       // Remains pending\n```',
  },
  {
    number: 51,
    question: "How should errors be handled in asynchronous code?",
    answer:
      "Promise errors should be handled with `.catch()` or with `try...catch` around awaited operations.\n\nErrors should normally be represented by `Error` objects, handled at a level that can meaningfully recover, and rethrown when the caller still needs to know that the operation failed.",
    explanation:
      'A `try...catch` block can catch an awaited Promise rejection:\n\n```js\ntry {\n  const user = await fetchUser();\n} catch (error) {\n  console.error("Unable to load user", error);\n}\n```',
    details:
      'Promise chains propagate rejections to the nearest rejection handler:\n\n```js\nfetchUser()\n  .then(user => fetchOrders(user.id))\n  .then(orders => renderOrders(orders))\n  .catch(error => {\n    showErrorMessage(error);\n  });\n```\n\nA `try...catch` block does not catch a Promise rejection unless the Promise is awaited:\n\n```js\ntry {\n  fetchUser(); // Rejection is not caught here\n} catch (error) {\n  console.error(error);\n}\n```\n\nCorrect version:\n\n```js\ntry {\n  await fetchUser();\n} catch (error) {\n  console.error(error);\n}\n```\n\nFetch only rejects for certain network-level failures. HTTP error statuses must be checked explicitly:\n\n```js\nconst response = await fetch("/api/user");\n\nif (!response.ok) {\n  throw new Error(`Request failed: ${response.status}`);\n}\n```\n\nAvoid silently swallowing errors:\n\n```js\ntry {\n  await saveUser();\n} catch (error) {\n  // Empty catch hides the failure\n}\n```\n\nAdd contextual information while preserving the original cause:\n\n```js\ntry {\n  await saveUser();\n} catch (error) {\n  throw new Error("Could not save user", {\n    cause: error\n  });\n}\n```\n\n`finally` is appropriate for cleanup:\n\n```js\nsetLoading(true);\n\ntry {\n  await loadData();\n} finally {\n  setLoading(false);\n}\n```',
  },
  {
    number: 52,
    question: "How can an HTTP request be cancelled?",
    answer:
      "Requests made with the Fetch API can be cancelled using an `AbortController`.\n\nIts `signal` is passed to `fetch`, and calling `abort()` tells the operation to stop. The Fetch Promise then rejects, normally with an abort-related error.",
    explanation:
      'Cancellation is useful when a component is removed, the user starts a newer request, or an operation exceeds a time limit.\n\n```js\nconst controller = new AbortController();\n\nfetch("/api/users", {\n  signal: controller.signal\n});\n\ncontroller.abort();\n```',
    details:
      'Cancellation can be handled explicitly:\n\n```js\nconst controller = new AbortController();\n\ntry {\n  const response = await fetch("/api/users", {\n    signal: controller.signal\n  });\n\n  const users = await response.json();\n} catch (error) {\n  if (error.name === "AbortError") {\n    console.log("Request cancelled");\n  } else {\n    throw error;\n  }\n}\n```\n\nA single signal can cancel multiple related operations:\n\n```js\nconst controller = new AbortController();\n\nconst requests = [\n  fetch("/api/user", {\n    signal: controller.signal\n  }),\n  fetch("/api/products", {\n    signal: controller.signal\n  })\n];\n\ncontroller.abort();\n```\n\nSome environments support timeout signals:\n\n```js\nconst response = await fetch("/api/users", {\n  signal: AbortSignal.timeout(5000)\n});\n```\n\nWhen implementing search suggestions, cancel the previous request before starting a new one:\n\n```js\nlet currentController;\n\nasync function search(query) {\n  currentController?.abort();\n  currentController = new AbortController();\n\n  const response = await fetch(\n    `/api/search?q=${encodeURIComponent(query)}`,\n    { signal: currentController.signal }\n  );\n\n  return response.json();\n}\n```\n\nAborting Fetch does not guarantee that the server stops work already started. It primarily stops the client from continuing to wait for or process the response.',
  },
  {
    number: 53,
    question: "How can multiple asynchronous operations be executed in parallel?",
    answer:
      "Start all independent asynchronous operations before awaiting their combined result.\n\n`Promise.all` is usually appropriate when every operation must succeed. `Promise.allSettled` is useful when all outcomes are needed even if some operations fail.",
    explanation:
      "Sequential awaits unnecessarily delay independent operations:\n\n```js\nconst user = await fetchUser();\nconst products = await fetchProducts();\n```\n\nParallel version:\n\n```js\nconst [user, products] = await Promise.all([\n  fetchUser(),\n  fetchProducts()\n]);\n```",
    details:
      "Suppose each operation takes one second.\n\nSequential execution takes approximately two seconds:\n\n```js\nconst user = await fetchUser();\nconst products = await fetchProducts();\n```\n\nThe second operation does not begin until the first finishes.\n\nParallel execution takes approximately one second:\n\n```js\nconst userPromise = fetchUser();\nconst productsPromise = fetchProducts();\n\nconst [user, products] = await Promise.all([\n  userPromise,\n  productsPromise\n]);\n```\n\nOnly independent operations should run in parallel. Dependent operations must wait for the required value:\n\n```js\nconst user = await fetchUser();\nconst orders = await fetchOrders(user.id);\n```\n\nWhen processing an array in parallel:\n\n```js\nconst users = await Promise.all(\n  userIds.map(id => fetchUser(id))\n);\n```\n\nAvoid `forEach` with async callbacks:\n\n```js\n// Does not wait for completion\nuserIds.forEach(async id => {\n  await fetchUser(id);\n});\n```\n\nUnrestricted parallelism can overload a browser, network, or server when the input is large. A concurrency limit may be necessary:\n\n```js\nasync function processInBatches(items, batchSize) {\n  const results = [];\n\n  for (\n    let index = 0;\n    index \u003c items.length;\n    index += batchSize\n  ) {\n    const batch = items.slice(index, index + batchSize);\n\n    const batchResults = await Promise.all(\n      batch.map(item => processItem(item))\n    );\n\n    results.push(...batchResults);\n  }\n\n  return results;\n}\n```\n\nParallel execution improves total duration when operations are independent, but it does not make the individual operations themselves execute faster.",
  },
  {
    number: 54,
    question: "What is the DOM?",
    answer:
      "The Document Object Model, or DOM, is the browser’s object-based representation of an HTML or XML document.\n\nIt represents the document as a tree of nodes and provides APIs that JavaScript can use to read, create, update, move, and remove content.",
    explanation:
      'HTML elements become objects that JavaScript can interact with:\n\n```js\nconst heading = document.querySelector("h1");\nheading.textContent = "Updated title";\n```',
    details:
      'The DOM tree contains several node types:\n\n- Document nodes\n- Element nodes\n- Text nodes\n- Comment nodes\n- Document fragments\n\nFor this HTML:\n\n```html\n\u003cmain>\n  \u003ch1>Hello\u003c/h1>\n\u003c/main>\n```\n\nA simplified tree is:\n\n```text\ndocument\n  └── html\n      └── body\n          └── main\n              └── h1\n                  └── "Hello"\n```\n\nThe DOM is not part of the JavaScript language itself. It is a Web API supplied by browser environments.\n\nJavaScript can use DOM APIs to:\n\n- Find elements\n- Change content and attributes\n- Modify styles and classes\n- Attach event listeners\n- Create or remove elements\n- Measure element positions and sizes\n\nDOM updates may trigger browser rendering work, so large or repeated changes can affect performance.',
  },
  {
    number: 55,
    question: "What is the difference between the DOM and the HTML source?",
    answer:
      "The HTML source is the text initially sent to the browser. The DOM is the live object tree the browser creates after parsing that source.\n\nJavaScript, browser error correction, user interaction, and dynamic rendering can change the DOM without changing the original HTML response.",
    explanation:
      'The page source and current DOM may contain different content after JavaScript runs.\n\n```js\ndocument.body.append("Added by JavaScript");\n```\n\nThis changes the DOM but not the server’s original HTML file.',
    details:
      "Browsers may correct invalid markup while building the DOM:\n\n```html\n\u003ctable>\n  \u003ctr>\n    \u003ctd>Item\u003c/td>\n  \u003c/tr>\n\u003c/table>\n```\n\nThe browser may insert a `\u003ctbody>` element into the DOM even if it was not present in the source.\n\nThe source is generally static for a particular response. The DOM is live and can change because of:\n\n- JavaScript\n- Form input\n- Browser extensions\n- Client-side frameworks\n- DOM APIs\n- Browser parsing rules\n\n“View Page Source” normally shows the original response. Browser developer tools normally show the current DOM.\n\nThe DOM is also different from the visual rendering of the page. CSS can hide, reorder, or visually transform elements without changing their DOM structure.",
  },
  {
    number: 56,
    question: "How do you select, create, update, and remove DOM elements?",
    answer:
      "Elements can be selected with methods such as `querySelector` and `querySelectorAll`, created with `createElement`, updated through properties and attributes, and removed with `remove`.",
    explanation:
      'A common workflow is to create an element, configure it, and insert it into the document.\n\n```js\nconst item = document.createElement("li");\nitem.textContent = "New item";\n\ndocument.querySelector("ul").append(item);\n```',
    details:
      'Common selection methods include:\n\n```js\ndocument.querySelector(".card");\ndocument.querySelectorAll(".card");\ndocument.getElementById("profile");\n```\n\n`querySelector` returns the first match or `null`. `querySelectorAll` returns a static `NodeList`.\n\nContent and properties can be updated directly:\n\n```js\nconst image = document.querySelector("img");\n\nimage.src = "/avatar.png";\nimage.alt = "User avatar";\n```\n\nAttributes can be managed explicitly:\n\n```js\nelement.setAttribute("aria-label", "Close");\nelement.getAttribute("aria-label");\nelement.removeAttribute("aria-label");\n```\n\nClasses can be managed with `classList`:\n\n```js\nelement.classList.add("active");\nelement.classList.remove("hidden");\nelement.classList.toggle("selected");\n```\n\nElements can be inserted with:\n\n```js\nparent.append(element);\nparent.prepend(element);\nreference.before(element);\nreference.after(element);\n```\n\nThey can be replaced or removed:\n\n```js\noldElement.replaceWith(newElement);\nelement.remove();\n```\n\nFor many insertions, a `DocumentFragment` can group work before insertion:\n\n```js\nconst fragment = document.createDocumentFragment();\n\nfor (const name of names) {\n  const item = document.createElement("li");\n  item.textContent = name;\n  fragment.append(item);\n}\n\nlist.append(fragment);\n```',
  },
  {
    number: 57,
    question: "What is event bubbling?",
    answer:
      "Event bubbling is the phase in which an event moves from its target element upward through its ancestors.\n\nMost common DOM events bubble, allowing parent elements to respond to events that began on their descendants.",
    explanation:
      'Clicking the button can trigger listeners on both the button and its parent:\n\n```js\nparent.addEventListener("click", () => {\n  console.log("Parent");\n});\n\nbutton.addEventListener("click", () => {\n  console.log("Button");\n});\n```\n\nThe button listener normally runs before the parent listener.',
    details:
      'An event generally moves through three phases:\n\n1. Capturing: from the document toward the target\n2. Target: at the element where the event occurred\n3. Bubbling: from the target back toward the document\n\nBubbling makes event delegation possible:\n\n```js\nlist.addEventListener("click", event => {\n  const item = event.target.closest("li");\n\n  if (item) {\n    console.log(item.dataset.id);\n  }\n});\n```\n\nNot every event bubbles. For example, `focus` and `blur` do not normally bubble, although `focusin` and `focusout` do.\n\nBubbling can be stopped:\n\n```js\nevent.stopPropagation();\n```\n\nHowever, stopping propagation can interfere with unrelated parent behavior and should be used intentionally.',
  },
  {
    number: 58,
    question: "What is event capturing?",
    answer:
      "Event capturing is the phase in which an event travels from the top of the document tree down toward its target.\n\nEvent listeners use the bubbling phase by default. A listener can participate in capturing by setting the `capture` option to `true`.",
    explanation:
      'A capturing parent listener runs before a target’s ordinary bubbling listener.\n\n```js\nparent.addEventListener(\n  "click",\n  () => console.log("Parent capture"),\n  { capture: true }\n);\n```',
    details:
      'The event order is generally:\n\n```text\ndocument capture\n  → ancestor capture\n  → target\n  → ancestor bubble\n  → document bubble\n```\n\nThe shorter boolean form is also supported:\n\n```js\nparent.addEventListener("click", handler, true);\n```\n\nThe options object is clearer and can include other settings:\n\n```js\nelement.addEventListener("click", handler, {\n  capture: true,\n  once: true,\n  passive: true\n});\n```\n\nCapture listeners can be useful when:\n\n- An event does not bubble normally\n- A parent must observe an event before a child handles it\n- Centralized interception is required\n\nA listener must normally be removed with the same capture setting used when it was added:\n\n```js\nelement.removeEventListener("click", handler, {\n  capture: true\n});\n```',
  },
  {
    number: 59,
    question: "What is event delegation?",
    answer:
      "Event delegation attaches one listener to a shared ancestor instead of attaching separate listeners to every descendant.\n\nThe ancestor uses event bubbling and information such as `event.target` to determine which descendant initiated the event.",
    explanation:
      'Delegation reduces the number of listeners and automatically supports matching elements added later.\n\n```js\nlist.addEventListener("click", event => {\n  const button = event.target.closest(".delete-button");\n\n  if (!button || !list.contains(button)) {\n    return;\n  }\n\n  deleteItem(button.dataset.id);\n});\n```',
    details:
      'Without delegation, every button needs a listener:\n\n```js\ndocument\n  .querySelectorAll(".delete-button")\n  .forEach(button => {\n    button.addEventListener("click", handleDelete);\n  });\n```\n\nNewly added buttons would require additional listeners.\n\nWith delegation, the ancestor’s listener handles current and future descendants:\n\n```js\nlist.insertAdjacentHTML(\n  "beforeend",\n  \'\u003cbutton class="delete-button">Delete\u003c/button>\'\n);\n```\n\n`closest()` is usually safer than checking only `event.target.matches()` because the click may originate from a nested icon or text element.\n\nConfirm that the matched element belongs to the intended container:\n\n```js\nif (!button || !list.contains(button)) {\n  return;\n}\n```\n\nDelegation works best for bubbling events. For non-bubbling behavior, use a related bubbling event or a capture listener where appropriate.',
  },
  {
    number: 60,
    question: "What is the difference between event.target and event.currentTarget?",
    answer:
      "`event.target` is the element where the event originated.\n\n`event.currentTarget` is the element whose event listener is currently running.",
    explanation:
      'During event delegation, the target may be a descendant while the current target is the ancestor holding the listener.\n\n```js\nlist.addEventListener("click", event => {\n  console.log(event.target);\n  console.log(event.currentTarget); // list\n});\n```',
    details:
      'Given this markup:\n\n```html\n\u003cbutton id="save">\n  \u003cspan>Save\u003c/span>\n\u003c/button>\n```\n\nAnd this listener:\n\n```js\nconst button = document.querySelector("#save");\n\nbutton.addEventListener("click", event => {\n  console.log(event.target);\n  console.log(event.currentTarget);\n});\n```\n\nClicking the `\u003cspan>` produces:\n\n- `event.target`: the `\u003cspan>`\n- `event.currentTarget`: the `\u003cbutton>`\n\n`target` normally stays the same as the event propagates. `currentTarget` changes for each listener.\n\nOutside the listener callback, `currentTarget` is typically `null`:\n\n```js\nbutton.addEventListener("click", event => {\n  setTimeout(() => {\n    console.log(event.currentTarget); // null\n  });\n});\n```\n\nSave the value during the callback if it is needed later.',
  },
  {
    number: 61,
    question: "What do preventDefault and stopPropagation do?",
    answer:
      "`preventDefault()` prevents the browser’s default action for an event.\n\n`stopPropagation()` prevents the event from continuing through the remaining ancestors during capture or bubbling.\n\nThey affect different parts of event handling.",
    explanation:
      'Preventing a form submission does not automatically stop the event from bubbling.\n\n```js\nform.addEventListener("submit", event => {\n  event.preventDefault();\n});\n```',
    details:
      'Common default browser actions include:\n\n- Following a link\n- Submitting a form\n- Checking a checkbox\n- Opening a context menu\n- Performing some drag-and-drop actions\n\nA default action can be prevented only when the event is cancelable:\n\n```js\nif (event.cancelable) {\n  event.preventDefault();\n}\n```\n\n`stopPropagation()` stops movement to other elements but does not stop other listeners on the same element.\n\nTo stop those as well, use:\n\n```js\nevent.stopImmediatePropagation();\n```\n\nPassive listeners cannot call `preventDefault()`:\n\n```js\nelement.addEventListener("touchmove", handler, {\n  passive: true\n});\n```\n\nPassive listeners help scrolling performance by promising that the handler will not cancel the event.\n\nBoth methods should be used carefully because preventing expected browser behavior or propagation may break accessibility and other components.',
  },
  {
    number: 62,
    question: "What is the difference between DOMContentLoaded and load?",
    answer:
      "`DOMContentLoaded` fires after the initial HTML has been parsed and the DOM has been constructed.\n\n`load` fires later, after the document and dependent resources such as images, stylesheets, and frames have finished loading.",
    explanation:
      'Use `DOMContentLoaded` when code only needs the DOM. Use `load` when it depends on all page resources being available.\n\n```js\ndocument.addEventListener("DOMContentLoaded", () => {\n  console.log("DOM ready");\n});\n\nwindow.addEventListener("load", () => {\n  console.log("Page resources loaded");\n});\n```',
    details:
      'Deferred scripts execute after parsing and before `DOMContentLoaded`:\n\n```html\n\u003cscript defer src="app.js">\u003c/script>\n```\n\nModule scripts are deferred by default:\n\n```html\n\u003cscript type="module" src="app.js">\u003c/script>\n```\n\nA normal script placed at the end of `\u003cbody>` can often access earlier DOM elements without waiting for `DOMContentLoaded`.\n\nThe `load` event also exists on individual resources:\n\n```js\nimage.addEventListener("load", () => {\n  console.log("Image loaded");\n});\n```\n\nIf code may run after `DOMContentLoaded` has already fired, check the document state:\n\n```js\nfunction initialize() {\n  // Set up application\n}\n\nif (document.readyState === "loading") {\n  document.addEventListener(\n    "DOMContentLoaded",\n    initialize,\n    { once: true }\n  );\n} else {\n  initialize();\n}\n```',
  },
  {
    number: 63,
    question: "What is the difference between innerHTML, innerText, and textContent?",
    answer:
      "- `innerHTML` reads or writes HTML markup.\n- `textContent` reads or writes the textual content of a node and its descendants.\n- `innerText` represents rendered text and takes visibility and layout into account.",
    explanation:
      'Use `textContent` for plain text. Use `innerHTML` only when HTML parsing is required and the content is trusted or properly sanitized.\n\n```js\nelement.textContent = "\u003cstrong>Hello\u003c/strong>";\n// Displays the markup as text\n\nelement.innerHTML = "\u003cstrong>Hello\u003c/strong>";\n// Creates a strong element\n```',
    details:
      '`innerHTML` parses assigned strings as markup:\n\n```js\ncontainer.innerHTML = `\n  \u003cbutton class="save">Save\u003c/button>\n`;\n```\n\nAssigning untrusted input to it can create an XSS vulnerability:\n\n```js\ncontainer.innerHTML = userProvidedContent;\n```\n\n`textContent` does not parse markup, making it appropriate for user-provided plain text:\n\n```js\ncontainer.textContent = userProvidedContent;\n```\n\n`textContent` includes text from hidden elements and returns content close to the underlying DOM structure.\n\n`innerText` reflects rendered text:\n\n```html\n\u003cdiv id="message">\n  Visible\n  \u003cspan hidden>Hidden\u003c/span>\n\u003c/div>\n```\n\n`textContent` includes `"Hidden"`, while `innerText` generally does not.\n\nReading `innerText` may require layout calculation, making it potentially more expensive than `textContent`.\n\nSetting `textContent` or `innerHTML` replaces existing child nodes and removes their directly attached event listeners.',
  },
  {
    number: 64,
    question: "What are custom events?",
    answer:
      "Custom events are application-defined DOM events used to communicate that a meaningful action or state change has occurred.\n\nThey are created with `CustomEvent` and can carry additional information through the `detail` property.",
    explanation:
      'Custom events let components communicate without calling each other’s internal functions directly.\n\n```js\nconst event = new CustomEvent("cart:item-added", {\n  detail: {\n    productId: 42\n  }\n});\n\nelement.dispatchEvent(event);\n```',
    details:
      'A listener receives the custom data through `event.detail`:\n\n```js\nelement.addEventListener("cart:item-added", event => {\n  console.log(event.detail.productId);\n});\n```\n\nCustom events do not bubble by default. Enable bubbling when delegation or ancestor communication is required:\n\n```js\nconst event = new CustomEvent("cart:item-added", {\n  bubbles: true,\n  detail: {\n    productId: 42\n  }\n});\n```\n\nThey can also be cancelable:\n\n```js\nconst event = new CustomEvent("dialog:before-close", {\n  cancelable: true\n});\n\nconst allowed = dialog.dispatchEvent(event);\n\nif (allowed) {\n  dialog.close();\n}\n```\n\nA listener can prevent the action:\n\n```js\ndialog.addEventListener("dialog:before-close", event => {\n  if (hasUnsavedChanges) {\n    event.preventDefault();\n  }\n});\n```\n\nCustom event names should be descriptive and namespaced to reduce collisions.',
  },
  {
    number: 65,
    question: "What are the differences between cookies, localStorage, and sessionStorage?",
    answer:
      "All three store data in the browser, but they differ in lifetime, capacity, server interaction, and scope.\n\n- Cookies can be sent automatically with HTTP requests and support security attributes.\n- `localStorage` persists until explicitly cleared.\n- `sessionStorage` lasts for the lifetime of a particular browser tab.",
    explanation:
      "Web Storage is convenient for non-sensitive client-side data. Cookies are appropriate when the server must receive the value, particularly for session management.",
    details:
      'A simplified comparison:\n\n| Feature | Cookies | `localStorage` | `sessionStorage` |\n|---|---|---|---|\n| Lifetime | Configurable | Until cleared | Current tab session |\n| Sent to server | Usually, when applicable | No | No |\n| API | String-based cookie header | Key/value | Key/value |\n| Typical capacity | Small | Larger | Larger |\n| Server can set it | Yes | No | No |\n\nWeb Storage stores strings:\n\n```js\nlocalStorage.setItem(\n  "preferences",\n  JSON.stringify({ theme: "dark" })\n);\n\nconst preferences = JSON.parse(\n  localStorage.getItem("preferences")\n);\n```\n\nStorage operations are synchronous and can block the main thread when overused.\n\nSensitive data should not be placed in Web Storage because JavaScript running on the origin can access it. An XSS vulnerability could expose it.\n\nAuthentication cookies can use protections such as:\n\n- `HttpOnly`\n- `Secure`\n- `SameSite`\n- Restricted `Domain` and `Path`\n- Appropriate expiration\n\n`HttpOnly` cookies cannot be read through JavaScript, reducing token theft through XSS, although XSS remains dangerous.',
  },
  {
    number: 66,
    question: "What is the same-origin policy?",
    answer:
      "The same-origin policy is a browser security rule that restricts how documents or scripts from one origin can access resources and data from another origin.\n\nAn origin is defined by its scheme, host, and port.",
    explanation:
      "These URLs do not all share the same origin:\n\n```text\nhttps://example.com\nhttp://example.com\nhttps://api.example.com\nhttps://example.com:8443\n```\n\nEach differs by scheme, host, or port.",
    details:
      'The policy restricts operations such as:\n\n- Reading cross-origin responses\n- Accessing another origin’s DOM\n- Reading another origin’s storage\n- Inspecting cross-origin frames\n\nSome cross-origin operations are allowed in limited ways. For example, a page can often display a cross-origin image but cannot freely inspect its pixel data through a canvas.\n\nCross-origin network requests may be permitted through CORS. Cross-document communication can use `window.postMessage()`:\n\n```js\notherWindow.postMessage(\n  { type: "READY" },\n  "https://trusted.example"\n);\n```\n\nThe receiving page should validate the sender:\n\n```js\nwindow.addEventListener("message", event => {\n  if (event.origin !== "https://trusted.example") {\n    return;\n  }\n\n  console.log(event.data);\n});\n```\n\nThe same-origin policy is enforced by browsers. It does not prevent direct server-to-server requests.',
  },
  {
    number: 67,
    question: "What is CORS?",
    answer:
      "Cross-Origin Resource Sharing, or CORS, is an HTTP-header mechanism that allows a server to tell browsers which origins may read its responses.\n\nCORS relaxes selected same-origin policy restrictions. It is configured by the server and enforced by the browser.",
    explanation:
      "The server may allow a particular origin with a response header:\n\n```http\nAccess-Control-Allow-Origin: https://app.example.com\n```\n\nThe frontend cannot grant itself CORS permission.",
    details:
      'Some cross-origin requests are considered simple and may be sent directly. The browser still checks the response headers before exposing the response to JavaScript.\n\nOther requests trigger a preflight request using `OPTIONS`:\n\n```http\nOPTIONS /api/users\nOrigin: https://app.example.com\nAccess-Control-Request-Method: PUT\nAccess-Control-Request-Headers: Content-Type\n```\n\nThe server responds with allowed methods, headers, and origins.\n\nCredentialed requests require explicit configuration:\n\n```js\nfetch("https://api.example.com/user", {\n  credentials: "include"\n});\n```\n\nThe server must allow credentials and cannot use `*` as the allowed origin for that credentialed response.\n\nCORS is not an authentication system. A server must still validate permissions and credentials.\n\nCORS also does not stop non-browser clients from making requests. Its main purpose is controlling whether browser JavaScript can access cross-origin responses.',
  },
  {
    number: 68,
    question: "How does the Fetch API work?",
    answer:
      "The Fetch API performs HTTP requests and returns a Promise that fulfills with a `Response` object.\n\nThe response body is read asynchronously using methods such as `json`, `text`, `blob`, or `arrayBuffer`.",
    explanation:
      'Fetch does not automatically reject for HTTP error statuses such as `404` or `500`. The response status must be checked.\n\n```js\nconst response = await fetch("/api/users");\n\nif (!response.ok) {\n  throw new Error(`HTTP ${response.status}`);\n}\n\nconst users = await response.json();\n```',
    details:
      'A POST request can be made with:\n\n```js\nconst response = await fetch("/api/users", {\n  method: "POST",\n  headers: {\n    "Content-Type": "application/json"\n  },\n  body: JSON.stringify({\n    name: "Alex"\n  })\n});\n```\n\nUseful response properties include:\n\n```js\nresponse.ok;\nresponse.status;\nresponse.statusText;\nresponse.headers;\nresponse.url;\n```\n\nThe body is a stream and normally can be consumed only once:\n\n```js\nawait response.json();\nawait response.text(); // Error: body already consumed\n```\n\nClone the response first if two consumers are genuinely required:\n\n```js\nconst copy = response.clone();\n```\n\nFetch can include an abort signal:\n\n```js\nconst controller = new AbortController();\n\nfetch("/api/users", {\n  signal: controller.signal\n});\n```\n\nRequest options also control credentials, caching, redirects, and request mode. Application code should handle:\n\n- Network failures\n- HTTP failures\n- Invalid response data\n- Cancellation\n- Timeouts\n- Loading and retry states',
  },
  {
    number: 69,
    question: "What is the difference between Fetch and XMLHttpRequest?",
    answer:
      "Fetch is the modern Promise-based API for HTTP requests. `XMLHttpRequest`, or XHR, is an older event- and callback-based API.\n\nFetch usually provides cleaner composition, better integration with `async`/`await`, and direct access to request and response streams.",
    explanation:
      "Fetch is preferred for most modern application code, while XHR may still appear in older systems or specialized upload-progress implementations.",
    details:
      'A Fetch request:\n\n```js\nconst response = await fetch("/api/users");\n\nif (!response.ok) {\n  throw new Error(`HTTP ${response.status}`);\n}\n\nconst users = await response.json();\n```\n\nA comparable XHR request:\n\n```js\nconst request = new XMLHttpRequest();\n\nrequest.open("GET", "/api/users");\n\nrequest.onload = () => {\n  if (request.status >= 200 && request.status \u003c 300) {\n    const users = JSON.parse(request.responseText);\n    console.log(users);\n  }\n};\n\nrequest.onerror = () => {\n  console.error("Network error");\n};\n\nrequest.send();\n```\n\nKey differences include:\n\n- Fetch uses Promises.\n- XHR uses events and callbacks.\n- Fetch supports modern stream APIs.\n- XHR has established upload-progress events.\n- Fetch requires explicit checking of HTTP failure statuses.\n- Cancellation uses `AbortController` with Fetch and `abort()` with XHR.\n\nDownload progress can be implemented with Fetch streams, although it requires manual processing. Upload progress support has historically been simpler with XHR.',
  },
  {
    number: 70,
    question: "What is the History API?",
    answer:
      "The History API lets browser applications interact with the current tab’s session history.\n\nIt can add or replace history entries and respond when the user navigates backward or forward.",
    explanation:
      'Single-page applications use it to update the URL without performing a full page reload.\n\n```js\nhistory.pushState(\n  { page: "profile" },\n  "",\n  "/profile"\n);\n```',
    details:
      'The main methods are:\n\n```js\nhistory.pushState(state, "", url);\nhistory.replaceState(state, "", url);\nhistory.back();\nhistory.forward();\nhistory.go(-2);\n```\n\n`pushState` adds a new entry. `replaceState` updates the current entry without adding another one.\n\nBack and forward navigation can be observed with `popstate`:\n\n```js\nwindow.addEventListener("popstate", event => {\n  renderRoute(location.pathname, event.state);\n});\n```\n\nCalling `pushState` or `replaceState` does not itself trigger `popstate`.\n\nThe new URL must generally have the same origin as the current page.\n\nClient-side routing also requires server configuration. If a user directly loads `/profile`, the server must return the application rather than an unrelated `404` response.\n\nThe History API should be paired with semantic links where possible so navigation remains accessible and behaves correctly with normal browser features.',
  },
  {
    number: 71,
    question: "What are Web Workers, and when should they be used?",
    answer:
      "Web Workers run JavaScript in a background thread separate from the page’s main JavaScript thread.\n\nThey are useful for CPU-intensive work that would otherwise block rendering and user interaction.",
    explanation:
      'Workers cannot directly access the DOM. They communicate with the main thread by exchanging messages.\n\n```js\nconst worker = new Worker("./worker.js");\n\nworker.postMessage({ values: [1, 2, 3] });\n\nworker.addEventListener("message", event => {\n  console.log(event.data);\n});\n```',
    details:
      'Worker code receives and sends messages:\n\n```js\n// worker.js\nself.addEventListener("message", event => {\n  const result = performExpensiveCalculation(\n    event.data.values\n  );\n\n  self.postMessage(result);\n});\n```\n\nSuitable use cases include:\n\n- Processing large datasets\n- Image or audio manipulation\n- Complex calculations\n- Parsing large files\n- Compression\n- Cryptographic work\n- Background data transformation\n\nWorkers are usually unnecessary for ordinary network requests because Fetch is already asynchronous.\n\nData is normally transferred using the structured clone algorithm, so sending very large values has a cost:\n\n```js\nworker.postMessage(largeObject);\n```\n\nTransferable objects can transfer ownership of certain data without copying:\n\n```js\nworker.postMessage(buffer, [buffer]);\n```\n\nAfter transfer, the original thread can no longer use that buffer.\n\nCommon worker types include:\n\n- Dedicated Workers, used by one page context\n- Shared Workers, potentially shared by multiple contexts\n- Service Workers, used for network interception, caching, and offline capabilities\n\nThese worker types solve different problems and have different lifecycles and APIs.',
  },
  {
    number: 72,
    question: "What are debounce and throttle?",
    answer:
      "Debounce and throttle limit how frequently a function executes.\n\n- Debounce waits until calls have stopped for a specified period before executing.\n- Throttle allows execution at most once during each specified interval.",
    explanation:
      "Debounce is useful when only the final action matters, such as search input. Throttle is useful when continuous updates are needed at a controlled rate, such as scroll tracking.",
    details:
      'A debounced search waits until the user pauses typing:\n\n```js\nconst search = debounce(query => {\n  fetchResults(query);\n}, 300);\n\ninput.addEventListener("input", event => {\n  search(event.target.value);\n});\n```\n\nA throttled scroll handler runs periodically while scrolling continues:\n\n```js\nconst handleScroll = throttle(() => {\n  updateScrollPosition(window.scrollY);\n}, 200);\n\nwindow.addEventListener("scroll", handleScroll);\n```\n\nA basic debounce implementation:\n\n```js\nfunction debounce(callback, delay) {\n  let timeoutId;\n\n  return function (...args) {\n    clearTimeout(timeoutId);\n\n    timeoutId = setTimeout(() => {\n      callback.apply(this, args);\n    }, delay);\n  };\n}\n```\n\nDebounce and throttle implementations may support:\n\n- Leading-edge execution\n- Trailing-edge execution\n- Cancellation\n- Immediate flushing\n- A maximum waiting time\n\nFor visual updates tied to the browser’s refresh cycle, `requestAnimationFrame` may be more appropriate than time-based throttling.',
  },
  {
    number: 73,
    question: "What causes memory leaks in frontend applications?",
    answer:
      "A memory leak occurs when an application keeps references to data that it no longer needs, preventing garbage collection.\n\nCommon causes include forgotten event listeners, active timers, retained DOM nodes, long-lived closures, unbounded caches, subscriptions, and detached components.",
    explanation:
      "Memory leaks gradually increase memory usage and may make an application slow or unstable during long sessions.",
    details:
      'A listener can keep its related data reachable:\n\n```js\nfunction mount() {\n  const largeData = loadLargeData();\n\n  function handleResize() {\n    useData(largeData);\n  }\n\n  window.addEventListener("resize", handleResize);\n}\n```\n\nIf the component is removed but the listener remains, `handleResize` and `largeData` may stay in memory.\n\nCleanup should remove the listener:\n\n```js\nwindow.removeEventListener("resize", handleResize);\n```\n\nAn `AbortController` can manage listener cleanup:\n\n```js\nconst controller = new AbortController();\n\nwindow.addEventListener("resize", handleResize, {\n  signal: controller.signal\n});\n\ncontroller.abort();\n```\n\nOther common cleanup operations include:\n\n```js\nclearInterval(intervalId);\nobserver.disconnect();\nsubscription.unsubscribe();\ncontroller.abort();\n```\n\nDetached DOM nodes can remain in memory if JavaScript still references them:\n\n```js\nconst removedElement = document.querySelector(".panel");\nremovedElement.remove();\n\n// removedElement still references the detached node\n```\n\nBrowser memory tools, heap snapshots, and allocation timelines help identify objects that remain reachable unexpectedly.',
  },
  {
    number: 74,
    question: "How does JavaScript garbage collection work?",
    answer:
      "JavaScript automatically reclaims memory occupied by values that are no longer reachable from active roots.\n\nModern engines primarily use reachability-based garbage collection, commonly involving tracing algorithms such as mark-and-sweep along with generational and incremental optimizations.",
    explanation:
      "Developers do not manually free JavaScript objects. They help garbage collection by removing unnecessary references and cleaning up external resources.",
    details:
      "Garbage-collection roots typically include:\n\n- The global object\n- Active call-stack variables\n- Active closures\n- Registered callbacks\n- Reachable DOM and platform objects\n\nDuring a simplified mark-and-sweep process, the engine:\n\n1. Starts from known roots.\n2. Marks every value reachable from those roots.\n3. Reclaims unmarked values.\n\nCircular references are not automatically leaks:\n\n```js\nlet first = {};\nlet second = {};\n\nfirst.other = second;\nsecond.other = first;\n\nfirst = null;\nsecond = null;\n```\n\nThe objects can be collected because neither remains reachable from a root.\n\nSetting a local variable to `null` is not usually necessary. It can help when a long-lived object holds a large value that is no longer needed:\n\n```js\ncache.largeResult = null;\n```\n\nGarbage collection is nondeterministic. Code cannot rely on it happening at a particular moment.\n\nResources such as event listeners, network requests, observers, and timers often require explicit cleanup even though their associated JavaScript memory is managed automatically.",
  },
  {
    number: 75,
    question: "What are reflow and repaint?",
    answer:
      "Reflow, often called layout, recalculates the size and position of elements.\n\nRepaint redraws pixels when visual properties change without necessarily changing layout. Compositing combines rendered layers into the final image.",
    explanation:
      "Layout changes are usually more expensive because they can affect multiple elements and may be followed by paint and compositing work.",
    details:
      "Properties that can trigger layout include:\n\n- Width and height\n- Margin and padding\n- Font size\n- Positioning values\n- Content changes\n- Adding or removing elements\n\nPaint-only changes can include properties such as colors, backgrounds, borders, and shadows.\n\nProperties such as `transform` and `opacity` can often be handled mainly during compositing, making them preferable for animations:\n\n```css\n.panel {\n  transform: translateX(100px);\n  opacity: 0.5;\n}\n```\n\nRepeatedly mixing DOM reads and writes can cause layout thrashing:\n\n```js\nfor (const element of elements) {\n  const width = element.offsetWidth;\n  element.style.width = `${width + 10}px`;\n}\n```\n\nGroup reads and writes instead:\n\n```js\nconst widths = elements.map(\n  element => element.offsetWidth\n);\n\nelements.forEach((element, index) => {\n  element.style.width = `${widths[index] + 10}px`;\n});\n```\n\nNot every style change forces immediate work. Browsers often batch rendering until JavaScript yields, but reading layout-dependent measurements may force pending calculations.",
  },
  {
    number: 76,
    question: "What is the difference between async and defer on script elements?",
    answer:
      "Both `async` and `defer` let external scripts download without blocking HTML parsing.\n\n- `async` executes each script as soon as it finishes downloading, without preserving document order.\n- `defer` waits until HTML parsing finishes and preserves script order.",
    explanation:
      "Use `defer` for application scripts that depend on the DOM or each other. Use `async` for independent scripts whose execution order does not matter.",
    details:
      'A normal external script blocks parsing:\n\n```html\n\u003cscript src="app.js">\u003c/script>\n```\n\nThe browser pauses HTML parsing while it downloads and executes the script.\n\nDeferred scripts preserve their document order:\n\n```html\n\u003cscript defer src="library.js">\u003c/script>\n\u003cscript defer src="app.js">\u003c/script>\n```\n\n`library.js` executes before `app.js`, even if `app.js` downloads first. Deferred scripts execute before `DOMContentLoaded`.\n\nAsync scripts execute as soon as they are ready:\n\n```html\n\u003cscript async src="analytics.js">\u003c/script>\n```\n\nMultiple async scripts may execute in any order and may run before or after parsing completes.\n\nModule scripts are deferred by default:\n\n```html\n\u003cscript type="module" src="app.js">\u003c/script>\n```\n\nThe `async` attribute can also be added to a module script when immediate execution after dependency loading is appropriate.\n\n`async` and `defer` apply to external classic scripts. Their behavior does not meaningfully apply to an ordinary inline classic script without a `src`.',
  },
  {
    number: 77,
    question: "What is lazy loading?",
    answer:
      "Lazy loading delays downloading, initializing, or rendering a resource until it is likely to be needed.\n\nIt reduces initial page work and can improve startup time, bandwidth usage, and responsiveness.",
    explanation:
      'Images and iframes can use native lazy loading:\n\n```html\n\u003cimg\n  src="/photo.jpg"\n  alt="Mountain landscape"\n  loading="lazy"\n>\n```',
    details:
      'Common lazy-loaded resources include:\n\n- Images\n- Iframes\n- JavaScript modules\n- Routes\n- Large components\n- Videos\n- Data requested for off-screen content\n\nJavaScript can detect when content approaches the viewport with `IntersectionObserver`:\n\n```js\nconst observer = new IntersectionObserver(entries => {\n  for (const entry of entries) {\n    if (!entry.isIntersecting) {\n      continue;\n    }\n\n    loadContent(entry.target);\n    observer.unobserve(entry.target);\n  }\n});\n\nobserver.observe(document.querySelector(".gallery"));\n```\n\nModules can be loaded dynamically:\n\n```js\nconst module = await import("./editor.js");\nmodule.initializeEditor();\n```\n\nLazy loading has tradeoffs:\n\n- Loading too late can create visible delays.\n- Layout space should be reserved to prevent shifts.\n- Critical above-the-fold content should not normally be lazy-loaded.\n- Errors and loading states must be handled.\n- Excessively small chunks may add request overhead.\n\nPrefetching can complement lazy loading by downloading likely future resources during idle time.',
  },
  {
    number: 78,
    question: "What is code splitting?",
    answer:
      "Code splitting divides a JavaScript application into smaller bundles that can be loaded independently.\n\nIt reduces the amount of JavaScript required for the initial page and delays less important code until it is needed.",
    explanation:
      'Dynamic `import()` creates a natural split point in modern bundlers.\n\n```js\nconst editorModule = await import("./editor.js");\neditorModule.openEditor();\n```',
    details:
      'Common splitting strategies include:\n\n- Route-based splitting\n- Component-based splitting\n- Feature-based splitting\n- Splitting large third-party libraries\n- Loading administrative tools only for authorized users\n\nFor example, an application does not need to load checkout code on its home page:\n\n```js\nif (route === "/checkout") {\n  const checkout = await import("./checkout.js");\n  checkout.render();\n}\n```\n\nCode splitting can improve:\n\n- Initial bundle size\n- Parse and compilation time\n- Time to interactivity\n- Cache efficiency\n\nIt can also create costs:\n\n- Additional network requests\n- Loading states\n- Chunk-load failures\n- Dependency duplication\n- Delays when a feature is first opened\n\nPreloading or prefetching important future chunks can reduce interaction delays. Bundle analysis tools help identify large dependencies and ineffective split points.',
  },
  {
    number: 79,
    question: "What is tree shaking?",
    answer:
      "Tree shaking is a build optimization that removes exported code determined to be unused.\n\nIt relies primarily on the static structure of ES modules, allowing build tools to analyze `import` and `export` statements without executing the code.",
    explanation:
      'If a module exports several functions but an application imports only one, a bundler may exclude the unused exports from the production bundle.\n\n```js\n// utilities.js\nexport function add(a, b) {\n  return a + b;\n}\n\nexport function unusedFeature() {\n  // ...\n}\n```\n\n```js\nimport { add } from "./utilities.js";\n```',
    details:
      'Static ES module syntax is easier to analyze than dynamic module patterns:\n\n```js\nimport { add } from "./math.js";\n```\n\nTree shaking is most effective when:\n\n- ES modules are used\n- Production optimizations are enabled\n- Modules avoid unnecessary top-level side effects\n- Packages correctly describe side-effect behavior\n- Imports target tree-shakable entry points\n\nA top-level side effect may require a module to remain:\n\n```js\nregisterGlobalPlugin();\n```\n\nPackage metadata can identify files that must be preserved for their side effects:\n\n```json\n{\n  "sideEffects": [\n    "./src/global.css",\n    "./src/register.js"\n  ]\n}\n```\n\nTree shaking is different from minification:\n\n- Tree shaking removes unused code.\n- Minification rewrites retained code into a smaller form.\n\nImporting an entire library through a non-tree-shakable entry point may prevent unused features from being removed.',
  },
  {
    number: 80,
    question: "What is memoization?",
    answer:
      "Memoization caches a function’s result based on its inputs and returns the cached result when the same inputs occur again.\n\nIt exchanges additional memory and cache-management complexity for reduced computation time.",
    explanation:
      "Memoization is most suitable for deterministic, expensive functions that receive repeated inputs.\n\n```js\nfunction memoize(callback) {\n  const cache = new Map();\n\n  return argument => {\n    if (cache.has(argument)) {\n      return cache.get(argument);\n    }\n\n    const result = callback(argument);\n    cache.set(argument, result);\n    return result;\n  };\n}\n```",
    details:
      'A memoized calculation:\n\n```js\nconst calculate = memoize(number => {\n  console.log("Calculating");\n  return number * number;\n});\n\ncalculate(5); // Calculates and returns 25\ncalculate(5); // Returns cached 25\n```\n\nMultiple arguments require a reliable cache-key strategy:\n\n```js\nfunction memoizeTwoArguments(callback) {\n  const cache = new Map();\n\n  return (first, second) => {\n    const key = `${first}:${second}`;\n\n    if (!cache.has(key)) {\n      cache.set(key, callback(first, second));\n    }\n\n    return cache.get(key);\n  };\n}\n```\n\nString-based keys can collide for complex inputs. Nested `Map` objects or identity-based caches may be safer.\n\nObject arguments are compared by reference:\n\n```js\ncache.get({ id: 1 }); // Different object each time\n```\n\nMemoization is less useful when:\n\n- Inputs rarely repeat\n- Computation is inexpensive\n- Results change despite identical inputs\n- The cache can grow indefinitely\n- Cached data becomes stale\n\nFrontend frameworks may provide memoization tools, but memoization itself has a cost. It should address a measured performance problem rather than be added automatically.',
  },
  {
    number: 81,
    question: "How can frontend JavaScript performance be measured and improved?",
    answer:
      "Frontend performance should be measured with browser tools and real-user data before optimization.\n\nImprovements usually target loading, JavaScript execution, rendering, memory usage, network activity, and responsiveness.",
    explanation:
      "A useful process is:\n\n1. Measure the current experience.\n2. Identify the largest bottleneck.\n3. Apply a focused change.\n4. Measure again.\n5. Confirm that user-facing performance improved.",
    details:
      'Useful browser tools and APIs include:\n\n- Performance panel\n- Network panel\n- Memory profiler\n- Lighthouse\n- Performance API\n- `PerformanceObserver`\n- Coverage and bundle-analysis tools\n\nCustom timings can be recorded:\n\n```js\nperformance.mark("render-start");\n\nrenderDashboard();\n\nperformance.mark("render-end");\n\nperformance.measure(\n  "dashboard-render",\n  "render-start",\n  "render-end"\n);\n```\n\nCommon improvements include:\n\n- Reducing initial JavaScript\n- Code splitting and lazy loading\n- Removing unused dependencies\n- Optimizing images and fonts\n- Caching static resources\n- Avoiding layout thrashing\n- Virtualizing long lists\n- Moving heavy work to Web Workers\n- Debouncing or throttling frequent handlers\n- Reducing unnecessary renders\n- Breaking up long tasks\n\nImportant user-facing metrics include:\n\n- Largest Contentful Paint\n- Interaction to Next Paint\n- Cumulative Layout Shift\n- Time to First Byte\n\nLaboratory tests provide controlled comparisons, while real-user monitoring captures actual devices, networks, locations, and user behavior. Both are valuable.\n\nOptimizing a fast operation that does not affect the user is usually less valuable than fixing a measured rendering, loading, or interaction bottleneck.',
  },
  {
    number: 82,
    question: "What is the difference between requestAnimationFrame and setTimeout?",
    answer:
      "`requestAnimationFrame` schedules a callback before the browser’s next repaint.\n\n`setTimeout` schedules a task after a minimum time delay, without synchronizing it with rendering.",
    explanation:
      "Use `requestAnimationFrame` for visual updates and JavaScript-driven animations. Use `setTimeout` for general delayed work.\n\n```js\nrequestAnimationFrame(timestamp => {\n  updateAnimation(timestamp);\n});\n```",
    details:
      "A `requestAnimationFrame` loop can animate based on elapsed time:\n\n```js\nlet previousTime;\n\nfunction animate(currentTime) {\n  if (previousTime !== undefined) {\n    const elapsed = currentTime - previousTime;\n    updatePosition(elapsed);\n  }\n\n  previousTime = currentTime;\n  requestAnimationFrame(animate);\n}\n\nrequestAnimationFrame(animate);\n```\n\nUsing timestamps makes movement independent of the display’s refresh rate.\n\nThe browser can coordinate animation-frame callbacks with layout, paint, and compositing. It may pause or reduce them in background tabs to save resources.\n\n`setTimeout` provides only a minimum delay:\n\n```js\nsetTimeout(callback, 16);\n```\n\nThe callback may execute later if the call stack or task queue is busy. Repeated 16-millisecond timers are not reliably synchronized with the display.\n\nAnimation frames can be cancelled:\n\n```js\nconst frameId = requestAnimationFrame(animate);\ncancelAnimationFrame(frameId);\n```\n\nTimers use separate cancellation:\n\n```js\nconst timeoutId = setTimeout(callback, 1000);\nclearTimeout(timeoutId);\n```\n\n`requestAnimationFrame` is not automatically appropriate for expensive nonvisual calculations. Long callbacks still block the main thread and can cause dropped frames.",
  },
  {
    number: 83,
    question: "How do try, catch, finally, and throw work?",
    answer:
      "These statements provide structured error handling:\n\n- `try` contains code that may throw an error.\n- `catch` handles an error thrown inside the associated `try`.\n- `finally` runs after `try` and `catch`, whether an error occurred or not.\n- `throw` creates or propagates an exception.",
    explanation:
      'Use `try...catch` when code can fail in a way the application can meaningfully handle.\n\n```js\ntry {\n  const data = JSON.parse(input);\n  console.log(data);\n} catch (error) {\n  console.error("Invalid JSON", error);\n} finally {\n  console.log("Parsing attempt finished");\n}\n```',
    details:
      'Any JavaScript value can technically be thrown, but an `Error` object should normally be used:\n\n```js\nthrow new Error("Unable to load user");\n```\n\n`Error` objects provide useful information such as:\n\n- `name`\n- `message`\n- `stack`\n- `cause`\n\nCustom validation can throw an error:\n\n```js\nfunction setAge(age) {\n  if (!Number.isInteger(age) || age \u003c 0) {\n    throw new RangeError(\n      "Age must be a non-negative integer"\n    );\n  }\n\n  return age;\n}\n```\n\nA caught error can be handled, transformed, or rethrown:\n\n```js\ntry {\n  await saveUser(user);\n} catch (error) {\n  throw new Error("Could not save user", {\n    cause: error\n  });\n}\n```\n\n`finally` is useful for cleanup:\n\n```js\nsetLoading(true);\n\ntry {\n  await loadData();\n} catch (error) {\n  showError(error);\n} finally {\n  setLoading(false);\n}\n```\n\nA `return` inside `finally` overrides an earlier return or thrown error, so it should generally be avoided:\n\n```js\nfunction example() {\n  try {\n    throw new Error("Failed");\n  } finally {\n    return "Success"; // Suppresses the error\n  }\n}\n```\n\n`try...catch` handles synchronous exceptions and awaited Promise rejections. It does not catch an unawaited Promise rejection:\n\n```js\ntry {\n  fetchData(); // A later rejection is not caught here\n} catch (error) {\n  // Does not handle the Promise rejection\n}\n```\n\nCorrect version:\n\n```js\ntry {\n  await fetchData();\n} catch (error) {\n  console.error(error);\n}\n```',
  },
  {
    number: 84,
    question: "What is the difference between syntax, runtime, and logical errors?",
    answer:
      "- A syntax error means the code does not follow JavaScript’s grammar and cannot be parsed correctly.\n- A runtime error occurs while otherwise valid code is executing.\n- A logical error allows the program to run but produces incorrect behavior or results.",
    explanation:
      "Syntax and runtime errors usually produce exceptions. Logical errors may not produce any error message, making tests and debugging particularly important.",
    details:
      'A syntax error prevents the affected script from being parsed:\n\n```js\nfunction greet( {\n  console.log("Hello");\n}\n```\n\nA runtime error happens during execution:\n\n```js\nconst user = null;\nconsole.log(user.name);\n// TypeError\n```\n\nOther common runtime errors include:\n\n- `ReferenceError`\n- `TypeError`\n- `RangeError`\n- `URIError`\n- Custom application errors\n\nA logical error produces the wrong result:\n\n```js\nfunction calculateDiscount(price) {\n  return price * 1.2; // Increases the price\n}\n```\n\nThe code is valid and executes successfully, but the business logic is incorrect.\n\nTools for finding these errors include:\n\n- Syntax highlighting and linters\n- Browser developer tools\n- Breakpoints\n- Stack traces\n- Logging\n- Static type checking\n- Automated tests\n- Code review\n\nTests are especially valuable for logical errors because the JavaScript engine cannot know the intended business behavior.',
  },
  {
    number: 85,
    question: "What is cross-site scripting (XSS), and how can it be prevented?",
    answer:
      "Cross-site scripting is a vulnerability in which untrusted content is interpreted and executed as code in another user’s browser within a trusted application’s context.\n\nPrevention requires context-appropriate output encoding, safe DOM APIs, HTML sanitization when HTML is intentionally accepted, framework protections, and defense-in-depth controls.",
    explanation:
      "Untrusted text should be inserted with a safe text API instead of being parsed as HTML:\n\n```js\nmessage.textContent = userInput;\n```\n\nAvoid inserting untrusted content directly with `innerHTML`.",
    details:
      "Common categories include:\n\n- Stored XSS, where malicious content is persisted\n- Reflected XSS, where malicious input is included in an immediate response\n- DOM-based XSS, where unsafe client-side code writes attacker-controlled data into a dangerous DOM location\n\nUnsafe example:\n\n```js\noutput.innerHTML = location.hash.slice(1);\n```\n\nSafer plain-text version:\n\n```js\noutput.textContent = location.hash.slice(1);\n```\n\nSecurity depends on where the value is inserted. HTML content, attributes, URLs, CSS, and JavaScript have different parsing rules and require context-specific protections.\n\nImportant protections include:\n\n- Use framework output escaping correctly.\n- Use safe sinks such as `textContent`.\n- Sanitize untrusted HTML with a well-maintained sanitizer when HTML must be supported.\n- Validate URL schemes and destinations.\n- Avoid dangerous APIs such as `document.write`.\n- Avoid constructing executable JavaScript from strings.\n- Use Content Security Policy as an additional layer.\n- Consider Trusted Types where supported.\n\nSanitization is different from validation. Validation checks whether input matches expected rules. Sanitization transforms allowed HTML to remove unsafe content.\n\nContent Security Policy can reduce impact but should not be treated as the only XSS defense. Cookie attributes may also reduce certain consequences but do not prevent malicious script execution.\n\nSee the [OWASP Cross-Site Scripting Prevention Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Cross_Site_Scripting_Prevention_Cheat_Sheet.html).",
  },
  {
    number: 86,
    question: "What is cross-site request forgery (CSRF), and how can it be prevented?",
    answer:
      "Cross-site request forgery is an attack that tricks an authenticated browser into sending an unwanted state-changing request to a trusted application.\n\nIt commonly works when the browser automatically includes authentication credentials, such as session cookies, but the server does not verify that the intended application initiated the request.",
    explanation:
      "CSRF prevention is primarily enforced on the server through protections such as unpredictable CSRF tokens, appropriate `SameSite` cookies, and origin validation.",
    details:
      'A malicious page might attempt to trigger an authenticated action:\n\n```html\n\u003cform\n  action="https://bank.example/transfer"\n  method="POST"\n>\n  \u003cinput name="amount" value="1000">\n  \u003cinput name="destination" value="attacker">\n\u003c/form>\n```\n\nIf the browser automatically sends the victim’s session cookie and the server accepts the request without additional verification, the action may succeed.\n\nCommon protections include:\n\n- Synchronizer CSRF tokens\n- Properly implemented signed double-submit cookies\n- `SameSite` cookie attributes\n- Verification of `Origin` or `Referer`\n- Reauthentication for highly sensitive actions\n- Custom request headers where appropriate\n- Avoiding state changes through safe methods such as `GET`\n\nA token-based request may look like:\n\n```js\nawait fetch("/api/profile", {\n  method: "PATCH",\n  headers: {\n    "Content-Type": "application/json",\n    "X-CSRF-Token": csrfToken\n  },\n  credentials: "same-origin",\n  body: JSON.stringify(profile)\n});\n```\n\nThe server must verify that the token is valid, secret, unpredictable, and associated with the correct session.\n\n`SameSite` is valuable, but it should generally be treated as defense in depth unless the application meets strict conditions allowing it to stand alone.\n\nCORS is not a complete CSRF defense. Some cross-origin requests can be sent without CORS permission even when JavaScript cannot read the response.\n\nXSS can often defeat CSRF protections because malicious script running within the trusted origin can access application capabilities. Both vulnerabilities must be addressed.\n\nSee the [OWASP Cross-Site Request Forgery Prevention Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html).',
  },
  {
    number: 87,
    question: "Why is using eval dangerous?",
    answer:
      "`eval()` parses and executes a string as JavaScript code.\n\nIt is dangerous because untrusted input can become executable code, it makes program behavior harder to analyze, and it prevents many engine and build-time optimizations.",
    explanation:
      "Never pass user-controlled or externally controlled content to `eval`.\n\n```js\neval(userInput); // Dangerous\n```\n\nPrefer normal JavaScript operations, data parsing, or explicit mappings.",
    details:
      'Code injection can occur when data is combined with executable source:\n\n```js\nconst expression = getUserInput();\nconst result = eval(expression);\n```\n\nAn attacker may supply code that reads page data, changes application behavior, or performs authenticated actions.\n\nSafer alternatives depend on the intended task.\n\nParse JSON with:\n\n```js\nconst data = JSON.parse(jsonText);\n```\n\nAccess a dynamic property with bracket notation:\n\n```js\nconst value = object[propertyName];\n```\n\nSelect a known operation with a mapping:\n\n```js\nconst operations = {\n  add: (a, b) => a + b,\n  subtract: (a, b) => a - b\n};\n\nconst operation = operations[operationName];\n\nif (!operation) {\n  throw new Error("Unsupported operation");\n}\n\nconst result = operation(first, second);\n```\n\nSet a timer with a function rather than a string:\n\n```js\nsetTimeout(() => {\n  runTask();\n}, 1000);\n```\n\nAvoid:\n\n```js\nsetTimeout("runTask()", 1000);\n```\n\nThe `Function` constructor has similar code-generation risks:\n\n```js\nnew Function(userInput);\n```\n\nIndirect `eval` changes scope behavior but does not make untrusted code safe.\n\nSee the [MDN `eval()` reference](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/eval).',
  },
  {
    number: 88,
    question: "What is strict mode?",
    answer:
      'Strict mode is a restricted JavaScript execution mode that converts some silent mistakes into errors, prevents certain unsafe behavior, and simplifies language semantics.\n\nIt can be enabled for a classic script or individual function with `"use strict"`. ES modules and class bodies use strict mode automatically.',
    explanation:
      'Strict mode catches accidental global-variable creation:\n\n```js\n"use strict";\n\nusername = "Alex";\n// ReferenceError\n```',
    details:
      'In non-strict classic code, assigning to an undeclared identifier may create a property on the global object. Strict mode rejects it.\n\nStrict mode also changes standalone function `this`:\n\n```js\n"use strict";\n\nfunction showThis() {\n  console.log(this);\n}\n\nshowThis(); // undefined\n```\n\nWithout strict mode in an older classic browser script, `this` may become the global object.\n\nOther strict-mode behaviors include:\n\n- Rejecting assignments to non-writable properties\n- Rejecting deletion of plain identifiers\n- Preventing duplicate parameter names in ordinary function syntax\n- Reserving certain identifiers\n- Restricting some `eval` behavior\n- Removing some historically error-prone semantics\n\nEnable it for an entire classic script by placing the directive first:\n\n```js\n"use strict";\n\nfunction run() {\n  // Strict mode applies here\n}\n```\n\nOr for one function:\n\n```js\nfunction run() {\n  "use strict";\n  // Strict only inside this function\n}\n```\n\nBlocks cannot independently enable strict mode.\n\nModern projects using ES modules already receive strict-mode behavior:\n\n```html\n\u003cscript type="module" src="app.js">\u003c/script>\n```\n\nSee the [MDN strict mode reference](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Strict_mode).',
  },
  {
    number: 89,
    question: "What is optional chaining?",
    answer:
      "Optional chaining uses `?.` to access a property, call a function, or read an indexed value only when the preceding value is not `null` or `undefined`.\n\nIf the preceding value is nullish, the expression short-circuits and returns `undefined`.",
    explanation:
      "It avoids errors when optional intermediate values may be missing.\n\n```js\nconst city = user.address?.city;\n```\n\nIf `user.address` is `null` or `undefined`, `city` becomes `undefined`.",
    details:
      'Optional property access:\n\n```js\nuser.profile?.name;\n```\n\nOptional element access:\n\n```js\nusers?.[0];\ndictionary?.[key];\n```\n\nOptional function call:\n\n```js\noptions.onComplete?.();\n```\n\nOptional chaining checks only for `null` and `undefined`. Other falsy values remain valid:\n\n```js\nconst data = {\n  count: 0,\n  label: ""\n};\n\ndata.count?.toString(); // "0"\n```\n\nChaining can continue across multiple optional levels:\n\n```js\nconst country =\n  response.data?.user?.address?.country;\n```\n\nBe careful with grouping:\n\n```js\nuser?.profile.name;\n```\n\nThis protects access to `user`, but if `user` exists and `profile` is nullish, accessing `.name` still fails. Safer version:\n\n```js\nuser?.profile?.name;\n```\n\nOptional chaining cannot be used on the left side of assignment:\n\n```js\nuser?.name = "Alex"; // SyntaxError\n```\n\nIt should not hide required-data errors. If a value must exist, explicit validation may communicate the contract better.',
  },
  {
    number: 90,
    question: "What is nullish coalescing?",
    answer:
      "The nullish coalescing operator `??` returns its right-hand operand only when its left-hand operand is `null` or `undefined`.\n\nUnlike `||`, it does not replace other falsy values such as `0`, `false`, or an empty string.",
    explanation:
      "It is useful when falsy values are valid data:\n\n```js\nconst count = 0;\n\ncount || 10; // 10\ncount ?? 10; // 0\n```",
    details:
      'Examples:\n\n```js\nnull ?? "default";      // "default"\nundefined ?? "default"; // "default"\nfalse ?? true;          // false\n0 ?? 100;               // 0\n"" ?? "untitled";       // ""\n```\n\nNullish coalescing is often combined with optional chaining:\n\n```js\nconst city =\n  user.address?.city ?? "Unknown city";\n```\n\nThe right-hand expression is evaluated only when needed:\n\n```js\nconst value = cachedValue ?? calculateValue();\n```\n\n`calculateValue()` runs only if `cachedValue` is `null` or `undefined`.\n\nNullish assignment updates a variable only when it is nullish:\n\n```js\nsettings.theme ??= "light";\n```\n\nJavaScript does not allow `??` to be mixed directly with `&&` or `||` without parentheses:\n\n```js\nvalue || fallback ?? defaultValue;\n// SyntaxError\n```\n\nThe intended grouping must be explicit:\n\n```js\n(value || fallback) ?? defaultValue;\n```',
  },
  {
    number: 91,
    question: "What are common ways to test JavaScript code?",
    answer:
      "JavaScript code can be tested through automated unit, integration, component, end-to-end, accessibility, performance, and visual tests.\n\nStatic analysis, type checking, linting, and manual exploratory testing complement automated tests but test different qualities.",
    explanation:
      "Effective test suites focus on observable behavior and combine fast focused tests with a smaller number of realistic full-system tests.",
    details:
      'Common testing layers include:\n\n- Unit tests for isolated logic\n- Integration tests for cooperating modules\n- Component tests for UI behavior\n- End-to-end tests for complete user journeys\n- Contract tests for service boundaries\n- Visual regression tests for unexpected layout changes\n- Accessibility tests for detectable accessibility issues\n- Performance tests for speed and resource budgets\n\nA simple behavior-focused test might look like:\n\n```js\nfunction calculateTotal(items) {\n  return items.reduce(\n    (total, item) => total + item.price,\n    0\n  );\n}\n\ntest("calculates the total price", () => {\n  const items = [\n    { price: 10 },\n    { price: 15 }\n  ];\n\n  expect(calculateTotal(items)).toBe(25);\n});\n```\n\nUI tests should generally interact with the interface as a user would:\n\n```js\nconst button = screen.getByRole("button", {\n  name: "Save"\n});\n\nawait user.click(button);\n\nexpect(\n  screen.getByText("Saved successfully")\n).toBeVisible();\n```\n\nUseful testing principles include:\n\n- Test behavior rather than implementation details.\n- Keep tests deterministic.\n- Avoid unnecessary mocks.\n- Test success, failure, empty, and boundary cases.\n- Give tests descriptive names.\n- Ensure failures clearly identify broken behavior.\n- Run important tests automatically in continuous integration.\n\nAutomated accessibility tools identify many issues but cannot replace manual keyboard, screen-reader, and usability testing.',
  },
  {
    number: 92,
    question: "What is the difference between unit, integration, and end-to-end testing?",
    answer:
      "- Unit tests verify a small piece of logic in isolation.\n- Integration tests verify that multiple parts work correctly together.\n- End-to-end tests verify complete user workflows through the running application.\n\nThe levels differ in scope, realism, speed, maintenance cost, and diagnostic precision.",
    explanation:
      "A balanced test strategy uses each level for the behavior it can verify most effectively.",
    details:
      'A unit test might verify a formatting function:\n\n```js\ntest("formats a full name", () => {\n  expect(\n    formatName({\n      first: "Alex",\n      last: "Smith"\n    })\n  ).toBe("Alex Smith");\n});\n```\n\nUnit tests are typically:\n\n- Fast\n- Focused\n- Easy to run frequently\n- Good at identifying the source of a failure\n\nAn integration test might render a form, submit it, and verify its interaction with a mocked or local API layer:\n\n```js\ntest("submits valid profile data", async () => {\n  render(\u003cProfileForm />);\n\n  await user.type(\n    screen.getByLabelText("Name"),\n    "Alex"\n  );\n\n  await user.click(\n    screen.getByRole("button", {\n      name: "Save"\n    })\n  );\n\n  expect(api.saveProfile).toHaveBeenCalledWith({\n    name: "Alex"\n  });\n});\n```\n\nIntegration tests cover boundaries that isolated unit tests may miss.\n\nAn end-to-end test might:\n\n1. Open the application in a real browser.\n2. Sign in.\n3. Add an item to a cart.\n4. Complete checkout.\n5. Verify the confirmation page.\n\nEnd-to-end tests provide strong confidence in important user journeys but are generally slower, more expensive to maintain, and harder to debug.\n\nNot every function needs a unit test, and not every variation needs an end-to-end test. High-value business rules benefit from focused tests, while critical user journeys benefit from a smaller number of reliable end-to-end tests.',
  },
  {
    number: 93,
    question: "How would you reverse a string?",
    answer:
      'For simple strings, convert the string to an array, reverse the array, and join it again.\n\n```js\nfunction reverseString(value) {\n  return [...value].reverse().join("");\n}\n```',
    explanation:
      'Strings are immutable, so the function creates and returns a new string.\n\n```js\nreverseString("hello"); // "olleh"\n```',
    details:
      'Spread syntax handles Unicode code points better than `split("")`:\n\n```js\n[..."A😀B"];\n// ["A", "😀", "B"]\n```\n\nBy comparison, `split("")` separates the emoji into UTF-16 code units:\n\n```js\n"A😀B".split("");\n// ["A", "\\uD83D", "\\uDE00", "B"]\n```\n\nA manual implementation can use a loop:\n\n```js\nfunction reverseString(value) {\n  let result = "";\n\n  for (const character of value) {\n    result = character + result;\n  }\n\n  return result;\n}\n```\n\nBoth versions have linear time complexity, `O(n)`, and require additional memory.\n\nSome visible characters consist of multiple Unicode code points, such as letters with combining marks or family emojis. Correctly reversing user-perceived characters requires grapheme segmentation:\n\n```js\nfunction reverseGraphemes(value, locale = "en") {\n  const segmenter = new Intl.Segmenter(locale, {\n    granularity: "grapheme"\n  });\n\n  return [...segmenter.segment(value)]\n    .map(segment => segment.segment)\n    .reverse()\n    .join("");\n}\n```\n\nIn an interview, clarify whether the expected solution must handle basic characters, Unicode code points, or complete grapheme clusters.',
  },
  {
    number: 94,
    question: "How would you check whether a string is a palindrome?",
    answer:
      'Normalize the string according to the requirements and compare it with its reversed form.\n\n```js\nfunction isPalindrome(value) {\n  const normalized = value\n    .toLowerCase()\n    .replace(/[^a-z0-9]/g, "");\n\n  return (\n    normalized ===\n    [...normalized].reverse().join("")\n  );\n}\n```',
    explanation:
      'A palindrome reads the same forward and backward.\n\n```js\nisPalindrome("Racecar"); // true\nisPalindrome("Hello");   // false\n```',
    details:
      'A two-pointer solution avoids creating a reversed string:\n\n```js\nfunction isPalindrome(value) {\n  const normalized = value\n    .toLowerCase()\n    .replace(/[^a-z0-9]/g, "");\n\n  let left = 0;\n  let right = normalized.length - 1;\n\n  while (left \u003c right) {\n    if (normalized[left] !== normalized[right]) {\n      return false;\n    }\n\n    left++;\n    right--;\n  }\n\n  return true;\n}\n```\n\nTime complexity is `O(n)`. The normalization step still creates an additional string.\n\nRequirements should be clarified:\n\n- Should letter casing be ignored?\n- Should spaces and punctuation be ignored?\n- Should accented characters be normalized?\n- Should all Unicode letters and numbers be supported?\n- Is an empty string considered a palindrome?\n\nA Unicode-aware normalization can use property escapes:\n\n```js\nconst normalized = value\n  .normalize("NFC")\n  .toLocaleLowerCase()\n  .replace(/[^\\p{L}\\p{N}]/gu, "");\n```\n\nThis retains Unicode letters and numbers instead of only ASCII characters.',
  },
  {
    number: 95,
    question: "How would you flatten a nested array?",
    answer:
      "Use `flat(Infinity)` when arrays may have any nesting depth.\n\n```js\nfunction flatten(array) {\n  return array.flat(Infinity);\n}\n```",
    explanation:
      "`flat` creates a new array and removes the specified number of nested array levels.\n\n```js\nflatten([1, [2, [3, 4]]]);\n// [1, 2, 3, 4]\n```",
    details:
      "A recursive implementation can demonstrate the underlying algorithm:\n\n```js\nfunction flatten(array) {\n  const result = [];\n\n  for (const value of array) {\n    if (Array.isArray(value)) {\n      result.push(...flatten(value));\n    } else {\n      result.push(value);\n    }\n  }\n\n  return result;\n}\n```\n\nA reducer version is also possible:\n\n```js\nfunction flatten(array) {\n  return array.reduce((result, value) => {\n    if (Array.isArray(value)) {\n      return result.concat(flatten(value));\n    }\n\n    result.push(value);\n    return result;\n  }, []);\n}\n```\n\nDeep recursion may exceed the call stack. An iterative solution avoids that risk:\n\n```js\nfunction flatten(array) {\n  const stack = [...array].reverse();\n  const result = [];\n\n  while (stack.length > 0) {\n    const value = stack.pop();\n\n    if (Array.isArray(value)) {\n      stack.push(...value.slice().reverse());\n    } else {\n      result.push(value);\n    }\n  }\n\n  return result;\n}\n```\n\nTime complexity is `O(n)`, where `n` includes all nested values. The result requires `O(n)` additional space.",
  },
  {
    number: 96,
    question: "How would you group an array of objects by a property?",
    answer:
      "Iterate over the array and store each object in a collection associated with its property value.\n\n```js\nfunction groupBy(items, property) {\n  return items.reduce((groups, item) => {\n    const key = item[property];\n\n    groups[key] ??= [];\n    groups[key].push(item);\n\n    return groups;\n  }, {});\n}\n```",
    explanation:
      'Each property value becomes a key whose value is an array of matching objects.\n\n```js\ngroupBy(\n  [\n    { name: "Alex", role: "admin" },\n    { name: "Sam", role: "user" },\n    { name: "Lee", role: "admin" }\n  ],\n  "role"\n);\n```',
    details:
      'The result is:\n\n```js\n{\n  admin: [\n    { name: "Alex", role: "admin" },\n    { name: "Lee", role: "admin" }\n  ],\n  user: [\n    { name: "Sam", role: "user" }\n  ]\n}\n```\n\nA callback makes the function more flexible:\n\n```js\nfunction groupBy(items, getKey) {\n  return items.reduce((groups, item) => {\n    const key = getKey(item);\n\n    groups[key] ??= [];\n    groups[key].push(item);\n\n    return groups;\n  }, {});\n}\n\ngroupBy(users, user => user.role);\n```\n\nA `Map` supports keys of any type and avoids object-key coercion:\n\n```js\nfunction groupByMap(items, getKey) {\n  const groups = new Map();\n\n  for (const item of items) {\n    const key = getKey(item);\n    const group = groups.get(key) ?? [];\n\n    group.push(item);\n    groups.set(key, group);\n  }\n\n  return groups;\n}\n```\n\nModern JavaScript environments may provide `Object.groupBy()` and `Map.groupBy()`:\n\n```js\nconst groups = Object.groupBy(\n  users,\n  user => user.role\n);\n```\n\nCheck runtime compatibility when using newer built-in methods.',
  },
  {
    number: 97,
    question: "How would you implement debounce?",
    answer:
      "Return a wrapper function that resets a timer after every call. Execute the original function only when no new call occurs during the delay.\n\n```js\nfunction debounce(callback, delay) {\n  let timeoutId;\n\n  return function (...args) {\n    clearTimeout(timeoutId);\n\n    timeoutId = setTimeout(() => {\n      callback.apply(this, args);\n    }, delay);\n  };\n}\n```",
    explanation:
      'Debouncing is useful when only the final call matters, such as waiting for a user to stop typing before searching.\n\n```js\nconst search = debounce(fetchResults, 300);\ninput.addEventListener("input", search);\n```',
    details:
      "`apply` preserves the wrapper’s dynamic `this` and forwards its arguments.\n\nA version with cancellation and flushing:\n\n```js\nfunction debounce(callback, delay) {\n  let timeoutId;\n  let latestArgs;\n  let latestThis;\n\n  function debounced(...args) {\n    latestArgs = args;\n    latestThis = this;\n\n    clearTimeout(timeoutId);\n\n    timeoutId = setTimeout(() => {\n      timeoutId = undefined;\n      callback.apply(latestThis, latestArgs);\n    }, delay);\n  }\n\n  debounced.cancel = () => {\n    clearTimeout(timeoutId);\n    timeoutId = undefined;\n    latestArgs = undefined;\n    latestThis = undefined;\n  };\n\n  debounced.flush = () => {\n    if (timeoutId === undefined) {\n      return;\n    }\n\n    clearTimeout(timeoutId);\n    timeoutId = undefined;\n    callback.apply(latestThis, latestArgs);\n  };\n\n  return debounced;\n}\n```\n\nImportant design choices include:\n\n- Leading or trailing execution\n- Preserving `this`\n- Forwarding arguments\n- Returning results\n- Cancellation\n- Maximum wait time\n- Handling async callbacks\n\nA production implementation should also clean up pending timers when the owning UI component is removed.",
  },
  {
    number: 98,
    question: "How would you implement throttle?",
    answer:
      "Return a wrapper that permits the original function to execute at most once during each specified interval.\n\n```js\nfunction throttle(callback, interval) {\n  let lastExecutionTime = 0;\n\n  return function (...args) {\n    const currentTime = Date.now();\n\n    if (\n      currentTime - lastExecutionTime >= interval\n    ) {\n      lastExecutionTime = currentTime;\n      callback.apply(this, args);\n    }\n  };\n}\n```",
    explanation:
      "Throttling is useful when regular updates are needed during a frequent event such as scrolling, resizing, or pointer movement.",
    details:
      'The basic implementation executes on the leading edge but may discard the final call.\n\nA leading-and-trailing implementation preserves a final call:\n\n```js\nfunction throttle(callback, interval) {\n  let lastExecutionTime = 0;\n  let timeoutId;\n  let latestArgs;\n  let latestThis;\n\n  return function (...args) {\n    const currentTime = Date.now();\n    const remaining =\n      interval - (currentTime - lastExecutionTime);\n\n    latestArgs = args;\n    latestThis = this;\n\n    if (remaining \u003c= 0) {\n      clearTimeout(timeoutId);\n      timeoutId = undefined;\n      lastExecutionTime = currentTime;\n\n      callback.apply(latestThis, latestArgs);\n    } else if (timeoutId === undefined) {\n      timeoutId = setTimeout(() => {\n        timeoutId = undefined;\n        lastExecutionTime = Date.now();\n\n        callback.apply(latestThis, latestArgs);\n      }, remaining);\n    }\n  };\n}\n```\n\nDebounce and throttle solve different problems:\n\n- Debounce waits for activity to stop.\n- Throttle produces periodic updates while activity continues.\n\nFor visual updates, `requestAnimationFrame` can act as a rendering-aware throttle:\n\n```js\nlet frameRequested = false;\n\nwindow.addEventListener("scroll", () => {\n  if (frameRequested) {\n    return;\n  }\n\n  frameRequested = true;\n\n  requestAnimationFrame(() => {\n    updateInterface(window.scrollY);\n    frameRequested = false;\n  });\n});\n```',
  },
  {
    number: 99,
    question: "How would you implement a deep clone?",
    answer:
      "In modern JavaScript, use `structuredClone()` when the input contains supported structured-clone values.\n\n```js\nconst copy = structuredClone(original);\n```\n\nA custom implementation is necessary only when the application has specific cloning rules or unsupported values.",
    explanation:
      'A deep clone creates independent nested data, unlike spread syntax, which copies only the top level.\n\n```js\nconst copy = structuredClone(original);\ncopy.settings.theme = "dark";\n```',
    details:
      '`structuredClone()` supports many built-in types:\n\n- Objects and arrays\n- `Date`\n- `Map`\n- `Set`\n- Typed arrays\n- `ArrayBuffer`\n- Circular references\n\nIt does not clone every kind of value. Functions and DOM nodes are notable unsupported examples.\n\nA basic custom implementation:\n\n```js\nfunction deepClone(value, seen = new WeakMap()) {\n  if (\n    value === null ||\n    typeof value !== "object"\n  ) {\n    return value;\n  }\n\n  if (seen.has(value)) {\n    return seen.get(value);\n  }\n\n  if (value instanceof Date) {\n    return new Date(value.getTime());\n  }\n\n  if (value instanceof Map) {\n    const copy = new Map();\n    seen.set(value, copy);\n\n    for (const [key, item] of value) {\n      copy.set(\n        deepClone(key, seen),\n        deepClone(item, seen)\n      );\n    }\n\n    return copy;\n  }\n\n  if (value instanceof Set) {\n    const copy = new Set();\n    seen.set(value, copy);\n\n    for (const item of value) {\n      copy.add(deepClone(item, seen));\n    }\n\n    return copy;\n  }\n\n  const copy = Array.isArray(value)\n    ? []\n    : Object.create(Object.getPrototypeOf(value));\n\n  seen.set(value, copy);\n\n  for (const key of Reflect.ownKeys(value)) {\n    copy[key] = deepClone(value[key], seen);\n  }\n\n  return copy;\n}\n```\n\nEven this implementation does not preserve every possible property descriptor, internal slot, or built-in type.\n\nJSON conversion is not a general-purpose deep clone:\n\n```js\nJSON.parse(JSON.stringify(value));\n```\n\nIt loses or changes values such as `undefined`, `Date`, `Map`, `Set`, `BigInt`, symbols, functions, and circular references.\n\nIn an interview, explain the supported data types before presenting a custom clone.',
  },
  {
    number: 100,
    question: "How would you implement a simplified version of Promise.all?",
    answer:
      "Convert the input into an array, resolve each value with `Promise.resolve`, store results by input index, and resolve when all operations fulfill.\n\nReject immediately when any input rejects.\n\n```js\nfunction promiseAll(iterable) {\n  const values = Array.from(iterable);\n\n  return new Promise((resolve, reject) => {\n    if (values.length === 0) {\n      resolve([]);\n      return;\n    }\n\n    const results = new Array(values.length);\n    let completed = 0;\n\n    values.forEach((value, index) => {\n      Promise.resolve(value).then(result => {\n        results[index] = result;\n        completed++;\n\n        if (completed === values.length) {\n          resolve(results);\n        }\n      }, reject);\n    });\n  });\n}\n```",
    explanation:
      "Results must remain in input order even when the operations finish in a different order.",
    details:
      'Example:\n\n```js\nconst slow = new Promise(resolve => {\n  setTimeout(() => resolve("slow"), 100);\n});\n\nconst fast = Promise.resolve("fast");\n\npromiseAll([slow, fast, 42]).then(console.log);\n// ["slow", "fast", 42]\n```\n\nThe important behaviors are:\n\n- Accept an iterable\n- Accept both Promises and ordinary values\n- Preserve input order\n- Fulfill when every value fulfills\n- Reject when the first rejection is observed\n- Fulfill with an empty array for empty input\n\n`Promise.resolve(value)` also adopts compatible thenable values:\n\n```js\nPromise.resolve({\n  then(resolve) {\n    resolve("thenable result");\n  }\n});\n```\n\nRejecting the combined Promise does not cancel the other operations:\n\n```js\npromiseAll([\n  fetch("/first"),\n  Promise.reject(new Error("Failed")),\n  fetch("/third")\n]);\n```\n\nThe Fetch operations may continue even though the combined Promise has rejected.\n\nA fully specification-compatible implementation has additional details involving iterator behavior, Promise subclasses, thenables, and error handling. Interview implementations are normally expected to demonstrate the core behavior.',
  },
  {
    number: 101,
    question: "How would you retry a failed asynchronous operation?",
    answer:
      "Wrap the operation in a loop, catch failures, and try again until it succeeds or reaches the maximum number of attempts.\n\nInclude a delay, preferably exponential backoff with jitter, and retry only failures that are likely to be temporary.",
    explanation:
      "Not every error should be retried. Validation failures and most client errors will not improve through repetition.\n\n```js\nasync function retry(operation, attempts = 3) {\n  let lastError;\n\n  for (let attempt = 1; attempt \u003c= attempts; attempt++) {\n    try {\n      return await operation();\n    } catch (error) {\n      lastError = error;\n    }\n  }\n\n  throw lastError;\n}\n```",
    details:
      'A more complete implementation:\n\n```js\nfunction wait(milliseconds, signal) {\n  return new Promise((resolve, reject) => {\n    const timeoutId = setTimeout(\n      resolve,\n      milliseconds\n    );\n\n    signal?.addEventListener(\n      "abort",\n      () => {\n        clearTimeout(timeoutId);\n        reject(signal.reason);\n      },\n      { once: true }\n    );\n  });\n}\n\nasync function retry(\n  operation,\n  {\n    attempts = 3,\n    initialDelay = 300,\n    factor = 2,\n    shouldRetry = () => true,\n    signal\n  } = {}\n) {\n  let lastError;\n\n  for (let attempt = 1; attempt \u003c= attempts; attempt++) {\n    signal?.throwIfAborted();\n\n    try {\n      return await operation({\n        attempt,\n        signal\n      });\n    } catch (error) {\n      lastError = error;\n\n      if (\n        attempt === attempts ||\n        !shouldRetry(error)\n      ) {\n        throw error;\n      }\n\n      const baseDelay =\n        initialDelay * factor ** (attempt - 1);\n\n      const jitter = Math.random() * baseDelay * 0.2;\n\n      await wait(baseDelay + jitter, signal);\n    }\n  }\n\n  throw lastError;\n}\n```\n\nExponential backoff increases the delay:\n\n```text\n300 ms → 600 ms → 1200 ms\n```\n\nJitter prevents many clients from retrying simultaneously.\n\nGood candidates for retry may include temporary network failures, rate limits, and some server errors. Respect server guidance such as a `Retry-After` header.\n\nBe careful when retrying non-idempotent operations. Repeating a payment or creation request could duplicate an action unless the API supports idempotency keys.',
  },
  {
    number: 102,
    question: "How would you build an event emitter?",
    answer:
      "Store listeners by event name and provide methods to subscribe, unsubscribe, and emit events.\n\nA `Map` of event names to `Set` collections prevents duplicate listener entries and supports efficient removal.\n\n```js\nclass EventEmitter {\n  #events = new Map();\n\n  on(eventName, listener) {\n    const listeners =\n      this.#events.get(eventName) ?? new Set();\n\n    listeners.add(listener);\n    this.#events.set(eventName, listeners);\n\n    return () => this.off(eventName, listener);\n  }\n\n  off(eventName, listener) {\n    const listeners = this.#events.get(eventName);\n\n    if (!listeners) {\n      return;\n    }\n\n    listeners.delete(listener);\n\n    if (listeners.size === 0) {\n      this.#events.delete(eventName);\n    }\n  }\n\n  emit(eventName, ...args) {\n    const listeners = this.#events.get(eventName);\n\n    if (!listeners) {\n      return;\n    }\n\n    for (const listener of [...listeners]) {\n      listener(...args);\n    }\n  }\n}\n```",
    explanation:
      "Event emitters allow components to communicate without requiring the producer to know each consumer directly.",
    details:
      'Usage:\n\n```js\nconst emitter = new EventEmitter();\n\nconst unsubscribe = emitter.on(\n  "user:updated",\n  user => {\n    console.log(user.name);\n  }\n);\n\nemitter.emit("user:updated", {\n  name: "Alex"\n});\n\nunsubscribe();\n```\n\nCopying the listener set during emission provides predictable behavior if a listener removes itself while the event is being processed:\n\n```js\nfor (const listener of [...listeners]) {\n  listener(...args);\n}\n```\n\nA `once` method can automatically unsubscribe:\n\n```js\nonce(eventName, listener) {\n  const unsubscribe = this.on(\n    eventName,\n    (...args) => {\n      unsubscribe();\n      listener(...args);\n    }\n  );\n\n  return unsubscribe;\n}\n```\n\nDesign decisions include:\n\n- Should duplicate listeners be allowed?\n- Should listener errors stop later listeners?\n- Should listeners run synchronously or asynchronously?\n- What happens when listeners are added during emission?\n- Should wildcard events be supported?\n- Is there a maximum-listener warning?\n\nLong-lived emitters can cause memory leaks when consumers forget to unsubscribe. Returning a cleanup function makes correct lifecycle management easier.',
  },
  {
    number: 103,
    question: "How would you cache the result of a function?",
    answer:
      "Wrap the function with a cache that stores results using the function arguments as keys. When the same input appears again, return the stored result instead of recomputing it.\n\nThis technique is called memoization.\n\n```js\nfunction memoize(callback) {\n  const cache = new Map();\n\n  return function (argument) {\n    if (cache.has(argument)) {\n      return cache.get(argument);\n    }\n\n    const result = callback.call(this, argument);\n    cache.set(argument, result);\n\n    return result;\n  };\n}\n```",
    explanation:
      "Memoization is most valuable for deterministic, expensive functions that receive repeated inputs.",
    details:
      'A multi-argument implementation can use nested maps without converting arguments to strings:\n\n```js\nfunction memoize(callback) {\n  const root = new Map();\n  const resultSymbol = Symbol("result");\n\n  return function (...args) {\n    let current = root;\n\n    for (const argument of args) {\n      if (!current.has(argument)) {\n        current.set(argument, new Map());\n      }\n\n      current = current.get(argument);\n    }\n\n    if (current.has(resultSymbol)) {\n      return current.get(resultSymbol);\n    }\n\n    const result = callback.apply(this, args);\n    current.set(resultSymbol, result);\n\n    return result;\n  };\n}\n```\n\nStringifying arguments is simple but unreliable for general values:\n\n```js\nconst key = JSON.stringify(args);\n```\n\nProblems include:\n\n- Property-order differences\n- Unsupported values\n- Circular references\n- Large serialization costs\n- Semantically different values producing similar keys\n\nAsync functions require special consideration. Caching the Promise prevents duplicate concurrent work:\n\n```js\nfunction memoizeAsync(callback) {\n  const cache = new Map();\n\n  return function (key) {\n    if (!cache.has(key)) {\n      const promise = callback.call(this, key);\n\n      cache.set(key, promise);\n\n      promise.catch(() => {\n        cache.delete(key);\n      });\n    }\n\n    return cache.get(key);\n  };\n}\n```\n\nRemoving rejected Promises allows a later call to retry.\n\nProduction caches may also need:\n\n- Expiration\n- Size limits\n- Least-recently-used eviction\n- Manual invalidation\n- Weak references for object keys\n- Protection against stale data\n\nMemoization should not be used for impure functions whose result changes despite identical arguments.',
  },
  {
    number: 104,
    question: "How would you diagnose and improve a slow frontend application?",
    answer:
      "Begin by measuring the user-visible problem, reproduce it consistently, and use browser tools to identify whether the main bottleneck is network loading, JavaScript execution, rendering, memory, or backend latency.\n\nApply a targeted improvement and measure again to verify the result.",
    explanation:
      "Performance work should follow evidence:\n\n```text\nMeasure → identify bottleneck → optimize → verify\n```\n\nA faster internal operation is not valuable unless it improves the user experience or prevents a meaningful resource problem.",
    details:
      'Start by clarifying the symptom:\n\n- Is the initial page slow?\n- Is an interaction delayed?\n- Is scrolling or animation uneven?\n- Does performance degrade over time?\n- Is the problem limited to certain devices or networks?\n- Is the backend response slow?\n\nUseful browser tools include:\n\n- Network panel\n- Performance profiler\n- Memory profiler\n- Rendering diagnostics\n- Lighthouse\n- Coverage tools\n- Framework-specific profilers\n- Bundle analyzers\n\nFor slow initial loading, inspect:\n\n- Large JavaScript bundles\n- Render-blocking resources\n- Slow API responses\n- Unoptimized images or fonts\n- Missing compression or caching\n- Excessive third-party scripts\n- Duplicate dependencies\n\nPotential improvements include:\n\n- Code splitting\n- Lazy loading\n- Tree shaking\n- Image optimization\n- Resource caching\n- Preloading critical assets\n- Reducing third-party code\n- Server-side or static rendering where appropriate\n\nFor slow interactions, inspect the main-thread timeline for long tasks:\n\n```js\nperformance.mark("operation-start");\n\nperformOperation();\n\nperformance.mark("operation-end");\n\nperformance.measure(\n  "operation",\n  "operation-start",\n  "operation-end"\n);\n```\n\nPotential improvements include:\n\n- Reducing unnecessary work\n- Breaking long tasks into smaller units\n- Moving CPU-heavy work to Web Workers\n- Debouncing or throttling frequent events\n- Virtualizing large lists\n- Avoiding unnecessary component renders\n- Caching expensive calculations\n\nFor rendering problems, look for:\n\n- Layout thrashing\n- Large DOM trees\n- Expensive CSS selectors\n- Frequent style recalculation\n- Large paint areas\n- Animations using layout-triggering properties\n\nFor degradation over time, inspect:\n\n- Forgotten event listeners\n- Active timers\n- Detached DOM nodes\n- Unbounded caches\n- Retained subscriptions\n- Repeatedly created resources\n\nCompare results before and after the change using the same conditions. Confirm improvements with real-user monitoring when possible because local development hardware may not represent actual users.',
  },
];

export const reactQuestionRoadmap: QuestionRoadmapSection[] = [
  {
    title: "Rendering and component identity",
    questions: [
      {
        number: 1,
        title: "What happens when a React component renders?",
      },
      {
        number: 2,
        title: "How does React reconciliation work?",
      },
      {
        number: 3,
        title: "How do state snapshots and automatic batching work?",
      },
      {
        number: 4,
        title: "How do keys affect component identity and state?",
      },
      {
        number: 26,
        title: "What does Strict Mode do in React?",
      },
    ],
  },
  {
    title: "State management and forms",
    questions: [
      {
        number: 5,
        title: "What is the difference between controlled and uncontrolled components?",
      },
      {
        number: 6,
        title: "How should state be structured in a large React application?",
      },
      {
        number: 11,
        title: "How do you prevent Context from causing unnecessary renders?",
      },
      {
        number: 12,
        title: "When would you choose useReducer over useState?",
      },
      {
        number: 22,
        title: "How should React subscribe to an external store?",
      },
      {
        number: 23,
        title: "How would you choose a state-management approach for a large React application?",
      },
    ],
  },
  {
    title: "Effects, refs, and reusable Hooks",
    questions: [
      {
        number: 7,
        title: "When should you use useEffect, and when should you avoid it?",
      },
      {
        number: 8,
        title: "How should Effect dependencies, stale closures, and cleanup be handled?",
      },
      {
        number: 9,
        title: "What is the difference between useEffect and useLayoutEffect?",
      },
      {
        number: 10,
        title: "When should refs be used instead of state?",
      },
      {
        number: 13,
        title: "What makes a good custom Hook?",
      },
    ],
  },
  {
    title: "Performance and concurrent rendering",
    questions: [
      {
        number: 14,
        title: "How do memo, useMemo, and useCallback differ?",
      },
      {
        number: 15,
        title: "How would you diagnose and improve React rendering performance?",
      },
      {
        number: 16,
        title: "What are transitions and useDeferredValue?",
      },
      {
        number: 17,
        title: "How do Suspense and lazy loading work?",
      },
    ],
  },
  {
    title: "Server rendering and modern React",
    questions: [
      {
        number: 19,
        title:
          "What is the difference between client-side rendering, server-side rendering, static generation, and hydration?",
      },
      {
        number: 20,
        title: "What are React Server Components?",
      },
      {
        number: 21,
        title: "What are Actions, useActionState, and useOptimistic?",
      },
    ],
  },
  {
    title: "Data fetching and error handling",
    questions: [
      {
        number: 18,
        title: "What are Error Boundaries, and what do they catch?",
      },
      {
        number: 28,
        title: "How would you handle data fetching and race conditions in React?",
      },
    ],
  },
  {
    title: "React fundamentals",
    questions: [
      {
        number: 29,
        title:
          "What is the difference between a React element, a component, and a component instance?",
      },
      {
        number: 30,
        title: "What are the Rules of Hooks, and why must Hooks be called unconditionally?",
      },
      {
        number: 31,
        title: "What is the difference between props and state?",
      },
      {
        number: 32,
        title: "What is composition, and why is it generally preferred over inheritance in React?",
      },
      {
        number: 33,
        title: "What are Fragments, and when are they useful?",
      },
      {
        number: 34,
        title:
          "How does React handle events, and how do React events differ from native DOM events?",
      },
      {
        number: 35,
        title: "What are portals, and how do events propagate through them?",
      },
    ],
  },
  {
    title: "State and component design",
    questions: [
      {
        number: 36,
        title: "What is state colocation, and why does it matter?",
      },
      {
        number: 37,
        title: "When should state be lifted, and when should it remain local?",
      },
      {
        number: 38,
        title: "How can you avoid prop drilling?",
      },
      {
        number: 39,
        title: "What is the difference between derived state and synchronized state?",
      },
      {
        number: 40,
        title: "How should complex form state and validation be managed?",
      },
      {
        number: 41,
        title: "How can component state be intentionally preserved or reset?",
      },
      {
        number: 42,
        title: "When should state be represented with a state machine?",
      },
    ],
  },
  {
    title: "React Hooks",
    questions: [
      {
        number: 43,
        title: "How do dependency arrays work in useEffect, useMemo, and useCallback?",
      },
      {
        number: 44,
        title: "How would you debug an infinite useEffect loop?",
      },
      {
        number: 45,
        title: "How can asynchronous race conditions occur inside an Effect?",
      },
      {
        number: 46,
        title: "When should logic remain in an event handler instead of moving into an Effect?",
      },
      {
        number: 47,
        title:
          "How do you read the latest value inside a callback without unnecessarily restarting an Effect?",
      },
      {
        number: 48,
        title: "What is useId, and why should it not be used for list keys?",
      },
      {
        number: 49,
        title: "What does the use API do?",
      },
      {
        number: 50,
        title: "When would you use useImperativeHandle?",
      },
      {
        number: 57,
        title: "What do useTransition and useDeferredValue solve?",
      },
      {
        number: 77,
        title: "What does useActionState solve?",
      },
    ],
  },
  {
    title: "Rendering and performance",
    questions: [
      {
        number: 51,
        title: "Why does a child component render when its parent renders?",
      },
      {
        number: 52,
        title: "What causes a memoized component to render again?",
      },
      {
        number: 53,
        title: "Why can inline objects and functions affect memoization?",
      },
      {
        number: 54,
        title: "How would you render a list containing thousands of items efficiently?",
      },
      {
        number: 55,
        title: "What is list virtualization?",
      },
      {
        number: 56,
        title: "How do code splitting and route-based lazy loading differ?",
      },
      {
        number: 58,
        title: "What problems does React Compiler solve?",
      },
      {
        number: 59,
        title: "When can memoization make performance worse?",
      },
      {
        number: 60,
        title: "How would you investigate a component that renders too frequently?",
      },
    ],
  },
  {
    title: "Suspense and reliability",
    questions: [
      {
        number: 61,
        title: "What is the difference between Suspense and an Error Boundary?",
      },
      {
        number: 62,
        title: "Where should Suspense boundaries be placed?",
      },
      {
        number: 63,
        title: "How do nested Suspense boundaries coordinate progressive loading?",
      },
      {
        number: 64,
        title: "How can an Error Boundary be reset?",
      },
      {
        number: 65,
        title: "How should loading, empty, error, and stale states be represented?",
      },
      {
        number: 66,
        title: "How would you prevent an outdated request from replacing newer data?",
      },
    ],
  },
  {
    title: "Server rendering and architecture",
    questions: [
      {
        number: 67,
        title: "What causes hydration mismatches, and how would you debug them?",
      },
      {
        number: 68,
        title: "What is streaming server rendering?",
      },
      {
        number: 69,
        title: "What is selective hydration?",
      },
      {
        number: 70,
        title:
          "What determines whether a component should be a Server Component or a Client Component?",
      },
      {
        number: 71,
        title: "What data can cross the Server Component and Client Component boundary?",
      },
      {
        number: 72,
        title: "What is the difference between a Server Component and SSR?",
      },
      {
        number: 73,
        title: "What are Server Functions, and what security considerations apply to them?",
      },
      {
        number: 74,
        title: "How would you avoid network request waterfalls?",
      },
      {
        number: 75,
        title: "How would you design cache invalidation for server data?",
      },
    ],
  },
  {
    title: "Forms and React Actions",
    questions: [
      {
        number: 76,
        title: "How do React form Actions work?",
      },
      {
        number: 78,
        title:
          "What is the difference between useOptimistic and immediately updating ordinary state?",
      },
      {
        number: 79,
        title: "How should an optimistic update be rolled back after failure?",
      },
      {
        number: 80,
        title: "What does useFormStatus provide?",
      },
      {
        number: 81,
        title: "How do pending, validation, and submission errors affect accessible form design?",
      },
    ],
  },
  {
    title: "Component architecture",
    questions: [
      {
        number: 82,
        title: "What are compound components?",
      },
      {
        number: 83,
        title: "What is the render-prop pattern?",
      },
      {
        number: 84,
        title: "What are higher-order components, and when are they still useful?",
      },
      {
        number: 85,
        title: "How would you design a reusable modal or dialog component?",
      },
      {
        number: 86,
        title: "How would you design a reusable data table?",
      },
      {
        number: 87,
        title: "How do you balance abstraction with duplication?",
      },
      {
        number: 88,
        title: "How do you prevent a design-system component API from becoming overly complex?",
      },
      {
        number: 89,
        title: "What belongs in a component, custom Hook, utility, service, or store?",
      },
    ],
  },
  {
    title: "Testing and quality",
    questions: [
      {
        number: 90,
        title: "What should be mocked in a React test?",
      },
      {
        number: 91,
        title: "Why can testing implementation details make tests fragile?",
      },
      {
        number: 92,
        title: "How do you test custom Hooks?",
      },
      {
        number: 93,
        title: "How do you test components that use Context?",
      },
      {
        number: 94,
        title: "How do you test loading, errors, Suspense, and asynchronous updates?",
      },
      {
        number: 95,
        title: "What causes React test warnings about updates not being wrapped in act?",
      },
      {
        number: 96,
        title: "When are snapshot tests useful, and when are they harmful?",
      },
      {
        number: 97,
        title: "How would you test keyboard and focus behavior?",
      },
    ],
  },
  {
    title: "Security and accessibility",
    questions: [
      {
        number: 98,
        title: "What are the risks of dangerouslySetInnerHTML?",
      },
      {
        number: 99,
        title: "How should untrusted HTML be rendered safely?",
      },
      {
        number: 100,
        title: "How should focus be managed when opening and closing a modal?",
      },
      {
        number: 101,
        title: "What is the difference between hiding and unmounting content?",
      },
      {
        number: 102,
        title: "How should asynchronous status updates be announced to assistive technologies?",
      },
      {
        number: 103,
        title: "How do you make custom interactive components keyboard accessible?",
      },
    ],
  },
  {
    title: "Architecture, testing, and accessibility",
    questions: [
      {
        number: 27,
        title: "How would you design reusable React component APIs?",
      },
      {
        number: 24,
        title: "How should senior engineers test React applications?",
      },
      {
        number: 25,
        title: "How do you build accessible React components?",
      },
    ],
  },
  {
    title: "JSX and update mechanics",
    questions: [
      {
        number: 104,
        title: "What does JSX produce, and how is it different from HTML?",
      },
      {
        number: 105,
        title: "How does React render null, booleans, arrays, strings, and numbers?",
      },
      {
        number: 106,
        title: "How does React process queued state updates?",
      },
      {
        number: 107,
        title: "Why can declaring a component inside another component reset state?",
      },
      {
        number: 108,
        title: "What does flushSync do, and when is it appropriate?",
      },
      {
        number: 109,
        title: "What happens if a component updates state during rendering?",
      },
    ],
  },
  {
    title: "Modern React and concurrency",
    questions: [
      {
        number: 110,
        title: "What problem does useEffectEvent solve, and when should it not be used?",
      },
      {
        number: 111,
        title:
          "How does \u003cActivity> differ from conditional rendering, CSS hiding, and unmounting?",
      },
      {
        number: 112,
        title: "How do callback refs and ref cleanup work?",
      },
      {
        number: 113,
        title: "When is useInsertionEffect appropriate?",
      },
      {
        number: 114,
        title: "What does concurrent rendering mean in React?",
      },
      {
        number: 115,
        title: "What is tearing, and how does useSyncExternalStore help prevent it?",
      },
      {
        number: 116,
        title: "How do React Performance Tracks help diagnose slow interactions?",
      },
      {
        number: 117,
        title: "How should a team adopt React Compiler, and what does it not guarantee?",
      },
      {
        number: 118,
        title: "What do cache and cacheSignal do in a Server Component environment?",
      },
    ],
  },
  {
    title: "Emerging rendering APIs",
    questions: [
      {
        number: 119,
        title: "What is partial pre-rendering, and how does it differ from ordinary streaming SSR?",
      },
      {
        number: 120,
        title:
          "What problem does React's \u003cViewTransition> solve, and is it ready for general production use?",
      },
    ],
  },
];

export const reactQuestionAnswers: QuestionAnswer[] = [
  {
    number: 1,
    question: "What happens when a React component renders?",
    answer:
      "React rendering has two main phases:\n\n- **Render phase:** React calls components and calculates what the UI should look like.\n- **Commit phase:** React applies the necessary changes to the DOM and runs commit-related logic.\n\nRendering does not necessarily mean that React changes the DOM.",
    explanation:
      "React compares the newly produced element tree with the previous tree and commits only the required updates.",
    details:
      "A render may be triggered by:\n\n- An initial root render\n- A state update\n- A parent render\n- A context update\n- An external-store notification\n- A subscribed framework update\n\nComponent functions must remain pure during rendering. They should not modify the DOM, start subscriptions, make uncontrolled network calls, or mutate external state.\n\nReact may start, pause, restart, or discard render work. Side effects in render are therefore unsafe.\n\nAfter render work is complete, React commits DOM changes. It then runs layout effects and later passive effects.\n\nA component may render without producing a DOM update if its output is equivalent to the previous output.",
  },
  {
    number: 2,
    question: "How does React reconciliation work?",
    answer:
      "Reconciliation is the process React uses to compare a new element tree with the previous tree and determine which parts of the rendered interface should be updated, preserved, replaced, or removed.\n\nReact uses element types, positions, and keys as important identity signals.",
    explanation:
      "When the element type at a position changes, React generally replaces that subtree and resets its state.",
    details:
      'If an element keeps the same type and identity, React can reuse its existing DOM node and component state:\n\n```jsx\n\u003cProfile name="Alex" />\n\u003cProfile name="Sam" />\n```\n\nThe `Profile` instance is preserved and receives new props.\n\nChanging the type resets the subtree:\n\n```jsx\n{isEditing\n  ? \u003cEditProfile />\n  : \u003cProfile />}\n```\n\nReact does not perform a generic character-by-character comparison of arbitrary trees. Its practical rules rely heavily on stable types and keys.\n\nReconciliation produces changes during render, but those changes do not affect the DOM until the commit phase.',
  },
  {
    number: 3,
    question: "How do state snapshots and automatic batching work?",
    answer:
      "Each render receives a snapshot of state. Calling a state setter schedules a future render; it does not change the state variable captured by the current render.\n\nReact batches multiple state updates so they can often be processed in one render.",
    explanation:
      "Repeatedly using a captured state value may produce only one effective increment:\n\n```jsx\nsetCount(count + 1);\nsetCount(count + 1);\nsetCount(count + 1);\n```",
    details:
      'Each call above uses the same `count` snapshot. Use updater functions when the next state depends on the previous state:\n\n```jsx\nsetCount(count => count + 1);\nsetCount(count => count + 1);\nsetCount(count => count + 1);\n```\n\nThis increments by three.\n\nState should be treated as immutable:\n\n```jsx\nsetUser(previous => ({\n  ...previous,\n  name: "Sam"\n}));\n```\n\nBatching reduces unnecessary renders and prevents partially updated UI. Avoid assuming that state is updated immediately after calling its setter.',
  },
  {
    number: 4,
    question: "How do keys affect component identity and state?",
    answer:
      "Keys identify sibling elements across renders. React uses them to match previous elements with new elements when lists are reordered, inserted into, or deleted from.\n\nKeys also control whether component state is preserved or reset.",
    explanation:
      "A stable key should represent the underlying data rather than its current array position.\n\n```jsx\nusers.map(user => (\n  \u003cUserRow key={user.id} user={user} />\n))\n```",
    details:
      "Array indexes are risky when list order can change. State may become associated with the wrong item:\n\n```jsx\nusers.map((user, index) => (\n  \u003cUserRow key={index} user={user} />\n))\n```\n\nKeys need to be unique only among siblings, not globally.\n\nChanging a key intentionally resets a subtree:\n\n```jsx\n\u003cProfileEditor\n  key={selectedUserId}\n  userId={selectedUserId}\n/>\n```\n\nReact associates state with a component’s position, type, and key in the render tree. See [Preserving and Resetting State](https://react.dev/learn/preserving-and-resetting-state).",
  },
  {
    number: 26,
    question: "What does Strict Mode do in React?",
    answer:
      "Strict Mode enables additional development-only checks that expose unsafe rendering, missing Effect cleanup, and deprecated behavior.\n\nIt does not change production behavior.",
    explanation:
      "React may intentionally render components or repeat setup-and-cleanup cycles during development to reveal code that is not resilient.",
    details:
      "A component must remain correct when React calls its render function more than once:\n\n```jsx\nfunction List({ items }) {\n  const sortedItems = items.toSorted();\n  return \u003cResults items={sortedItems} />;\n}\n```\n\nMutating `items` during rendering would be exposed by repeated rendering.\n\nAn Effect must mirror setup with cleanup:\n\n```jsx\nuseEffect(() => {\n  connection.connect();\n\n  return () => {\n    connection.disconnect();\n  };\n}, []);\n```\n\nDo not remove Strict Mode merely to hide duplicate development behavior. Fix the impure render, missing cleanup, or lifecycle assumption it reveals.",
  },
  {
    number: 5,
    question: "What is the difference between controlled and uncontrolled components?",
    answer:
      "A controlled input receives its current value from React state and reports changes through a handler.\n\nAn uncontrolled input stores its current value in the DOM and is normally read through a ref or form submission.",
    explanation:
      "Controlled inputs make React state the source of truth:\n\n```jsx\n\u003cinput\n  value={name}\n  onChange={event => setName(event.target.value)}\n/>\n```",
    details:
      'Uncontrolled input:\n\n```jsx\nconst inputRef = useRef(null);\n\n\u003cinput\n  ref={inputRef}\n  defaultValue="Alex"\n/>;\n```\n\nControlled inputs are useful for:\n\n- Immediate validation\n- Conditional interface updates\n- Formatting\n- Coordinating related fields\n\nUncontrolled inputs can reduce state wiring and work well with native form behavior.\n\nAn input should not switch unexpectedly between controlled and uncontrolled modes. Use a defined initial value such as `""` for a controlled text input.\n\nControl is also an architectural concept: a controlled component receives important behavior through props, while an uncontrolled component manages more of its own state.',
  },
  {
    number: 6,
    question: "How should state be structured in a large React application?",
    answer:
      "Keep state as local as possible, lift it only to the nearest shared owner, avoid duplicated or contradictory state, and derive values that can be calculated during rendering.\n\nSeparate server state, URL state, form state, and client UI state according to their different lifecycles.",
    explanation:
      "Storing derived values can create synchronization bugs:\n\n```jsx\n// Avoid storing this separately\nconst fullName = `${firstName} ${lastName}`;\n```",
    details:
      "Good state design generally:\n\n- Groups values that change together\n- Avoids impossible combinations\n- Avoids duplicating props in state\n- Uses IDs instead of duplicating complete objects\n- Normalizes complex shared data when useful\n- Preserves a single source of truth\n\nDo not copy a prop into state unless the component intentionally needs an independent initial value.\n\nState ownership should follow the components that need to read and update it. Not all shared state needs a global store; composition, lifting state, context, or URL parameters may be sufficient.",
  },
  {
    number: 11,
    question: "How do you prevent Context from causing unnecessary renders?",
    answer:
      "Context consumers re-render when the provider supplies a different context value according to `Object.is`.\n\nReduce unnecessary updates by keeping providers focused, splitting unrelated contexts, stabilizing provider values when appropriate, and separating state from dispatch.",
    explanation:
      "A new object is created on every render here:\n\n```jsx\n\u003cUserContext.Provider\n  value={{ user, updateUser }}\n>\n  {children}\n\u003c/UserContext.Provider>\n```",
    details:
      "Stabilization may help when the provider frequently renders:\n\n```jsx\nconst value = useMemo(\n  () => ({ user, updateUser }),\n  [user, updateUser]\n);\n```\n\nDo not memoize automatically; first determine whether consumer rendering is a real problem.\n\nFor frequently changing large state, consider:\n\n- Multiple narrower contexts\n- State and dispatch contexts\n- Component composition\n- An external store with selectors\n- Moving the provider closer to consumers\n\n`memo` does not prevent a component from receiving updated context. Context design and subscription granularity matter more.",
  },
  {
    number: 12,
    question: "When would you choose useReducer over useState?",
    answer:
      "Use `useReducer` when state transitions are complex, multiple fields change together, or many handlers express related state operations.\n\nA reducer centralizes transition logic as a pure function of the current state and an action.",
    explanation:
      'Components dispatch what happened, while the reducer determines the next state.\n\n```jsx\nfunction reducer(state, action) {\n  switch (action.type) {\n    case "item-added":\n      return {\n        ...state,\n        items: [...state.items, action.item]\n      };\n    default:\n      throw new Error("Unknown action");\n  }\n}\n```',
    details:
      "Reducers are useful when:\n\n- Transitions need explicit names\n- State has related fields\n- Update logic is repeated\n- Transitions need focused tests\n- State updates are difficult to trace\n\nA reducer must not mutate existing state:\n\n```jsx\n// Avoid\nstate.items.push(action.item);\nreturn state;\n```\n\nUse `useState` when state is simple and independent. Reducers add structure, but unnecessary action types and boilerplate can make small components harder to understand.\n\nReducer plus Context can distribute state, but it does not automatically provide optimized subscriptions.",
  },
  {
    number: 22,
    question: "How should React subscribe to an external store?",
    answer:
      "Use `useSyncExternalStore` when React must read and subscribe to state owned outside React.\n\nIt gives React a consistent snapshot API and supports concurrent rendering and server rendering.",
    explanation:
      "Reading an external mutable value directly during render can cause inconsistent UI because React does not know when it changes.",
    details:
      "A store supplies subscription and snapshot functions:\n\n```jsx\nfunction useStore() {\n  return useSyncExternalStore(\n    store.subscribe,\n    store.getSnapshot,\n    store.getServerSnapshot\n  );\n}\n```\n\n`getSnapshot` should return a cached, immutable snapshot until the store actually changes. Returning a new object on every call can cause repeated rendering.\n\nThe server snapshot should agree with the initial client snapshot to prevent hydration problems.\n\nLibraries typically wrap this low-level API and may add selectors, equality comparison, devtools, persistence, and middleware.",
  },
  {
    number: 23,
    question: "How would you choose a state-management approach for a large React application?",
    answer:
      "Choose based on state ownership, update frequency, persistence, server synchronization, subscription granularity, debugging needs, and team constraints.\n\nStart with React’s local state, reducer, composition, and Context before introducing broader infrastructure.",
    explanation:
      "Different categories of state often need different tools rather than one universal global store.",
    details:
      "Distinguish among:\n\n- Local component state\n- Shared client state\n- Server-cached state\n- URL and navigation state\n- Form state\n- Persistent browser state\n- External real-time state\n\nEvaluate a solution by:\n\n- Selector and subscription behavior\n- Concurrent-rendering support\n- Server-rendering support\n- Cache invalidation\n- Optimistic updates\n- Developer tooling\n- Type safety\n- Bundle and runtime cost\n- Migration and testing complexity\n\nAvoid placing all application data in Context merely because it is built in. Also avoid adding a global store when lifting state or component composition solves the problem clearly.",
  },
  {
    number: 7,
    question: "When should you use useEffect, and when should you avoid it?",
    answer:
      "Use `useEffect` to synchronize a component with an external system after React commits the UI.\n\nExternal systems include browser APIs, subscriptions, timers, network connections, analytics services, and non-React widgets.",
    explanation:
      "If a value can be calculated during render or an action belongs in an event handler, an Effect is usually unnecessary.",
    details:
      "Appropriate synchronization:\n\n```jsx\nuseEffect(() => {\n  const connection = createConnection(roomId);\n  connection.connect();\n\n  return () => connection.disconnect();\n}, [roomId]);\n```\n\nAvoid using Effects to:\n\n- Derive renderable data from props or state\n- Handle a specific button click\n- Reset state that could be reset with a key\n- Chain state updates that can be calculated together\n\nEffects run on the client, not during server rendering. After dependencies change, React runs the old cleanup and then the new setup.\n\nReact’s guidance defines Effects as synchronization with external systems: [React `useEffect`](https://react.dev/reference/react/useEffect).",
  },
  {
    number: 8,
    question: "How should Effect dependencies, stale closures, and cleanup be handled?",
    answer:
      "Every reactive value read by an Effect should normally appear in its dependency list.\n\nCleanup must undo the setup performed by the Effect. Stale closures should be solved by correcting dependencies or changing the state design, not by suppressing lint rules.",
    explanation:
      "An empty dependency array does not mean “run whenever convenient.” It declares that the Effect reads no reactive values.",
    details:
      "A subscription should clean up using the same dependency values:\n\n```jsx\nuseEffect(() => {\n  const unsubscribe = subscribe(userId, handleUpdate);\n  return unsubscribe;\n}, [userId]);\n```\n\nCommon stale-closure solutions include:\n\n- Functional state updates\n- Moving calculations into the Effect\n- Stabilizing necessary callbacks\n- Using refs for mutable non-rendering data\n- Separating reactive and non-reactive logic\n\nAsync work should guard against stale results or support cancellation:\n\n```jsx\nuseEffect(() => {\n  const controller = new AbortController();\n\n  loadUser(userId, controller.signal)\n    .then(setUser)\n    .catch(handleError);\n\n  return () => controller.abort();\n}, [userId]);\n```\n\nCleanup may run more than only at unmount: it also runs before an Effect is repeated with changed dependencies.",
  },
  {
    number: 9,
    question: "What is the difference between useEffect and useLayoutEffect?",
    answer:
      "`useEffect` generally runs after the browser has had an opportunity to paint.\n\n`useLayoutEffect` runs after DOM changes are committed but before the browser paints, allowing code to measure layout and synchronously adjust the UI.",
    explanation:
      "Prefer `useEffect`. Use `useLayoutEffect` only when code must read or modify layout before the user sees the frame.",
    details:
      "Tooltip measurement is a typical layout-effect use case:\n\n```jsx\nuseLayoutEffect(() => {\n  const rectangle =\n    tooltipRef.current.getBoundingClientRect();\n\n  setHeight(rectangle.height);\n}, []);\n```\n\nBecause `useLayoutEffect` can delay painting, excessive work inside it hurts responsiveness.\n\nNeither hook runs during server rendering. Code using `useLayoutEffect` may need to be isolated to client-only components.\n\nIf an Effect performs a visual adjustment and the user sees flickering, that is a signal that a layout effect might be appropriate.",
  },
  {
    number: 10,
    question: "When should refs be used instead of state?",
    answer:
      "Use a ref for a mutable value that must survive renders but should not cause rendering when it changes.\n\nUse state for information that affects rendered output.",
    explanation:
      "Common ref uses include DOM access, timer identifiers, previous values, and integration with imperative libraries.\n\n```jsx\nconst inputRef = useRef(null);\n\nfunction focusInput() {\n  inputRef.current.focus();\n}\n```",
    details:
      "Updating `ref.current` does not trigger a render:\n\n```jsx\nconst requestIdRef = useRef(null);\nrequestIdRef.current = requestId;\n```\n\nDo not read or write refs during rendering except for predictable initialization. Rendering should remain pure.\n\nA component can expose a limited imperative API rather than its entire DOM node:\n\n```jsx\nuseImperativeHandle(ref, () => ({\n  focus() {\n    inputRef.current.focus();\n  }\n}), []);\n```\n\nModern React allows `ref` to be received as a prop in supported component patterns. Callback refs can also return cleanup functions.",
  },
  {
    number: 13,
    question: "What makes a good custom Hook?",
    answer:
      "A custom Hook extracts reusable stateful behavior while preserving React’s declarative model.\n\nIt should have a focused responsibility, a clear interface, correct dependency handling, and complete resource cleanup.",
    explanation:
      "Custom Hooks share logic, not a single state instance. Every call receives independent state unless the hook connects to a shared external source.",
    details:
      'Example:\n\n```jsx\nfunction useOnlineStatus() {\n  const [online, setOnline] = useState(\n    navigator.onLine\n  );\n\n  useEffect(() => {\n    const enable = () => setOnline(true);\n    const disable = () => setOnline(false);\n\n    window.addEventListener("online", enable);\n    window.addEventListener("offline", disable);\n\n    return () => {\n      window.removeEventListener("online", enable);\n      window.removeEventListener("offline", disable);\n    };\n  }, []);\n\n  return online;\n}\n```\n\nGood custom Hooks:\n\n- Begin with `use`\n- Follow the Rules of Hooks\n- Hide implementation details\n- Return a minimal, stable API\n- Avoid premature abstraction\n- Remain composable\n\nA wrapper around a single trivial line is not automatically a useful abstraction.',
  },
  {
    number: 14,
    question: "How do memo, useMemo, and useCallback differ?",
    answer:
      "- `memo` can skip rendering a component when its props are unchanged.\n- `useMemo` caches a calculated value between renders.\n- `useCallback` caches a function reference between renders.\n\nAll three are performance optimizations, not semantic guarantees.",
    explanation:
      "Memoization is valuable only when avoided work costs more than comparison and cache management.",
    details:
      "`memo`:\n\n```jsx\nconst UserRow = memo(function UserRow({ user }) {\n  return \u003cli>{user.name}\u003c/li>;\n});\n```\n\n`useMemo`:\n\n```jsx\nconst visibleItems = useMemo(\n  () => filterItems(items, query),\n  [items, query]\n);\n```\n\n`useCallback`:\n\n```jsx\nconst handleSelect = useCallback(\n  id => selectUser(id),\n  [selectUser]\n);\n```\n\nMemoization can fail when props are recreated every render. Custom equality functions are risky if they ignore changing callbacks or cost more than rendering.\n\nReact Compiler can automatically apply memoization in compiled applications, reducing the need for manual memoization. Manual tools still matter for deliberate identity contracts and uncompiled code.",
  },
  {
    number: 15,
    question: "How would you diagnose and improve React rendering performance?",
    answer:
      "Measure with the React Profiler and browser performance tools, identify expensive commits or interactions, and optimize the actual bottleneck.\n\nDo not treat every render as a performance bug.",
    explanation:
      "A render that produces no DOM changes may be inexpensive. Focus on user-visible delay, long tasks, repeated expensive calculations, and unnecessarily broad updates.",
    details:
      "Use a measure-first workflow instead of adding memoization immediately.\n\n1. **Define the slow interaction.** Record a concrete action such as typing in search, opening a panel, or changing a filter. Note the device, data size, and network conditions so the result is reproducible.\n2. **Separate React work from browser work.** Use the browser Performance panel to check whether time is spent in JavaScript, style calculation, layout, paint, network activity, or React rendering. React cannot optimize a slow image decode or an expensive CSS layout with `memo`.\n3. **Profile the React update.** Record the interaction with React DevTools Profiler. Inspect long commits, components with high self time, and components that render many times. Use “Why did this render?” information to trace changed props, state, Context, or external-store snapshots.\n4. **Find the update source.** Look for state placed too high in the tree, broad Context providers, unstable object or callback props, Effect-driven update chains, duplicated derived state, and subscriptions that notify more consumers than necessary.\n5. **Apply the smallest relevant fix.** Change the architecture before adding widespread memoization.\n6. **Measure again.** Compare the same interaction and confirm that user-visible latency improved without breaking behavior.\n\nMatch the optimization to the bottleneck:\n\n- **Too many components update:** colocate state, split Context, narrow store subscriptions, or compose children so unrelated subtrees are not recreated.\n- **One component performs expensive work:** simplify the calculation, precompute it, move it off the critical path, or memoize it when its dependencies are stable.\n- **A large collection creates too much DOM work:** paginate or virtualize it and keep row keys stable.\n- **An urgent interaction waits for non-urgent UI:** use a Transition or `useDeferredValue`. This changes scheduling; it does not make the calculation cheaper.\n- **Effects cause repeated commits:** remove derived-state Effects, fix dependency cycles, and keep user-triggered work in event handlers.\n- **The initial load is slow:** reduce client JavaScript, split code at meaningful boundaries, and investigate data or asset waterfalls.\n\n`memo`, `useMemo`, and `useCallback` are useful only when they skip work that costs more than their comparisons and cache maintenance. They can be ineffective when dependencies or props receive new identities on every render.\n\nProfile a production build with representative data. Development mode adds warnings and Strict Mode checks, and a fast desktop with a tiny dataset can hide the bottleneck users actually experience.",
  },
  {
    number: 16,
    question: "What are transitions and useDeferredValue?",
    answer:
      "Transitions mark state updates as non-urgent so React can keep urgent interactions responsive while rendering other work in the background.\n\n`useDeferredValue` provides a lagging version of a value, allowing part of the UI to update later.",
    explanation:
      "Text input should update urgently, while an expensive result list can update as non-urgent work.\n\n```jsx\nconst deferredQuery = useDeferredValue(query);\n```",
    details:
      "`useTransition` provides pending state:\n\n```jsx\nconst [isPending, startTransition] =\n  useTransition();\n\nstartTransition(() => {\n  setSelectedTab(nextTab);\n});\n```\n\nTransition work can be interrupted and restarted when urgent updates occur.\n\nTransitions should not control the value of text inputs because inputs require immediate synchronous updates.\n\n`useDeferredValue` is helpful when the component receives a value but cannot control the setter that changes it.\n\nThese APIs do not make slow calculations faster. They improve scheduling and perceived responsiveness. See [React `useTransition`](https://react.dev/reference/react/useTransition).",
  },
  {
    number: 17,
    question: "How do Suspense and lazy loading work?",
    answer:
      "A Suspense boundary displays fallback UI when supported content below it is not ready.\n\n`lazy` defers loading a component’s code until React first attempts to render it.",
    explanation:
      'Suspense coordinates loading boundaries and progressive disclosure:\n\n```jsx\nconst Reports = lazy(() => import("./Reports"));\n\n\u003cSuspense fallback={\u003cReportsSkeleton />}>\n  \u003cReports />\n\u003c/Suspense>\n```',
    details:
      "Suspense can be activated by supported mechanisms such as:\n\n- Components loaded with `lazy`\n- Promises read with `use`\n- Suspense-enabled frameworks and data sources\n- Streaming server rendering\n\nIt does not detect arbitrary data fetching started inside an Effect.\n\nBoundary placement is a user-experience decision. One large boundary may hide too much, while many tiny boundaries can create visual noise.\n\nTransitions can help preserve already visible content rather than replacing it with a fallback during non-urgent updates.\n\nSuspense also supports streaming server rendering and selective hydration. See [React Suspense](https://react.dev/reference/react/Suspense).",
  },
  {
    number: 19,
    question:
      "What is the difference between client-side rendering, server-side rendering, static generation, and hydration?",
    answer:
      "- Client-side rendering builds the interface primarily in the browser.\n- Server-side rendering produces HTML for each request.\n- Static generation produces HTML ahead of requests.\n- Hydration attaches React behavior to server-generated HTML.",
    explanation:
      "Server-generated HTML can improve initial content delivery, but the client and server must initially produce matching output.",
    details:
      "Client-only roots use:\n\n```jsx\ncreateRoot(container).render(\u003cApp />);\n```\n\nServer-rendered HTML uses:\n\n```jsx\nhydrateRoot(container, \u003cApp />);\n```\n\nHydration mismatches can be caused by:\n\n- Reading browser-only values during server rendering\n- Rendering current timestamps\n- Random values\n- Locale differences\n- Invalid HTML nesting\n- Different server and client data\n\nMismatches should be treated as bugs rather than broadly hidden with `suppressHydrationWarning`.\n\nStreaming can send completed server-rendered sections progressively. Frameworks usually coordinate routing, data loading, streaming, and hydration. See [React `hydrateRoot`](https://react.dev/reference/react-dom/client/hydrateRoot).",
  },
  {
    number: 20,
    question: "What are React Server Components?",
    answer:
      "Server Components render in a server environment and send their rendered result to the client without shipping their component code for client execution.\n\nClient Components are used where browser APIs, event handlers, client state, or Effects are required.",
    explanation:
      "Server Components can reduce client JavaScript and access server-side resources directly, but they require framework or bundler integration.",
    details:
      'Server Components can generally:\n\n- Read server-side data\n- Access trusted backend resources\n- Render Client Components\n- Pass serializable data to Client Components\n- Use async rendering patterns supported by the framework\n\nThey cannot directly use client-only Hooks such as `useState` or browser event handlers.\n\n`"use client"` marks a client module boundary:\n\n```jsx\n"use client";\n\nimport { useState } from "react";\n\nexport default function Counter() {\n  const [count, setCount] = useState(0);\n\n  return (\n    \u003cbutton onClick={() => setCount(count + 1)}>\n      {count}\n    \u003c/button>\n  );\n}\n```\n\nServer Components are not the same as traditional SSR. SSR produces HTML; Server Components produce a serialized component representation that can be combined with the client tree.',
  },
  {
    number: 21,
    question: "What are Actions, useActionState, and useOptimistic?",
    answer:
      "Actions represent transitions that may include asynchronous work and state updates.\n\n`useActionState` manages state produced by an Action, while `useOptimistic` temporarily displays an expected result before the underlying operation finishes.",
    explanation:
      "These APIs help coordinate submissions, pending states, errors, and optimistic feedback.",
    details:
      "Optimistic update:\n\n```jsx\nconst [optimisticMessages, addOptimisticMessage] =\n  useOptimistic(messages, (current, message) => [\n    ...current,\n    {\n      ...message,\n      sending: true\n    }\n  ]);\n```\n\nA good optimistic workflow must consider:\n\n- Failure rollback\n- Duplicate submissions\n- Race conditions\n- Server validation\n- Ordering\n- Accessibility of pending and error states\n\nOptimistic state does not replace server authority. The server result must reconcile the final state.\n\nActions may integrate with forms and transitions through React frameworks. Their exact server behavior depends on the framework’s Server Function implementation.",
  },
  {
    number: 18,
    question: "What are Error Boundaries, and what do they catch?",
    answer:
      "Error Boundaries catch rendering errors in descendant components and display fallback UI instead of allowing the entire React tree to fail.\n\nThey also provide a place to report component errors.",
    explanation:
      "An Error Boundary does not generally catch errors from event handlers, asynchronous callbacks, server-side rendering, or errors thrown inside the boundary itself.",
    details:
      "A traditional boundary is a class component:\n\n```jsx\nclass ErrorBoundary extends React.Component {\n  state = { failed: false };\n\n  static getDerivedStateFromError() {\n    return { failed: true };\n  }\n\n  componentDidCatch(error, info) {\n    reportError(error, info);\n  }\n\n  render() {\n    if (this.state.failed) {\n      return \u003cErrorFallback />;\n    }\n\n    return this.props.children;\n  }\n}\n```\n\nBoundaries should be placed around meaningful failure regions such as routes, dashboards, or independent panels.\n\nEvent-handler errors should be handled in the event handler. Async errors should be handled through Promise error handling or framework facilities.\n\nA changed key can reset a boundary and retry its subtree.",
  },
  {
    number: 28,
    question: "How would you handle data fetching and race conditions in React?",
    answer:
      "Prefer framework-integrated data loading or a dedicated server-state solution when available.\n\nWhen fetching in an Effect, handle cancellation, stale responses, loading, errors, caching, and repeated requests explicitly.",
    explanation: "A slower earlier request must not overwrite the result of a newer request.",
    details:
      'Cancellation example:\n\n```jsx\nuseEffect(() => {\n  const controller = new AbortController();\n\n  async function load() {\n    try {\n      const response = await fetch(\n        `/api/users/${userId}`,\n        { signal: controller.signal }\n      );\n\n      if (!response.ok) {\n        throw new Error(`HTTP ${response.status}`);\n      }\n\n      setUser(await response.json());\n    } catch (error) {\n      if (error.name !== "AbortError") {\n        setError(error);\n      }\n    }\n  }\n\n  load();\n\n  return () => controller.abort();\n}, [userId]);\n```\n\nSenior design considerations include:\n\n- Request deduplication\n- Cache invalidation\n- Retry policies\n- Optimistic updates\n- Pagination\n- Prefetching\n- Suspense integration\n- SSR and hydration\n- Authentication failures\n- Offline behavior\n\nFetching data independently in many Effects often creates waterfalls and duplicated server state.',
  },
  {
    number: 29,
    question:
      "What is the difference between a React element, a component, and a component instance?",
    answer:
      "A React element is an immutable description of UI. A component is a function or class that produces elements. An instance is React's mounted representation of a component at a particular tree position.",
    explanation:
      "JSX creates elements; React calls components and preserves their state according to tree identity.",
    details:
      "A React element is the value created by JSX:\n\n```jsx\nconst element = \u003cProfile user={user} />;\n```\n\nConceptually, it records the element type (`Profile`), its props, and identity information such as its key. It is a lightweight description, not a DOM node, a rendered component, or something whose props should be mutated.\n\nA component is the reusable definition:\n\n```jsx\nfunction Profile({ user }) {\n  return \u003ch2>{user.name}\u003c/h2>;\n}\n```\n\nReact calls the component while rendering and uses the returned elements to calculate the next UI. Call components through JSX rather than invoking `Profile()` yourself; React needs to control their rendering and Hook lifecycle.\n\n“Component instance” requires care. Class components have a literal class instance. Function components do not: React stores their Hook state and lifecycle data internally. In everyday discussion, an instance usually means one mounted occurrence of a component at a particular position in the tree.\n\nTwo `\u003cProfile />` elements can therefore represent two independent mounted occurrences with separate state. React preserves an occurrence when its type, position, and key still match; changing those identity signals can create a new occurrence and reset its state.",
  },
  {
    number: 30,
    question: "What are the Rules of Hooks, and why must Hooks be called unconditionally?",
    answer:
      "Hooks must be called at the top level of React components or custom Hooks, never inside conditions, loops, nested functions, or ordinary JavaScript functions.",
    explanation:
      "React associates Hook state with call order. Conditional calls can shift that order between renders and connect state to the wrong Hook.",
    details:
      "React relies on Hooks being called in the same order on every render:\n\n```jsx\nfunction Profile({ userId, enabled }) {\n  const [user, setUser] = useState(null); // Hook 1\n\n  if (enabled) {\n    useEffect(() => {                     // 🚩 sometimes Hook 2\n      loadUser(userId).then(setUser);\n    }, [userId]);\n  }\n}\n```\n\nIf `enabled` changes, the Hook sequence changes. React can no longer reliably associate each call with its previous state, Effect, or memoized value.\n\nKeep the Hook unconditional and put the condition inside it:\n\n```jsx\nuseEffect(() => {\n  if (!enabled) return;\n\n  const controller = new AbortController();\n  loadUser(userId, { signal: controller.signal })\n    .then(setUser);\n\n  return () => controller.abort();\n}, [enabled, userId]);\n```\n\nHooks may be called only while React is rendering a function component or another custom Hook. Do not call them in event handlers, callbacks, module scope, class methods, or ordinary utility functions. Extract a component when an entire conditional branch needs its own Hooks, or extract a custom Hook when reusable React behavior is needed.\n\nThe `use` API has special conditional and loop support, but it still may only run from a component or Hook and should not be wrapped in `try`/`catch`. It does not relax the rules for APIs such as `useState` or `useEffect`.\n\nUse `eslint-plugin-react-hooks`; runtime symptoms can be confusing, while the linter catches call-order and component/Hook-boundary mistakes during development.",
  },
  {
    number: 31,
    question: "What is the difference between props and state?",
    answer:
      "Props are inputs supplied by a parent. State is component-specific memory managed by React. Both are immutable snapshots during a render.",
    explanation:
      "A component must not mutate props or state directly; it requests state changes with setters or dispatches actions.",
    details:
      "Props describe what a parent wants a child to render or do. They can contain data, callbacks, elements, or children:\n\n```jsx\n\u003cSearchInput\n  value={query}\n  onValueChange={setQuery}\n/>\n```\n\n`SearchInput` reads `value`, but the parent owns it. The child requests a change by calling `onValueChange`; it must not assign to or mutate the prop.\n\nState is memory owned by a mounted component occurrence:\n\n```jsx\nconst [isOpen, setIsOpen] = useState(false);\n```\n\nCalling the setter queues another render. It does not mutate the state variable captured by the current render, so code should treat both props and state as immutable snapshots. When the next value depends on the previous one, use a functional update:\n\n```jsx\nsetCount(current => current + 1);\n```\n\nOwnership determines where state belongs:\n\n- Keep state local when only one subtree needs it.\n- Lift it to the nearest common owner when multiple components must stay coordinated.\n- Put shareable navigation state in the URL when appropriate.\n- Treat remote server data separately from transient interface state.\n\nAvoid copying a prop into state by default:\n\n```jsx\nconst [name, setName] = useState(user.name); // does not follow later user.name changes\n```\n\nUsually, render directly from the prop or derive the required value. A separate state value is appropriate when it intentionally has its own lifecycle, such as an editable draft. In that case, define explicitly when the draft resets, whether later prop changes are merged, and how unsaved edits are handled.",
  },
  {
    number: 32,
    question: "What is composition, and why is it generally preferred over inheritance in React?",
    answer:
      "Composition builds interfaces by combining smaller components through props, `children`, slots, and Hooks. It is usually preferred because relationships remain explicit and behavior can be assembled without a rigid inheritance hierarchy.",
    explanation:
      "It makes dependencies explicit and allows behavior and presentation to be combined without rigid class hierarchies.",
    details:
      "Common composition techniques include slots, compound components, render props, and custom Hooks:\n\n```jsx\n\u003cCard>\n  \u003cCard.Header>Account\u003c/Card.Header>\n  \u003cCard.Body>\u003cProfile />\u003c/Card.Body>\n\u003c/Card>\n```\n\nUse component composition for UI structure and custom Hooks for reusable stateful behavior. Inheritance is still a JavaScript feature, but React component reuse rarely needs it. A senior answer should also warn that composition can become obscure when a component exposes too many implicit slots or relies on hidden Context contracts.",
  },
  {
    number: 33,
    question: "What are Fragments, and when are they useful?",
    answer:
      "Fragments group multiple children without adding an extra DOM element, which is useful when an extra wrapper would break semantics, layout, or styling.",
    explanation: "They preserve valid structure and avoid unnecessary wrapper elements.",
    details:
      "Use `\u003c>...\u003c/>` for ordinary grouping. Use `\u003cFragment key={id}>...\u003c/Fragment>` when a keyed group is rendered in a list:\n\n```jsx\nitems.map(item => (\n  \u003cFragment key={item.id}>\n    \u003cdt>{item.term}\u003c/dt>\n    \u003cdd>{item.description}\u003c/dd>\n  \u003c/Fragment>\n));\n```\n\nThe shorthand syntax cannot receive a key. A Fragment affects the React tree but does not create a DOM node, styling hook, accessibility landmark, or event target; use a real semantic element when one is required.",
  },
  {
    number: 34,
    question:
      "How does React handle events, and how do React events differ from native DOM events?",
    answer:
      "React accepts event handlers as props and provides normalized event objects while integrating event dispatch with React's update system.",
    explanation:
      "Handlers receive an event with familiar APIs such as `target`, `currentTarget`, `preventDefault`, and `stopPropagation`.",
    details:
      "React events mostly follow DOM propagation. `event.currentTarget` is the element whose handler is running; `event.target` is where the event originated. Returning `false` does not prevent default behavior—call `preventDefault()` explicitly.\n\nEvents from portals bubble through the React tree, and React batches state updates made by handlers. Use native listeners when integrating with browser or third-party APIs, attach them in an Effect, and remove the exact same listener during cleanup. Do not reach for native listeners merely to avoid understanding React propagation.",
  },
  {
    number: 35,
    question: "What are portals, and how do events propagate through them?",
    answer:
      "A portal renders children into a different DOM container while keeping them in the same React tree.",
    explanation:
      "Context and React event propagation follow the React tree, not the portal's physical DOM position.",
    details:
      "Portals are useful for dialogs, popovers, and tooltips that must escape clipping or stacking containers:\n\n```jsx\nreturn createPortal(\u003cDialog />, document.body);\n```\n\nA click inside the dialog can still trigger an ancestor React handler even though the DOM nodes are elsewhere. Stop propagation only when that is the intended interaction contract. Portals do not automatically solve focus management, background inertness, stacking, keyboard behavior, server rendering, or accessible naming.",
  },
  {
    number: 36,
    question: "What is state colocation, and why does it matter?",
    answer: "State colocation keeps state close to the smallest subtree that needs it.",
    explanation:
      "It reduces unnecessary renders, coupling, synchronization, and global-store complexity.",
    details:
      "Lift state only when multiple branches genuinely need coordinated access. For example, keep an accordion item's hover state local, but lift the selected item when a details panel and a list must agree.\n\nOwnership also depends on the kind of state: shareable navigation state may belong in the URL, remote records in a server-state cache, field state in the form, and transient interaction state in the component. “Put everything in one store” is not a scalable state model; it broadens subscriptions and hides ownership.",
  },
  {
    number: 37,
    question: "When should state be lifted, and when should it remain local?",
    answer:
      "Lift state to the nearest common owner when components must share a single source of truth. Keep it local when no outside component needs to coordinate it.",
    explanation:
      "Excessively high state broadens render impact and makes components harder to reuse.",
    details:
      "Sometimes composition or moving related components together avoids lifting. When state is lifted, pass both the value and an intentional update API so there is one owner:\n\n```jsx\n\u003cSearchInput value={query} onQueryChange={setQuery} />\n\u003cResults query={query} />\n```\n\nDo not globalize state solely to avoid passing one or two props. Global state is justified when distant consumers need coordinated access, persistence, external updates, or selective subscriptions—not simply because prop passing is visible.",
  },
  {
    number: 38,
    question: "How can you avoid prop drilling?",
    answer:
      "First decide whether prop drilling is actually a problem. A few explicit props are often the clearest solution. When many intermediate components forward data they do not use, reduce the distance with state colocation or composition, use focused Context for shared subtree dependencies, and introduce an external store only when broad access and selective subscriptions justify it.",
    explanation:
      "Props make dependencies visible and keep components reusable. Context and stores remove forwarding code, but they introduce implicit dependencies and can broaden the impact of updates. The goal is better ownership and component boundaries—not zero prop passing.",
    details:
      "Use the smallest solution that matches the problem.\n\n**1. Keep ordinary props when the dependency is direct.**\n\nPassing data through one or two components is not harmful:\n\n```jsx\n\u003cProfilePage user={user} />\n```\n\nProps document what `ProfilePage` needs, work naturally with TypeScript, and make the data flow easy to trace. Do not add Context solely because a prop appears more than once.\n\n**2. Colocate state with the components that use it.**\n\nProp drilling often indicates that state was placed too high. If only `SearchPanel` needs `query`, keep it there instead of storing it at the page or application root. Colocation reduces both forwarding and unrelated rerenders.\n\n**3. Use composition to bypass intermediaries.**\n\nA layout does not need to know every dependency of its descendants:\n\n```jsx\nfunction App() {\n  return (\n    \u003cPageLayout\n      sidebar={\u003cUserMenu user={user} />}\n    >\n      \u003cDashboard />\n    \u003c/PageLayout>\n  );\n}\n```\n\n`PageLayout` receives complete UI rather than accepting `user` only to forward it to `UserMenu`. This keeps the layout generic and moves knowledge to the component that performs the composition.\n\n**4. Use Context for a shared subtree dependency.**\n\nContext fits values such as theme, locale, authenticated-user capabilities, or a compound component's shared state:\n\n```jsx\nconst PermissionsContext = createContext(null);\n\nfunction App({ permissions }) {\n  return (\n    \u003cPermissionsContext value={permissions}>\n      \u003cWorkspace />\n    \u003c/PermissionsContext>\n  );\n}\n```\n\nKeep each Context focused. Memoize a provider object when its identity would otherwise change unnecessarily, and split unrelated values that update at different rates. A Context update rerenders consumers that read that Context; `memo` does not block it.\n\n**5. Use an external store for genuinely shared mutable state.**\n\nA store becomes useful when distant parts of the application need the same changing data, updates can originate outside React, or consumers require selectors and narrow subscriptions. Examples include a collaborative document, normalized client cache, or complex cross-route workflow. A store should solve ownership and subscription requirements, not merely hide props.\n\nIn an interview, explain the trade-off: props favor explicit dependencies, Context favors convenient subtree access, and stores favor independently subscribed shared state. Choosing among them is an architectural decision about ownership, update frequency, and reuse.",
  },
  {
    number: 39,
    question: "What is the difference between derived state and synchronized state?",
    answer:
      "Derived data is calculated from existing props or state during rendering. Synchronized state is a separate stored value kept aligned with another source.",
    explanation: "Prefer derivation because duplicated state can become inconsistent.",
    details:
      "Derive during render:\n\n```jsx\nconst visibleTodos = todos.filter(todo => matches(todo, filter));\n```\n\nAvoid storing `visibleTodos` and updating it in an Effect; that adds an extra render and creates an invalid intermediate state. Memoize an expensive derivation only after measuring. Store a separate value when it has an independent lifecycle, such as an editable draft initialized from server data. Decide explicitly whether later server updates should reset, merge with, or leave that draft alone.",
  },
  {
    number: 40,
    question: "How should complex form state and validation be managed?",
    answer:
      "Separate field values, validation, submission, and server errors; use native form semantics; and choose controlled, uncontrolled, or library-managed state according to requirements.",
    explanation:
      "Validate at appropriate times without making typing unresponsive, and always validate again on the server.",
    details:
      "Start with the platform: meaningful field names, labels, suitable input types, native constraints, and `FormData`. Controlled state is useful when values immediately drive other UI; uncontrolled fields or field-level subscriptions can avoid rerendering an entire large form on every keystroke.\n\nComplex forms benefit from a shared validation schema, but client validation is only feedback—the server must validate and authorize again. Distinguish field errors from form-level and transport errors, preserve user input after failure, focus or summarize invalid fields accessibly, prevent accidental duplicate submission, and define what reset means after success. Reach for a form library when it reduces these requirements, not merely to store input values.",
  },
  {
    number: 41,
    question: "How can component state be intentionally preserved or reset?",
    answer:
      "React preserves state while the same component type remains at the same position in the rendered tree with the same key. To reset a subtree, give it a different key, render a different component type at that position, or remove it from the tree. Preserve state by keeping those identity signals stable.",
    explanation:
      "State is associated with a component’s identity in the rendered tree—not with a JSX variable, a particular conditional branch in the source code, or the component function by itself. React uses type, position, and key to decide whether the next element represents the existing mounted occurrence.",
    details:
      '**Preserving state**\n\nIf both branches place the same component at the same tree position, its state is preserved:\n\n```jsx\nreturn isCompact\n  ? \u003cCounter compact />\n  : \u003cCounter compact={false} />;\n```\n\nThe props change, but the element is still a `Counter` in the same position. Moving JSX into a variable or helper does not change identity by itself; the resulting tree is what matters.\n\nStable data keys preserve the correct item state when a list is reordered:\n\n```jsx\ntodos.map(todo => (\n  \u003cTodoEditor key={todo.id} todo={todo} />\n));\n```\n\nAn array index is unsafe when items can move, be inserted, or be removed because state may become attached to the wrong item.\n\n**Resetting state**\n\nA key can declare that two otherwise identical elements represent different identities:\n\n```jsx\n\u003cProfileEditor\n  key={selectedUserId}\n  userId={selectedUserId}\n/>\n```\n\nWhen `selectedUserId` changes, React unmounts the previous editor, runs its Effect cleanup, creates new state from the initial values, and mounts a fresh editor. This is often clearer than an Effect that tries to reset several fields after the new user has already rendered.\n\nRendering a different type also resets the subtree:\n\n```jsx\n{isEditing\n  ? \u003cProfileForm />\n  : \u003cProfileSummary />}\n```\n\nRemoving a component with conditional rendering resets it when it later returns. CSS hiding usually leaves it mounted, so state and Effects remain active. `\u003cActivity mode="hidden">` is another option when state should be retained while Effects are temporarily cleaned up.\n\n**Choose the reset boundary carefully.** A key resets the entire keyed subtree, including focus, uncontrolled input values, child state, refs, and Effects. Use a narrower key or an explicit state update when only one field should reset. Before resetting an edited form, also decide what should happen to unsaved work and where focus should move.\n\nAvoid defining a component inside another component:\n\n```jsx\nfunction Page() {\n  function Editor() {\n    return \u003cinput />;\n  }\n\n  return \u003cEditor />;\n}\n```\n\n`Editor` is a new component type on every `Page` render, so React can reset its subtree unexpectedly. Declare component types at module scope unless creating a new type is intentional.',
  },
  {
    number: 42,
    question: "When should state be represented with a state machine?",
    answer:
      "A state machine is useful when behavior has explicit modes, constrained transitions, concurrency, or impossible combinations that Boolean flags cannot model safely.",
    explanation: "It makes allowed states and events explicit and testable.",
    details:
      'Replace contradictory flags such as `isLoading`, `isSuccess`, and `hasError` with one discriminated state:\n\n```ts\ntype RequestState =\n  | { status: "idle" }\n  | { status: "pending" }\n  | { status: "success"; data: User }\n  | { status: "error"; error: Error };\n```\n\nA reducer may be enough when transitions are local. A state-machine library becomes valuable for guarded transitions, parallel states, retries, timeouts, and visualization. Examples include payment flows, multi-step forms, uploads, and authentication. Simple independent state does not need a machine.',
  },
  {
    number: 43,
    question: "How do dependency arrays work in useEffect, useMemo, and useCallback?",
    answer:
      "React compares each dependency with its previous value using `Object.is` and reruns or recalculates when a dependency changes.",
    explanation:
      "Dependencies are determined by reactive values read by the callback, not by which values a developer prefers to track.",
    details:
      "Every reactive value referenced by the callback—props, state, and variables or functions declared in the component—belongs in the dependency list unless it is proven non-reactive. Objects and functions created during rendering have new identities, so depending on them can retrigger work:\n\n```jsx\nuseEffect(() => {\n  const options = { roomId };\n  return connect(options);\n}, [roomId]);\n```\n\nMoving `options` inside the Effect removes the unnecessary object dependency. Other fixes include moving constants outside the component, using functional state updates, or extracting non-reactive Effect Events. `useMemo` and `useCallback` are performance tools, not loopholes for incorrect dependencies. Restructure code before suppressing the exhaustive-dependencies lint rule.",
  },
  {
    number: 44,
    question: "How would you debug an infinite useEffect loop?",
    answer:
      "Identify the state updated by the Effect and the dependency that changes because of that update.",
    explanation:
      "An infinite loop requires the Effect to update state and that update to change one of its dependencies.",
    details:
      "Add temporary logging to compare dependencies with `Object.is`, then trace the cycle: Effect → state update → render → changed dependency → Effect. A common example is depending on a newly created object or setting derived state on every run.\n\nFirst ask whether the Effect synchronizes with an external system. If not, derive the value during render or move action-specific logic into its event handler. Otherwise use a functional update, move object creation into the Effect, or stabilize only the genuinely required identity. Do not blindly remove dependencies, add an empty array, or use a ref to conceal a reactive value; those changes trade a visible loop for stale behavior.",
  },
  {
    number: 45,
    question: "How can asynchronous race conditions occur inside an Effect?",
    answer: "An older request may finish after a newer request and overwrite newer state.",
    explanation: "Completion order does not necessarily match request order.",
    details:
      'Cancel obsolete work and still guard the result because not every asynchronous stage observes an abort:\n\n```jsx\nuseEffect(() => {\n  const controller = new AbortController();\n\n  loadUser(userId, { signal: controller.signal })\n    .then(user => setUser(user))\n    .catch(error => {\n      if (error.name !== "AbortError") setError(error);\n    });\n\n  return () => controller.abort();\n}, [userId]);\n```\n\nA request ID or ignore flag can protect non-cancellable work. For real applications, a router or server-state library can additionally provide deduplication, caching, retries, and request identity. Cleanup prevents stale UI; it is not a substitute for server-side correctness.',
  },
  {
    number: 46,
    question: "When should logic remain in an event handler instead of moving into an Effect?",
    answer:
      "Keep logic in an event handler when a particular interaction causes it. Use an Effect when rendering the component—or a change to its reactive inputs—requires synchronization with an external system. If the value can be calculated from props or state, calculate it during rendering instead of using either mechanism to maintain duplicate state.",
    explanation:
      "Event handlers run once for the interaction that triggered them and have access to that render’s snapshot. Effects run after a commit and may run again whenever synchronization dependencies change. Moving action-specific work into an Effect loses its cause and can repeat it after remounting, navigation, or state restoration.",
    details:
      'Ask: **“Why should this code run?”**\n\nIf the answer is “because the user clicked Submit,” keep it in the handler:\n\n```jsx\nasync function handleSubmit(event) {\n  event.preventDefault();\n  await placeOrder(cart);\n  navigate("/confirmation");\n}\n```\n\nDo not set `submitted` and watch it with an Effect merely to call `placeOrder`. That separates the operation from its cause, adds another render, and risks duplicate submission if state is restored or the component remounts.\n\nIf the answer is “because this component is currently showing room 42,” use an Effect:\n\n```jsx\nuseEffect(() => {\n  const connection = connect(roomId);\n  connection.open();\n\n  return () => connection.close();\n}, [roomId]);\n```\n\nThis expresses synchronization: while a particular `roomId` is rendered, the matching connection should exist. Cleanup mirrors setup whenever the room changes or the component unmounts.\n\nDo neither when the result is derivable:\n\n```jsx\nconst fullName = `${firstName} ${lastName}`;\n```\n\nStoring `fullName` and updating it in an Effect creates redundant state and an extra commit. A useful interview summary is: rendering calculates UI, handlers perform interaction-caused work, and Effects synchronize committed UI with systems outside React.',
  },
  {
    number: 47,
    question:
      "How do you read the latest value inside a callback without unnecessarily restarting an Effect?",
    answer:
      "Choose the technique according to what the callback needs: use a functional state update when calculating from previous state, `useEffectEvent` when Effect-owned event logic needs the latest committed values without resubscribing, and a ref for mutable non-rendering data used by an external callback. Do not omit real dependencies merely to keep an Effect from restarting.",
    explanation:
      "Closures capture values from the render that created them. That snapshot is normally desirable, but a long-lived timer, subscription, or third-party callback may need a current value. The correct solution depends on whether that value is reactive to the synchronization itself.",
    details:
      '**Use a functional update when only the previous state is needed:**\n\n```jsx\nuseEffect(() => {\n  const id = setInterval(() => {\n    setCount(count => count + 1);\n  }, 1000);\n\n  return () => clearInterval(id);\n}, []);\n```\n\nThe updater receives the latest queued state, so `count` does not need to be captured by the interval Effect.\n\n**Use `useEffectEvent` for non-reactive logic triggered by an Effect:**\n\n```jsx\nconst onConnected = useEffectEvent(() => {\n  showNotification(`Connected as ${user.name}`, theme);\n});\n\nuseEffect(() => {\n  const connection = connect(roomId);\n  connection.on("connected", onConnected);\n  return () => connection.disconnect();\n}, [roomId]);\n```\n\nChanging `roomId` must reconnect, so it remains a dependency. Changing `theme` should only affect the next notification, so the Effect Event reads its latest value without reconnecting. Effect Events must be called from Effects or other Effect Events, must remain local to the owning component or Hook, and must not be used to hide dependencies that should resynchronize the Effect.\n\n**Use a ref for imperative mutable data that does not render:**\n\n```jsx\nconst latestPosition = useRef(position);\n\nuseEffect(() => {\n  latestPosition.current = position;\n}, [position]);\n```\n\nA third-party listener can read `latestPosition.current`, but changing it will not rerender the component. If users should see the value, it belongs in state. Prefer an honest reactive design first; refs and Effect Events are targeted escape hatches, not replacements for dependency arrays.',
  },
  {
    number: 48,
    question: "What is useId, and why should it not be used for list keys?",
    answer:
      "`useId` generates an identifier that is stable for a component occurrence and coordinates correctly between server rendering and hydration. It is intended for accessibility relationships and reusable component markup—not for identifying application data.",
    explanation:
      "A list key tells React which data item a child represents across insertions and reordering. A `useId` value is associated with where the Hook is called in the component tree, so it cannot provide the data identity that reconciliation needs.",
    details:
      "Use the generated prefix to connect related elements inside a reusable component:\n\n```jsx\nfunction PasswordField({ hint }) {\n  const id = useId();\n  const inputId = `${id}-input`;\n  const hintId = `${id}-hint`;\n\n  return (\n    \u003cdiv>\n      \u003clabel htmlFor={inputId}>Password\u003c/label>\n      \u003cinput id={inputId} aria-describedby={hintId} />\n      \u003cp id={hintId}>{hint}\u003c/p>\n    \u003c/div>\n  );\n}\n```\n\nMultiple `PasswordField` occurrences receive distinct relationships without requiring callers to coordinate IDs. This is especially useful in component libraries and avoids server/client ID mismatches that can occur with a module counter or random value during rendering.\n\nDo not call `useId` inside a loop to generate keys. Hooks cannot be called in loops, and the resulting value would still describe Hook position rather than item identity:\n\n```jsx\nitems.map(item => (\n  \u003cRow key={item.id} item={item} />\n));\n```\n\nUse an ID from the data or create one when the item itself is created. `useId` is also not a database ID, analytics identifier, CSS selector contract, or globally persistent value. If an explicit `id` prop is part of the component API, accept it and use `useId` as the fallback.",
  },
  {
    number: 49,
    question: "What does the use API do?",
    answer:
      "`use` reads a supported resource during rendering. With a Promise, it returns the fulfilled value, suspends at the nearest Suspense boundary while pending, or sends a rejection to the nearest Error Boundary. With Context, it reads the nearest provider value similarly to `useContext`.",
    explanation:
      "Unlike an Effect, `use` participates in rendering, so React can coordinate pending and failed resources with declarative boundaries. Unlike most Hooks, `use` may be called conditionally or in a loop, but it must still be called while React is rendering a component or custom Hook.",
    details:
      'A Server Component can create a Promise and pass it to a Client Component:\n\n```jsx\n// Server Component\nfunction Page() {\n  const commentsPromise = loadComments();\n\n  return (\n    \u003cSuspense fallback={\u003cCommentsSkeleton />}>\n      \u003cComments commentsPromise={commentsPromise} />\n    \u003c/Suspense>\n  );\n}\n```\n\n```jsx\n"use client";\n\nfunction Comments({ commentsPromise }) {\n  const comments = use(commentsPromise);\n  return comments.map(comment => (\n    \u003cComment key={comment.id} comment={comment} />\n  ));\n}\n```\n\nDo not create a new uncached Promise during every Client Component render; retries can continually produce new work and React warns about unsupported uncached Promises. Prefer a framework data source, a cached server-created Promise, or another Suspense-compatible resource.\n\nContext reads may be conditional:\n\n```jsx\nif (shouldUseTheme) {\n  const theme = use(ThemeContext);\n  return \u003cPanel theme={theme} />;\n}\n```\n\n`use` cannot be called in `try`/`catch` to handle a rejected Promise. Use an Error Boundary for failures and Suspense for pending UI. It also does not by itself define caching, request invalidation, or server authorization; those responsibilities belong to the resource and framework integration.',
  },
  {
    number: 50,
    question: "When would you use useImperativeHandle?",
    answer:
      "Use `useImperativeHandle` when a parent legitimately needs a small imperative capability—such as `focus()`, `scrollToError()`, or `reset()`—but should not receive the child’s entire DOM node or internal implementation.",
    explanation:
      "Most component coordination should remain declarative through props and state. An imperative handle is an escape hatch for operations that naturally describe commands and cannot be expressed cleanly as rendered output.",
    details:
      'In React 19, a function component can receive `ref` as a prop and expose a constrained handle:\n\n```jsx\nfunction SearchInput({ ref }) {\n  const inputRef = useRef(null);\n\n  useImperativeHandle(ref, () => ({\n    focus() {\n      inputRef.current?.focus();\n    },\n    select() {\n      inputRef.current?.select();\n    }\n  }), []);\n\n  return \u003cinput ref={inputRef} type="search" />;\n}\n```\n\nThe parent can call `searchRef.current.focus()` but cannot mutate arbitrary properties of the internal input. React versions before 19 commonly use `forwardRef` to receive the ref.\n\nInclude every reactive value used to create the handle in its dependency array. Otherwise methods can close over stale props or state. Keep the API small and behavioral: `focus()` is a better boundary than exposing internal nodes, state setters, or child implementation details.\n\nAvoid using an imperative handle to coordinate ordinary data flow:\n\n- Use props to configure rendering.\n- Use callbacks to report child events.\n- Lift state when parent and child need one source of truth.\n- Use refs only for commands that are genuinely imperative.\n\nAlso consider timing and lifecycle: the handle is available after commit, becomes `null` when the child unmounts, and should normally be used from an event handler or Effect rather than during rendering.',
  },
  {
    number: 57,
    question: "What do useTransition and useDeferredValue solve?",
    answer:
      "Both APIs let React render non-urgent work in the background so urgent interactions remain responsive. `useTransition` marks state updates that you initiate as non-urgent and exposes pending state. `useDeferredValue` gives a component a deferred version of a value when it cannot control the update that produced that value.",
    explanation:
      "React may pause, restart, or discard Transition rendering when a newer urgent update arrives, while the currently committed UI remains interactive. Priority changes when work is shown; it does not make the underlying calculation faster.",
    details:
      'Separate the immediate input update from the expensive result update:\n\n```jsx\nconst [query, setQuery] = useState("");\nconst [filter, setFilter] = useState("");\nconst [isPending, startTransition] = useTransition();\n\nfunction handleChange(event) {\n  const nextQuery = event.target.value;\n  setQuery(nextQuery); // urgent: controls the input\n\n  startTransition(() => {\n    setFilter(nextQuery); // non-urgent: updates expensive results\n  });\n}\n```\n\nDo not place the controlled input’s `setQuery` inside the Transition; React-controlled inputs must reflect typing synchronously. Use `isPending` to communicate background work without replacing useful existing content with a global spinner.\n\n`useDeferredValue(query)` is useful when a component receives `query` but does not control the update that produces it. The deferred value initially remains stale while React attempts the background render.\n\nTransition caveats include:\n\n- Transition work can be interrupted and restarted, so rendering must remain pure.\n- Updates after an `await` may need another `startTransition` to retain Transition priority.\n- Multiple concurrent Transitions may currently be batched together.\n- A Transition does not debounce, throttle, cancel network requests, or reduce CPU work.\n- Do not use it when stale UI would be incorrect or unsafe.\n\nIf a calculation itself blocks the main thread for too long, optimize or move that work; scheduling alone cannot make the browser responsive during one uninterrupted JavaScript task.',
  },
  {
    number: 77,
    question: "What does useActionState solve?",
    answer:
      "`useActionState` wraps an Action with state derived from its latest result. It returns the current state, an Action dispatcher, and pending status, making it useful for server validation messages, mutation results, and form submission state that should survive React’s Action flow.",
    explanation:
      "Unlike an ordinary submit handler plus several state setters, the Action receives the previous result state and submitted arguments, then returns the next result state. React associates pending and returned data with that specific Action.",
    details:
      '```jsx\nconst initialState = {\n  message: "",\n  fieldErrors: {}\n};\n\nfunction ProfileForm() {\n  const [state, submitAction, isPending] = useActionState(\n    saveProfile,\n    initialState\n  );\n\n  return (\n    \u003cform action={submitAction}>\n      \u003clabel htmlFor="display-name">Display name\u003c/label>\n      \u003cinput\n        id="display-name"\n        name="displayName"\n        aria-invalid={Boolean(state.fieldErrors.displayName)}\n        aria-describedby="display-name-error"\n      />\n      \u003cp id="display-name-error">\n        {state.fieldErrors.displayName}\n      \u003c/p>\n      \u003cbutton disabled={isPending}>Save\u003c/button>\n      \u003cp role="status">{state.message}\u003c/p>\n    \u003c/form>\n  );\n}\n```\n\nThe corresponding Action receives `previousState` before the submitted `FormData`:\n\n```jsx\nasync function saveProfile(previousState, formData) {\n  const result = validateProfile(formData);\n\n  if (!result.success) {\n    return {\n      message: "Check the highlighted fields.",\n      fieldErrors: result.fieldErrors\n    };\n  }\n\n  await persistProfile(result.data);\n  return { message: "Profile saved.", fieldErrors: {} };\n}\n```\n\nPass the returned dispatcher to `form action`, `button formAction`, or call it within an Action context. Do not call it as an arbitrary render-time function. Keep returned state small and serializable when a Server Function or progressive-enhancement transport carries it across the server boundary.\n\n`useActionState` models the latest Action result; it is not a general cache or a complete concurrent-mutation manager. Overlapping submissions still need domain-specific ordering, idempotency, and reconciliation.',
  },
  {
    number: 51,
    question: "Why does a child component render when its parent renders?",
    answer:
      "When a parent renders, React normally evaluates the child elements it returns and calls their component functions to calculate the next subtree. This does not mean React remounts the children or changes their DOM. State is preserved when identity matches, and React commits only actual host changes.",
    explanation:
      "A parent’s new render can produce different child props or structure, so React generally needs the child’s next output before reconciliation can determine what changed. Render propagation and DOM mutation are separate concepts.",
    details:
      'Consider this update:\n\n```jsx\nfunction Parent() {\n  const [count, setCount] = useState(0);\n\n  return (\n    \u003c>\n      \u003cbutton onClick={() => setCount(value => value + 1)}>\n        {count}\n      \u003c/button>\n      \u003cProfile name="Ada" />\n    \u003c/>\n  );\n}\n```\n\nClicking the button renders `Parent` and, by default, calls `Profile` again even though its visible prop is unchanged. If `Profile` returns equivalent output, React may perform no DOM mutation for it. Its local state is also preserved because it remains the same type at the same position.\n\nThis default behavior keeps data flow predictable and is often cheap. Investigate only when profiling shows meaningful cost. Possible boundaries include:\n\n- `memo` for a component whose unchanged props frequently allow expensive work to be skipped.\n- State colocation so an update starts lower in the tree.\n- Narrower Context or external-store subscriptions.\n- Composition that lets a stateful wrapper receive an already-created child element.\n\nDo not confuse rerendering with remounting. A remount creates new state, DOM, refs, and Effects; a rerender recalculates an existing occurrence. Also do not memoize every child preemptively—the comparison and identity-management costs may exceed the render work.',
  },
  {
    number: 52,
    question: "What causes a memoized component to render again?",
    answer:
      "A component wrapped in `memo` can render when a prop is not `Object.is`-equal to its previous value, its own state changes, a Context it reads changes, or an external subscription supplies a new snapshot. `memo` is a props-based optimization, not a guarantee that React will never call the component again.",
    explanation:
      "By default, `memo` compares each prop shallowly with `Object.is`. It can skip render work caused by the parent when props are equal, but a component must still react to the state and shared data it consumes directly.",
    details:
      "```jsx\nconst UserRow = memo(function UserRow({ user, onSelect }) {\n  const theme = useContext(ThemeContext);\n  return (\n    \u003cbutton className={theme} onClick={() => onSelect(user.id)}>\n      {user.name}\n    \u003c/button>\n  );\n});\n```\n\n`UserRow` can render again because:\n\n- `user` is a different object reference, even if its fields contain the same values.\n- `onSelect` is a newly created function.\n- Its own state changes.\n- `ThemeContext` provides a changed value.\n- A store Hook used inside it returns a changed snapshot.\n\nA parent render alone can be skipped only when all props compare equal. Passing children also counts as passing a prop; freshly created JSX can therefore break a memo boundary.\n\nA custom comparator is possible, but it must compare every prop, including callbacks that may close over changing values. Deep comparison can be slower than rendering and can freeze stale behavior when implemented incorrectly.\n\nReact may call memoized components again for development checks or discard cached work for implementation reasons. Components must remain correct without memoization. Use the Profiler to confirm both that the component is costly and that its props remain stable often enough for `memo` to help.",
  },
  {
    number: 53,
    question: "Why can inline objects and functions affect memoization?",
    answer:
      "Inline object, array, function, and JSX expressions usually create new references on every render. When those references are props of a memoized child or dependencies of a Hook, React sees them as changed even if their contents or behavior look equivalent.",
    explanation:
      "Referential identity is not inherently a performance problem. It matters only at a boundary that compares references, such as `memo`, a dependency array, a Context provider value, or a selector cache.",
    details:
      'This defeats the `memo` boundary on every parent render:\n\n```jsx\n\u003cResults\n  options={{ sort: "name" }}\n  onSelect={item => setSelectedId(item.id)}\n/>\n```\n\nPossible fixes depend on what the values capture:\n\n```jsx\nconst options = useMemo(\n  () => ({ sort }),\n  [sort]\n);\n\nconst handleSelect = useCallback(\n  item => setSelectedId(item.id),\n  []\n);\n\n\u003cResults options={options} onSelect={handleSelect} />;\n```\n\nPrefer simpler fixes first: pass primitives instead of a configuration object, move a true constant to module scope, accept an item ID instead of closing over an item, or move object creation inside the Effect that uses it.\n\n`useMemo` and `useCallback` do not prevent creation of all values or make a function faster. They preserve a reference between renders while dependencies remain equal. That has its own comparison, memory, and readability cost.\n\nInline handlers on ordinary DOM elements are normally fine. If there is no meaningful identity-sensitive boundary or measured bottleneck, keeping the inline expression is clearer than adding defensive memoization everywhere.',
  },
  {
    number: 54,
    question: "How would you render a list containing thousands of items efficiently?",
    answer:
      "Start by limiting how much data and DOM the interface needs at once. Paginate or incrementally load remote data, virtualize large scrollable collections, keep row identity stable, and prevent unrelated state changes from rerendering every row. Then profile React work and browser layout or paint separately.",
    explanation:
      "Large lists have several independent costs: fetching and transforming data, calling row components, creating DOM nodes, calculating layout, painting, and retaining memory. Optimizing only React renders may not fix the dominant browser cost.",
    details:
      "Use a staged approach:\n\n1. **Reduce the dataset.** Apply server-side filtering and pagination when users do not need every record locally. Avoid downloading 100,000 rows merely to display 20.\n2. **Keep data operations efficient.** Do not repeatedly sort or filter the whole collection in every row. Compute once per relevant change, move expensive processing to the server or a worker when appropriate, and debounce only input that should intentionally lag.\n3. **Keep identity stable.** Use a durable row key such as `row.id`, not the array index. Preserve row objects when their data has not changed so selective memoization or store subscriptions can work.\n4. **Limit DOM size.** Virtualize a genuinely large scrollable list so only visible rows plus a small overscan region are mounted.\n5. **Limit update scope.** Colocate hover or edit state in the row, subscribe rows to only the data they need, and avoid a Context value that changes for every pointer or keystroke event.\n\nVirtualization is not free. Variable row heights require measurement, and unmounting off-screen items complicates browser find, printing, focus, screen-reader navigation, sticky content, and scroll restoration. Pagination may provide a better user experience for searchable business data; virtualization is often better for continuous feeds or dense explorers.\n\nTest with representative row counts and interactions. Measure scripting, layout, paint, memory, and responsiveness—not only the number of React renders.",
  },
  {
    number: 55,
    question: "What is list virtualization?",
    answer:
      "List virtualization, or windowing, renders only the items in or near the viewport while simulating the full collection’s scrollable size. As the user scrolls, the rendered window moves and DOM nodes are created, removed, or reused.",
    explanation:
      "The virtualizer calculates which item indices intersect the viewport from scroll position and item measurements. Padding, spacers, or positioned rows preserve the scrollbar geometry even though most items are not mounted.",
    details:
      "A simplified fixed-height calculation is:\n\n```js\nconst start = Math.floor(scrollTop / rowHeight);\nconst visibleCount = Math.ceil(viewportHeight / rowHeight);\nconst from = Math.max(0, start - overscan);\nconst to = Math.min(items.length, start + visibleCount + overscan);\n```\n\nOnly `items.slice(from, to)` is rendered, offset to its logical position. Overscan renders a few extra rows before and after the viewport so fast scrolling does not reveal blank gaps.\n\nProduction implementations must address:\n\n- Dynamic heights and measurement changes\n- Resize and responsive layout\n- Stable keys and item reordering\n- Scroll anchoring and restoration\n- Focus moving to an item that would otherwise unmount\n- Keyboard navigation to an off-screen item\n- Screen-reader expectations and collection metadata\n- Sticky headers, printing, and browser find\n\nUse a mature virtualizer unless the requirements are extremely simple. Provide programmatic scrolling for focused or selected items and test zoom, reduced motion, keyboard-only use, and assistive technology. For modest collections, ordinary rendering may be faster to build, more accessible, and sufficiently performant.",
  },
  {
    number: 56,
    question: "How do code splitting and route-based lazy loading differ?",
    answer:
      "Code splitting divides application code into independently loadable chunks. Route-based lazy loading is a code-splitting strategy that loads the code for a route only when navigation requires—or deliberately prefetches—it. Routes are good default boundaries, while large optional features may justify additional component-level splits.",
    explanation:
      "Splitting lowers the initial JavaScript download, parse, and execution cost, but it moves some work to later navigation. A useful boundary ships less unused code without creating a waterfall of tiny chunks and disruptive loading states.",
    details:
      '`lazy` loads a component module when React first attempts to render it:\n\n```jsx\nconst ReportsPage = lazy(() => import("./ReportsPage.js"));\n\nfunction App() {\n  return (\n    \u003cSuspense fallback={\u003cPageSkeleton />}>\n      \u003cReportsPage />\n    \u003c/Suspense>\n  );\n}\n```\n\nThe import Promise and resolved module are cached. If loading fails, the rejection should reach an Error Boundary that can offer retry or navigation recovery. The basic `lazy` contract expects the module’s `.default` export to be a valid component.\n\nFramework routers usually provide stronger route-level integration: matching routes before rendering, loading code and data in parallel, streaming, prefetching likely destinations, and producing route-specific error UI. Prefer that integration over adding a single Suspense boundary around the whole application.\n\nGood additional split points include rarely opened editors, charts, admin tools, and large optional libraries. Avoid splitting a tiny component used immediately on every page; request overhead and fallback churn may outweigh the saved bytes.\n\nEvaluate:\n\n- Initial and route-specific transferred JavaScript\n- Parse and execution time, not only compressed bundle size\n- Cache stability between deployments\n- Code-and-data waterfalls\n- Loading, error, and offline behavior\n- Whether hover, viewport, or idle prefetching improves navigation\n\nCode splitting improves delivery. It does not reduce the runtime cost of code after that code has loaded.',
  },
  {
    number: 58,
    question: "What problems does React Compiler solve?",
    answer:
      "React Compiler analyzes components and Hooks at build time and automatically applies memoization where it can safely reuse values or UI. Its main benefit is reducing routine manual `memo`, `useMemo`, and `useCallback` work while enabling more precise updates.",
    explanation:
      "The compiler relies on the Rules of React—especially pure rendering and correct Hook usage—to prove that reuse is safe. It optimizes valid React code; it is not a runtime diffing replacement or a tool that repairs incorrect component behavior.",
    details:
      "Without the compiler, developers often stabilize a child and its props manually:\n\n```jsx\nconst visibleTodos = useMemo(\n  () => filterTodos(todos, tab),\n  [todos, tab]\n);\n\nconst handleSelect = useCallback(\n  id => setSelectedId(id),\n  []\n);\n\nreturn \u003cTodoList todos={visibleTodos} onSelect={handleSelect} />;\n```\n\nThe compiler can infer many equivalent reuse opportunities from component data flow. This reduces identity plumbing and the risk of adding memoization in the wrong places.\n\nAdoption still requires engineering work:\n\n- Enable the recommended Hooks and compiler lint rules.\n- Fix render-time mutation and other Rules-of-React violations.\n- Roll out incrementally when the application has risky legacy patterns.\n- Monitor diagnostics and avoid broadly suppressing skipped compilation.\n- Run behavior tests and compare production performance profiles.\n\nExisting manual memoization does not need to be removed immediately. It may document an intentional identity contract or support uncompiled code. Remove it only when compiler coverage, tests, and measurement show that simpler code remains correct and performant.\n\nThe compiler does not solve poor state ownership, broad Context updates, inefficient algorithms, network waterfalls, large DOM trees, layout or paint bottlenecks, or memory leaks. Components must remain correct when a cached value is recomputed; memoization is still an optimization rather than semantic state.",
  },
  {
    number: 59,
    question: "When can memoization make performance worse?",
    answer:
      "Memoization can make performance and maintainability worse when the skipped work is cheaper than dependency checks, prop comparison, and cache retention; when inputs change almost every render; or when developers add expensive custom equality logic. Incorrect dependencies can additionally preserve stale values and behavior.",
    explanation:
      "Memoization is not free: React must retain a value, compare dependencies or props, and maintain another identity contract that future code must understand. It helps only when a cache hit skips meaningful work often enough to repay those costs.",
    details:
      "Low-value memoization often looks like:\n\n```jsx\nconst fullName = useMemo(\n  () => `${firstName} ${lastName}`,\n  [firstName, lastName]\n);\n```\n\nConcatenating two strings is cheaper and clearer than maintaining this cache. Memoization is more plausible when a measured expensive calculation repeats with unchanged inputs or a costly memoized child frequently receives otherwise stable props.\n\nCommon failure modes include:\n\n- A new object or callback dependency invalidates the cache every render.\n- A custom `memo` comparator performs a deep comparison slower than rendering.\n- A comparator ignores a callback and preserves a stale closure.\n- Large cached values increase retained memory.\n- `useCallback` is added even though the function is not passed to an identity-sensitive consumer.\n- Dependency arrays are intentionally incomplete, turning an optimization into a correctness bug.\n\nPrefer structural improvements first: colocate state, derive rather than synchronize values, narrow Context or store subscriptions, reduce DOM size, and eliminate Effect update chains. These changes reduce the work itself rather than attempting to cache around an overly broad update.\n\nUse production profiling to compare the complete interaction before and after. Count neither `useMemo` calls nor rerenders as the success metric; measure user-visible latency, commit duration, main-thread work, and memory where relevant.",
  },
  {
    number: 60,
    question: "How would you investigate a component that renders too frequently?",
    answer:
      "First determine whether the renders are both unexpected and expensive. Reproduce one interaction, record it with React DevTools Profiler, identify what scheduled each update, and trace changed props, local state, Context, or external-store snapshots. Fix the narrowest update source, then profile the same interaction again.",
    explanation:
      "Render count alone is not a performance metric. A frequently rendered small component may be harmless, while one infrequent render can block the main thread. Diagnosis must connect an update to measured render or commit cost and user-visible delay.",
    details:
      "Use this investigation sequence:\n\n1. **Establish the trigger.** Choose one action—typing a character, selecting a row, or receiving a store update—and reproduce it with representative data.\n2. **Profile a production build.** Development Strict Mode deliberately repeats some rendering and Effect work. It can reveal bugs, but it is not representative performance data.\n3. **Find the costly commit.** In React DevTools Profiler, inspect which components rendered, their self time, and why their props or state changed. Use browser Performance tools when layout, paint, or other JavaScript may dominate.\n4. **Trace the update source.** Follow the state setter, Context provider, reducer dispatch, router update, or external-store notification that scheduled the work.\n5. **Verify the fix.** Repeat the same profile and compare user-visible timing, not only the component count.\n\nFrequent causes include:\n\n- State is lifted above a large subtree even though only one branch needs it.\n- A provider creates `{ user, permissions }` on every render or combines values with different update frequencies.\n- An Effect sets derived state, producing a second commit after every relevant render.\n- A store selector returns a fresh object or subscribes to the entire store.\n- A parent recreates props that defeat a valuable `memo` boundary.\n- A component sets state unconditionally during an Effect or subscription callback.\n- A list uses unstable keys, causing remounts rather than ordinary rerenders.\n\nMatch the fix to the cause: colocate state, split Context, return stable store snapshots, remove derived-state Effects, preserve data identity, or memoize only a measured expensive boundary. If the render is cheap and produces no user-visible problem, the correct fix may be no change.",
  },
  {
    number: 61,
    question: "What is the difference between Suspense and an Error Boundary?",
    answer:
      "Suspense handles supported work that is not ready yet and shows a fallback while React waits. An Error Boundary handles errors thrown while rendering a descendant tree and replaces that failed region with error UI. Pending and failed are different states, so robust interfaces commonly use both boundaries.",
    explanation:
      "A Suspense-compatible resource communicates pending work to React, while a rejected resource or rendering exception propagates to an Error Boundary. Suspense does not catch errors, and an Error Boundary is not a loading indicator.",
    details:
      "Combine the boundaries around a meaningful region:\n\n```jsx\n\u003cErrorBoundary fallback={\u003cReportsError />}>\n  \u003cSuspense fallback={\u003cReportsSkeleton />}>\n    \u003cReports />\n  \u003c/Suspense>\n\u003c/ErrorBoundary>\n```\n\nIf `Reports` reads a pending lazy import or supported Promise, the Suspense fallback appears. If rendering throws or that Promise rejects, the Error Boundary displays its fallback.\n\nSuspense works only with integrated sources such as:\n\n- Components loaded with `lazy`\n- Promises read with `use`\n- Suspense-enabled framework data sources\n- Streaming server rendering\n\nIt does not detect a `fetch` started inside an Effect or automatically show a fallback for any arbitrary asynchronous operation.\n\nError Boundaries catch errors in descendant rendering and relevant lifecycle work. They do not normally catch errors from event handlers, arbitrary asynchronous callbacks, server rendering, or the boundary’s own fallback. Handle an event failure in the event workflow and put it into state when the UI should display it.\n\nBoundary order controls scope. A boundary outside Suspense can handle both the content and loading subtree. More focused boundaries can let one failed widget degrade without replacing an entire page. Always provide recovery where possible and report errors with useful component and request context.",
  },
  {
    number: 62,
    question: "Where should Suspense boundaries be placed?",
    answer:
      "Place a Suspense boundary around a user-visible region that can load and reveal as one unit, has a useful fallback, and can remain independent from surrounding content. Use boundaries to express the intended loading experience—not around every asynchronous component mechanically.",
    explanation:
      "One page-level boundary can replace useful existing content with a large spinner, while many tiny boundaries produce layout shifts and a patchwork of skeletons. The correct granularity follows the design’s reveal sequence and navigation behavior.",
    details:
      "Good boundary candidates include a route body, search-results region, comments panel, or secondary dashboard widget. Ask:\n\n- Can this region show a meaningful skeleton or retained previous state?\n- Should it reveal independently from nearby content?\n- If it is slow, should the rest of the page remain interactive?\n- On the server, is it a useful streaming boundary?\n- Where should errors and retries be isolated?\n\n```jsx\n\u003cArticle />\n\n\u003cSuspense fallback={\u003cCommentsSkeleton />}>\n  \u003cComments articleId={articleId} />\n\u003c/Suspense>\n```\n\nThe article can remain visible while comments load. In contrast, placing one boundary around both would hide the already useful article.\n\nWhen already displayed content suspends during a non-urgent update, use a Transition or deferred value where appropriate so React can retain stale content instead of immediately replacing it with a fallback. Communicate that refresh is pending rather than making the interface appear to disappear.\n\nDesign fallbacks to preserve approximate dimensions, accessible labels, and context. Avoid spinner-only interfaces for large regions and avoid announcing every nested boundary independently. Boundary placement should be agreed with the product loading design, not decided only from component-file boundaries.",
  },
  {
    number: 63,
    question: "How do nested Suspense boundaries coordinate progressive loading?",
    answer:
      "Nested Suspense boundaries create a reveal sequence. The outer boundary controls the first larger region; after it can reveal its primary content, an inner boundary may continue showing its own fallback until its slower subtree is ready.",
    explanation:
      "Nesting allows React to reveal useful content progressively without exposing an incomplete subtree outside the loading states designed for it. Each boundary coordinates everything directly inside it until a deeper boundary takes responsibility.",
    details:
      "```jsx\n\u003cSuspense fallback={\u003cPageSkeleton />}>\n  \u003cBiography />\n\n  \u003cSuspense fallback={\u003cAlbumsSkeleton />}>\n    \u003cAlbums />\n  \u003c/Suspense>\n\u003c/Suspense>\n```\n\nInitially, `PageSkeleton` appears if the outer primary content cannot reveal. Once `Biography` and the structure containing the inner boundary are ready, React can show them while `AlbumsSkeleton` remains. `Albums` replaces only its local fallback when ready.\n\nSibling components inside one boundary reveal together from the user’s perspective. Give them separate boundaries only when independent reveal is desirable. If a fallback itself suspends, React looks for the next Suspense boundary above it, so fallbacks should usually be simple and immediately renderable.\n\nNested boundaries do not automatically prevent network waterfalls. Start independent code and data requests in parallel through the router, framework, or resource layer; otherwise the inner request may not begin until an earlier component renders.\n\nWatch for layout movement and repeated announcements as fallbacks are replaced. Skeletons should reserve useful space, and accessibility messaging should describe meaningful loading progress without producing noise for every small region.",
  },
  {
    number: 64,
    question: "How can an Error Boundary be reset?",
    answer:
      "An Error Boundary must clear its recorded error before it can attempt to render its children again. Reset it through an explicit retry supported by the boundary, or change its key when a route, entity, or other identity change should create a fresh boundary and subtree.",
    explanation:
      "Simply rendering the same broken children again can immediately throw the same error. Recovery should first change the failed condition—retry a request, discard invalid state, load a different entity, or deploy corrected code—and then reset the boundary.",
    details:
      "A class Error Boundary commonly stores `hasError` in state and exposes a retry action that clears it:\n\n```jsx\nclass ErrorBoundary extends Component {\n  state = { error: null };\n\n  static getDerivedStateFromError(error) {\n    return { error };\n  }\n\n  retry = () => {\n    this.setState({ error: null });\n  };\n\n  render() {\n    if (this.state.error) {\n      return \u003cErrorFallback onRetry={this.retry} />;\n    }\n\n    return this.props.children;\n  }\n}\n```\n\nIn production, the retry usually also invalidates or restarts the failed resource. A library boundary may provide `resetErrorBoundary`, reset keys, and an `onReset` callback for this coordination.\n\nA changed key performs a full reset:\n\n```jsx\n\u003cErrorBoundary key={projectId} fallback={\u003cProjectError />}>\n  \u003cProject projectId={projectId} />\n\u003c/ErrorBoundary>\n```\n\nThis is appropriate when the project identity changes, but it also remounts the entire subtree and loses its local state. Do not use an excessively broad key when only the failed request needs retrying.\n\nThe fallback should explain what failed, offer a safe next action, remain keyboard accessible, and avoid retry loops. Report the original error with component context while filtering secrets and user data from logs.",
  },
  {
    number: 65,
    question: "How should loading, empty, error, and stale states be represented?",
    answer:
      "Represent asynchronous UI with explicit states and metadata rather than unrelated Booleans. Distinguish initial loading, successful empty data, successful populated data, blocking failure, background refresh, and stale data with a refresh error. Some of these states legitimately overlap.",
    explanation:
      "`isLoading`, `isError`, and `hasData` can produce contradictory combinations and encourage the UI to discard useful content. A state model should preserve the information needed to choose the correct user experience.",
    details:
      'A discriminated model makes invalid states harder to express:\n\n```ts\ntype ResourceState\u003cT> =\n  | { status: "idle" }\n  | { status: "loading" }\n  | { status: "success"; data: T; refreshing: boolean }\n  | { status: "error"; error: Error }\n  | { status: "stale-error"; data: T; error: Error };\n```\n\nRender each state deliberately:\n\n- **Initial loading:** show a skeleton or progress indicator because no useful content exists yet.\n- **Empty success:** explain that the request succeeded but returned no results; provide an appropriate next action rather than showing an error.\n- **Populated success:** render the data normally.\n- **Background refresh:** retain existing data, indicate subtle progress, and avoid replacing the whole region with a loading fallback.\n- **Blocking error:** show failure UI and a retry path when no usable data exists.\n- **Stale error:** retain cached data, mark it as potentially outdated, and explain that refresh failed.\n\nEmpty must be defined by the domain: an empty filtered result is different from a new account with no records. Likewise, a `404`, validation error, permission failure, offline state, and server failure may need different actions.\n\nAccessibility is part of the model. Give loading indicators accessible names, announce meaningful asynchronous completion with a restrained live region, associate field errors with controls, and avoid moving focus for ordinary background refreshes. The state model should also carry retry eligibility, timestamps, and request identity when the interface needs them.',
  },
  {
    number: 66,
    question: "How would you prevent an outdated request from replacing newer data?",
    answer:
      "Abort obsolete work when possible and independently guard state updates with request identity. Cancellation saves resources, while an identity check ensures that only the result belonging to the latest active query can update the UI. A router or server-state library should provide this behavior when data fetching is application-wide.",
    explanation:
      "Network completion order is nondeterministic. If request A starts first, request B starts second, and A finishes last, an unconditional A result can overwrite the newer B result. Unmounting and rapid dependency changes create the same class of stale completion.",
    details:
      'Combine cleanup with a request guard:\n\n```jsx\nconst requestIdRef = useRef(0);\n\nuseEffect(() => {\n  const controller = new AbortController();\n  const requestId = ++requestIdRef.current;\n\n  setState(current => ({\n    ...current,\n    status: current.data ? "refreshing" : "loading"\n  }));\n\n  search(query, { signal: controller.signal })\n    .then(data => {\n      if (requestId === requestIdRef.current) {\n        setState({ status: "success", data });\n      }\n    })\n    .catch(error => {\n      if (\n        error.name !== "AbortError" &&\n        requestId === requestIdRef.current\n      ) {\n        setState({ status: "error", error });\n      }\n    });\n\n  return () => controller.abort();\n}, [query]);\n```\n\n`AbortController` tells cooperative APIs such as `fetch` to stop, but cancellation is not a complete correctness guarantee. Work may already have resolved, parsing may continue, or a custom Promise may ignore the signal. The request ID protects the commit even when cancellation cannot stop the work.\n\nFor mutations, “latest wins” may be incorrect: every write may matter, ordering may be significant, and retry can create duplicates. Use operation IDs, idempotency keys, optimistic-update reconciliation, or serialization according to the domain.\n\nPrefer router loaders or a server-state library when you also need cache keys, deduplication, invalidation, retries, pagination, SSR, and shared subscriptions. The component should not reimplement an incomplete request cache in Effects.',
  },
  {
    number: 67,
    question: "What causes hydration mismatches, and how would you debug them?",
    answer:
      "A hydration mismatch occurs when the client’s first render does not produce the same structure and content as the server HTML React is attaching to. Common causes include time or randomness, locale and time-zone differences, browser-only branches, invalid HTML nesting, changed data, and DOM modification before React starts.",
    explanation:
      "Hydration reuses existing server DOM instead of constructing it from scratch. React therefore expects the server render and initial client render to describe equivalent UI; a mismatch can force recovery and may attach behavior to an unexpected structure.",
    details:
      'Diagnose the first divergence rather than the largest subtree mentioned by the warning:\n\n1. Capture the full hydration warning and component stack.\n2. Inspect the original server response before client scripts mutate it.\n3. Reproduce with the same URL, authentication state, locale, time zone, feature flags, and initial data.\n4. Compare the server HTML with the client’s first render—not with the DOM after Effects run.\n5. Validate HTML nesting; browsers may repair invalid markup before React sees it.\n\nTypical nondeterministic code includes:\n\n```jsx\n// 🚩 Different values on the server and client\n\u003cp>{new Date().toLocaleString()}\u003c/p>\n\u003cspan>{Math.random()}\u003c/span>\n{typeof window !== "undefined" && \u003cClientMenu />}\n```\n\nPrefer deterministic initial output. Serialize the same initial data to the client, format with an explicit shared locale and time zone, or render a stable placeholder and update it after hydration. For browser-only behavior, move the behavior—not arbitrary markup differences—behind a Client Component or Effect.\n\nAlso check third-party scripts, password managers, browser extensions, CDNs that rewrite HTML, CSS-in-JS configuration, and incorrectly nested tags. A mismatch reported at one node may originate from invalid markup above it.\n\n`suppressHydrationWarning` is a limited escape hatch for deliberately unavoidable differences such as a timestamp. It works only at limited depth and does not repair the content. Do not apply it broadly to hide an unexplained mismatch.',
  },
  {
    number: 68,
    question: "What is streaming server rendering?",
    answer:
      "Streaming server rendering sends an initial HTML shell as soon as it is ready, then progressively streams later Suspense regions as their server work completes. The browser can display useful content before the entire React tree has finished rendering.",
    explanation:
      "Traditional all-or-nothing SSR waits for every server dependency before sending the page. Streaming lets fast and slow regions progress independently, improving time to first content while preserving a declarative loading design.",
    details:
      "Suspense boundaries define the streaming units:\n\n```jsx\n\u003cPageLayout>\n  \u003cArticle />\n  \u003cSuspense fallback={\u003cCommentsSkeleton />}>\n    \u003cComments />\n  \u003c/Suspense>\n\u003c/PageLayout>\n```\n\nThe server can send the layout, article, and comments fallback first. When `Comments` is ready, React streams additional HTML and instructions that replace the fallback in the correct location.\n\nReact provides server APIs such as `renderToPipeableStream` for Node streams and `renderToReadableStream` for Web Streams, but frameworks normally manage routing, data loading, status codes, head metadata, caching, aborts, and deployment integration.\n\nA production design must decide:\n\n- Which content belongs in the initial shell\n- Where Suspense and Error Boundaries isolate slow or failed regions\n- When headers and HTTP status become committed\n- How disconnected clients abort server work\n- Whether proxies and hosting infrastructure buffer the stream\n- How streamed HTML is protected by the site’s CSP and nonce strategy\n\nStreaming improves delivery order; it does not make slow data sources faster. Start independent requests early and in parallel, otherwise the server can still stream a waterfall one delayed boundary at a time.",
  },
  {
    number: 69,
    question: "What is selective hydration?",
    answer:
      "Selective hydration lets React attach interactivity to server-rendered Suspense regions according to code/data readiness and user priority instead of hydrating the entire application as one blocking operation. An interaction can cause React to prioritize the relevant boundary.",
    explanation:
      "Server HTML may already be visible while its Client Component JavaScript is still loading or waiting to hydrate. React can hydrate available regions independently and prioritize an attempted interaction so the page becomes useful sooner.",
    details:
      "Imagine a streamed page containing an article and a comments widget. If the comments bundle is delayed, React can hydrate the article without waiting. If the user interacts with the comments region, React can raise that boundary’s priority and replay supported events after hydration completes.\n\nSuspense boundaries provide the units React can coordinate. Streaming controls when server HTML arrives; code splitting controls when Client Component code arrives; selective hydration controls when React attaches behavior to each region. These features complement one another but describe different stages.\n\nSelective hydration is not the same as completely independent “islands.” The regions remain part of one React tree with normal Context and event semantics. It also does not remove the cost of hydration—large Client Component trees still ship, parse, and execute JavaScript.\n\nImprove hydration by keeping Client Component boundaries narrow, splitting code at useful interaction boundaries, avoiding large synchronous initialization, and ensuring server/client output matches. Framework integration normally coordinates these details.",
  },
  {
    number: 70,
    question:
      "What determines whether a component should be a Server Component or a Client Component?",
    answer:
      "Make a component a Client Component when its module needs client-only capabilities such as state, Effects, event handlers, refs, browser APIs, or client Context. Keep data access, secret-bearing work, and non-interactive composition in Server Components where possible. Choose the boundary from dependencies, not from whether the component sounds like a page or widget.",
    explanation:
      'A `"use client"` directive creates a client module boundary: that module and the client-side dependencies it imports become part of the client graph. A narrow interactive leaf can therefore preserve a mostly server-rendered tree and reduce shipped JavaScript.',
    details:
      'Server Components are suitable for:\n\n- Reading databases or internal services close to the data source\n- Using server-only credentials without exposing them to the bundle\n- Transforming large data into minimal UI props\n- Rendering non-interactive structure and content\n- Composing Client Components\n\nClient Components are required for:\n\n- `useState`, `useReducer`, Effects, and most browser-oriented Hooks\n- Event handlers such as `onClick`\n- DOM refs and imperative browser APIs\n- Client-side Context providers and consumers\n- Browser storage, observers, geolocation, and similar APIs\n\nKeep the boundary near the interaction:\n\n```jsx\n// Server Component\nexport default async function ProductPage({ id }) {\n  const product = await getProduct(id);\n\n  return (\n    \u003carticle>\n      \u003cProductDescription product={product} />\n      \u003cAddToCartButton productId={product.id} />\n    \u003c/article>\n  );\n}\n```\n\nOnly `AddToCartButton` needs to be client code. Marking the entire page `"use client"` can move unnecessary dependencies and data-fetching work into the browser.\n\nA Client Component cannot directly import a Server Component as ordinary client code, but a Server Component can compose server-rendered content into a Client Component through supported props such as `children`. Audit third-party packages too: a dependency using browser APIs can force the boundary upward or require a small client wrapper.',
  },
  {
    number: 71,
    question: "What data can cross the Server Component and Client Component boundary?",
    answer:
      "Values passed from a Server Component to a Client Component must be serializable by React’s Server Component transport. Supported data includes common serializable values and React-specific references, but not arbitrary executable closures, DOM nodes, database connections, request objects, or secret-bearing server resources.",
    explanation:
      "The boundary is a network serialization boundary, not an in-memory component call. React must encode the value on the server and reconstruct a supported representation for the client, so the public prop contract must be designed accordingly.",
    details:
      "Prefer a minimal view model:\n\n```jsx\n// Server Component\nconst account = await database.account.findById(accountId);\n\nreturn (\n  \u003cAccountCard\n    account={{\n      id: account.id,\n      displayName: account.displayName,\n      plan: account.plan\n    }}\n  />\n);\n```\n\nDo not pass the database model, connection, session object, or unused private fields merely because the client currently ignores them. Serialized props can be inspected by the user and may appear in framework payloads.\n\nFunctions are not generally serializable. Server Function references are a deliberate supported exception: React and the framework encode a reference that the client can invoke as a server request. Treat that function as a public endpoint and authenticate, authorize, and validate every call.\n\nReact’s transport supports more than JSON in supported configurations, but exact types and framework behavior should be checked against the current integration. Convert custom class instances into explicit data contracts instead of relying on prototypes or methods surviving the boundary.\n\nSecurity does not follow visual ownership. A value read in a Server Component is not automatically safe to send to a Client Component. Apply data minimization and authorization before constructing client props, and never include secrets, internal tokens, or unrestricted records.",
  },
  {
    number: 72,
    question: "What is the difference between a Server Component and SSR?",
    answer:
      "SSR renders React output to HTML so users can see an initial page before client JavaScript hydrates it. Server Components render into a serialized React component payload and do not ship their component code to the browser. They can be used together, but they optimize different parts of the architecture.",
    explanation:
      "SSR addresses initial HTML delivery for a React tree that may include Client Components. Server Components address where component code and data access execute, reducing client JavaScript and enabling server-only composition across initial loads and later navigations.",
    details:
      "Compare their responsibilities:\n\n- **SSR output:** HTML for the initial visual result.\n- **SSR client behavior:** Client Component JavaScript still downloads and hydrates to become interactive.\n- **Server Component output:** a serialized React tree representation that a framework merges with the client tree.\n- **Server Component JavaScript:** remains on the server and is not hydrated in the browser.\n\nA Client Component can participate in both systems:\n\n```jsx\n// Server Component\nexport default async function ProductPage({ id }) {\n  const product = await getProduct(id);\n  return \u003cBuyButton productId={product.id} />;\n}\n```\n\n`ProductPage` executes only on the server. `BuyButton` may be included in server-generated HTML for the first load and then hydrate in the browser because it needs interaction.\n\nSSR without Server Components can still produce fast initial HTML, but the component modules generally belong to the client application and their data-fetching logic needs a separate server strategy. Server Components without initial SSR could still provide a server-generated component payload, but users would not receive the same immediately rendered HTML experience.\n\nFrameworks combine Server Components, SSR, streaming, routing, caching, and code splitting. In an interview, avoid saying that Server Components “replace SSR” or that anything rendered on a server is automatically a Server Component.",
  },
  {
    number: 73,
    question: "What are Server Functions, and what security considerations apply to them?",
    answer:
      "A Server Function is a function that runs on the server but can be referenced from client-facing React code through framework integration, commonly as a form Action or mutation callback. Invocation crosses a network boundary, so it must be designed and protected like any other server endpoint.",
    explanation:
      "The framework may generate the transport automatically, but the browser and every submitted argument remain untrusted. A hidden input, disabled button, TypeScript type, or Server Component parent is not an authorization control.",
    details:
      'A protected mutation should derive identity from the verified session and authorize the specific resource:\n\n```jsx\n"use server";\n\nasync function updateProject(projectId, formData) {\n  const session = await requireSession();\n  const input = parseProjectUpdate(formData);\n\n  const project = await loadProject(projectId);\n  if (!canEditProject(session.user, project)) {\n    throw new ForbiddenError();\n  }\n\n  await saveProject(project.id, input);\n}\n```\n\nRequired controls include:\n\n- Authenticate every protected invocation.\n- Authorize the operation against the requested record, tenant, or role.\n- Validate and normalize every argument at runtime.\n- Derive user identity from the session, not a submitted `userId`.\n- Apply CSRF protection according to the framework and authentication model.\n- Add rate limits, idempotency, and audit logging where the operation requires them.\n- Return minimal safe errors and outputs without secrets or internal details.\n\nBound arguments and hidden form fields improve API ergonomics but can still be manipulated or replayed. Recheck current permissions at execution time because authorization may change after the UI was rendered.\n\nMutations must also handle concurrency. Use transactions, version checks, idempotency keys, or conflict responses when duplicate or out-of-order submissions could corrupt data. Invalidate or update affected caches only after the authoritative write succeeds.',
  },
  {
    number: 74,
    question: "How would you avoid network request waterfalls?",
    answer:
      "Start independent requests as early and in parallel as possible, colocate dependent requests with the data they require, preload predictable resources, and move request initiation to router or server boundaries when nested client rendering would delay it. First identify whether the waterfall is code, data, or both.",
    explanation:
      "If request B starts only after request A completes, total latency includes both waits. This sequencing is necessary when B needs A’s result, but accidental waterfalls commonly come from fetching only after a child mounts or loading code before discovering its data dependency.",
    details:
      "Start independent server work before awaiting it:\n\n```jsx\nasync function Dashboard() {\n  const userPromise = getUser();\n  const projectsPromise = getProjects();\n\n  const [user, projects] = await Promise.all([\n    userPromise,\n    projectsPromise\n  ]);\n\n  return \u003cDashboardView user={user} projects={projects} />;\n}\n```\n\nA real dependency remains sequential:\n\n```jsx\nconst user = await getUser();\nconst permissions = await getPermissions(user.id);\n```\n\nDo not force parallelism when the second request cannot be formed safely without the first result.\n\nCommon waterfall sources and fixes:\n\n- **Fetch-on-mount children:** use route loaders, Server Components, or a query layer that starts known requests before child Effects run.\n- **Code then data:** preload the route module and its data together instead of waiting for the lazy component to render.\n- **Many backend round trips:** aggregate on the server or provide a purpose-built endpoint while avoiding an unmaintainable “fetch everything” response.\n- **Duplicate reads:** deduplicate by a stable request or cache key.\n- **Late discovered assets:** use framework preloading or appropriate resource hints for predictable critical resources.\n\nSuspense allows independent regions to stream as they finish, but it does not automatically start requests early. The data source must initiate work before or during the correct render boundary without serial parent-child discovery.\n\nVerify with a browser network waterfall and server tracing. Parallel requests can increase backend load, so respect connection limits, rate limits, and dependencies rather than applying `Promise.all` indiscriminately.",
  },
  {
    number: 75,
    question: "How would you design cache invalidation for server data?",
    answer:
      "Design invalidation from the data’s consistency requirements. Define what a cache entry represents, how it is keyed and scoped, how long it may be stale, which mutations affect it, and whether consumers need immediate consistency or may use stale-while-revalidate behavior.",
    explanation:
      "Caching creates another copy of server state. Without an ownership and invalidation model, it can return data for the wrong user, preserve obsolete relationships, overwrite optimistic changes, or trigger repeated refetching that removes the original benefit.",
    details:
      "Start with a precise key that includes every input affecting the result:\n\n```text\nproject:{tenantId}:{projectId}\nprojects:{tenantId}:status={status}:page={page}\n```\n\nNever share an authorization-sensitive entry across users or tenants unless the cached value is intentionally public and the keying model proves that separation is unnecessary.\n\nCommon strategies include:\n\n- **Time-based freshness:** treat data as fresh for a known interval, then revalidate.\n- **Mutation-driven invalidation:** invalidate the affected detail and collection keys after a successful write.\n- **Tag or dependency invalidation:** associate related entries with a domain tag such as `project:42`.\n- **Event-driven invalidation:** consume database, queue, or webhook events when changes can originate outside the current process.\n- **Direct cache update:** merge an authoritative mutation response when the new value is complete and unambiguous.\n- **Stale while revalidate:** display cached data immediately while refreshing in the background.\n\nFor an optimistic mutation, keep enough information to reconcile success or roll back only that operation’s change. Overlapping operations need unique identities so one failed mutation does not undo a later successful one.\n\nDefine behavior for races: an older revalidation response must not replace a newer mutation result. Version numbers, timestamps from the authority, cancellation, or request identity can establish ordering.\n\nDistinguish React request memoization, framework data caches, browser HTTP caches, CDN caches, and client server-state caches. They have different lifetimes and invalidation APIs. Document ownership, observe hit rate and staleness, and prefer a simple refetch when correctness matters more than a complex manual merge.",
  },
  {
    number: 76,
    question: "How do React form Actions work?",
    answer:
      "A React DOM `\u003cform>` can receive a function in its `action` prop. React calls that Action with the submitted `FormData`, coordinates the submission as a Transition, and exposes pending state to descendant controls. Frameworks can connect the same model to Server Functions and progressive enhancement.",
    explanation:
      "The browser’s form semantics remain the foundation, while React coordinates asynchronous mutation state, optimistic UI, and returned validation results. This avoids rebuilding every submission around `preventDefault`, loading Booleans, and manually collected input values.",
    details:
      'A client Action receives `FormData` directly:\n\n```jsx\nfunction RenameProject({ projectId }) {\n  async function rename(formData) {\n    const name = formData.get("name");\n    await updateProject(projectId, { name });\n  }\n\n  return (\n    \u003cform action={rename}>\n      \u003clabel>\n        Project name\n        \u003cinput name="name" required />\n      \u003c/label>\n      \u003cSubmitButton />\n    \u003c/form>\n  );\n}\n```\n\nInputs still need names because native form submission constructs `FormData` from successful controls. Preserve labels, input types, constraints, Enter-to-submit behavior, and a real submit button rather than treating the form as a generic click container.\n\nA function Action may be asynchronous. While it runs, `useFormStatus` can expose pending submission data to descendants, `useActionState` can store the Action’s returned result, and `useOptimistic` can derive temporary UI. After a successful Action, React resets uncontrolled form fields; controlled values remain owned by their state.\n\nIn a Server Function integration, JavaScript can enhance a form submission while the framework may preserve useful behavior before hydration. Exact navigation, permalink, error, and cache-revalidation behavior belongs to the framework and deployment model.\n\nActions do not replace server guarantees. Parse untrusted `FormData`, authenticate the caller, authorize the resource, handle duplicate submissions, and return safe field or form errors. Use a normal event handler when the operation is not semantically a form submission.',
  },
  {
    number: 78,
    question:
      "What is the difference between useOptimistic and immediately updating ordinary state?",
    answer:
      "`useOptimistic` derives a temporary view from authoritative state while an Action is in progress. When the Action finishes, React renders from the current authoritative value again. Updating ordinary state changes the source of truth immediately, so the application must manually distinguish provisional and confirmed data and implement rollback.",
    explanation:
      "Optimistic UI is a projection of what the interface expects the server to confirm, not confirmed domain state. `useOptimistic` makes that temporary layer explicit and keeps the base state separate.",
    details:
      '```jsx\nfunction MessageList({ messages, sendMessage }) {\n  const [optimisticMessages, addOptimisticMessage] = useOptimistic(\n    messages,\n    (current, draft) => [\n      ...current,\n      { ...draft, pending: true }\n    ]\n  );\n\n  async function action(formData) {\n    const draft = {\n      clientId: crypto.randomUUID(),\n      text: formData.get("message")\n    };\n\n    addOptimisticMessage(draft);\n    await sendMessage(draft);\n  }\n\n  return (\n    \u003c>\n      \u003cMessages messages={optimisticMessages} />\n      \u003cform action={action}>{/* controls */}\u003c/form>\n    \u003c/>\n  );\n}\n```\n\nThe update function must be pure. Give optimistic entries stable client-generated identities so pending operations can be rendered and reconciled independently. Show provisional status visually and accessibly rather than making an unconfirmed item indistinguishable from saved data.\n\nOrdinary state remains appropriate when the update is purely local or should become authoritative immediately. `useOptimistic` is useful when an asynchronous Action has a separate authoritative result and users benefit from seeing the likely outcome early.\n\nOptimism is a product decision. Avoid it for operations whose failure would be dangerous or confusing, such as final payment confirmation, permission changes, or irreversible deletion without a recovery model.',
  },
  {
    number: 79,
    question: "How should an optimistic update be rolled back after failure?",
    answer:
      "Roll back by returning to the last confirmed server state, removing or marking only the failed optimistic operation, and showing an actionable error. Do not restore one old snapshot over the whole collection when other operations may have succeeded in the meantime.",
    explanation:
      "Optimistic state is provisional and operation-specific. Correct recovery depends on which request failed, what the server accepted, and whether later optimistic changes were based on the failed result.",
    details:
      "Track each mutation with a client operation ID and a status such as `pending`, `confirmed`, or `failed`. On success, reconcile the temporary entity with the authoritative response—often replacing a temporary ID with a server ID. On failure, remove that operation’s projection or keep it visibly failed with Retry and Discard actions.\n\nConsider three rollback strategies:\n\n- **Refetch:** invalidate and reload authoritative data. This is simple and reliable when the result is small and connectivity is available.\n- **Inverse operation:** store enough information to undo exactly the optimistic change, such as restoring a previous field value.\n- **Failed-item state:** keep the attempted item locally with an error marker so the user can edit and retry without losing input.\n\nOverlapping mutations make a whole-state snapshot unsafe. If operation A fails after operation B succeeds, restoring the snapshot from before A would erase B. Reconcile by operation identity or use a mutation queue when order is semantically important.\n\nRetries can duplicate writes after an ambiguous timeout—the server may have committed even though the client did not receive the response. Use idempotency keys for operations such as orders or payments. For irreversible or high-risk actions, prefer confirmed UI, an undo/compensating operation, or a staged workflow over aggressive optimism.\n\nAnnounce the failure, preserve user input, and explain whether the interface has reverted or retained a failed draft. Silent rollback makes the UI appear unreliable.",
  },
  {
    number: 80,
    question: "What does useFormStatus provide?",
    answer:
      "`useFormStatus` exposes the status of a parent `\u003cform>` submission. It returns `pending`, `data`, `method`, and `action` for the latest submission handled by that form.",
    explanation:
      "Descendant controls can respond to submission state without manually threading props through the form. The component calling the Hook must be rendered inside the form; calling it in the same component that renders the form does not observe that form.",
    details:
      'Put submission UI in a descendant component of the form:\n\n```jsx\nfunction SubmitButton() {\n  const { pending } = useFormStatus();\n\n  return (\n    \u003cbutton type="submit" disabled={pending}>\n      {pending ? "Saving…" : "Save"}\n    \u003c/button>\n  );\n}\n\nfunction ProfileForm({ saveProfile }) {\n  return (\n    \u003cform action={saveProfile}>\n      \u003cinput name="displayName" />\n      \u003cSubmitButton />\n    \u003c/form>\n  );\n}\n```\n\nThe Hook returns:\n\n- `pending`: whether the parent form is currently submitting.\n- `data`: the active submission’s `FormData`, or `null` when there is no submission.\n- `method`: the form method, normally `get` or `post`.\n- `action`: the submitted function Action, or `null` for unsupported action forms.\n\nIt observes only the nearest parent form. Calling `useFormStatus` in the same component that returns `\u003cform>` does not observe that form, and it does not report a child form’s status.\n\nUse `data` carefully: it can contain sensitive field values. Do not render passwords, tokens, or private submitted data in status messages or logs. `pending` can prevent accidental duplicate submission and drive progress text, but disabling every form control can make review or cancellation unnecessarily difficult.\n\n`useActionState` and `useFormStatus` solve different scopes. The former owns the returned state of one Action; the latter lets any descendant control observe its parent form’s current submission.',
  },
  {
    number: 81,
    question: "How do pending, validation, and submission errors affect accessible form design?",
    answer:
      "Preserve native form semantics, label every control, associate validation errors programmatically, keep user input after failure, expose pending state without removing context, and move focus only when it helps users recover or when submission navigates to a new context.",
    explanation:
      "Color, spinners, and disabled styling may be invisible or ambiguous to assistive-technology users. Accessible forms communicate relationships and state through labels, descriptions, focus, and restrained status announcements.",
    details:
      'For field errors, connect the message to the control:\n\n```jsx\n\u003clabel htmlFor="email">Email\u003c/label>\n\u003cinput\n  id="email"\n  name="email"\n  type="email"\n  aria-invalid={Boolean(errors.email)}\n  aria-describedby={errors.email ? "email-error" : undefined}\n/>\n{errors.email && (\n  \u003cp id="email-error">{errors.email}\u003c/p>\n)}\n```\n\nOn an invalid submission:\n\n- Preserve entered values whenever safe.\n- Show a clear error summary for long forms.\n- Move focus to the summary or first invalid field when users would otherwise need to search for the problem.\n- Keep field messages next to their controls and avoid relying on placeholders as labels.\n\nDuring submission, keep the button’s name understandable and expose progress:\n\n```jsx\n\u003cbutton disabled={pending}>\n  {pending ? "Saving profile…" : "Save profile"}\n\u003c/button>\n\u003cp role="status">\n  {pending ? "Saving profile." : statusMessage}\n\u003c/p>\n```\n\nAvoid replacing the entire form with a spinner, which removes the user’s context. Do not use `aria-live="assertive"` for routine progress, and avoid repeatedly announcing every small state change. If the operation takes long enough, explain whether it can be cancelled.\n\nDistinguish validation, authorization, network, and server errors because they require different recovery. After success, announce the result and move focus only if the page context changed—for example, to a confirmation heading after navigation. Test keyboard submission, error recovery, pending behavior, and screen-reader announcements rather than checking ARIA attributes alone.',
  },
  {
    number: 82,
    question: "What are compound components?",
    answer:
      "Compound components are a family of components that share state and behavior while letting consumers compose the visible structure. A parent usually owns the state and exposes a focused Context to related children, as with tabs, menus, accordions, or select controls.",
    explanation:
      "The pattern replaces a large configuration object or many layout props with explicit JSX composition. Consumers control arrangement, while the component family preserves one behavioral and accessibility contract.",
    details:
      '```jsx\n\u003cTabs defaultValue="details">\n  \u003cTabs.List aria-label="Product sections">\n    \u003cTabs.Trigger value="details">Details\u003c/Tabs.Trigger>\n    \u003cTabs.Trigger value="reviews">Reviews\u003c/Tabs.Trigger>\n  \u003c/Tabs.List>\n\n  \u003cTabs.Panel value="details">\n    \u003cProductDetails />\n  \u003c/Tabs.Panel>\n  \u003cTabs.Panel value="reviews">\n    \u003cReviews />\n  \u003c/Tabs.Panel>\n\u003c/Tabs>\n```\n\n`Tabs` can own the selected value or support a controlled `value` and `onValueChange` pair. Descendants read a narrow internal Context containing the current value, generated IDs, and intentional actions such as `select(value)`.\n\nA robust implementation should:\n\n- Throw a useful development error when a compound child is used outside its provider.\n- Generate stable relationships between triggers and panels.\n- Implement the expected keyboard pattern, focus movement, roles, and ARIA state.\n- Keep internal and public Context contracts separate.\n- Support controlled and uncontrolled state without switching modes unexpectedly.\n- Avoid rerendering every descendant for unrelated provider-value changes.\n\nPrefer Context over inspecting or cloning arbitrary `children`; cloning is fragile when children are wrapped, reordered, rendered through portals, or composed by another component. Context also lets related parts be nested inside layout markup.\n\nThe trade-off is hidden coupling: `Tabs.Trigger` only works meaningfully inside `Tabs`. Use compound components when that relationship is part of the public design, not for unrelated components that merely happen to share data.',
  },
  {
    number: 83,
    question: "What is the render-prop pattern?",
    answer:
      "A render prop is a function passed to a component so the component can provide state or behavior while the caller controls the rendered UI. The function may be named, such as `renderItem`, or supplied as `children`.",
    explanation:
      "The owner component controls when and with what data the function is called; the consumer controls the resulting elements. This is useful when reuse requires a specific render scope or lifecycle rather than only reusable Hook logic.",
    details:
      "```jsx\n\u003cMousePosition>\n  {({ x, y }) => \u003cCursor x={x} y={y} />}\n\u003c/MousePosition>\n```\n\nA more practical collection API might expose rendering decisions without exposing its state implementation:\n\n```jsx\n\u003cVirtualList\n  items={messages}\n  estimateSize={48}\n  renderItem={({ item, style, isVisible }) => (\n    \u003cMessageRow\n      message={item}\n      style={style}\n      highlighted={isVisible && item.id === activeId}\n    />\n  )}\n/>\n```\n\nThe virtualizer must control which items render and supply positioning information, so a render prop expresses the boundary better than a Hook alone.\n\nPrefer a custom Hook when consumers only need reusable stateful behavior and can render normally:\n\n```jsx\nconst { x, y } = usePointerPosition();\nreturn \u003cCursor x={x} y={y} />;\n```\n\nRender-prop trade-offs include nesting, a larger callback API, and function identities that may affect memoized consumers. Document whether the callback may run many times, whether it must return one element, and which provided values are stable.\n\nDo not call a render prop outside rendering or treat it like an event handler. It participates in rendering and must remain pure. Use the pattern when the component genuinely owns render timing, placement, or scope—not simply because passing a function feels flexible.",
  },
  {
    number: 84,
    question: "What are higher-order components, and when are they still useful?",
    answer:
      "A higher-order component, or HOC, is a function that accepts a component type and returns a new component type that adds data, behavior, or rendering policy. HOCs remain useful in legacy code and library integrations that must wrap components rather than run inside their implementation.",
    explanation:
      "Hooks usually provide clearer reuse inside function components, but they cannot always replace an API that decorates an unknown component supplied by a consumer. HOCs also appear in routing, state, internationalization, error-reporting, and compatibility layers.",
    details:
      '```jsx\nfunction withPermission(requiredPermission) {\n  return function wrap(Component) {\n    function WithPermission(props) {\n      const permissions = usePermissions();\n\n      if (!permissions.has(requiredPermission)) {\n        return \u003cForbidden />;\n      }\n\n      return \u003cComponent {...props} />;\n    }\n\n    WithPermission.displayName =\n      `withPermission(${Component.displayName ?? Component.name ?? "Component"})`;\n\n    return WithPermission;\n  };\n}\n```\n\nCreate the enhanced component at module scope:\n\n```jsx\nconst ProtectedSettings = withPermission("settings:write")(Settings);\n```\n\nCreating an HOC inside another component’s render produces a new component type each time and can reset the wrapped subtree.\n\nImportant API concerns include:\n\n- Forward unrelated props without silently overwriting them.\n- Namespace or document injected props to avoid collisions.\n- Decide deliberately whether and how refs reach the wrapped component.\n- Preserve required static metadata when the framework or library reads it.\n- Give wrappers useful display names for DevTools and errors.\n- Compose several HOCs carefully; wrapper stacks can obscure data ownership.\n\nPrefer a custom Hook for new logic when the component can opt into the behavior directly. Use an HOC when component-type transformation is the actual requirement, not merely because the codebase historically used decorators.',
  },
  {
    number: 85,
    question: "How would you design a reusable modal or dialog component?",
    answer:
      "Design a dialog as a coordinated accessibility and interaction primitive: controlled open state, correct modal semantics, accessible naming, initial focus, contained keyboard interaction, Escape and outside-interaction policy, background inertness, focus restoration, portal and stacking behavior, and animation-aware mounting.",
    explanation:
      "A fixed overlay may look like a modal while keyboard focus remains behind it, screen readers continue navigating background content, or closing loses the user’s position. The reusable API must own these cross-cutting behaviors consistently.",
    details:
      'A composable API can separate the trigger, surface, title, description, and actions:\n\n```jsx\n\u003cDialog open={open} onOpenChange={setOpen}>\n  \u003cDialog.Trigger>Edit profile\u003c/Dialog.Trigger>\n  \u003cDialog.Content>\n    \u003cDialog.Title>Edit profile\u003c/Dialog.Title>\n    \u003cDialog.Description>\n      Changes are visible to your team.\n    \u003c/Dialog.Description>\n    \u003cProfileForm />\n  \u003c/Dialog.Content>\n\u003c/Dialog>\n```\n\nThe primitive should provide or enforce:\n\n- `role="dialog"` and `aria-modal="true"`, or correct native `\u003cdialog>` behavior.\n- An accessible name through a title or explicit label.\n- Meaningful initial focus—not automatically the first destructive action.\n- Tab containment while modal, visible focus, and Escape behavior.\n- An explicit outside-click policy; destructive or multi-step flows may not dismiss on outside interaction.\n- Background inertness and scroll locking without breaking scrollbar layout.\n- Restoration to the trigger or a logical successor when the trigger disappeared.\n\nUse a portal to escape clipping and stacking contexts, but remember that portals do not implement accessibility or focus behavior. Coordinate z-index, nested dialogs and popovers, pointer events, and event propagation through one overlay system.\n\nKeep content mounted until its exit animation finishes, but prevent hidden exiting content from remaining interactive. Support reduced motion and avoid restoring focus before the closing transition is logically complete.\n\nPrefer a well-tested platform or headless-dialog primitive over rebuilding focus containment. Test keyboard-only use, screen-reader naming, long content, mobile viewport and virtual keyboard behavior, nested overlays, interrupted animations, and removal of the original trigger.',
  },
  {
    number: 86,
    question: "How would you design a reusable data table?",
    answer:
      "Build a data table from a generic row model and declarative column definitions, then keep sorting, filtering, selection, pagination, and virtualization as independent controlled capabilities. Preserve native table semantics for genuinely tabular data and let applications own server-driven state.",
    explanation:
      "A reusable table should not assume employee-shaped rows or embed one fetching strategy. Columns describe how to identify, render, and optionally sort values; the application decides where rows come from and which state belongs in the URL, server query, or local UI.",
    details:
      'A useful generic contract is:\n\n```ts\ntype ColumnDef\u003cT> = {\n  id: string;\n  header: ReactNode;\n  cell: (row: T) => ReactNode;\n  sortValue?: (row: T) => string | number | Date | null;\n  align?: "start" | "center" | "end";\n};\n\ntype DataTableProps\u003cT> = {\n  rows: readonly T[];\n  columns: readonly ColumnDef\u003cT>[];\n  getRowId: (row: T) => string;\n  sort?: SortState;\n  onSortChange?: (sort: SortState) => void;\n  selectedIds?: ReadonlySet\u003cstring>;\n  onSelectedIdsChange?: (ids: ReadonlySet\u003cstring>) => void;\n};\n```\n\n`getRowId` provides durable identity; never derive selection from an array index. A column separates rendered content from its sortable value so formatted dates, badges, and links do not require parsing DOM text.\n\nDecide which features are client- or server-owned. A small in-memory table can sort and paginate locally. A large dataset should emit controlled query state so the server can filter, order, and paginate authoritative data. Clearly communicate whether selection applies to visible rows, the current page, or all matching records.\n\nAccessibility and semantics include:\n\n- A `\u003ccaption>` or another programmatic table name.\n- Correct `\u003cth scope="col">` and row-header relationships.\n- Real buttons for sortable headers with current sort direction exposed through `aria-sort`.\n- Keyboard-operable selection controls with specific accessible names.\n- Loading, empty, error, and stale states that preserve column context.\n\nDo not add grid-style arrow-key navigation to an ordinary reading table. Use the more complex ARIA grid interaction model only when cells are truly interactive like a spreadsheet.\n\nFor large collections, consider pagination or virtualization, but test focus retention, sticky headers, responsive overflow, printing, and screen-reader behavior. Keep feature logic modular; one universal table with dozens of interacting Boolean props becomes harder to understand than composable state Hooks and a semantic rendering core.',
  },
  {
    number: 87,
    question: "How do you balance abstraction with duplication?",
    answer:
      "Accept small duplication until repeated cases reveal a stable concept and change pattern. Abstract when one shared rule, behavior, or policy should change consistently across consumers—not merely because two code blocks currently look similar.",
    explanation:
      "Duplication costs repeated maintenance, but a wrong abstraction couples unrelated requirements and grows flags, branches, and escape hatches. The goal is minimizing the cost of future change, not minimizing the number of repeated lines.",
    details:
      "Evaluate a possible abstraction with these questions:\n\n- Do the cases share the same domain rule or only similar markup?\n- Do they change together for the same reasons?\n- Can the shared contract be named clearly?\n- Are variations intentional extension points or evidence that the cases differ?\n- Will consumers understand the abstraction without reading its implementation?\n\nThree similar implementations often provide enough evidence, but “rule of three” is a heuristic rather than a quota. A security rule, analytics contract, or accessible interaction pattern may deserve centralization immediately because inconsistency is costly. Two small presentational fragments may remain duplicated indefinitely if they evolve independently.\n\nWarning signs of premature abstraction include:\n\n- Many Boolean props describing unrelated modes\n- Callbacks that expose internal implementation steps\n- Consumers overriding most defaults\n- Conditional branches named after individual product screens\n- A shared component changed repeatedly for only one caller\n\nPrefer the smallest stable layer: a pure utility, focused Hook, primitive component, or shared policy may remove the important knowledge duplication without forcing entire features into one universal abstraction.\n\nKeep escape hatches deliberate and observable. If every consumer needs arbitrary internal overrides, the abstraction may be at the wrong level. It is acceptable to inline or split a failed abstraction when requirements diverge; maintaining it is not automatically better than restoring some duplication.",
  },
  {
    number: 88,
    question: "How do you prevent a design-system component API from becoming overly complex?",
    answer:
      "Keep design-system APIs small by separating stable primitives from product composition, representing supported variation with constrained variants, exposing a few deliberate slots, forwarding appropriate native attributes, and making accessibility and design tokens defaults rather than optional add-ons.",
    explanation:
      "Every public prop creates a long-term compatibility and testing obligation. One-off Boolean additions quickly allow contradictory combinations and move product-specific policy into a shared primitive.",
    details:
      'Prefer a constrained model:\n\n```tsx\n\u003cButton\n  variant="danger"\n  size="compact"\n  loading={isDeleting}\n>\n  Delete project\n\u003c/Button>\n```\n\nover unrelated switches:\n\n```tsx\n\u003cButton red small bold rounded noShadow withSpinner />\n```\n\n`variant` encodes a supported semantic decision; the Boolean collection exposes implementation details and permits combinations the design system may never have tested.\n\nAPI guidelines include:\n\n- Start from a native element and forward compatible attributes and refs.\n- Use semantic tokens instead of accepting arbitrary internal colors and spacing.\n- Define controlled and uncontrolled behavior consistently across components.\n- Keep state props distinct from visual variants.\n- Expose composition slots for content structure instead of a prop for every possible child.\n- Avoid polymorphic `as` APIs unless semantics, typing, and ref behavior remain reliable.\n- Reject or warn about invalid combinations during development when practical.\n\nAccessibility invariants belong inside the primitive: disabled and pending semantics, keyboard behavior, focus visibility, accessible naming requirements, and reduced-motion behavior should not depend on every product team remembering them.\n\nEstablish an API review process. Require a repeated use case, document ownership, test the supported variant matrix, publish migration guidance, and deprecate before removing. Use product-level wrapper components for local policy instead of adding each screen’s exception to the global primitive.\n\nAn escape hatch such as `className` can be reasonable, but it should not make internal DOM structure a permanent public contract. Track repeated overrides; they are evidence that a missing token, variant, slot, or separate component may be needed.',
  },
  {
    number: 89,
    question: "What belongs in a component, custom Hook, utility, service, or store?",
    answer:
      "Put code where its lifecycle and dependencies naturally belong: components describe UI, custom Hooks compose React state and synchronization, utilities perform pure framework-independent work, services implement external-system operations, and stores own shared mutable state with a subscription model.",
    explanation:
      "File length is not an architectural boundary. Separating code by responsibility keeps domain logic testable, React lifecycles explicit, external effects replaceable, and shared state ownership understandable.",
    details:
      "**Component**\n\nUse a component when the responsibility is rendering structure and connecting user interaction to application behavior. Components may orchestrate Hooks, but dense domain transformations and transport details should not be buried in JSX.\n\n**Custom Hook**\n\nUse a Hook when logic composes React Hooks or exposes a React lifecycle:\n\n```jsx\nfunction useOnlineStatus() {\n  return useSyncExternalStore(\n    subscribeToOnlineStatus,\n    getOnlineSnapshot,\n    getServerOnlineSnapshot\n  );\n}\n```\n\nA function does not become a Hook merely because its name starts with `use`. If it needs no React state, Context, Effect, ref, or subscription lifecycle, it may be a utility instead.\n\n**Utility or domain function**\n\nKeep parsing, validation, sorting, calculations, and state transitions pure when possible. Pure functions can run on the server, client, worker, or test runner without a React environment.\n\n**Service**\n\nA service integrates an external boundary such as HTTP, storage, analytics, or a payment SDK. It should expose domain-oriented operations, accept cancellation and dependencies where relevant, normalize transport errors, and avoid importing component state.\n\n**Store**\n\nA store owns shared mutable state outside an individual component and provides subscriptions. Use it when distant consumers need coordinated updates, updates originate outside React, or selector-based subscriptions are important. Do not create a global store merely to avoid passing props.\n\nA typical flow is:\n\n```text\nComponent → custom Hook → service → external system\n                ↓\n           utility/domain logic\n```\n\nThese are responsibilities, not mandatory layers. A small component can call a service through a focused Hook without five wrapper files. Split code when doing so clarifies ownership, enables reuse at the correct level, or isolates effects—not to satisfy an arbitrary folder convention.",
  },
  {
    number: 90,
    question: "What should be mocked in a React test?",
    answer:
      "Mock at external or nondeterministic boundaries that the test does not intend to exercise—such as network transport, time, randomness, browser APIs, analytics, or a payment SDK. Keep your own components, Hooks, state transitions, and meaningful provider integration real whenever practical.",
    explanation:
      "A mock replaces production behavior with a second implementation maintained by the test. Mocking internal collaborators can make the test pass even when those collaborators no longer integrate correctly, while boundary mocks give deterministic control without hiding application behavior.",
    details:
      'Prefer intercepting the network boundary over mocking the data Hook:\n\n```jsx\nserver.use(\n  http.get("/api/profile", () =>\n    HttpResponse.json({ name: "Ada" })\n  )\n);\n\nrender(\u003cProfile />);\nexpect(await screen.findByText("Ada")).toBeVisible();\n```\n\nThis keeps the component, query layer, loading state, response parsing, and rendering connected while making the server response controllable.\n\nGood mock candidates include:\n\n- HTTP responses and transport failures\n- Current time, timers, and randomness when determinism matters\n- Unsupported environment APIs such as `ResizeObserver`\n- Irreversible third-party side effects such as analytics or payment submission\n- Slow infrastructure outside the scope of a component test\n\nAvoid mocking:\n\n- `useState`, `useContext`, or React itself\n- Your custom Hook merely to force a component branch\n- Child components whose real interaction is central to the behavior\n- Internal functions only so the test can assert that they were called\n\nUse the narrowest faithful substitute and make it fail realistically: status codes, latency control, malformed payloads, cancellation, and retry may matter. Reset mocks between tests and avoid shared mutable fixtures. Keep some integration and end-to-end coverage with real boundaries so contract drift is detected.',
  },
  {
    number: 91,
    question: "Why can testing implementation details make tests fragile?",
    answer:
      "Tests become fragile when they assert how a component works—private state, Hook calls, helper invocations, CSS structure, or child composition—instead of what users and external systems can observe. A harmless refactor then breaks tests even though the behavior is unchanged.",
    explanation:
      "Public behavior is the stable contract. Testing through roles, labels, user interactions, visible outcomes, navigation, or emitted requests allows the implementation to evolve while still detecting regressions users would notice.",
    details:
      'Fragile test:\n\n```jsx\nexpect(wrapper.find(UserRow)).toHaveLength(3);\nexpect(setSelectedId).toHaveBeenCalledWith("42");\n```\n\nThis assumes a component boundary and state setter. The same feature may later use virtualization, a reducer, or different composition.\n\nBehavior-oriented test:\n\n```jsx\nrender(\u003cUserPicker users={users} />);\n\nawait user.click(\n  screen.getByRole("option", { name: "Ada Lovelace" })\n);\n\nexpect(\n  screen.getByRole("status")\n).toHaveTextContent("Ada Lovelace selected");\n```\n\nPrefer queries in the order users perceive the interface: role and accessible name, label text, visible text, then a test ID only when no meaningful semantic query exists. A test ID is not inherently wrong, but it should represent a stable product contract rather than a DOM path.\n\nImplementation assertions are appropriate when the implementation itself is the contract—for example, a memoization utility’s cache semantics, an analytics integration event, or a low-level library primitive. Even then, test through the narrowest public API.\n\nDo not equate “behavior test” with an enormous end-to-end test. A focused component test can still render realistic providers, perform one user workflow, and assert one observable result with a clear failure reason.',
  },
  {
    number: 92,
    question: "How do you test custom Hooks?",
    answer:
      "Test a custom Hook through the smallest realistic React consumer when its value is primarily visible through component behavior. Use a Hook test harness when the Hook itself exposes a reusable public API whose rerenders, returned callbacks, errors, or cleanup require focused verification.",
    explanation:
      "Hooks depend on React’s render and commit lifecycle. Calling the Hook as an ordinary function cannot reproduce state retention, Effect cleanup, Context, Strict Mode checks, or rerender behavior.",
    details:
      'For a Hook that drives UI, a consumer is often clearest:\n\n```jsx\nfunction Status() {\n  const online = useOnlineStatus();\n  return \u003cp>{online ? "Online" : "Offline"}\u003c/p>;\n}\n\nrender(\u003cStatus />);\nexpect(screen.getByText("Online")).toBeVisible();\n```\n\nFor a reusable Hook API, a harness can inspect returned behavior:\n\n```jsx\nconst { result, rerender, unmount } = renderHook(\n  ({ delay }) => useDebouncedValue("query", delay),\n  { initialProps: { delay: 300 } }\n);\n```\n\nCover the public contract:\n\n- Initial returned value\n- Behavior after callbacks or external notifications\n- New props supplied through rerender\n- Asynchronous transitions with controlled time or Promises\n- Subscription and timer cleanup on dependency change and unmount\n- Error and cancellation behavior\n- Required providers through a wrapper\n\nAvoid asserting how many times an internal Effect ran or which private state variables exist unless lifecycle count is the documented contract. In development Strict Mode, setup and cleanup may be stress-tested more than once.\n\nIf a Hook mostly coordinates a service and returns data to a component, one integration-oriented component test may provide more confidence than separately mocking the service, Hook, and consumer in three isolated tests.',
  },
  {
    number: 93,
    question: "How do you test components that use Context?",
    answer:
      "Render the component under the real Context provider or a faithful test provider and verify behavior through the consumer’s public UI. Supply the smallest realistic value needed by each scenario, and test provider updates when reactivity is part of the contract.",
    explanation:
      "Mocking `useContext` bypasses provider lookup, default behavior, value shape, and rerender propagation. A provider wrapper preserves the integration React actually runs while keeping the test setup concise.",
    details:
      '```jsx\nfunction renderWithPermissions(\n  ui,\n  { permissions = new Set(), ...options } = {}\n) {\n  return render(\n    \u003cPermissionsContext value={permissions}>\n      {ui}\n    \u003c/PermissionsContext>,\n    options\n  );\n}\n\nrenderWithPermissions(\u003cProjectActions />, {\n  permissions: new Set(["project:delete"])\n});\n\nexpect(\n  screen.getByRole("button", { name: "Delete project" })\n).toBeEnabled();\n```\n\nTest meaningful cases: missing provider behavior, default value, permitted and forbidden branches, provider updates, and callbacks that modify provider-owned state. If the production provider contains important reducer or subscription logic, include integration coverage with that provider rather than always replacing it with a static value.\n\nA shared `renderApp` helper can install router, query, theme, locale, and authentication providers, but keep overrides explicit. An opaque helper with many hidden defaults makes tests pass under a setup that may not resemble the scenario being described.\n\nDo not put unrelated values into one test Context merely for convenience. Tests should reinforce the same focused provider boundaries expected in production.',
  },
  {
    number: 94,
    question: "How do you test loading, errors, Suspense, and asynchronous updates?",
    answer:
      "Make asynchronous work deterministic: control when the request or Promise resolves, assert the initial loading or retained-content state, resolve or reject it deliberately, and await the final observable result. Test Suspense and Error Boundaries as they are composed in production.",
    explanation:
      "Fixed delays make tests slow and timing-dependent. Controlling the boundary lets the test prove each state transition and fail because behavior is wrong rather than because a machine was temporarily slower.",
    details:
      'A reliable happy-path test follows the user-visible sequence:\n\n```jsx\nrender(\u003cProfilePage />);\n\nexpect(\n  screen.getByRole("status", { name: "Loading profile" })\n).toBeVisible();\n\nexpect(\n  await screen.findByRole("heading", { name: "Ada Lovelace" })\n).toBeVisible();\n```\n\nConfigure the network handler or controllable Promise before rendering. Use `findBy...` when an element should eventually appear and `waitFor` when repeatedly checking a broader observable condition. Do not use `waitFor` to conceal an interaction that was never awaited.\n\nTest separate scenarios for:\n\n- Initial loading followed by success\n- Empty successful data\n- Rejection handled by the production Error Boundary\n- Retry that replaces the failure with content\n- Background refresh that retains stale data\n- Two requests resolving out of order\n- Cancellation or unmount cleanup when it is part of the contract\n\nFor Suspense, use a stable controllable Promise or framework test integration. Assert the fallback before resolving, then resolve inside the testing environment’s supported async flow and await the content. Reject in a separate test and verify the nearest Error Boundary, error reporting, and reset behavior.\n\nFake timers are useful for debounce, retry delay, or timeout behavior, but advance them deliberately and restore real timers after the test. Timers do not automatically flush Promises, so await both the user-visible interaction and resulting UI.\n\nVerify accessibility as part of async behavior: loading indicators need names, important completion may need a restrained live-region announcement, and a background Transition should not unnecessarily remove useful content or focus.',
  },
  {
    number: 95,
    question: "What causes React test warnings about updates not being wrapped in act?",
    answer:
      "An `act` warning means React observed an update that the test did not include in a completed interaction or async boundary before making assertions or finishing. It usually signals a missing `await`, an unresolved timer or Promise, an external subscription update, or unhandled cleanup—not a need to wrap the entire test in `act`.",
    explanation:
      "`act` tells React to flush updates and Effects associated with a test operation so assertions observe a settled state. Modern rendering and user-event utilities wrap their own operations, but the test must still await those operations and any observable outcome they trigger.",
    details:
      'Start with the first asynchronous action:\n\n```jsx\n// 🚩 The click starts updates, but the test does not await them.\nuser.click(screen.getByRole("button", { name: "Save" }));\n\n// ✅ Await the interaction and the resulting UI.\nawait user.click(screen.getByRole("button", { name: "Save" }));\nexpect(await screen.findByText("Saved")).toBeVisible();\n```\n\nCommon causes include:\n\n- Missing `await` on `user.click`, `user.type`, or another async helper\n- A request finishing after the test ends\n- Fake timers advanced outside the supported `act` flow\n- A subscription emitting after render without a controlled test API\n- An Effect cleanup or state update occurring during unmount\n- Asserting only the initial state while a known update remains pending\n\nPrefer waiting for the visible consequence rather than adding `await act(async () => {})` without a triggering operation. Manual `act` is appropriate when the test directly controls a source outside React testing utilities, such as invoking a custom store emitter or advancing a timer.\n\nDo not silence console warnings globally. Treat the warning as evidence that the test has not modeled the complete lifecycle, locate the originating update, and make its completion or cleanup explicit.',
  },
  {
    number: 96,
    question: "When are snapshot tests useful, and when are they harmful?",
    answer:
      "Snapshot tests are useful for small, stable, intentionally reviewable serialized contracts. They become harmful when a large component tree, volatile markup, generated IDs, or broad page output creates noisy diffs that reviewers update mechanically instead of understanding.",
    explanation:
      "A snapshot records everything without explaining which details matter. Focused assertions name the expected behavior and produce failures that point directly to the broken contract.",
    details:
      'Reasonable snapshot targets include:\n\n- A small compiler or formatter output\n- A stable accessibility-tree fragment\n- A compact serialized state migration\n- A design token or generated configuration contract\n\nWeak targets include an entire route, a deeply nested component tree, timestamps, random IDs, CSS implementation structure, and output that changes for unrelated reasons.\n\nPrefer an explicit assertion when the contract is specific:\n\n```jsx\nexpect(\n  screen.getByRole("button", { name: "Save" })\n).toBeDisabled();\n```\n\nThis communicates more intent than a page snapshot containing a disabled attribute somewhere among hundreds of lines.\n\nKeep snapshots close to the behavior they document, normalize only truly irrelevant nondeterminism, and review every diff as production code. Never update snapshots merely to make CI green.\n\nDOM snapshots do not prove keyboard behavior, focus order, screen-reader announcements, responsive layout, or visual appearance. Use interaction assertions, accessibility tooling, and visual-regression tests for those distinct risks.',
  },
  {
    number: 97,
    question: "How would you test keyboard and focus behavior?",
    answer:
      "Exercise the component with realistic keyboard input and assert focus after every meaningful step, along with the resulting selection, expansion, dismissal, or activation state. Test the keyboard pattern appropriate to the widget rather than assuming every interactive element uses Tab for internal navigation.",
    explanation:
      "Keyboard accessibility depends on both event handling and focus management. A control can respond to a synthetic key event while still being unreachable in the real tab order, trapping focus, or failing to expose state semantically.",
    details:
      'Test through user-level keyboard APIs:\n\n```jsx\nrender(\u003cDialogExample />);\n\nconst trigger = screen.getByRole("button", {\n  name: "Edit profile"\n});\n\ntrigger.focus();\nawait user.keyboard("{Enter}");\n\nconst dialog = screen.getByRole("dialog", {\n  name: "Edit profile"\n});\nexpect(dialog).toBeVisible();\nexpect(screen.getByLabelText("Display name")).toHaveFocus();\n\nawait user.keyboard("{Escape}");\nexpect(trigger).toHaveFocus();\n```\n\nCover the relevant interaction contract:\n\n- Natural Tab and Shift+Tab order\n- Enter and Space activation differences for native controls\n- Escape dismissal and focus restoration\n- Arrow, Home, and End navigation for tabs, menus, listboxes, or grids\n- Roving `tabIndex` or `aria-activedescendant` behavior\n- Disabled-item and typeahead behavior where the widget pattern requires it\n- Focus after insertion, deletion, validation failure, and route changes\n\nAvoid calling `.focus()` for every step when the purpose is to test reachability; use Tab navigation to prove the element can actually be reached. Direct focus is appropriate for setting the initial precondition.\n\nDOM test environments do not reproduce every browser focus behavior, layout dependency, Shadow DOM interaction, or screen-reader announcement. Keep browser-level coverage for critical dialogs, menus, command palettes, and other complex widgets, and include representative manual assistive-technology testing.',
  },
  {
    number: 98,
    question: "What are the risks of dangerouslySetInnerHTML?",
    answer:
      "`dangerouslySetInnerHTML` asks React to insert an HTML string directly into a DOM element. It bypasses React’s normal text escaping, so untrusted or incorrectly sanitized content can execute script-capable markup, create unsafe links, alter page structure, or enable cross-site scripting.",
    explanation:
      "Rendering `{userInput}` creates text, so characters such as `\u003c` do not become markup. Passing the same value through `dangerouslySetInnerHTML` changes the value’s interpretation from text to HTML and transfers responsibility for its safety to the application.",
    details:
      "Safe ordinary rendering:\n\n```jsx\n\u003cp>{comment.body}\u003c/p>\n```\n\nRaw HTML sink:\n\n```jsx\n\u003carticle\n  dangerouslySetInnerHTML={{ __html: comment.html }}\n/>\n```\n\nThe explicit name is a warning, not protection. Dangerous content is broader than a `\u003cscript>` tag. Attackers may use event-handler attributes, unsafe URL schemes, SVG or MathML features, CSS-capable constructs, malformed markup, or browser parsing behavior.\n\nOther risks include:\n\n- Replacing React-managed descendants of that element\n- Hydration differences when server and client sanitization disagree\n- Unsafe links or embedded resources even when script execution is removed\n- Future policy regressions when new tags or attributes are allowed\n\nPrefer structured data rendered through JSX, plain text, or a constrained format whose nodes map to approved React components. If arbitrary rich HTML is a genuine requirement, sanitize it with a maintained parser-based policy before it reaches this sink.\n\nContent Security Policy and Trusted Types can reduce exploitability and enforce safer sinks, but they are defense in depth. They do not make untrusted HTML intrinsically safe or replace sanitization and output minimization.",
  },
  {
    number: 99,
    question: "How should untrusted HTML be rendered safely?",
    answer:
      "Treat HTML as untrusted until a maintained, parser-based sanitizer transforms it according to a narrow allowlist. Sanitize at a trusted boundary, render only the sanitized result, and avoid accepting arbitrary HTML when plain text or a structured rich-text model can meet the requirement.",
    explanation:
      "Validation answers whether input matches an expected data shape; sanitization removes or rewrites dangerous markup for a specific output context. Escaping would display HTML as text, while sanitization deliberately retains an approved subset as markup.",
    details:
      "Define a policy from product requirements:\n\n- Allow only needed elements such as paragraphs, lists, emphasis, and links.\n- Allow only needed attributes.\n- Restrict URL schemes and normalize links.\n- Decide whether images, embedded media, inline style, SVG, and MathML are necessary.\n- Add safe link attributes where external navigation requires them.\n\n```jsx\nconst safeHtml = sanitizer.sanitize(untrustedHtml, policy);\n\nreturn (\n  \u003carticle\n    dangerouslySetInnerHTML={{ __html: safeHtml }}\n  />\n);\n```\n\nSanitize as close as practical to the trust boundary and avoid mutating the markup afterward. A transformation performed after sanitization can reintroduce unsafe attributes or URLs. If both server and client render the content, use compatible policies and versions to avoid hydration inconsistencies.\n\nDo not write an HTML sanitizer with regular expressions. HTML parsing, namespaces, entity decoding, and browser correction rules are too complex. Use a security-maintained library appropriate to the runtime, keep it updated, and test known malicious payload categories as well as the allowed formatting contract.\n\nConsider representing rich text as validated structured nodes:\n\n```jsx\nconst renderers = {\n  paragraph: node => \u003cp>{render(node.children)}\u003c/p>,\n  strong: node => \u003cstrong>{render(node.children)}\u003c/strong>\n};\n```\n\nThis makes supported content explicit and avoids exposing a general HTML sink. Still validate URLs and every node type because structured input can contain unsafe values too.\n\nCSP, Trusted Types, sandboxed iframes for isolated third-party documents, and server-side storage policy are additional layers. Record where sanitized content originated and avoid assuming that “stored” content is trusted merely because it came from your database.",
  },
  {
    number: 100,
    question: "How should focus be managed when opening and closing a modal?",
    answer:
      "When a modal opens, save the invoking context and move focus to a deliberate element inside it. While open, keep focus and interaction within the modal and make the background inert. When it closes, restore focus to the trigger or another logical location if that trigger no longer exists.",
    explanation:
      "Visual position does not control keyboard or assistive-technology focus. Without explicit management, users may continue interacting with obscured content, lose their place after closing, or land on a destructive action unexpectedly.",
    details:
      '**On open**\n\nStore the element that triggered the modal before moving focus. Choose initial focus according to the task:\n\n- A heading or container for long informational content\n- The first invalid field for a correction flow\n- The primary field for a short input dialog\n- The least destructive action for a destructive confirmation\n\nThe modal needs an accessible name through a visible title or explicit label. Initial focus should be visible and should not unexpectedly scroll important context out of view.\n\n**While open**\n\nA modal interaction model should:\n\n- Keep Tab and Shift+Tab within the dialog.\n- Prevent pointer and keyboard interaction with the background.\n- Support Escape unless the product has a strong documented reason not to.\n- Define whether outside interaction dismisses the modal.\n- Preserve focus through validation, loading, and content updates.\n- Handle nested popovers or dialogs through one coordinated overlay system.\n\nUse native `\u003cdialog>` behavior or a well-tested accessible primitive when possible. A portal, `role="dialog"`, and `aria-modal="true"` provide structure and semantics, but do not alone implement focus containment or restoration.\n\n**On close**\n\nRestore focus to the original trigger when it still exists and remains meaningful. If deleting an item also removes its trigger, move focus to the next item, collection heading, or another predictable successor. Do not send focus to `\u003cbody>` and force keyboard users to restart navigation.\n\nCoordinate focus with exit animations: prevent the closing surface from remaining interactive, but restore focus at a point that does not create visible focus jumps. Test keyboard opening, Tab wrapping, Escape, validation failure, nested overlays, trigger removal, reduced motion, and browser back navigation.',
  },
  {
    number: 101,
    question: "What is the difference between hiding and unmounting content?",
    answer:
      "Unmounting removes a subtree from React and the DOM, runs Effect cleanup, clears refs, and discards its local state. Hiding usually keeps the subtree mounted, but the exact behavior depends on whether it is hidden with CSS, `hidden`, `inert`, or React’s `\u003cActivity>`—these choices differ in accessibility, interaction, and Effect lifecycle.",
    explanation:
      "“Not visible” is not one lifecycle state. A visually hidden element may still consume memory, run subscriptions, receive programmatic focus, or remain exposed to assistive technology. Choose a mechanism from the required state and behavior, not appearance alone.",
    details:
      '| Technique | React state | DOM | Effects | Interaction and accessibility |\n|---|---|---|---|---|\n| Conditional unmount | Reset when mounted again | Removed | Cleaned up | Removed |\n| `display: none` or `hidden` | Preserved | Retained | Continue running | Normally not rendered or exposed |\n| `visibility: hidden` | Preserved | Retains layout space | Continue running | Not interactive; accessibility behavior should still be verified |\n| `inert` | Preserved | Retained | Continue running | Removed from normal interaction and focus; may remain visually present |\n| `\u003cActivity mode="hidden">` | Preserved | React hides the subtree | Effects are cleaned up while hidden | Intended for retained, inactive UI |\n\nUse unmounting when returning should start a fresh workflow or retaining the subtree wastes resources:\n\n```jsx\n{isOpen && \u003cCheckoutForm />}\n```\n\nUse CSS hiding when the content should remain fully active and immediate reappearance matters, such as a short purely visual transition. Ensure hidden controls cannot remain in the tab order or be announced unexpectedly.\n\nUse `inert` for content that may remain visible but must not be interactive, commonly the background behind a modal. It does not stop Effects, network work, timers, or store subscriptions.\n\nUse `\u003cActivity>` when preserving state is valuable but hidden Effects should be torn down. On visibility restoration, React recreates those Effects. This can support inactive routes or prepared content, but retained DOM and state still consume resources.\n\nAlso consider focus. Before hiding or unmounting the currently focused element, move focus to a logical visible target. Animation libraries must keep exiting content long enough to animate without leaving it keyboard-accessible after the interaction has logically closed.',
  },
  {
    number: 102,
    question: "How should asynchronous status updates be announced to assistive technologies?",
    answer:
      "When important content changes without moving focus, update visible text inside an appropriate live region so assistive technologies can announce it. Use polite status messaging for routine progress and reserve assertive alerts for urgent information that requires immediate attention.",
    explanation:
      "Sighted users may notice a spinner, toast, or changed color elsewhere on the page, while a screen-reader user’s virtual cursor remains unchanged. A live region creates a non-focus-based announcement channel, but excessive announcements become distracting and can interrupt more useful speech.",
    details:
      'Keep the live-region container mounted before the asynchronous update and change its text:\n\n```jsx\n\u003cp role="status" aria-live="polite">\n  {statusMessage}\n\u003c/p>\n```\n\n`role="status"` provides a polite live region suitable for messages such as “Profile saved” or “12 results loaded.” Many screen readers do not announce initial content inserted together with a newly mounted live-region node, so mounting an empty persistent region and updating it later is more reliable.\n\nUse `role="alert"` for urgent errors or conditions that need immediate attention. It is effectively assertive and can interrupt current speech, so it should not announce routine loading, every validation keystroke, or background refresh.\n\nWrite useful messages:\n\n- “Saving profile” when a noticeable operation starts\n- “Profile saved” when it completes\n- “Could not save profile. Check your connection and try again” on failure\n\nAvoid messages such as “Done,” repeated percentage changes, or duplicated text that focus already communicates. Debounce rapidly changing search-result counts so announcements do not queue faster than they can be spoken.\n\n`aria-live` controls urgency, while `aria-atomic` determines whether the whole region or only the changed part is presented. Prefer one concise text update when possible rather than assembling a sentence from several independently changing nodes.\n\nMoving focus is better than a live region when the user must interact with the new context—for example, focusing an error summary after a failed long-form submission. Use live regions when focus should remain where the user is working. Test with representative screen readers because announcement timing varies across browser and assistive-technology combinations.',
  },
  {
    number: 103,
    question: "How do you make custom interactive components keyboard accessible?",
    answer:
      "Start with the closest native HTML element. If the design genuinely requires a custom widget, implement the complete established interaction pattern: semantic roles and relationships, keyboard commands, focus management, state exposure, disabled behavior, and visible focus—not only click handling.",
    explanation:
      'ARIA changes what assistive technology is told; it does not add browser behavior. A `\u003cdiv role="button">` does not automatically become focusable, activate with Enter and Space, submit forms correctly, or receive native disabled semantics.',
    details:
      'Prefer:\n\n```jsx\n\u003cbutton type="button" onClick={save} disabled={pending}>\n  Save\n\u003c/button>\n```\n\nover recreating button behavior on a generic element. Native controls provide semantics, focusability, keyboard activation, high-contrast behavior, form integration, and platform accessibility APIs.\n\nFor a composite widget, select and follow one established pattern. Tabs typically use:\n\n- One Tab stop within the tab list\n- Left and Right Arrow to move between tabs\n- Home and End for first and last tab\n- `role="tablist"`, `role="tab"`, and `role="tabpanel"`\n- `aria-selected` and ID relationships between each tab and panel\n- Either automatic or manual activation, documented consistently\n\nOther widgets have different contracts. A menu is for application commands, not ordinary site navigation; a listbox is not a collection of arbitrary interactive cards. Choosing the wrong role creates expectations the implementation cannot meet.\n\nComposite focus is commonly implemented with:\n\n- **Roving `tabIndex`:** one item has `tabIndex={0}` and receives DOM focus; other items use `-1`.\n- **`aria-activedescendant`:** focus stays on a container while an ID identifies the active option.\n\nThe choice affects scrolling, browser focus styling, virtualization, and assistive-technology support. Keep visible focus distinct from selection; moving focus does not always mean committing a value.\n\nHandle disabled items, dynamic insertion and removal, typeahead, orientation, RTL arrow behavior, focus restoration, and Escape according to the chosen pattern. Avoid positive `tabIndex`, global key handlers, and preventing default browser behavior for keys the widget does not own.\n\nValidate with semantic queries and automated accessibility checks, then test real Tab order, keyboard commands, zoom, high contrast, and representative screen readers in browsers. Automation can confirm attributes and focus movement but cannot prove that the experience is understandable.',
  },
  {
    number: 27,
    question: "How would you design reusable React component APIs?",
    answer:
      "Design the smallest API that expresses a stable responsibility. Prefer semantic defaults, composition for structure, constrained variants for supported appearance, predictable controlled and uncontrolled state, and a few deliberate extension points. Make common behavior easy and invalid or inaccessible combinations difficult to represent.",
    explanation:
      "A reusable API is a long-lived contract, not merely extracted JSX. It must hide implementation details while preserving enough control for real use cases, and every public prop creates compatibility, documentation, accessibility, and testing obligations.",
    details:
      'Start by defining ownership and invariants:\n\n- What behavior does the component guarantee?\n- Which state does it own, and which state may the consumer control?\n- Which native semantics and keyboard behavior must always be preserved?\n- Which variations are supported product decisions rather than arbitrary styling?\n- What must remain private so the implementation can evolve?\n\nUse an explicit controlled contract when the application needs coordination:\n\n```jsx\n\u003cDialog\n  open={isOpen}\n  onOpenChange={setIsOpen}\n>\n  \u003cDialog.Content>{children}\u003c/Dialog.Content>\n\u003c/Dialog>\n```\n\nOffer `defaultOpen` only when an uncontrolled mode is genuinely useful. Do not switch between controlled and uncontrolled ownership during the component’s lifetime, and keep event naming consistent across the design system.\n\nPrefer composition over configuration for structure. Compound components or slots let consumers arrange meaningful parts without exposing internal DOM nodes. Use a render prop only when the component must control render timing or provide rendering-specific state; use a custom Hook when consumers only need reusable React behavior.\n\nAvoid Boolean-prop accumulation:\n\n```jsx\n\u003cDialog\n  compact\n  warning\n  centered\n  noPadding\n  rounded\n/>\n```\n\nThese switches permit contradictory combinations and expose styling mechanics. A constrained model is clearer:\n\n```jsx\n\u003cDialog\n  variant="warning"\n  size="compact"\n/>\n```\n\nStrong APIs also:\n\n- Forward appropriate native attributes and refs.\n- Use durable data identity rather than array position.\n- Expose behavioral callbacks, not private state setters or lifecycle steps.\n- Keep Context focused and validate compound-child provider usage.\n- Offer a narrow imperative handle only for commands such as `focus()`.\n- Preserve accessible naming, focus behavior, reduced motion, and disabled semantics.\n- Define server/client compatibility when the library supports Server Components.\n\nTypeScript can constrain variants and mutually exclusive modes, but runtime semantics must still be correct. Test the public contract through consumer behavior, keyboard interaction, controlled updates, ref forwarding, and representative composition.\n\nAdd an extension point only after a repeated requirement shows where the stable boundary belongs. If consumers need to understand internal markup or override most defaults, the abstraction is probably at the wrong level.',
  },
  {
    number: 24,
    question: "How should senior engineers test React applications?",
    answer:
      "Use a risk-based test portfolio. Test pure domain logic directly, React behavior through realistic component integration, external contracts at their boundaries, and a smaller set of critical journeys in a real browser. Prefer observable user and system outcomes over component internals.",
    explanation:
      "No single test level provides enough confidence. Small tests localize failures and cover edge cases cheaply; integration tests catch wiring errors; browser tests cover routing, focus, layout-dependent behavior, and deployed boundaries. Senior engineers choose the cheapest level capable of detecting each important risk.",
    details:
      'Match tests to risk:\n\n- **Unit tests:** parsers, validators, reducers, sorting, state-machine transitions, and other pure domain rules.\n- **Component tests:** rendering from meaningful inputs, user interaction, forms, providers, loading and error states, and accessible semantics.\n- **Integration or contract tests:** network adapters, cache behavior, routing, serialization, authentication boundaries, and third-party interfaces.\n- **End-to-end tests:** a small set of revenue-, security-, or workflow-critical journeys in a real browser.\n- **Visual tests:** layout, responsive behavior, themes, and component-state appearance.\n- **Accessibility testing:** semantic automation plus real keyboard and representative assistive-technology checks.\n\nTest through the interface users perceive:\n\n```jsx\nrender(\u003cProfileForm />);\n\nawait user.type(\n  screen.getByLabelText("Display name"),\n  "Ada"\n);\nawait user.click(\n  screen.getByRole("button", { name: "Save profile" })\n);\n\nexpect(\n  await screen.findByRole("status")\n).toHaveTextContent("Profile saved");\n```\n\nInclude the failure paths that change product behavior: validation, empty data, permissions, stale responses, retry, optimistic rollback, offline or timeout handling, and interrupted navigation. Do not chase 100% line coverage while important state transitions remain untested; coverage reveals unexecuted code, not whether assertions are meaningful.\n\nMock at external boundaries and keep application collaboration real. Network interception is usually stronger than mocking a custom data Hook because it retains parsing, cache, loading, and rendering integration. Maintain some real contract or end-to-end coverage so mocks cannot drift unnoticed.\n\nKeep tests deterministic without making them unrealistic: control time, random IDs, and request completion explicitly; avoid fixed sleeps; isolate mutable data; and await user interactions and visible outcomes. Treat flaky tests as engineering defects rather than adding retries indefinitely.\n\nFinally, optimize the suite as a system. Run fast focused checks during development, parallelize reliable tests, collect failure artifacts for browser tests, and assign ownership for unstable or obsolete coverage. A useful test should fail for a comprehensible product reason and help the team diagnose it quickly.',
  },
  {
    number: 25,
    question: "How do you build accessible React components?",
    answer:
      "Build accessibility into the component contract: start with semantic HTML, preserve native keyboard behavior, provide programmatic names and relationships, manage focus when context changes, support visual accessibility preferences, and use ARIA only to express semantics native HTML cannot provide.",
    explanation:
      "Accessibility is behavior across markup, state, focus, input methods, and assistive technology—not a collection of attributes added after visual implementation. Native elements provide a tested platform baseline that custom widgets must otherwise recreate.",
    details:
      'Start with native semantics:\n\n```jsx\n\u003cbutton type="button" onClick={save}>\n  Save\n\u003c/button>\n```\n\nAvoid recreating a button with a generic element:\n\n```jsx\n\u003cdiv onClick={save}>Save\u003c/div>\n```\n\nThe `\u003cdiv>` lacks native focus, Enter and Space activation, disabled behavior, high-contrast adaptation, and button semantics. Adding `role="button"` fixes only part of that contract.\n\nDesign each component across these layers:\n\n1. **Structure:** semantic headings, landmarks, lists, tables, buttons, links, labels, and fieldsets.\n2. **Name and relationships:** accessible names, descriptions, error associations, expanded state, selection, and ownership.\n3. **Keyboard and focus:** logical Tab order, widget-specific keys, visible focus, initial focus, and restoration after overlays or deletion.\n4. **Dynamic behavior:** loading and result announcements, validation recovery, retained context, and no unexpected focus movement.\n5. **Visual access:** contrast, zoom and reflow, target size, non-color indicators, forced-colors support, and reduced motion.\n\nDynamic form errors should be connected to their inputs:\n\n```jsx\n\u003clabel htmlFor="name">Name\u003c/label>\n\u003cinput\n  id="name"\n  aria-invalid={Boolean(error)}\n  aria-describedby={error ? "name-error" : undefined}\n/>\n\n{error && (\n  \u003cp id="name-error">\n    {error}\n  \u003c/p>\n)}\n```\n\nFor custom composite widgets such as tabs or listboxes, follow the established ARIA Authoring Practices interaction pattern. Decide whether focus or selection moves, implement roving `tabIndex` or `aria-activedescendant` correctly, and avoid inventing keyboard commands.\n\nMake accessible behavior part of reusable primitives so product teams receive correct defaults. A dialog primitive should own naming, focus containment, background inertness, Escape behavior, and focus restoration; consumers should not rebuild those rules for every modal.\n\nTest in layers:\n\n- Semantic component queries and focused interaction tests\n- Automated accessibility checks\n- Keyboard-only browser testing\n- Zoom, responsive reflow, forced colors, and reduced motion\n- Representative screen-reader combinations for critical workflows\n\nAutomated tools catch only a subset of problems and cannot determine whether focus order, instructions, announcements, or interaction design make sense. Include disabled users and accessibility specialists in design and usability feedback where possible, document component guarantees, and treat regressions as product defects rather than optional polish.',
  },
  {
    number: 104,
    question: "What does JSX produce, and how is it different from HTML?",
    answer:
      "JSX is syntax that a build tool transforms into calls that create React elements. It describes UI as JavaScript values; it is not an HTML string and it is not inserted into the DOM directly.",
    explanation:
      "JSX can embed JavaScript expressions, pass non-string props, and represent custom components. React later uses the resulting element descriptions during rendering and reconciliation.",
    details:
      'Conceptually, a JSX expression such as:\n\n```jsx\n\u003cButton tone="primary">Save\u003c/Button>\n```\n\nproduces an element description containing the component type, props, and children. Modern JSX transforms usually call functions from the JSX runtime rather than `React.createElement` directly.\n\nImportant differences from HTML include:\n\n- Component names beginning with a capital letter refer to JavaScript values.\n- Props can contain functions, objects, arrays, and other JavaScript values.\n- Most DOM property names follow JavaScript conventions such as `className`.\n- Expressions use braces; statements cannot appear directly inside JSX.\n- React escapes ordinary text values before placing them in the DOM.\n\nJSX syntax does not determine whether a component runs on the client or server. The renderer and framework establish that execution environment.',
  },
  {
    number: 105,
    question: "How does React render null, booleans, arrays, strings, and numbers?",
    answer:
      "React renders strings and numbers as text, flattens arrays and fragments into children, and renders `null`, `undefined`, and booleans as nothing.",
    explanation:
      "This explains why `condition && \u003cPanel />` normally works but `count && \u003cPanel />` can display `0` when `count` is zero.",
    details:
      "Avoid relying on the truthiness of a number when the falsy value is renderable:\n\n```jsx\n// May render 0\n{items.length && \u003cResults items={items} />}\n\n// Expresses the condition explicitly\n{items.length > 0 && \u003cResults items={items} />}\n```\n\nArrays may contain elements and other renderable values, but siblings produced from a collection need stable keys. Plain objects are not valid React children because React does not know how they should be represented visually.\n\nOrdinary string interpolation is escaped. Rendering trusted markup through `dangerouslySetInnerHTML` deliberately bypasses that protection and requires a separate sanitization strategy.",
  },
  {
    number: 106,
    question: "How does React process queued state updates?",
    answer:
      "React queues state updates and processes them for a future render. A value update replaces the queued state for that step, while an updater function receives the result of the preceding queued update.",
    explanation:
      "Updater functions are required when the next value depends on state already queued in the same batch.",
    details:
      "These calls all capture the same `count` snapshot:\n\n```jsx\nsetCount(count + 1);\nsetCount(count + 1);\nsetCount(count + 1);\n```\n\nThese updaters are evaluated in sequence:\n\n```jsx\nsetCount(value => value + 1);\nsetCount(value => value + 1);\nsetCount(value => value + 1);\n```\n\nReact may call updater functions more than once in development to verify purity, so an updater must not mutate data or perform side effects.\n\nThe queue model also explains mixed updates. A replacement can override the preceding queued result, while a later updater operates on the replacement. Prefer one clear update when possible; complicated queues are difficult to review even when their behavior is defined.",
  },
  {
    number: 107,
    question: "Why can declaring a component inside another component reset state?",
    answer:
      "Every render creates a new inner component function. React sees that new function as a different component type, so it replaces the subtree and resets its state.",
    explanation:
      "Component identity depends on the type placed at a tree position. A newly created function is not identical to the function from the previous render.",
    details:
      'Avoid this pattern:\n\n```jsx\nfunction ProfilePage() {\n  function Editor() {\n    const [name, setName] = useState("");\n    return \u003cinput value={name} onChange={event => setName(event.target.value)} />;\n  }\n\n  return \u003cEditor />;\n}\n```\n\nMove `Editor` to module scope. Pass values through props when it needs information from `ProfilePage`.\n\nDefining a small render helper that is called as an ordinary function is different, but that helper does not create an independent component boundary. Prefer explicit components when independent state, Hooks, memoization, or error boundaries are needed.',
  },
  {
    number: 108,
    question: "What does flushSync do, and when is it appropriate?",
    answer:
      "`flushSync` forces React to synchronously flush updates inside its callback so the DOM is updated before the next statement runs.",
    explanation:
      "It is an integration escape hatch for code that must coordinate immediately with browser or third-party APIs. It should not be used as a general way to make state feel synchronous.",
    details:
      "A browser API may require updated DOM before a callback returns:\n\n```jsx\nflushSync(() => {\n  setExpanded(true);\n});\n\npanelRef.current.scrollIntoView();\n```\n\nForcing synchronous work can hurt responsiveness, reveal Suspense fallbacks, and flush updates outside the immediate callback. First consider whether the imperative work belongs in a layout Effect, whether the browser API supports a later callback, or whether the state design can avoid the dependency.",
  },
  {
    number: 109,
    question: "What happens if a component updates state during rendering?",
    answer:
      "Updating the component currently rendering schedules another render immediately. React permits limited guarded render-time adjustment, but unconditional updates cause an infinite rendering loop.",
    explanation:
      "Rendering should normally calculate UI without causing updates. Most synchronization belongs in an event handler or Effect, and most derived values should be calculated directly.",
    details:
      "This is invalid because every render schedules another one:\n\n```jsx\nfunction Counter() {\n  const [count, setCount] = useState(0);\n  setCount(count + 1);\n  return count;\n}\n```\n\nUpdating a different component during render is also unsafe because it couples partially evaluated trees.\n\nA guarded adjustment of the same component can occasionally replace an Effect-based reset, but it should be rare and must converge. Changing a key or deriving the value is usually clearer.",
  },
  {
    number: 110,
    question: "What problem does useEffectEvent solve, and when should it not be used?",
    answer:
      "`useEffectEvent` extracts event-like logic from an Effect so that logic can read the latest committed props and state without making the surrounding Effect re-synchronize.",
    explanation:
      "Use it when an external system fires an event whose handler needs current values, but changes to those values should not reconnect or recreate that external system.",
    details:
      '```jsx\nconst onConnected = useEffectEvent(() => {\n  showNotification("Connected", theme);\n});\n\nuseEffect(() => {\n  const connection = connect(roomId);\n  connection.on("connected", onConnected);\n  return () => connection.disconnect();\n}, [roomId]);\n```\n\nA theme change affects the notification but should not reconnect the room.\n\nEffect Events:\n\n- Are called only from Effects or other Effect Events\n- Always observe the latest committed values\n- Are not included in dependency arrays\n- Must remain local to the component or custom Hook that owns the Effect\n\nDo not use `useEffectEvent` merely to silence the dependency linter. If a value determines what the Effect synchronizes with, that value remains a dependency.',
  },
  {
    number: 111,
    question:
      "How does \u003cActivity> differ from conditional rendering, CSS hiding, and unmounting?",
    answer:
      "`\u003cActivity>` can hide a subtree while preserving its state. In hidden mode, React hides the content, cleans up its Effects, and deprioritizes its updates.",
    explanation:
      "Conditional rendering removes the subtree and normally loses its state. CSS hiding keeps the subtree and its Effects active. Activity provides a lifecycle-aware middle ground.",
    details:
      '```jsx\n\u003cActivity mode={active ? "visible" : "hidden"}>\n  \u003cSearchPage />\n\u003c/Activity>\n```\n\nUseful cases include:\n\n- Preserving state when navigating away and back\n- Preparing likely next content in the background\n- Hiding an expensive interface without leaving subscriptions active\n\nActivity is not automatically the right choice for every conditional. Unmount content when its state should reset or when retaining it wastes resources. Use CSS when content must remain fully active while merely invisible.',
  },
  {
    number: 112,
    question: "How do callback refs and ref cleanup work?",
    answer:
      "A callback ref runs when React attaches a node and can return a cleanup function that runs when the ref is detached or the callback changes.",
    explanation:
      "Callback refs are useful when attaching imperative behavior to a changing set of nodes or when setup and cleanup should follow the exact node lifecycle.",
    details:
      "```jsx\n\u003cdiv\n  ref={node => {\n    const observer = new ResizeObserver(handleResize);\n    observer.observe(node);\n\n    return () => observer.disconnect();\n  }}\n/>\n```\n\nKeep the callback identity stable when repeated detach-and-attach work is costly. Object refs remain simpler when the code only needs to read a single DOM node.\n\nIn modern React, function components can receive `ref` as a prop in supported patterns. Expose the DOM node only when consumers need it; `useImperativeHandle` can expose a smaller API such as `focus()` or `reset()`.",
  },
  {
    number: 113,
    question: "When is useInsertionEffect appropriate?",
    answer:
      "`useInsertionEffect` is primarily for CSS-in-JS libraries that must insert styles before layout Effects read layout. Application components should almost never need it.",
    explanation:
      "Inserting styles in a passive Effect is too late for layout measurement, while inserting them during rendering violates render purity.",
    details:
      "`useInsertionEffect` runs around the commit process before layout Effects, but it has important restrictions and is not a faster substitute for `useLayoutEffect`.\n\nPrefer static CSS, extracted styles, or the styling system's documented integration. Use a layout Effect to measure or synchronously adjust committed DOM. Use a passive Effect for ordinary external synchronization that does not need to block paint.",
  },
  {
    number: 114,
    question: "What does concurrent rendering mean in React?",
    answer:
      "Concurrent rendering means React can prepare some updates interruptibly: it may pause, resume, restart, or abandon render work before committing a complete result.",
    explanation:
      "It does not mean component functions execute simultaneously on multiple threads. It means rendering work can be scheduled with different priorities while committed UI remains consistent.",
    details:
      "Consequences include:\n\n- Render logic must be pure and idempotent.\n- A render is not evidence that a commit occurred.\n- Side effects belong in event handlers or commit-phase Effects.\n- Transitions allow urgent updates to interrupt non-urgent rendering.\n- External mutable stores need a concurrency-safe subscription interface.\n\nReact still commits a coherent tree. Users should not observe a half-committed result merely because rendering was interrupted.",
  },
  {
    number: 115,
    question: "What is tearing, and how does useSyncExternalStore help prevent it?",
    answer:
      "Tearing occurs when different components render inconsistent snapshots of the same external mutable state during one logical update.",
    explanation:
      "`useSyncExternalStore` gives React a subscription and repeatable snapshot reader so React can detect changes and keep the committed UI consistent.",
    details:
      "The snapshot must be referentially stable until the store actually changes:\n\n```jsx\nconst value = useSyncExternalStore(\n  store.subscribe,\n  store.getSnapshot,\n  store.getServerSnapshot\n);\n```\n\nReturning a freshly allocated object on every `getSnapshot` call makes React believe the store continuously changed. Mutable objects that change without producing a new snapshot are equally problematic.\n\nLibraries usually wrap this API with selectors and equality comparison. Components should use the library integration instead of reading mutable store fields directly during render.",
  },
  {
    number: 116,
    question: "How do React Performance Tracks help diagnose slow interactions?",
    answer:
      "React Performance Tracks add React scheduling and component information to browser performance profiles, connecting user-visible main-thread work to the updates and components that produced it.",
    explanation:
      "The Scheduler track shows update priority and scheduling behavior, while the Components track shows rendering and Effect work.",
    details:
      "A useful investigation asks:\n\n1. Which interaction feels slow?\n2. Is time spent in network activity, JavaScript, layout, paint, or React work?\n3. Which update scheduled the React work?\n4. Was it blocking or transition work?\n5. Which components rendered or ran Effects?\n6. Did React yield, restart, or wait for paint?\n\nPerformance Tracks complement the React Profiler; they do not replace measurement of the complete browser timeline. Profile a production build under representative device and network conditions.",
  },
  {
    number: 117,
    question: "How should a team adopt React Compiler, and what does it not guarantee?",
    answer:
      "Adopt React Compiler incrementally, verify that the code follows the Rules of React, monitor compiler diagnostics, and confirm behavior and performance with tests and profiling.",
    explanation:
      "The Compiler can automatically memoize eligible code, but it does not repair impure components, remove the need for sound state design, or guarantee that every interaction becomes fast.",
    details:
      "A practical rollout includes:\n\n- Enable the current Hooks and Compiler lint rules.\n- Fix render-time mutation and other Rules-of-React violations.\n- Compile a limited surface first.\n- Run behavior tests and compare performance profiles.\n- Inspect compilation failures instead of suppressing them broadly.\n- Expand coverage gradually.\n\nExisting `memo`, `useMemo`, and `useCallback` calls do not need to be removed immediately. Manual memoization can still express an intentional identity contract or control a dependency. Remove it only when measurement and testing show that doing so is safe and clearer.",
  },
  {
    number: 118,
    question: "What do cache and cacheSignal do in a Server Component environment?",
    answer:
      "`cache` memoizes work for React's server rendering context so repeated calls with the same arguments can share a result. `cacheSignal` provides an abort signal tied to that cache lifetime.",
    explanation:
      "Together they can deduplicate server work and cancel operations whose cached result will no longer be used.",
    details:
      "```jsx\nconst getUser = cache(async id => {\n  return database.users.find(id, {\n    signal: cacheSignal()\n  });\n});\n```\n\nDistinguish this mechanism from:\n\n- Browser HTTP caching\n- Framework data caches\n- A durable distributed cache\n- Client-side server-state caching\n\nCache scope and invalidation depend on the rendering environment and framework integration. Never assume memoization is an authorization boundary; every protected operation must still verify the current user and requested resource.",
  },
  {
    number: 119,
    question: "What is partial pre-rendering, and how does it differ from ordinary streaming SSR?",
    answer:
      "Partial pre-rendering creates a reusable static shell ahead of time and later resumes server rendering to fill dynamic regions. Streaming SSR starts rendering for a request and progressively sends completed content, but does not necessarily reuse a precomputed shell.",
    explanation:
      "The goal is to combine fast delivery of stable content with request-time rendering for personalized or fresh content. Suspense boundaries identify where prerendering can pause and where resumed work can continue.",
    details:
      "React's server APIs separate the work into two stages:\n\n1. Prerender as much of the tree as possible and save the result plus resumable state.\n2. Resume later with request-specific data and stream the remaining content.\n\nPartial pre-rendering is infrastructure for framework authors and is normally consumed through a framework rather than assembled directly in application code. It does not make all content static, remove hydration, or replace sensible cache and data-loading decisions.\n\nIn an interview, explain the trade-off: a static shell can improve time to first content and cacheability, while dynamic regions still pay request-time data and rendering costs.",
  },
  {
    number: 120,
    question:
      "What problem does React's \u003cViewTransition> solve, and is it ready for general production use?",
    answer:
      "`\u003cViewTransition>` coordinates animated transitions for DOM changes caused by React updates. It is currently available in React's Canary channel, so teams should not treat it as a stable API for general production adoption yet.",
    explanation:
      "React can activate a browser View Transition when an update reveals, hides, reorders, or changes content inside a transition boundary. This lets the animation follow React's commit rather than relying on manual DOM snapshots.",
    details:
      "```jsx\n\u003cViewTransition>\n  \u003cPage route={route} />\n\u003c/ViewTransition>\n```\n\nTransition-aware updates commonly originate from a Transition, Suspense reveal, or navigation integration. Stable `name` values can connect matching elements across the old and new UI, but duplicate names can prevent a transition.\n\nPrefer progressive enhancement: the interface must remain correct without animation, honor reduced-motion preferences, and avoid transitions that obscure focus or interaction state. Because the component and its related APIs are Canary, verify the current React and framework documentation before adopting them.",
  },
];
